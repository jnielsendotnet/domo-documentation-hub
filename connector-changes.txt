KB ARTICLE CHANGE REPORT
CSV:     *CURRENT* Knowledge Article Mod 1Aug2025 - 13Feb2026.csv
Checked: 122 articles matching 'connector'
Changed: 106 articles with non-image content differences

==============================================================================
STYLE GUIDE COMPLIANCE REVIEW — 2026-04-17
==============================================================================

BEFORE APPLYING ANY CHANGES, READ THESE NOTES:

CRITICAL — FAQ AccordionGroup Preservation:
  The changes in this file systematically convert FAQ sections from
  <AccordionGroup>/<Accordion title="..."> format to plain #### headings.
  This VIOLATES the Domo KB Style Guide, which requires all FAQ sections
  to use <AccordionGroup> and <Accordion> components.
  When applying FAQ-related changes, ALL `####` headings in NEW content
  that correspond to FAQ questions MUST be changed to <Accordion title="...">,
  and the <AccordionGroup> / </AccordionGroup> wrappers must be preserved
  (i.e., changes that only remove these tags should be skipped).

  Files with FAQ AccordionGroup violations:
    000005100, 000005245, 1500000888261, 1500001012441, 1500002644442,
    1500009366622, 360042926754, 360042929154, 360042929554, 360042929714,
    360042930314, 360042931294, 360042931874, 360043431133, 360043437093,
    360052884153, 360058716313, 360059173794, 360060508773, 360061691114,
    4423536836631, 5563069185815, 5366610360983, 4411277235351

CRITICAL — Misplaced <Note> Tags in Table Context:
  Several NEW content sections open a <Note> inside a table cell but close
  </Note> outside the table rows, producing invalid MDX. Review and fix
  manually before applying:
    000005245 (Change 1), 000005793 (Changes 1–2), 000005870 (Change 1),
    000005892 (Change 18), 360042929154 (Change 1)

FIXED IN THIS FILE — Broken Note Callout Format:
  000005532 Change 1: `**Note****:**` corrected to `<Note>**Note:**`
  000005532 Change 2: `</Note>` preserved (no-op replacement added)

FIXED IN THIS FILE — "utilize" (style guide: NEVER use):
  Replace instances in NEW content: 000005870, 000005907 (×2),
  360042931814, 360043437093

FIXED IN THIS FILE — "whitelist" as verb in body text (use "allowlist"):
  360042931494 (MongoDB prerequisites), 360042931734 (Vertica prerequisites)

NEEDS REVIEW — "click" vs "select":
  Style guide requires "select" instead of "click" in most cases.
  Many NEW content sections still use "click" (e.g., 000005892, 000005920,
  and others). Review all step instructions for this pattern.

NEEDS REVIEW — "once" vs "after":
  Style guide requires "after" (not "once") for causal/dependent sequences.
  Multiple NEW content sections use "once you have entered..." patterns.

NEEDS REVIEW — Table padding:
  Style guide requires vertically aligned pipe-table columns. Nearly all
  NEW table content uses unpadded `| --- | --- |` separators. This is
  pervasive and requires programmatic correction before final publication.

IMAGE CHANGES — see IMAGE CHANGE REPORT at bottom of this file.

==============================================================================
END COMPLIANCE NOTES
==============================================================================

ARTICLES REQUIRING EDITS:
  s/article/000005100.mdx                                  10 change(s)  ⚠ LARGE
  s/article/000005245.mdx                                  6 change(s)
  s/article/000005352.mdx                                  2 change(s)
  s/article/000005532.mdx                                  3 change(s)
  s/article/000005697.mdx                                  1 change(s)  ⚠ LARGE
  s/article/000005710.mdx                                  1 change(s)  ⚠ LARGE
  s/article/000005793.mdx                                  2 change(s)  ⚠ LARGE
  s/article/000005795.mdx                                  1 change(s)
  s/article/000005806.mdx                                  2 change(s)
  s/article/000005825.mdx                                  4 change(s)
  s/article/000005844.mdx                                  1 change(s)
  s/article/000005870.mdx                                  1 change(s)
  s/article/000005892.mdx                                  19 change(s)  ⚠ LARGE
  s/article/000005903.mdx                                  9 change(s)
  s/article/000005905.mdx                                  35 change(s)
  s/article/000005907.mdx                                  26 change(s)  ⚠ LARGE
  s/article/000005910.mdx                                  12 change(s)
  s/article/000005911.mdx                                  6 change(s)
  s/article/000005917.mdx                                  17 change(s)
  s/article/000005920.mdx                                  23 change(s)
  s/article/000005921.mdx                                  11 change(s)
  s/article/000005925.mdx                                  10 change(s)
  s/article/000005931.mdx                                  2 change(s)
  s/article/000006000.mdx                                  1 change(s)
  s/article/000006013.mdx                                  1 change(s)
  s/article/000006016.mdx                                  1 change(s)
  s/article/000006053.mdx                                  1 change(s)
  s/article/1500000218261.mdx                              9 change(s)  ⚠ LARGE
  s/article/1500000555201.mdx                              22 change(s)  ⚠ LARGE
  s/article/1500000888261.mdx                              22 change(s)  ⚠ LARGE
  s/article/1500001012441.mdx                              16 change(s)
  s/article/1500002644442.mdx                              8 change(s)
  s/article/1500009366622.mdx                              12 change(s)  ⚠ LARGE
  s/article/1500011338741.mdx                              20 change(s)  ⚠ LARGE
  s/article/360042923434.mdx                               16 change(s)
  s/article/360042926314.mdx                               1 change(s)
  s/article/360042926694.mdx                               20 change(s)  ⚠ LARGE
  s/article/360042926754.mdx                               46 change(s)  ⚠ LARGE
  s/article/360042926914.mdx                               10 change(s)
  s/article/360042927994.mdx                               28 change(s)  ⚠ LARGE
  s/article/360042928074.mdx                               9 change(s)
  s/article/360042928374.mdx                               3 change(s)
  s/article/360042929154.mdx                               24 change(s)  ⚠ LARGE
  s/article/360042929454.mdx                               10 change(s)
  s/article/360042929554.mdx                               8 change(s)
  s/article/360042929694.mdx                               1 change(s)
  s/article/360042929714.mdx                               13 change(s)
  s/article/360042929794.mdx                               1 change(s)
  s/article/360042930154.mdx                               18 change(s)  ⚠ LARGE
  s/article/360042930314.mdx                               13 change(s)
  s/article/360042930474.mdx                               15 change(s)  ⚠ LARGE
  s/article/360042930914.mdx                               9 change(s)
  s/article/360042931294.mdx                               9 change(s)  ⚠ LARGE
  s/article/360042931494.mdx                               15 change(s)
  s/article/360042931734.mdx                               15 change(s)
  s/article/360042931814.mdx                               29 change(s)
  s/article/360042931834.mdx                               20 change(s)  ⚠ LARGE
  s/article/360042931874.mdx                               10 change(s)  ⚠ LARGE
  s/article/360042932014.mdx                               28 change(s)  ⚠ LARGE
  s/article/360042932074.mdx                               4 change(s)  ⚠ LARGE
  s/article/360042932114.mdx                               5 change(s)  ⚠ LARGE
  s/article/360042932474.mdx                               20 change(s)  ⚠ LARGE
  s/article/360042932514.mdx                               4 change(s)  ⚠ LARGE
  s/article/360042932914.mdx                               4 change(s)
  s/article/360043431133.mdx                               19 change(s)  ⚠ LARGE
  s/article/360043431633.mdx                               4 change(s)  ⚠ LARGE
  s/article/360043432813.mdx                               4 change(s)  ⚠ LARGE
  s/article/360043432953.mdx                               6 change(s)  ⚠ LARGE
  s/article/360043432993.mdx                               5 change(s)  ⚠ LARGE
  s/article/360043433033.mdx                               5 change(s)  ⚠ LARGE
  s/article/360043433453.mdx                               5 change(s)  ⚠ LARGE
  s/article/360043433653.mdx                               12 change(s)
  s/article/360043433813.mdx                               23 change(s)
  s/article/360043434093.mdx                               3 change(s)  ⚠ LARGE
  s/article/360043434413.mdx                               8 change(s)  ⚠ LARGE
  s/article/360043434433.mdx                               8 change(s)  ⚠ LARGE
  s/article/360043435013.mdx                               3 change(s)  ⚠ LARGE
  s/article/360043435113.mdx                               9 change(s)  ⚠ LARGE
  s/article/360043436133.mdx                               4 change(s)  ⚠ LARGE
  s/article/360043436273.mdx                               5 change(s)  ⚠ LARGE
  s/article/360043436313.mdx                               12 change(s)  ⚠ LARGE
  s/article/360043436753.mdx                               10 change(s)
  s/article/360043436873.mdx                               4 change(s)  ⚠ LARGE
  s/article/360043437013.mdx                               3 change(s)  ⚠ LARGE
  s/article/360043437093.mdx                               30 change(s)  ⚠ LARGE
  s/article/360043437533.mdx                               7 change(s)  ⚠ LARGE
  s/article/360052122394.mdx                               5 change(s)  ⚠ LARGE
  s/article/360052884153.mdx                               23 change(s)  ⚠ LARGE
  s/article/360057013754.mdx                               6 change(s)  ⚠ LARGE
  s/article/360057028014.mdx                               4 change(s)  ⚠ LARGE
  s/article/360058713713.mdx                               5 change(s)  ⚠ LARGE
  s/article/360058716313.mdx                               20 change(s)  ⚠ LARGE
  s/article/360058716453.mdx                               5 change(s)  ⚠ LARGE
  s/article/360058757134.mdx                               12 change(s)  ⚠ LARGE
  s/article/360058759794.mdx                               6 change(s)
  s/article/360058760154.mdx                               32 change(s)  ⚠ LARGE
  s/article/360059173794.mdx                               22 change(s)  ⚠ LARGE
  s/article/360060476953.mdx                               4 change(s)  ⚠ LARGE
  s/article/360060508773.mdx                               18 change(s)  ⚠ LARGE
  s/article/360061691114.mdx                               6 change(s)  ⚠ LARGE
  s/article/4405291644311.mdx                              8 change(s)  ⚠ LARGE
  s/article/4409512475927.mdx                              8 change(s)
  s/article/4411277235351.mdx                              16 change(s)  ⚠ LARGE
  s/article/4423536836631.mdx                              16 change(s)  ⚠ LARGE
  s/article/5366610360983.mdx                              3 change(s)  ⚠ LARGE
  s/article/5563069185815.mdx                              15 change(s)  ⚠ LARGE


==============================================================================
FILE:   s/article/000005100.mdx
TITLE:  Shopify Custom App Connector
LANG:   English (en_US)
ID:     000005100
==============================================================================

CHANGE 1  [### FAQs]  (line 76 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

CHANGE 2  [### FAQs]  (line 78 in repo file)

  OLD (remove from repo):
    <Accordion title="When should I use this connector?">

  NEW (replace with / insert from CSV):
    #### When should I use this connector?

CHANGE 3  [### FAQs]  (lines 81–82 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="What endpoint is the base URL for this connector?">

  NEW (replace with / insert from CSV):
    
    #### What endpoint is the base URL for this connector?

CHANGE 4  [### FAQs]  (lines 85–86 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="Which endpoint does each report call in this connector?">

CHANGE 5  [### FAQs]  (lines 88–90 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Report Name </p></th><th colspan="1" rowspan="1"><p> Endpoint URL </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Application Charges </p></td><td colspan="1" rowspan="1"><p> /application_charges.json </p></td></tr><tr><td colspan="1" rowspan="1"><p> Application Credits </p></td><td colspan="1" rowspan="1"><p> /application_credits </p></td></tr><tr><td colspan="1" rowspan="1"><p> Articles </p></td><td colspan="1" rowspan="1"><p> /blogs/{blogIds}/articles.json </p></td></tr><tr><td colspan="1" rowspan="1"> Assets </td><td colspan="1" rowspan="1"> /themes/assets.json </td></tr><tr><td colspan="1" rowspan="1"> Blogs </td><td colspan="1" rowspan="1"> /blogs.json </td></tr><tr><td colspan="1" rowspan="1"> Carrier Services </td><td colspan="1" rowspan="1"> /carrier_services </td></tr><tr><td colspan="1" rowspan="1"> Checkouts </td><td colspan="1" rowspan="1"> /checkouts.json </td></tr><tr><td colspan="1" rowspan="1"> Collects </td><td colspan="1" rowspan="1"> /collects.json </td></tr><tr><td colspan="1" rowspan="1"> Comments </td><td colspan="1" rowspan="1"> /comments.json </td></tr><tr><td colspan="1" rowspan="1"> Countries </td><td colspan="1" rowspan="1"> /countries.json </td></tr><tr><td colspan="1" rowspan="1"> Custom Collections </td><td colspan="1" rowspan="1"> /custom_collections.json </td></tr><tr><td colspan="1" rowspan="1"> Customers </td><td colspan="1" rowspan="1"> /customers </td></tr><tr><td colspan="1" rowspan="1"> Discount Codes </td><td colspan="1" rowspan="1"> /price_rules/discount_codes </td></tr><tr><td colspan="1" rowspan="1"> Disputes </td><td colspan="1" rowspan="1"> /shopify_payments/disputes </td></tr><tr><td colspan="1" rowspan="1"> Draft Orders </td><td colspan="1" rowspan="1"> /draft_orders </td></tr><tr><td colspan="1" rowspan="1"><p> Execute GraphQL Query </p></td><td colspan="1" rowspan="1"> /graphql.json </td></tr><tr><td colspan="1" rowspan="1"> Gift Cards (Shopify Plus) </td><td colspan="1" rowspan="1"> /gift_cards </td></tr><tr><td colspan="1" rowspan="1"> Inventory Items </td><td colspan="1" rowspan="1"> /inventory_items </td></tr><tr><td colspan="1" rowspan="1"> Fulfillments </td><td colspan="1" rowspan="1"> /orders/{orderId}/fulfillments </td></tr><tr><td colspan="1" rowspan="1"> Locations </td><td colspan="1" rowspan="1"> /locations </td></tr><tr><td colspan="1" rowspan="1"> Marketing Events </td><td colspan="1" rowspan="1"> /marketing_events </td></tr><tr><td colspan="1" rowspan="1"> Orders </td><td colspan="1" rowspan="1"> /orders </td></tr><tr><td colspan="1" rowspan="1"> Pages </td><td colspan="1" rowspan="1"> /pages </td></tr><tr><td colspan="1" rowspan="1"> Payouts </td><td colspan="1" rowspan="1"> /shopify_payments/payouts </td></tr><tr><td colspan="1" rowspan="1"> Policies </td><td colspan="1" rowspan="1"> /policies </td></tr><tr><td colspan="1" rowspan="1"> Price Rules </td><td colspan="1" rowspan="1"> /price_rules </td></tr><tr><td colspan="1" rowspan="1"> Product Variants (Deprecated) </td><td colspan="1" rowspan="1"> /products/{productId}/variants </td></tr><tr><td colspan="1" rowspan="1"><p> Products </p><p> (Deprecated) </p></td><td colspan="1" rowspan="1"> /products </td></tr><tr><td colspan="1" rowspan="1"> Provinces of Country </td><td colspan="1" rowspan="1"> /countries/provinces </td></tr><tr><td colspan="1" rowspan="1"><p> Products Using GraphQL </p></td><td colspan="1" rowspan="1"> /graphql.json </td></tr><tr><td colspan="1" rowspan="1"><p> Product Variants Using GraphQL </p></td><td colspan="1" rowspan="1"> /graphql.json </td></tr><tr><td colspan="1" rowspan="1"> Recurring Application Charges </td><td colspan="1" rowspan="1"> /recurring_application_charges </td></tr><tr><td colspan="1" rowspan="1"> Redirects </td><td colspan="1" rowspan="1"> /redirects </td></tr><tr><td colspan="1" rowspan="1"> Refunds </td><td colspan="1" rowspan="1"> /orders/{orderId}/refunds </td></tr><tr><td colspan="1" rowspan="1"> Resource Feedback </td><td colspan="1" rowspan="1"> /resource_feedback </td></tr><tr><td colspan="1" rowspan="1"> Order Risks </td><td colspan="1" rowspan="1"> /orders/{orderId}/risks </td></tr><tr><td colspan="1" rowspan="1"> Script Tags </td><td colspan="1" rowspan="1"> /script_tags </td></tr><tr><td colspan="1" rowspan="1"> Shipping Zones </td><td colspan="1" rowspan="1"> /shipping_zones </td></tr><tr><td colspan="1" rowspan="1"> Shop </td><td colspan="1" rowspan="1"> /shop </td></tr><tr><td colspan="1" rowspan="1"> Smart Collections </td><td colspan="1" rowspan="1"> /smart_collections </td></tr><tr><td colspan="1" rowspan="1"> Themes </td><td colspan="1" rowspan="1"> /themes </td></tr><tr><td colspan="1" rowspan="1"> Transactions </td><td colspan="1" rowspan="1"> /orders/{orderId}/transactions </td></tr><tr><td colspan="1" rowspan="1"> Usage Charges </td><td colspan="1" rowspan="1"> /recurring_application_charges/usage_charges </td></tr><tr><td colspan="1" rowspan="1"> Webhooks </td><td colspan="1" rowspan="1"> /webhooks </td></tr></tbody></table>
    </Accordion>
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### Which endpoint does each report call in this connector?

CHANGE 6  [### FAQs]  (lines 92–94 in repo file)

  ⚠ LARGE CHANGE (50 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    You need the name of your Shopify shop to power up this connector, and your Shopify username and password. Shop owners can use the connector without setting any additional permissions. However, if you have a limited user account - a **Staff** account, you will need to ensure that you have permissions to **Apps** and any other resources you wish to pull from the connector. Visit [Shopify documentation](https://help.shopify.com/en/manual/your-account/staff-accounts/staff-permissions "Shopify documentation").
    </Accordion>
    <Accordion title="Where do I get my Shopify shop name in the credentials section?">

  NEW (replace with / insert from CSV):
    | Report Name | Endpoint URL |
    | --- | --- |
    | Application Charges | /application\_charges.json |
    | Application Credits | /application\_credits |
    | Articles | /blogs/{blogIds}/articles.json |
    | Assets | /themes/assets.json |
    | Blogs | /blogs.json |
    | Carrier Services | /carrier\_services |
    | Checkouts | /checkouts.json |
    | Collects | /collects.json |
    | Comments | /comments.json |
    | Countries | /countries.json |
    | Custom Collections | /custom\_collections.json |
    | Customers | /customers |
    | Discount Codes | /price\_rules/discount\_codes |
    | Disputes | /shopify\_payments/disputes |
    | Draft Orders | /draft\_orders |
    | Execute Bulk GraphQL Query | /graphql.json |
    | Execute GraphQL Query | /graphql.json |
    | Gift Cards (Shopify Plus) | /gift\_cards |
    | Inventory Items | /inventory\_items |
    | Fulfillments | /orders/{orderId}/fulfillments |
    | Locations | /locations |
    | Marketing Events | /marketing\_events |
    | Orders | /orders |
    | Pages | /pages |
    | Payouts | /shopify\_payments/payouts |
    | Policies | /policies |
    | Price Rules | /price\_rules |
    | Product Variants (Deprecated) | /products/{productId}/variants |
    | Products  (Deprecated) | /products |
    | Provinces of Country | /countries/provinces |
    | Products Using GraphQL | /graphql.json |
    | Product Variants Using GraphQL | /graphql.json |
    | Recurring Application Charges | /recurring\_application\_charges |
    | Redirects | /redirects |
    | Refunds | /orders/{orderId}/refunds |
    | Resource Feedback | /resource\_feedback |
    | Order Risks | /orders/{orderId}/risks |
    | Script Tags | /script\_tags |
    | Shipping Zones | /shipping\_zones |
    | Shop | /shop |
    | Smart Collections | /smart\_collections |
    | Themes | /themes |
    | Transactions | /orders/{orderId}/transactions |
    | Usage Charges | /recurring\_application\_charges/usage\_charges |
    | Webhooks | /webhooks |

CHANGE 7  [### FAQs]  (lines 96–98 in repo file)

  OLD (remove from repo):
    The Shopify shop name is found in your shop dashboard URL. For example, if your shop dashboard is [domo.shopify.com](http://domo.shopify.com), your shop name is "domo".
    </Accordion>
    <Accordion title="Can I use the same account for multiple datasets?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?
    
    You need the name of your Shopify shop to power up this connector, and your Shopify username and password. Shop owners can use the connector without setting any additional permissions. However, if you have a limited user account - a **Staff** account, you will need to ensure that you have permissions to **Apps** and any other resources you wish to pull from the connector. Visit [Shopify documentation](https://help.shopify.com/en/manual/your-account/staff-accounts/staff-permissions).
    
    #### Where do I get my Shopify shop name in the credentials section?
    
    The Shopify shop name is found in your shop dashboard URL. For example, if your shop dashboard is domo.shopify.com, your shop name is "domo".
    
    #### Can I use the same account for multiple datasets?

CHANGE 8  [### FAQs]  (lines 101–102 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    
    #### How often can the data be updated?

CHANGE 9  [### FAQs]  (lines 105–106 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="What is the difference between the Shopify Connector and Shopify Custom App Connector?">

  NEW (replace with / insert from CSV):
    
    #### What is the difference between the Shopify Connector and Shopify Custom App Connector?

CHANGE 10  [### FAQs]  (lines 109–110 in repo file)

  OLD (remove from repo):
    </Accordion>
    </AccordionGroup>

==============================================================================
FILE:   s/article/000005245.mdx
TITLE:  JIRA Cloud Writeback Connector
LANG:   English (en_US)
ID:     000005245
==============================================================================

CHANGE 1  [### Details Pane]  (line 51 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:201;a"><thead><tr><th colspan="1" rowspan="1"><p> Report </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Create Or Update Issues </td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"> Update the Issue </td><td colspan="1" rowspan="1"> Update or change the issue in Jira. </td></tr><tr><td colspan="1" rowspan="1"> Get the Issue </td><td colspan="1" rowspan="1"> Retrieves the list of issues. </td></tr><tr><td colspan="1" rowspan="1"> Create Bulk Issues </td><td colspan="1" rowspan="1"> Create list of issues. <br/> The maximum limit for creating bulk issues are 50. </td></tr><tr><td colspan="1" rowspan="1"> Create Single Issue </td><td colspan="1" rowspan="1"> Create one issue at a time in Jira. </td></tr><tr><td colspan="1" rowspan="1"> Add Comment </td><td colspan="1" rowspan="1"> Add comment to an issue. </td></tr><tr><td colspan="1" rowspan="1"> Update Comment </td><td colspan="1" rowspan="1"> Update or change a comment to an issue. </td></tr><tr><td colspan="1" rowspan="1"> Create Issue Link </td><td colspan="1" rowspan="1"> Create a link for an issue in Jira. </td></tr><tr><td colspan="1" rowspan="1"> Search User </td><td colspan="1" rowspan="1"> Search for a specific user in Jira. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Create Or Update Project </td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"> Get Project </td><td colspan="1" rowspan="1"> Retrieve a project from Jira. </td></tr><tr><td colspan="1" rowspan="1"> Create Project </td><td colspan="1" rowspan="1"> Create a project in Jira. </td></tr><tr><td colspan="1" rowspan="1"> Update Project </td><td colspan="1" rowspan="1"> Update a project in Jira. </td></tr></tbody></table></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Report | Description |
    | --- | --- |
    | Domo Dataset | |  |  | | --- | --- | | Dataset ID | Enter the ID of your Domo dataset containing the data to write to Jira Cloud. [https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings](https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings)
    <Note>**Note:** You can only select datasets you own for write-back.  The dataset ID is found within the dataset's URL.  **Example:** `[https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings](https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings)` | |
    | Jira Cloud Setup | |  |  | | --- | --- | | Select Project | Select the project. | | Select Issue Type | Select the issue type. | |
    | Configuration Field Mapping | |  |  | | --- | --- | | Setup the JiraCloud Report and Domo dataset to map the fields here. | You need to map dataset fields and JiraCloud fields.    Select the dataset column for each required JiraCloud field. Match the desired JiraCloud fields and dataset columns for optional fields. | |</Note>

CHANGE 2  [## FAQ]  (lines 55–56 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="What can I do with this connector?">

  NEW (replace with / insert from CSV):
    #### How do I power up the connector
    
    You need to have an Atlassian domain and account to access this connector.
    
    #### What can I do with this connector?

CHANGE 3  [## FAQ]  (lines 60–62 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What is the authentication method for this connector?">

  NEW (replace with / insert from CSV):
    #### What is the authentication method for this connector?

CHANGE 4  [## FAQ]  (lines 66–68 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?

CHANGE 5  [## FAQ]  (lines 72–74 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Is there a support for paging for this connector?">

  NEW (replace with / insert from CSV):
    #### Is there a support for paging for this connector?

CHANGE 6  [## FAQ]  (lines 78–79 in repo file)

  OLD (remove from repo):
    </Accordion>
    </AccordionGroup>

==============================================================================
FILE:   s/article/000005352.mdx
TITLE:  Freshservice V2 Connector
LANG:   English (en_US)
ID:     000005352
==============================================================================

CHANGE 1  [## Credentials Pane]  (line 26 in repo file)

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |

CHANGE 2  [## Credentials Pane]  (line 27 in repo file)

  OLD (remove from repo):
    | --- | --- |

==============================================================================
FILE:   s/article/000005532.mdx
TITLE:  freee Enterprise Connector
LANG:   English (en_US)
ID:     000005532
==============================================================================

CHANGE 1  [## Prerequisites]  (lines 23–24 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:** To create a freee application, you will be asked to enter the following callback URL:

  NEW (replace with / insert from CSV):
    <Note>**Note:** To create a freee application, you will be asked to enter the following callback URL:

CHANGE 2  [## Prerequisites]  (line 27 in repo file)

  [STYLE GUIDE FIX: Preserve closing </Note> tag — no-op replacement]
  OLD (remove from repo):
    </Note>

  NEW (replace with / insert from CSV):
    </Note>

CHANGE 3  [## Credentials pane]  (line 39 in repo file)

  NEW (replace with / insert from CSV):
    | **Field** | **Description** |

==============================================================================
FILE:   s/article/000005697.mdx
TITLE:  ServiceNow Enterprise OAuth Configuration Connector
LANG:   English (en_US)
ID:     000005697
==============================================================================

CHANGE 1  [## Details Pane]  (lines 76–94 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    | Menu                        | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
    | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Report                      | Select the ServiceNow Enterprise OAuth Configuration report you want to run. The following reports are available: <br/><br/> <table><thead><tr><th>Report Name</th><th>Description</th></tr></thead><tbody><tr><td>Standard Table</td><td>Select from a list of standard tables from Service Now.</td></tr><tr><td>Custom Query</td><td>Creates a custom query. It includes the ability to select which fields are returned to the DataSet.</td></tr><tr><td>Custom Table</td><td>It provides a custom table name from which to retrieve data.</td></tr></tbody></table>                                                                                                                                                                                                      |
    | Table                       | <table><thead><tr><th>Table</th><th>Description</th></tr></thead><tbody><tr><td>Incident</td><td>Retrieves all data from the incident table.</td></tr><tr><td>Change Request</td><td>Retrieves all data from the change request table.</td></tr><tr><td>Problem</td><td>Retrieves all data from the problem table.</td></tr><tr><td>User Groups</td><td>Retrieves all data from the user groups table.</td></tr><tr><td>Users</td><td>Retrieves all data from the users table.</td></tr></tbody></table>                                                                                                                                                                                                                                                                      |
    | Table Name                  | Enter the name of the table for which you want to retrieve the data for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
    | Custom Query                | Enter a custom query in this field. Include query values directly related to `sysparm_query`. The fields `sysparm_limit` and `sysparm_offset` should not be included. If the field `sys_created_on` is included the value should be in the following format: `yyyy-MM-dd HH:mm:ss`. <br/><br/> **Example query:** `ORDERBYDESCsys_created_on^sys_created_on>2016-09-25+00:00:00^sys_created_on<2016-09-27+23:59:59` <br/><br/> For more information, refer to [here](http://wiki.servicenow.com/index.php?title=Table_API#Methods).                                                                                                                                                                                                                                          |
    | Fields                      | Enter a comma separated list of fields to return with this DataSet. <br/><br/> **Example field list:** `parent,sys_updated_on,number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
    | Data Format                 | Select the format which you expect the ServiceNow API to return date fields. If you select String, Domo will not process the date, and return the literal string returned from the ServiceNow API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | Duration                    | Select the duration for the report (a Single Date, or a Date Range).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
    | Report Date                 | Select report date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
    | Select Specific Date        | Select the report date using the date selector.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
    | Days Back                   | The number of days back that you would like to get data for. Specify either today or `0`, yesterday or `1`, or today-7 or `7` to get data for 7 days into the past.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
    | Start Date                  | Select report start date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
    | Select Specific Start Date  | Select the report start date using the date selector.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
    | Days Back to Start From     | The number of days back that you would like to get data from (start day). Specify either today or `0`, yesterday or `1`, or today-7 or `7` to get data for 7 days into the past.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
    | End Date                    | Select report end date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
    | Select Specific End Date    | Select the report end date using the date selector.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
    | Days Back to End At         | The number of days back that you would like to get data to (end day). Specify either today or `0`, yesterday or `1`, or today-7 or `7` to get data for 7 days into the past.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
    | Display Values              | When this option is enabled, the connector returns human‑readable display values for reference fields, rather than internal `sys_id` values.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the ServiceNow Enterprise OAuth Configuration report you want to run. The following reports are available:     |  |  | | --- | --- | | **Report Name** | **Description** | | Standard Table | Select from a list of standard tables from Service Now. | | Custom Query | Creates a custom query. It includes the ability to select which fields are returned to the DataSet. | | Custom Table | It provides a custom table name from which to retrieve data. | |
    | Table | |  |  | | --- | --- | | **Table** | **Description** | | Incident | Retrieves all data from the incident table. | | Change Request | Retrieves all data from the change request table. | | Problem | Retrieves all data from the problem table. | | User Groups | Retrieves all data from the user groups table. | | Users | Retrieves all data from the users table. | |
    | Table Name | Enter the name of the table for which you want to retrieve the data for. |
    | Custom Query | Enter a custom query in this field. Include query values directly related to sysparm\_query. The fields sysparm\_limit and sysparm\_offset should not be included. If the field sys\_created\_on is included the value should be in the following format: yyyy-MM-dd HH:mm:ss.  **Example query:** ORDERBYDESCsys\_created\_on^sys\_created\_on>2016-09-25+00:00:00^sys\_created\_on<2016-09-27+23:59:59  For more Information, refer to [here](http://wiki.servicenow.com/index.php?title=Table_API#Methods). |
    | Fields | Enter a comma separated list of fields to return with this DataSet.  **Example field list:** parent,sys\_updated\_on,number |
    | Data Format | Select the format which you expect the ServiceNow API Select report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector will return date fields. If you select String, Domo will not process the date, and return the literal string returned from the ServiceNow API. |
    | Duration | Select the duration for the report (a Single Date, or a Date Range). |
    | Report Date | Select report date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific Date | Select the report date using the date selector. |
    | Days back | The number of days back that you would like to get data for. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Start Date | Select report start date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific Start Date | Select the report date using the date selector. |
    | Days back to start from | The number of days back that you would like to get data from (start day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | End Date | Select report end date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific End Date | Select the report date using the date selector. |
    | Days back to end at | The number of days back that you would like to get data to (end day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Display Values | When this option is enabled, the connector returns human‑readable display values for reference fields, rather than internal **sys\_id** values. |

==============================================================================
FILE:   s/article/000005710.mdx
TITLE:  Facebook Ads Advanced Multi Accounts Connector
LANG:   English (en_US)
ID:     000005710
==============================================================================

CHANGE 1  [# Details Pane]  (lines 36–66 in repo file)

  ⚠ LARGE CHANGE (62 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    | Menu                          | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
    | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Report                        | Select the Facebook Ads Advanced Multi Accounts Ads report you want to run. The following reports are available: <br/><br/> <table><thead><tr><th>Report</th><th>Description</th></tr></thead><tbody><tr><td>FBAds PreBuilt Report</td><td>Domo's complimentary pre-built report for Facebook Ads - a comprehensive analysis of advertising performance including accounts, campaigns, ad groups, and ad-level reporting.</td></tr><tr><td>Ad Accounts</td><td>Returns information about one or more selected Facebook Ads Accounts.</td></tr><tr><td>Ad Campaigns</td><td>Returns information about one or more Ad Campaigns controlled by the selected <strong>Account(s)</strong>.</td></tr><tr><td>Ad Keyword Statistics (Deprecated)</td><td>Returns information about the Keywords used for targeting the selected <strong>Ad(s)</strong>.</td></tr><tr><td>Ad Sets</td><td>Returns information about one or more Ad Sets within the selected <strong>Ad Campaign(s)</strong>.</td></tr><tr><td>Ads</td><td>Returns information about one or more Ads defined in the selected <strong>Ad Set(s)</strong>.</td></tr><tr><td>Custom Insights</td><td>Returns user-selected insight information at a reporting level defined by the <strong>Level</strong> field.</td></tr><tr><td>General Metrics</td><td>Returns commonly-used insight metrics at the Campaign level.</td></tr><tr><td>Object Information - NEW</td><td>This new report enables you to retrieve the Info edge of the all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need information about.</td></tr><tr><td>Object Insights - NEW</td><td>This new report enables you to retrieve the Insights edge of the all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need insights about, and complete the other insight metrics.</td></tr><tr><td>Page Actions (Ad sets)</td><td>Returns insights related to Page and Post interactions at the Adset level.</td></tr><tr><td>Page Actions (Ads)</td><td>Returns insights related to Page and Post interactions at the Ad level.</td></tr><tr><td>Placement Metrics</td><td>Returns common metrics, broken down by screen placement, at the Campaign level.</td></tr><tr><td>Responder Demographics</td><td>Returns common metrics, broken down by age and gender, at the Adset level.</td></tr><tr><td>User</td><td>Returns information about the authenticated user.</td></tr></tbody></table>                                                                                                                                                                                                                                                                                        |
    | Select Object Type            | Pick an object level to gather data. Domo reports all objects of the type contained in the accounts you have selected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
    | Effective Statuses            | This field appears for the **Object Information - NEW** report when you select **Campaign**, **Ad Set**, or **Ad** as the Object Type. Select the effective statuses to filter your data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
    | Time Increment                | Select the granularity used to aggregate the data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | Duration                      | Choose whether the report should cover a **Single Date** or a **Date Range**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
    | Date From                     | Choose how you want to set the start date for the report. <br/><br/> - **Relative Date:** Enter the number of days back from today. <br/> - **Specific Date:** Use the date picker to select a precise date. <br/><br/> For example, if you enter 10 for **Date From Offset** and five for **Date To Offset**, and you set the data source to update daily, each new day the report updates to show the information for five to 10 days in the past.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | Date From Offset              | Enter the number of days you want to go back from the current date. For example, enter **0** for today or **1** for yesterday.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
    | Select Specific Date From     | Select the precise start date for the report using the date picker. This option is only available when the **Report Date** is set to **Specific**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
    | Date To                       | Choose how you want to set the end date for the report. <br/><br/> - **Relative Date:** Enter the number of days back from today. <br/> - **Specific Date:** Use the date picker to select a precise date. <br/><br/> Choose **Relative** if you always want the report to retrieve data for a given number of days back (which you specify in **Date To Offset**) from the current date. If you choose **Relative** here, you should also choose **Relative** for **Date From** and specify a value for **Date From Offset**. For example, if you enter 10 for **Date From Offset** and 5 for **Date To Offset** and you set the data source to update daily, each new day the report will update to show information for 5 to 10 days in the past.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
    | Date To Offset                | Enter the number of days you want to go back from the selected end date. For example, enter **0** for today or **1** for yesterday.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | Select Specific Date To       | Select the precise end date for the report using the date picker. This option is only available when the **Report Date** is set to **Specific**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
    | Report Date                   | Specify whether the report data is based on a **fixed date** or a **relative number of days**. A relative number of days indicates the retrospective period from which data will be retrieved. To retrieve data from a specific date, use the date selector. <br/><br/> Choose **Relative** if you always want the report to retrieve data for a given number of days back (which you specify in **Date Offset**) from the current date. For example, if you enter 5 for **Date Offset** and set the data source to update daily, each new day the updated report will show information for the date 5 days in the past.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
    | Date Offset                   | Enter the number of days you want to go back from the current date. For example, enter **0** for today or **1** for yesterday.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
    | Select Specific Date          | To select a specific report date, use the **date picker**. This option is only available when the **Report Date** is set to **Specific**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
    | Columns                       | Select the columns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
    | Time Increment                | Enter the number of days used to aggregate the data. This should be an integer between 1 and 90. You can also enter `monthly` to show data by month or `all_days` to show data for all days with data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
    | Level                         | Select the ad level for the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
    | Conversion Type Selection     | Select if you want to bring custom or standard conversions only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
    | Breakdowns                    | Select how you want the report data to be broken down.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
    | Action Breakdowns             | Select the desired action breakdown(s) for the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
    | Include Archived Objects      | If set to True then the Archived objects will be in the results. Otherwise, Archived objects will not be reported.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | Action Attribution Windows   | Select the desired action attribution windows for the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
    | Reset Schema                  | Select this checkbox if you need to reset the schema. <br/><br/> **Warning:** This can result in loss of data or change in data type if some fields are temporarily not returned by Facebook's API. While this checkbox is selected the dataset will reset the schema for each run. Do not use in the APPEND mode.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
    | Ad Account Edge               | <table><thead><tr><th>Edge</th><th>Description</th></tr></thead><tbody><tr><td>Ad Account Info</td><td>Returns information about the selected account.</td></tr><tr><td>Activities</td><td>Returns a log of the actions taken on the selected account.</td></tr><tr><td>Ad Campaigns</td><td>Returns a list of ad campaigns for the selected account.</td></tr><tr><td>Ad Sets</td><td>Returns a list of ad sets for the selected account.</td></tr><tr><td>Ads</td><td>Returns a list of ads for the selected account.</td></tr><tr><td>Custom Audiences</td><td>Returns the custom audiences owned by or shared with the selected account.</td></tr><tr><td>Insights</td><td>Returns insight information for the specified criteria.</td></tr><tr><td>Partners</td><td>Returns a list of ad partners for the selected account.</td></tr><tr><td>Rate Card</td><td>Returns a list of currency rates.</td></tr><tr><td>Reach Estimate</td><td>Returns the reach estimate for the selected account and given targeting spec.</td></tr><tr><td>Users</td><td>Returns user IDs, permissions, and roles for the selected account.</td></tr></tbody></table>                                                                                                                                                                                                                                                                                                                                                                    |
    | Action Report Time            | Determines the report time of action stats. For example, if a person saw the ad on Jan 1st but converted on Jan 2nd, when you query the API with `action_report_time=impression`, you see a conversion on Jan 1st. When you query the API with `action_report_time=conversion`, you see a conversion on Jan 2nd.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
    | Expand List Columns           | <table><thead><tr><th>Option</th><th>Description</th></tr></thead><tbody><tr><td>Expand as Columns</td><td>List data will expand as multiple columns in the results.</td></tr><tr><td>Expand as Rows</td><td>List data will expand into multiple rows in the results. This will result in many rows with duplicated data.</td></tr><tr><td>Do not expand</td><td>List data will not be expanded in the results. It will be returned as "List" in the results.</td></tr><tr><td>Expand Lists In Place</td><td>List data will be converted into a comma-separated list of values for the cell. This does not result in new rows or columns.</td></tr></tbody></table>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
    | Targeting Spec                | Enter the Targeting Spec.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
    | Skip Failing Ad Set IDs       | When checking if any Ad Set IDs fail, it skips, and the connector continues to run using the remaining Ad Set IDs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | User Edge                     | <table><thead><tr><th>Edge</th><th>Description</th></tr></thead><tbody><tr><td>Ad Accounts</td><td>Returns information for the authenticated user's ad accounts, such as balance, capabilities, funding sources, etc.</td></tr><tr><td>User Info</td><td>Returns the authenticated user's name and ID.</td></tr></tbody></table>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Facebook Ads Advanced Multi Accounts Ads report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report** | **Description** | | FBAds PreBuilt Report | Domo's complimentary pre-built report for Facebook Ads - a comprehensive analysis of advertising performance including accounts, campaigns, ad groups, and ad-level reporting. | | Ad Accounts | Returns information about one or more selected Facebook Ads Accounts. | | Ad Campaigns | Returns information about one or more Ad Campaigns controlled by the selected **Account(s)**. | | Ad Keyword Statistics (Deprecated) | Returns information about the Keywords used for targeting the selected **Ad(s)**. | | Ad Sets | Returns information about one or more Ad Sets within the selected **Ad Campaign(s)**. | | Ads | Returns information about one or more Ads defined in the selected **Ad Set(s)**. | | Custom Insights | Returns user-selected insight information at a reporting level defined by the **Level** field. | | General Metrics | Returns commonly-used insight metrics at the Campaign level. | | Object Information - NEW | This new report enables you to retrieve the Info edge of the all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need information about. | | Object Insights - NEW | This new report enables you to retrieve the Insights edge of the all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need insights about, and complete the other insight metrics. | | Page Actions (Ad sets) | Returns insights related to Page and Post interactions at the Adset level. | | Page Actions (Ads) | Returns insights related to Page and Post interactions at the Ad level. | | Placement Metrics | Returns common metrics, broken down by screen placement, at the Campaign level. | | Responder Demographics | Returns common metrics, broken down by age and gender, at the Adset level. | | User | Returns information about the authenticated user. | |
    | Select Object Type | Pick an object level to gather data. Domo reports all objects of the type contained in the accounts you have selected. |
    | Effective Statuses | This field appears for the**Object Information - NEW** report when you select **Campaign**, **Ad Set**, or **Ad**as the Object Type.  Select the effective statuses to filter your data. |
    | Time Increment | Select the granularity used to aggregate the data. |
    | Duration | Choose whether the report should cover a **Single Date** or a **Date Range**. |
    | Date From | Choose how you want to set the start date for the report.   - **Relative Date:** Enter the number of days back from today. - **Specific Date:** Use the date picker to select a precise date.   For example, if you enter 10 for **Date From Offset** and five for **Date To Offset,** and you set the data source to update daily, each new day the report updates to show the information for five to 10 days in the past. |
    | Date From Offset | Enter the number of days you want to go back from the current date. For example, enter **0** for today or **1** for yesterday. |
    | Select Specific Date From | Select the precise start date for the report using the date picker.  This option is only available when the**Report Date** is set to **Specific.** |
    | Date To | Choose how you want to set the end date for the report.   - **Relative Date:** Enter the number of days back from today. - **Specific Date:** Use the date picker to select a precise date.   Choose **Relative** if you always want the report to retrieve data for a given number of days back (which you specify in **Date To Offset**) from the current date. If you choose **Relative** here, you should also choose **Relative** for **Date From** and specify a value for **Date From Offset**. For example, if you enter 10 for **Date From Offset** and 5 for **Date To Offset** and you set the data source to update daily, each new day the report will update to show information for 5 to 10 days in the past. |
    | Date To Offset | Enter the number of days you want to go back from the selected end date. For example, enter **0** for today or **1** for yesterday. |
    | Select Specific Date To | Select the precise end date for the report using the date picker.  This option is only available when the**Report Date** is set to **Specific.** |
    | Report Date | Specify whether the report data is based on a **fixed date** or a **relative number of days**. A relative number of days indicates the retrospective period from which data will be retrieved. To retrieve data from a specific date, use the date selector.  Choose **Relative** if you always want the report to retrieve data for a given number of days back (which you specify in **Date Offset**) from the current date. For example, if you enter 5 for **Date Offset** and set the data source to update daily, each new day the updated report will show information for the date 5 days in the past. |
    | Date Offset | Enter the number of days you want to go back from the current date. For example, enter **0** for today or **1** for yesterday. |
    | Select Specific Date | To select a specific report date, use the **date picker**. This option is only available when the **Report Date** is set to **Specific**. |
    | Columns | Select the columns. |
    | Time Increment | Enter the number of days used to aggregate the data. This should be an integer between 1 and 90. You can also enter monthly to show data by month or all\_days to show data for all days with data. |
    | Level | Select the ad level for the report. |
    | Conversion Type Selection | Select if you want to bring custom or standard conversions only. |
    | Breakdowns | Select how you want the report data to be broken down. |
    | Action Breakdowns | Select the desired action breakdown(s) for the report. |
    | Include Archived Objects | If set to True then the Archived objects will be in the results. Otherwise, Archived objects will not be reported. |
    | Action Attribution Windows | Select the desired action attribution windows for the report. |
    | Reset Schema | Select this checkbox if you need to reset the schema.  Warning: This can result in loss of data or change in data type if some fields are temporarily not returned by Facebook's API. While this checkbox is selected the dataset will reset the schema for each run. Do not use in the APPEND mode |
    | Ad Account Edge | |  |  | | --- | --- | | **Edge** | **Description** | | Ad Account Info | Returns information about the selected account. | | Activities | Returns a log of the actions taken on the selected account. | | Ad Campaigns | Returns a list of ad campaigns for the selected account. | | Ad Sets | Returns a list of ad sets for the selected account. | | Ads | Returns a list of ads for the selected account. | | Custom Audiences | Returns the custom audiences owned by or shared with the selected account. | | Insights | Returns insight information for the specified criteria. | | Partners | Returns a list of ad partners for the selected account. | | Rate Card | Returns a list of currency rates. | | Reach Estimate | Returns the reach estimate for the selected account and given targeting spec. | | Users | Returns user IDs, permissions, and roles for the selected account. | |
    | Action Report Time | Determines the report time of action stats. For example, if a person saw the ad on Jan 1st but converted on Jan 2nd, when you query the API with action\_report\_time=impression, you see a conversion on Jan 1st. When you query the API with action\_report\_time=conversion, you see a conversion on Jan 2nd. |
    | Expand List columns | |  |  | | --- | --- | | **Option** | **Description** | | Expand as Columns | List data will expand as multiple columns in the results. | | Expand as Rows | List data will expand into multiple rows in the results. This will result in many rows with duplicated data. | | Do not expand | List data will not be expanded in the results. It will be returned as "List" in the results. | | Expand Lists In Place | List data will be converted into a comma-separated list of values for the cell. This does not result in new rows or columns. | |
    | Targeting Spec | Enter the Targeting Spec. |
    | Skip Failing Ad Set IDs | When checking if any Ad Set IDs fail, it skips, and the connector continues to run using the remaining Ad Set IDs. |
    | User Edge | |  |  | | --- | --- | | **Edge** | **Description** | | Ad Accounts | Returns information for the authenticated user's ad accounts, such as balance, capabilities, funding sources, etc. | | User Info | Returns the authenticated user's name and ID. | |

==============================================================================
FILE:   s/article/000005793.mdx
TITLE:  Microsoft OneDrive Business Enterprise OAuth Configuration Connector
LANG:   English (en_US)
ID:     000005793
==============================================================================

CHANGE 1  [## Details Pane]  (lines 31–52 in repo file)

  ⚠ LARGE CHANGE (45 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    | Menu                                      | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
    | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Report                                    | Select the Microsoft OneDrive Business Enterprise OAuth Configuration report you want to run. The following reports are available: <br/><br/> <table><thead><tr><th></th><th>Report Name</th><th>Description</th></tr></thead><tbody><tr><td><strong>Most Used Reports</strong></td><td>Download Shared File (Using SharingURL)</td><td>This report requires a URL input for the file to download. The advanced options are identical to those of other reports.</td></tr><tr><td rowspan="11"><strong>Other Reports</strong></td><td>Children</td><td>Lists the children of the item with the specified folder or path.</td></tr><tr><td>Default Drive</td><td>Returns the metadata about the authenticated user's default drive in OneDrive.</td></tr><tr><td>Download Children Shared File (SharedWithMe Endpoint) - DEPRECATED</td><td><blockquote><strong>Note:</strong> This report has been deprecated. Use the <strong>Download Shared File (Using SharingURL)</strong> report instead.</blockquote> Downloads a shared file from children's folder into Domo as a DataSet. You specify the file type, file or folder path, and other information.</td></tr><tr><td>Download File</td><td>Downloads a file into Domo as a DataSet. You specify the file type, file or folder path, and other information.</td></tr><tr><td>Download Nested Children Shared File - DEPRECATED</td><td><blockquote><strong>Note:</strong> This report has been deprecated. Use the <strong>Download Shared File (Using SharingURL)</strong> report instead.</blockquote> Downloads a shared file from the nested children folder into Domo as a DataSet. You specify the folder name and the complete file path, along with other information.</td></tr><tr><td>Download Shared File (Search Endpoint) - DEPRECATED</td><td><blockquote><strong>Note:</strong> This report has been deprecated. Use the <strong>Download Shared File (Using SharingURL)</strong> report instead.</blockquote> Downloads a shared file into Domo as a DataSet. You can specify the file type, file or folder path, and other information.</td></tr><tr><td>Download Shared File (SharedWithMe Endpoint) - DEPRECATED</td><td><blockquote><strong>Note:</strong> This report has been deprecated. Use the <strong>Download Shared File (Using SharingURL)</strong> report instead.</blockquote> Downloads a shared file into Domo as a DataSet. You specify the file type, file or folder path, and other information.</td></tr><tr><td>Drive By Id</td><td>Lists the drives for a selected drive ID.</td></tr><tr><td>Drives</td><td>Lists the available drives for the authenticated user.</td></tr><tr><td>Properties</td><td>Returns the properties for the item with the specified file/folder or path.</td></tr><tr><td>Recent Items</td><td>Returns a list of items that have been recently used by the authenticated user. This list includes items in the user's drive as well as items they have access to from other drives.</td></tr><tr><td>Shared Items - DEPRECATED</td><td><blockquote><strong>Note:</strong> This report has been deprecated. Use the <strong>Download Shared File (Using SharingURL)</strong> report instead.</blockquote> Lists items that are shared with the authenticated user.</td></tr></tbody></table>                                                                                                                                                        |
    | Drive Id                                  | The ID for the requested drive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
    | Addressing                                | Select the type of addressing with which the item can be found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
    | Path                                      | Enter the relative path of the file/folder to retrieve the report. <br/><br/> For Shared File, enter the relative path of the file/folder of the user who has shared the file/folder. <br/><br/> e.g. `/Documents/test.csv`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
    | Folder Name                               | Enter the folder name.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
    | Shared File Name                          | Enter the shared file name with extension.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | File/Folder Name                          | Enter the name (or a portion of the name), without extension, of the MS OneDrive file/folder to retrieve. The connector returns the most recently created or modified file matching the name, based on what you have selected in the **Date Field** menu. <br/><br/> **Note:** For the **Children** report, a Folder Name (or a portion of the Folder name) is expected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
    | Shared Folder Name                        | Enter the parent shared folder name.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
    | Nested Shared File Path                   | Enter the complete nested path of the shared file with extension. <br/><br/> For example: `Folder1/Folder2/test.csv`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
    | Match Type                                | Specify whether the retrieved file starts with or contains the name you have entered in the **File Name**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | File Or Folder                            | Specify whether the item is a folder or a file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
    | File Type                                 | Select the file type for the data file you want to retrieve.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
    | File Name or Portion to Search for in Zip | Enter the name of the zipped file to be retrieved. Leave the field blank to retrieve the first zipped file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
    | Zipped File Type                          | Select the file type for the zipped data file you want to retrieve.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
    | Parent Folder Name                        | Enter a complete folder name (not case-sensitive) to limit the file search to a specific folder's root contents. Subfolders are ignored. If you leave this blank, the search includes all files in all folders for which you have access.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
    | Date Field                                | Specify whether to retrieve a file based on its creation or last modified date. This is useful when there are multiple files containing the name you have entered in the **File Name**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
    | Is Password Protected?                    | Select **Yes** if the Excel file is password protected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
    | File Password                             | Enter the file password.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
    | Sheet Name                                | Enter the sheet name you want to retrieve from the specified spreadsheet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
    | Sheet Name Match Type                     | Select the type of match to perform on the sheet name.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Microsoft OneDrive Business Enterprise OAuth Configuration report you want to run. The following reports are available:     |  |  |  | | --- | --- | --- | |  | **Report Name** | **Description** | | Most Used Reports | Download Shared File (Using SharingURL) | This report requires a URLinput for the file to download. The advanced options are identical to those of other reports. | | Other Reports | Children | Lists the children of the item with the specified folder or path. | | Default Drive | Returns the metadata about the authenticated user's default drive in OneDrive. | | Download Children Shared File (SharedWithMe Endpoint) - DEPRECATED |
    <Note>**Note:** This report has been deprecated. Use the **Download Shared File (Using SharingURL)**report instead.  Downloads a shared file from children's folder into Domo as a DataSet. You specify the file type, file or folder path, and other information. | | Download File | Downloads a file into Domo as a DataSet. You specify the file type, file or folder path, and other information. | | Download Nested Children Shared File - DEPRECATED | **Note:**This report has been deprecated. Use the **Download Shared File (Using SharingURL)**report instead.  Downloads a shared file from the nested children folder into Domo as a DataSet. You specify the folder name and the complete file path, along with other information. | | Download Shared File (Search Endpoint) - DEPRECATED | **Note:**This report has been deprecated. Use the **Download Shared File (Using SharingURL)**report instead.  Downloads a shared file into Domo as a DataSet. You can specify the file type, file or folder path, and other information. | | Download Shared File (SharedWithMe Endpoint) - DEPRECATED | **Note:**This report has been deprecated. Use the **Download Shared File (Using SharingURL)**report instead.  Downloads a shared file into Domo as a DataSet. You specify the file type, file or folder path, and other information. | | Drive By Id | Lists the drives for a selected drive ID. | | Drives | Lists the available drives for the authenticated user. | | Properties | Returns the properties for the item with the specified file/folder or path. | | Recent Items | Returns a list of items that have been recently used by the authenticated user. This list includes the items that are in the user's drive as well as items they have access to from other drives. | | Shared Items - DEPRECATED | **Note:**This report has been deprecated. Use the **Download Shared File (Using SharingURL)**report instead.  Lists items that are shared with the authenticated user. | |
    | Drive Id | The ID for the requested drive. |
    | Addressing | Select the type of addressing with which the item can be found. |
    | Path | Enter the relative path of the file/folder to retrieve the the report.  For Shared File Enter the relative path of the file/folder of the user who has shared the file/folder.  e.g. /Documents/test.csv |
    | Folder Name | Enter the folder name. |
    | Shared File Name | Enter the shared file name with extension. |
    | File/Folder Name | Enter the name (or a portion of the name), without extension, of the MS OneDrive file/folder to retrieve. The connector returns the most recently created or modified file matching the name, based on what you have selected in the **Date Field** menu.  **Note:** For **Children** Report, Folder Name (or a portion of the Folder name) is expected. |
    | Shared Folder Name | Enter the Parent shared folder name. |
    | Nested shared file Path | Enter the complete nested path of the shared file with extension.  For example,. Folder1/Folder2/test.csv |
    | Match Type | Specify whether the retrieved file starts with or contains the name you have entered in the **File Name**. |
    | File Or Folder | Specify whether the item is a folder or a file. |
    | File Type | Select the file type for the data file you want to retrieve. |
    | File name or portion of file name to search for in zip | Enter the name of the zipped file to be retrieved. Leave the fields as blank to retrieve the first zipped file. |
    | Zipped File Type | Select the file type for the zipped data file you want to retrieve. |
    | Parent Folder Name | Enter a complete folder name (not case-sensitive) to limit the file search to a specific folder's root contents. Subfolders are ignored. If you leave this blank, the search includes all files in all folders for which you have access. |
    | Date Field | Specify whether to retrieve a file based on its creation or last modified date. This is useful when there are multiple files containing the name you have entered in the **File Name**. |
    | Is Password Protected ? | Select **Yes**, if the excel file is password protected. |
    | File Password | Enter the file password. |
    | Sheet Name | Enter the sheet name you want to retrieve from the specified spreadsheet. |
    | Sheet Name Match Type | Select the type of match to perform on the sheet name. |</Note>

CHANGE 2  [## Advanced]  (lines 56–71 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    | Menu                                | Description                                                                                                                                                                                                       |
    | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Enter your data tag                 | Enter the tag where your data resides in the XML text.                                                                                                                                                            |
    | Do you require attributes in data?  | Select **Yes** if you require attribute values as a part of data.                                                                                                                                                 |
    | Enter fields to exclude             | Provides a comma separated list of fields you want to exclude from the XML import.                                                                                                                                |
    | Enter XPath expression              | Enter your XPath query using standard XPath syntax as defined by the World Wide Web Consortium. ([http://www.w3.org/TR/2014/REC-xpath-30-20140408/](http://www.w3.org/TR/2014/REC-xpath-30-20140408/))            |
    | Header Start Row                    | Enter the number for the header row in the data set.                                                                                                                                                              |
    | Data Start Row                      | Enter the number for the first data row in the data set.                                                                                                                                                          |
    | Footer                              | Enter the number of rows at the end of the data set to skip. For example, to skip the last two rows you would enter `2`.                                                                                          |
    | Delimiter                           | Select the desired delimiter for parsing CSV files.                                                                                                                                                               |
    | Custom Delimiter                    | Enter the desired CSV delimiter character. Enter `\t` for tab or Unicode chars in `\\uxxxx` form (e.g. `\\u005E`).                                                                                                |
    | Quote Character                     | Select the desired quote character for parsing CSV files. (A double quote is the CSV standard.)                                                                                                                   |
    | Custom Quote Character              | Enter the desired CSV quote character. Enter Unicode chars in `\\uxxxx` form (e.g. `\\u005E`).                                                                                                                    |
    | Escape Character                    | Select the desired escape character for parsing CSV files.                                                                                                                                                        |
    | Custom Escape Character             | Enter the desired CSV escape character. Enter Unicode chars in `\\uxxxx` form (e.g. `\\u005E`).                                                                                                                   |
    | File Encoding                       | Select the file encoding. Defaults to **UTF-8**.                                                                                                                                                                  |

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Menu** | **Description** |
    | Enter your data tag | Enter the tag where your data resides in the XML text. |
    | Do you require attributes in data? | Select 'Yes' if you require attributes values as a part of data. |
    | Enter fields to exclude | Provides a comma separated list of fields you want to exclude from the XML import. |
    | Enter XPath expression | Enter your XPath query using standard XPathsyntax as defined by the World Wide Web Consortium. ([http://www.w3.org/TR/2014/REC-xpath-30-20140408/](https://www.w3schools.com/%22http://www.w3.org/TR/2014/REC-xpath-30-20140408//%22) |
    | Header Start Row | Enter the number for the header row in the data set. |
    | Data Start Row | Enter the number for the first data row in the data set. |
    | Footer | Enter the number of rows at the end of the data set to skip. For example, to skip the last two rows you would enter 2. |
    | Delimiter | Select the desired delimiter for parsing CSV files. |
    | Custom Delimiter | Enter the desired CSV delimiter character. Enter \t for tab or Unicode chars in \\uxxxx form (e.g. \\u005E). |
    | Quote Character | Select the desired quote character for parsing CSV files. (A double quote is the CSV standard.) |
    | Custom Quote Character | Enter the desired CSV quote character. Enter Unicode chars in \\uxxxx form (e.g. \\u005E). |
    | Escape character | Select the desired escape character for parsing CSV files. |
    | Custom Escape Character | Enter the desired CSV escape character. Enter Unicode chars in \\uxxxx form (e.g. \\u005E). |
    | File Encoding | Please select file encoding. Defaults to **UTF-8**. |

==============================================================================
FILE:   s/article/000005795.mdx
TITLE:  Google Ads Multi Accounts Connector
LANG:   English (en_US)
ID:     000005795
==============================================================================

CHANGE 1  [## Details Pane]  (lines 50–52 in repo file)

  OLD (remove from repo):
    | Object | Select the Google Ads Multi Accounts report you want to run. The following reports are available under this section: <br/><br/> <table><thead><tr><th>Report Name</th><th>Description</th></tr></thead><tbody><tr><td>Ad</td><td>Returns the ad details.</td></tr><tr><td>Ad Group</td><td>Returns the ad group details.</td></tr><tr><td>Ad Group Audience View</td><td>Returns the ad group audience view details.</td></tr><tr><td>Age Range View</td><td>Returns the age range view details.</td></tr><tr><td>Bidding Strategy</td><td>Returns the bidding strategy details.</td></tr><tr><td>Call View</td><td>Returns the call tracking of call-only ads or call extensions details.</td></tr><tr><td>Campaign</td><td>Returns the campaign details.</td></tr><tr><td>Campaign Audience View</td><td>Returns the campaign audience view details.</td></tr><tr><td>Campaign Budget</td><td>Returns the campaign budget details.</td></tr><tr><td>Conversion Action</td><td>Returns the conversion action details.</td></tr><tr><td>Customer Client Link</td><td>Represents customer client link relationship.</td></tr><tr><td>Customer</td><td>Returns customer details.</td></tr><tr><td>Dynamic Search Ads Search Term View</td><td>Returns dynamic search ads search term view details.</td></tr><tr><td>Geographic View</td><td>Returns geographic view details. Geographic View includes all metrics aggregated at the country level, one row per country. It reports metrics at either actual physical location of the user or an area of interest. If other segment fields are used, you may get more than one row per country.</td></tr><tr><td>Gender View</td><td>Returns gender view details.</td></tr><tr><td>Keyword View</td><td>Returns keyword view details.</td></tr><tr><td>Landing Page View</td><td>Returns the landing page view details.</td></tr><tr><td>Managed Placement View</td><td>Returns the placement view details.</td></tr><tr><td>Product Group View</td><td>Returns the product group details.</td></tr><tr><td>Search Term View</td><td>Returns search term view details.</td></tr><tr><td>Shopping Performance View</td><td>Returns the shopping performance details.</td></tr></tbody></table> |
    | Fields | The following fields appear under this section, from where you can select multiple options: <br/><br/> - Resource Fields <br/> - Metrics <br/> - Segments |
    | Dates | Select the date to run each dataset. <br/><br/> - **Date Selection:** Specify whether the data in this report is for a single date or a range of dates. <br/> - **Date Type:** Select Specific or Relative from the drop-down list. <br/> - **Days Back:** Choose which day you would like to receive the data for by entering a number relative to today. For example, enter 0 for today, 1 for yesterday, or 7 for 7 days ago. <br/> - **Date:** Select the date from the date selection tool. <br/> - **Start Date - Relative - Days Back** - Choose which day you want to start receiving the data for by entering a number relative to today. For example, enter 1 for a start date of yesterday, 7 for a start date of 1 week ago, and 30 for a start date of 1 month ago. <br/> - **End Date - Relative - Days Back** - Choose which day you want to stop receiving the data for by entering a number relative to today. For example, enter 0 for an end date of today, or 1 for an end date of yesterday. |

  NEW (replace with / insert from CSV):
    | Object | Select the Google Ads Multi Accounts report you want to run. The following reports are available under this section:   |  |  | | --- | --- | | **Report Name** | **Description** | | Ad | Returns the ad details. | | Ad Group | Returns the ad group details | | Ad Group Audience View | Returns the ad group audience view details. | | Age Range View | Returns the age range view details. | | Bidding Strategy | Returns the bidding strategy details. | | Call View | Returns the call tracking of call-only ads or call extensions details. | | Campaign | Returns the campaign details. | | Campaign Audience View | Returns the campaign audience view details. | | Campaign Budget | Returns the campaign budget details. | | Conversion Action | Returns the conversion action details. | | Customer Client Link | Represents customer client link relationship. | | Customer | Returns customer details. | | Dynamic Search Ads Search Term View | Returns dynamic search ads search term view details. | | Geographic View | Returns geographic view details. Geographic View includes all metrics aggregated at the country level, one row per country. It reports metrics at either actual physical location of the user or an area of interest. If other segment fields are used, you may get more than one row per country. | | Gender View | Returns gender view details. | | Keyword view | Returns keyword view details. | | Landing Page View | Returns the landing page view details. | | Managed Placement View | Returns the placement view details. | | Product Group View | Returns the product group details. | | Search Term View | Returns search term view details. | | Shopping Performance View | Returns the shopping performance details. | |
    | Fields | The following fields appear under this section, from where you can select multiple options:   - Resource Fields - Metrics - Segments |
    | Dates | Select the date to run each dataset.   - **Date Selection:**Specify whether the data in this report is for a single date or a range of dates. - **Date Type:** Select Specific or Relative from the drop-down list. - **Days Back:** Choose which day you would like to receive the data for by entering a number relative to today. For example, enter 0 for today, 1 for yesterday, or 7 for 7 days ago. - **Date:** Select the date from the date selection tool. - **Start Date - Relative - Days Back** - Choose which day you want to start receiving the data for by entering a number relative to today. For example, enter 1 for a start date of yesterday, 7 for a start date of 1 week ago, and 30 for a start date of 1 month ago. - **End Date - Relative - Days Back** - Choose which day you want to stop receiving the data for by entering a number relative to today. For example, enter 0 for an end date of today, or 1 for an end date of yesterday. |

==============================================================================
FILE:   s/article/000005806.mdx
TITLE:  Shopify Multi Accounts Connector
LANG:   English (en_US)
ID:     000005806
==============================================================================

CHANGE 1  [### Details Pane]  (line 36 in repo file)

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |

CHANGE 2  [### Details Pane]  (line 37 in repo file)

  OLD (remove from repo):
    | --- | --- |

==============================================================================
FILE:   s/article/000005825.mdx
TITLE:  Powering Up Multi Account Connectors
LANG:   English (en_US)
ID:     000005825
==============================================================================

CHANGE 1  [# Reading the DataSet]  (line 67 in repo file)

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |

CHANGE 2  [# Reading the DataSet]  (line 68 in repo file)

  OLD (remove from repo):
    | --- | --- |

CHANGE 3  [# Dataset Update History]  (line 91 in repo file)

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |

CHANGE 4  [# Dataset Update History]  (line 92 in repo file)

  OLD (remove from repo):
    | --- | --- |

==============================================================================
FILE:   s/article/000005844.mdx
TITLE:  Entrata Via API Key Connector
LANG:   English (en_US)
ID:     000005844
==============================================================================

CHANGE 1  [## Details Pane]  (line 79 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Entrata Via API Key report you want to run. The following reports are available: </p><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Report Name </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"><p> Lease Details </p></td><td colspan="1" rowspan="1"> Retrieves the charge details information for all leases for the selected property. </td></tr><tr><td colspan="1" rowspan="1"><p> Leases Expiring </p></td><td colspan="1" rowspan="1"> Returns the list of expiring leases. </td></tr><tr><td colspan="1" rowspan="1"><p> Report Data </p></td><td colspan="1" rowspan="1"> Retrieves the data about a report based on the user-supplied filters. </td></tr><tr><td colspan="1" rowspan="1"><p> Financial PickList </p></td><td colspan="1" rowspan="1"> Retrieves the financial picklist. </td></tr><tr><td colspan="1" rowspan="1"><p> Invoices </p></td><td colspan="1" rowspan="1"> Retrieves the invoice batch. </td></tr><tr><td colspan="1" rowspan="1"><p> Lead Events </p></td><td colspan="1" rowspan="1"> Retrieves the lead events for the selected property. </td></tr><tr><td colspan="1" rowspan="1"><p> Leads </p></td><td colspan="1" rowspan="1"> Retrieves the leads for the selected property. </td></tr><tr><td colspan="1" rowspan="1"><p> Leases </p></td><td colspan="1" rowspan="1"> Retrieves the lease information for a selected property. </td></tr><tr><td colspan="1" rowspan="1"><p> Property PickList </p></td><td colspan="1" rowspan="1"> Retrieves a pick list for properties. </td></tr><tr><td colspan="1" rowspan="1"><p> Specials </p></td><td colspan="1" rowspan="1"> Retrieves specials/concessions for the selected property. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Select Properties </td><td colspan="1" rowspan="1"> Specify whether you want to retrieve all properties or a specific property. </td></tr><tr><td colspan="1" rowspan="1"> Property </td><td colspan="1" rowspan="1"> Select the property you want to view. </td></tr><tr><td colspan="1" rowspan="1"> Picklist </td><td colspan="1" rowspan="1"> Select the property picklist you want to view. </td></tr><tr><td colspan="1" rowspan="1"> Date Filter Type </td><td colspan="1" rowspan="1"> Select the Date Type to filter. </td></tr><tr><td colspan="1" rowspan="1"> Lease Details Sub Report </td><td colspan="1" rowspan="1"> Select the sub-report data you would like to include for lease details. As per your selection, the connector will create a new row of data per record. For example, if a lease has 5 charges, you will see 5 rows of data for that lease. </td></tr><tr><td colspan="1" rowspan="1"> Report Name </td><td colspan="1" rowspan="1"> Select the name of the Entrata report for which you want to retrieve the data. </td></tr><tr><td colspan="1" rowspan="1"> Report Version </td><td colspan="1" rowspan="1"> Select the version of the Entrata report for which you would like to retrieve the data. </td></tr><tr><td colspan="1" rowspan="1"> Report Filters </td><td colspan="1" rowspan="1"><p> Enter that the user-customized report filters to limit the returned data. Available filters can be found in the Report Filters report. Filters must be in JSON format, and any date fields in the filters must have the m/d/Y value replaced with an appropriate date string. </p><p><br/> For example, if you find daterange-start, daterange-end, or date parameters, replace m/d/Y with a string formatted like the following: 3/1/2022. If you are replacing the date parameter, this will select reports on the date of March 1st, 2022. </p><p><br/> Ensure to only include the inner filters and not the filters key. So, instead of `"filters": {"Filter_One": {"name": "property", "required": null},"Filter_Two":{"name": "property_stats", "required": true}}` you would supply the following inner JSON objects `"Filter_One":{"name": "property", "required": null},"Filter_Two": {"name": "property_stats", "required": true}` </p></td></tr><tr><td colspan="1" rowspan="1"> Field To Flatten </td><td colspan="1" rowspan="1"><p> Enter the column name for Entrata Data that you would like to parse and flatten to retrieve the data that is normally returned as a List. You may only enter one column at a time. </p></td></tr><tr><td colspan="1" rowspan="1"> Select Dates </td><td colspan="1" rowspan="1"><p> Select dates. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Entrata Via API Key report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | AR Payments | Retrieves the AR payments details. | | AR Transactions | Retrieves a list of all the transactions. | | Customers | Retrieves a list of customers for a property. | | Financial General Ledger Transactions | Returns all General Ledger transactions for a property. | | Financial General Ledger Trees | Retrieves the General Ledger accounts information. | | Financial PickList | Retrieves the financial picklist. | | Invoices | Retrieves the invoice batch. | | Lead Events | Retrieves the lead events for the selected property. | | Leads | Retrieves the leads for the selected property. | | Lease Details | Retrieves the charge details information for all leases for the selected property. | | Leases | Retrieves the lease information for a selected property. | | Leases Expiring | Returns the list of expiring leases. | | Maintenance Work Orders | Retrieves the work orders information. | | Properties | Retrieves property details for the management company. | | Property PickList | Retrieves a pick list for properties. | | Property Unit Types | Pulls a list of unit types. | | Property Units Availability and Pricing | Pulls a list of Unit availability and their pricing. | | Property Units MITS - ILS Units | Retrieves property ILS unit information. | | Report Data | Retrieves the data about a report based on the user-supplied filters. | | Report Filters | Retrieves the filters available for an individual report. | | Specials | Retrieves specials/concessions for the selected property. | |
    | Select Properties | Specify whether you want to retrieve all properties or a specific property. |
    | Property | Select the property you want to view. |
    | Picklist | Select the property picklist you want to view. |
    | Date Filter Type | Select the Date Type to filter. |
    | Lease Details Sub Report | Select the sub-report data you would like to include for lease details. As per your selection, the connector will create a new row of data per record. For example, if a lease has 5 charges, you will see 5 rows of data for that lease. |
    | Report Name | Select the name of the Entrata report for which you want to retrieve the data. |
    | Report Version | Select the version of the Entrata report for which you would like to retrieve the data. |
    | Report Filters | Enter that the user-customized report filters to limit the returned data. Available filters can be found in the Report Filters report. Filters must be in JSON format, and any date fields in the filters must have the m/d/Y value replaced with an appropriate date string.  For example, if you find daterange-start, daterange-end, or date parameters, replace m/d/Y with a string formatted like the following: 3/1/2022. If you are replacing the date parameter, this will select reports on the date of March 1st, 2022.  Ensure to only include the inner filters and not the filters key. So, instead of \"filters\": {\"Filter\_One\": {\"name\": \"property\", \"required\": null},\"Filter\_Two\":{\"name\": \"property\_stats\", \"required\": true}} you would supply the following inner JSON objects \"Filter\_One\":{\"name\": \"property\", \"required\": null},\"Filter\_Two\": {\"name\": \"property\_stats\", \"required\": true} |
    | Field To Flatten | Enter the column name for Entrata Data that you would like to parse and flatten to retrieve the data that is normally returned as a List. You may only enter one column at a time. |
    | Select Dates | Select dates. |

==============================================================================
FILE:   s/article/000005870.mdx
TITLE:  Quickbooks Multi Account V2 Connector
LANG:   English (en_US)
ID:     000005870
==============================================================================

CHANGE 1  [### Details Pane]  (line 38 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><th colspan="1" rowspan="1"> Report </th><th colspan="1" rowspan="1"> Description </th></tr><tr><td colspan="1" rowspan="1"> Accounts </td><td colspan="1" rowspan="1"> Retrieves a list of accounts. </td></tr><tr><td colspan="1" rowspan="1"> Account List </td><td colspan="1" rowspan="1"> Shows the name, type, and balance for each account. </td></tr><tr><td colspan="1" rowspan="1"> Accounts Payable Aging Detail </td><td colspan="1" rowspan="1"> Shows detailed information for unpaid bills in accounts payable. </td></tr><tr><td colspan="1" rowspan="1"> Accounts Payable Aging Summary </td><td colspan="1" rowspan="1"> Summarizes the status of unpaid bills in accounts payable. </td></tr><tr><td colspan="1" rowspan="1"> Accounts Receivable Aging Detail </td><td colspan="1" rowspan="1"> Shows detailed information for outstanding amounts in accounts receivable. </td></tr><tr><td colspan="1" rowspan="1"> Accounts Receivable Aging Summary </td><td colspan="1" rowspan="1"> Shows accounts receivable, along with the amount of time the amounts have been outstanding. </td></tr><tr><td colspan="1" rowspan="1"> Balance Sheet </td><td colspan="1" rowspan="1"> Summarizes assets, liabilities, and shareholders' equity. </td></tr><tr><td colspan="1" rowspan="1"> Budget </td><td colspan="1" rowspan="1"> Retrieves a list of budgets. </td></tr><tr><td colspan="1" rowspan="1"> Expenses by Vendor </td><td colspan="1" rowspan="1"> Shows expenses by vendor. </td></tr><tr><td colspan="1" rowspan="1"> Profit and Loss Detail </td><td colspan="1" rowspan="1"> Shows detailed information about income and expenses. </td></tr><tr><td colspan="1" rowspan="1"> Profit and Loss Summary </td><td colspan="1" rowspan="1"> Summarizes income and expenses. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Transaction Date </td><td colspan="1" rowspan="1"> Select the start date for the report. </td></tr><tr><td colspan="1" rowspan="1"> Report Date </td><td colspan="1" rowspan="1"><p> This option appears when you select the <b> Custom Single Date </b> option from the <b> Transaction Date </b>. </p><p> Specify whether the report data is based on a <b> fixed date </b> or a <b> relative number of days </b>. A relative number of days indicates the retrospective period from which data will be retrieved. To retrieve data from a specific date, utilize the date selector. </p></td></tr><tr><td colspan="1" rowspan="1"> Select Specific Date </td><td colspan="1" rowspan="1"><p> This option is available when you select the <b> Report Date </b> as <b> Specific </b>. </p><p> To select a specific report date, use the <b> date picker </b>. This option is only available when the <b> Report Date </b> is set to <b> Specific </b>. </p></td></tr><tr><td colspan="1" rowspan="1"> Date Offset </td><td colspan="1" rowspan="1"><p> Enter the number of days you want to go back from the current date. For example, enter <b> 0 </b> for today or <b> 1 </b> for yesterday. </p></td></tr><tr><td colspan="1" rowspan="1"> Aging Method </td><td colspan="1" rowspan="1"> Select the aging method. </td></tr><tr><td colspan="1" rowspan="1"> Days Per Aging Period </td><td colspan="1" rowspan="1"> Enter the number of days per aging period. </td></tr><tr><td colspan="1" rowspan="1"> Number of Periods </td><td colspan="1" rowspan="1"> Enter the number of aging periods to be shown in the report. </td></tr><tr><td colspan="1" rowspan="1"> Due Date Duration </td><td colspan="1" rowspan="1"> Choose whether the report should cover a <b> Single Date </b> or a <b> Date Range </b>. </td></tr><tr><td colspan="1" rowspan="1"> Date From </td><td colspan="1" rowspan="1"><p> Choose how you want to set the start date for the report. </p><ul><li><b> Relative Date: </b> Enter the number of days back from today. </li><li><b> Specific Date: </b> Use the date picker to select a precise date. </li></ul></td></tr><tr><td colspan="1" rowspan="1"> Select Specific Date From </td><td colspan="1" rowspan="1"><p> Select the precise start date for the report using the date picker. This option is only available when the <b> Report Date </b> is set to <b> Specific. </b></p></td></tr><tr><td colspan="1" rowspan="1"> Date From Offset </td><td colspan="1" rowspan="1"><p> Enter the number of days you want to go back from the current date. For example, enter <b> 0 </b> for today or <b> 1 </b> for yesterday. </p></td></tr><tr><td colspan="1" rowspan="1"> Date To </td><td colspan="1" rowspan="1"><p> Choose how you want to set the end date for the report. </p><ul><li><b> Relative Date: </b> Enter the number of days back from today. </li><li><b> Specific Date: </b> Use the date picker to select a precise date. </li></ul></td></tr><tr><td colspan="1" rowspan="1"> Select Specific Date To </td><td colspan="1" rowspan="1"><p> Select the precise end date for the report using the date picker. This option is only available when the <b> Report Date </b> is set to <b> Specific. </b></p></td></tr><tr><td colspan="1" rowspan="1"> Date To Offset </td><td colspan="1" rowspan="1"><p> Enter the number of days you want to go back from the selected end date. For example, enter <b> 0 </b> for today or <b> 1 </b> for yesterday. </p></td></tr><tr><td colspan="1" rowspan="1"> Transaction Date Duration </td><td colspan="1" rowspan="1"> Specify the reporting period. </td></tr><tr><td colspan="1" rowspan="1"> Summarize Column By </td><td colspan="1" rowspan="1"> Select how you want to categorize your report data. </td></tr><tr><td colspan="1" rowspan="1"> Budget Date Selection </td><td colspan="1" rowspan="1"><p> Choose whether the report data covers a single date or a date range. </p></td></tr><tr><td colspan="1" rowspan="1"> Single Date - <b> Days Back </b></td><td colspan="1" rowspan="1"> Specify the number of days before today for which you would like to receive the data. For example, enter 0 for today or 7 for seven days ago. </td></tr><tr><td colspan="1" rowspan="1"> Start Date - <b> Days Back </b></td><td colspan="1" rowspan="1"> Choose the day for which you would like to start receiving data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. </td></tr><tr><td colspan="1" rowspan="1"> End Date - <b> Days Back </b></td><td colspan="1" rowspan="1"> Choose the day for which you would like to stop receiving data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. </td></tr><tr><td colspan="1" rowspan="1"> Time Period </td><td colspan="1" rowspan="1"> Specify the timeframe for data retrieval. </td></tr><tr><td colspan="1" rowspan="1"> Select Columns (checkbox) </td><td colspan="1" rowspan="1"><p> Select the columns you want to include. If you don't make a selection, the connector uses the default columns. The schema remains the same, even if you change column selections. </p><b> Note: </b> This feature is for advanced users. Incorrect column selections may cause errors in Quickbooks. </td></tr><tr><td colspan="1" rowspan="1"> Select Columns </td><td colspan="1" rowspan="1"><p> This field appears when you select the Select Column checkbox. </p><p> Select the columns you want to include. A single selection may result in multiple columns. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the report you want to run. The following reports are available:   | Report | Description | | --- | --- | | Accounts | Retrieves a list of accounts. | | Account List | Shows the name, type, and balance for each account. | | Accounts Payable Aging Detail | Shows detailed information for unpaid bills in accounts payable. | | Accounts Payable Aging Summary | Summarizes the status of unpaid bills in accounts payable. | | Accounts Receivable Aging Detail | Shows detailed information for outstanding amounts in accounts receivable. | | Accounts Receivable Aging Summary | Shows accounts receivable, along with the amount of time the amounts have been outstanding. | | Balance Sheet | Summarizes assets, liabilities, and shareholders' equity. | | Budget | Retrieves a list of budgets. | | Expenses by Vendor | Shows expenses by vendor. | | Profit and Loss Detail | Shows detailed information about income and expenses. | | Profit and Loss Summary | Summarizes income and expenses. | |
    | Transaction Date | Select the start date for the report. |
    | Report Date | This option appears when you select the **Custom Single Date** option from the **Transaction Date**.  Specify whether the report data is based on a **fixed date** or a **relative number of days**. A relative number of days indicates the retrospective period from which data will be retrieved. To retrieve data from a specific date, use the date selector. |
    | Select Specific Date | This option is available when you select the **Report Date** as **Specific**.  To select a specific report date, use the **date picker**. This option is only available when the **Report Date** is set to **Specific**. |
    | Date Offset | Enter the number of days you want to go back from the current date. For example, enter **0** for today or **1** for yesterday. |
    | Aging Method | Select the aging method. |
    | Days Per Aging Period | Enter the number of days per aging period. |
    | Number of Periods | Enter the number of aging periods to be shown in the report. |
    | Due Date Duration | Choose whether the report should cover a **Single Date** or a **Date Range**. |
    | Date From | Choose how you want to set the start date for the report.   - **Relative Date:** Enter the number of days back from today. - **Specific Date:** Use the date picker to select a precise date. |
    | Select Specific Date From | Select the precise start date for the report using the date picker.  This option is only available when the **Report Date** is set to **Specific.** |
    | Date From Offset | Enter the number of days you want to go back from the current date. For example, enter **0** for today or **1** for yesterday. |
    | Date To | Choose how you want to set the end date for the report.   - **Relative Date:** Enter the number of days back from today. - **Specific Date:** Use the date picker to select a precise date. |
    | Select Specific Date To | Select the precise end date for the report using the date picker.  This option is only available when the **Report Date** is set to **Specific.** |
    | Date To Offset | Enter the number of days you want to go back from the selected end date. For example, enter **0** for today or **1** for yesterday. |
    | Transaction Date Duration | Specify the reporting period. |
    | Summarize Column By | Select how you want to categorize your report data. |
    | Budget Date Selection | Choose whether the report data covers a single date or a date range. |
    | Single Date - **Days Back** | Specify the number of days before today for which you would like to receive the data. For example, enter 0 for today or 7 for seven days ago. |
    | Start Date - **Days Back** | Choose the day for which you would like to start receiving data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. |
    | End Date - **Days Back** | Choose the day for which you would like to stop receiving data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. |
    | Time Period | Specify the timeframe for data retrieval. |
    | Select Columns (checkbox) | Select the columns you want to include. If you don't make a selection, the connector uses the default columns. The schema remains the same, even if you change column selections.
    <Note>**Note:** This feature is for advanced users. Incorrect column selections may cause errors in Quickbooks. |
    | Select Columns | This field appears when you select the Select Column checkbox.  Select the columns you want to include. A single selection may result in multiple columns. |</Note>

==============================================================================
FILE:   s/article/000005892.mdx
TITLE:  Adobe Analytics v2 OAuth Partition Connector
LANG:   English (en_US)
ID:     000005892
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    With the Domo Adobe Analytics v2 OAuth Partition connector, you can retrieve data about customers, various metrics, dimensions, and segments to view a big web engagement picture. With Domo, you can easily access Adobe's web analytics metrics and combine them with data from CRM, POS, finance, and any other system to make faster, better-informed decisions. This connector supports partitioning and uses custom reports. To learn more about the Adobe Analytics API, visit their website ([https://adobedocs.github.io/analytics-2.0-apis/#/](https://adobedocs.github.io/analytics-2.0-apis/#/ "https://adobedocs.github.io/analytics-2.0-apis/#/") ).

  NEW (replace with / insert from CSV):
    With the Domo Adobe Analytics v2 OAuth Partition connector, you can retrieve data about customers, various metrics, dimensions, and segments to view a big web engagement picture. With Domo, you can easily access Adobe's web analytics metrics and combine them with data from CRM, POS, finance, and any other system to make faster, better-informed decisions. This connector supports partitioning and uses custom reports. To learn more about the Adobe Analytics API, visit their website ([https://adobedocs.github.io/analytics-2.0-apis/#/](https://adobedocs.github.io/analytics-2.0-apis/#/) ).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to the Adobe Analytics API in the Data Center. This topic discusses the fields and menus that are specific to the Adobe Analytics v2 OAuth Partition connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    You connect to the Adobe Analytics API in the Data Center. This topic discusses the fields and menus that are specific to the Adobe Analytics v2 OAuth Partition connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (line 15 in repo file)

  OLD (remove from repo):
    Visit [Adobe Developer Console](https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/AuthenticationOverview/OAuthIntegration.md "Adobe Developer Console") to learn more about obtaining your Client ID and Client Secret.

  NEW (replace with / insert from CSV):
    Visit [Adobe Developer Console](https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/AuthenticationOverview/OAuthIntegration.md) to learn more about obtaining your Client ID and Client Secret.

CHANGE 4  [## Prerequisites]  (line 17 in repo file)

  OLD (remove from repo):
    ### Creating a new OAuth project in Adobe Developer Console

  NEW (replace with / insert from CSV):
    ### Creating a new OAuth project in Adobe Developer Console

CHANGE 5  [### Creating a new OAuth project in Adobe Developer Console]  (lines 21–22 in repo file)

  OLD (remove from repo):
    1. Log into
    [https://console.adobe.io/projects](https://console.adobe.io/projects "Follow link").

  NEW (replace with / insert from CSV):
    1. Log into [https://console.adobe.io/projects](https://console.adobe.io/projects).

CHANGE 6  [### Creating a new OAuth project in Adobe Developer Console]  (lines 24–25 in repo file)

  OLD (remove from repo):
    2. Click
    **Create new project** button.

  NEW (replace with / insert from CSV):
    2. Click **Create new project** button.

CHANGE 7  [### Creating a new OAuth project in Adobe Developer Console]  (lines 27–28 in repo file)

  OLD (remove from repo):
    3. Click on
    **Add API** to start using the Adobe services.

  NEW (replace with / insert from CSV):
    3. Click on **Add API** to start using the Adobe services.

CHANGE 8  [### Creating a new OAuth project in Adobe Developer Console]  (lines 30–31 in repo file)

  OLD (remove from repo):
    4. The APIs available to you will be listed in the
    **Add an API** window. Select **Adobe Analytics**.

  NEW (replace with / insert from CSV):
    4. The APIs available to you will be listed in the **Add an API** window. Select **Adobe Analytics**.

CHANGE 9  [### Creating a new OAuth project in Adobe Developer Console]  (lines 33–34 in repo file)

  OLD (remove from repo):
    5. Now, you need to select the authentication type. Select
    **OAuth** here.

  NEW (replace with / insert from CSV):
    5. Now, you need to select the authentication type. Select **OAuth** here.

CHANGE 10  [### Creating a new OAuth project in Adobe Developer Console]  (lines 36–37 in repo file)

  OLD (remove from repo):
    6. Select the
    **Web** platform to configure the API.

  NEW (replace with / insert from CSV):
    6. Select the **Web**platform to configure the API.

CHANGE 11  [### Creating a new OAuth project in Adobe Developer Console]  (line 39 in repo file)

  OLD (remove from repo):
    Enter the **Default** **redirect URI** as `https://oauth.domo.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange`

  NEW (replace with / insert from CSV):
        Enter the **Default** **redirect URI** as **https://oauth.domo.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange**

CHANGE 12  [### Creating a new OAuth project in Adobe Developer Console]  (line 41 in repo file)

  OLD (remove from repo):
    `Enter the Redirect URI pattern as https://oauth/.domo/.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange`

  NEW (replace with / insert from CSV):
        Enter the **Redirect URI pattern** as **https://oauth/.domo/.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange**

CHANGE 13  [### Creating a new OAuth project in Adobe Developer Console]  (lines 43–44 in repo file)

  OLD (remove from repo):
    7. Click
    **Save configured API**.

  NEW (replace with / insert from CSV):
    7. Click **Save configured API**.

CHANGE 14  [## Connecting to the Adobe Analytics API v2]  (line 48 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Adobe Analytics v2 OAuth Partition page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Adobe Analytics v2 OAuth Partition page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 15  [## Credentials Pane]  (line 54 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p> Field </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Client ID </td><td colspan="1" rowspan="1"> The Client ID is obtained from the <a href="https://console.adobe.io/home" target="_blank"> Adobe Developer Console </a>. For more information see <a href="https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/AuthenticationOverview/OAuthIntegration.md" target="_blank"> Adobe’s documentation </a>. </td></tr><tr><td colspan="1" rowspan="1"> Client Secret </td><td colspan="1" rowspan="1"> The Client Secret obtained from the <a href="https://console.adobe.io/home" target="_blank"> Adobe Developer Console </a>. For more information see <a href="https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/AuthenticationOverview/OAuthIntegration.md" target="_blank"> Adobe’s documentation </a>. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | The Client ID is obtained from the [Adobe Developer Console](https://console.adobe.io/home). For more information see [Adobe’s documentation](https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/AuthenticationOverview/OAuthIntegration.md). |
    | Client Secret | The Client Secret obtained from the [Adobe Developer Console](https://console.adobe.io/home). For more information see [Adobe’s documentation](https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/AuthenticationOverview/OAuthIntegration.md). |

CHANGE 16  [## Credentials Pane]  (line 56 in repo file)

  OLD (remove from repo):
    Once you have entered valid credentials, you can use the same account any time you go to create a new Adobe Analytics v2 DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](https://domo-support.domo.com/s/article/360042926054?language=en_US "Managing User Accounts for Connectors").

  NEW (replace with / insert from CSV):
    Once you have entered valid credentials, you can use the same account any time you go to create a new Adobe Analytics v2 DataSet. You can manage connector accounts in the **Accounts**tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 17  [## Details Pane]  (line 60 in repo file)

  OLD (remove from repo):
    This pane contains the information about the predefined Adobe Analytics v2 reports.

  NEW (replace with / insert from CSV):
    This pane contains the information about the predefined Adobe Analytics v2 reports.

CHANGE 18  [## Details Pane]  (line 62 in repo file)

  ⚠ LARGE CHANGE (31 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Adobe Analytics v2 report you want to run: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"> Calculated Metrics </td><td colspan="1" rowspan="1"> This report returns a list of calculated metrics. </td></tr><tr><td colspan="1" rowspan="1"> Collections </td><td colspan="1" rowspan="1"> This report returns a list of report suites. </td></tr><tr><td colspan="1" rowspan="1"> Dimensions </td><td colspan="1" rowspan="1"> This report returns a list of dimensions for a given report suite. </td></tr><tr><td colspan="1" rowspan="1"> Metrics </td><td colspan="1" rowspan="1"> This report returns a list of metrics for a given report suite. </td></tr><tr><td colspan="1" rowspan="1"> Report Data </td><td colspan="1" rowspan="1"> This report returns reporting data for the requested metric and dimension for the given date range. </td></tr><tr><td colspan="1" rowspan="1"> Segments </td><td colspan="1" rowspan="1"> This report returns a list of segments. </td></tr><tr><td colspan="1" rowspan="1"> Users </td><td colspan="1" rowspan="1"> This report returns a list of users for the company. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Global Company ID </p></td><td colspan="1" rowspan="1"><p> Select the company. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Report Suite id </p></td><td colspan="1" rowspan="1"><p> Select the Report Suite ID. </p></td></tr><tr><td colspan="1" rowspan="1"><p></p></td><td colspan="1" rowspan="1"><p></p></td></tr><tr><td colspan="1" rowspan="1"><p> Enter the report body JSON object instead of selecting metrics, dimensions and segments </p></td><td colspan="1" rowspan="1"><p> Select this checkbox to enter the report body JSON object. <br/> Clear the checkbox to select the metrics, dimensions, and segments, and Domo will create the report body JSON for you. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Body </p></td><td colspan="1" rowspan="1"><p> Paste the Body here, according to Adobe specifications (see <a href="https://www.w3schools.com/%22https://adobedocs.github.io/analytics-2.0-apis/#/reports/runReport\%22" target="_blank"> Adobe Swagger UI </a> ). <br/> Do not include the dimensions or date range in the report body; instead, specify the dimensions and date range below. Domo uses the provided dimensions to create the recursive report bodies to get the multiple dimension data. <br/> The report body must be a valid JSON and must contain the rsid and metrics. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Metrics </p></td><td colspan="1" rowspan="1"><p> Select the metrics. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Use label as column name </p></td><td colspan="1" rowspan="1"><p> Select the checkbox to use labels instead of IDs as column names for calculated metrics. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Dimension 1 </p></td><td colspan="1" rowspan="1"><p> Select the dimensions in drill order. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Dimension 2 </p></td><td colspan="1" rowspan="1"><p> Select the dimensions in drill order. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Dimension 3 </p></td><td colspan="1" rowspan="1"><p> Select the dimensions in drill order. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Dimension 4 </p></td><td colspan="1" rowspan="1"><p> Select the dimensions in drill order. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Dimension 5 </p></td><td colspan="1" rowspan="1"><p> Select the dimensions in drill order. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Dimension 6 </p></td><td colspan="1" rowspan="1"><p> Select the dimensions in drill order. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Segments </p></td><td colspan="1" rowspan="1"><p> Select one or more segments to filter the data by segments </p></td></tr><tr><td colspan="1" rowspan="1"><p> Include Anomaly Detection </p></td><td colspan="1" rowspan="1"><p> Select the checkbox to include anomaly detection. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Date Selection type </p></td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Report Data </b> report. </p><p> Select the date selection type for the report. You can either select a custom date range or use predefined date ranges from Adobe. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Predefined Date Range </p></td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Predefined Date Range from Adobe </b> option in the <b> Data Selection type </b> field. </p><p> Select the predefined date range from Adobe. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Report Dates </p></td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Custom Date Range </b> option in the <b> Data Selection type </b> field. </p><p> Select the report dates. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Include Type </p></td><td colspan="1" rowspan="1"><p> Include additional calculated metrics not owned by the user. The 'All' option takes precedence over 'Shared. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Date Selection </p></td><td colspan="1" rowspan="1"><p> Select the specific or relative date range. You can either select Single date or Date range from the drop-down list. </p></td></tr><tr><td colspan="1" rowspan="1"> Start Date - Specific </td><td colspan="1" rowspan="1"> Select the first date in your date range using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> End Date - Specific </td><td colspan="1" rowspan="1"> Select the last date in your date range using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> Start Date - Relative </td><td colspan="1" rowspan="1"><p> Enter the number of days back that you would like to get data from (start day). Combine with End Date to create a range of represented days. </p><p> For example, if you enter10 for Start Date and 5 for the End Date, the report would contain data starting from10 days ago until 5 days ago. </p></td></tr><tr><td colspan="1" rowspan="1"> End Date - Relative </td><td colspan="1" rowspan="1"><p> Enter the number of days back that you would like to get data to (end day). Combine with Start Date to create a range of represented days. </p><p></p><p> For example, if you enter 10 for Start Date and 5 for End Date, the report would contain data for 10 days ago up until 5 days ago. </p></td></tr><tr><td colspan="1" rowspan="1"> Reset Data </td><td colspan="1" rowspan="1"><p> Select the box to reset the data if required. Run the data once by selecting the check box and then clear the checkbox to proceed normally. <b> Note: </b> This will replace all the data in your dataset, and may result in loss of data if the dataset uses the Append mode. </p></td></tr><tr><td colspan="1" rowspan="1"> Exclude Formula Definition (checkbox) </td><td colspan="1" rowspan="1"><p> Check the box to exclude formula definition columns. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Adobe Analytics v2 report you want to run:   |  |  | | --- | --- | | Calculated Metrics | This report returns a list of calculated metrics. | | Collections | This report returns a list of report suites. | | Dimensions | This report returns a list of dimensions for a given report suite. | | Metrics | This report returns a list of metrics for a given report suite. | | Report Data | This report returns reporting data for the requested metric and dimension for the given date range. | | Segments | This report returns a list of segments. | | Users | This report returns a list of users for the company. | |
    | Global Company ID | Select the company. |
    | Report Suite id | Select the Report Suite ID. |
    |  |  |
    | Enter the report body JSON object instead of selecting metrics, dimensions and segments | Select this checkbox to enter the report body JSON object. Clear the checkbox to select the metrics, dimensions, and segments, and Domo will create the report body JSON for you. |
    | Body | Paste the Body here, according to Adobe specifications (see [Adobe Swagger UI](https://www.w3schools.com/%22https://adobedocs.github.io/analytics-2.0-apis/#/reports/runReport\%22)). Do not include the dimensions or date range in the report body; instead, specify the dimensions and date range below. Domo uses the provided dimensions to create the recursive report bodies to get the multiple dimension data. The report body must be a valid JSON and must contain the rsid and metrics. |
    | Metrics | Select the metrics. |
    | Use label as column name | Select the checkbox to use labels instead of IDs as column names for calculated metrics. |
    | Dimension 1 | Select the dimensions in drill order. |
    | Dimension 2 | Select the dimensions in drill order. |
    | Dimension 3 | Select the dimensions in drill order. |
    | Dimension 4 | Select the dimensions in drill order. |
    | Dimension 5 | Select the dimensions in drill order. |
    | Dimension 6 | Select the dimensions in drill order. |
    | Segments | Select one or more segments to filter the data by segments |
    | Include Anomaly Detection | Select the checkbox to include anomaly detection. |
    | Date Selection type | This field appears when you select the **Report Data** report.  Select the date selection type for the report. You can either select a custom date range or use predefined date ranges from Adobe. |
    | Predefined Date Range | This field appears when you select the **Predefined Date Range from Adobe** option in the **Data Selection type** field.  Select the predefined date range from Adobe. |
    | Report Dates | This field appears when you select the **Custom Date Range** option in the **Data Selection type** field.  Select the report dates. |
    | Include Type | Include additional calculated metrics not owned by the user. The 'All' option takes precedence over 'Shared. |
    | Date Selection | Select the specific or relative date range. You can either select Single date or Date range from the drop-down list. |
    | Start Date - Specific | Select the first date in your date range using the date selector. |
    | End Date - Specific | Select the last date in your date range using the date selector. |
    | Start Date - Relative | Enter the number of days back that you would like to get data from (start day). Combine with End Date to create a range of represented days.  For example, if you enter10 for Start Date and 5 for the End Date, the report would contain data starting from10 days ago until 5 days ago. |
    | End Date - Relative | Enter the number of days back that you would like to get data to (end day). Combine with Start Date to create a range of represented days.    For example, if you enter 10 for Start Date and 5 for End Date, the report would contain data for 10 days ago up until 5 days ago. |
    | Reset Data | Select the box to reset the data if required. Run the data once by selecting the check box and then clear the checkbox to proceed normally.
    <Note>**Note:** This will replace all the data in your dataset, and may result in loss of data if the dataset uses the Append mode. |
    | Exclude Formula Definition (checkbox) | Check the box to exclude formula definition columns. |</Note>

CHANGE 19  [## Other Panes]  (line 66 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005903.mdx
TITLE:  Consensus Connector
LANG:   English (en_US)
ID:     000005903
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Consensus is a demo automation platform that helps sales and revenue teams scale by delivering on-demand, personalized video demos, interactive product tours, and automated product simulations. Consensus connector provides bulk data on key platform elements, including users, demo boards, and customer sales. For information, refer to the API documentation [https://app.goconsensus.com/api-documentation/](https://app.goconsensus.com/api-documentation/ "https://app.goconsensus.com/api-documentation/").

  NEW (replace with / insert from CSV):
    Consensus is a demo automation platform that helps sales and revenue teams scale by delivering on-demand, personalized video demos, interactive product tours, and automated product simulations. Consensus connector provides bulk data on key platform elements, including users, demo boards, and customer sales. For information, refer to the API documentation [https://app.goconsensus.com/api-documentation/](https://app.goconsensus.com/api-documentation/).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You can connect to your Consensus account in the Data Center. This topic discusses the fields and menus that are specific to the Consensus connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You can connect to your Consensus account in the Data Center. This topic discusses the fields and menus that are specific to the Consensus connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (lines 15–16 in repo file)

  OLD (remove from repo):
    * The API Key and API Secret associated with your Consensus account
    * Enter the email ID used to log in to your Consensus account

  NEW (replace with / insert from CSV):
    - The API Key and API Secret associated with your Consensus account
    - Enter the email ID used to log in to your Consensus account

CHANGE 4  [## Connecting to Your Consensus Account]  (line 20 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Consensus Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Consensus Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 5  [## Credentials Pane]  (line 24 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Consensus account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Consensus account. The following table describes what is needed for each field:

CHANGE 6  [## Credentials Pane]  (line 26 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> API Key </td><td colspan="1" rowspan="1"> Enter the API Key associated with your Consensus account. The API Key is provided in the <b> Integration </b> &gt; <b> API Credentials </b> section on <a href="http://goconsensus.com/" target="_blank"> goconsensus.com. </a></td></tr><tr><td colspan="1" rowspan="1"> API Secret </td><td colspan="1" rowspan="1"> Enter the API Secret associated with your Consensus account. The API Secret is provided in the <b> Integration </b> &gt; <b> API Credentials </b> section on <a href="http://goconsensus.com/" target="_blank"> goconsensus.com. </a></td></tr><tr><td colspan="1" rowspan="1"> User Email </td><td colspan="1" rowspan="1"> Enter the user email ID used to log in to your Consensus account. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter the API Key associated with your Consensus account. The API Key is provided in the **Integration** > **API Credentials** section on [goconsensus.com.](http://goconsensus.com/) |
    | API Secret | Enter the API Secret associated with your Consensus account. The API Secret is provided in the **Integration** > **API Credentials** section on [goconsensus.com.](http://goconsensus.com/) |
    | User Email | Enter the user email ID used to log in to your Consensus account. |

CHANGE 7  [## Credentials Pane]  (line 28 in repo file)

  OLD (remove from repo):
    Once you have entered valid Consensus credentials, you can use the same account at any time to create a new Consensus DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Consensus credentials, you can use the same account at any time to create a new Consensus DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 8  [## Details Pane]  (line 32 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Consensus report you want to run. The following reports are available: </p><p></p><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Report Name </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"><p> Demos Details </p></td><td colspan="1" rowspan="1"> Returns the Consensus Demo metadata. </td></tr><tr><td colspan="1" rowspan="1"><p> Manage Users </p></td><td colspan="1" rowspan="1"> Returns the Consensus Users metadata. </td></tr><tr><td colspan="1" rowspan="1"><p> Sales by Customer Detailed </p></td><td colspan="1" rowspan="1"> Returns the Consensus Sales Demolytics Report Data. </td></tr><tr><td colspan="1" rowspan="1"><p> Track Demo Boards </p></td><td colspan="1" rowspan="1"> Returns the Consensus Demo Board Report Data. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Date Range </td><td colspan="1" rowspan="1"> Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2020, and must not be a future date. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Consensus report you want to run. The following reports are available:     |  |  | | --- | --- | | **Report Name** | **Description** | | Demos Details | Returns the Consensus Demo metadata. | | Manage Users | Returns the Consensus Users metadata. | | Sales by Customer Detailed | Returns the Consensus Sales Demolytics Report Data. | | Track Demo Boards | Returns the Consensus Demo Board Report Data. | |
    | Date Range | Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2020, and must not be a future date. |

CHANGE 9  [## Other Panes]  (line 36 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005905.mdx
TITLE:  Microsoft Fabric Service Principal Authentication Connector
LANG:   English (en_US)
ID:     000005905
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Microsoft Fabric is an end-to-end analytics and data platform designed for enterprises that require a unified solution. It encompasses data movement, processing, ingestion, transformation, real-time event routing, and report building. It offers a comprehensive suite of services, including Data Engineering, Data Factory, Data Science, Real-Time Analytics, Data Warehouse, and Databases. Use Domo's Microsoft Fabric connector for ingesting data into Domo. To learn more about the Power BI API, visit their page (https://docs.microsoft.com/en-us/rest/api/power-bi/).

  NEW (replace with / insert from CSV):
    Microsoft Fabric is an end-to-end analytics and data platform designed for enterprises that require a unified solution. It encompasses data movement, processing, ingestion, transformation, real-time event routing, and report building. It offers a comprehensive suite of services, including Data Engineering, Data Factory, Data Science, Real-Time Analytics, Data Warehouse, and Databases. Use Domo's Microsoft Fabric connector for ingesting data into Domo. To learn more about the Power BI API, visit their page ([https://docs.microsoft.com/en-us/rest/api/power-bi/)](https://docs.microsoft.com/en-us/rest/api/power-bi/)).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You configure your Microsoft Fabric Service Principal Authentication account in the Data Center. This topic discusses the fields and menus that are specific to the Microsoft Fabric Service Principal Authentication connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360063090013?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You configure your Microsoft Fabric Service Principal Authentication account in the Data Center. This topic discusses the fields and menus that are specific to the Microsoft Fabric Service Principal Authentication connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360063090013).

CHANGE 3  [## Prerequisites]  (lines 15–19 in repo file)

  OLD (remove from repo):
    * An installed Microsoft Azure app.
    * Server Name
    * Port number, database name
    * Azure App Client ID and Client Secret
    * HostNameInCertificate

  NEW (replace with / insert from CSV):
    - An installed Microsoft Azure app.
    - Server Name
    - Port number, database name
    - Azure App Client ID and Client Secret
    - HostNameInCertificate

CHANGE 4  [## Prerequisites]  (line 21 in repo file)

  OLD (remove from repo):
    #### To obtain the Azure App Client ID :

  NEW (replace with / insert from CSV):
    #### **To obtain the Azure App Client ID**:

CHANGE 5  [#### To obtain the Azure App Client ID :]  (lines 23–24 in repo file)

  OLD (remove from repo):
    1. Go to
    [https://portal.azure.com](https://portal.azure.com/ "https://portal.azure.com").

  NEW (replace with / insert from CSV):
    1. Go to [https://portal.azure.com](https://portal.azure.com/).

CHANGE 6  [#### To obtain the Azure App Client ID :]  (lines 30–31 in repo file)

  OLD (remove from repo):
    4. The "application ID" is your
    **Client ID**.

  NEW (replace with / insert from CSV):
    4. The "application ID" is your **Client ID**.

CHANGE 7  [#### To obtain the Azure App Client ID :]  (line 33 in repo file)

  OLD (remove from repo):
    #### To obtain a new Azure App Client secret :

  NEW (replace with / insert from CSV):
    #### **To obtain a new Azure App Client secret**:

CHANGE 8  [#### To obtain a new Azure App Client secret :]  (lines 35–36 in repo file)

  OLD (remove from repo):
    1. Go to
    [https://portal.azure.com](https://portal.azure.com/ "https://portal.azure.com").

  NEW (replace with / insert from CSV):
    1. Go to [https://portal.azure.com](https://portal.azure.com/).

CHANGE 9  [#### To obtain a new Azure App Client secret :]  (lines 44–45 in repo file)

  OLD (remove from repo):
    5. Click
    **+New client secret**.

  NEW (replace with / insert from CSV):
    5. Click **+New client secret**.

CHANGE 10  [#### To obtain a new Azure App Client secret :]  (lines 47–48 in repo file)

  OLD (remove from repo):
    6. Provide the client secret description and the expiry period for your secret. Click
    **Add**.

  NEW (replace with / insert from CSV):
    6. Provide the client secret description and the expiry period for your secret. Click **Add**.

CHANGE 11  [#### To obtain a new Azure App Client secret :]  (line 52 in repo file)

  OLD (remove from repo):
    ### Creating an Azure App for Enterprise OAuth

  NEW (replace with / insert from CSV):
    ### Creating an Azure App for Enterprise OAuth

CHANGE 12  [### Creating an Azure App for Enterprise OAuth]  (lines 56–57 in repo file)

  OLD (remove from repo):
    1. Log into
    [https://portal.azure.com](https://portal.azure.com/ "https://portal.azure.com").

  NEW (replace with / insert from CSV):
    1. Log into [https://portal.azure.com](https://portal.azure.com/).

CHANGE 13  [### Creating an Azure App for Enterprise OAuth]  (lines 59–60 in repo file)

  OLD (remove from repo):
    2. Click on
    **App registrations**.

  NEW (replace with / insert from CSV):
    2. Click on **App registrations**.

CHANGE 14  [### Creating an Azure App for Enterprise OAuth]  (lines 62–63 in repo file)

  OLD (remove from repo):
    3. Click
    **New registration**.

  NEW (replace with / insert from CSV):
    3. Click **New registration**.

CHANGE 15  [### Creating an Azure App for Enterprise OAuth]  (lines 65–66 in repo file)

  OLD (remove from repo):
    4. Enter the application name and select the supported account type as below. Enter the Redirect URI for Web as
    **`https://oauth.domo.com/api/data/v1/oauth/providers/power-bi-writeback-aad/exchange`**

  NEW (replace with / insert from CSV):
    4. Enter the application name and select the supported account type as below. Enter the Redirect URI for Web as **https://oauth.domo.com/api/data/v1/oauth/providers/power-bi-writeback-aad/exchange**

CHANGE 16  [### Creating an Azure App for Enterprise OAuth]  (lines 68–69 in repo file)

  OLD (remove from repo):
    5. Click
    **Register**. Once the application is registered, you can view the Client ID (application ID), client secret, and tenant ID.

  NEW (replace with / insert from CSV):
    5. Click **Register**. Once the application is registered, you can view the Client ID (application ID), client secret, and tenant ID.

CHANGE 17  [### Creating an Azure App for Enterprise OAuth]  (lines 73–74 in repo file)

  OLD (remove from repo):
    7. Click
    **API Permissions > +Add a permission**.

  NEW (replace with / insert from CSV):
    7. Click **API Permissions > +Add a permission**.

CHANGE 18  [### Creating an Azure App for Enterprise OAuth]  (lines 80–81 in repo file)

  OLD (remove from repo):
    10. Click on
    **Grant admin consent**, then click **Yes** to allow the admin consent for your permissions. The added permissions will appear in the **Configured permissions** section with their details.

  NEW (replace with / insert from CSV):
    10. Click on **Grant admin consent**, then click **Yes**to allow the admin consent for your permissions.The added permissions will appear in the **Configured permissions** section with their details.

CHANGE 19  [### Creating an Azure App for Enterprise OAuth]  (lines 83–84 in repo file)

  OLD (remove from repo):
    11. Now, click on
    **Certificates & Secrets**. Certificates can be used as secrets to prove the application’s identity when requesting a token. Also, can be referred to as public keys.

  NEW (replace with / insert from CSV):
    11. Now, click on **Certificates & Secrets**. Certificates can be used as secrets to prove the application’s identity when requesting a token. Also, can be referred to as public keys.

CHANGE 20  [### Creating an Azure App for Enterprise OAuth]  (lines 86–87 in repo file)

  OLD (remove from repo):
    12. Click on
    **+ New Client Secret** button.

  NEW (replace with / insert from CSV):
    12. Click on **+ New Client Secret** button.

CHANGE 21  [### Creating an Azure App for Enterprise OAuth]  (line 89 in repo file)

  OLD (remove from repo):
    13. Specify the client secret description and the expiry period for your client secret, and click **Add**.

  NEW (replace with / insert from CSV):
    13. Specify the client secret description and the expiry period for your client secret, and click **Add**.

CHANGE 22  [### Creating an Azure App for Enterprise OAuth]  (line 91 in repo file)

  OLD (remove from repo):
    14. Copy the **value** and paste it into the **Client Secret** field in the Connector credentials section in Domo.

  NEW (replace with / insert from CSV):
    14. Copy the **value** and paste it into the **Client Secret** field in the Connector credentials section in Domo.

CHANGE 23  [### Creating an Azure App for Enterprise OAuth]  (lines 93–96 in repo file)

  OLD (remove from repo):
    <Note>
      **Note:** Ensure to copy the Value and *not* the Secret ID.
    
    </Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** Ensure to copy the Value and *not* the Secret ID.</Note>

CHANGE 24  [### Enabling the 'Allow public client flows']  (lines 100–101 in repo file)

  OLD (remove from repo):
    1. Go to
    [https://portal.azure.com](https://portal.azure.com/ "https://portal.azure.com").

  NEW (replace with / insert from CSV):
    1. Go to [https://portal.azure.com](https://portal.azure.com/).

CHANGE 25  [### Enabling the 'Allow public client flows']  (lines 107–108 in repo file)

  OLD (remove from repo):
    4. Click on
    **Authentication**.

  NEW (replace with / insert from CSV):
    4. Click on **Authentication**.

CHANGE 26  [### Enabling the 'Allow public client flows']  (lines 110–111 in repo file)

  OLD (remove from repo):
    5. Enable the following mobile and desktop flows by selecting
    **YES**.

  NEW (replace with / insert from CSV):
    5. Enable the following mobile and desktop flows by selecting **YES**.

CHANGE 27  [### Granting Admin consent in App registration]  (lines 115–116 in repo file)

  OLD (remove from repo):
    1. Go to
    [https://portal.azure.com](https://portal.azure.com/ "https://portal.azure.com").

  NEW (replace with / insert from CSV):
    1. Go to [https://portal.azure.com](https://portal.azure.com/).

CHANGE 28  [### Granting Admin consent in App registration]  (lines 124–125 in repo file)

  OLD (remove from repo):
    5. Review the permissions that the application requires. If you agree, select
    **Grant admin consent**.

  NEW (replace with / insert from CSV):
    5. Review the permissions that the application requires. If you agree, select  **Grant admin consent**.

CHANGE 29  [## Configuring the Connection]  (line 129 in repo file)

  OLD (remove from repo):
    This section lists the options in the **Credentials** and **Details** panes on the Microsoft Fabric Service Principal Authentication Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most Connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section lists the options in the **Credentials** and **Details** panes on the Microsoft Fabric Service Principal Authentication Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most Connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 30  [### Credentials Pane]  (line 133 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Power BI account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Power BI account. The following table describes what is needed for each field:

CHANGE 31  [### Credentials Pane]  (line 135 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:198;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Server Name </td><td colspan="1" rowspan="1"> Enter the server name. </td></tr><tr><td colspan="1" rowspan="1"> Port Number </td><td colspan="1" rowspan="1"> Enter the port number. </td></tr><tr><td colspan="1" rowspan="1"> Database </td><td colspan="1" rowspan="1"> Enter the database name. </td></tr><tr><td colspan="1" rowspan="1"> Azure App Client ID </td><td colspan="1" rowspan="1"> Enter the Azure App Client Id. </td></tr><tr><td colspan="1" rowspan="1"> Azure App Client Secret </td><td colspan="1" rowspan="1"> Enter the Azure App Client Secret. </td></tr><tr><td colspan="1" rowspan="1"> HostNameInCertificate </td><td colspan="1" rowspan="1"> Enter the hostNameInCertificate obtained from the show database connection strings. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Server Name | Enter the server name. |
    | Port Number | Enter the port number. |
    | Database | Enter the database name. |
    | Azure App Client ID | Enter the Azure App Client Id. |
    | Azure App Client Secret | Enter the Azure App Client Secret. |
    | HostNameInCertificate | Enter the hostNameInCertificate obtained from the show database connection strings. |

CHANGE 32  [### Credentials Pane]  (line 137 in repo file)

  OLD (remove from repo):
    For more information about obtaining these credentials, see [Prerequisites](#h_04264b0c-3d07-4765-accb-0708e0e6d456 "Prerequisites"), above.

  NEW (replace with / insert from CSV):
    For more information about obtaining these credentials, see [Prerequisites](#h_04264b0c-3d07-4765-accb-0708e0e6d456), above.

CHANGE 33  [### Credentials Pane]  (line 139 in repo file)

  OLD (remove from repo):
    Once you have entered valid Microsoft Fabric Service Principal Authentication credentials, you can use the same account any time to create a new Microsoft Fabric Service Principal Authentication DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Microsoft Fabric Service Principal Authentication credentials, you can use the same account any time to create a new Microsoft Fabric Service Principal Authentication DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 34  [### Details Pane]  (line 145 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:198;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Query Type </td><td colspan="1" rowspan="1"><p> Select the query type from the drop-down list. </p><ul><li> Custom Query </li><li> Query Builder </li></ul><table><tbody><tr><th colspan="1" rowspan="1"><p> QueryType </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr><tr><td colspan="1" rowspan="1"> Custom Query </td><td colspan="1" rowspan="1"> Enter the query to execute. </td></tr><tr><td colspan="1" rowspan="1"> Query Builder </td><td colspan="1" rowspan="1"> Select a table and fields to autogenerate your query. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Query </td><td colspan="1" rowspan="1"> Enter the SQL query to execute. </td></tr><tr><td colspan="1" rowspan="1"> Database Schema </td><td colspan="1" rowspan="1"> Select the schema from the drop-down list. </td></tr><tr><td colspan="1" rowspan="1"> Database Tables </td><td colspan="1" rowspan="1"> Select the database table name from the drop-down list. </td></tr><tr><td colspan="1" rowspan="1"> Table Columns </td><td colspan="1" rowspan="1"> Select the table Columns. </td></tr><tr><td colspan="1" rowspan="1"> Query Helper </td><td colspan="1" rowspan="1"> This query is <b> automatically generated </b> from your field selections. Manual edits here will not update the selections in the Field table above. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Query Type | Select the query type from the drop-down list.   - Custom Query - Query Builder  | QueryType | Description | | --- | --- | | Custom Query | Enter the query to execute. | | Query Builder | Select a table and fields to autogenerate your query. | |
    | Query | Enter the SQL query to execute. |
    | Database Schema | Select the schema from the drop-down list. |
    | Database Tables | Select the database table name from the drop-down list. |
    | Table Columns | Select the table Columns. |
    | Query Helper | This query is **automatically generated** from your field selections. Manual edits here will not update the selections in the Field table above. |

CHANGE 35  [### Other Panes]  (line 149 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005907.mdx
TITLE:  Google Sheets Metadata Connector
LANG:   English (en_US)
ID:     000005907
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    The Google Sheets Metadata Connector retrieves metadata from a specified Google Sheet located within your Google Drive for file management and reporting purposes. To utilize, select the target Google Sheet and then choose the desired metadata fields to generate a report.

  NEW (replace with / insert from CSV):

    The Google Sheets Metadata Connector retrieves metadata from a specified Google Sheet located within your Google Drive for file management and reporting purposes. To use this connector, select the target Google Sheet and then choose the desired metadata fields to generate a report.

CHANGE 2  [## Intro]  (lines 11–27 in repo file)

  ⚠ LARGE CHANGE (34 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    * **Spreadsheet Name**
    * **Spreadsheet ID**
    * **Spreadsheet URL**
    * **Time Zone**
    * **Locale**
    * **Sheet Count**
    * **Sheet Name(s)**
    * **Sheet ID(s)**
    * **Sheet Index(es)**
    * **Column Count**
    * **Row Count**
    * **Column Headers**
    * **Owner Email(s)**
    * **Shared With** (Emails and Access Level)
    * **File Size**
    * **Last Edited Time**
    * **Last Editing User Email**

  NEW (replace with / insert from CSV):
    - **Spreadsheet Name**
    - **Spreadsheet ID**
    - **Spreadsheet URL**
    - **Time Zone**
    - **Locale**
    - **Sheet Count**
    - **Sheet Name(s)**
    - **Sheet ID(s)**
    - **Sheet Index(es)**
    - **Column Count**
    - **Row Count**
    - **Column Headers**
    - **Owner Email(s)**
    - **Shared With** (Emails and Access Level)
    - **File Size**
    - **Last Edited Time**
    - **Last Editing User Email**

CHANGE 3  [## Intro]  (line 29 in repo file)

  OLD (remove from repo):
    You can connect to your Google Sheets Metadata account in the Data Center. This topic discusses the fields and menus that are specific to the Google Sheets Metadata connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You can connect to your Google Sheets Metadata account in the Data Center. This topic discusses the fields and menus that are specific to the Google Sheets Metadata connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 4  [### Creating a Google Cloud Project]  (lines 39–45 in repo file)

  OLD (remove from repo):
    1. Navigate to the **Google Cloud Console** at `https://cloud.google.com/cloud-console`.
    2. In the top right corner of the screen, select **Console**.
    3. If you are accessing Google Cloud Console for the first time, then verify that you're logged into the correct account and that your country of residence is correct. Agree to the terms of service, and then click **AGREE AND CONTINUE**.
    4. In the top left corner of your screen, click the **project name**. (If you're a first-time user, this will display as **"Select a project."**)
    5. In the top right corner of the window that appears, click **NEW PROJECT**.
    6. Name your project as **Domo Sheets Connector**. You may optionally select an organization or choose **No organization,** and click **CREATE.**
    7. After clicking **CREATE,** you will return to the home screen. Go back to the top left and click the project name (or **"Select a project"**) once more.

  NEW (replace with / insert from CSV):
    1. Navigate to the **Google Cloud Console** at `[https://cloud.google.com/cloud-console](https://cloud.google.com/cloud-console)`.
    2. In the top right corner of the screen, select **Console**.
    3. If you are accessing Google Cloud Console for the first time, then verify that you're logged into the correct account and that your country of residence is correct. Agree to the terms of service, and then click **AGREE AND CONTINUE**.
    4. In the top left corner of your screen, click the **project name**. (If you're a first-time user, this will display as **"Select a project."**)
    5. In the top right corner of the window that appears, click **NEW PROJECT**.
    6. Name your project as **Domo Sheets Connector**. You may optionally select an organization or choose **No organization,** and click **CREATE.**
    7. After clicking **CREATE,** you will return to the home screen. Go back to the top left and click the project name (or **"Select a project"**) once more.

CHANGE 5  [### Creating a Google Cloud Project]  (line 47 in repo file)

  OLD (remove from repo):
    9. Scroll down to the **Quick access** menu in the middle of the page and select the **APIs & Services** checkbox.

  NEW (replace with / insert from CSV):
    9. Scroll down to the **Quick access** menu in the middle of the page and select the **APIs & Services** checkbox.

CHANGE 6  [### Setting up OAuth 2.0]  (lines 63–65 in repo file)

  OLD (remove from repo):
    1. From the left navigation menu on the **APIs & Services** page, select **OAuth consent screen.**
    2. For **User Type**, select **External**.
    3. Click **CREATE.** This will take you to a page where you can begin configuring the details of your OAuth consent screen (these settings can be modified later).

  NEW (replace with / insert from CSV):
    1. From the left navigation menu on the **APIs & Services** page, select **OAuth consent screen.**
    2. For **User Type**, select **External**.
    3. Click **CREATE.** This will take you to a page where you can begin configuring the details of your OAuth consent screen (these settings can be modified later).

CHANGE 7  [### Configuring the OAuth 2.0 Consent Screen Details]  (line 76 in repo file)

  OLD (remove from repo):
       * Select **ADD DOMAIN,** and enter `domo.com` in the text box.

  NEW (replace with / insert from CSV):
       - Select **ADD DOMAIN,** and enter `domo.com` in the text box.

CHANGE 8  [### Configuring the OAuth 2.0 Consent Screen Details]  (line 78 in repo file)

  OLD (remove from repo):
    6. Select **SAVE AND CONTINUE.** This action saves your details and advances you to the next configuration step: **Scopes**.

  NEW (replace with / insert from CSV):
    6. Select **SAVE AND CONTINUE.** This action saves your details and advances you to the next configuration step: **Scopes**.

CHANGE 9  [### Configuring OAuth 2.0 Scopes]  (lines 86–91 in repo file)

  OLD (remove from repo):
    1. At the top of the **Scopes** page, select **ADD OR REMOVE SCOPES.**
    2. In the **Enter property name or value** search bar enter the following Google Drive Metadata scope exactly as shown: `https://www.googleapis.com/auth/drive.metadata.readonly`
    3. Click the suggestion that appears under **Values.**
    4. In the next screen, select the checkbox to the left of **Google Drive API.**
    5. Click **UPDATE.**
    6. The window will close. Verify that the following scope is now listed under **Your restricted scopes :** `.../auth/drive.metadata.readonly`

  NEW (replace with / insert from CSV):
    1. At the top of the **Scopes** page, select **ADD OR REMOVE SCOPES.**
    2. In the **Enter property name or value** search bar enter the following Google Drive Metadata scope exactly as shown: `[https://www.googleapis.com/auth/drive.metadata.readonly](https://www.googleapis.com/auth/drive.metadata.readonly)`
    3. Click the suggestion that appears under **Values.**
    4. In the next screen, select the checkbox to the left of **Google Drive API.**
    5. Click **UPDATE.**
    6. The window will close. Verify that the following scope is now listed under **Your restricted scopes :** `.../auth/drive.metadata.readonly`

CHANGE 10  [### Configuring OAuth 2.0 Scopes]  (lines 95–98 in repo file)

  OLD (remove from repo):
    1. Repeat the process by selecting **ADD OR REMOVE SCOPES** again.
    2. In the search bar, type the following **Google Sheets** scope exactly as shown: `https://www.googleapis.com/auth/spreadsheets.readonly`
    3. Click the suggestion that appears, select the checkbox next to **Google Sheets API,** and click **UPDATE.**
    4. Verify that the following scope is now listed under **Your sensitive scopes**: `...auth/sheets.metadata.readonly`

  NEW (replace with / insert from CSV):
    1. Repeat the process by selecting **ADD OR REMOVE SCOPES** again.
    2. In the search bar, type the following **Google Sheets** scope exactly as shown: `[https://www.googleapis.com/auth/spreadsheets.readonly](https://www.googleapis.com/auth/spreadsheets.readonly)`
    3. Click the suggestion that appears, select the checkbox next to **Google Sheets API,** and click **UPDATE.**
    4. Verify that the following scope is now listed under **Your sensitive scopes**: `...auth/sheets.metadata.readonly`

CHANGE 11  [### Configuring OAuth 2.0 Scopes]  (line 100 in repo file)

  OLD (remove from repo):
    ***Note:*** *It is essential that both scopes are entered **exactly** as specified above to match the connector's requirements.*

  NEW (replace with / insert from CSV):
    *
    <Note>**Note:** * *It is essential that both scopes are entered **exactly** as specified above to match the connector's requirements.*</Note>

CHANGE 12  [### Configuring OAuth 2.0 Scopes]  (line 104 in repo file)

  OLD (remove from repo):
    Once you have confirmed that both scopes are listed, click **SAVE AND CONTINUE.**

  NEW (replace with / insert from CSV):
    Once you have confirmed that both scopes are listed, click **SAVE AND CONTINUE.**

CHANGE 13  [### Configuring OAuth 2.0 Test Users]  (line 110 in repo file)

  OLD (remove from repo):
    1. Select **ADD USERS.**

  NEW (replace with / insert from CSV):
    1. Select **ADD USERS.**

CHANGE 14  [### Configuring OAuth 2.0 Test Users]  (line 112 in repo file)

  OLD (remove from repo):
    3. Click **ADD** (Users can be added or removed in later edits).

  NEW (replace with / insert from CSV):
    3. Click **ADD** (Users can be added or removed in later edits).

CHANGE 15  [### Configuring OAuth 2.0 Test Users]  (line 114 in repo file)

  OLD (remove from repo):
    ***Note:*** Even if an email address was used in a previous step (like the User Support Email), you must still add it to this list to grant connection access.

  NEW (replace with / insert from CSV):
    *
    <Note>**Note:** * Even if an email address was used in a previous step (like the User Support Email), you must still add it to this list to grant connection access.</Note>

CHANGE 16  [### Configuring OAuth 2.0 Test Users]  (line 116 in repo file)

  OLD (remove from repo):
    4. Click **SAVE AND CONTINUE** once you've added all necessary users.

  NEW (replace with / insert from CSV):
    4. Click **SAVE AND CONTINUE** once you've added all necessary users.

CHANGE 17  [### Configuring OAuth 2.0 Test Users]  (line 118 in repo file)

  OLD (remove from repo):
    ***Important:*** Adding users here only allows them to use the created credentials for the OAuth 2.0 process within the connector. It does not grant them access to your Google Cloud Console project or your Google Drive content.

  NEW (replace with / insert from CSV):
    *
    <Warning>**Important:** * Adding users here only allows them to use the created credentials for the OAuth 2.0 process within the connector. It does not grant them access to your Google Cloud Console project or your Google Drive content.</Warning>

CHANGE 18  [### Review and Complete OAuth 2.0 Setup]  (line 125 in repo file)

  OLD (remove from repo):
    2. Once confirmed, click **BACK TO DASHBOARD.**

  NEW (replace with / insert from CSV):
    2. Once confirmed, click **BACK TO DASHBOARD.**

CHANGE 19  [### Creating the OAuth Client ID]  (lines 132–137 in repo file)

  OLD (remove from repo):
    2. On the top of the Credentials Dashboard, click **CREATE CREDENTIALS** and select **OAuth client ID** from the dropdown menu.
    3. On the next screen, from the top of page, select **Web Application** from the **Application Type** dropdown menu.
    4. Enter a **Client name** (we recommend **Domo Sheets Connector Client**).
    5. Scroll down to the **Authorized redirect URIs** section and click **ADD URI.**
    6. Enter the following exact URI: `https://oauth.domo.com/api/data/v2/oauth/providers/google-drive-metadata-98e4dc30-1712-4b80-85ec-ed6cf3cb1e6b/exchange`
    7. Click **CREATE** at the bottom of the page.

  NEW (replace with / insert from CSV):
    2. On the top of the Credentials Dashboard, click **CREATE CREDENTIALS** and select **OAuth client ID** from the dropdown menu.
    3. On the next screen, from the top of page, select **Web Application** from the **Application Type** dropdown menu.
    4. Enter a **Client name** (we recommend **Domo Sheets Connector Client**).
    5. Scroll down to the **Authorized redirect URIs** section and click **ADD URI.**
    6. Enter the following exact URI: `[https://oauth.domo.com/api/data/v2/oauth/providers/google-drive-metadata-98e4dc30-1712-4b80-85ec-ed6cf3cb1e6b/exchange](https://oauth.domo.com/api/data/v2/oauth/providers/google-drive-metadata-98e4dc30-1712-4b80-85ec-ed6cf3cb1e6b/exchange)`
    7. Click **CREATE** at the bottom of the page.

CHANGE 20  [### Creating the OAuth Client ID]  (lines 143–144 in repo file)

  OLD (remove from repo):
    * **Securely save** both the **Client ID** and **Client Secret**, as you will need them to set up the connector in Domo.
    * You can also view these credentials later by returning to the **Credentials Dashboard** or downloading them in **JSON format**.

  NEW (replace with / insert from CSV):
    - **Securely save** both the **Client ID** and **Client Secret**, as you will need them to set up the connector in Domo.
    - You can also view these credentials later by returning to the **Credentials Dashboard** or downloading them in **JSON format**.

CHANGE 21  [## Connecting to Your Google Sheets Metadata Account]  (line 148 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Google Sheets Metadata Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Google Sheets Metadata Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 22  [## Credentials Pane]  (line 152 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Google Sheets Metadata account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Google Sheets Metadata account. The following table describes what is needed for each field:

CHANGE 23  [## Credentials Pane]  (line 154 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:197;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Client ID </td><td colspan="1" rowspan="1"> Enter your application Client Id. To create the Client ID, refer to the Creating the OAuth Client ID section above. </td></tr><tr><td colspan="1" rowspan="1"> Client Secret </td><td colspan="1" rowspan="1"> Enter your application Client Secret. To create the Client Secret, refer to the Creating the OAuth Client ID section above. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter your application Client Id. To create the Client ID, refer to the Creating the OAuth Client ID section above. |
    | Client Secret | Enter your application Client Secret. To create the Client Secret, refer to the Creating the OAuth Client ID section above. |

CHANGE 24  [## Credentials Pane]  (line 156 in repo file)

  OLD (remove from repo):
    Once you have entered valid Google Sheets Metadata credentials, you can use the same account at any time to create a new Google Sheets Metadata DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Google Sheets Metadata credentials, you can use the same account at any time to create a new Google Sheets Metadata DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 25  [## Details Pane]  (line 160 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:197;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> The Google Sheets Metadata has a single report to run. </p><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Report Name </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"><p> Sheets </p></td><td colspan="1" rowspan="1"> Fetches the metadata from a selected Google Sheet from within your Google Drive. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Sheet Id </td><td colspan="1" rowspan="1"> Select the Google Sheet for which you would like to receive the data. </td></tr><tr><td colspan="1" rowspan="1"> Metadata </td><td colspan="1" rowspan="1"> Select the metadata you would like to include in your report. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | The Google Sheets Metadata has a single report to run.   |  |  | | --- | --- | | **Report Name** | **Description** | | Sheets | Fetches the metadata from a selected Google Sheet from within your Google Drive. | |
    | Sheet Id | Select the Google Sheet for which you would like to receive the data. |
    | Metadata | Select the metadata you would like to include in your report. |

CHANGE 26  [## Other Panes]  (line 164 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005910.mdx
TITLE:  Aprimo Connector
LANG:   English (en_US)
ID:     000005910
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Aprimo helps you manage your marketing and content operations. Use Aprimo for digital asset management (**DAM**), workflow automation, planning, budgeting, and team collaboration. Industry analysts, like Gartner, recognize Aprimo as a leader in this space. For more information, refer to the [API documentation](https://developers.aprimo.com/docs/OAuth2#oauth-20-flow---client-credentials "https://developers.aprimo.com/docs/OAuth2#oauth-20-flow---client-credentials").

  NEW (replace with / insert from CSV):
    Aprimo helps you manage your marketing and content operations. Use Aprimo for digital asset management (**DAM**), workflow automation, planning, budgeting, and team collaboration. Industry analysts, like Gartner, recognize Aprimo as a leader in this space. For more information, refer to the [API documentation](https://developers.aprimo.com/docs/OAuth2#oauth-20-flow---client-credentials).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You can connect to your Aprimo account in the Data Center. This topic discusses the fields and menus that are specific to the Aprimo connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You can connect to your Aprimo account in the Data Center. This topic discusses the fields and menus that are specific to the Aprimo connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (lines 15–16 in repo file)

  OLD (remove from repo):
    * Client ID and Client Secret
    * Domain name

  NEW (replace with / insert from CSV):
    - Client ID and Client Secret
    - Domain name

CHANGE 4  [### Obtaining an Access Token]  (lines 24–27 in repo file)

  OLD (remove from repo):
    * **Client Name:** A descriptive identifier for the registration.
    * **Description:** The stated purpose for the registration's existence and use.
    * **Client Secret:** A permanent, confidential key generated during registration. Ensure to store this client secret securely **.**
    * **Redirect URL:** Mandatory if utilizing the Authorization Code flow.

  NEW (replace with / insert from CSV):
    - **Client Name:** A descriptive identifier for the registration.
    - **Description:** The stated purpose for the registration's existence and use.
    - **Client Secret:** A permanent, confidential key generated during registration. Ensure to store this client secret securely**.**
    - **Redirect URL:** Mandatory if utilizing the Authorization Code flow.

CHANGE 5  [### OAuth Flow Type Selection]  (line 33 in repo file)

  OLD (remove from repo):
    <table><thead><tr><td colspan="1" rowspan="1"> OAuth Flow Type </td><td colspan="1" rowspan="1"> Recommended Use Case </td></tr></thead><tbody><tr><td colspan="1" rowspan="1"><b> Authorization Code (PKCE) </b></td><td colspan="1" rowspan="1"> Recommended for interactive auth flows. </td></tr><tr><td colspan="1" rowspan="1"><b> Client Credentials </b></td><td colspan="1" rowspan="1"> Use for service-based API calls. </td></tr><tr><td colspan="1" rowspan="1"><b> Resource Owner Password </b></td><td colspan="1" rowspan="1"> Scenarios requiring multiple user authentications without a browser-based login interface. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | OAuth Flow Type | Recommended Use Case |
    | --- | --- |
    | **Authorization Code (PKCE)** | Recommended for interactive auth flows. |
    | **Client Credentials** | Use for service-based API calls. |
    | **Resource Owner Password** | Scenarios requiring multiple user authentications without a browser-based login interface. |

CHANGE 6  [### Register the Client Application]  (line 47 in repo file)

  OLD (remove from repo):
    **Note:** Allow up to 15 minutes for registrations to update.

  NEW (replace with / insert from CSV):
    <Note>**Note:** Allow up to 15 minutes for registrations to update.</Note>

CHANGE 7  [## Connecting to Your Aprimo Account]  (line 51 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Aprimo Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Aprimo Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 8  [## Credentials Pane]  (line 55 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Aprimo account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Aprimo account. The following table describes what is needed for each field:

CHANGE 9  [## Credentials Pane]  (line 57 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Client ID </td><td colspan="1" rowspan="1"> Enter the customer's Client ID. To obtain the client ID, refer to the <a href="https://developers.aprimo.com/docs/OAuth2#oauth-20-flow---client-credentials" target="_blank"> API documentation </a> or refer to the Prerequisites section. </td></tr><tr><td colspan="1" rowspan="1"> Client Secret </td><td colspan="1" rowspan="1"> Enter the customer's Client Secret. To obtain the client Secret, refer to the <a href="https://developers.aprimo.com/docs/OAuth2#oauth-20-flow---client-credentials" target="_blank"> API documentation </a> or refer to the Prerequisites section. </td></tr><tr><td colspan="1" rowspan="1"> Domain </td><td colspan="1" rowspan="1"> Enter the domain name associated with the Aprimo account. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter the customer's Client ID. To obtain the client ID, refer to the [API documentation](https://developers.aprimo.com/docs/OAuth2#oauth-20-flow---client-credentials)or refer to the Prerequisites section. |
    | Client Secret | Enter the customer's Client Secret. To obtain the client Secret, refer to the [API documentation](https://developers.aprimo.com/docs/OAuth2#oauth-20-flow---client-credentials) or refer to the Prerequisites section. |
    | Domain | Enter the domain name associated with the Aprimo account. |

CHANGE 10  [## Credentials Pane]  (line 59 in repo file)

  OLD (remove from repo):
    Once you have entered valid Aprimo credentials, you can use the same account at any time to create a new Aprimo DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Aprimo credentials, you can use the same account at any time to create a new Aprimo DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 11  [## Details Pane]  (line 63 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Aprimo report you want to run. The following reports are available: </p><p></p><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Report Name </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"><p> Analytics Report </p></td><td colspan="1" rowspan="1"> Retrieves the data about your analytics report. </td></tr><tr><td colspan="1" rowspan="1"><p> Get All Classifications </p></td><td colspan="1" rowspan="1"> Retrieves the data about your classifications. </td></tr><tr><td colspan="1" rowspan="1"><p> Get All Groups </p></td><td colspan="1" rowspan="1"> Retrieves the data about your groups. </td></tr><tr><td colspan="1" rowspan="1"><p> Get Record Details </p></td><td colspan="1" rowspan="1"> Retrieves the data about your record details. </td></tr><tr><td colspan="1" rowspan="1"><p> Get All Records </p></td><td colspan="1" rowspan="1"> Retrieves the data about your records. </td></tr><tr><td colspan="1" rowspan="1"><p> Get User Details </p></td><td colspan="1" rowspan="1"> Retrieves the data about your user details. </td></tr><tr><td colspan="1" rowspan="1"><p> Reporting API </p></td><td colspan="1" rowspan="1"> Retrieves the data about your reporting API. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Record Id Selection Criteria </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Get Record Details </b> report. </p><p> You fetch the record details of all the record IDs or select specific record IDs. </p><ul><li> All Record IDs </li><li> Select Record IDs </li></ul></td></tr><tr><td colspan="1" rowspan="1"> Record IDs </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Select Record IDs </b> option from the <b> Record Id Selection Criteria </b> drop-down, and the report should be <b> Get Record Details </b>. </p><p> Select the record IDs to be included in the report. </p></td></tr><tr><td colspan="1" rowspan="1"> User Id Selection Criteria </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Get User Details </b> report. </p><p> Use this option to retrieve the user details of all user IDs or specific user IDs. </p><ul><li> All User IDs </li><li> Select User IDs </li></ul></td></tr><tr><td colspan="1" rowspan="1"> User IDs </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Select User IDs </b> option from the <b> User </b><b> Id Selection Criteria </b> drop-down, and the report should be <b> Get User Details </b>. </p><p> Select the user IDs to fetch the user details. </p></td></tr><tr><td colspan="1" rowspan="1"> Data Domain </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Analytics Report </b>. </p><p> Select the analytics data domain to determine the measures that should populate. </p></td></tr><tr><td colspan="1" rowspan="1"> Measures </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Analytics Report </b> and select any Data Domain. </p><p> Select the measures (metrics) to include in the analytics report. </p></td></tr><tr><td colspan="1" rowspan="1"> Dimensions </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Analytics Report </b> and select any Data Domain. </p><p> Select the dimensions (grouping fields) to include in the analytics report. </p></td></tr><tr><td colspan="1" rowspan="1"> Report ID </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Reporting API </b> report. </p><p> Enter the unique identifier for the report execution. </p></td></tr><tr><td colspan="1" rowspan="1"> Date Range </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Reporting API </b> report. </p><p> Choose whether the report should cover a <b> Single Date </b> or a <b> Date Range </b>. </p><ul><li><b> Single Date: </b> Select whether the report data is for a specific date or for a relative number of days back from today. <ul><li><b> Relative Date: </b> Enter the number of days back from today. <ul><li><b> Days Back: </b> Enter the number of days relative to today for which you want to receive the data. Use 0 for today, 1 for yesterday, 7 for 7days ago, and so on. </li></ul></li><li><b> Specific Date: </b> Use the date picker to select a precise date. <ul><li><b> Date: </b> Select the precise date. (The date cannot be before January 1, 2020, and must not be a future date.) </li></ul></li></ul></li><li><p><b> Date Range: </b> Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2020, and must not be a future date. </p><ul><li><p><b> Start Date: </b> Select the start date to fetch the data. </p><ul><li><b> Relative Date: </b> Enter the number of days back from today. <ul><li><b> Days Back: </b> Enter the number of days relative to today for the desired start date. For example, enter 1 for yesterday, 7 for 1 week ago, or 30 for 1 month ago. </li></ul></li><li><b> Specific Date: </b> Use the date picker to select a precise start date. <ul><li><b> Date: </b> Select the precise start date. (The date cannot be before January 1, 2020, and must not be a future date.) </li></ul></li></ul></li><li><p><b> End Date: </b> Select the end date to fetch the data. </p><ul><li><b> Relative Date: </b> Enter the number of days back from today. <ul><li><b> Days Back: </b> Enter the number of days relative to today for the desired end date. For example, enter 0 for today, or 1 for an end date of yesterday. </li></ul></li><li><b> Specific Date: </b> Use the date picker to select a precise end date. <ul><li><b> Date: </b> Select the precise end date. (The date cannot be before January 1, 2020, and must not be a future date.) </li></ul></li></ul></li></ul></li><li><b> Time Period: </b> Select the time period for data retrieval. </li></ul><p></p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Aprimo report you want to run. The following reports are available:     |  |  | | --- | --- | | **Report Name** | **Description** | | Analytics Report | Retrieves the data about your analytics report. | | Get All Classifications | Retrieves the data about your classifications. | | Get All Groups | Retrieves the data about your groups. | | Get Record Details | Retrieves the data about your record details. | | Get All Records | Retrieves the data about your records. | | Get User Details | Retrieves the data about your user details. | | Reporting API | Retrieves the data about your reporting API. | |
    | Record Id Selection Criteria | This field appears when you select the **Get Record Details** report.  You fetch the record details of all the record IDs or select specific record IDs.   - All Record IDs - Select Record IDs |
    | Record IDs | This field appears when you select the **Select Record IDs** option from the **Record Id Selection Criteria** drop-down, and the report should be **Get Record Details**.  Select the record IDs to be included in the report. |
    | User Id Selection Criteria | This field appears when you select the **Get User Details** report.  Use this option to retrieve the user details of all user IDs or specific user IDs.   - All User IDs - Select User IDs |
    | User IDs | This field appears when you select the **Select User IDs** option from the **User** **Id Selection Criteria** drop-down, and the report should be **Get User Details**.  Select the user IDs to fetch the user details. |
    | Data Domain | This field appears when you select the **Analytics Report**.  Select the analytics data domain to determine the measures that should populate. |
    | Measures | This field appears when you select the **Analytics Report** and select any Data Domain.  Select the measures (metrics) to include in the analytics report. |
    | Dimensions | This field appears when you select the **Analytics Report** and select any Data Domain.  Select the dimensions (grouping fields) to include in the analytics report. |
    | Report ID | This field appears when you select the **Reporting API** report.  Enter the unique identifier for the report execution. |
    | Date Range | This field appears when you select the **Reporting API** report.  Choose whether the report should cover a **Single Date** or a **Date Range**.   - **Single Date:** Select whether the report data is for a specific date or for a relative number of days back from today.   - **Relative Date:** Enter the number of days back from today.     - **Days Back:** Enter the number of days relative to today for which you want to receive the data. Use 0 for today, 1 for yesterday, 7 for 7days ago, and so on.   - **Specific Date:** Use the date picker to select a precise date.     - **Date:** Select the precise date. (The date cannot be before January 1, 2020, and must not be a future date.) - **Date Range:** Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2020, and must not be a future date.    - **Start Date:** Select the start date to fetch the data.      - **Relative Date:** Enter the number of days back from today.       - **Days Back:** Enter the number of days relative to today for the desired start date. For example, enter 1 for yesterday, 7 for 1 week ago, or 30 for 1 month ago.     - **Specific Date:** Use the date picker to select a precise start date.       - **Date:** Select the precise start date. (The date cannot be before January 1, 2020, and must not be a future date.)   - **End Date:**Select the end date to fetch the data.      - **Relative Date:** Enter the number of days back from today.       - **Days Back:** Enter the number of days relative to today for the desired end date.  For example, enter 0 for today, or 1 for an end date of yesterday.     - **Specific Date:** Use the date picker to select a precise end date.       - **Date:** Select the precise end date. (The date cannot be before January 1, 2020, and must not be a future date.) - **Time Period:** Select the time period for data retrieval. |

CHANGE 12  [## Other Panes]  (line 67 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005911.mdx
TITLE:  Vibe Connector
LANG:   English (en_US)
ID:     000005911
==============================================================================

CHANGE 1  [## Connecting to Your Vibe Account]  (line 40 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Vibe Connector page. The components of the other panes on this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Vibe Connector page. The components of the other panes on this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 2  [## Credentials Pane]  (line 44 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Vibe account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Vibe account. The following table describes what is needed for each field:

CHANGE 3  [## Credentials Pane]  (line 46 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> API Key </td><td colspan="1" rowspan="1"> Enter the API Key. To generate the API Key, refer to the Prerequisites section or <a href="https://help.vibe.co/en/articles/8943325-vibe-api-reporting#h_415d822295" target="_blank"> API documentation </a>. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter the API Key. To generate the API Key, refer to the Prerequisites section or [API documentation](https://help.vibe.co/en/articles/8943325-vibe-api-reporting#h_415d822295). |

CHANGE 4  [## Credentials Pane]  (line 48 in repo file)

  OLD (remove from repo):
    Once you have entered valid Vibe credentials, you can use the same account at any time to create a new Vibe DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Vibe credentials, you can use the same account at any time to create a new Vibe DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 5  [## Details Pane]  (line 52 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Vibe report you want to run. The following reports are available: </p><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Report Name </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Details Report </p></td><td colspan="1" rowspan="1"><p> Returns a list of campaigns and strategies for an advertiser. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Create and Run Report </p></td><td colspan="1" rowspan="1"><p> Define the report dimensions and metrics, run the report, and then download the result. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Purchases Details Report </p></td><td colspan="1" rowspan="1"> Returns detailed purchase information, including timestamps, values, and associated campaign data. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Advertiser Ids Selection Criteria </p></td><td colspan="1" rowspan="1"><p> Select how you want to retrieve advertisers. </p></td></tr><tr><td colspan="1" rowspan="1"> Advertiser IDs </td><td colspan="1" rowspan="1"><p> This field appears when you select <b> Create and Run Report </b> or <b> Purchases Details Report </b>. </p><p> Select the advertisers for the report. </p></td></tr><tr><td colspan="1" rowspan="1"> Metrics </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Create and Run Report </b>. </p><p> Select the metrics for the report. </p></td></tr><tr><td colspan="1" rowspan="1"> Dimensions </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Create and Run Report </b>. </p><p> Select the dimensions for the report. </p></td></tr><tr><td colspan="1" rowspan="1"> Segment Dimensions </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Create and Run Report </b>. </p><p> Select the segment-related dimension for the report. </p></td></tr><tr><td colspan="1" rowspan="1"> Attribution Window </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Create and Run Report </b>. </p><p> Select the desired attribution window duration for web-pixel metrics. This will affect web-pixel metrics. <b> 7 days </b> is selected by default. </p></td></tr><tr><td colspan="1" rowspan="1"> Granularity </td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Create and Run Report </b>. </p><p> Select the desired time aggregation granularity. <b> Day </b> is selected by default. </p></td></tr><tr><td colspan="1" rowspan="1"> App IDs to Filter </td><td colspan="1" rowspan="1"><p> This field appears when you select both Create and Run Report and Purchases Details Report. </p><p> Select the app IDs as a filter for the report. </p></td></tr><tr><td colspan="1" rowspan="1"> Campaign IDs to Filter </td><td colspan="1" rowspan="1"><p> Select the campaign IDs as a filter for the report. </p></td></tr><tr><td colspan="1" rowspan="1"> Strategy IDs to Filter </td><td colspan="1" rowspan="1"><p> Select the strategy IDs as a filter for the report. </p></td></tr><tr><td colspan="1" rowspan="1"> Date Range </td><td colspan="1" rowspan="1"><p> This field appears when you select <b> Create and Run Report </b> or <b> Purchases Details Report </b>. </p><p> Choose whether the report should cover a <b> Single Date </b> or a <b> Date Range </b>. </p><ul><li><b> Single Date: </b> Select whether the report data is for a specific date or for a relative number of days back from today. <ul><li><b> Relative Date: </b> Enter the number of days back from today. <ul><li><b> Days Back: </b> Enter the number of days relative to today for which you want to receive the data. Use 0 for today, 1 for yesterday, 7 for 7days ago, and so on. </li></ul></li><li><b> Specific Date: </b> Use the date picker to select a precise date. <ul><li><b> Date: </b> Select the precise date. (The date cannot be before January 1, 2020, and must not be a future date.) </li></ul></li></ul></li><li><p><b> Date Range: </b> Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2020, and must not be a future date. </p><ul><li><p><b> Start Date: </b> Select the start date to fetch the data. </p><ul><li><b> Relative Date: </b> Enter the number of days back from today. <ul><li><b> Days Back: </b> Enter the number of days relative to today for the desired start date. For example, enter 1 for yesterday, 7 for 1 week ago, or 30 for 1 month ago. </li></ul></li><li><b> Specific Date: </b> Use the date picker to select a precise start date. <ul><li><b> Date: </b> Select the precise start date. (The date cannot be before January 1, 2020, and must not be a future date.) </li></ul></li></ul></li><li><p><b> End Date: </b> Select the end date to fetch the data. </p><ul><li><b> Relative Date: </b> Enter the number of days back from today. <ul><li><b> Days Back: </b> Enter the number of days relative to today for the desired end date. For example, enter 0 for today, or 1 for an end date of yesterday. </li></ul></li><li><b> Specific Date: </b> Use the date picker to select a precise end date. <ul><li><b> Date: </b> Select the precise end date. (The date cannot be before January 1, 2020, and must not be a future date.) </li></ul></li></ul></li></ul></li><li><b> Time Period: </b> Select the time period for data retrieval. </li></ul></td></tr><tr><td colspan="1" rowspan="1"> Timezones </td><td colspan="1" rowspan="1"><p> Select the timezone for the returned data. <b> UTC </b> is selected by default. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Vibe report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Campaign Details Report | Returns a list of campaigns and strategies for an advertiser. | | Create and Run Report | Define the report dimensions and metrics, run the report, and then download the result. | | Purchases Details Report | Returns detailed purchase information, including timestamps, values, and associated campaign data. | |
    | Advertiser Ids Selection Criteria | Select how you want to retrieve advertisers. |
    | Advertiser IDs | This field appears when you select **Create and Run Report** or **Purchases Details Report**.  Select the advertisers for the report. |
    | Metrics | This field appears when you select the **Create and Run Report**.  Select the metrics for the report. |
    | Dimensions | This field appears when you select the **Create and Run Report**.  Select the dimensions for the report. |
    | Segment Dimensions | This field appears when you select the **Create and Run Report**.  Select the segment-related dimension for the report. |
    | Attribution Window | This field appears when you select the **Create and Run Report**.  Select the desired attribution window duration for web-pixel metrics. This will affect web-pixel metrics. **7 days** is selected by default. |
    | Granularity | This field appears when you select the **Create and Run Report**.  Select the desired time aggregation granularity. **Day** is selected by default. |
    | App IDs to Filter | This field appears when you select both Create and Run Report and Purchases Details Report.  Select the app IDs as a filter for the report. |
    | Campaign IDs to Filter | Select the campaign IDs as a filter for the report. |
    | Strategy IDs to Filter | Select the strategy IDs as a filter for the report. |
    | Date Range | This field appears when you select **Create and Run Report** or **Purchases Details Report**.  Choose whether the report should cover a **Single Date** or a **Date Range**.   - **Single Date:** Select whether the report data is for a specific date or for a relative number of days back from today.   - **Relative Date:** Enter the number of days back from today.     - **Days Back:** Enter the number of days relative to today for which you want to receive the data. Use 0 for today, 1 for yesterday, 7 for 7days ago, and so on.   - **Specific Date:** Use the date picker to select a precise date.     - **Date:**Select the precise date. (The date cannot be before January 1, 2020, and must not be a future date.) - **Date Range:**Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2020, and must not be a future date.    - **Start Date:**Select the start date to fetch the data.      - **Relative Date:** Enter the number of days back from today.       - **Days Back:** Enter the number of days relative to today for the desired start date. For example, enter 1 for yesterday, 7 for 1 week ago, or 30 for 1 month ago.     - **Specific Date:** Use the date picker to select a precise start date.       - **Date:**Select the precise start date. (The date cannot be before January 1, 2020, and must not be a future date.)   - **End Date:**Select the end date to fetch the data.      - **Relative Date:** Enter the number of days back from today.       - **Days Back:** Enter the number of days relative to today for the desired end date.  For example, enter 0 for today, or 1 for an end date of yesterday.     - **Specific Date:** Use the date picker to select a precise end date.       - **Date:**Select the precise end date. (The date cannot be before January 1, 2020, and must not be a future date.) - **Time Period:** Select the time period for data retrieval. |
    | Timezones | Select the timezone for the returned data. **UTC**  is selected by default. |

CHANGE 6  [## Other Panes]  (line 56 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005917.mdx
TITLE:  Sage Intacct Rest Connector
LANG:   English (en_US)
ID:     000005917
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Intacct provides a cloud-based accounting solution for independent financial management. By leveraging Domo, stakeholders can synthesize and visualize Intacct financial data with information from disparate systems on a real-time dashboard. This integration minimizes the need for manual report generation, facilitating comprehensive, enterprise-wide insight. For more information, refer to the [API documentation](https://developer.sage.com/intacct/docs/1/sage-intacct-rest-api/get-started "API documentation").

  NEW (replace with / insert from CSV):
    Intacct provides a cloud-based accounting solution for independent financial management. By leveraging Domo, stakeholders can synthesize and visualize Intacct financial data with information from disparate systems on a real-time dashboard. This integration minimizes the need for manual report generation, facilitating comprehensive, enterprise-wide insight. For more information, refer to the [API documentation](https://developer.sage.com/intacct/docs/1/sage-intacct-rest-api/get-started).

CHANGE 2  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    You can connect to your Sage Intacct Rest account in the Data Center. This topic discusses the fields and menus that are specific to the Sage Intacct Rest connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You can connect to your Sage Intacct Rest account in the Data Center. This topic discusses the fields and menus that are specific to the Sage Intacct Rest connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (lines 17–18 in repo file)

  OLD (remove from repo):
    * Client ID
    * Client Secret

  NEW (replace with / insert from CSV):
    - Client ID
    - Client Secret

CHANGE 4  [### Sage Intacct API Key Generation]  (line 22 in repo file)

  OLD (remove from repo):
    This section outlines the procedure for obtaining the necessary credentials (Sender ID, Sender Password, and Company ID) and generating **OAuth Client Keys** (Client ID and Client Secret) required for third-party application integration via the Sage Intacct Web Services API.

  NEW (replace with / insert from CSV):
    This section outlines the procedure for obtaining the necessary credentials (Sender ID, Sender Password, and Company ID) and generating **OAuth Client Keys** (Client ID and Client Secret) required for third-party application integration via the Sage Intacct Web Services API.

CHANGE 5  [### Sage Intacct API Key Generation]  (lines 26–28 in repo file)

  OLD (remove from repo):
    1. Locate Sender ID and Company ID
    
       <table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Credential </b></td><td colspan="1" rowspan="1"><b> Navigation Path </b></td><td colspan="1" rowspan="1"><b> Action </b></td></tr><tr><td colspan="1" rowspan="1"> Sender ID </td><td colspan="1" rowspan="1"> Log in to Intacct &gt; Company&gt; Setup &gt; Configuration &gt; Company &gt; Security &gt; Web Services Authorizations. </td><td colspan="1" rowspan="1"> Identify and copy the Sender ID. </td></tr><tr><td colspan="1" rowspan="1"> Company ID </td><td colspan="1" rowspan="1"> Company &gt; Setup &gt; Configuration &gt; Company &gt; General Information </td><td colspan="1" rowspan="1"> Identify and copy the Company ID. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    1. Locate Sender ID and Company ID

CHANGE 6  [### Sage Intacct API Key Generation]  (lines 30–32 in repo file)

  OLD (remove from repo):
    2. Retrieve Sender Password
    
       The Sender Password is delivered through email upon issuance of the Developer License. If the password is not available, contact Sage Support immediately to initiate a password reset request.

  NEW (replace with / insert from CSV):
       |  |  |  |
       | --- | --- | --- |
       | **Credential** | **Navigation Path** | **Action** |
       | Sender ID | Log in to Intacct > Company> Setup > Configuration > Company > Security > Web Services Authorizations. | Identify and copy the Sender ID. |
       | Company ID | Company > Setup > Configuration > Company > General Information | Identify and copy the Company ID. |
    2. Retrieve Sender Password
       The Sender Password is delivered through email upon issuance of the Developer License. If the password is not available, contact Sage Support immediately to initiate a password reset request.

CHANGE 7  [### Sage Intacct API Key Generation]  (lines 37–43 in repo file)

  OLD (remove from repo):
    1. Create a Developer Workspace
       1. Navigate to the Sage Intacct Developer Portal: **`https://developer.sage.com/intacct/`**.
       2. Click **View workspaces**.
       3. Log in or create a new Sage developer account.
       4. Click **Add workspace**.
       5. Provide a name and contact email, then click **Create**.
    2. Create a Client Application

  NEW (replace with / insert from CSV):
    1. 1. Create a Developer Workspace
          1. Navigate to the Sage Intacct Developer Portal: **`[https://developer.sage.com/intacct/](https://developer.sage.com/intacct/)`**.
          2. Click **View workspaces**.
          3. Log in or create a new Sage developer account.
          4. Click **Add workspace**.
          5. Provide a name and contact email, then click **Create**.
       2. Create a Client Application

CHANGE 8  [### Sage Intacct API Key Generation]  (lines 45–49 in repo file)

  OLD (remove from repo):
       1. Within the newly created workspace, navigate to **Apps**.
       2. Click **Add application**.
       3. Complete the required fields: **Application Name**, **Homepage URL**, and **Contact Email**.
       4. Click **Create**.
    3. Generate OAuth Client Keys

  NEW (replace with / insert from CSV):
          1. Within the newly created workspace, navigate to **Apps**.
          2. Click **Add application**.
          3. Complete the required fields: **Application Name**, **Homepage URL**, and **Contact Email**.
          4. Click **Create**.
       3. Generate OAuth Client Keys

CHANGE 9  [### Sage Intacct API Key Generation]  (lines 51–53 in repo file)

  OLD (remove from repo):
    1. Click the newly created app.
    2. Click **Create API Keys**.
    3. Configure the following parameters:

  NEW (replace with / insert from CSV):
    1. 1. 1. Click the newly created app.
          2. Click **Create API Keys**.
          3. Configure the following parameters:

CHANGE 10  [### Sage Intacct API Key Generation]  (lines 55–59 in repo file)

  OLD (remove from repo):
       * **Sage Product API**: `Sage Intacct`
       * **Redirect URI**: `https://app.getknit.dev/oauth/authorize`
       * **Allowed Origin Domains**: `https://api.getknit.dev`
    4. Enter the previously retrieved Sender ID and Sender Password.
    5. Click **Create**.

  NEW (replace with / insert from CSV):
             - **Sage Product API**: `Sage Intacct`
             - **Redirect URI**: `[https://app.getknit.dev/oauth/authorize](https://app.getknit.dev/oauth/authorize)`
             - **Allowed Origin Domains**: `[https://api.getknit.dev](https://api.getknit.dev)`
          4. Enter the previously retrieved Sender ID and Sender Password.
          5. Click **Create**.

CHANGE 11  [### Sage Intacct API Key Generation]  (line 61 in repo file)

  OLD (remove from repo):
    The system will display the **Client ID** and **Client Secret**. These credentials are now authorized for use in OAuth integration with the Knit UI authentication component.

  NEW (replace with / insert from CSV):
    The system will display the **Client ID** and **Client Secret**. These credentials are now authorized for use in OAuth integration with the Knit UI authentication component.

CHANGE 12  [## Connecting to Your Sage Intacct Rest Account]  (line 65 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Sage Intacct Rest Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Sage Intacct Rest Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 13  [## Credentials Pane]  (line 69 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Sage Intacct Rest account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Sage Intacct Rest account. The following table describes what is needed for each field:

CHANGE 14  [## Credentials Pane]  (line 71 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Client ID </td><td colspan="1" rowspan="1"> Enter the Client ID. </td></tr><tr><td colspan="1" rowspan="1"> Client Secret </td><td colspan="1" rowspan="1"> Enter the Client Secret. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter the Client ID. |
    | Client Secret | Enter the Client Secret. |

CHANGE 15  [## Credentials Pane]  (line 73 in repo file)

  OLD (remove from repo):
    Once you have entered valid Sage Intacct Rest credentials, you can use the same account at any time to create a new Sage Intacct Rest DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Sage Intacct Rest credentials, you can use the same account at any time to create a new Sage Intacct Rest DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 16  [## Details Pane]  (line 77 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Sage Intacct Rest report you want to run. The following reports are available: </p><table><tbody><tr><th colspan="1" rowspan="1"> Report Name </th><th colspan="1" rowspan="1"> Description </th></tr><tr><td colspan="1" rowspan="1"> AP Account Label </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Payable Account Label accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> AP Adjustment </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Payable Adjustment accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> AP Bill </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Payable Bill accounting reports. <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> AP Bill Detail </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Payable Bill Detail accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> AP Payment </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Payable Payment accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> AP Term </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Payable Term accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> AR Account Label </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Receivable Account Label accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> AR Adjustment </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Receivable Adjustment accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> AR Invoice </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Receivable Invoice accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> AR Invoice Detail </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Receivable Invoice Detail accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> AR Payment </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Receivable Payment accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> AR Payment Summary </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Receivable Payment Summary accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> AR Term </td><td colspan="1" rowspan="1"> Receives data about Intacct Accounts Receivable Term accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Bank Account </td><td colspan="1" rowspan="1"> Receives data about Intacct Bank Account accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Class </td><td colspan="1" rowspan="1"> Receives data about Intacct Class accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Contact </td><td colspan="1" rowspan="1"> Receives data about Intacct Contact accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Contact Tax Group </td><td colspan="1" rowspan="1"> Receives data about Intacct Contact Tax Group accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Cust GL Group </td><td colspan="1" rowspan="1"> Receives data about Intacct Cust General Group accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Customer </td><td colspan="1" rowspan="1"> Receives data about Intacct Customer accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Department </td><td colspan="1" rowspan="1"> Receives data about Intacct Department accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Employee </td><td colspan="1" rowspan="1"> Receives data about Intacct Employee accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Expense Report </td><td colspan="1" rowspan="1"> Receives data about Intacct Expense Report accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Expense Report Summary </td><td colspan="1" rowspan="1"> Receives data about Intacct Expense Report Summary accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Expense Types </td><td colspan="1" rowspan="1"> Receives data about Intacct Expense Types accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> GL Account </td><td colspan="1" rowspan="1"> Receives data about Intacct General Account accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> GL Account Group </td><td colspan="1" rowspan="1"> Receives data about Intacct General Account Group accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> GL Account Group Member </td><td colspan="1" rowspan="1"> Receives data about Intacct General Account Group Member accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> GL Batch </td><td colspan="1" rowspan="1"> Receives data about Intacct General Batch accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> GL Budget </td><td colspan="1" rowspan="1"> Receives data about Intacct General Budget accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> GL Budget Item </td><td colspan="1" rowspan="1"> Receives data about Intacct General Budget Item accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> GL Account Category </td><td colspan="1" rowspan="1"> Receives data about Intacct General Account Category accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> GL Entry </td><td colspan="1" rowspan="1"> Receives data about Intacct General Entry accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> IC Item </td><td colspan="1" rowspan="1"> Receives data about Intacct Inventory Control Item accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> IC Transaction Definition </td><td colspan="1" rowspan="1"> Receives data about Intacct Inventory Control Transaction Definition accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Item </td><td colspan="1" rowspan="1"> Receives data about Intacct Item accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Item GL Group </td><td colspan="1" rowspan="1"> Receives data about Intacct Item General Group accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Item Tax Group </td><td colspan="1" rowspan="1"> Receives data about Intacct Item Tax Group accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Journal </td><td colspan="1" rowspan="1"> Receives data about Intacct Journal accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Location </td><td colspan="1" rowspan="1"> Receives data about Intacct Location accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Location Entity </td><td colspan="1" rowspan="1"> Receives data about Intacct Location Entity accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Location Group </td><td colspan="1" rowspan="1"> Receives data about Intacct Location Group accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> PO Price List </td><td colspan="1" rowspan="1"> Receives data about Intacct Purchase Order Price List accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Product Line </td><td colspan="1" rowspan="1"> Receives data about Intacct Product Line accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Project </td><td colspan="1" rowspan="1"> Receives data about Intacct Project accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Renewal Macro </td><td colspan="1" rowspan="1"> Receives data about Intacct Renewal Macro accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Reporting Period </td><td colspan="1" rowspan="1"> Receives data about Intacct Reporting Period accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Revenue Recognition Schedule </td><td colspan="1" rowspan="1"> Receives data about Intacct Revenue Recognition Schedule accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Revenue Recognition Schedule Entry </td><td colspan="1" rowspan="1"> Receives data about Intacct Revenue Recognition Schedule Entry accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Revenue Recognition Template </td><td colspan="1" rowspan="1"> Receives data about Intacct Revenue Recognition Schedule Template accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Sales Document </td><td colspan="1" rowspan="1"> Receives data about Intacct Sales Document reports. </td></tr><tr><td colspan="1" rowspan="1"> Sales Document Detail </td><td colspan="1" rowspan="1"> Receives data about Intacct Sales Document Detail reports. </td></tr><tr><td colspan="1" rowspan="1"> Sales Document Subtotals </td><td colspan="1" rowspan="1"> Receives data about Intacct Sales Document Subtotal reports. </td></tr><tr><td colspan="1" rowspan="1"> SO Price List </td><td colspan="1" rowspan="1"> Receives data about Intacct Sales Order Price List accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Stat GL Account </td><td colspan="1" rowspan="1"> Receives data about Intacct Stat General Ledger Account accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Statistical Journal </td><td colspan="1" rowspan="1"> Receives data about Intacct Statistical Journal accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Tax Detail </td><td colspan="1" rowspan="1"> Receives data about Intacct Tax Detail accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Tax Schedule </td><td colspan="1" rowspan="1"> Receives data about Intacct Tax Schedule accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Tax Schedule Detail </td><td colspan="1" rowspan="1"> Receives data about Intacct Tax Schedule Detail accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Territory </td><td colspan="1" rowspan="1"> Receives data about Intacct Territory accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Unit of Measure </td><td colspan="1" rowspan="1"> Receives data about Intacct Unit of Measure accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Vendor GL Group </td><td colspan="1" rowspan="1"> Receives data about Intacct Vendor General Group accounting reports. </td></tr><tr><td colspan="1" rowspan="1"> Vendor </td><td colspan="1" rowspan="1"> Receives data about Intacct Vendor accounting reports. A <b> Date Filter </b> is required for this report. </td></tr><tr><td colspan="1" rowspan="1"> Warehouse </td><td colspan="1" rowspan="1"> Receives data about Intacct Warehouse accounting reports. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Fixed or relative date range </td><td colspan="1" rowspan="1"> Choose an absolute or relative date range </td></tr><tr><td colspan="1" rowspan="1"> Fixed Start Date </td><td colspan="1" rowspan="1"> Select a start date. </td></tr><tr><td colspan="1" rowspan="1"> Fixed End Date </td><td colspan="1" rowspan="1"> Select an end date. </td></tr><tr><td colspan="1" rowspan="1"> Days Back (Incremental Days) </td><td colspan="1" rowspan="1"> Enter an initial backfill if you are using a daily incremental data pull. </td></tr><tr><td colspan="1" rowspan="1"> Date Filter </td><td colspan="1" rowspan="1"> Select the Date Filter. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Sage Intacct Rest report you want to run. The following reports are available:   | Report Name | Description | | --- | --- | | AP Account Label | Receives data about Intacct Accounts Payable Account Label accounting reports. | | AP Adjustment | Receives data about Intacct Accounts Payable Adjustment accounting reports. A **Date Filter** is required for this report. | | AP Bill | Receives data about Intacct Accounts Payable Bill accounting reports. **Date Filter** is required for this report. | | AP Bill Detail | Receives data about Intacct Accounts Payable Bill Detail accounting reports. | | AP Payment | Receives data about Intacct Accounts Payable Payment accounting reports. A **Date Filter** is required for this report. | | AP Term | Receives data about Intacct Accounts Payable Term accounting reports. | | AR Account Label | Receives data about Intacct Accounts Receivable Account Label accounting reports. | | AR Adjustment | Receives data about Intacct Accounts Receivable Adjustment accounting reports. A **Date Filter** is required for this report. | | AR Invoice | Receives data about Intacct Accounts Receivable Invoice accounting reports. A **Date Filter** is required for this report. | | AR Invoice Detail | Receives data about Intacct Accounts Receivable Invoice Detail accounting reports. | | AR Payment | Receives data about Intacct Accounts Receivable Payment accounting reports. A **Date Filter** is required for this report. | | AR Payment Summary | Receives data about Intacct Accounts Receivable Payment Summary accounting reports. A **Date Filter** is required for this report. | | AR Term | Receives data about Intacct Accounts Receivable Term accounting reports. | | Bank Account | Receives data about Intacct Bank Account accounting reports. | | Class | Receives data about Intacct Class accounting reports. | | Contact | Receives data about Intacct Contact accounting reports. | | Contact Tax Group | Receives data about Intacct Contact Tax Group accounting reports. | | Cust GL Group | Receives data about Intacct Cust General Group accounting reports. | | Customer | Receives data about Intacct Customer accounting reports. | | Department | Receives data about Intacct Department accounting reports. | | Employee | Receives data about Intacct Employee accounting reports. | | Expense Report | Receives data about Intacct Expense Report accounting reports. | | Expense Report Summary | Receives data about Intacct Expense Report Summary accounting reports. | | Expense Types | Receives data about Intacct Expense Types accounting reports. | | GL Account | Receives data about Intacct General Account accounting reports. | | GL Account Group | Receives data about Intacct General Account Group accounting reports. | | GL Account Group Member | Receives data about Intacct General Account Group Member accounting reports. | | GL Batch | Receives data about Intacct General Batch accounting reports. A **Date Filter** is required for this report. | | GL Budget | Receives data about Intacct General Budget accounting reports. | | GL Budget Item | Receives data about Intacct General Budget Item accounting reports. | | GL Account Category | Receives data about Intacct General Account Category accounting reports. | | GL Entry | Receives data about Intacct General Entry accounting reports. A **Date Filter** is required for this report. | | IC Item | Receives data about Intacct Inventory Control Item accounting reports. | | IC Transaction Definition | Receives data about Intacct Inventory Control Transaction Definition accounting reports. | | Item | Receives data about Intacct Item accounting reports. | | Item GL Group | Receives data about Intacct Item General Group accounting reports. | | Item Tax Group | Receives data about Intacct Item Tax Group accounting reports. | | Journal | Receives data about Intacct Journal accounting reports. | | Location | Receives data about Intacct Location accounting reports. | | Location Entity | Receives data about Intacct Location Entity accounting reports. | | Location Group | Receives data about Intacct Location Group accounting reports. | | PO Price List | Receives data about Intacct Purchase Order Price List accounting reports. | | Product Line | Receives data about Intacct Product Line accounting reports. | | Project | Receives data about Intacct Project accounting reports. | | Renewal Macro | Receives data about Intacct Renewal Macro accounting reports. | | Reporting Period | Receives data about Intacct Reporting Period accounting reports. | | Revenue Recognition Schedule | Receives data about Intacct Revenue Recognition Schedule accounting reports. | | Revenue Recognition Schedule Entry | Receives data about Intacct Revenue Recognition Schedule Entry accounting reports. | | Revenue Recognition Template | Receives data about Intacct Revenue Recognition Schedule Template accounting reports. | | Sales Document | Receives data about Intacct Sales Document reports. | | Sales Document Detail | Receives data about Intacct Sales Document Detail reports. | | Sales Document Subtotals | Receives data about Intacct Sales Document Subtotal reports. | | SO Price List | Receives data about Intacct Sales Order Price List accounting reports. | | Stat GL Account | Receives data about Intacct Stat General Ledger Account accounting reports. | | Statistical Journal | Receives data about Intacct Statistical Journal accounting reports. | | Tax Detail | Receives data about Intacct Tax Detail accounting reports. | | Tax Schedule | Receives data about Intacct Tax Schedule accounting reports. | | Tax Schedule Detail | Receives data about Intacct Tax Schedule Detail accounting reports. | | Territory | Receives data about Intacct Territory accounting reports. | | Unit of Measure | Receives data about Intacct Unit of Measure accounting reports. | | Vendor GL Group | Receives data about Intacct Vendor General Group accounting reports. | | Vendor | Receives data about Intacct Vendor accounting reports. A **Date Filter** is required for this report. | | Warehouse | Receives data about Intacct Warehouse accounting reports. | |
    | Fixed or relative date range | Choose an absolute or relative date range |
    | Fixed Start Date | Select a start date. |
    | Fixed End Date | Select an end date. |
    | Days Back (Incremental Days) | Enter an initial backfill if you are using a daily incremental data pull. |
    | Date Filter | Select the Date Filter. |

CHANGE 17  [## Other Panes]  (line 81 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005920.mdx
TITLE:  YouTube Multi Account Connector
LANG:   English (en_US)
ID:     000005920
==============================================================================

CHANGE 1  [### Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your YouTube Multi Account Connector in the Data Center. This topic discusses the fields and menus specific to the YouTube Multi Accounts connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector") and [Powering up Multi Accounts Connectors](/s/article/000005825 "Powering up Multi Accounts Connectors").

  NEW (replace with / insert from CSV):
    You connect to your YouTube Multi Account Connector in the Data Center. This topic discusses the fields and menus specific to the YouTube Multi Accounts connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274) and [Powering up Multi Accounts Connectors](/s/article/000005825).

CHANGE 2  [# Multi-Account Connector Vs Single Account Connector]  (line 13 in repo file)

  OLD (remove from repo):
    Every Multi-Account connector has a corresponding single-account connector. For example, the YouTube Multi Account connector has a corresponding YouTube single Account connector.

  NEW (replace with / insert from CSV):
    Every Multi-Account connector has a corresponding single-account connector. For example, the YouTube Multi Account connector has a corresponding YouTube single Account connector.

CHANGE 3  [# Multi-Account Connector Vs Single Account Connector]  (line 15 in repo file)

  OLD (remove from repo):
    You might prefer to use the YouTube Multi Account if the following are true:

  NEW (replace with / insert from CSV):
    You might prefer to use the YouTube Multi Account if the following are true:

CHANGE 4  [## Generating Client ID and Client Secret]  (lines 24–26 in repo file)

  OLD (remove from repo):
    1. Use your Google account to log in to the Google Developer Dashboard by using
       [https://console.developers.google.com.](https://console.developers.google.com/ "https://console.developers.google.com.")
    2. On the top left, click **Open project picker** option.

  NEW (replace with / insert from CSV):
    1. Use your Google account to log in to the Google Developer Dashboard by using [https://console.developers.google.com.](https://console.developers.google.com/)
    2. On the top left, click **Open project picker** option.

CHANGE 5  [## Generating Client ID and Client Secret]  (lines 28–29 in repo file)

  OLD (remove from repo):
       <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxIOb.jpg" style={{width: 889, height: 228}}/></Frame>
    3. Click **New Project**.

  NEW (replace with / insert from CSV):
       <Frame>![Screenshot](/images/kb/0EMVq00000CxIOb.jpg)</Frame>
    3. Click **New Project**.

CHANGE 6  [## Generating Client ID and Client Secret]  (lines 31–32 in repo file)

  OLD (remove from repo):
       <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxIbV.jpg" style={{width: 584, height: 179}}/></Frame>
    4. In the **New Project** page, enter a **Project name** (*example:* YouTube Connection), and click **Create**.

  NEW (replace with / insert from CSV):
       <Frame>![Screenshot](/images/kb/0EMVq00000CxIbV.jpg)</Frame>
    4. In the **New Project** page, enter a **Project name** (*example:* YouTube Connection), and click **Create**.

CHANGE 7  [## Generating Client ID and Client Secret]  (lines 34–35 in repo file)

  OLD (remove from repo):
       <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxIq1.jpg" style={{width: 688, height: 305}}/></Frame>
    5. Click the **Library** section.

  NEW (replace with / insert from CSV):
       <Frame>![Screenshot](/images/kb/0EMVq00000CxIq1.jpg)</Frame>
    5. Click the **Library** section.

CHANGE 8  [## Generating Client ID and Client Secret]  (lines 37–38 in repo file)

  OLD (remove from repo):
       <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxJXZ.jpg" style={{width: 723, height: 175}}/></Frame>
    6. Scroll below the page, click **YouTube Analytics API**, and click **Enable**.

  NEW (replace with / insert from CSV):
       <Frame>![Screenshot](/images/kb/0EMVq00000CxJXZ.jpg)</Frame>
    6. Scroll below the page, click **YouTube Analytics API**, and click **Enable**.

CHANGE 9  [## Generating Client ID and Client Secret]  (lines 40–41 in repo file)

  OLD (remove from repo):
       <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxJAz.jpg" style={{width: 831, height: 326}}/></Frame>
    7. Click **Credentials** > **Create Credentials** > **OAuth client ID.**

  NEW (replace with / insert from CSV):
       <Frame>![Screenshot](/images/kb/0EMVq00000CxJAz.jpg)</Frame>
    7. Click **Credentials** > **Create Credentials** > **OAuth client ID.**

CHANGE 10  [## Generating Client ID and Client Secret]  (lines 43–44 in repo file)

  OLD (remove from repo):
       <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxO5p.jpg" style={{width: 835, height: 244}}/></Frame>
    8. Click **Configure consent screen**.

  NEW (replace with / insert from CSV):
       <Frame>![Screenshot](/images/kb/0EMVq00000CxO5p.jpg)</Frame>
    8. Click **Configure consent screen**.

CHANGE 11  [## Generating Client ID and Client Secret]  (line 48 in repo file)

  OLD (remove from repo):
       1. Under **App Information**, enter the **App name** (enter the same name as the project name) **,** select the **User support email**, and click **Next**.

  NEW (replace with / insert from CSV):
       1. Under **App Information**, enter the **App name** (enter the same name as the project name)**,**select the **User support email**, and click **Next**.

CHANGE 12  [## Generating Client ID and Client Secret]  (lines 50–51 in repo file)

  OLD (remove from repo):
          <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxLHe.jpg" style={{width: 537, height: 298}}/></Frame>
       2. Under **Audience**, select **External,** and click **Next**.

  NEW (replace with / insert from CSV):
          <Frame>![Screenshot](/images/kb/0EMVq00000CxLHe.jpg)</Frame>
       2. Under **Audience**, select **External,** and click **Next**.

CHANGE 13  [## Generating Client ID and Client Secret]  (lines 55–56 in repo file)

  OLD (remove from repo):
       4. Under **Finish**, select the checkbox, click **Continue,** and finally click **Create.**
    10. Click **Create OAuth Client**.

  NEW (replace with / insert from CSV):
       4. Under **Finish**, select the checkbox, click **Continue,** and finally click **Create.**
    10. Click **Create OAuth Client**.

CHANGE 14  [## Generating Client ID and Client Secret]  (lines 58–59 in repo file)

  OLD (remove from repo):
        <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-0EMVq00000CxP57.jpg" style={{width: 587, height: 180}}/></Frame>
    11. Select **Web application** from the **Application type** drop-down.

  NEW (replace with / insert from CSV):
        <Frame>![Screenshot](/images/kb/0EMVq00000CxP57.jpg)</Frame>
    11. Select **Web application** from the **Application type** drop-down.

CHANGE 15  [## Generating Client ID and Client Secret]  (lines 67–68 in repo file)

  OLD (remove from repo):
        * [https://oauth.dev.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange](https://oauth.dev.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange "https://oauth.dev.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange")
        * [https://oauth.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange](https://oauth.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange "https://oauth.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange")

  NEW (replace with / insert from CSV):
        - [https://oauth.dev.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange](https://oauth.dev.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange)
        - [https://oauth.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange](https://oauth.domo.com/api/data/v1/oauth/providers/youtube-multiaccount/exchange)

CHANGE 16  [# Credentials Pane]  (line 77 in repo file)

  OLD (remove from repo):
    For more information on selecting and powering the accounts, see [Powering Up A Multi-Account Connector](/s/article/000005825 "Powering Up A Multi-Account connector") (this article explains the process of powering up multiple Connector Accounts and successfully creating a dataset).

  NEW (replace with / insert from CSV):
    For more information on selecting and powering the accounts, see [Powering Up A Multi-Account Connector](/s/article/000005825) (this article explains the process of powering up multiple Connector Accounts and successfully creating a dataset).

CHANGE 17  [# Credentials Pane]  (line 81 in repo file)

  OLD (remove from repo):
    You can manage Connector Accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing Connectors Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Managing Connectors Accounts").

  NEW (replace with / insert from CSV):
    You can manage Connector Accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing Connectors Accounts](/s/article/360042926054).

CHANGE 18  [# Details Pane]  (line 85 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus that may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus that may or may not appear depending on the report type you select.

CHANGE 19  [# Details Pane]  (line 87 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> The YouTube Multi Account connector has a single report to run. </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><b> Report </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"><p> Basic User Activity </p></td><td colspan="1" rowspan="1"> Returns basic information about how users watch your videos. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> API </p></td><td colspan="1" rowspan="1"><p> Select an API from the drop-down list. </p><table><tbody><tr><th colspan="1" rowspan="1"> API </th><th colspan="1" rowspan="1"> Description </th></tr><tr><td colspan="1" rowspan="1"> Analytics API </td><td colspan="1" rowspan="1"><p> The Analytics API enables you to understand your users and their interaction with your channel and videos. </p></td></tr></tbody></table><p></p></td></tr><tr><td colspan="1" rowspan="1"> Report Type </td><td colspan="1" rowspan="1"><p> Select the report type from the drop-down list. </p><ul><li><p> Select <b> Break down by public video </b> to return data for public videos. </p></li><li><p> Select <b> Break down by my videos </b> to return data for all videos associated with the authenticated account. </p></li><li><p> Select <b> Summary by videos </b> to retrieve data on behalf of a Content Owner. </p></li></ul></td></tr><tr><td colspan="1" rowspan="1"> Call On Behalf Of Content Owner </td><td colspan="1" rowspan="1"><p> Retrieve data for the specified Content Owner. </p></td></tr><tr><td colspan="1" rowspan="1"> Content Owner Id </td><td colspan="1" rowspan="1"><p> Retrieves data for the specified Content Owner. If you are unsure of the value, consult your partner manager. </p></td></tr><tr><td colspan="1" rowspan="1"> Videos Published After (Days Ago) </td><td colspan="1" rowspan="1"><ul><li> Enter the number of past days to retrieve data for all videos published after that date. <ul><li><p><em> Example: </em> Entering <code> 20 </code> returns information on all videos published within the last 20 days. </p></li><li><p><em> Note: </em> Leaving this field blank returns a breakdown of all videos. </p></li></ul></li></ul></td></tr><tr><td colspan="1" rowspan="1"> Videos Published Before (Days Ago) </td><td colspan="1" rowspan="1"><ul><li> Enter the number of past days to retrieve data for all videos published before that date. <ul><li><p><em> Example: </em> Entering <b><code> 20 </code></b> returns information on all videos published more than 20 days ago. </p></li><li><p><em> Note: </em> Leaving this field blank returns a breakdown of all videos. </p></li></ul></li></ul></td></tr><tr><td colspan="1" rowspan="1"> Number of Days Ago </td><td colspan="1" rowspan="1"><ul><li> Enter the most recent past day for which you want to retrieve the data. <ul><li><p><em> Example: </em> Enter <code> 2 </code> to retrieve data for the day before yesterday. </p></li><li><em> Note </em> : YouTube typically does not report data that is less than 2 days old. </li></ul></li></ul></td></tr><tr><td colspan="1" rowspan="1"> Backfill Days </td><td colspan="1" rowspan="1"><p> Enter the number of days before today for which you want to retrieve data during the initial run of the data source. </p></td></tr><tr><td colspan="1" rowspan="1"> Prior Days </td><td colspan="1" rowspan="1"><p> Enter the number of days before <b> Number of Days Ago </b> for which you want to retrieve data on the second and subsequent runs of the data source. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | The YouTube Multi Account connector has a single report to run.   |  |  | | --- | --- | | **Report** | **Description** | | Basic User Activity | Returns basic information about how users watch your videos. | |
    | API | Select an API from the drop-down list.   | API | Description | | --- | --- | | Analytics API | The Analytics API enables you to understand your users and their interaction with your channel and videos. | |
    | Report Type | Select the report type from the drop-down list.   - Select **Break down by public video** to return data for public videos. - Select **Break down by my videos** to return data for all videos associated with the authenticated account. - Select **Summary by videos**to retrieve data on behalf of a Content Owner. |
    | Call On Behalf Of Content Owner | Retrieve data for the specified Content Owner. |
    | Content Owner Id | Retrieves data for the specified Content Owner. If you are unsure of the value, consult your partner manager. |
    | Videos Published After (Days Ago) | - Enter the number of past days to retrieve data for all videos published after that date.   - *Example:* Entering `20` returns information on all videos published within the last 20 days.   - *Note:* Leaving this field blank returns a breakdown of all videos. |
    | Videos Published Before (Days Ago) | - Enter the number of past days to retrieve data for all videos published before that date.   - *Example:* Entering **`20`** returns information on all videos published more than 20 days ago.   - *Note:* Leaving this field blank returns a breakdown of all videos. |
    | Number of Days Ago | - Enter the most recent past day for which you want to retrieve the data.   - *Example:* Enter `2` to retrieve data for the day before yesterday.   - *Note*: YouTube typically does not report data that is less than 2 days old. |
    | Backfill Days | Enter the number of days before today for which you want to retrieve data during the initial run of the data source. |
    | Prior Days | Enter the number of days before **Number of Days Ago** for which you want to retrieve data on the second and subsequent runs of the data source. |

CHANGE 20  [# DataSet Update History]  (line 91 in repo file)

  OLD (remove from repo):
    Multi-Account Connectors display the history of running the Connector for each Account on the ‘History’ tab in the DataSet details. For more information on the update history for a Multi-Accounts Connector, and re-running a Multi-Accounts Connector dataset, see https://domo-support.domo.com/s/article/000005825?language=en_US .

  NEW (replace with / insert from CSV):
    Multi-Account Connectors display the history of running the Connector for each Account on the ‘History’ tab in the DataSet details. For more information on the update history for a Multi-Accounts Connector, and re-running a Multi-Accounts Connector dataset, see [https://domo-support.domo.com/s/article/000005825?language=en\_US](/s/article/000005825).

CHANGE 21  [# Troubleshooting and FAQs]  (lines 103–104 in repo file)

  OLD (remove from repo):
    1. The run time is much shorter than updating the entire dataset.
    2. Re-running specific Accounts does not affect any data successfully brought in by other Accounts.

  NEW (replace with / insert from CSV):
    1. 1. The run time is much shorter than updating the entire dataset.
       2. Re-running specific Accounts does not affect any data successfully brought in by other Accounts.

CHANGE 22  [# Troubleshooting and FAQs]  (line 106 in repo file)

  OLD (remove from repo):
    For more information on re-running one or more Accounts, see https://domo-support.domo.com/s/article/000005825?language=en_US .

  NEW (replace with / insert from CSV):
    For more information on re-running one or more Accounts, see [https://domo-support.domo.com/s/article/000005825?language=en\_US](/s/article/000005825).
    

CHANGE 23  [# Others Panes]  (line 111 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926274?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Manage Connector Accounts](/s/article/360042926274).

  [6 image-only change(s) suppressed — handle separately if needed]

==============================================================================
FILE:   s/article/000005921.mdx
TITLE:  Snowflake Key Pair Domo Managed Unload connector
LANG:   English (en_US)
ID:     000005921
==============================================================================

CHANGE 1  [(top of file)]  (line 2 in repo file)

  OLD (remove from repo):
    title: "Snowflake Key Pair Domo Managed Unload Connector"

  NEW (replace with / insert from CSV):
    title: "Snowflake Key Pair Domo Managed Unload connector"

CHANGE 2  [## Intro]  (lines 11–12 in repo file)

  OLD (remove from repo):
    * This connector first unloads data from your Snowflake instance into an Amazon S3 bucket.
    * The unloaded files are then automatically imported from Amazon S3 into Domo.

  NEW (replace with / insert from CSV):
    - This connector first unloads data from your Snowflake instance into an Amazon S3 bucket.
    - The unloaded files are then automatically imported from Amazon S3 into Domo.

CHANGE 3  [## Intro]  (line 14 in repo file)

  OLD (remove from repo):
    **Note:** Choose this connector as the preferred method over the standard Snowflake connector when processing extremely large query result sets.

  NEW (replace with / insert from CSV):
    <Note>**Note:** Choose this connector as the preferred method over the standard Snowflake connector when processing extremely large query result sets.</Note>

CHANGE 4  [## Intro]  (line 16 in repo file)

  OLD (remove from repo):
    You can connect to your Snowflake Key Pair Domo Managed Unload account in the Data Center. This topic discusses the fields and menus that are specific to the Snowflake Key Pair Domo Managed Unload connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You can connect to your Snowflake Key Pair Domo Managed Unload account in the Data Center. This topic discusses the fields and menus that are specific to the Snowflake Key Pair Domo Managed Unload connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 5  [## Prerequisites]  (lines 22–24 in repo file)

  OLD (remove from repo):
    * Account Identifier
    * Username
    * Private Key

  NEW (replace with / insert from CSV):
    - Account Identifier
    - Username
    - Private Key

CHANGE 6  [## Connecting to Your Snowflake Key Pair Domo Managed Unload Account]  (line 28 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Snowflake Key Pair Domo Managed Unload Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Snowflake Key Pair Domo Managed Unload Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 7  [## Credentials Pane]  (line 32 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Snowflake Key Pair Domo Managed Unload account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Snowflake Key Pair Domo Managed Unload account. The following table describes what is needed for each field:

CHANGE 8  [## Credentials Pane]  (line 34 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Account Identifier </td><td colspan="1" rowspan="1"><p> Specify the name of your Snowflake account based on its region: </p><ul><li><p><b> US West Region: </b> Use the format <code> account_name </code>. </p></li><li><p><b> All Other Regions: </b> Use the format <code> account_name.region_id </code>. </p></li></ul></td></tr><tr><td colspan="1" rowspan="1"> Username </td><td colspan="1" rowspan="1"> Enter the username to connect to your Snowflake host. </td></tr><tr><td colspan="1" rowspan="1"> Private Key </td><td colspan="1" rowspan="1"> Enter the Private Key. </td></tr><tr><td colspan="1" rowspan="1"> Passphrase </td><td colspan="1" rowspan="1"><p> If you choose to create an encrypted private key, OpenSSL will prompt you to enter a passphrase <b>. </b> This passphrase is used to encrypt the private key file. Enter the required passphrase when prompted. </p></td></tr><tr><td colspan="1" rowspan="1"> Role </td><td colspan="1" rowspan="1"><p> Enter your role. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Identifier | Specify the name of your Snowflake account based on its region:   - **US West Region:** Use the format `account_name`. - **All Other Regions:** Use the format `account_name.region_id`. |
    | Username | Enter the username to connect to your Snowflake host. |
    | Private Key | Enter the Private Key. |
    | Passphrase | If you choose to create an encrypted private key, OpenSSL will prompt you to enter a passphrase**.** This passphrase is used to encrypt the private key file. Enter the required passphrase when prompted. |
    | Role | Enter your role. |

CHANGE 9  [## Credentials Pane]  (line 36 in repo file)

  OLD (remove from repo):
    Once you have entered valid Snowflake Key Pair Domo Managed Unload credentials, you can use the same account at any time to create a new Snowflake Key Pair Domo Managed Unload DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Snowflake Key Pair Domo Managed Unload credentials, you can use the same account at any time to create a new Snowflake Key Pair Domo Managed Unload DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 10  [## Details Pane]  (line 40 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> How would you like to import data into Domo? </td><td colspan="1" rowspan="1"><p> Select any one of the methods below to import your data into Domo. </p><ul><li> Standard Update </li><li> Use Partition </li><li> Use Upsert </li></ul></td></tr><tr><td colspan="1" rowspan="1"> Query Type </td><td colspan="1" rowspan="1"><p> Select the query type from the drop-down list. </p><table><tbody><tr><th colspan="1" rowspan="1"> QueryType </th><th colspan="1" rowspan="1"> Description </th></tr><tr><td colspan="1" rowspan="1"> Custom Query </td><td colspan="1" rowspan="1"> Enter the SQL query you want to execute. </td></tr><tr><td colspan="1" rowspan="1"> Query Builder </td><td colspan="1" rowspan="1"> Select a table and the fields to autogenerate your query. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Warehouses </td><td colspan="1" rowspan="1"><p> Select the warehouse. </p></td></tr><tr><td colspan="1" rowspan="1"> Databases </td><td colspan="1" rowspan="1"><p> Select the database. </p></td></tr><tr><td colspan="1" rowspan="1"> Schemas </td><td colspan="1" rowspan="1"><p> Select the schema. </p></td></tr><tr><td colspan="1" rowspan="1"> Database Objects </td><td colspan="1" rowspan="1"><p> Choose the database object. </p></td></tr><tr><td colspan="1" rowspan="1"> Tables </td><td colspan="1" rowspan="1"><p> Select the table. </p></td></tr><tr><td colspan="1" rowspan="1"> Views </td><td colspan="1" rowspan="1"><p> Select the view. </p></td></tr><tr><td colspan="1" rowspan="1"> Table Column Names </td><td colspan="1" rowspan="1"><p> Select the table columns you want to include in your report. </p></td></tr><tr><td colspan="1" rowspan="1"> View Column Names </td><td colspan="1" rowspan="1"><p> Select the view columns you want to include in your report. </p></td></tr><tr><td colspan="1" rowspan="1"> Table Query Helper </td><td colspan="1" rowspan="1"><p> Enter the generated query based on the selected fields. </p></td></tr><tr><td colspan="1" rowspan="1"> View Query Helper </td><td colspan="1" rowspan="1"><p> A query is generated based on the selected fields. </p></td></tr><tr><td colspan="1" rowspan="1"> Query </td><td colspan="1" rowspan="1"><p> Enter the SQL query you want to execute. </p></td></tr><tr><td colspan="1" rowspan="1"> Partition Criteria </td><td colspan="1" rowspan="1"><p> Select whether you want to partition your data using the date keys, non-date keys, or a meta query. </p></td></tr><tr><td colspan="1" rowspan="1"> Partition Date Key Table Column Name </td><td colspan="1" rowspan="1"><p> Select the partition column name. </p></td></tr><tr><td colspan="1" rowspan="1"> Partition Date Key View Column Name </td><td colspan="1" rowspan="1"><p> Select the partition column name. Only date fields will be displayed. </p></td></tr><tr><td colspan="1" rowspan="1"> Enter Partition Date Key Column Name </td><td colspan="1" rowspan="1"><p> Enter the partition column name. </p></td></tr><tr><td colspan="1" rowspan="1"> Past Days </td><td colspan="1" rowspan="1"><p> Enter the number of past days for which you want to retrieve the data. This value must be a positive integer (e.g., 30). </p></td></tr><tr><td colspan="1" rowspan="1"> Date Format </td><td colspan="1" rowspan="1"><p> Select the required date format. By default, <b> yyyy-MM-dd </b> will be used. </p></td></tr><tr><td colspan="1" rowspan="1"> Custom Date Format </td><td colspan="1" rowspan="1"><p> Enter the custom date format. </p></td></tr><tr><td colspan="1" rowspan="1"> Partition Non Date Key Table Column Name </td><td colspan="1" rowspan="1"><p> Select the partition column name. </p></td></tr><tr><td colspan="1" rowspan="1"> Partition Non Date Key View Column Name </td><td colspan="1" rowspan="1"><p> Select a non-date key column name to partition the data. All the fields, excluding Date fields, will be displayed. </p></td></tr><tr><td colspan="1" rowspan="1"> Enter Partition Non Date Key Column Name </td><td colspan="1" rowspan="1"><p> Enter the partition column name. </p></td></tr><tr><td colspan="1" rowspan="1"> Partition Query to determine partition tags </td><td colspan="1" rowspan="1"><p> Enter the partition query to determine the distinct partition tags. The column containing the <b> Date </b> data is your partition column. Example: Select <b> Date </b> from Table_Name. </p></td></tr><tr><td colspan="1" rowspan="1"> Upsert Key Column(s) </td><td colspan="1" rowspan="1"><p> Enter the upsert key column name or a comma-separated list of upsert key column names. Required only when the Update method is Merge. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | How would you like to import data into Domo? | Select any one of the methods below to import your data into Domo.   - Standard Update - Use Partition - Use Upsert |
    | Query Type | Select the query type from the drop-down list.   | QueryType | Description | | --- | --- | | Custom Query | Enter the SQL query you want to execute. | | Query Builder | Select a table and the fields to autogenerate your query. | |
    | Warehouses | Select the warehouse. |
    | Databases | Select the database. |
    | Schemas | Select the schema. |
    | Database Objects | Choose the database object. |
    | Tables | Select the table. |
    | Views | Select the view. |
    | Table Column Names | Select the table columns you want to include in your report. |
    | View  Column Names | Select the view columns you want to include in your report. |
    | Table Query Helper | Enter the generated query based on the selected fields. |
    | View Query Helper | A query is generated based on the selected fields. |
    | Query | Enter the SQL query you want to execute. |
    | Partition Criteria | Select whether you want to partition your data using the date keys, non-date keys, or a meta query. |
    | Partition Date Key Table Column Name | Select the partition column name. |
    | Partition Date Key View Column Name | Select the partition column name. Only date fields will be displayed. |
    | Enter Partition Date Key Column Name | Enter the partition column name. |
    | Past Days | Enter the number of past days for which you want to retrieve the data. This value must be a positive integer (e.g., 30). |
    | Date Format | Select the required date format. By default, **yyyy-MM-dd** will be used. |
    | Custom Date Format | Enter the custom date format. |
    | Partition Non Date Key Table Column Name | Select the partition column name. |
    | Partition Non Date Key View Column Name | Select a non-date key column name to partition the data. All the fields, excluding Date fields, will be displayed. |
    | Enter Partition Non Date Key Column Name | Enter the partition column name. |
    | Partition Query to determine partition tags | Enter the partition query to determine the distinct partition tags. The column containing the **Date** data is your partition column. Example: Select **Date** from Table\_Name. |
    | Upsert Key Column(s) | Enter the upsert key column name or a comma-separated list of upsert key column names. Required only when the Update method is Merge. |

CHANGE 11  [## Other Panes]  (line 44 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005925.mdx
TITLE:  Google Sheets OAuth Writeback WIF Connector
LANG:   English (en_US)
ID:     000005925
==============================================================================

CHANGE 1  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You can connect to your Google Sheets OAuth Writeback WIF account in the Data Center. This topic discusses the fields and menus that are specific to the Google Sheets OAuth Writeback WIF connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You can connect to your Google Sheets OAuth Writeback WIF account in the Data Center. This topic discusses the fields and menus that are specific to the Google Sheets OAuth Writeback WIF connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 2  [## Prerequisites]  (lines 17–18 in repo file)

  OLD (remove from repo):
    * If you have an existing Workload Identity Federation, you need the following:
      + If you select to enter the configuration values manually, then you need:

  NEW (replace with / insert from CSV):
    - If you have an existing Workload Identity Federation, you need the following:
      - If you select to enter the configuration values manually, then you need:

CHANGE 3  [## Prerequisites]  (line 25 in repo file)

  OLD (remove from repo):
      + If you prefer to enter the JSON Configuration from the Google Cloud Console, then you need the following:

  NEW (replace with / insert from CSV):
      - If you prefer to enter the JSON Configuration from the Google Cloud Console, then you need the following:

CHANGE 4  [## Prerequisites]  (lines 28–29 in repo file)

  OLD (remove from repo):
    * If you select the **Go through prerequisites steps**, then enter the following:
      + - Project Number

  NEW (replace with / insert from CSV):
    - If you select the **Go through prerequisites steps**,then enter the following:
      - - Project Number

CHANGE 5  [## Connecting to Your Google Sheets OAuth Writeback WIF Account]  (line 38 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Google Sheets OAuth Writeback WIF Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Google Sheets OAuth Writeback WIF Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 6  [## Credentials Pane]  (line 42 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Google Sheets OAuth Writeback WIF account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Google Sheets OAuth Writeback WIF account. The following table describes what is needed for each field:

CHANGE 7  [## Credentials Pane]  (line 44 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Project Number </td><td colspan="1" rowspan="1"> Enter the project number of the newly created .project. </td></tr><tr><td colspan="1" rowspan="1"> Project ID </td><td colspan="1" rowspan="1"> Enter the project ID of the newly created project. </td></tr><tr><td colspan="1" rowspan="1"> Pool ID </td><td colspan="1" rowspan="1"> Enter the chosen pool ID. </td></tr><tr><td colspan="1" rowspan="1"> Provider ID </td><td colspan="1" rowspan="1"> Enter the chosen provider ID. </td></tr><tr><td colspan="1" rowspan="1"> Service Account Email </td><td colspan="1" rowspan="1"> Enter the service account email address generated for the chosen service account ID. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Project Number | Enter the project number of the newly created .project. |
    | Project ID | Enter the project ID of the newly created project. |
    | Pool ID | Enter the chosen pool ID. |
    | Provider ID | Enter the chosen provider ID. |
    | Service Account Email | Enter the service account email address generated for the chosen service account ID. |

CHANGE 8  [## Credentials Pane]  (line 46 in repo file)

  OLD (remove from repo):
    Once you have entered valid Google Sheets OAuth Writeback WIF credentials, you can use the same account at any time to create a new Google Sheets OAuth Writeback WIF DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Google Sheets OAuth Writeback WIF credentials, you can use the same account at any time to create a new Google Sheets OAuth Writeback WIF DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 9  [## Details Pane]  (line 50 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:196;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> Input DataSet Id </td><td colspan="1" rowspan="1"><p> Enter your Domo dataset ID(GUID) located in the dataset URL. </p><p> Example: <a href="https://customer.domo.com/datasources/" rel="noopener" target="_blank"> https://customer.domo.com/datasources/ </a><b> aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee </b> /details/settings. </p></td></tr><tr><td colspan="1" rowspan="1"> Input Sheet Id </td><td colspan="1" rowspan="1"><p> Enter the Google Sheet ID shared with the Service Account. </p><p> Example: <a href="https://docs.google.com/spreadsheets/d/" rel="noopener" target="_blank"> https://docs.google.com/spreadsheets/d/ </a><b> 18zN0RQ8HIaEj2USwtAkCMX7AKBl-x-_vq24kPd4_Tsk </b> /edit#gid=0&lt;br&gt; </p><p><b> Note: </b> - All the data in this sheet will be replaced. </p></td></tr><tr><td colspan="1" rowspan="1"> Sheet Name </td><td colspan="1" rowspan="1"><p> Enter the sheet name. The default sheet name should be Sheet1. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Input DataSet Id | Enter your Domo dataset ID(GUID) located in the dataset URL.  Example: https://customer.domo.com/datasources/**aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee**/details/settings. |
    | Input Sheet Id | Enter the Google Sheet ID shared with the Service Account.  Example: https://docs.google.com/spreadsheets/d/**18zN0RQ8HIaEj2USwtAkCMX7AKBl-x-\_vq24kPd4\_Tsk**/edit#gid=0<br>
    <Note>**Note:** - All the data in this sheet will be replaced. |
    | Sheet Name | Enter the sheet name. The default sheet name should be Sheet1. |</Note>

CHANGE 10  [## Other Panes]  (line 54 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/000005931.mdx
TITLE:  Salesforce Rest Writeback Connector
LANG:   English (en_US)
ID:     000005931
==============================================================================

CHANGE 1  [### Generating Domo Client ID and Client Secret]  (line 23 in repo file)

  OLD (remove from repo):
    To generate the Domo Client ID and Secret, follow the steps below:

  NEW (replace with / insert from CSV):
     To generate the Domo Client ID and Secret, follow the steps below:

CHANGE 2  [### Generating Domo Client ID and Client Secret]  (line 25 in repo file)

  OLD (remove from repo):
    1. From the navigation header, select **More** > <img alt="" src="/images/kb/0EMVq00000EYlkP.jpg" style={{width: 18, height: 18, display: 'inline', verticalAlign: 'start', margin: '0'}}/> **Admin**.

  NEW (replace with / insert from CSV):
    1. From the navigation header, select **More** >
    
       <img alt="" src="/images/kb/0EMVq00000EYlkP.jpg" style={{width: 18, height: 18, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    
       **Admin**.

==============================================================================
FILE:   s/article/000006000.mdx
TITLE:  ProcoreV2 Connector
LANG:   English (en_US)
ID:     000006000
==============================================================================

CHANGE 1  [## Details Pane]  (lines 27–33 in repo file)

  OLD (remove from repo):
    | Menu                          | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
    | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Report                        | Select the ProcoreV2 report you want to run. The following reports are available: <br/><br/> <table><thead><tr><th>Report Name</th><th>Description</th></tr></thead><tbody><tr><td>Companies</td><td>Returns a list of companies that the authenticated user can view.</td></tr><tr><td>Cost Codes</td><td>Returns a list of all standard cost code lists at the company level.</td></tr><tr><td>Change Event Settings</td><td>Retrieve values for change event settings.</td></tr><tr><td>Company Configurable Field Sets</td><td>Returns a list of all configurable field sets associated with a company.</td></tr><tr><td>Direct Costs</td><td>Returns a list of all direct cost items for a project.</td></tr><tr><td>Equipment</td><td>Returns a list of all project equipment.</td></tr><tr><td>Locations</td><td>Returns a list of locations associated with a project.</td></tr><tr><td>Project Dates</td><td>Returns a list of project dates.</td></tr><tr><td>Project Folders</td><td>Returns a list of folders and files for a specified project.</td></tr><tr><td>Projects</td><td>Returns a list of active projects.</td></tr><tr><td>Submittals Details</td><td>Returns a list of all submittals.</td></tr><tr><td>Trades</td><td>Returns a list of all trades associated with a company.</td></tr></tbody></table> |
    | Company                       | Select the company to run the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
    | Project Filter                | Select the project filter for project types.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
    | Configurable Field Set Filter | Select the filter type for the configurable field set.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | Project                       | Select the project to run the report. To run the report for all projects in the selected company, search for **All Projects** in the search box or scroll through the list and select **All Projects**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the ProcoreV2 report you want to run. The following reports are available:     |  |  | | --- | --- | | **Report Name** | **Description** | | Companies | Returns a list of companies that the authenticated user can view. | | Cost ​Codes | Returns a list of all standard cost code lists at the company level. | | Change ​Event ​Settings | Retrieve values for change event settings. | | Company ​Configurable ​Field ​Sets | Returns a list of all configurable field sets associated with a company. | | Direct ​Costs | Returns a list of all direct cost items for a project. | | Equipment | Returns a list of all project equipment. | | Locations | Returns a list of locations associated with a project. | | Project ​Dates | Returns a list of project dates. | | Project ​Folders | Returns a list of folders and files for a specified Project. | | Projects | Returns a list of active projects. | | Submittals ​Details | Returns a list of all submittals. | | Trades | Returns a list of all trades associated with a company. | |
    | Company | Select the company to run the report. |
    | Project Filter | Select the project filter for project types. |
    | Configurable Field Set Filter | Select the filter type for the configurable field Set. |
    | Project | Select the project to run the report. To run the report for all projects in the selected company, search for **All Projects** in the search box or scroll through the list and select **All Projects**. |

==============================================================================
FILE:   s/article/000006013.mdx
TITLE:  Clio Connector
LANG:   English (en_US)
ID:     000006013
==============================================================================

CHANGE 1  [## Details Pane]  (lines 53–57 in repo file)

  OLD (remove from repo):
    | Menu        | Description                                                                                                                                                                                                                                                                                                                  |
    | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Report      | Select the Clio report you want to run. The following reports are available: <br/><br/> <table><thead><tr><th>Report Name</th><th>Description</th></tr></thead><tbody><tr><td>Create and Run Report</td><td>Create and run a report by specifying the report parameters.</td></tr><tr><td>Get Details of All Reports</td><td>Returns the data for all the reports generated in Clio.</td></tr></tbody></table> |
    | Report Type | Select the type of report you want to generate.                                                                                                                                                                                                                                                                              |
    | Date Range  | Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2000, and must not be a future date.                                                                                                                                                                                  |

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Clio report you want to run. The following reports are available:     |  |  | | --- | --- | | **Report Name** | **Description** | | Create and Run Report | Create and run a report by specifying the report parameters. | | Get Details of All Reports | Returns the data for all the reports generated in Clio. | |
    | Report Type | Select the type of report you want to generate. |
    | Date Range | Select the date range for which you want to fetch the data. The date range cannot be before January 1, 2000, and must not be a future date. |

==============================================================================
FILE:   s/article/000006016.mdx
TITLE:  Google Ad Manager REST Connector
LANG:   English (en_US)
ID:     000006016
==============================================================================

CHANGE 1  [## Details Pane]  (lines 29–39 in repo file)

  OLD (remove from repo):
    | Menu                    | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
    | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Report                  | Select the Google Ad Manager REST report you want to run. The following reports are available: <br/><br/> <table><thead><tr><th>Report Name</th><th>Description</th></tr></thead><tbody><tr><td>Create and Run Interactive Report</td><td>Retrieves data for a custom interactive report.</td></tr><tr><td>Interactive Reports List</td><td>Retrieves a list of available interactive reports.</td></tr><tr><td>Networks</td><td>Returns the networks that the user can access.</td></tr><tr><td>Run Existing Interactive Report</td><td>Runs and retrieves data for an existing interactive report.</td></tr></tbody></table> |
    | Network Code            | Choose the network code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
    | Interactive Report ID   | Select the interactive report to run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
    | Report Type             | Select the report type you want to use.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
    | Dimensions              | Select one or more dimensions to include in the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
    | Metrics                 | Select one or more metrics to include in the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
    | Date Type               | Select the date type for the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
    | Predefined Date Range   | Select the predefined date range for the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
    | Report Dates            | Select the start date and end date for the report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Google Ad Manager REST report you want to run. The following reports are available:     |  |  | | --- | --- | | **Report Name** | **Description** | | Create and run interactive report | Retrieves data for a custom interactive report. | | Interactive Reports List | Retrieves a list of available interactive reports. | | Networks | This report returns the networks that the user can access. | | Run existing interactive report | Runs and retrieves data for an existing interactive report. | |
    | Network Code | Choose the network code. |
    | Interactive Report ID | Select the interactive report to run. |
    | Report Type | Select the report type you want to use. |
    | Dimensions | Select one or more dimensions to include in the report. |
    | Metrics | Select one or more metrics to include in the report |
    | Date type | Select the date type for the report. |
    | Predefined Date Range | Select the predefined date range for the report. |
    | Report Dates | Select the start date and end date for the report. |

==============================================================================
FILE:   s/article/000006053.mdx
TITLE:  Azure Data Lake Storage Gen2 Using AAD WriteBack Connector
LANG:   English (en_US)
ID:     000006053
==============================================================================

CHANGE 1  [### Details Pane]  (lines 70–78 in repo file)

  OLD (remove from repo):
    | Menu                                             | Description                                                                                                                                                                                                                              |
    | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | How would you like to choose the directory name? | Select how you want to name the directory.                                                                                                                                                                                               |
    | Discover Directory Name                          | Select the directory name.                                                                                                                                                                                                               |
    | Enter Directory Name                             | Enter the directory name. <br/><br/> <table><thead><tr><th>Example</th><th>Description</th></tr></thead><tbody><tr><td><code>test</code></td><td>Example for the main or root directory.</td></tr><tr><td><code>test/test</code></td><td>Example for a sub-directory.</td></tr><tr><td><code>test/test/test</code></td><td>Example for a sub-sub-directory.</td></tr></tbody></table> |
    | Input DataSet ID                                 | Enter your Domo DataSet ID (GUID) located in the DataSet URL. For example: `https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings`                                                                 |
    | Select Filename                                  | Specify whether you want to use the input DataSet GUID or enter the file name manually.                                                                                                                                                  |
    | File Name                                        | Enter the output file name.                                                                                                                                                                                                              |
    | My Domo Input DataSet is very large              | Select this checkbox if you are exporting a large DataSet from Domo to Azure Data Lake. When selected, the connector uses a faster export process.                                                                                       |

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | How would you like to choose the directory name? | Select how you want to name the directory. |
    | Discover Directory Name | Select the directory name. |
    | Enter Directory Name | Enter the directory name   |  |  | | --- | --- | | **test** | Example for the main or root directory | | **test/test** | Example for sub directory | | **test/test/test** | Example for sub-sub-directory | |
    | Input DataSet Id | Enter your Domo DataSet ID (GUID) located in the DataSet URL. For example: https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings |
    | Select filename | Specify whether you want to use the input DataSet GUID or enter the file name manually. |
    | FileName | Enter the output file name. |
    | My Domo Input DataSet is very large | Select this checkbox if you are exporting a large DataSet from Domo to Azure Data Lake. When selected, the connector uses a faster export process. |

==============================================================================
FILE:   s/article/1500000218261.mdx
TITLE:  Tiktok Ads Connector
LANG:   English (en_US)
ID:     1500000218261
==============================================================================

CHANGE 1  [## Prerequisites]  (lines 17–22 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:**
    - Once you select your time zone during the account registration process, you cannot change it later. The Daylight saving time is not supported in Ads Manager.
      To find your selected time zone:
        1. ​On your TikTok Ads Manager dashboard, select your account name on the top-right corner, a drop-down menu appears. The blue card pinned on the top of the drop-down menu is the account you are currently using.
        2. Select the "**Ad Account Settings**" icon to the right of the account information. Under your Basic Information, you will find your Time Zone. For more information refer, [About Time Zone.](https://ads.tiktok.com/help/article/time-zone?lang=en)

  NEW (replace with / insert from CSV):
    <Note>**Note:** - Once you select your time zone during the account registration process, you cannot change it later. The Daylight saving time is not supported in Ads Manager.
      To find your selected time zone:</Note>
    
    1. 1. ​On your TikTok Ads Manager dashboard, select your account name on the top-right corner, a drop-down menu appears. The blue card pinned on the top of the drop-down menu is the account you are currently using.
       2. Select the "**Ad Account Settings**" icon to the right of the account information. Under your Basic Information, you will find your Time Zone. For more information refer, [About Time Zone.](https://ads.tiktok.com/help/article/time-zone?lang=en)

CHANGE 2  [## Prerequisites]  (line 25 in repo file)

  OLD (remove from repo):
    </Note>

CHANGE 3  [## Connecting to Your TikTok Account]  (line 29 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the TikTok Ads Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the TikTok Ads Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [## Credentials Pane]  (line 33 in repo file)

  OLD (remove from repo):
    The Domo TikTok Ads connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select ** Add Account** if you have existing TikTok accounts in Domo) to open the TikTok OAuth screen where you can enter your TikTok credentials. Once you have entered valid TikTok credentials, you can use the same account any time you go to create a new TikTok Ads DataSet. You can manage connector accounts in the ** Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    The Domo TikTok Ads connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing TikTok accounts in Domo) to open the TikTok OAuth screen where you can enter your TikTok credentials. Once you have entered valid TikTok credentials, you can use the same account any time you go to create a new TikTok Ads DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 5  [## Credentials Pane]  (line 35 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you are already logged into TikTok when you connect in Domo, you are authenticated automatically when you click ** Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of that TikTok account.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into TikTok when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of that TikTok account.</Note>

CHANGE 6  [## Details Pane]  (lines 53–159 in repo file)

  ⚠ LARGE CHANGE (133 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the TikTok Ads report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Level</p></td>
          <td colspan="1" rowspan="1"><p>Select which level of audience data to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Advertisers Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to input the advertisers.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ad Service Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the ad service type.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Data Level</p></td>
          <td colspan="1" rowspan="1"><p>Select the data level.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Advertisers Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to input the advertisers.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Advertiser ID</p></td>
          <td colspan="1" rowspan="1"><p>Select the Advertiser ID(s) for which the data is to be fetched.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Fields</p></td>
          <td colspan="1" rowspan="1"><p>Select the fields to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>ID Dimension</p></td>
          <td colspan="1" rowspan="1"><p>Select if you would like to group data by ID dimension.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Time Dimension</p></td>
          <td colspan="1" rowspan="1"><p>Select whether daily or hourly data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Metrics</p></td>
          <td colspan="1" rowspan="1"><p>Select the metrics to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Time Granularity</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to get daily or hourly data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Dimensions</p></td>
          <td colspan="1" rowspan="1"><p>Select the dimensional grouping according to which the data to be retrieved.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select the date format for your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Single Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Relative Date</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data for in the <b>Days Back</b> field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Range</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific or relative date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the last date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data from (start day). Combine with <b><i></b><b></i>End Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b><b><i></b></i>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data to (end day). Combine with <b><i></b><b></i>Start Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b><b><i></b></i>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Time Period</p></td>
          <td colspan="1" rowspan="1"><p>Specify the time period that you would like to receive data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Starting Day of the Week</p></td>
          <td colspan="1" rowspan="1"><p>Select the day you would like your week to start with.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the TikTok Ads report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Ad Groups | Returns a list of ad groups. | | Ads | Returns a list of information related to the ad to be displayed. | | Advertisers | Returns a list of advertiser accounts that authorized the Domo app. | | Campaigns | Returns a list of campaigns. | | Creatives | Returns the basic performance metrics at the creative asset level as well as creative asset attributes. | | Custom Audience Details | Returns details of custom audiences. | | Custom Audience List | Returns a list of custom audiences. | | Performance Report V3 (NEW) | This NEW Performance report has replaced the Ad Audience, Ad Performance, Ad Group Performance, Advertiser Performance, and Campaign Performance reports. Use this report to get Ad Audience Data, Ad, Ad Group, Campaign, and Advertiser spending and Performance data. | |
    | Report Level | Select which level of audience data to retrieve. |
    | Advertisers Selection | Select how you want to input the advertisers. |
    | Ad Service Type | Select the ad service type. |
    | Data Level | Select the data level. |
    | Advertisers Selection | Select how you want to input the advertisers. |
    | Advertiser ID | Select the Advertiser ID(s) for which the data is to be fetched. |
    | Fields | Select the fields to retrieve. |
    | ID Dimension | Select if you would like to group data by ID dimension. |
    | Time Dimension | Select whether daily or hourly data. |
    | Metrics | Select the metrics to retrieve. |
    | Time Granularity | Select whether to get daily or hourly data. |
    | Dimensions | Select the dimensional grouping according to which the data to be retrieved. |
    | Date Selection | Select the date format for your data. |
    | Single Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Specific Date | Select the specific date using the date selector. |
    | Relative Date | Enter the number of days back that you would like to get data for in the ****Days Back**** field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Date Range | Select the specific or relative date range. |
    | Start Date - Specific | Select the first date in your date range using the date selector. |
    | End Date - Specific | Select the last date in your date range using the date selector. |
    | Start Date - Relative | Enter the number of days back that you would like to get data from (start day). Combine with ****************End Date**************** to create a range of represented days.  For example, if you entered 10 for ****************Start Date**************** and 5 for ****************End Date****************, the report would contain data for 10 days ago up until 5 days ago. |
    | End Date - Relative | Enter the number of days back that you would like to get data to (end day). Combine with ****************Start Date****************to create a range of represented days.  For example, if you entered 10 for ****************Start Date**************** and 5 for ****************End Date****************, the report would contain data for 10 days ago up until 5 days ago. |
    | Time Period | Specify the time period that you would like to receive data for. |
    | Starting Day of the Week | Select the day you would like your week to start with. |

CHANGE 7  [## Troubleshooting]  (lines 171–251 in repo file)

  ⚠ LARGE CHANGE (97 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Metric 1</p></th>
          <th colspan="1" rowspan="1"><p>Metric 2 (metric to be ignored if both metric 1 and metric 2 are used)</p></th>
          <th colspan="1" rowspan="1"><p>Shared New Name</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_register</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register</p></td>
          <td colspan="1" rowspan="1"><p>registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_register\_cost{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_register\_rate{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_rate</p></td>
          <td colspan="1" rowspan="1"><p>registration\_rate</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_pay{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay</p></td>
          <td colspan="1" rowspan="1"><p>purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_pay\_cost{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_pay\_rate{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_rate</p></td>
          <td colspan="1" rowspan="1"><p>purchase\_rate</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_register\_show{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_show</p></td>
          <td colspan="1" rowspan="1"><p>vta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_register\_show\_cost{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_show\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_vta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_pay\_show{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_show</p></td>
          <td colspan="1" rowspan="1"><p>vta\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_pay\_show\_cost{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_show\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_vta\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_register\_click{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_click</p></td>
          <td colspan="1" rowspan="1"><p>cta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_register\_click\_cost{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_click\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_cta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_pay\_click\_cost{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_click\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_cta\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>active\_pay\_click{}</p></td>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_click</p></td>
          <td colspan="1" rowspan="1"><p>cta\_purchase</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Metric 1 | Metric 2 (metric to be ignored if both metric 1 and metric 2 are used) | Shared New Name |
    | --- | --- | --- |
    | active\_register | time\_attr\_active\_register | registration |
    | active\_register\_cost{} | time\_attr\_active\_register\_cost | cost\_per\_registration |
    | active\_register\_rate{} | time\_attr\_active\_register\_rate | registration\_rate |
    | active\_pay{} | time\_attr\_active\_pay | purchase |
    | active\_pay\_cost{} | time\_attr\_active\_pay\_cost | cost\_per\_purchase |
    | active\_pay\_rate{} | time\_attr\_active\_pay\_rate | purchase\_rate |
    | active\_register\_show{} | time\_attr\_active\_register\_show | vta\_registration |
    | active\_register\_show\_cost{} | time\_attr\_active\_register\_show\_cost | cost\_per\_vta\_registration |
    | active\_pay\_show{} | time\_attr\_active\_pay\_show | vta\_purchase |
    | active\_pay\_show\_cost{} | time\_attr\_active\_pay\_show\_cost | cost\_per\_vta\_purchase |
    | active\_register\_click{} | time\_attr\_active\_register\_click | cta\_registration |
    | active\_register\_click\_cost{} | time\_attr\_active\_register\_click\_cost | cost\_per\_cta\_registration |
    | active\_pay\_click\_cost{} | time\_attr\_active\_pay\_click\_cost | cost\_per\_cta\_purchase |
    | active\_pay\_click{} | time\_attr\_active\_pay\_click | cta\_purchase |

CHANGE 8  [## Troubleshooting]  (lines 261–347 in repo file)

  ⚠ LARGE CHANGE (104 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Metric 1</p></th>
          <th colspan="1" rowspan="1"><p>Metric 2</p></th>
          <th colspan="1" rowspan="1"><p>Shared Returned Column Name</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>add\_billing</p></td>
          <td colspan="1" rowspan="1"><p>total\_add\_payment\_info</p></td>
          <td colspan="1" rowspan="1"><p>Total Add Payment Info</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>cost\_per\_registration</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_total\_registration</p></td>
          <td colspan="1" rowspan="1"><p>Cost per Registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>total\_app\_event\_add\_to\_cart\_value</p></td>
          <td colspan="1" rowspan="1"><p>total\_web\_event\_add\_to\_cart\_value</p></td>
          <td colspan="1" rowspan="1"><p>Total Add to Cart Value</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>cost\_per\_total\_app\_event\_add\_to\_cart</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_web\_event\_add\_to\_cart</p></td>
          <td colspan="1" rowspan="1"><p>Cost per Add to Cart</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>value\_per\_total\_app\_event\_add\_to\_cart</p></td>
          <td colspan="1" rowspan="1"><p>value\_per\_web\_event\_add\_to\_cart</p></td>
          <td colspan="1" rowspan="1"><p>Value per Add to Cart</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>cost\_per\_product\_details\_page\_browse</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_total\_view\_content</p></td>
          <td colspan="1" rowspan="1"><p>Cost per View Content</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>value\_per\_product\_details\_page\_browse</p></td>
          <td colspan="1" rowspan="1"><p>value\_per\_total\_view\_content</p></td>
          <td colspan="1" rowspan="1"><p>Value per View Content</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>cost\_per\_purchase</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_total\_purchase</p></td>
          <td colspan="1" rowspan="1"><p>Cost per Purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>cost\_per\_search</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_total\_search</p></td>
          <td colspan="1" rowspan="1"><p>Cost per Search</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>product\_details\_page\_browse</p></td>
          <td colspan="1" rowspan="1"><p>total\_view\_content</p></td>
          <td colspan="1" rowspan="1"><p>Total View Content</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>total\_product\_details\_page\_browse\_value</p></td>
          <td colspan="1" rowspan="1"><p>total\_view\_content\_value</p></td>
          <td colspan="1" rowspan="1"><p>Total View Content Value</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>total\_subscribe</p></td>
          <td colspan="1" rowspan="1"><p>on\_web\_subscribe</p></td>
          <td colspan="1" rowspan="1"><p>Total Subscribe</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>total\_subscribe\_value</p></td>
          <td colspan="1" rowspan="1"><p>total\_on\_web\_subscribe\_value</p></td>
          <td colspan="1" rowspan="1"><p>Total Subscribe Value</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>value\_per\_total\_subscribe</p></td>
          <td colspan="1" rowspan="1"><p>value\_per\_on\_web\_subscribe</p></td>
          <td colspan="1" rowspan="1"><p>Value per Subscribe</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>cost\_per\_total\_subscribe</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_on\_web\_subscribe</p></td>
          <td colspan="1" rowspan="1"><p>Cost per Subscribe</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Metric 1 | Metric 2 | Shared Returned Column Name |
    | --- | --- | --- |
    | add\_billing | total\_add\_payment\_info | Total Add Payment Info |
    | cost\_per\_registration | cost\_per\_total\_registration | Cost per Registration |
    | total\_app\_event\_add\_to\_cart\_value | total\_web\_event\_add\_to\_cart\_value | Total Add to Cart Value |
    | cost\_per\_total\_app\_event\_add\_to\_cart | cost\_per\_web\_event\_add\_to\_cart | Cost per Add to Cart |
    | value\_per\_total\_app\_event\_add\_to\_cart | value\_per\_web\_event\_add\_to\_cart | Value per Add to Cart |
    | cost\_per\_product\_details\_page\_browse | cost\_per\_total\_view\_content | Cost per View Content |
    | value\_per\_product\_details\_page\_browse | value\_per\_total\_view\_content | Value per View Content |
    | cost\_per\_purchase | cost\_per\_total\_purchase | Cost per Purchase |
    | cost\_per\_search | cost\_per\_total\_search | Cost per Search |
    | product\_details\_page\_browse | total\_view\_content | Total View Content |
    | total\_product\_details\_page\_browse\_value | total\_view\_content\_value | Total View Content Value |
    | total\_subscribe | on\_web\_subscribe | Total Subscribe |
    | total\_subscribe\_value | total\_on\_web\_subscribe\_value | Total Subscribe Value |
    | value\_per\_total\_subscribe | value\_per\_on\_web\_subscribe | Value per Subscribe |
    | cost\_per\_total\_subscribe | cost\_per\_on\_web\_subscribe | Cost per Subscribe |

CHANGE 9  [## Troubleshooting]  (lines 357–437 in repo file)

  ⚠ LARGE CHANGE (97 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Selectable v1 Metric</p></th>
          <th colspan="1" rowspan="1"><p>Selectable v2 Metric</p></th>
          <th colspan="1" rowspan="1"><p>Corresponding column name in Dataset</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register</p></td>
          <td colspan="1" rowspan="1"><p>active\_register</p></td>
          <td colspan="1" rowspan="1"><p>registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_cost</p></td>
          <td colspan="1" rowspan="1"><p>active\_register\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_rate</p></td>
          <td colspan="1" rowspan="1"><p>active\_register\_rate</p></td>
          <td colspan="1" rowspan="1"><p>registration\_rate</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay</p></td>
          <td colspan="1" rowspan="1"><p>active\_pay</p></td>
          <td colspan="1" rowspan="1"><p>purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_cost</p></td>
          <td colspan="1" rowspan="1"><p>active\_pay\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_rate</p></td>
          <td colspan="1" rowspan="1"><p>active\_pay\_rate</p></td>
          <td colspan="1" rowspan="1"><p>purchase\_rate</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_show</p></td>
          <td colspan="1" rowspan="1"><p>active\_register\_show</p></td>
          <td colspan="1" rowspan="1"><p>vta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_show\_cost</p></td>
          <td colspan="1" rowspan="1"><p>active\_register\_show\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_vta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_show</p></td>
          <td colspan="1" rowspan="1"><p>active\_pay\_show</p></td>
          <td colspan="1" rowspan="1"><p>vta\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_show\_cost</p></td>
          <td colspan="1" rowspan="1"><p>active\_pay\_show\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_vta\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_click</p></td>
          <td colspan="1" rowspan="1"><p>active\_register\_click</p></td>
          <td colspan="1" rowspan="1"><p>cta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_register\_click\_cost</p></td>
          <td colspan="1" rowspan="1"><p>active\_register\_click\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_cta\_registration</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_click\_cost</p></td>
          <td colspan="1" rowspan="1"><p>active\_pay\_click\_cost</p></td>
          <td colspan="1" rowspan="1"><p>cost\_per\_cta\_purchase</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>time\_attr\_active\_pay\_click</p></td>
          <td colspan="1" rowspan="1"><p>active\_pay\_click</p></td>
          <td colspan="1" rowspan="1"><p>cta\_purchase</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Selectable v1 Metric | Selectable v2 Metric | Corresponding column name in Dataset |
    | --- | --- | --- |
    | time\_attr\_active\_register | active\_register | registration |
    | time\_attr\_active\_register\_cost | active\_register\_cost | cost\_per\_registration |
    | time\_attr\_active\_register\_rate | active\_register\_rate | registration\_rate |
    | time\_attr\_active\_pay | active\_pay | purchase |
    | time\_attr\_active\_pay\_cost | active\_pay\_cost | cost\_per\_purchase |
    | time\_attr\_active\_pay\_rate | active\_pay\_rate | purchase\_rate |
    | time\_attr\_active\_register\_show | active\_register\_show | vta\_registration |
    | time\_attr\_active\_register\_show\_cost | active\_register\_show\_cost | cost\_per\_vta\_registration |
    | time\_attr\_active\_pay\_show | active\_pay\_show | vta\_purchase |
    | time\_attr\_active\_pay\_show\_cost | active\_pay\_show\_cost | cost\_per\_vta\_purchase |
    | time\_attr\_active\_register\_click | active\_register\_click | cta\_registration |
    | time\_attr\_active\_register\_click\_cost | active\_register\_click\_cost | cost\_per\_cta\_registration |
    | time\_attr\_active\_pay\_click\_cost | active\_pay\_click\_cost | cost\_per\_cta\_purchase |
    | time\_attr\_active\_pay\_click | active\_pay\_click | cta\_purchase |

==============================================================================
FILE:   s/article/1500000555201.mdx
TITLE:  Snowflake KeyPair Unload V2 Connector
LANG:   English (en_US)
ID:     1500000555201
==============================================================================

CHANGE 1  [## Prerequisites]  (line 16 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region **account\_name** and All other regions ** account\_name.region\_id**. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region ****account\_name**** and All other regions ****account\_name.region\_id****. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

CHANGE 2  [## Prerequisites]  (line 24 in repo file)

  OLD (remove from repo):
    - Your AWS access key. It is available in the AWS Console in the **Security Credentials** section or under ** Users** if you are using ** IAM**.

  NEW (replace with / insert from CSV):
    - Your AWS access key. It is available in the AWS Console in the **Security Credentials** section or under **Users** if you are using **IAM**.

CHANGE 3  [### Configuring the public/private key pair]  (lines 74–103 in repo file)

  ⚠ LARGE CHANGE (36 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Property</p></th>
          <th colspan="1" rowspan="1"><p>Value</p></th>
          <th colspan="1" rowspan="1"><p>Default</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Name</p></td>
          <td colspan="1" rowspan="1"><p>JSMITH</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Name</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_FP</p></td>
          <td colspan="1" rowspan="1"><p>SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA=</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's RSA public key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_2\_FP</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's second RSA public key.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
       | Property | Value | Default | Description |
       | --- | --- | --- | --- |
       | Name | JSMITH | null | Name |
       | RSA\_PUBLIC\_KEY\_FP | SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA= | null | Fingerprint of user's RSA public key. |
       | RSA\_PUBLIC\_KEY\_2\_FP | null | null | Fingerprint of user's second RSA public key. |
    

CHANGE 4  [### Configuring the public/private key pair]  (line 106 in repo file)

  OLD (remove from repo):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:</Note>

  NEW (replace with / insert from CSV):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:
       - Update the security parameters:
         - <path> specifies the local path to the private key file you created.
       - Update the session parameters:
         - <user> specifies your Snowflake login name.
         - <account> specifies the name of your account (provided by Snowflake).
       - The sample code is as follows:</Note>

CHANGE 5  [### Configuring the public/private key pair]  (lines 108–113 in repo file)

  OLD (remove from repo):
    - Update the security parameters:
    - <path> specifies the local path to the private key file you created.
    - Update the session parameters:
    - <user> specifies your Snowflake login name.
    - <account> specifies the name of your account (provided by Snowflake).
    - The sample code is as follows:

  NEW (replace with / insert from CSV):
         `import java.util.Properties;
          import java.sql.Connection;
          import java.sql.Statement;
          import java.sql.ResultSet;
          import java.sql.DriverManager;
          import java.io.File;
          import java.io.FileInputStream;
          import java.io.DataInputStream;
          import java.util.Base64;
          import java.security.spec.PKCS8EncodedKeySpec;
          import java.security.KeyFactory;
          import java.security.PrivateKey;
          import javax.crypto.EncryptedPrivateKeyInfo;
          import javax.crypto.SecretKeyFactory;
          import javax.crypto.spec.PBEKeySpec;`

CHANGE 6  [### Configuring the public/private key pair]  (lines 115–130 in repo file)

  OLD (remove from repo):
          ```java
          import java.util.Properties;
          import java.sql.Connection;
          import java.sql.Statement;
          import java.sql.ResultSet;
          import java.sql.DriverManager;
          import java.io.File;
          import java.io.FileInputStream;
          import java.io.DataInputStream;
          import java.util.Base64;
          import java.security.spec.PKCS8EncodedKeySpec;
          import java.security.KeyFactory;
          import java.security.PrivateKey;
          import javax.crypto.EncryptedPrivateKeyInfo;
          import javax.crypto.SecretKeyFactory;
          import javax.crypto.spec.PBEKeySpec;

  NEW (replace with / insert from CSV):
         `public class TestJdbc
          {
              public static void main(String[] args)
                  throws Exception
              {
                File f = new File("<path>/rsa_key.p8");
                FileInputStream fis = new FileInputStream(f);
                DataInputStream dis = new DataInputStream(fis);
                byte[] keyBytes = new byte[(int) f.length()];
                dis.readFully(keyBytes);
                dis.close();`

CHANGE 7  [### Configuring the public/private key pair]  (lines 132–142 in repo file)

  OLD (remove from repo):
          public class TestJdbc
          {
              public static void main(String[] args)
                  throws Exception
              {
                File f = new File("<path>/rsa_key.p8");
                FileInputStream fis = new FileInputStream(f);
                DataInputStream dis = new DataInputStream(fis);
                byte[] keyBytes = new byte[(int) f.length()];
                dis.readFully(keyBytes);
                dis.close();

  NEW (replace with / insert from CSV):
         `String encrypted = new String(keyBytes);
                String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
                encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
                encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
                EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
                PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
                SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
                PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
                KeyFactory keyFactory = KeyFactory.getInstance("RSA");
                PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);`

CHANGE 8  [### Configuring the public/private key pair]  (lines 144–153 in repo file)

  OLD (remove from repo):
                String encrypted = new String(keyBytes);
                String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
                encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
                encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
                EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
                PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
                SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
                PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
                KeyFactory keyFactory = KeyFactory.getInstance("RSA");
                PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);

  NEW (replace with / insert from CSV):
         `String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
                Properties prop = new Properties();
                prop.put("user", "<user>");
                prop.put("account", "<account>");
                prop.put("privateKey", encryptedPrivateKey);`

CHANGE 9  [### Configuring the public/private key pair]  (lines 155–169 in repo file)

  OLD (remove from repo):
                String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
                Properties prop = new Properties();
                prop.put("user", "<user>");
                prop.put("account", "<account>");
                prop.put("privateKey", encryptedPrivateKey);
    
                Connection conn = DriverManager.getConnection(url, prop);
                Statement stat = conn.createStatement();
                ResultSet res = stat.executeQuery("select 1");
                res.next();
                System.out.println(res.getString(1));
                conn.close();
              }
          }
          ```

  NEW (replace with / insert from CSV):
         `Connection conn = DriverManager.getConnection(url, prop);
                Statement stat = conn.createStatement();
                ResultSet res = stat.executeQuery("select 1");
                res.next();
                System.out.println(res.getString(1));
                conn.close();
              }
          }`

CHANGE 10  [## Connecting to Your Snowflake Account]  (line 193 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake KeyPair Unload V2 Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake KeyPair Unload V2 Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 11  [### Credentials Pane]  (lines 199–248 in repo file)

  ⚠ LARGE CHANGE (62 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the full name of your account (provided by Snowflake.) For US West Region <b>account\_name</b> and All other regions <b> account\_name.region\_id</b></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username used to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Private Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the private key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Passphrase</p></td>
          <td colspan="1" rowspan="1"><p>If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS access key. It is available in the AWS Console in the Security Credentials section or under Users if you are using IAM.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Secret Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS secret access key. It was provided when you created your Access Key. You can generate a new one in the AWS Console.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Bucket</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the AWS S3 bucket that your Snowflake data will be unloaded into.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Region</p></td>
          <td colspan="1" rowspan="1"><p>Select the Amazon S3 Region.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Encryption Type</p></td>
          <td colspan="1" rowspan="1"><p>Specify the encryption type for unloading data to files in storage locations.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the full name of your account (provided by Snowflake.) For US West Region ****account\_name**** and All other regions ****account\_name.region\_id**** |
    | Username | Enter the username used to connect to your Snowflake host. |
    | Private Key | Enter the private key. |
    | Passphrase | If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here. |
    | Role | Enter your role. |
    | Access Key | Enter your AWS access key. It is available in the AWS Console in the Security Credentials section or under Users if you are using IAM. |
    | Secret Key | Enter your AWS secret access key. It was provided when you created your Access Key. You can generate a new one in the AWS Console. |
    | Bucket | Enter the name of the AWS S3 bucket that your Snowflake data will be unloaded into. |
    | Region | Select the Amazon S3 Region. |
    | Encryption Type | Specify the encryption type for unloading data to files in storage locations. |

CHANGE 12  [### Details Pane]  (lines 256–365 in repo file)

  ⚠ LARGE CHANGE (137 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to import data into Domo?</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to import your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Type</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates with all warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve.  The list populates with all databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Schemas</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates with all schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Objects</p></td>
          <td colspan="1" rowspan="1"><p>Choose the database object.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to retrieve. The list populates with all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Views</p></td>
          <td colspan="1" rowspan="1"><p>Select the view.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns</p></td>
          <td colspan="1" rowspan="1"><p>Select the table columns you would like to include in your report. If you do not select any table column, the connector will retrieve your entire selected database table.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View  Column Names</p></td>
          <td colspan="1" rowspan="1"><p>Select the view columns that you would like to include in your report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query to execute.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>Generated the query based on the selected fields.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>A query is generated based on the selected fields.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to partition your data using the date keys, non-date keys, or meta query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Partition Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name. Only date fields will be shown in this discovery.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Partition Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that you want to fetch the data for. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the required date format. By default, <b>yyyy-MM-dd</b> will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom date format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select a non-date key column name to partition the data. All the fields excluding Date fields will be shown in this discovery.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Query to determine partition tags</p></td>
          <td colspan="1" rowspan="1"><p>Enter the partition query to determine the distinct partition tags. The column containing the <b>Date</b> data is your partition column. Example: Select <b> Date</b> from Table\_Name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Upsert Key Column(s)</p></td>
          <td colspan="1" rowspan="1"><p>Please enter upsert key column name or a comma separated list of upsert key column names. <b>Required only when Update method is Merge</b>.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | How would you like to import data into Domo? | Select how you want to import your data. |
    | Query Type | | QueryType | Description | | --- | --- | | Custom Query | Enter the SQL query to execute. | | Query Builder | Select a table and fields to auto generate your query. | |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates with all warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve.  The list populates with all databases you have access to based on your credentials. |
    | Schemas | Select the database schema containing the data you want to retrieve. The list populates with all schemas you have access to based on your credentials. |
    | Database Objects | Choose the database object. |
    | Tables | Select the database table you want to retrieve. The list populates with all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Views | Select the view. |
    | Table Columns | Select the table columns you would like to include in your report. If you do not select any table column, the connector will retrieve your entire selected database table. |
    | View  Column Names | Select the view columns that you would like to include in your report. |
    | Query | Enter the SQL query to execute. |
    | Table Query Helper | Generated the query based on the selected fields. |
    | View Query Helper | A query is generated based on the selected fields. |
    | Partition Criteria | Select whether you want to partition your data using the date keys, non-date keys, or meta query. |
    | Select Partition Date Key Column Name | Select the partition column name. |
    | Partition Column Name | Select the partition column name. Only date fields will be shown in this discovery. |
    | Enter Partition Date Key Column Name | Enter the partition column name. |
    | Past Days | Enter the number of past days that you want to fetch the data for. Value can be X, where X is a positive integer. For example: 30. |
    | Date Format | Select the required date format. By default, **yyyy-MM-dd** will be used. |
    | Custom Date Format | Enter the custom date format. |
    | Select Partition Non Date Key Column Name | Select the partition column name. |
    | Partition Non Date Key Column Name | Select a non-date key column name to partition the data. All the fields excluding Date fields will be shown in this discovery. |
    | Enter Partition Non Date Key Column Name | Enter the partition column name. |
    | Partition Query to determine partition tags | Enter the partition query to determine the distinct partition tags. The column containing the **Date** data is your partition column. Example: Select **Date** from Table\_Name. |
    | Upsert Key Column(s) | Please enter upsert key column name or a comma separated list of upsert key column names. **Required only when Update method is Merge**. |

CHANGE 13  [## FAQs]  (line 373 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### Are there any API limits I should be aware of?

CHANGE 14  [## FAQs]  (line 375 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits I should be aware of?">

CHANGE 15  [## FAQs]  (line 377 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 16  [## FAQs]  (line 379 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 17  [## FAQs]  (line 381 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 18  [## FAQs]  (line 383 in repo file)

  OLD (remove from repo):
    <Accordion title="What should I be aware of while writing a query?">

  NEW (replace with / insert from CSV):
    #### What should I be aware of while writing a query?
    

CHANGE 19  [## FAQs]  (line 385 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 20  [## FAQs]  (line 387 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?
    

CHANGE 21  [## FAQs]  (line 389 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 22  [## FAQs]  (line 391 in repo file)

  OLD (remove from repo):
    </AccordionGroup>

==============================================================================
FILE:   s/article/1500000888261.mdx
TITLE:  Snowflake KeyPair Partition Connector
LANG:   English (en_US)
ID:     1500000888261
==============================================================================

CHANGE 1  [## Intro]  (lines 10–11 in repo file)

  OLD (remove from repo):
    ---
    

CHANGE 2  [## Prerequisites]  (line 17 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region **account\_name** and All other regions ** account\_name.region\_id**. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region ****account\_name**** and All other regions ****account\_name.region\_id****. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

CHANGE 3  [### Configuring the public/private key pair]  (lines 71–100 in repo file)

  ⚠ LARGE CHANGE (36 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Property</p></th>
          <th colspan="1" rowspan="1"><p>Value</p></th>
          <th colspan="1" rowspan="1"><p>Default</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Name</p></td>
          <td colspan="1" rowspan="1"><p>JSMITH</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Name</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_FP</p></td>
          <td colspan="1" rowspan="1"><p>SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA=</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's RSA public key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_2\_FP</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's second RSA public key.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
       | Property | Value | Default | Description |
       | --- | --- | --- | --- |
       | Name | JSMITH | null | Name |
       | RSA\_PUBLIC\_KEY\_FP | SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA= | null | Fingerprint of user's RSA public key. |
       | RSA\_PUBLIC\_KEY\_2\_FP | null | null | Fingerprint of user's second RSA public key. |
    

CHANGE 4  [### Configuring the public/private key pair]  (line 103 in repo file)

  OLD (remove from repo):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:</Note>

  NEW (replace with / insert from CSV):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:
       - Update the security parameters:
         - <path> specifies the local path to the private key file you created.
       - Update the session parameters:
         - <user> specifies your Snowflake login name.
         - <account> specifies the name of your account (provided by Snowflake).
       - The sample code is as follows:</Note>

CHANGE 5  [### Configuring the public/private key pair]  (lines 105–110 in repo file)

  OLD (remove from repo):
    - Update the security parameters:
    - <path> specifies the local path to the private key file you created.
    - Update the session parameters:
    - <user> specifies your Snowflake login name.
    - <account> specifies the name of your account (provided by Snowflake).
    - The sample code is as follows:

  NEW (replace with / insert from CSV):
         `import java.util.Properties;
          import java.sql.Connection;
          import java.sql.Statement;
          import java.sql.ResultSet;
          import java.sql.DriverManager;
          import java.io.File;
          import java.io.FileInputStream;
          import java.io.DataInputStream;
          import java.util.Base64;
          import java.security.spec.PKCS8EncodedKeySpec;
          import java.security.KeyFactory;
          import java.security.PrivateKey;
          import javax.crypto.EncryptedPrivateKeyInfo;
          import javax.crypto.SecretKeyFactory;
          import javax.crypto.spec.PBEKeySpec;`

CHANGE 6  [### Configuring the public/private key pair]  (lines 112–127 in repo file)

  OLD (remove from repo):
          ```java
          import java.util.Properties;
          import java.sql.Connection;
          import java.sql.Statement;
          import java.sql.ResultSet;
          import java.sql.DriverManager;
          import java.io.File;
          import java.io.FileInputStream;
          import java.io.DataInputStream;
          import java.util.Base64;
          import java.security.spec.PKCS8EncodedKeySpec;
          import java.security.KeyFactory;
          import java.security.PrivateKey;
          import javax.crypto.EncryptedPrivateKeyInfo;
          import javax.crypto.SecretKeyFactory;
          import javax.crypto.spec.PBEKeySpec;

  NEW (replace with / insert from CSV):
         `public class TestJdbc
          {
              public static void main(String[] args)
                  throws Exception
              {
                File f = new File("<path>/rsa_key.p8");
                FileInputStream fis = new FileInputStream(f);
                DataInputStream dis = new DataInputStream(fis);
                byte[] keyBytes = new byte[(int) f.length()];
                dis.readFully(keyBytes);
                dis.close();`

CHANGE 7  [### Configuring the public/private key pair]  (lines 129–139 in repo file)

  OLD (remove from repo):
          public class TestJdbc
          {
             public static void main(String[] args)
                throws Exception
             {
                File f = new File("<path>/rsa_key.p8");
                FileInputStream fis = new FileInputStream(f);
                DataInputStream dis = new DataInputStream(fis);
                byte[] keyBytes = new byte[(int) f.length()];
                dis.readFully(keyBytes);
                dis.close();

  NEW (replace with / insert from CSV):
         `String encrypted = new String(keyBytes);
                String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
                encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
                encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
                EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
                PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
                SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
                PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
                KeyFactory keyFactory = KeyFactory.getInstance("RSA");
                PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);`

CHANGE 8  [### Configuring the public/private key pair]  (lines 141–150 in repo file)

  OLD (remove from repo):
                String encrypted = new String(keyBytes);
                String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
                encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
                encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
                EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
                PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
                SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
                PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
                KeyFactory keyFactory = KeyFactory.getInstance("RSA");
                PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);

  NEW (replace with / insert from CSV):
         `String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
                Properties prop = new Properties();
                prop.put("user", "<user>");
                prop.put("account", "<account>");
                prop.put("privateKey", encryptedPrivateKey);`

CHANGE 9  [### Configuring the public/private key pair]  (lines 152–166 in repo file)

  OLD (remove from repo):
                String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
                Properties prop = new Properties();
                prop.put("user", "<user>");
                prop.put("account", "<account>");
                prop.put("privateKey", encryptedPrivateKey);
    
                Connection conn = DriverManager.getConnection(url, prop);
                Statement stat = conn.createStatement();
                ResultSet res = stat.executeQuery("select 1");
                res.next();
                System.out.println(res.getString(1));
                conn.close();
             }
          }
          ```

  NEW (replace with / insert from CSV):
         `Connection conn = DriverManager.getConnection(url, prop);
                Statement stat = conn.createStatement();
                ResultSet res = stat.executeQuery("select 1");
                res.next();
                System.out.println(res.getString(1));
                conn.close();
              }
          }`

CHANGE 10  [## Connecting to Your Snowflake Account]  (line 190 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake KeyPair Partition Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake KeyPair Partition Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 11  [### Credentials Pane]  (lines 196–225 in repo file)

  ⚠ LARGE CHANGE (37 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the full name of your account (provided by Snowflake.) For US West Region <b>account\_name</b> and All other regions <b> account\_name.region\_id</b></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username used to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Private Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the private key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Passphrase</p></td>
          <td colspan="1" rowspan="1"><p>If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name for your role.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the full name of your account (provided by Snowflake.) For US West Region ****account\_name**** and All other regions ****account\_name.region\_id**** |
    | Username | Enter the username used to connect to your Snowflake host. |
    | Private Key | Enter the private key. |
    | Passphrase | If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here. |
    | Role | Enter the name for your role. |

CHANGE 12  [### Details Pane]  (lines 233–350 in repo file)

  ⚠ LARGE CHANGE (147 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to import data into Domo?</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to import your data.  The following methods are available:   - Standard Update (Replace/Append) - Use Partition - Use Upsert</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to build your Query?</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates with all warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve.  The list populates with all databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Schemas</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates with all schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Objects</p></td>
          <td colspan="1" rowspan="1"><p>Choose a database object.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Column Names</p></td>
          <td colspan="1" rowspan="1"><p>Select the table columns that you want to include in your report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>A query is generated based on the selected fields.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Views</p></td>
          <td colspan="1" rowspan="1"><p>Select the view.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View Column Names</p></td>
          <td colspan="1" rowspan="1"><p>Select the view columns that you want to include in your report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>A query is generated based on the selected fields.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to partition your data using the date keys, non-date keys, or a meta query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name. Only date fields will be displayed.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Support Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select a non-date key column name to partition the data. All the fields, excluding Date fields, will be displayed.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the query to execute. The query format must be like: select \* from \"databaseName\".\"schemaName\".\"tableName\""</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Query to determine partition tags</p></td>
          <td colspan="1" rowspan="1"><p>Enter the partition meta query to determine the distinct partition tags(or keys). The column containing the <b>Date</b> data is your partition column. Example: SELECT DISTINCT(DATE\_COLUMN\_NAME) FROM TABLENAME</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Years</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past years for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Future Years</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of future years for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Months</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past months for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Future Months</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of future months for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Data Format</p></td>
          <td colspan="1" rowspan="1"><p>Select/enter the proper date format according to the date format present in the partition column. By default, <b>yyyy-MM-dd</b> will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom date format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Upsert Key Column(s)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the upsert key column name or a comma-separated list of upsert key column names. <b>Required only when the Update method is Merge</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Tag</p></td>
          <td colspan="1" rowspan="1"><p>Optional parameter that can be used to tag connector SQL executions in Snowflake as a <b>Query Tag</b>.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | How would you like to import data into Domo? | Select how you want to import your data.  The following methods are available:   - Standard Update (Replace/Append) - Use Partition - Use Upsert |
    | How would you like to build your Query? | | QueryType | Description | | --- | --- | | Custom Query | Enter the SQL query you want to execute. | | Query Builder | Select a table and fields to autogenerate your query. | |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates with all warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve.  The list populates with all databases you have access to based on your credentials. |
    | Schemas | Select the database schema containing the data you want to retrieve. The list populates with all schemas you have access to based on your credentials. |
    | Database Objects | Choose a database object. |
    | Tables | Select the database table you want to retrieve. |
    | Table Column Names | Select the table columns that you want to include in your report. |
    | Table Query Helper | A query is generated based on the selected fields. |
    | Views | Select the view. |
    | View Column Names | Select the view columns that you want to include in your report. |
    | View Query Helper | A query is generated based on the selected fields. |
    | Partition Criteria | Select whether you want to partition your data using the date keys, non-date keys, or a meta query. |
    | Partition Column Name | Select the partition column name. Only date fields will be displayed. |
    | Partition Support Format | Select the partition format. |
    | Partition Non Date Key Column Name | Select a non-date key column name to partition the data. All the fields, excluding Date fields, will be displayed. |
    | Query | Enter the query to execute. The query format must be like: select \* from \"databaseName\".\"schemaName\".\"tableName\"" |
    | Partition Query to determine partition tags | Enter the partition meta query to determine the distinct partition tags(or keys). The column containing the **Date** data is your partition column. Example: SELECT DISTINCT(DATE\_COLUMN\_NAME) FROM TABLENAME |
    | Past Days | Enter the number of past days for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30. |
    | Past Years | Enter the number of past years for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30. |
    | Future Years | Enter the number of future years for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30. |
    | Past Months | Enter the number of past months for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30. |
    | Future Months | Enter the number of future months for which you want to fetch the data. Value can be X, where X is a positive integer. For example: 30. |
    | Data Format | Select/enter the proper date format according to the date format present in the partition column. By default, **yyyy-MM-dd** will be used. |
    | Custom Date Format | Enter the custom date format. |
    | Upsert Key Column(s) | Enter the upsert key column name or a comma-separated list of upsert key column names. **Required only when the Update method is Merge**. |
    | Query Tag | Optional parameter that can be used to tag connector SQL executions in Snowflake as a **Query Tag**. |

CHANGE 13  [## FAQs]  (line 358 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### Are there any API limits I should be aware of?

CHANGE 14  [## FAQs]  (line 360 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits I should be aware of?">

CHANGE 15  [## FAQs]  (line 362 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 16  [## FAQs]  (line 364 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 17  [## FAQs]  (line 366 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 18  [## FAQs]  (line 368 in repo file)

  OLD (remove from repo):
    <Accordion title="What should I be aware of while writing a query?">

  NEW (replace with / insert from CSV):
    #### What should I be aware of while writing a query?
    

CHANGE 19  [## FAQs]  (line 370 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 20  [## FAQs]  (line 372 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?
    

CHANGE 21  [## FAQs]  (line 374 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 22  [## FAQs]  (line 376 in repo file)

  OLD (remove from repo):
    </AccordionGroup>

==============================================================================
FILE:   s/article/1500001012441.mdx
TITLE:  Hubspot Writeback Connector
LANG:   English (en_US)
ID:     1500001012441
==============================================================================

CHANGE 1  [## Intro]  (lines 11–13 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:**
    - The Hubspot Writeback connector requires the connected hubspot account to be a special type of account, a ** Marketing Hub** account.

  NEW (replace with / insert from CSV):
    <Note>**Note:** - The Hubspot Writeback connector requires the connected hubspot account to be a special type of account, a **Marketing Hub** account.

CHANGE 2  [## Intro]  (lines 16–17 in repo file)

  OLD (remove from repo):
    - The Writeback Connector performs an upsert. If it identifies a match between the row in the input dataset and Hubspot, it will update the Hubspot Account. However, if it does not finds a match, it will create a new Account in Hubspot.
    </Note>

  NEW (replace with / insert from CSV):
    - The Writeback Connector performs an upsert. If it identifies a match between the row in the input dataset and Hubspot, it will update the Hubspot Account. However, if it does not finds a match, it will create a new Account in Hubspot.</Note>

CHANGE 3  [## Prerequisites]  (line 26 in repo file)

  OLD (remove from repo):
    2. In the top right corner under **My Account** click ** New Client**.

  NEW (replace with / insert from CSV):
    2. In the top right corner under **My Account** click **New Client**.

CHANGE 4  [## Prerequisites]  (line 28 in repo file)

  OLD (remove from repo):
    4. Provide the application scope by selecting the checkboxes for **Data** and ** User**.

  NEW (replace with / insert from CSV):
    4. Provide the application scope by selecting the checkboxes for **Data** and **User**.

CHANGE 5  [## Prerequisites]  (line 32 in repo file)

  OLD (remove from repo):
    Your **Client Secret** will appear in the ** Manage Client** section.

  NEW (replace with / insert from CSV):
    Your **Client Secret** will appear in the **Manage Client** section.

CHANGE 6  [## Configuring the Connection]  (line 36 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Hubspot Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Hubspot Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 7  [## Credentials Pane]  (lines 42–59 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client secret.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domo Client ID | Enter your Domo client ID. |
    | Domo Client Secret | Enter your Domo client secret. |

CHANGE 8  [## Credentials Pane]  (line 63 in repo file)

  OLD (remove from repo):
    Once you have entered valid credentials, you can use the same account any time you go to create a new Domo-Hubspot connection. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    Once you have entered valid credentials, you can use the same account any time you go to create a new Domo-Hubspot connection. You can manage connector accounts in the **Accounts**tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

CHANGE 9  [## Details Pane]  (lines 71–73 in repo file)

  OLD (remove from repo):
    | Hubspot Object | There are seven types of Hubspot Object, which include Companies, Contacts, Deals, Tickets, Custom Object, Appointments, and Listings. <ul><li>**Companies:** Writes data from Domo to HubSpot companies.</li><li>**Contacts:** Writes data from Domo to HubSpot contacts.</li><li>**Deals:** Writes data from Domo to Hubspot deals.</li><li>**Tickets:** Writes data from Domo to HubSpot tickets.</li><li>**Custom Object:** Writes data from Domo to a custom object in HubSpot.</li><li>**Appointments:** Writes data from Domo to Hubspot appointments.</li><li>**Listings:** Writes data from Domo to HubSpot listings.</li></ul> | |
    | Domo Dataset | Dataset ID | Enter the id of your Domo dataset which contains the data you want to write to Hubspot. The id is located in the dataset url. For example: [https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings](https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings). |
    | Configuration Mapping | Once you enter the Dataset ID, the **Use automated mapping** and **Select mapping manually** radio button appears. Select whether to use automated mapping (if your dataset fields match the Hubspot fields) or if you want to define the mapping manually. <Note>**Note:** The fields that appear in this section varies, and it depends on the **Hubspot Object** selected.</Note> | **Use automated mapping:** If the Hubspot fields and the dataset fields match exactly, then you can use automated mapping. <br/><br/> **Select mapping manually:** For manual mapping, you need to match the desired Hubspot fields and the dataset columns manually. <ul><li>**Required Fields:** Shows the list of required Hubspot fields, where you need to match with the corresponding Dataset field.</li><li>**Optional Fields:** To update and insert records into Hubspot, you need to map the Hubspot fields "Record ID" with the corresponding matching field from the input dataset.</li></ul> |

  NEW (replace with / insert from CSV):
    | Hubspot Object | There are seven types of Hubspot Object, which include Companies, Contacts, Deals, Tickets, Custom Object, Appointments, and Listings.  **Companies:**Writes data from Domo to HubSpot companies.  **Contacts:**Writes data from Domo to HubSpot contacts.  **Deals:** Writes data from Domo to Hubspot deals.  **Tickets**.**:** Writes data from Domo to HubSpot tickets.  **Custom Object:** Writes data from Domo to a custom object in HubSpot.  **Appointments:** Writes data from Domo to Hubspot appointments.  **Listings:** Writes data from Domo to HubSpot listings. |  |
    | Domo Dataset | Dataset ID | Enter the id of your Domo dataset which contains the data you want to write to Hubspot.  The id is located in the dataset url. For example: [https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings](https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings). |
    | Configuration Mapping | Once you enter the Dataset ID,  the **Use automated mapping** and **Select mapping manually** radio button appears. Select, whether to use automated mapping (if your dataset fields match the Hubspot fields) or if you want to define the mapping manually.
    <Note>**Note:** The fields that appear in this section varies, and it depends on the **Hubspot Object** selected. | **Use automated mapping:** If the Hubspot fields and the dataset fields match exactly, then you can use automated mapping.  **Select mapping manually:**For manual mapping, you need to match the desired Hubspot fields and the dataset columns manually.   - **Required Fields:** Shows the list of required Hubspot fields, where you need to match with the corresponding Dataset field. - **Optional Fields:**To update and insert records into Hubspot, you need to map the Hubspot fields "Record ID" with the corresponding matching field from the input dataset. |</Note>

CHANGE 10  [## FAQ]  (line 82 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 11  [## FAQ]  (line 83 in repo file)

  OLD (remove from repo):
    

CHANGE 12  [## FAQ]  (lines 88–97 in repo file)

  OLD (remove from repo):
    
    1. Log into the Domo developer account.
    2. In the top right corner under **My Account** click **New Client**.
    3. Enter the application name and description.
    4. Provide the application scope by selecting the checkboxes for **Data** and **User**.
    5. Click **Create**.
    6. Once you have created a client, you can manage the client by clicking on **Manage Client**.
    
    
    Your **Client Secret** will appear in the **Manage Client** section.

  NEW (replace with / insert from CSV):
    1. [Log into the Domo developer account.](https://developer.domo.com/login)
    2. In the top right corner under

CHANGE 13  [## FAQ]  (line 100 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    <Accordion title="My Account** click **New Client**.
    3. Enter the application name and description.
    4. Provide the application scope by selecting the checkboxes for **Data** and **User**.
    5. Click **Create**.
    6. Once you have created a client, you can manage the client by clicking on **Manage Client**.

CHANGE 14  [## FAQ]  (line 102 in repo file)

  NEW (replace with / insert from CSV):
    Your **Client Secret** will appear in the **Manage Client** section.
    
    **How frequently will my data update?">

CHANGE 15  [## FAQ]  (line 106 in repo file)

  NEW (replace with / insert from CSV):
    Your Domo input dataset id is in the URL of the dataset you are exporting data from. For example: https://customer.domo.com/datasources/
    </Accordion>

CHANGE 16  [## FAQ]  (lines 107–112 in repo file)

  OLD (remove from repo):
    Your Domo input dataset id is in the URL of the dataset you are exporting data from. For example:
    
    `https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings`
    </Accordion>
    </AccordionGroup>
    ## Troubleshooting

  NEW (replace with / insert from CSV):
    </AccordionGroup>## Troubleshooting

==============================================================================
FILE:   s/article/1500002644442.mdx
TITLE:  Azure DevOps Connector
LANG:   English (en_US)
ID:     1500002644442
==============================================================================

CHANGE 1  [## Connecting to Your Azure DevOps Account]  (line 21 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Azure DevOps Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Azure DevOps Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [## Credentials Pane]  (lines 27–48 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to sign into your Azure DevOps account. If this is an email address, the @subdomain portion may not be necessary.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Personal Access Token</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Personal Access Token that you can generate by going to <b>User Settings > Personal Access Token</b> in your <a href="https://dev.azure.com/">Azure DevOps</a> account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Organization</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Azure DevOps organization. For example, if you logged into your Azure DevOps instance at "https://dev.azure.com/domo", you would enter "domo" here.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Username | Enter the username you use to sign into your Azure DevOps account. If this is an email address, the @subdomain portion may not be necessary. |
    | Personal Access Token | Enter your Personal Access Token that you can generate by going to **User Settings > Personal Access Token** in your [Azure DevOps](https://dev.azure.com/) account. |
    | Organization | Enter your Azure DevOps organization. For example, if you logged into your Azure DevOps instance at "https://dev.azure.com/domo", you would enter "domo" here. |

CHANGE 3  [## Credentials Pane]  (line 50 in repo file)

  OLD (remove from repo):
    Once you have entered valid Azure DevOps credentials, you can use the same account any time you go to create a new Azure DevOps DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    Once you have entered valid Azure DevOps credentials, you can use the same account any time you go to create a new Azure DevOps DataSet. You can manage connector accounts in the **Accounts**tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 4  [## Details Pane]  (line 58 in repo file)

  OLD (remove from repo):
    | Report | Select the Azure DevOps report you want to run. The following reports are available: <table border="1"><tbody><tr><td><b>Report Name</b></td><td><b>Description</b></td></tr><tr><td>Areas</td><td>Returns the work item Area Paths, with properties for grouping and filtering by area hierarchy.</td></tr><tr><td>Boards</td><td>Returns the Kanban board cell locations, as identified by board column, swimlane, and split, includes historic board settings.</td></tr><tr><td>Fields</td><td>Retrieves fields from your project. Update mode supported: Replace Only</td></tr><tr><td>Iterations</td><td>Retrieves the work item Iteration Paths, with properties for grouping and filtering by iteration hierarchy.</td></tr><tr><td>Processes</td><td>Retrieves Backlog information used to expand or filter work items and work item types.</td></tr><tr><td>Profiles (Users)</td><td>Retrieves the User information.</td></tr><tr><td>Projects</td><td>Retrieves details about your projects. Update mode supported: Replace Only</td></tr><tr><td>Tags</td><td>Retrieves work item tags for the specified project.</td></tr><tr><td>Teams</td><td>Retrieves details about your teams. Update mode supported: Replace Only</td></tr><tr><td>Work Items</td><td>Retrieves details about your work items. Update mode supported: Merge Only</td></tr><tr><td>Work Item Links</td><td>Retrieves the links between work items.</td></tr><tr><td>Work Item Revisions</td><td>Retrieves all historic work item revisions, including the current revision.</td></tr><tr><td>Work Items with Custom Query</td><td>Retrieves details about your work items by customizing the query. Update mode supported: Merge Only</td></tr></tbody></table> |

  NEW (replace with / insert from CSV):
    | Report | Select the Azure DevOps report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Areas | Returns the work item Area Paths, with properties for grouping and filtering by area hierarchy. | | Boards | Returns the Kanban board cell locations, as identified by board column, swimlane, and split, includes historic board settings. | | Fields | Retrieves fields from your project. Update mode supported: Replace Only | | Iterations | Retrieves the work item Iteration Paths, with properties for grouping and filtering by iteration hierarchy. | | Processes | Retrieves Backlog information used to expand or filter work items and work item types. | | Profiles (Users) | Retrieves the User information. | | Projects | Retrieves details about your projects. Update mode supported: Replace Only | | Tags | Retrieves work item tags for the specified project. | | Teams | Retrieves details about your teams. Update mode supported: Replace Only | | Work Items | Retrieves details about your work items. Update mode supported: Merge Only | | Work Items Deleted | Retrieves the deleted work items from the Recycle Bin. | | Work Item Links | Retrieves the links between work items. | | Work Item Revisions | Retrieves All historic work item revisions, including the current revision. | | Work Items with Custom Query | Retrieves details about your work items by customizing the query. Update mode supported: Merge Only | |

CHANGE 5  [## Details Pane]  (line 60 in repo file)

  OLD (remove from repo):
    | Custom Query Params | Enter the query parameters that you want to provide while pulling the work items, separated by a newline. Example: `parameterName=value`. <Note>**Note:** The connector paginates the work items with the help of `System.ID` and `System.ChangedDate`, avoid providing them as the parameters.</Note> |

  NEW (replace with / insert from CSV):
    | Custom Query Params | Enter the query parameters that you want to provide while pulling the work items, separated by a newline. Example: parameterName=value.
    <Note>**Note:** The connector paginates the work items with the help of System.ID and System.ChangedDate, avoid providing them as the parameters. |

CHANGE 6  [## Details Pane]  (line 62 in repo file)

  OLD (remove from repo):
    | Exclude Kanban Fields | Select this checkbox if you want to exclude all the column names that contain Kanban in the field name. For example, if the field `WEF_3C7EF45428404E54BBDB86AE473EE9B3_Kanban.Column` or `WEF_3C7EF45428404E54BBDB86AE473EE9B3_Kanban.Column.Done` is available in Work items report, the connector will exclude those fields in your Work items report. |

  NEW (replace with / insert from CSV):
    | Exclude Kanban Fields | Select this checkbox if you want to exclude all the column names that contain Kanban in the field name. For example, if the field WEF\_3C7EF45428404E54BBDB86AE473EE9B3\_Kanban.Column or WEF\_3C7EF45428404E54BBDB86AE473EE9B3\_Kanban.Column.Done is available in Work items report; the connector will exclude those fields in your Work items report. |

CHANGE 7  [## Details Pane]  (line 66 in repo file)

  OLD (remove from repo):
    | Preserve Schema | Select this checkbox if you want to preserve the column names within a dataset. |

  NEW (replace with / insert from CSV):
    | Preserve Schema | Select this checkbox if you want to preserve the column names within a dataset. |</Note>

CHANGE 8  [## Details Pane]  (line 68 in repo file)

  OLD (remove from repo):
    <Note>**Note:** In the very first successful run, the ** Work Items** and ** Work Items with Custom Query** reports use the Backfill Date as the Start Date for retrieving all work items list.

  NEW (replace with / insert from CSV):
    <Note>**Note:** In the very first successful run, the **Work Items** and **Work Items with Custom Query** reports use the Backfill Date as the Start Date for retrieving all work items list.

==============================================================================
FILE:   s/article/1500009366622.mdx
TITLE:  Cornerstone OnDemand Connector
LANG:   English (en_US)
ID:     1500009366622
==============================================================================

CHANGE 1  [## Connecting to Your Cornerstone OnDemand Account]  (line 22 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the Credentials and Details panes in the Cornerstone OnDemand Connector page. The components of the other panes in this page, **Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the Credentials and Details panes in the Cornerstone OnDemand Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [### Credentials Pane]  (lines 28–57 in repo file)

  ⚠ LARGE CHANGE (37 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>API Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the API Key. It can be retrieved from the Manage API tab in Integration Center.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>API Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter the API Secret. It can be retrieved from the Manage API tab in Integration Center.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Corpname</p></td>
          <td colspan="1" rowspan="1"><p>Enter the client assigned hostname for your CornerStone OnDemand application (Corpname).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Environment</p></td>
          <td colspan="1" rowspan="1"><p>Select the environment (Pilot, Stage, or Production).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter a valid username associated with an active account in the portal.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter the API Key. It can be retrieved from the Manage API tab in Integration Center. |
    | API Secret | Enter the API Secret. It can be retrieved from the Manage API tab in Integration Center. |
    | Corpname | Enter the client assigned hostname for your CornerStone OnDemand application (Corpname). |
    | Environment | Select the environment (Pilot, Stage, or Production). |
    | Username | Enter a valid username associated with an active account in the portal. |

CHANGE 3  [### Details Pane]  (lines 65–86 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>API</p></td>
          <td colspan="1" rowspan="1"><p>Select the API you want to use to pull data from Cornerstone OnDemand.   - Reporting API - Data Exporter API</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Views (Reporting API)</p></td>
          <td colspan="1" rowspan="1"><p>This field appears when you select the <b>Reporting API</b>.  Select a view from the dropdown. The views that are accessible to you are dependent on your access level.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Views (Data Exporter API)</p></td>
          <td colspan="1" rowspan="1"><p>This field appears when you select the <b>Data Exporter API</b>.  Select the Object from the drop-down.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | API | Select the API you want to use to pull data from Cornerstone OnDemand.   - Reporting API - Data Exporter API |
    | Views (Reporting API) | This field appears when you select the **Reporting API**.  Select a view from the dropdown. The views that are accessible to you are dependent on your access level. |
    | Views (Data Exporter API) | This field appears when you select the **Data Exporter API**.  Select the Object from the drop-down. |

CHANGE 4  [## FAQs]  (line 94 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### What endpoint is the base URL for this connector?

CHANGE 5  [## FAQs]  (line 96 in repo file)

  OLD (remove from repo):
    <Accordion title="What endpoint is the base URL for this connector?">

CHANGE 6  [## FAQs]  (lines 99–121 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Environment</p></th>
          <th colspan="1" rowspan="1"><p>Base URL</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Pilot</p></td>
          <td colspan="1" rowspan="1"><p>https://{corpname}-pilot.csod.com/services/api/x/odata/api</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Stage</p></td>
          <td colspan="1" rowspan="1"><p>https://{corpname}-stg.csod.com/services/api/x/odata/api</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Production</p></td>
          <td colspan="1" rowspan="1"><p>https://{corpname}.csod.com/services/api/x/odata/api</p></td>
        </tr>
      </tbody>
    </table>
    </Accordion>

  NEW (replace with / insert from CSV):
    | Environment | Base URL |
    | --- | --- |
    | Pilot | https://{corpname}-pilot.csod.com/services/api/x/odata/api |
    | Stage | https://{corpname}-stg.csod.com/services/api/x/odata/api |
    | Production | https://{corpname}.csod.com/services/api/x/odata/api |

CHANGE 7  [## FAQs]  (line 123 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?
    

CHANGE 8  [## FAQs]  (line 125 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 9  [## FAQs]  (line 127 in repo file)

  OLD (remove from repo):
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?
    

CHANGE 10  [## FAQs]  (line 129 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 11  [## FAQs]  (line 131 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 12  [## FAQs]  (lines 133–135 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/1500011338741.mdx
TITLE:  Snowflake OAuth Writeback Connector
LANG:   English (en_US)
ID:     1500011338741
==============================================================================

CHANGE 1  [## Best Practices]  (line 15 in repo file)

  OLD (remove from repo):
    <Warning>**Important:** Snowflake asserts your need to manually re-connect your Domo account every time the account expires. The Snowflake integration controls how often the account will expire. If it is set to expire the refresh tokens every 90 days, then you will need to reconnect the account every 90 days. If you would like to increase the refresh timeout to more than 90 days, then you will need to contact the ** Snowflake Support**, and they can increase the maximum timeout from 90 days to 1 year.</Warning>

  NEW (replace with / insert from CSV):
    <Warning>**Important:** Snowflake asserts your need to manually re-connect your Domo account every time the account expires. The Snowflake integration controls how often the account will expire. If it is set to expire the refresh tokens every 90 days, then you will need to reconnect the account every 90 days. If you would like to increase the refresh timeout to more than 90 days, then you will need to contact the **Snowflake Support**, and they can increase the maximum timeout from 90 days to 1 year.</Warning>

CHANGE 2  [## Prerequisites]  (line 23 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345." <img alt="Region.png" src="/images/kb/0EM5w000005vO8p.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> - The Client ID and Client Secret provided by Snowflake when the client is registered.

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    
    
      <img alt="Region.png" src="/images/kb/0EM5w000005vO8p.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    - The Client ID and Client Secret provided by Snowflake when the client is registered.

CHANGE 3  [## Configuring the connection]  (line 36 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake OAuth Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake OAuth Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 42–67 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the account name for your Snowflake account. For information about finding your account name, see "Prerequisites."</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Client ID provided by Snowflake when the client is registered.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Client Secret provided by Snowflake when the client is registered.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the account name for your Snowflake account. For information about finding your account name, see "Prerequisites." |
    | Client ID | Enter your Client ID provided by Snowflake when the client is registered. |
    | Client Secret | Enter your Client Secret provided by Snowflake when the client is registered. |
    | Role | Enter your role. |
    

CHANGE 5  [### Details Pane]  (lines 74–119 in repo file)

  ⚠ LARGE CHANGE (58 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Input DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the DataSet ID (GUID) for the DataSet you want to copy to Snowflake. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to name the table where data will be copied.   - <b>Use Input DataSet GUID-</b> The table name will be the number you entered for <b> Input</b><b>DataSet ID</b>. - <b> Enter Table Name-</b> You will give the table a custom name in the <b> Custom Table Name</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the Snowflake table you want your data copied to (spaces and special characters will be replaced with underscores).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the table you want to send data to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the table you want to send data to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Schema</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Use All Caps for Column Names and Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Select 'True' if you want to write back your column names and table name in ALL CAPS so the names are case insensitive within Snowflake; otherwise, select 'False.'</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>My Domo Input DataSet is very large</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to export a very large DataSet from Domo to Snowflake. When this box is selected, the connector uses a different, faster process.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client session keep alive heart beat</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to reduce the default client session keep alive heart beat frequency.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Input DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to Snowflake. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4. |
    | Select Table Name | Select how you want to name the table where data will be copied.   - ****Use Input DataSet GUID-**** The table name will be the number you entered for **Input******DataSet ID****. - **Enter Table Name-** You will give the table a custom name in the ****Custom Table Name**** field. |
    | Enter Table Name | Enter the name of the Snowflake table you want your data copied to (spaces and special characters will be replaced with underscores). |
    | Warehouses | Select the warehouse containing the table you want to send data to. |
    | Databases | Select the database containing the table you want to send data to. |
    | Database Schema | Select the database schema containing the data you want to retrieve. |
    | Use All Caps for Column Names and Table Name | Select 'True' if you want to write back your column names and table name in ALL CAPS so the names are case insensitive within Snowflake; otherwise, select 'False.' |
    | My Domo Input DataSet is very large | Select this checkbox to export a very large DataSet from Domo to Snowflake. When this box is selected, the connector uses a different, faster process. |
    | Client session keep alive heart beat | Select this checkbox to reduce the default client session keep alive heart beat frequency. |
    

CHANGE 6  [## FAQs]  (line 126 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### When should I use this connector?

CHANGE 7  [## FAQs]  (line 128 in repo file)

  OLD (remove from repo):
    <Accordion title="When should I use this connector?">

CHANGE 8  [## FAQs]  (line 130 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 9  [## FAQs]  (line 132 in repo file)

  OLD (remove from repo):
    <Accordion title="I am receiving an error saying, 'OAuth access token expired'. Why?">

  NEW (replace with / insert from CSV):
    #### I am receiving an error saying, "OAuth access token expired." Why?

CHANGE 10  [## FAQs]  (line 134 in repo file)

  OLD (remove from repo):
    To avoid this error, you need to refresh the token. Snowflake asserts your need to manually re-connect your Domo account every time the account expires. The Snowflake integration controls how often the account will expire. If it is set to expire the refresh tokens every 90 days, then you will need to reconnect the account every 90 days. If you would like to increase the refresh timeout to more than 90 days, then you will need to contact the **Snowflake Support**, and they can increase the maximum timeout from 90 days to 1 year.

  NEW (replace with / insert from CSV):
    To avoid this error, you need to refresh the token. Snowflake asserts your need to manually re-connect your Domo account every time the account expires. The Snowflake integration controls how often the account will expire. If it is set to expire the refresh tokens every 90 days, then you will need to reconnect the account every 90 days. If you would like to increase the refresh timeout to more than 90 days, then you will need to contact the **Snowflake Support**, and they can increase the maximum timeout from 90 days to 1 year.

CHANGE 11  [## FAQs]  (lines 136–138 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?

CHANGE 12  [## FAQs]  (line 142 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Are there any API limits I should be aware of?

CHANGE 13  [## FAQs]  (line 144 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits I should be aware of?">

CHANGE 14  [## FAQs]  (line 146 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 15  [## FAQs]  (line 148 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 16  [## FAQs]  (line 150 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 17  [## FAQs]  (line 152 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I find the Input Dataset ID?">

  NEW (replace with / insert from CSV):
    #### How do I find the Input Dataset ID?
    

CHANGE 18  [## FAQs]  (line 155 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 19  [## FAQs]  (line 157 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?
    

CHANGE 20  [## FAQs]  (lines 159–161 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360042923434.mdx
TITLE:  SAP Concur Connector
LANG:   English (en_US)
ID:     360042923434
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    SAP Concur is a business expense and travel software platform. To learn more about SAP Concur's API, visit their page [https://developer.concur.com/api-reference/](https://developer.concur.com/api-reference/ "https://developer.concur.com/api-reference/").

  NEW (replace with / insert from CSV):
    SAP Concur is a business expense and travel software platform. To learn more about SAP Concur's API, visit their page [https://developer.concur.com/api-reference/](https://developer.concur.com/api-reference/).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You can connect to your SAP Concur account in the Data Center. This topic discusses the fields and menus that are specific to the SAP Concur connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    You can connect to your SAP Concur account in the Data Center. This topic discusses the fields and menus that are specific to the SAP Concur connector user interface. To add DataSets, set update schedules, and edit DataSet information, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (lines 15–17 in repo file)

  OLD (remove from repo):
    * Client ID
    * Client Secret
    * The username and password used to log into your SAP Concur account

  NEW (replace with / insert from CSV):
    - Client ID
    - Client Secret
    - The username and password used to log into your SAP Concur account

CHANGE 4  [## Prerequisites]  (line 19 in repo file)

  OLD (remove from repo):
    To obtain your Client ID and Secret, contact your SAP Concur Partner Enablement Manager or Partner Account Manager. Once you have registered an application, you will receive a Client ID and Client Secret. For more information on getting started with SAP Concur's API, see [https://developer.concur.com/api-reference/authentication/getting-started.html](https://developer.concur.com/api-reference/authentication/getting-started.html "https://developer.concur.com/api-reference/authentication/getting-started.html")

  NEW (replace with / insert from CSV):
    To obtain your Client ID and Secret, contact your SAP Concur Partner Enablement Manager or Partner Account Manager. Once you have registered an application, you will receive a Client ID and Client Secret. For more information on getting started with SAP Concur's API, see [https://developer.concur.com/api-reference/authentication/getting-started.html](https://developer.concur.com/api-reference/authentication/getting-started.html)

CHANGE 5  [## Connecting to Your SAP Concur Account]  (line 23 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the SAP Concur Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the SAP Concur Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in great detail in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 6  [### Credentials Pane]  (line 29 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Field </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr><tr><td colspan="1" rowspan="1"><p> Client ID </p></td><td colspan="1" rowspan="1"><p> Enter the Client ID obtained from registering your Concur App. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Client Secret </p></td><td colspan="1" rowspan="1"><p> Enter the Client Secret obtained from registering your Concur App. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Username </p></td><td colspan="1" rowspan="1"><p> Enter the username used to log into your Concur account. </p></td></tr><tr><td colspan="1" rowspan="1"><br/> Password <br/></td><td colspan="1" rowspan="1"> Enter the password used to log into your Concur account. </td></tr><tr><td colspan="1" rowspan="1"><span> Base URL Override (Advanced users only) </span></td><td colspan="1" rowspan="1"><p><span><b> Warning </b>: Do not enter data here if you do not understand how this will affect your API requests. Concur returns a URL to call API endpoints as a part of the Authentication process. If you would like to ignore the URL concur sends and use something else, enter it here. </span></p><p><span> Example: </span><a href="https://us.api.concursolutions.com" target="_blank" title="https://us.api.concursolutions.com"><span> https://us.api.concursolutions.com </span></a><span>. <br/> Click </span><a href="https://developer.concur.com/platform/base-uris.html" target="_blank" title="here"><span> here </span></a><span> for more information. </span></p></td></tr></thead></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter the Client ID obtained from registering your Concur App. |
    | Client Secret | Enter the Client Secret obtained from registering your Concur App. |
    | Username | Enter the username used to log into your Concur account. |
    | Password | Enter the password used to log into your Concur account. |
    | Base URL Override (Advanced users only) | **Warning**: Do not enter data here if you do not understand how this will affect your API requests. Concur returns a URL to call API endpoints as a part of the Authentication process. If you would like to ignore the URL concur sends and use something else, enter it here.  Example: [https://us.api.concursolutions.com](https://us.api.concursolutions.com) . Click [here](https://developer.concur.com/platform/base-uris.html)for more information. |

CHANGE 7  [### Credentials Pane]  (line 31 in repo file)

  OLD (remove from repo):
    Once you have entered valid SAP Concur credentials, you can use the same account any time to create a new SAP Concur DataSet. You can manage connector accounts in the Accounts tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054 "Managing User Accounts for Connectors").

  NEW (replace with / insert from CSV):
    Once you have entered valid SAP Concur credentials, you can use the same account any time to create a new SAP Concur DataSet. You can manage connector accounts in the Accounts tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 8  [### Details Pane]  (line 35 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 9  [### Details Pane]  (line 37 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the SAP Concur report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><th colspan="1" rowspan="1"> Report </th><th colspan="1" rowspan="1"> Description </th></tr><tr><td colspan="1" rowspan="1"> Company Card Transactions </td><td colspan="1" rowspan="1"> Receives a list of unassigned company card charges for the user. </td></tr><tr><td colspan="1" rowspan="1"> Entry Attendee Associations </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Entry Attendee Associations. </td></tr><tr><td colspan="1" rowspan="1"> Expense Allocations </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Allocations. </td></tr><tr><td colspan="1" rowspan="1"> Expense Attendees </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Attendees. </td></tr><tr><td colspan="1" rowspan="1"> Expense Attendee Types </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Attendee Types. </td></tr><tr><td colspan="1" rowspan="1"> Expense Delegators </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Delegators. </td></tr><tr><td colspan="1" rowspan="1"> Expense Entries </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Entries. </td></tr><tr><td colspan="1" rowspan="1"> Expense Form </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Form. </td></tr><tr><td colspan="1" rowspan="1"> Expense Group Configurations </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Group Configuration. </td></tr><tr><td colspan="1" rowspan="1"> Expense Itemizations </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Itemizations. </td></tr><tr><td colspan="1" rowspan="1"> Expense Reports </td><td colspan="1" rowspan="1"> Receives data about Concur Expense Reports. </td></tr><tr><td colspan="1" rowspan="1"> Expense Reports Detail </td><td colspan="1" rowspan="1"> Receives detailed data about Concur Expense Reports. </td></tr><tr><td colspan="1" rowspan="1"> Extract Definitions </td><td colspan="1" rowspan="1"> Receives data about Concur Extract Definitions. </td></tr><tr><td colspan="1" rowspan="1"> Extract Jobs </td><td colspan="1" rowspan="1"> Receives data about Concur Extract Jobs. </td></tr><tr><td colspan="1" rowspan="1"> List Items </td><td colspan="1" rowspan="1"> Receives data about Concur List Items. </td></tr><tr><td colspan="1" rowspan="1"> Locations </td><td colspan="1" rowspan="1"> Receives details of locations that are used by Concur and that are valid at the company of user. </td></tr><tr><td colspan="1" rowspan="1"> Receipt Images </td><td colspan="1" rowspan="1"> Receives data about Concur Receipt Image. </td></tr><tr><td colspan="1" rowspan="1"> Reports Detail </td><td colspan="1" rowspan="1"> (deprecated. Use Expense Report Details) Retrieves the full set of information for the reports. </td></tr><tr><td colspan="1" rowspan="1"> Travel Itineraries </td><td colspan="1" rowspan="1"> Receives data about Concur Travel Itineraries. </td></tr><tr><td colspan="1" rowspan="1"> User Information </td><td colspan="1" rowspan="1"> Receives data about Concur User Information. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Status </td><td colspan="1" rowspan="1"><p> When selecting the <b> Reports Detail </b> report, you must choose a report status. Options include: </p><ul><li><p> Active </p></li><li><p> Unsubmitted </p></li><li><p> Pending </p></li><li><p> Accounting Review </p></li><li><p> Processing </p></li><li><p> Integrated </p></li><li><p> Pending External Validation </p></li><li><p> Payment Confirmed </p></li></ul></td></tr><tr><td colspan="1" rowspan="1"> Definition </td><td colspan="1" rowspan="1"> When selecting the Extract Jobs report, choose the definition. </td></tr><tr><td colspan="1" rowspan="1"><span> User ID </span></td><td colspan="1" rowspan="1"> Enter the user ID. </td></tr><tr><td colspan="1" rowspan="1"><span> Approval Status </span></td><td colspan="1" rowspan="1"><span> Select to filter reports by the specified status. </span></td></tr><tr><td colspan="1" rowspan="1"><span> Date Filter </span></td><td colspan="1" rowspan="1"><span> Select the date field that will be used to filter the expense report. </span></td></tr><tr><td colspan="1" rowspan="1"><p> Duration </p></td><td colspan="1" rowspan="1"><p> Select whether you want to pull data for a specific date or a date range. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Report Date </p></td><td colspan="1" rowspan="1"><p> Select whether the report data is for a specific date or for a relative number of days back from today. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific Date </p></td><td colspan="1" rowspan="1"><p> Select the date for the report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Days Back </p></td><td colspan="1" rowspan="1"><p> Enter the number of past days that should appear in the report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Start Date </p></td><td colspan="1" rowspan="1"><p> Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b> End Date </b>. </p></td></tr><tr><td colspan="1" rowspan="1"><p> End Date </p></td><td colspan="1" rowspan="1"><p> Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in <b> Start Date </b>. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific Start Date </p></td><td colspan="1" rowspan="1"><p> Select the first date in your date range. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific End Date </p></td><td colspan="1" rowspan="1"><p> Select the second date in your date range. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Days Back to Start From </p></td><td colspan="1" rowspan="1"><p> Enter the number of the farthest day back that should be represented in the report. Combine with <b> Days Back to End At </b> to create a range of represented days. </p><p> For example, if you entered <span class="mt-font-courier-new"> 10 </span> for <b> Days Back to Start From </b> and <span class="mt-font-courier-new"> 5 </span> for <b> Days Back to End At </b>, the report would contain data for 10 days ago up until 5 days ago. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Days Back to End At </p></td><td colspan="1" rowspan="1"><p> Enter the number of the most recent day back that should be represented in the report. Combine with <b> Days Back to Start From </b> to create a range of represented days. </p><p> For example, if you entered <span class="mt-font-courier-new"> 10 </span> for <b> Days Back to Start From </b> and <span class="mt-font-courier-new"> 5 </span> for <b> Days Back to End At </b>, the report would contain data for 10 days ago up until 5 days ago. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the SAP Concur report you want to run. The following reports are available:   | Report | Description | | --- | --- | | Company Card Transactions | Receives a list of unassigned company card charges for the user. | | Entry Attendee Associations | Receives data about Concur Expense Entry Attendee Associations. | | Expense Allocations | Receives data about Concur Expense Allocations. | | Expense Attendees | Receives data about Concur Expense Attendees. | | Expense Attendee Types | Receives data about Concur Expense Attendee Types. | | Expense Delegators | Receives data about Concur Expense Delegators. | | Expense Entries | Receives data about Concur Expense Entries. | | Expense Form | Receives data about Concur Expense Form. | | Expense Group Configurations | Receives data about Concur Expense Group Configuration. | | Expense Itemizations | Receives data about Concur Expense Itemizations. | | Expense Reports | Receives data about Concur Expense Reports. | | Expense Reports Detail | Receives detailed data about Concur Expense Reports. | | Extract Definitions | Receives data about Concur Extract Definitions. | | Extract Jobs | Receives data about Concur Extract Jobs. | | Invoice Digests V3 | Retrieves all invoice digests based on the search criteria. | | Invoice Vendors | Retrieves all vendors associated with a specific entity. | | Invoice Sales Tax Validation V3 | Retrieves invoices for calculating tax. | | List Items | Receives data about Concur List Items. | | Locations | Receives details of locations that are used by Concur and that are valid at the company of user. | | Receipt Images | Receives data about Concur Receipt Image. | | Reports Detail | (deprecated. Use Expense Report Details) Retrieves the full set of information for the reports. | | Travel Itineraries | Receives data about Concur Travel Itineraries. | | User Information | Receives data about Concur User Information. | |
    | Status | When selecting the **Reports Detail** report, you must choose a report status. Options include:   - Active - Unsubmitted - Pending - Accounting Review - Processing - Integrated - Pending External Validation - Payment Confirmed |
    | Definition | When selecting the Extract Jobs report, choose the definition. |
    | User ID | Enter the user ID. |
    | Approval Status | Select to filter reports by the specified status. |
    | Date Filter | Select the date field that will be used to filter the expense report. |
    | Duration | Select whether you want to pull data for a specific date or a date range. |
    | Report Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date | Select the date for the report. |
    | Days Back | Enter the number of past days that should appear in the report. |
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |
    | Select Specific End Date | Select the second date in your date range. |
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |

CHANGE 10  [### Other Panes]  (line 41 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 11  [## FAQ]  (lines 45–46 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="What credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    ##### What credentials do I need to power up this connector?

CHANGE 12  [## FAQ]  (lines 50–52 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How do I know that my SAP Concur credentials are secure?">

  NEW (replace with / insert from CSV):
    ##### How do I know that my SAP Concur credentials are secure?

CHANGE 13  [## FAQ]  (lines 56–58 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How frequently can the data be updated?">

  NEW (replace with / insert from CSV):
    ##### How frequently can the data be updated?

CHANGE 14  [## FAQ]  (lines 62–64 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How does my user role affect the data returned in the SAP Concur connector?">

  NEW (replace with / insert from CSV):
    ##### How does my user role affect the data returned in the SAP Concur connector?

CHANGE 15  [## FAQ]  (lines 68–70 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Can I use the same account for generating different reports?">

  NEW (replace with / insert from CSV):
    ##### Can I use the same account for generating different reports?

CHANGE 16  [## FAQ]  (lines 73–76 in repo file)

  OLD (remove from repo):
    
    </Accordion>
    </AccordionGroup>
    

==============================================================================
FILE:   s/article/360042926314.mdx
TITLE:  Act-On Connector
LANG:   English (en_US)
ID:     360042926314
==============================================================================

CHANGE 1  [## Details Pane]  (line 35 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Act-On report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p> All Contacts Details </p></td><td colspan="1" rowspan="1"><div><div><div> Returns the details of Contacts using List API. </div></div></div></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Message </p></td><td colspan="1" rowspan="1"><p> Returns campaign messages. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Message List All Types </p></td><td colspan="1" rowspan="1"><p> Returns a campaign messages list of all types. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Message Report </p></td><td colspan="1" rowspan="1"><p> Returns campaign message report data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Message Report Drilldown </p></td><td colspan="1" rowspan="1"><p> Returns a campaign message drilldown report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Message Rollup </p></td><td colspan="1" rowspan="1"><p> Returns campaign message rollup data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Contact API Subscription Category </p></td><td colspan="1" rowspan="1"><p> Returns a list of contact API subscription categories. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Contact By Email Id </p></td><td colspan="1" rowspan="1"><div><div><div> Returns contacts from the list for the entered email Id. </div></div></div></td></tr><tr><td colspan="1" rowspan="1"><p> Contact Fact and Score Data </p></td><td colspan="1" rowspan="1"><div><div><div><div><div><div> Returns the facts and score the available contacts in the list. </div></div></div></div></div></div></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Form List </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API forms. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Form Report </p></td><td colspan="1" rowspan="1"><p> Returns content API form report data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Header Fetch </p></td><td colspan="1" rowspan="1"><p> Returns content API header fetch data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Header List </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API headers. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Image Fetch </p></td><td colspan="1" rowspan="1"><p> Returns content API image fetch data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Image List </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API images. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Logo List </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API logos. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Media List </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API media. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Media Message Drilldown </p></td><td colspan="1" rowspan="1"><p> Returns content API media message drilldown data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Media Rollup Report </p></td><td colspan="1" rowspan="1"><p> Returns content API media rollup report data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Media Timeline Drilldown </p></td><td colspan="1" rowspan="1"><p> Returns content API media timeline drilldown data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Media Views Drilldown </p></td><td colspan="1" rowspan="1"><p> Returns content API media views drilldown data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Page List </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API pages. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Page Report </p></td><td colspan="1" rowspan="1"><p> Returns content API page report data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Page URLs </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API page URLs. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Content API Promotional URLs </p></td><td colspan="1" rowspan="1"><p> Returns a list of content API promotional URLs. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Fact API Contact Record Facts </p></td><td colspan="1" rowspan="1"><p> Returns a list of fact API content record facts. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Fact API List Optout </p></td><td colspan="1" rowspan="1"><p> Returns fact API list optout data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List API </p></td><td colspan="1" rowspan="1"><p> Returns list API data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List API Hard Bounce </p></td><td colspan="1" rowspan="1"><p> Returns list API hard bounce data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List API Spam Complaint </p></td><td colspan="1" rowspan="1"><p> Returns list API spam complaint data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Messages By Campaign </p></td><td colspan="1" rowspan="1"><div><div><div> Returns the messages reports related to the selected campaign. </div></div></div></td></tr><tr><td colspan="1" rowspan="1"><p> SEO API Keywords </p></td><td colspan="1" rowspan="1"><p> Returns a list of SEO API keywords. </p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Email Id </p></td><td colspan="1" rowspan="1"><p> Enter your email address to get all available contacts from your lists. </p><p><b> Note: </b> You can find your email address in the <b> E-mail Address </b> column of the All contacts details report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Fetch contacts for List IDs </p></td><td colspan="1" rowspan="1"><table><tbody><tr><th colspan="1" rowspan="1"> Parameter </th><th colspan="1" rowspan="1"> Description </th></tr><tr><td colspan="1" rowspan="1"> All Source IDs </td><td colspan="1" rowspan="1"> Returns the contact details of all present source IDs. </td></tr><tr><td colspan="1" rowspan="1"> Specifc Source Ids </td><td colspan="1" rowspan="1"> Returns the contact details of selected Source IDs. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Source Id </p></td><td colspan="1" rowspan="1"> Select the SourceIds associated with the account. </td></tr><tr><td colspan="1" rowspan="1"><p> Campaigns </p></td><td colspan="1" rowspan="1"> Select the Campaign associated with the account. </td></tr><tr><td colspan="1" rowspan="1"><p> Option to Expand </p></td><td colspan="1" rowspan="1"><table><tbody><tr><td colspan="1" rowspan="1"><b> Parameters </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"> Score Details </td><td colspan="1" rowspan="1"> Expands the list of score details of the specific contacts. </td></tr><tr><td colspan="1" rowspan="1"> List of Activities </td><td colspan="1" rowspan="1"> Expands the list of activities for the contacts. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Listing Type </p></td><td colspan="1" rowspan="1"><p> Select the type of content list you want to pull into your report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Period </p></td><td colspan="1" rowspan="1"><p> Select a filter for your data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Date </p></td><td colspan="1" rowspan="1"><p> Select the date for your report. Note that this is a monthly report so only the month and year will be used. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List Id Selection Criteria </p></td><td colspan="1" rowspan="1"><p> Select whether to retrieve data for all or some list IDs. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List ID </p></td><td colspan="1" rowspan="1"><p> Select the list IDs to report. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Act-On report you want to run. The following reports are available:   |  |  | | --- | --- | | All Contacts Details | Returns the details of Contacts using List API. | | Campaign Message | Returns campaign messages. | | Campaign Message List All Types | Returns a campaign messages list of all types. | | Campaign Message Report | Returns campaign message report data. | | Campaign Message Report Drilldown | Returns a campaign message drilldown report. | | Campaign Message Rollup | Returns campaign message rollup data. | | Contact API Subscription Category | Returns a list of contact API subscription categories. | | Contact By Email Id | Returns contacts from the list for the entered email Id. | | Contact Fact and Score Data | Returns the facts and score the available contacts in the list. | | Content API Form List | Returns a list of content API forms. | | Content API Form Report | Returns content API form report data. | | Content API Header Fetch | Returns content API header fetch data. | | Content API Header List | Returns a list of content API headers. | | Content API Image Fetch | Returns content API image fetch data. | | Content API Image List | Returns a list of content API images. | | Content API Logo List | Returns a list of content API logos. | | Content API Media List | Returns a list of content API media. | | Content API Media Message Drilldown | Returns content API media message drilldown data. | | Content API Media Rollup Report | Returns content API media rollup report data. | | Content API Media Timeline Drilldown | Returns content API media timeline drilldown data. | | Content API Media Views Drilldown | Returns content API media views drilldown data. | | Content API Page List | Returns a list of content API pages. | | Content API Page Report | Returns content API page report data. | | Content API Page URLs | Returns a list of content API page URLs. | | Content API Promotional URLs | Returns a list of content API promotional URLs. | | Fact API Contact Record Facts | Returns a list of fact API content record facts. | | Fact API List Optout | Returns fact API list optout data. | | List API | Returns list API data. | | List API Hard Bounce | Returns list API hard bounce data. | | List API Spam Complaint | Returns list API spam complaint data. | | Messages By Campaign | Returns the messages reports related to the selected campaign. | | SEO API Keywords | Returns a list of SEO API keywords. | |
    | Email Id | Enter your email address to get all available contacts from your lists.
    <Note>**Note:** You can find your email address in the **E-mail Address** column of the All contacts details report. |
    | Fetch contacts for List IDs | | Parameter | Description | | --- | --- | | All Source IDs | Returns the contact details of all present source IDs. | | Specifc Source Ids | Returns the contact details of selected Source IDs. | |
    | Source Id | Select the SourceIds associated with the account. |
    | Campaigns | Select the Campaign associated with the account. |
    | Option to Expand | |  |  | | --- | --- | | **Parameters** | **Description** | | Score Details | Expands the list of score details of the specific contacts. | | List of Activities | Expands the list of activities for the contacts. | |
    | Listing Type | Select the type of content list you want to pull into your report. |
    | Period | Select a filter for your data. |
    | Date | Select the date for your report. Note that this is a monthly report so only the month and year will be used. |
    | List Id Selection Criteria | Select whether to retrieve data for all or some list IDs. |
    | List ID | Select the list IDs to report. |</Note>

==============================================================================
FILE:   s/article/360042926694.mdx
TITLE:  DCM via Google Cloud Storage Connector
LANG:   English (en_US)
ID:     360042926694
==============================================================================

CHANGE 1  [(top of file)]  (lines 5–6 in repo file)

  OLD (remove from repo):
    <Warning>
    **Important:** Due to Google’s change in allowing Domo connectors to use OAuth authentication, all users must now use service account key authentication to create new data connections. All pre-existing DataSets will continue to run normally. Current Connectors that rely on Google OAuth authentication will no longer be able to power up new DataSets. These connectors include the following:

  NEW (replace with / insert from CSV):
    <Warning>**Important:** Due to Google’s change in allowing Domo connectors to use OAuth authentication, all users must now use service account key authentication to create new data connections. All pre-existing DataSets will continue to run normally. Current Connectors that rely on Google OAuth authentication will no longer be able to power up new DataSets. These connectors include the following:</Warning>

CHANGE 2  [(top of file)]  (lines 8–15 in repo file)

  OLD (remove from repo):
    * Google BigQuery
    * Google Adwords via Google BigQuery
    * [DCM via Google Cloud Storage (DTV2)](/s/article/360042926694 "DCM via Google Cloud Storage (DTV2) Connector")
    * [Google Cloud Storage](/s/article/360043433313 "Google Cloud Storage")
    * [Google Contacts](/s/article/360043435173 "Google Contacts")
    * Google Drive
    * [Google Gmail](/s/article/360042930594 "Google Gmail")
    * [Google Admin SDK Reports](/s/article/360042928714 "Google Admin SDK Reports")

  NEW (replace with / insert from CSV):
    - [Google BigQuery](/s/article/360042928734)
    - [Google Adwords via Google BigQuery](/s/article/360043431293)
    - [DCM via Google Cloud Storage (DTV2)](/s/article/360042926694)
    - [Google Cloud Storage](/s/article/360043433313)
    - [Google Contacts](/s/article/360043435173)
    - [Google Drive](/s/article/360043436613)
    - [Google Gmail](/s/article/360042930594)
    - [Google Admin SDK Reports](/s/article/360042928714)

CHANGE 3  [(top of file)]  (lines 19–25 in repo file)

  OLD (remove from repo):
    * [Google BigQuery Service](/s/article/360043436593 "Google BigQuery Service")
    * [Google Ads (fomerly AdWords) via Google BigQuery Service](/s/article/360043431253 "Google Ads (fomerly AdWords) via Google BigQuery Service")
    * [Google Drive Service](/s/article/360042932014 "Google Drive Service")
    * [Google Cloud Storage Service](/s/article/360042928774 "Google Cloud Storage Service")
    * [Google Contacts Service](/s/article/360042930574 "Google Contacts Service")
    * [Google Gmail Service](/s/article/360043435193 "Google Gmail Service")
    * [Google Admin SDK Reports Service](/s/article/360043433273 "Google Admin SDK Reports Service")

  NEW (replace with / insert from CSV):
    - [Google BigQuery Service](/s/article/360043436593)
    - [Google Ads (fomerly AdWords) via Google BigQuery Service](/s/article/360043431253)
    - [Google Drive Service](/s/article/360042932014)
    - [Google Cloud Storage Service](/s/article/360042928774)
    - [Google Contacts Service](/s/article/360042930574)
    - [Google Gmail Service](/s/article/360043435193)
    - [Google Admin SDK Reports Service](/s/article/360043433273)

CHANGE 4  [(top of file)]  (line 27 in repo file)

  OLD (remove from repo):
    All other equivalent Google Service Account connectors are currently in development. Domo will communicate when these connectors become available. You will need to look for the Service Account equivalent connector in Data Center or the App Store and use that connector to get their data.

  NEW (replace with / insert from CSV):
    All other equivalent Google Service Account connectors are currently in development. Domo will communicate when these connectors become available. You will need to look for the Service Account equivalent connector in Data Center or the App Store and use that connector to get their data.

CHANGE 5  [(top of file)]  (lines 29–30 in repo file)

  OLD (remove from repo):
    Domo recommends that you proactively repower existing Google connections by creating new DataSets utilizing the new Service Account Key connectors as the connectors become available.  Domo will notify you if any further migration changes should arise.
    </Warning>

  NEW (replace with / insert from CSV):
    Domo recommends that you proactively repower existing Google connections by creating new DataSets utilizing the new Service Account Key connectors as the connectors become available.  Domo will notify you if any further migration changes should arise.

CHANGE 6  [## Intro]  (line 34 in repo file)

  OLD (remove from repo):
    Google Cloud Storage is an Internet service used to store data in Google's cloud. You can use Domo’s DCM via Google Cloud Storage Connector to retrieve DoubleClick Campaign Manager and DoubleClick Bid Manager data. The connector brings in impression-level data and is able to join the data automatically. To learn more about the DoubleClick Campaign Manager API, visit their page ([https://developers.google.com/double...k-advertisers/](https://developers.google.com/doubleclick-advertisers/ "https://developers.google.com/doubleclick-advertisers/") ).

  NEW (replace with / insert from CSV):
    Google Cloud Storage is an Internet service used to store data in Google's cloud. You can use Domo’s DCM via Google Cloud Storage Connector to retrieve DoubleClick Campaign Manager and DoubleClick Bid Manager data. The connector brings in impression-level data and is able to join the data automatically. To learn more about the DoubleClick Campaign Manager API, visit their page ( [https://developers.google.com/double...k-advertisers/](https://developers.google.com/doubleclick-advertisers/) ).

CHANGE 7  [## Intro]  (lines 36–38 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:** The user interface for building this connector is not yet available in our Connectors listing in the Data Center, meaning you will not be able to configure the connector on your own. However, we at Domo are more than happy to configure the connector for you so you can begin using it to pull your data into Domo. Please don't hesitate to reach out to your CSM or Domo Support.
    </Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** The user interface for building this connector is not yet available in our Connectors listing in the Data Center, meaning you will not be able to configure the connector on your own. However, we at Domo are more than happy to configure the connector for you so you can begin using it to pull your data into Domo. Please don't hesitate to reach out to your CSM or Domo Support.</Note>

CHANGE 8  [## Intro]  (lines 40–85 in repo file)

  ⚠ LARGE CHANGE (53 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table class="mt-responsive-table" data-aura-rendered-by="33:207;a"><tbody><tr><td class="mt-noheading" rowspan="1" colspan="1">
    <p><strong>Primary Use Cases</strong></p>
    </td><td class="mt-noheading" rowspan="1" colspan="1">
    <p>This connector pulls DCM tables from Google Cloud Storage into Domo. If you indicate that you want match tables included, these tables will be joined.</p>
    </td></tr><tr><td class="mt-noheading" rowspan="1" colspan="1">
    <p><strong>Primary Metrics</strong></p>
    </td><td class="mt-noheading" rowspan="1" colspan="1">
    <p>The connector brings in raw data, but you can aggregate the data to show metrics such as...</p>
    <ul><li>
    <p>Impressions</p>
    </li><li>
    <p>Clicks</p>
    </li><li>
    <p>CTR</p>
    </li><li>
    <p>Rich media delivered</p>
    </li><li>
    <p>Activities for DCM campaigns</p>
    </li></ul>
    </td></tr><tr><td class="mt-noheading" rowspan="1" colspan="1">
    <p><strong>Primary Company Roles</strong></p>
    </td><td class="mt-noheading" rowspan="1" colspan="1">
    <ul><li>
    <p>Data specialists</p>
    </li><li>
    <p>Analysts</p>
    </li><li>
    <p>Major Domos</p>
    </li></ul>
    </td></tr><tr><td class="mt-noheading" rowspan="1" colspan="1">
    <p><strong>Average Implementation Time</strong></p>
    </td><td class="mt-noheading" rowspan="1" colspan="1">
    <p>Average time for the phases of implementation for this connector are as follows:</p>
    <ul><li>
    <p>Configuring an export of your 4 tables to Google Cloud Storage - 2 hours</p>
    </li><li>
    <p>Configuring credentials in Domo - 15 minutes or less</p>
    </li><li>
    <p>Configuring the connector - 20 minutes</p>
    </li></ul>
    <p>The total time may also depend on the size of the files coming from DCM.</p>
    </td></tr><tr><td class="mt-noheading" rowspan="1" colspan="1">
    <p><strong>Ease of Use (on a 1-to-10 scale with 1 being easiest)</strong></p>
    </td><td class="mt-noheading" rowspan="1" colspan="1">
    <p>7</p>
    </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector pulls DCM tables from Google Cloud Storage into Domo. If you indicate that you want match tables included, these tables will be joined. |
    | **Primary Metrics** | The connector brings in raw data, but you can aggregate the data to show metrics such as...   - Impressions - Clicks - CTR - Rich media delivered - Activities for DCM campaigns |
    | **Primary Company Roles** | - Data specialists - Analysts - Major Domos |
    | **Average Implementation Time** | Average time for the phases of implementation for this connector are as follows:   - Configuring an export of your 4 tables to Google Cloud Storage - 2 hours - Configuring credentials in Domo - 15 minutes or less - Configuring the connector - 20 minutes   The total time may also depend on the size of the files coming from DCM. |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 7 |

CHANGE 9  [## Intro]  (line 87 in repo file)

  OLD (remove from repo):
    You can connect to your DoubleClick Campaign Manager account in the Data Center. This topic discusses the fields and menus that are specific to the DoubleClick Campaign Manager connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    You can connect to your DoubleClick Campaign Manager account in the Data Center. This topic discusses the fields and menus that are specific to the DoubleClick Campaign Manager connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 10  [## Prerequisites]  (line 91 in repo file)

  OLD (remove from repo):
    To connect to a Google account and create a DataSet, you must have the username and password for the account you want to connect to.

  NEW (replace with / insert from CSV):
    To connect to a Google account and create a DataSet, you must have the username and password for the account you want to connect to.

CHANGE 11  [## Prerequisites]  (lines 95–98 in repo file)

  OLD (remove from repo):
    * Activities
    * Clicks
    * Impressions
    * Rich Media

  NEW (replace with / insert from CSV):
    - Activities
    - Clicks
    - Impressions
    - Rich Media

CHANGE 12  [## Prerequisites]  (line 102 in repo file)

  OLD (remove from repo):
    [https://developers.google.com/doubleclick-advertisers/udt/reference/v1/file-format](https://developers.google.com/doubleclick-advertisers/udt/reference/v1/file-format "https://developers.google.com/doubleclick-advertisers/udt/reference/v1/file-format")

  NEW (replace with / insert from CSV):
    [https://developers.google.com/doubleclick-advertisers/udt/reference/v1/file-format](https://developers.google.com/doubleclick-advertisers/udt/reference/v1/file-format)

CHANGE 13  [## Prerequisites]  (line 106 in repo file)

  OLD (remove from repo):
    ## Connecting to Your DCM via Google Cloud Storage Account

  NEW (replace with / insert from CSV):
    ## Connecting to Your DCM via Google Cloud Storage Account

CHANGE 14  [## Connecting to Your DCM via Google Cloud Storage Account]  (line 108 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the DCM via Google Cloud Storage Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the DCM via Google Cloud Storage Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 15  [### Credentials Pane]  (line 112 in repo file)

  OLD (remove from repo):
    The Domo DCM via Google Cloud Storage connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing DCM via Google Cloud Storage accounts in Domo) to open the Google OAuth screen where you can enter your Google credentials. Once you have entered valid Google credentials, you can use the same account any time you go to create a new DCM via Google Cloud Storage DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](https://domo-support.domo.com/s/article/360042926054?language=en_US "Managing User Accounts for Connectors").

  NEW (replace with / insert from CSV):
    The Domo DCM via Google Cloud Storage connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing DCM via Google Cloud Storage accounts in Domo) to open the Google OAuth screen where you can enter your Google credentials. Once you have entered valid Google credentials, you can use the same account any time you go to create a new DCM via Google Cloud Storage DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 16  [### Credentials Pane]  (lines 114–116 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:** If you are already logged into Google when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Google.
    </Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into Google when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Google.</Note>

CHANGE 17  [### Details Pane]  (line 120 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 18  [### Details Pane]  (line 122 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:201;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the DCM via Google Cloud Storage report you want to run. The Master Report pulls all 4 DCM tables into a single report. (see <a href="#h_34407b13-454e-4a76-b215-974ff29a27ab" target="_blank" title="Prerequisites"> Prerequisites </a> for more information about these tables). All other reports allow you to retrieve individual DCM tables, with or without match tables. Match tables are used to perform joins between tables. </p><p> The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"> Activities Raw Data </td><td colspan="1" rowspan="1"> Retrieves the raw data from your DCM Activities table. </td></tr><tr><td colspan="1" rowspan="1"> Activities Report with Match Tables </td><td colspan="1" rowspan="1"> Retrieves your DCM Activities data with match tables. </td></tr><tr><td colspan="1" rowspan="1"> Clicks Raw Data </td><td colspan="1" rowspan="1"> Retrieves the raw data from your DCM Clicks table. </td></tr><tr><td colspan="1" rowspan="1"> Clicks Report with Match Tables </td><td colspan="1" rowspan="1"> Retrieves your DCM Clicks data with match tables. </td></tr><tr><td colspan="1" rowspan="1"> Impressions Raw Data </td><td colspan="1" rowspan="1"> Retrieves the raw data from your DCM Impressions table. </td></tr><tr><td colspan="1" rowspan="1"> Impressions Report with Match Tables </td><td colspan="1" rowspan="1"> Retrieves your DCM Impressions data with match tables. </td></tr><tr><td colspan="1" rowspan="1"> Master Report </td><td colspan="1" rowspan="1"> Compiles a report with data from all of your DCM tables—Activities, Clicks, Impressions, and Rich Media. </td></tr><tr><td colspan="1" rowspan="1"> Rich Media Raw Data </td><td colspan="1" rowspan="1"> Retrieves the raw data from your DCM Rich Media table. </td></tr><tr><td colspan="1" rowspan="1"> Rich Media Report with Match Tables </td><td colspan="1" rowspan="1"> Retrieves your DCM Rich Media data with match tables. </td></tr></tbody></table><p></p></td></tr><tr><td colspan="1" rowspan="1"> Bucket Name </td><td colspan="1" rowspan="1"> Enter the name of the bucket in Google Cloud Storage where the DCM files are stored. </td></tr><tr><td colspan="1" rowspan="1"> Backfill Days for Initial Run </td><td colspan="1" rowspan="1"> Enter the number of past days you want to retrieve data for on the first run. Depending on the file size, you may not be able to backfill very many days. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the DCM via Google Cloud Storage report you want to run. The Master Report pulls all 4 DCM tables into a single report. (see [Prerequisites](#h_34407b13-454e-4a76-b215-974ff29a27ab)for more information about these tables). All other reports allow you to retrieve individual DCM tables, with or without match tables. Match tables are used to perform joins between tables.  The following reports are available:   |  |  | | --- | --- | | Activities Raw Data | Retrieves the raw data from your DCM Activities table. | | Activities Report with Match Tables | Retrieves your DCM Activities data with match tables. | | Clicks Raw Data | Retrieves the raw data from your DCM Clicks table. | | Clicks Report with Match Tables | Retrieves your DCM Clicks data with match tables. | | Impressions Raw Data | Retrieves the raw data from your DCM Impressions table. | | Impressions Report with Match Tables | Retrieves your DCM Impressions data with match tables. | | Master Report | Compiles a report with data from all of your DCM tables—Activities, Clicks, Impressions, and Rich Media. | | Rich Media Raw Data | Retrieves the raw data from your DCM Rich Media table. | | Rich Media Report with Match Tables | Retrieves your DCM Rich Media data with match tables. | |
    | Bucket Name | Enter the name of the bucket in Google Cloud Storage where the DCM files are stored. |
    | Backfill Days for Initial Run | Enter the number of past days you want to retrieve data for on the first run. Depending on the file size, you may not be able to backfill very many days. |

CHANGE 19  [### Other Panes]  (line 126 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 20  [## Troubleshooting]  (lines 130–132 in repo file)

  OLD (remove from repo):
    * Make sure you have the right bucket name and the report desired.
    * Make sure you have exported the required tables from DCM to Google Cloud Storage. (See
      [Prerequisites](#h_34407b13-454e-4a76-b215-974ff29a27ab "Prerequisites") for more information.)

  NEW (replace with / insert from CSV):
    - Make sure you have the right bucket name and the report desired.
    - Make sure you have exported the required tables from DCM to Google Cloud Storage. (See [Prerequisites](#h_34407b13-454e-4a76-b215-974ff29a27ab)for more information.)

==============================================================================
FILE:   s/article/360042926754.mdx
TITLE:  Facebook Ads Advanced Connector
LANG:   English (en_US)
ID:     360042926754
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Marketing experts use Facebook Ads to create targeted ads designed to reach different audiences and help them meet their business goals. To learn more about the Facebook API, visit their page ([https://developers.facebook.com/docs/marketing-apis](https://developers.facebook.com/docs/marketing-apis "https://developers.facebook.com/docs/marketing-apis") ).

  NEW (replace with / insert from CSV):
    Marketing experts use Facebook Ads to create targeted ads designed to reach different audiences and help them meet their business goals. To learn more about the Facebook API, visit their page ([https://developers.facebook.com/docs/marketing-apis](https://developers.facebook.com/docs/marketing-apis)).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    This Advanced version of the Facebook Ads connector allows you to access more reports than the standard Facebook Ads connector but also requires more configuration. For information about the simplified version, see [Facebook Ads Connector](https://domo-support.domo.com/s/article/360043431213?language=en_US "Facebook Ads Connector").

  NEW (replace with / insert from CSV):
    This Advanced version of the Facebook Ads connector allows you to access more reports than the standard Facebook Ads connector but also requires more configuration. For information about the simplified version, see [Facebook Ads Connector](/s/article/360043431213).

CHANGE 3  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    You connect to your Facebook account in the Data Center. This topic discusses the fields and menus that are specific to the Facebook Ads connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You connect to your Facebook account in the Data Center. This topic discusses the fields and menus that are specific to the Facebook Ads connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 4  [## Intro]  (line 13 in repo file)

  OLD (remove from repo):
    <table class="mt-responsive-table" data-aura-rendered-by="33:201;a"><tbody><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Use Cases </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><ul><li><p> Tracking how many people view individual ads </p></li><li><p> Tracking how many people are reached by each campaign </p></li><li><p> Tracking how much money has been spent on campaigns </p></li></ul></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Metrics </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><ul><li><p> Reach </p></li><li><p> Spend </p></li><li><p> Cost per Lead </p></li><li><p> Leads generated </p></li><li><p> Impressions </p></li><li><p> Clicks </p></li><li><p> Cost per Click </p></li></ul></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Company Roles </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> Online marketers </p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Average Implementation Time </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> 1-2 hours </p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Ease of Use (on a 1-to-10 scale with 1 being easiest) </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> 4 </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | - Tracking how many people view individual ads - Tracking how many people are reached by each campaign - Tracking how much money has been spent on campaigns |
    | **Primary Metrics** | - Reach - Spend - Cost per Lead - Leads generated - Impressions - Clicks - Cost per Click |
    | **Primary Company Roles** | Online marketers |
    | **Average Implementation Time** | 1-2 hours |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 4 |

CHANGE 5  [## Best Practices]  (lines 17–22 in repo file)

  OLD (remove from repo):
    * The Facebook Ads connector provides ready-made reports that you can use right away. The "Custom Insights" report is the most flexible report in this connector. It allows you the most control over the reports you generate.
    * This connector can take a long time to run if the date range is very long and/or the account selected has a large number of campaigns or ads. The long run time occurs because Facebook is taking a long time to calculate the selected metrics. If the DataSet is taking too long or doesn't complete, the date range needs to be smaller.
    * Metrics for ads placed through Instagram can be found by using the "Custom Insights" report and setting the
      **Breakdown** value to **Placement**. Instagram is then listed as one of the placement values in the results.
    * You can also return metrics for Instagram video views. This is discussed later in this article, under
      [Instagram Video Views](#FacebookAdsFeasibility-InstagramVideoViews "Facebook Ads Advanced Connector").

  NEW (replace with / insert from CSV):
    - The Facebook Ads connector provides ready-made reports that you can use right away. The "Custom Insights" report is the most flexible report in this connector. It allows you the most control over the reports you generate.
    - This connector can take a long time to run if the date range is very long and/or the account selected has a large number of campaigns or ads. The long run time occurs because Facebook is taking a long time to calculate the selected metrics. If the DataSet is taking too long or doesn't complete, the date range needs to be smaller.
    - Metrics for ads placed through Instagram can be found by using the "Custom Insights" report and setting the **Breakdown** value to **Placement**. Instagram is then listed as one of the placement values in the results.
    - You can also return metrics for Instagram video views. This is discussed later in this article, under [Instagram Video Views](#FacebookAdsFeasibility-InstagramVideoViews).

CHANGE 6  [## Prerequisites]  (lines 28–30 in repo file)

  OLD (remove from repo):
    * The email address associated with your Facebook account, or your telephone number
    * Your Facebook password
    * Access to your company's Facebook Ads account

  NEW (replace with / insert from CSV):
    - The email address associated with your Facebook account, or your telephone number
    - Your Facebook password
    - Access to your company's Facebook Ads account

CHANGE 7  [## Connecting to Your Facebook Account]  (line 34 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the Credentials and Details panes in the Facebook Ads Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the Credentials and Details panes in the Facebook Ads Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 8  [## Credentials Pane]  (line 38 in repo file)

  OLD (remove from repo):
    The Domo Facebook Ads connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Facebook Ads accounts in Domo) to open the Facebook OAuth screen where you can enter your Facebook credentials. Once you have entered valid Facebook credentials, you can use the same account any time you go to create a new Facebook Ads DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing Connectors Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Managing Connectors Accounts").

  NEW (replace with / insert from CSV):
    The Domo Facebook Ads connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Facebook Ads accounts in Domo) to open the Facebook OAuth screen where you can enter your Facebook credentials. Once you have entered valid Facebook credentials, you can use the same account any time you go to create a new Facebook Ads DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing Connectors Accounts](/s/article/360042926054).

CHANGE 9  [## Credentials Pane]  (lines 40–43 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:** If you are already logged into Facebook when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Facebook.
    
    </Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into Facebook when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Facebook.</Note>

CHANGE 10  [## Details Pane]  (line 47 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 11  [#### Reports and Edges]  (line 53 in repo file)

  ⚠ LARGE CHANGE (39 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:201;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Facebook Ads report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><b> Report Name </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"> Ad Accounts </td><td colspan="1" rowspan="1"> Returns information about one or more selected Facebook Ads Accounts. </td></tr><tr><td colspan="1" rowspan="1"> Ad Campaigns </td><td colspan="1" rowspan="1"> Returns information about one or more Ad Campaigns controlled by the selected <span></span><b> Account(s) </b>. </td></tr><tr><td colspan="1" rowspan="1"> Ad Keyword Statistics (Deprecated) </td><td colspan="1" rowspan="1"> Returns information about the Keywords used for targeting the selected <span></span><b> Ad(s) </b>. </td></tr><tr><td colspan="1" rowspan="1"> Ad Sets </td><td colspan="1" rowspan="1"> Returns information about one or more Ad Sets within the selected <span></span><b> Ad Campaign(s) </b>. </td></tr><tr><td colspan="1" rowspan="1"> Ads </td><td colspan="1" rowspan="1"> Returns information about one or more Ads defined in the selected <span></span><b> Ad Set(s) </b>. </td></tr><tr><td colspan="1" rowspan="1"><p> Ad Previews </p></td><td colspan="1" rowspan="1"> Returns ad previews for ad creatives. </td></tr><tr><td colspan="1" rowspan="1"> Custom Insights </td><td colspan="1" rowspan="1"> Returns user-selected insight information at a reporting level defined by the <span></span><b> Level </b><span></span> field. </td></tr><tr><td colspan="1" rowspan="1"> FBAds PreBuilt Report </td><td colspan="1" rowspan="1"><p> Domo's complimentary pre-built report for Facebook Ads - a comprehensive analysis of advertising performance including accounts, campaigns, ad groups, and ad-level reporting. </p></td></tr><tr><td colspan="1" rowspan="1"> General Metrics </td><td colspan="1" rowspan="1"> Returns commonly-used insight metrics at the Campaign level. </td></tr><tr><td colspan="1" rowspan="1"> Object Information - NEW </td><td colspan="1" rowspan="1"> This new report enables you to retrieve the Info edge of all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need information about. </td></tr><tr><td colspan="1" rowspan="1"> Object Insights - NEW </td><td colspan="1" rowspan="1"> This new report enables you to retrieve the Insights edge of all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need insights about, and complete the other insight metrics. </td></tr><tr><td colspan="1" rowspan="1"> Page Actions (Ad sets) </td><td colspan="1" rowspan="1"> Returns insights related to Page and Post interactions at the Adset level. </td></tr><tr><td colspan="1" rowspan="1"> Page Actions (Ads) </td><td colspan="1" rowspan="1"> Returns insights related to Page and Post interactions at the Ad level. </td></tr><tr><td colspan="1" rowspan="1"> Placement Metrics </td><td colspan="1" rowspan="1"> Returns common metrics, broken down by screen placement, at the Campaign level. </td></tr><tr><td colspan="1" rowspan="1"> Responder Demographics </td><td colspan="1" rowspan="1"> Returns common metrics, broken down by age and gender, at the Adset level. </td></tr><tr><td colspan="1" rowspan="1"> User </td><td colspan="1" rowspan="1"> Returns information about the authenticated user. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Select Status Of Account To Retrieve </p></td><td colspan="1" rowspan="1"><p> Select the status of the account. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Object Type </p></td><td colspan="1" rowspan="1"><p> Pick an object level to gather data. Domo will report on all objects of that type contained in the accounts you have selected. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Breakdowns </p></td><td colspan="1" rowspan="1"><p> Select how you want the report data to be broken down. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Time Increment </p></td><td colspan="1" rowspan="1"><p> Select the granularity used to aggregate the data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Duration </p></td><td colspan="1" rowspan="1"><p> Specify whether the data in this report is for a single date or a range of dates. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Date From </p></td><td colspan="1" rowspan="1"><p> Specify whether the first date in your date range is a specific or relative date. Choose <b> Relative </b> if you always want the report to retrieve data for a given number of days back (which you specify in <b> Date From Offset </b> ) from the current date. If you choose <b> Relative </b> here, you should also choose <b> Relative </b> for <b> Date To </b> and specify a value for <b> Date To Offset </b>. <br/> For example, if you enter <tt> 10 </tt> for <b> Date From Offset </b> and <tt> 5 </tt> for <b> Date To Offset </b> and you set the data source to update daily, each new day the report will update to show information for 5 to 10 days in the past. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Date From Offset </p></td><td colspan="1" rowspan="1"><p> Enter the number of days back from the current date that will be used as the first date in the report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific Date From </p></td><td colspan="1" rowspan="1"><p> Select the first date in your date range. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Date To </p></td><td colspan="1" rowspan="1"><p> Specify whether the end date in your date range is a specific or relative date. Choose <b> Relative </b> if you always want the report to retrieve data for a given number of days back (which you specify in <b> Date To Offset </b> ) from the current date. If you choose <b> Relative </b> here, you should also choose <b> Relative </b> for <b> Date From </b> and specify a value for <b> Date From Offset </b>. <br/> For example, if you enter <tt> 10 </tt> for <b> Date From Offset </b> and <tt> 5 </tt> for <b> Date To Offset </b> and you set the data source to update daily, each new day the report will update to show information for 5 to 10 days in the past. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Date To Offset </p></td><td colspan="1" rowspan="1"><p> Enter the number of days back from the current date that will be used as the last date in the report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific Date To </p></td><td colspan="1" rowspan="1"><p> Select the last date in your date range. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Write ID Columns as type String </p></td><td colspan="1" rowspan="1"><p> Select this checkbox if you want to write the columns 'AdId', 'AdsetId', CampaignId' and 'AccountId' as a String rather than as a Number. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Ad Account ID </p></td><td colspan="1" rowspan="1"><p> Select Ad Account. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Columns </p></td><td colspan="1" rowspan="1"><p> Select Columns. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Ad Campaign Type </p></td><td colspan="1" rowspan="1"><p> Select the ad campaign type. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Ad Campaign </p></td><td colspan="1" rowspan="1"><p> Select Ad Campaign. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Level </p></td><td colspan="1" rowspan="1"><p> Select the ad level for the report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Conversion Type Selection </p></td><td colspan="1" rowspan="1"><p> Select if you want to bring custom or standard conversions only. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Action Breakdowns </p></td><td colspan="1" rowspan="1"><p> Select the desired action breakdown(s) for the report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Include Archived Objects </p></td><td colspan="1" rowspan="1"><p> If set to True then Archived objects will be in the results. Otherwise, Archived objects will not be reported. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Action Attribution Windows </p></td><td colspan="1" rowspan="1"><p> Select the desired action attribution windows for the report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Reset Schema </p></td><td colspan="1" rowspan="1"><p> Select if you need to reset the schema. Warning: This can result in loss of data or change in data type if some fields are temporarily not returned by Facebook's API. While this checkbox is selected the dataset will reset the schema for each run. Do not use in the APPEND mode. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Ad Set </p></td><td colspan="1" rowspan="1"><p> Select the Ad Set. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Ad </p></td><td colspan="1" rowspan="1"><p> Select the Ad. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Ad Edge </p></td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Edge </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"> Ad Creatives </td><td colspan="1" rowspan="1"> Returns a list of creatives for the selected ad. </td></tr><tr><td colspan="1" rowspan="1"> Ad Info </td><td colspan="1" rowspan="1"> Returns information about the selected ad. </td></tr><tr><td colspan="1" rowspan="1"> Insights </td><td colspan="1" rowspan="1"> Returns insight information for the specified criteria. </td></tr><tr><td colspan="1" rowspan="1"> Reach Estimate </td><td colspan="1" rowspan="1"> Returns the reach estimate for the selected ad. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Action Report Time </p></td><td colspan="1" rowspan="1"> Determines the report time of action stats. For example, if a person saw the ad on Jan 1st but converted on Jan 2nd, when you query the API with action_report_time=impression, you see a conversion on Jan 1st. When you query the API with action_report_time=conversion, you see a conversion on Jan 2nd. </td></tr><tr><td colspan="1" rowspan="1"><p> Ad Account Edge </p></td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Edge </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"> Ad Account Info </td><td colspan="1" rowspan="1"> Ad Account Info </td></tr><tr><td colspan="1" rowspan="1"> Activities </td><td colspan="1" rowspan="1"> Returns a log of the actions taken on the selected account. </td></tr><tr><td colspan="1" rowspan="1"> Ad Campaigns </td><td colspan="1" rowspan="1"> Returns a list of ad campaigns for the selected account. </td></tr><tr><td colspan="1" rowspan="1"> Ad Sets </td><td colspan="1" rowspan="1"> Returns a list of ad sets for the selected account. </td></tr><tr><td colspan="1" rowspan="1"> Ads </td><td colspan="1" rowspan="1"> Returns a list of ads for the selected account. </td></tr><tr><td colspan="1" rowspan="1"> Custom Audiences </td><td colspan="1" rowspan="1"> Returns the custom audiences owned by or shared with the selected account. </td></tr><tr><td colspan="1" rowspan="1"> Insights </td><td colspan="1" rowspan="1"> Returns insight information for the specified criteria. </td></tr><tr><td colspan="1" rowspan="1"> Partners </td><td colspan="1" rowspan="1"> Returns a list of ad partners for the selected account. </td></tr><tr><td colspan="1" rowspan="1"> Rate Card </td><td colspan="1" rowspan="1"> Returns a list of currency rates. </td></tr><tr><td colspan="1" rowspan="1"> Reach Estimate </td><td colspan="1" rowspan="1"> Returns the reach estimate for the selected account and given targeting spec. </td></tr><tr><td colspan="1" rowspan="1"> Users </td><td colspan="1" rowspan="1"> Returns user IDs, permissions, and roles for the selected account. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Expand List Columns </p></td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Option </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"> Expand as Columns </td><td colspan="1" rowspan="1"> List data will expand as multiple columns in the results. </td></tr><tr><td colspan="1" rowspan="1"> Expand as Rows </td><td colspan="1" rowspan="1"> List data will expand into multiple rows in the results. This will result in many rows with duplicated data. </td></tr><tr><td colspan="1" rowspan="1"> Do not expand </td><td colspan="1" rowspan="1"> List data will not be expanded in the results. It will be returned as "List" in the results. </td></tr><tr><td colspan="1" rowspan="1"> Expand Lists In Place </td><td colspan="1" rowspan="1"> Expand Lists In Place </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Targeting Spec </p></td><td colspan="1" rowspan="1"> Enter the Targeting Spec. </td></tr><tr><td colspan="1" rowspan="1"><p> Ad Campaign Edge </p></td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Edge </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"> Ad Campaign Info </td><td colspan="1" rowspan="1"> Returns information about the selected campaign. </td></tr><tr><td colspan="1" rowspan="1"> Insights </td><td colspan="1" rowspan="1"> Returns insight information for the specified criteria. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Skip Failing Campaign IDs </p></td><td colspan="1" rowspan="1"> When checked any Campaign IDs that fail will be skipped and the Connector will continue to run using the remaining Campaign IDs. </td></tr><tr><td colspan="1" rowspan="1"><p> Ad Set Edge </p></td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"><b> Edge </b></td><td colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td colspan="1" rowspan="1"> Activities </td><td colspan="1" rowspan="1"> Returns a log of actions taken on the selected ad set. </td></tr><tr><td colspan="1" rowspan="1"> Ad Set Info </td><td colspan="1" rowspan="1"> Returns information about the selected ad set. </td></tr><tr><td colspan="1" rowspan="1"> Ad Creatives </td><td colspan="1" rowspan="1"> Returns a list of ad creatives for the selected ad set. </td></tr><tr><td colspan="1" rowspan="1"> Insights </td><td colspan="1" rowspan="1"> Returns insight information for the specified criteria. </td></tr><tr><td colspan="1" rowspan="1"> Reach Estimate </td><td colspan="1" rowspan="1"> Returns the reach estimate for the selected ad set. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Skip Failing Ad Set IDs </p></td><td colspan="1" rowspan="1"> When checked any Ad Set IDs that fail will be skipped and the Connector will continue to run using the remaining Ad Set IDs. </td></tr><tr><td colspan="1" rowspan="1"><p> User Edge </p></td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><td colspan="1" rowspan="1"> Edge </td><td colspan="1" rowspan="1"> Description </td></tr><tr><td colspan="1" rowspan="1"> Ad Accounts </td><td colspan="1" rowspan="1"> Returns information for the authenticated user's ad accounts, such as balance, capabilities, funding sources, etc. </td></tr><tr><td colspan="1" rowspan="1"> User Info </td><td colspan="1" rowspan="1"> Returns the authenticated user's name and ID. </td></tr></tbody></table></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Facebook Ads report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Ad Accounts | Returns information about one or more selected Facebook Ads Accounts. | | Ad Campaigns | Returns information about one or more Ad Campaigns controlled by the selected **Account(s)**. | | Ad Keyword Statistics (Deprecated) | Returns information about the Keywords used for targeting the selected **Ad(s)**. | | Ad Sets | Returns information about one or more Ad Sets within the selected **Ad Campaign(s)**. | | Ads | Returns information about one or more Ads defined in the selected **Ad Set(s)**. | | Ad Previews | Returns ad previews for ad creatives. | | Custom Insights | Returns user-selected insight information at a reporting level defined by the **Level** field. | | FBAds PreBuilt Report | Domo's complimentary pre-built report for Facebook Ads - a comprehensive analysis of advertising performance including accounts, campaigns, ad groups, and ad-level reporting. | | General Metrics | Returns commonly-used insight metrics at the Campaign level. | | Object Information - NEW | This new report enables you to retrieve the Info edge of all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need information about. | | Object Insights - NEW | This new report enables you to retrieve the Insights edge of all the Account, Campaign, Ad set, or Ad objects associated with the Ad Accounts you select. Select the Ad Accounts and the object type you need insights about, and complete the other insight metrics. | | Page Actions (Ad sets) | Returns insights related to Page and Post interactions at the Adset level. | | Page Actions (Ads) | Returns insights related to Page and Post interactions at the Ad level. | | Placement Metrics | Returns common metrics, broken down by screen placement, at the Campaign level. | | Responder Demographics | Returns common metrics, broken down by age and gender, at the Adset level. | | User | Returns information about the authenticated user. | |
    | Select Status Of Account To Retrieve | Select the status of the account. |
    | Select Object Type | Pick an object level to gather data. Domo will report on all objects of that type contained in the accounts you have selected. |
    | Effective Statuses | This field appears for the **Object Information - NEW** report when you select **Campaign**, **Ad Set**, or **Ad** as the Object Type.  Select the effective statuses to filter your data. |
    | Breakdowns | Select how you want the report data to be broken down. |
    | Time Increment | Select the granularity used to aggregate the data. |
    | Duration | Specify whether the data in this report is for a single date or a range of dates. |
    | Date From | Specify whether the first date in your date range is a specific or relative date. Choose **Relative** if you always want the report to retrieve data for a given number of days back (which you specify in **Date From Offset**) from the current date. If you choose **Relative** here, you should also choose **Relative** for **Date To** and specify a value for **Date To Offset**. For example, if you enter 10 for **Date From Offset** and 5 for **Date To Offset** and you set the data source to update daily, each new day the report will update to show information for 5 to 10 days in the past. |
    | Date From Offset | Enter the number of days back from the current date that will be used as the first date in the report. |
    | Select Specific Date From | Select the first date in your date range. |
    | Date To | Specify whether the end date in your date range is a specific or relative date. Choose **Relative** if you always want the report to retrieve data for a given number of days back (which you specify in **Date To Offset**) from the current date. If you choose **Relative** here, you should also choose **Relative** for **Date From** and specify a value for **Date From Offset**. For example, if you enter 10 for **Date From Offset** and 5 for **Date To Offset** and you set the data source to update daily, each new day the report will update to show information for 5 to 10 days in the past. |
    | Date To Offset | Enter the number of days back from the current date that will be used as the last date in the report. |
    | Select Specific Date To | Select the last date in your date range. |
    | Write ID Columns as type String | Select this checkbox if you want to write the columns 'AdId', 'AdsetId', CampaignId' and 'AccountId' as a String rather than as a Number. |
    | Ad Account ID | Select Ad Account. |
    | Columns | Select Columns. |
    | Ad Campaign Type | Select the ad campaign type. |
    | Ad Campaign | Select Ad Campaign. |
    | Level | Select the ad level for the report. |
    | Conversion Type Selection | Select if you want to bring custom or standard conversions only. |
    | Action Breakdowns | Select the desired action breakdown(s) for the report. |
    | Include Archived Objects | If set to True then Archived objects will be in the results. Otherwise, Archived objects will not be reported. |
    | Action Attribution Windows | Select the desired action attribution windows for the report. |
    | Reset Schema | Select if you need to reset the schema. Warning: This can result in loss of data or change in data type if some fields are temporarily not returned by Facebook's API. While this checkbox is selected the dataset will reset the schema for each run. Do not use in the APPEND mode. |
    | Ad Set | Select the Ad Set. |
    | Ad | Select the Ad. |
    | Ad Edge | |  |  | | --- | --- | | **Edge** | **Description** | | Ad Creatives | Returns a list of creatives for the selected ad. | | Ad Info | Returns information about the selected ad. | | Insights | Returns insight information for the specified criteria. | | Reach Estimate | Returns the reach estimate for the selected ad. | |
    | Action Report Time | Determines the report time of action stats. For example, if a person saw the ad on Jan 1st but converted on Jan 2nd, when you query the API with action\_report\_time=impression, you see a conversion on Jan 1st. When you query the API with action\_report\_time=conversion, you see a conversion on Jan 2nd. |
    | Ad Account Edge | |  |  | | --- | --- | | **Edge** | **Description** | | Ad Account Info | Ad Account Info | | Activities | Returns a log of the actions taken on the selected account. | | Ad Campaigns | Returns a list of ad campaigns for the selected account. | | Ad Sets | Returns a list of ad sets for the selected account. | | Ads | Returns a list of ads for the selected account. | | Custom Audiences | Returns the custom audiences owned by or shared with the selected account. | | Insights | Returns insight information for the specified criteria. | | Partners | Returns a list of ad partners for the selected account. | | Rate Card | Returns a list of currency rates. | | Reach Estimate | Returns the reach estimate for the selected account and given targeting spec. | | Users | Returns user IDs, permissions, and roles for the selected account. | |
    | Expand List Columns | |  |  | | --- | --- | | **Option** | **Description** | | Expand as Columns | List data will expand as multiple columns in the results. | | Expand as Rows | List data will expand into multiple rows in the results. This will result in many rows with duplicated data. | | Do not expand | List data will not be expanded in the results. It will be returned as "List" in the results. | | Expand Lists In Place | Expand Lists In Place | |
    | Targeting Spec | Enter the Targeting Spec. |
    | Ad Campaign Edge | |  |  | | --- | --- | | **Edge** | **Description** | | Ad Campaign Info | Returns information about the selected campaign. | | Insights | Returns insight information for the specified criteria. | |
    | Skip Failing Campaign IDs | When checked any Campaign IDs that fail will be skipped and the Connector will continue to run using the remaining Campaign IDs. |
    | Ad Set Edge | |  |  | | --- | --- | | **Edge** | **Description** | | Activities | Returns a log of actions taken on the selected ad set. | | Ad Set Info | Returns information about the selected ad set. | | Ad Creatives | Returns a list of ad creatives for the selected ad set. | | Insights | Returns insight information for the specified criteria. | | Reach Estimate | Returns the reach estimate for the selected ad set. | |
    | Skip Failing Ad Set IDs | When checked any Ad Set IDs that fail will be skipped and the Connector will continue to run using the remaining Ad Set IDs. |
    | User Edge | |  |  | | --- | --- | | Edge | Description | | Ad Accounts | Returns information for the authenticated user's ad accounts, such as balance, capabilities, funding sources, etc. | | User Info | Returns the authenticated user's name and ID. | |

CHANGE 12  [## Other Panes]  (line 57 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 13  [### Choosing Insight Columns]  (line 66 in repo file)

  OLD (remove from repo):
    2. Select **Columns > Customize Columns**.

  NEW (replace with / insert from CSV):
    2. Select **Columns > Customize Columns**.
       A dialog box opens that lets you choose and reorder columns in the report.
    3. Pick a column to add to the report and click **Apply**.
       If you look at the URL, you’ll see a parameter called "columns." This will look something like the following:

CHANGE 14  [### Choosing Insight Columns]  (lines 68–78 in repo file)

  OLD (remove from repo):
       A dialog box opens that lets you choose and reorder columns in the report.
    3. Pick a column to add to the report and click **Apply**.
    
       If you look at the URL, you’ll see a parameter called "columns." This will look something like the following:
       ```
       columns=["name"%2C"date_start"%2C"date_stop"%2C"reach"%2C"frequency"%2C"impressions"%2C"cpm"%2C"cpp"%2C"spend"%2C"clicks"%2C"unique_clicks"%2C"ctr"%2C"unique_ctr"%2C"cpc"%2C"actions%3Aoffsite_conversion.lead"%2C"social_reach"]
       ```
    
       Each of the items between quotes is a column, and this is the API name for it. The names that are shown in the **Columns** field are typically close (or identical) to these names.
    
       The tricky columns are part of a bigger category in the Domo UI. This includes columns like "Actions," which expands into several smaller fields, including actions%3Aoffsite_conversion.lead shown above (which is “Leads (Conversion)” in the UI).

  NEW (replace with / insert from CSV):
       columns=["name"%2C"date\_start"%2C"date\_stop"%2C"reach"%2C"frequency"%2C"impressions"%2C"cpm"%2C"cpp"%2C"spend"%2C"clicks"%2C"unique\_clicks"%2C"ctr"%2C"unique\_ctr"%2C"cpc"%2C"actions%3Aoffsite\_conversion.lead"%2C"social\_reach"]
    
       Each of the items between quotes is a column, and this is the API name for it. The names that are shown in the **Columns** field are typically close (or identical) to these names.
    
       The tricky columns are part of a bigger category in the Domo UI. This includes columns like "Actions," which expands into several smaller fields, including actions%3Aoffsite\_conversion.lead shown above (which is “Leads (Conversion)” in the UI).

CHANGE 15  [### Instagram Video Views]  (line 90 in repo file)

  OLD (remove from repo):
    This will return information for many different placement options. The most interesting one is called “instagramstream.” There should also be a column called “ActionsVideoView” that lists the number of video views.

  NEW (replace with / insert from CSV):
     This will return information for many different placement options. The most interesting one is called “instagramstream.” There should also be a column called “ActionsVideoView” that lists the number of video views.

CHANGE 16  [### Aggregating Statistics]  (line 96 in repo file)

  OLD (remove from repo):
    To avoid this problem, any time the user wants to get a value for a larger time period, the period needs to be defined in the DataSet itself, and not calculated afterward. If a user wants Reach for a particular month, the DataSet should be defined with the start and end dates for the month, and the **Time Increment** value should be set to all_days or left blank. The user can get monthly data, aggregated and deduplicated by Facebook, by setting a large date range (for example, Jan 1, 2016, to Dec 31, 2016) and entering monthly as the **Time Increment** value. Similarly, if a user wants to Reach data at the Account level, the user needs to set the DataSet's **Level** value to **Accounts** (not **Campaigns**), followed by summing the values in Domo.

  NEW (replace with / insert from CSV):
    To avoid this problem, any time the user wants to get a value for a larger time period, the period needs to be defined in the DataSet itself, and not calculated afterward. If a user wants Reach for a particular month, the DataSet should be defined with the start and end dates for the month, and the **Time Increment** value should be set to all\_days or left blank. The user can get monthly data, aggregated and deduplicated by Facebook, by setting a large date range (for example, Jan 1, 2016, to Dec 31, 2016) and entering monthly as the **Time Increment** value. Similarly, if a user wants to Reach data at the Account level, the user needs to set the DataSet's **Level** value to **Accounts** (not **Campaigns**), followed by summing the values in Domo.

CHANGE 17  [## Troubleshooting]  (line 102 in repo file)

  OLD (remove from repo):
    * The most common problem occurs when the returned data and the Facebook Ads UI do not match. This happens when the DataSet and the report in the Facebook UI are not configured in the same way. Make sure the following parameters match:

  NEW (replace with / insert from CSV):
    - The most common problem occurs when the returned data and the Facebook Ads UI do not match. This happens when the DataSet and the report in the Facebook UI are not configured in the same way. Make sure the following parameters match:

CHANGE 18  [## Troubleshooting]  (lines 104–109 in repo file)

  OLD (remove from repo):
       + Start Date
       + End Date
       + Breakdown
       + Level
       + Time Increment.
    * Metrics related to individuals (such as Reach) should NOT be aggregated across time periods in Domo. These metrics have duplicates already removed by the Facebook Ads system.

  NEW (replace with / insert from CSV):
      - Start Date
      - End Date
      - Breakdown
      - Level
      - Time Increment.
    - Metrics related to individuals (such as Reach) should NOT be aggregated across time periods in Domo. These metrics have duplicates already removed by the Facebook Ads system.

CHANGE 19  [## FAQ]  (line 114 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 20  [## FAQ]  (line 115 in repo file)

  OLD (remove from repo):
    

CHANGE 21  [## FAQ]  (line 118 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 22  [## FAQ]  (line 119 in repo file)

  OLD (remove from repo):
    

CHANGE 23  [## FAQ]  (line 122 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 24  [## FAQ]  (line 123 in repo file)

  OLD (remove from repo):
    

CHANGE 25  [## FAQ]  (lines 126–132 in repo file)

  OLD (remove from repo):
    1.  In the Facebook Ads UI, open the report that you want to duplicate in Domo.
    2.  On the right-hand side of the screen click the button labeled **Columns** and select **Customize Columns**. You should now see a dialog box that lets you choose and reorder columns that will be in the report.
    3.  Pick a column to add to the report and click **Apply**.
    4.  If you look at the URL, you'll see a URL parameter called 'columns'. This will look something like this:
        columns=\["name","date_start","date_stop","reach","cpm","cpp","spend","clicks","unique_clicks","ctr","unique_ctr","cpc","actions:offsite_conversion.lead","social_reach"\]
        *   Each of the items between quotes is a column. The names that are shown in the Columns field are identical or very close to the names presented by Domo.
        *   Columns that are part of a larger category may vary slightly in this interface and the Domo interface. For example, "actions" expands into several smaller fields, including "actions:offsite_conversion.lead". In the Domo interface, this column is called "Leads (Conversion)".

  NEW (replace with / insert from CSV):
    1. In the Facebook Ads UI, open the report that you want to duplicate in Domo.
    2. On the right-hand side of the screen click the button labeled

CHANGE 26  [## FAQ]  (line 134 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I get statistics about Instagram Views?">

CHANGE 27  [## FAQ]  (line 136 in repo file)

  NEW (replace with / insert from CSV):
    <Accordion title="Columns** and select **Customize Columns**. You should now see a dialog box that lets you choose and reorder columns that will be in the report.
    3. Pick a column to add to the report and click **Apply**.
    4. If you look at the URL, you'll see a URL parameter called 'columns'. This will look something like this:
       columns=[&quot;name&quot;,&quot;date\_start&quot;,&quot;date\_stop&quot;,&quot;reach&quot;,&quot;cpm&quot;,&quot;cpp&quot;,&quot;spend&quot;,&quot;clicks&quot;,&quot;unique\_clicks&quot;,&quot;ctr&quot;,&quot;unique\_ctr&quot;,&quot;cpc&quot;,&quot;actions:offsite\_conversion.lead&quot;,&quot;social\_reach&quot;]
       - Each of the items between quotes is a column. The names that are shown in the Columns field are identical or very close to the names presented by Domo.
       - Columns that are part of a larger category may vary slightly in this interface and the Domo interface. For example, &quot;actions&quot; expands into several smaller fields, including &quot;actions:offsite\_conversion.lead&quot;. In the Domo interface, this column is called &quot;Leads (Conversion)&quot;.
    
    **How do I get statistics about Instagram Views?">

CHANGE 28  [## FAQ]  (lines 138–142 in repo file)

  OLD (remove from repo):
    1.  Create a **Custom Insights** report.
    2.  Select interesting metrics. (For example, **Actions** should bring back a column about video views.)
    3.  Select **Placement** as the breakdown option.
    4.  Choose the level to report (Account, Campaign, AdSet, Ad).
    5.  Select the date range and time increment.

  NEW (replace with / insert from CSV):
    1. Create a
    </Accordion>
    
    <Accordion title="Custom Insights** report.
    2. Select interesting metrics. (For example, **Actions** should bring back a column about video views.)
    3. Select **Placement** as the breakdown option.
    4. Choose the level to report (Account, Campaign, AdSet, Ad).
    5. Select the date range and time increment.

CHANGE 29  [## FAQ]  (lines 145–146 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="How do I get information or insights for all my campaigns, ad sets, or ads, including new campaigns, ad sets, or ads I create in the future?">

CHANGE 30  [## FAQ]  (line 148 in repo file)

  NEW (replace with / insert from CSV):
    **How do I get information or insights for all my campaigns, ad sets, or ads, including new campaigns, ad sets, or ads I create in the future?">

CHANGE 31  [## FAQ]  (lines 150–151 in repo file)

  OLD (remove from repo):
    1.  Select the new **Object Information** report and the account(s) you wish to report on.
    2.  Select the **Object Type** (Account, Campaign, Ad Set, or Ad).

  NEW (replace with / insert from CSV):
    1. Select the new
    </Accordion>
    
    <Accordion title="Object Information** report and the account(s) you wish to report on.
    2. Select the **Object Type** (Account, Campaign, Ad Set, or Ad).

CHANGE 32  [## FAQ]  (lines 155–159 in repo file)

  OLD (remove from repo):
    1.  Select the **Ad Accounts** report and the account(s) you wish to report on.
    2.  Select the appropriate **Ad Account e**dge.
        *   To get information on all campaigns, select **Ad Campaigns**.
        *   To get information on all ad sets, select **Ad Sets**.
        *   To get information on all ads, select **Ads**.

  NEW (replace with / insert from CSV):
    1. Select the **Ad Accounts** report and the account(s) you wish to report on.
    2. Select the appropriate **Ad Account e**dge.
       - To get information on all campaigns, select **Ad Campaigns**.
       - To get information on all ad sets, select **Ad Sets**.
       - To get information on all ads, select **Ads**.

CHANGE 33  [## FAQ]  (lines 163–165 in repo file)

  OLD (remove from repo):
    1.  Select the new **Object Insights** report and the account(s) you wish to report on.
    2.  Select the **Object Type** (Account, Campaign, Ad Set, or Ad).
    3.  Select the desired parameters in all the other fields.

  NEW (replace with / insert from CSV):
    1. Select the new **Object Insights** report and the account(s) you wish to report on.
    2. Select the **Object Type** (Account, Campaign, Ad Set, or Ad).
    3. Select the desired parameters in all the other fields.

CHANGE 34  [## FAQ]  (lines 169–177 in repo file)

  OLD (remove from repo):
    1.  Select the **Ad Accounts** report and the account(s) you wish to report on.
    2.  Select the **Insights Ad Account** edge.
    3.  Select the appropriate Level.
        *   To get insights on all campaigns, select **Ad Campaign**.
        *   To get insights on all ad sets, select **Ad Set**.
        *   To get insights on all ads, select **Ad**.
    4.  Select the desired parameters in all the other fields.
    </Accordion>
    <Accordion title="What is the difference between the Facebook Ads and Facebook Ads Advanced connectors?">

  NEW (replace with / insert from CSV):
    1. Select the **Ad Accounts** report and the account(s) you wish to report on.
    2. Select the **Insights Ad Account** edge.
    3. Select the appropriate Level.
       - To get insights on all campaigns, select **Ad Campaign**.
       - To get insights on all ad sets, select **Ad Set**.
       - To get insights on all ads, select **Ad**.
    4. Select the desired parameters in all the other fields.

CHANGE 35  [## FAQ]  (line 179 in repo file)

  NEW (replace with / insert from CSV):
    **What is the difference between the Facebook Ads and Facebook Ads Advanced connectors?">

CHANGE 36  [## FAQ]  (line 183 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 37  [## FAQ]  (line 184 in repo file)

  OLD (remove from repo):
    

CHANGE 38  [## FAQ]  (line 187 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 39  [## FAQ]  (line 188 in repo file)

  OLD (remove from repo):
    

CHANGE 40  [## FAQ]  (line 191 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 41  [## FAQ]  (line 192 in repo file)

  OLD (remove from repo):
    

CHANGE 42  [## FAQ]  (line 195 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 43  [## FAQ]  (line 196 in repo file)

  NEW (replace with / insert from CSV):
    This connector uses version 23 of the Facebook API ([https://graph.facebook.com/v23.0](https://graph.facebook.com/v20.0).
    </Accordion>

CHANGE 44  [## FAQ]  (line 197 in repo file)

  OLD (remove from repo):
    This connector uses version 23 of the Facebook API ([https://graph.facebook.com/v23.0](https://graph.facebook.com/v20.0 "https://graph.facebook.com/v20.0")).

  NEW (replace with / insert from CSV):
    <Accordion title="Which endpoint(s) does each report call in this connector?">
    | Report Name | | Endpoint URL(s) |
    | --- | --- | --- |
    | FBAds PreBuilt Report | | /me/adaccounts |
    | Ad Accounts | | /{AD\_ACCOUNT\_ID}/info |
    | Ad Campaigns | | /{AD\_ACCOUNT\_ID}/adcampaign\_groups |
    | Ad Keyword Statistics | | /keywordstats |
    | Ad Sets | | /{AD\_ACCOUNT\_ID}/adcampaigns |
    | Ads | | /{AD\_ACCOUNT\_ID}/adgroups |
    | Ad Previews | | [https://graph.facebook.com/v20.0/previews](https://graph.facebook.com/v20.0) |
    | Custom Insights | | /{AD\_ACCOUNT\_ID}/insights |
    | General Metrics | | /{CAMPAIGN\_GROUP\_ID}/insights |
    | Object Information-NEW | Account Object | /{AD\_ACCOUNT\_ID}/insights |
    | Campaign Object | /{AD\_ACCOUNT\_ID}/insights |
    | Ad Sets Object | {AD\_ACCOUNT\_ID}/adsets |
    | Ads Object | /{AD\_ACCOUNT\_ID}/ads |
    | Object Insights-NEW | | /{AD\_ACCOUNT\_ID}/insights |
    | Page Actions (Ad sets) | | /{AD\_SET\_ID}/insights |
    | Page Actions (Ads) | | /{AD\_SET\_ID}/insights |
    | Placement Metrics | | /{CAMPAIGN\_GROUP\_ID}/insights |
    | Responder Demographics | | /{AD\_SET\_ID}/insights |
    | User | | /{USER\_ID} |

CHANGE 45  [## FAQ]  (line 199 in repo file)

  OLD (remove from repo):
    <Accordion title="Which endpoint(s) does each report call in this connector?">

CHANGE 46  [## FAQ]  (lines 201–222 in repo file)

  OLD (remove from repo):
    | Report Name |     | Endpoint URL(s) |
    | --- | --- | --- |
    | FBAds PreBuilt Report |     | `/me/adaccounts` |
    | Ad Accounts |     | `/{AD_ACCOUNT_ID}/info` |
    | Ad Campaigns |     | `/{AD_ACCOUNT_ID}/adcampaign_groups` |
    | Ad Keyword Statistics |     | `/keywordstats` |
    | Ad Sets |     | `/{AD_ACCOUNT_ID}/adcampaigns` |
    | Ads |     | `/{AD_ACCOUNT_ID}/adgroups` |
    | Ad Previews |     | [https://graph.facebook.com/v20.0/previews](https://graph.facebook.com/v20.0) |
    | Custom Insights |     | `/{AD_ACCOUNT_ID}/insights` |
    | General Metrics |     | `/{CAMPAIGN_GROUP_ID}/insights` |
    | Object Information-NEW | Account Object | `/{AD_ACCOUNT_ID}/insights` |
    | Campaign Object | `/{AD_ACCOUNT_ID}/insights` |
    | Ad Sets Object | `{AD_ACCOUNT_ID}/adsets` |
    | Ads Object | `/{AD_ACCOUNT_ID}/ads` |
    | Object Insights-NEW |     | `/{AD_ACCOUNT_ID}/insights` |
    | Page Actions (Ad sets) |     | `/{AD_SET_ID}/insights` |
    | Page Actions (Ads) |     | `/{AD_SET_ID}/insights` |
    | Placement Metrics |     | `/{CAMPAIGN_GROUP_ID}/insights` |
    | Responder Demographics |     | `/{AD_SET_ID}/insights` |
    | User |     | `/{USER_ID}` |
    </Accordion>

==============================================================================
FILE:   s/article/360042926914.mdx
TITLE:  Klaviyo Connector
LANG:   English (en_US)
ID:     360042926914
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Klaviyo allows e-commerce marketers to target, personalize, measure, and optimize email and Facebook campaigns. To learn more about the Klaviyo API, visit their page ([https://www.klaviyo.com/docs](https://www.klaviyo.com/docs "https://www.klaviyo.com/docs") ).

  NEW (replace with / insert from CSV):
    Klaviyo allows e-commerce marketers to target, personalize, measure, and optimize email and Facebook campaigns. To learn more about the Klaviyo API, visit their page ([https://www.klaviyo.com/docs](https://www.klaviyo.com/docs)).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your Klaviyo account in the Data Center. This topic discusses the fields and menus specific to the Klaviyo connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You connect to your Klaviyo account in the Data Center. This topic discusses the fields and menus specific to the Klaviyo connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (line 13 in repo file)

  OLD (remove from repo):
    To connect to your Klaviyo account and create a DataSet, you must have a Klaviyo API key. For information about obtaining an API key, visit [https://help.klaviyo.com/hc/en-us/ar...unt-s-API-Keys](https://help.klaviyo.com/hc/en-us/articles/115005062267-Manage-Your-Account-s-API-Keys "https://help.klaviyo.com/hc/en-us/articles/115005062267-Manage-Your-Account-s-API-Keys").

  NEW (replace with / insert from CSV):
    To connect to your Klaviyo account and create a DataSet, you must have a Klaviyo API key. For information about obtaining an API key, visit [https://help.klaviyo.com/hc/en-us/ar...unt-s-API-Keys](https://help.klaviyo.com/hc/en-us/articles/115005062267-Manage-Your-Account-s-API-Keys).

CHANGE 4  [## Connecting to Your Klaviyo Account]  (line 17 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes on the Klaviyo Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Klaviyo Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 5  [## Credentials Pane]  (line 21 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Klaviyo account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Klaviyo account. The following table describes what is needed for each field:

CHANGE 6  [## Credentials Pane]  (line 23 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"> API Key </td><td colspan="1" rowspan="1"> Enter your Klaviyo API key. For information about obtaining an API key, visit <a href="https://help.klaviyo.com/hc/en-us/articles/115005062267-Manage-Your-Account-s-API-Keys" target="_blank" title="https://help.klaviyo.com/hc/en-us/articles/115005062267-Manage-Your-Account-s-API-Keys"> https://help.klaviyo.com/hc/en-us/ar...unt-s-API-Keys </a>. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter your Klaviyo API key. For information about obtaining an API key, visit [https://help.klaviyo.com/hc/en-us/ar...unt-s-API-Keys](https://help.klaviyo.com/hc/en-us/articles/115005062267-Manage-Your-Account-s-API-Keys). |

CHANGE 7  [## Credentials Pane]  (line 25 in repo file)

  OLD (remove from repo):
    Once you have entered valid Klaviyo credentials, you can use the same account any time you go to create a new Klaviyo DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](https://domo-support.domo.com/s/article/360042926054?language=en_US "Manage Connector Accounts").

  NEW (replace with / insert from CSV):
    Once you have entered valid Klaviyo credentials, you can use the same account any time you go to create a new Klaviyo DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 8  [## Details Pane]  (line 29 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 9  [## Details Pane]  (line 31 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Klaviyo report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p><b> Report Name </b></p></td><td colspan="1" rowspan="1"><p><b> Description </b></p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Information </p></td><td colspan="1" rowspan="1"><p> Returns summary information for the selected campaign. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaigns in Account </p></td><td colspan="1" rowspan="1"><p> Returns a list of all the campaigns you've created. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Messages </p></td><td colspan="1" rowspan="1"><p> Returns a list of campaigns messages information in your account. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Recipient Estimation </p></td><td colspan="1" rowspan="1"><p> Returns a list of estimated recipient count for the provided campaign IDs in your account. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Values Report </p></td><td colspan="1" rowspan="1"><p> Returns the campaign analytic values data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Export Event Data </p></td><td colspan="1" rowspan="1"><p> Returns metrics about event data broken down by selected event property. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Flows </p></td><td colspan="1" rowspan="1"><p> Returns information about flows in your account. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Flow Messages </p></td><td colspan="1" rowspan="1"><p> Returns the flow message of a flow. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Flow Series </p></td><td colspan="1" rowspan="1"><p> Returns the requested flow analytics series data. </p></td></tr><tr><td colspan="1" rowspan="1"><p></p><p> Flow Values Report </p><p></p></td><td colspan="1" rowspan="1"><p> Returns the flow analytics values of the data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List All Templates </p></td><td colspan="1" rowspan="1"><p> Returns a list of all email templates you've created. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List Exclusions or Unsubscribes for a List </p></td><td colspan="1" rowspan="1"><p> Returns exclusions or unsubscribes for a selected list. Includes the person's email address and the reason for/time of the exclusion. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List of Metrics </p></td><td colspan="1" rowspan="1"><p> Returns a list of metrics in Klaviyo. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List Of Timeline Of All Events </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline of all events in your Klaviyo account. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List Of Timeline Of All Events Append Only </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline of all events in your Klaviyo account. This report works in Append mode only and fetches the data after the last successful run, assuming that the end date offset is 1. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Listing a Person's Complete Event Timeline </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline of all events for a specified person. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Listing a Person's Complete Event Timeline (With Extra Properties) </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline of all events for a specified person with extra properties. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Listing a Person's Complete Event Timeline (with Person's Data) </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline of all events for a specified person, with that person's data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Listing The Event Timeline For A Particular Metric </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline for one specific type of metric. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Listing The Event Timeline For A Particular Metric (With Extra Properties) </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline for one specific type of metric, with extra properties. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Listing The Event Timeline For A Particular Metric (With Person's Data) </p></td><td colspan="1" rowspan="1"><p> Returns a batched timeline for one specific type of metric with a person's data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Lists in Account </p></td><td colspan="1" rowspan="1"><p> Returns a list of accounts. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Segments In Account </p></td><td colspan="1" rowspan="1"><p> Returns a list of all the segments in your account. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Tags </p></td><td colspan="1" rowspan="1"><div><div><div><div><div><div><div><div><div> Returns a list of all the tags in your account. </div></div></div></div></div></div></div></div></div></td></tr><tr><td colspan="1" rowspan="1"><p> Tags For Campaigns </p></td><td colspan="1" rowspan="1"><div><div><div><div><div><div><div><div><div> Returns a list of all tags that belong to the given campaign. </div></div></div></div></div></div></div></div></div></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign Selection </p></td><td colspan="1" rowspan="1"><p> This field only appears when you select the <b> Tags For Campaigns </b> report or the <b> Campaign Recipient Estimation </b> report. </p><p> You can select any one of the three options from the drop-down list: </p><ul><li><b> All Campaigns </b> - Fetches all the campaigns. </li><li><b> Specific Campaigns </b> - When you select this option, it shows the available list of campaigns from the <b> Campaigns </b> field. You can select the specific campaigns. </li><li><b> Enter Campaigns </b> - When you select this option, the <b> Enter Campaigns </b> text box appears. You can enter the campaigns which are not available in the <b> Campaigns </b> field. </li></ul></td></tr><tr><td colspan="1" rowspan="1"><p> Campaign </p></td><td colspan="1" rowspan="1"><p> This field only appears when you select the <b> Campaign Information </b> report. </p><p> Select the campaign for which you want to retrieve the information. You can select only one campaign from the list of available campaigns. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Campaigns </p></td><td colspan="1" rowspan="1"><p> This field only appears when you select the <b> Tags For Campaigns </b> report or the <b> Campaign Recipient Estimation </b> report. </p><p> Select the campaign(s) for which you want to retrieve the information. You have the option to select multiple campaigns from the list of available campaigns. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Enter Campaigns </p></td><td colspan="1" rowspan="1"><p> This field appears when you select the <b> Enter Campaigns </b> option from the <b> Campaign Selection </b> drop-down list. </p><p> Enter the Comma Separated campaign IDs. <br/> To fetch the campaign IDs, run the 'Campaigns In Account' report and paste the comma-separated IDs without spaces. (For example: 123abc,789xyz). </p></td></tr><tr><td colspan="1" rowspan="1"><p> List </p></td><td colspan="1" rowspan="1"><p> Select the list you want to retrieve information for. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Person ID </p></td><td colspan="1" rowspan="1"><p> Enter the ID of the person you want to retrieve information for. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Metric </p></td><td colspan="1" rowspan="1"><p> Select the metric type you want to retrieve information for. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Type </p></td><td colspan="1" rowspan="1"><p> Select whether you want to filter results by list or segment. If you select <b> No Selection </b>, all results are shown. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Event Property </p></td><td colspan="1" rowspan="1"><p> Select the property to segment the data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select the granularity </p></td><td colspan="1" rowspan="1"><p> Select the granularity. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Measurement </p></td><td colspan="1" rowspan="1"><p> Select the measurement keys. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Duration </p></td><td colspan="1" rowspan="1"><p> Select the duration for the report (a Single Date, or a Date Range). </p></td></tr><tr><td colspan="1" rowspan="1"><p> Report Date </p></td><td colspan="1" rowspan="1"><p> Select report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific Date </p></td><td colspan="1" rowspan="1"><p> Select the report date using the date selector. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Days back </p></td><td colspan="1" rowspan="1"><p> The number of days back that you would like to get data for. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Start Date </p></td><td colspan="1" rowspan="1"><p> Select report start date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific Start Date </p></td><td colspan="1" rowspan="1"><p> Select the report date using the date selector. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Days back to start from </p></td><td colspan="1" rowspan="1"><p> The number of days back that you would like to get data from (start day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. </p></td></tr><tr><td colspan="1" rowspan="1"><p> End Date </p></td><td colspan="1" rowspan="1"><p> Select report end date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Specific End Date </p></td><td colspan="1" rowspan="1"><p> Select the report date using the date selector. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Days back to end at </p></td><td colspan="1" rowspan="1"><p> The number of days back that you would like to get data to (end day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Select Status of Flow Message to Retrieve </p></td><td colspan="1" rowspan="1"><p> Select the status of the Flow Message. </p><p> The following options appear when you select the drop-down list: </p><ul><li> Retrieve data for all Flow Messages </li><li> Select specific Flow Message </li></ul></td></tr><tr><td colspan="1" rowspan="1"><p> Select Flows </p></td><td colspan="1" rowspan="1"><p> This option appears when you select the <b> Select specific Flow Message </b> option from the <b> Select Status of Flow Message to Retrieve </b> drop-down list. </p><p> Select from the list of available flows. You need to select at least one flow. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Action Flows </p></td><td colspan="1" rowspan="1"><p> This option appears when you select at least one flow. <br/> Select from the list of available action flows. You need to select at least one action flow. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Flow Message IDs </p></td><td colspan="1" rowspan="1"><p> Select the Flows Message IDs. You need to select at least one flow message ID. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Klaviyo report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Campaign Information | Returns summary information for the selected campaign. | | Campaigns in Account | Returns a list of all the campaigns you've created. | | Campaign Messages | Returns a list of campaigns messages information in your account. | | Campaign Recipient Estimation | Returns a list of estimated recipient count for the provided campaign IDs in your account. | | Campaign Values Report | Returns the campaign analytic values data. | | Export Event Data | Returns metrics about event data broken down by selected event property. | | Flows | Returns information about flows in your account. | | Flow Messages | Returns the flow message of a flow. | | Flow Series | Returns the requested flow analytics series data. | | Flow Values Report | Returns the flow analytics values of the data. | | List All Templates | Returns a list of all email templates you've created. | | List Exclusions or Unsubscribes for a List | Returns exclusions or unsubscribes for a selected list. Includes the person's email address and the reason for/time of the exclusion. | | List of Metrics | Returns a list of metrics in Klaviyo. | | List Of Timeline Of All Events | Returns a batched timeline of all events in your Klaviyo account. | | List Of Timeline Of All Events Append Only | Returns a batched timeline of all events in your Klaviyo account. This report works in Append mode only and fetches the data after the last successful run, assuming that the end date offset is 1. | | Listing a Person's Complete Event Timeline | Returns a batched timeline of all events for a specified person. | | Listing a Person's Complete Event Timeline (With Extra Properties) | Returns a batched timeline of all events for a specified person with extra properties. | | Listing a Person's Complete Event Timeline (with Person's Data) | Returns a batched timeline of all events for a specified person, with that person's data. | | Listing The Event Timeline For A Particular Metric | Returns a batched timeline for one specific type of metric. | | Listing The Event Timeline For A Particular Metric (With Extra Properties) | Returns a batched timeline for one specific type of metric, with extra properties. | | Listing The Event Timeline For A Particular Metric (With Person's Data) | Returns a batched timeline for one specific type of metric with a person's data. | | Lists in Account | Returns a list of accounts. | | Segments In Account | Returns a list of all the segments in your account. | | Tags | Returns a list of all the tags in your account. | | Tags For Campaigns | Returns a list of all tags that belong to the given campaign. | |
    | Campaign Selection | This field only appears when you select the **Tags For Campaigns** report or the **Campaign Recipient Estimation** report.  You can select any one of the three options from the drop-down list:   - **All Campaigns** - Fetches all the campaigns. - **Specific Campaigns** - When you select this option, it shows the available list of campaigns from the **Campaigns** field. You can select the specific campaigns. - **Enter Campaigns** - When you select this option, the **Enter Campaigns** text box appears. You can enter the campaigns which are not available in the **Campaigns** field. |
    | Campaign | This field only appears when you select the **Campaign Information** report.  Select the campaign for which you want to retrieve the information. You can select only one campaign from the list of available campaigns. |
    | Campaigns | This field only appears when you select the **Tags For Campaigns** report or the **Campaign Recipient Estimation** report.  Select the campaign(s) for which you want to retrieve the information. You have the option to select multiple campaigns from the list of available campaigns. |
    | Enter Campaigns | This field appears when you select the **Enter Campaigns** option from the **Campaign Selection** drop-down list.  Enter the Comma Separated campaign IDs. To fetch the campaign IDs, run the 'Campaigns In Account' report and paste the comma-separated IDs without spaces. (For example: 123abc,789xyz). |
    | List | Select the list you want to retrieve information for. |
    | Person ID | Enter the ID of the person you want to retrieve information for. |
    | Metric | Select the metric type you want to retrieve information for. |
    | Type | Select whether you want to filter results by list or segment. If you select **No Selection**, all results are shown. |
    | Event Property | Select the property to segment the data. |
    | Select the granularity | Select the granularity. |
    | Measurement | Select the measurement keys. |
    | Duration | Select the duration for the report (a Single Date, or a Date Range). |
    | Report Date | Select report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. |
    | Select Specific Date | Select the report date using the date selector. |
    | Days back | The number of days back that you would like to get data for. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Start Date | Select report start date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. |
    | Select Specific Start Date | Select the report date using the date selector. |
    | Days back to start from | The number of days back that you would like to get data from (start day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | End Date | Select report end date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. |
    | Select Specific End Date | Select the report date using the date selector. |
    | Days back to end at | The number of days back that you would like to get data to (end day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Select Status of Flow Message to Retrieve | Select the status of the Flow Message.  The following options appear when you select the drop-down list:   - Retrieve data for all Flow Messages - Select specific Flow Message |
    | Select Flows | This option appears when you select the **Select specific Flow Message** option from the **Select Status of Flow Message to Retrieve** drop-down list.  Select from the list of available flows. You need to select at least one flow. |
    | Action Flows | This option appears when you select at least one flow. Select from the list of available action flows.  You need to select at least one action flow. |
    | Flow Message IDs | Select the Flows Message IDs. You need to select at least one flow message ID. |

CHANGE 10  [## Other Panes]  (line 35 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/360042927994.mdx
TITLE:  Shopify Connector
LANG:   English (en_US)
ID:     360042927994
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Shopify is an e-commerce platform that enables individuals and businesses to create online stores. To learn more about the Shopify API, go to [https://docs.shopify.com/api](https://docs.shopify.com/api "https://docs.shopify.com/api").

  NEW (replace with / insert from CSV):
    Shopify is an e-commerce platform that enables individuals and businesses to create online stores.  To learn more about the Shopify API, go to [https://docs.shopify.com/api](https://docs.shopify.com/api).

CHANGE 2  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    This topic discusses the fields and menus that are specific to the Shopify connector user interface. For general information about adding DataSets, setting update schedules, and editing DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274 "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This topic discusses the fields and menus that are specific to the Shopify connector user interface. For general information about adding DataSets, setting update schedules, and editing DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Intro]  (line 13 in repo file)

  OLD (remove from repo):
    <table class="mt-responsive-table" data-aura-rendered-by="33:207;a"><tbody><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Use Cases </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> This connector is appropriate for retrieving data for ecommerce sales, inventory, deliveries, and the like. It also supports blog and marketing information. </p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><b> Primary Metrics </b></td><td class="mt-noheading" colspan="1" rowspan="1"> Returns sales information for your ecommerce shop. You can also get metrics on the blog and marketing side of your business. </td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Company Roles </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><ul><li><p> Warehouse managers </p></li><li><p> Ecommerce shop admins </p></li><li><p> Online sales </p></li><li><p> Owner/C-Suite </p></li></ul></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Average Implementation Time </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> Less than an hour </p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Ease of Use (on a 1-to-10 scale with 1 being easiest) </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> 2 </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector is appropriate for retrieving data for ecommerce sales, inventory, deliveries, and the like. It also supports blog and marketing information. |
    | **Primary Metrics** | Returns sales information for your ecommerce shop. You can also get metrics on the blog and marketing side of your business. |
    | **Primary Company Roles** | - Warehouse managers - Ecommerce shop admins - Online sales - Owner/C-Suite |
    | **Average Implementation Time** | Less than an hour |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 2 |

CHANGE 4  [## Prerequisites]  (line 17 in repo file)

  OLD (remove from repo):
    Before connecting a Shopify store to Domo, you must install the Domo Shopify app ([https://apps.shopify.com/domo-inc](https://apps.shopify.com/domo-inc "https://apps.shopify.com/domo-inc") ) from your Shopify instance. You need to install the app in every store for which you want to connect to Domo.

  NEW (replace with / insert from CSV):
    Before connecting a Shopify store to Domo, you must install the Domo Shopify app ([https://apps.shopify.com/domo-inc](https://apps.shopify.com/domo-inc)) from your Shopify instance. You need to install the app in every store for which you want to connect to Domo.

CHANGE 5  [## Prerequisites]  (lines 21–23 in repo file)

  OLD (remove from repo):
    1. Navigate to https://apps.shopify.com/domo-inc.
    
       The below screen appears.

  NEW (replace with / insert from CSV):
    1. Navigate to [https://apps.shopify.com/domo-inc](https://apps.shopify.com/domo-inc).
       The below screen appears.

CHANGE 6  [## Prerequisites]  (lines 25–27 in repo file)

  OLD (remove from repo):
       <Note>
       **Note:** Ensure that you have selected the correct store in the top right corner of your page.
       </Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** Ensure that you have selected the correct store in the top right corner of your page.</Note>

CHANGE 7  [## Prerequisites]  (lines 31–32 in repo file)

  OLD (remove from repo):
    2. Click **Install**, to install the Domo app in your store.
    

  NEW (replace with / insert from CSV):
    2. Click **Install**,to install the Domo app in your store.

CHANGE 8  [## Prerequisites]  (lines 34–40 in repo file)

  OLD (remove from repo):
    3. From the **Install app** page, you will be prompted to confirm the Domo app's access to your store.
    
       <Note>
       **Note:** The data access request is for objects that can be pulled through the connector. If you are comfortable providing read access to the data, then click **Install**. Domo will only pull the data you select and on your selected schedule. Domo will not pull any data from Shopify that you do not request.
       </Note>
    
       <Frame><img alt="A screenshot of a computerAI-generated content may be incorrect." src="/images/kb/ka0Vq000000IONF-00N5w00000Ri7BU-0EMVq000006Tb3x.jpg" style={{width: 539, height: 358}}/></Frame>

  NEW (replace with / insert from CSV):
    3. From the **Install app** page, you will be prompted to confirm the Domo app's access to your store.
    
    <Note>**Note:** The data access request is for objects that can be pulled through the connector. If you are comfortable providing read access to the data, then click **Install**. Domo will only pull the data you select and on your selected schedule. Domo will not pull any data from Shopify that you do not request.</Note>

CHANGE 9  [## Prerequisites]  (lines 42–48 in repo file)

  OLD (remove from repo):
    4. When you click **Install**, a new window opens, from where you will be asked to sign in to Domo. Enter your Domo instance name and click **Continue**.
    
       <Note>
       **Note:** If you have not previously logged in to the Domo specified instance, you will be asked to log in. If you do not have a Domo instance, then click the **Sign up free** link to obtain a Domo instance.
       </Note>
    
       <Frame><img alt="A screenshot of a computerAI-generated content may be incorrect." src="/images/kb/ka0Vq000000IONF-00N5w00000Ri7BU-0EMVq000006Tb2M.jpg" style={{width: 517, height: 499}}/></Frame>

  NEW (replace with / insert from CSV):
       <Frame>![A screenshot of a computerAI-generated content may be incorrect.](/images/kb/0EMVq000006Tb3x.jpg)</Frame>
    4. When you click **Install**, a new window opens, from where you will be asked to sign in to Domo. Enter your Domo instance name and click **Continue**.
    
    <Note>**Note:** If you have not previously logged in to the Domo specified instance, you will be asked to log in. If you do not have a Domo instance, then click the **Sign up free** link to obtain a Domo instance.</Note>

CHANGE 10  [## Prerequisites]  (line 56 in repo file)

  OLD (remove from repo):
    ## Connecting to Your Shopify Account

  NEW (replace with / insert from CSV):
    ## Connecting to Your Shopify Account

CHANGE 11  [## Connecting to Your Shopify Account]  (line 60 in repo file)

  OLD (remove from repo):
    The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274 "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 12  [## Details Pane]  (line 64 in repo file)

  OLD (remove from repo):
    This pane contains one primary menu, **Report**, and other menus that appear depending on the report you select.

  NEW (replace with / insert from CSV):
    This pane contains one primary menu, **Report**, and other menus that appear depending on the report you select.

CHANGE 13  [## Details Pane]  (lines 66–68 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:** Currently you can enter only one search string in fields that request search strings, such as **Order Name**.
    </Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** Currently you can enter only one search string in fields that request search strings, such as **Order Name**.</Note>

CHANGE 14  [## Details Pane]  (line 70 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:207;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the desired Shopify report. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p><b> Report Name </b></p></td><td colspan="1" rowspan="1"><p><b> Description </b></p></td></tr><tr><td colspan="1" rowspan="1"><p> Application Charges </p></td><td colspan="1" rowspan="1"><p> Receives data about Shopify Application Charges. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Application Credits </p></td><td colspan="1" rowspan="1"><p> Receives data about Shopify Application Credits. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Articles </p></td><td colspan="1" rowspan="1"><p> Receives data about Shopify Blog Articles. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Assets </p></td><td colspan="1" rowspan="1"><p> Receives data about Shopify Assets. </p></td></tr><tr><td colspan="1" rowspan="1"> Blogs </td><td colspan="1" rowspan="1"> Receives data about Shopify Blogs. </td></tr><tr><td colspan="1" rowspan="1"> Carrier Services </td><td colspan="1" rowspan="1"> Receives data about Shopify Carrier Services. </td></tr><tr><td colspan="1" rowspan="1"> Checkouts </td><td colspan="1" rowspan="1"> Receives data about Shopify Checkouts. </td></tr><tr><td colspan="1" rowspan="1"> Collects </td><td colspan="1" rowspan="1"> Returns information about collections, such as creation and last update times, product IDs, and collection IDs. </td></tr><tr><td colspan="1" rowspan="1"> Comments </td><td colspan="1" rowspan="1"> Receives data about Shopify Blog Article Comments. </td></tr><tr><td colspan="1" rowspan="1"> Countries </td><td colspan="1" rowspan="1"> Receives data about Shopify Countries. </td></tr><tr><td colspan="1" rowspan="1"> Custom Collections </td><td colspan="1" rowspan="1"> Receives data about Shopify Custom Collections. </td></tr><tr><td colspan="1" rowspan="1"> Customers </td><td colspan="1" rowspan="1"> Receives data about Shopify Customers. </td></tr><tr><td colspan="1" rowspan="1"><p> Customers Using GraphQL </p></td><td colspan="1" rowspan="1"> Receives data about Shopify Customers using GraphQL. </td></tr><tr><td colspan="1" rowspan="1"> Disputes </td><td colspan="1" rowspan="1"> Receives data about Shopify Disputes. </td></tr><tr><td colspan="1" rowspan="1"> Discount Codes </td><td colspan="1" rowspan="1"> Receives data about Shopify Discount Codes. </td></tr><tr><td colspan="1" rowspan="1"> Draft Orders </td><td colspan="1" rowspan="1"> Receives data about Shopify Draft Orders. </td></tr><tr><td colspan="1" rowspan="1"><p> Execute GraphQL Query </p></td><td colspan="1" rowspan="1"> Executes your GraphQL Query. </td></tr><tr><td colspan="1" rowspan="1"> Fulfillments </td><td colspan="1" rowspan="1"> Receives data about Shopify Fulfillments. </td></tr><tr><td colspan="1" rowspan="1"> Gift Cards (Shopify Plus) </td><td colspan="1" rowspan="1"><p> Receives data about Shopify Gift Cards. </p><p><b> Note </b>: This requires Shopify Plus subscription. </p></td></tr><tr><td colspan="1" rowspan="1"> Inventory Items </td><td colspan="1" rowspan="1"> Recieves data about Shopify Inventory Items. This report complements the Product Variants report. </td></tr><tr><td colspan="1" rowspan="1"><p> Inventory Items Using GraphQL </p></td><td colspan="1" rowspan="1"> Recieves data about Shopify Inventory Items using GraphQL API. This report complements the Product Variants report. </td></tr><tr><td colspan="1" rowspan="1"> Locations </td><td colspan="1" rowspan="1"> Receives data about Shopify Locations. </td></tr><tr><td colspan="1" rowspan="1"> Marketing Events </td><td colspan="1" rowspan="1"> Receives data about Shopify Marketing Events. </td></tr><tr><td colspan="1" rowspan="1"><p> Metafield Definitions </p></td><td colspan="1" rowspan="1"> Receives data about Shopify Metafield Definitions. </td></tr><tr><td colspan="1" rowspan="1"> Orders </td><td colspan="1" rowspan="1"> Receives data about Shopify Orders. </td></tr><tr><td colspan="1" rowspan="1"> Order Risks </td><td colspan="1" rowspan="1"> Receives data about Shopify Order Risks. </td></tr><tr><td colspan="1" rowspan="1"> Pages </td><td colspan="1" rowspan="1"> Receives data about Shopify Pages. </td></tr><tr><td colspan="1" rowspan="1"> Payouts </td><td colspan="1" rowspan="1"> Receives data about Shopify Payouts. </td></tr><tr><td colspan="1" rowspan="1"> Policies </td><td colspan="1" rowspan="1"> Receives data about Shopify Store Policies. </td></tr><tr><td colspan="1" rowspan="1"> Price Rules </td><td colspan="1" rowspan="1"> Receives data about Shopify Price Rules. </td></tr><tr><td colspan="1" rowspan="1"> Product Variants (Deprecated) </td><td colspan="1" rowspan="1"> This report has been deprecated. You can use the Product Variants with GraphQL report instead. </td></tr><tr><td colspan="1" rowspan="1"> Products (Deprecated) </td><td colspan="1" rowspan="1"> This report has been deprecated. You can use the Products with GraphQL report instead. </td></tr><tr><td colspan="1" rowspan="1"><p> Product Variants Using GraphQL </p></td><td colspan="1" rowspan="1"> Receives the data about Shopify Product Variants. </td></tr><tr><td colspan="1" rowspan="1"><p> Products Using GraphQL </p></td><td colspan="1" rowspan="1"> Receives the data about Shopify Products. </td></tr><tr><td colspan="1" rowspan="1"> Provinces of Country </td><td colspan="1" rowspan="1"> Receives data about Shopify Provinces of Countries. </td></tr><tr><td colspan="1" rowspan="1"> Recurring Application Charges </td><td colspan="1" rowspan="1"> Receives data about Shopify Recurring Application Charges. </td></tr><tr><td colspan="1" rowspan="1"> Redirects </td><td colspan="1" rowspan="1"> Receives data about Shopify Redirects. </td></tr><tr><td colspan="1" rowspan="1"> Refunds </td><td colspan="1" rowspan="1"> Receives data about Shopify Order Refunds. </td></tr><tr><td colspan="1" rowspan="1"> Resource Feedback </td><td colspan="1" rowspan="1"> Receives data about Shopify Resource Feedback. </td></tr><tr><td colspan="1" rowspan="1"> Script Tags </td><td colspan="1" rowspan="1"> Receives data about Shopify Script Tags. </td></tr><tr><td colspan="1" rowspan="1"> Shipping Zones </td><td colspan="1" rowspan="1"> Receives data about Shopify Shipping Zones. </td></tr><tr><td colspan="1" rowspan="1"> Shop </td><td colspan="1" rowspan="1"> Receives data about Shopify Shop. </td></tr><tr><td colspan="1" rowspan="1"> Smart Collections </td><td colspan="1" rowspan="1"> Receives data about Shopify Smart Collections. </td></tr><tr><td colspan="1" rowspan="1"> Themes </td><td colspan="1" rowspan="1"> Receives data about Shopify Themes. </td></tr><tr><td colspan="1" rowspan="1"> Transactions </td><td colspan="1" rowspan="1"> Receives data about Shopify Transactions. </td></tr><tr><td colspan="1" rowspan="1"> Usage Charges </td><td colspan="1" rowspan="1"> Receives data about Shopify Usage Charges. </td></tr><tr><td colspan="1" rowspan="1"> Webhooks </td><td colspan="1" rowspan="1"> Receives data about Shopify Webhooks. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Subreport </td><td colspan="1" rowspan="1"> Select a sub-report to filter the data. Selecting a sub-report will expand additional data available for the report. </td></tr><tr><td colspan="1" rowspan="1"> Date Filter </td><td colspan="1" rowspan="1"> Select the date type to filter the data. <b> Created </b> receives data based on its created date, <b> Published </b> receives data based on its published date, and <b> Updated </b> receives data based on its updated date. It should be noted that when the requested object is a subset of another object, the date will filter based on the parent object. For example, Fulfillments are a part of the Order object, so when requesting the Fulfillments report, your date selection will be filtered on the Orders those fulfillments belong to, not the fulfillments themselves. </td></tr><tr><td colspan="1" rowspan="1"> Financial Status </td><td colspan="1" rowspan="1"> Filter orders by their financial status. </td></tr><tr><td colspan="1" rowspan="1"> Duration </td><td colspan="1" rowspan="1"> Select the duration for the report (a Single Date, or a Date Range). </td></tr><tr><td colspan="1" rowspan="1"> Report Date </td><td colspan="1" rowspan="1"> Select the report date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> Select Specific Date </td><td colspan="1" rowspan="1"> Select the report date using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> Days Back </td><td colspan="1" rowspan="1"> The number of days back that you would like to get data for. Specify either today or 0, yesterday or 1, or today or 7 to get data for 7 days into the past. </td></tr><tr><td colspan="1" rowspan="1"> Start Date </td><td colspan="1" rowspan="1"> Select the report start date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> Select Specific Start Date </td><td colspan="1" rowspan="1"> Select the report date using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> Days Back To Start From </td><td colspan="1" rowspan="1"> The number of days back that you would like to get data from (start day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. </td></tr><tr><td colspan="1" rowspan="1"> End Date </td><td colspan="1" rowspan="1"> Select the report end date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> Select Specific End Date </td><td colspan="1" rowspan="1"> Select the report date using the date selector. </td></tr><tr><td colspan="1" rowspan="1"> Days Back To End At </td><td colspan="1" rowspan="1"> The number of days back that you would like to get data to (end day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. </td></tr><tr><td colspan="1" rowspan="1"> Status </td><td colspan="1" rowspan="1"> Select whether the report contains information for cancelled, closed, or open items. If you select <b> Any </b>, all items appear regardless of the status. </td></tr><tr><td colspan="1" rowspan="1"> Published Status </td><td colspan="1" rowspan="1"> Select whether the report contains information for published or unpublished items. If you select <b> Any </b>, both published and unpublished items will appear. </td></tr><tr><td colspan="1" rowspan="1"> Fulfillment Status </td><td colspan="1" rowspan="1"> Select whether the report contains information for partial, shipped, or unshipped orders. If you select <b> Any </b>, all items appear regardless of the fulfillment status. </td></tr><tr><td colspan="1" rowspan="1"> Backfill Data </td><td colspan="1" rowspan="1"> Select this checkbox, to retrieve new data with APPEND mode, only in consecutive runs of the dataset. </td></tr><tr><td colspan="1" rowspan="1"> Merge Mode </td><td colspan="1" rowspan="1"> Select this option if you want to merge the functionality when the sub-report is "None". Scheduling set below will not have any effect if this option is selected. Does not work for the first run. </td></tr><tr><td colspan="1" rowspan="1"> GraphQL Query </td><td colspan="1" rowspan="1"> Enter the GraphQL query to execute. For more information on Shopify GraphQL queries, refer to the Shopify API documentation. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the desired Shopify report. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Application Charges | Receives data about Shopify Application Charges. | | Application Credits | Receives data about Shopify Application Credits. | | Articles | Receives data about Shopify Blog Articles. | | Assets | Receives data about Shopify Assets. | | Blogs | Receives data about Shopify Blogs. | | Carrier Services | Receives data about Shopify Carrier Services. | | Checkouts | Receives data about Shopify Checkouts. | | Collects | Returns information about collections, such as creation and last update times, product IDs, and collection IDs. | | Comments | Receives data about Shopify Blog Article Comments. | | Countries | Receives data about Shopify Countries. | | Custom Collections | Receives data about Shopify Custom Collections. | | Customers | Receives data about Shopify Customers. | | Customers Using GraphQL | Receives data about Shopify Customers using GraphQL. | | Disputes | Receives data about Shopify Disputes. | | Discount Codes | Receives data about Shopify Discount Codes. | | Draft Orders | Receives data about Shopify Draft Orders. | | Execute Bulk GraphQL Query | Executes your GraphQL Query as a Bulk Operation. | | Execute GraphQL Query | Executes your GraphQL Query. | | Fulfillments | Receives data about Shopify Fulfillments. | | Gift Cards (Shopify Plus) | Receives data about Shopify Gift Cards.  **Note**: This requires Shopify Plus subscription. | | Inventory Items | Recieves data about Shopify Inventory Items. This report complements the Product Variants report. | | Inventory Items Using GraphQL | Recieves data about Shopify Inventory Items using GraphQL API. This report complements the Product Variants report. | | Locations | Receives data about Shopify Locations. | | Marketing Events | Receives data about Shopify Marketing Events. | | Metafield Definitions | Receives data about Shopify Metafield Definitions. | | Orders | Receives data about Shopify Orders. | | Order Risks | Receives data about Shopify Order Risks. | | Pages | Receives data about Shopify Pages. | | Payouts | Receives data about Shopify Payouts. | | Policies | Receives data about Shopify Store Policies. | | Price Rules | Receives data about Shopify Price Rules. | | Product Variants (Deprecated) | This report has been deprecated. You can use the Product Variants with GraphQL report instead. | | Products (Deprecated) | This report has been deprecated. You can use the Products with GraphQL report instead. | | Product Variants Using GraphQL | Receives the data about Shopify Product Variants. | | Products Using GraphQL | Receives the data about Shopify Products. | | Provinces of Country | Receives data about Shopify Provinces of Countries. | | Recurring Application Charges | Receives data about Shopify Recurring Application Charges. | | Redirects | Receives data about Shopify Redirects. | | Refunds | Receives data about Shopify Order Refunds. | | Resource Feedback | Receives data about Shopify Resource Feedback. | | Script Tags | Receives data about Shopify Script Tags. | | Shipping Zones | Receives data about Shopify Shipping Zones. | | Shop | Receives data about Shopify Shop. | | Smart Collections | Receives data about Shopify Smart Collections. | | Themes | Receives data about Shopify Themes. | | Transactions | Receives data about Shopify Transactions. | | Usage Charges | Receives data about Shopify Usage Charges. | | Webhooks | Receives data about Shopify Webhooks. | |
    | Subreport | Select a sub-report to filter the data. Selecting a sub-report will expand additional data available for the report. |
    | Date Filter | Select the date type to filter the data. **Created** receives data based on its created date, **Published** receives data based on its published date, and **Updated** receives data based on its updated date. It should be noted that when the requested object is a subset of another object, the date will filter based on the parent object. For example, Fulfillments are a part of the Order object, so when requesting the Fulfillments report, your date selection will be filtered on the Orders those fulfillments belong to, not the fulfillments themselves. |
    | Financial Status | Filter orders by their financial status. |
    | Duration | Select the duration for the report (a Single Date, or a Date Range). |
    | Report Date | Select the report date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific Date | Select the report date using the date selector. |
    | Days Back | The number of days back that you would like to get data for. Specify either today or 0, yesterday or 1, or today or 7 to get data for 7 days into the past. |
    | Start Date | Select the report start date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific Start Date | Select the report date using the date selector. |
    | Days Back To Start From | The number of days back that you would like to get data from (start day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | End Date | Select the report end date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific End Date | Select the report date using the date selector. |
    | Days Back To End At | The number of days back that you would like to get data to (end day). Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Status | Select whether the report contains information for cancelled, closed, or open items. If you select **Any**, all items appear regardless of the status. |
    | Published Status | Select whether the report contains information for published or unpublished items. If you select **Any**, both published and unpublished items will appear. |
    | Fulfillment Status | Select whether the report contains information for partial, shipped, or unshipped orders. If you select **Any**, all items appear regardless of the fulfillment status. |
    | Backfill Data | Select this checkbox, to retrieve new data with APPEND mode, only in consecutive runs of the dataset. |
    | Merge Mode | Select this option if you want to merge the functionality when the sub-report is "None". Scheduling set below will not have any effect if this option is selected. Does not work for the first run. |
    | GraphQL Query | Enter the GraphQL query to execute. For more information on Shopify GraphQL queries, refer to the Shopify API documentation. |

CHANGE 15  [### Other Panes]  (line 74 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274 "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 16  [## Troubleshooting]  (line 78 in repo file)

  OLD (remove from repo):
    Ensure the **Shop Name** you enter in the **Credentials** pane is just the subdomain of your URL. For example, the **Shop Name** for [domo.shopify.com](http://domo.shopify.com) would be domo.

  NEW (replace with / insert from CSV):
    Ensure the **Shop Name** you enter in the **Credentials** pane is just the subdomain of your URL. For example, the **Shop Name** for domo.shopify.com would be domo.

CHANGE 17  [### FAQs]  (lines 82–83 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="What endpoint is the base URL for this connector?">

  NEW (replace with / insert from CSV):
    #### What endpoint is the base URL for this connector?

CHANGE 18  [### FAQs]  (lines 85–86 in repo file)

  OLD (remove from repo):
    The base URL for the Shopify connector is `https://{shop-name}.myshopify.com/admin/api/{apiVersion}`.
    </Accordion>

  NEW (replace with / insert from CSV):
    The base URL for the Shopify connector is https://{shop-name}.myshopify.com/admin/api/{apiVersion}.

CHANGE 19  [### FAQs]  (line 88 in repo file)

  OLD (remove from repo):
    <Accordion title="Which endpoint does each report call in this connector?">

  NEW (replace with / insert from CSV):
    #### Which endpoint does each report call in this connector?

CHANGE 20  [### FAQs]  (lines 90–91 in repo file)

  ⚠ LARGE CHANGE (51 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:207;a"><thead><tr><th colspan="1" rowspan="1"><p> Report Name </p></th><th colspan="1" rowspan="1"><p> Endpoint URL </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Application Charges </p></td><td colspan="1" rowspan="1"><p> /application_charges.json </p></td></tr><tr><td colspan="1" rowspan="1"><p> Application Credits </p></td><td colspan="1" rowspan="1"><p> /application_credits </p></td></tr><tr><td colspan="1" rowspan="1"><p> Articles </p></td><td colspan="1" rowspan="1"><p> /blogs/{blogIds}/articles.json </p></td></tr><tr><td colspan="1" rowspan="1"> Assets </td><td colspan="1" rowspan="1"> /themes/assets.json </td></tr><tr><td colspan="1" rowspan="1"> Blogs </td><td colspan="1" rowspan="1"> /blogs.json </td></tr><tr><td colspan="1" rowspan="1"> Carrier Services </td><td colspan="1" rowspan="1"> /carrier_services </td></tr><tr><td colspan="1" rowspan="1"> Checkouts </td><td colspan="1" rowspan="1"> /checkouts.json </td></tr><tr><td colspan="1" rowspan="1"> Collects </td><td colspan="1" rowspan="1"> /collects.json </td></tr><tr><td colspan="1" rowspan="1"> Comments </td><td colspan="1" rowspan="1"> /comments.json </td></tr><tr><td colspan="1" rowspan="1"> Countries </td><td colspan="1" rowspan="1"> /countries.json </td></tr><tr><td colspan="1" rowspan="1"> Custom Collections </td><td colspan="1" rowspan="1"> /custom_collections.json </td></tr><tr><td colspan="1" rowspan="1"> Customers </td><td colspan="1" rowspan="1"> /customers </td></tr><tr><td colspan="1" rowspan="1"> Discount Codes </td><td colspan="1" rowspan="1"> /price_rules/discount_codes </td></tr><tr><td colspan="1" rowspan="1"> Disputes </td><td colspan="1" rowspan="1"> /shopify_payments/disputes </td></tr><tr><td colspan="1" rowspan="1"> Draft Orders </td><td colspan="1" rowspan="1"> /draft_orders </td></tr><tr><td colspan="1" rowspan="1"><p> Execute GraphQL Query </p></td><td colspan="1" rowspan="1"><span><span dir="ltr"> /graphql.json </span></span></td></tr><tr><td colspan="1" rowspan="1"> Gift Cards (Shopify Plus) </td><td colspan="1" rowspan="1"> /gift_cards </td></tr><tr><td colspan="1" rowspan="1"> Inventory Items </td><td colspan="1" rowspan="1"> /inventory_items </td></tr><tr><td colspan="1" rowspan="1"><p> Inventory Items Using GraphQL </p></td><td colspan="1" rowspan="1"><span dir="ltr"> /graphql.json </span></td></tr><tr><td colspan="1" rowspan="1"> Fulfillments </td><td colspan="1" rowspan="1"> /orders/{orderId}/fulfillments </td></tr><tr><td colspan="1" rowspan="1"> Locations </td><td colspan="1" rowspan="1"> /locations </td></tr><tr><td colspan="1" rowspan="1"> Marketing Events </td><td colspan="1" rowspan="1"> /marketing_events </td></tr><tr><td colspan="1" rowspan="1"><p> Metafield Definitions </p></td><td colspan="1" rowspan="1"><span dir="ltr"> /graphql.json </span></td></tr><tr><td colspan="1" rowspan="1"> Orders </td><td colspan="1" rowspan="1"> /orders </td></tr><tr><td colspan="1" rowspan="1"> Pages </td><td colspan="1" rowspan="1"> /pages </td></tr><tr><td colspan="1" rowspan="1"> Payouts </td><td colspan="1" rowspan="1"> /shopify_payments/payouts </td></tr><tr><td colspan="1" rowspan="1"> Policies </td><td colspan="1" rowspan="1"> /policies </td></tr><tr><td colspan="1" rowspan="1"> Price Rules </td><td colspan="1" rowspan="1"> /price_rules </td></tr><tr><td colspan="1" rowspan="1"> Product Variants (Deprecated) </td><td colspan="1" rowspan="1"> /products/{productId}/variants </td></tr><tr><td colspan="1" rowspan="1"> Products (Deprecated) </td><td colspan="1" rowspan="1"> /products </td></tr><tr><td colspan="1" rowspan="1"><p> Product Variants Using GraphQL </p></td><td colspan="1" rowspan="1"><span><span dir="ltr"> /graphql.json </span></span></td></tr><tr><td colspan="1" rowspan="1"><p> Products Using GraphQL </p></td><td colspan="1" rowspan="1"><span><span dir="ltr"> /graphql.json </span></span></td></tr><tr><td colspan="1" rowspan="1"> Provinces of Country </td><td colspan="1" rowspan="1"> /countries/provinces </td></tr><tr><td colspan="1" rowspan="1"> Recurring Application Charges </td><td colspan="1" rowspan="1"> /recurring_application_charges </td></tr><tr><td colspan="1" rowspan="1"> Redirects </td><td colspan="1" rowspan="1"> /redirects </td></tr><tr><td colspan="1" rowspan="1"> Refunds </td><td colspan="1" rowspan="1"> /orders/{orderId}/refunds </td></tr><tr><td colspan="1" rowspan="1"> Resource Feedback </td><td colspan="1" rowspan="1"> /resource_feedback </td></tr><tr><td colspan="1" rowspan="1"> Order Risks </td><td colspan="1" rowspan="1"> /orders/{orderId}/risks </td></tr><tr><td colspan="1" rowspan="1"> Script Tags </td><td colspan="1" rowspan="1"> /script_tags </td></tr><tr><td colspan="1" rowspan="1"> Shipping Zones </td><td colspan="1" rowspan="1"> /shipping_zones </td></tr><tr><td colspan="1" rowspan="1"> Shop </td><td colspan="1" rowspan="1"> /shop </td></tr><tr><td colspan="1" rowspan="1"> Smart Collections </td><td colspan="1" rowspan="1"> /smart_collections </td></tr><tr><td colspan="1" rowspan="1"> Themes </td><td colspan="1" rowspan="1"> /themes </td></tr><tr><td colspan="1" rowspan="1"> Transactions </td><td colspan="1" rowspan="1"> /orders/{orderId}/transactions </td></tr><tr><td colspan="1" rowspan="1"> Usage Charges </td><td colspan="1" rowspan="1"> /recurring_application_charges/usage_charges </td></tr><tr><td colspan="1" rowspan="1"> Webhooks </td><td colspan="1" rowspan="1"> /webhooks </td></tr></tbody></table>
    </Accordion>

  NEW (replace with / insert from CSV):
    | Report Name | Endpoint URL |
    | --- | --- |
    | Application Charges | /application\_charges.json |
    | Application Credits | /application\_credits |
    | Articles | /blogs/{blogIds}/articles.json |
    | Assets | /themes/assets.json |
    | Blogs | /blogs.json |
    | Carrier Services | /carrier\_services |
    | Checkouts | /checkouts.json |
    | Collects | /collects.json |
    | Comments | /comments.json |
    | Countries | /countries.json |
    | Custom Collections | /custom\_collections.json |
    | Customers | /customers |
    | Discount Codes | /price\_rules/discount\_codes |
    | Disputes | /shopify\_payments/disputes |
    | Draft Orders | /draft\_orders |
    | Execute Bulk GraphQL Query | /graphql.json |
    | Execute GraphQL Query | /graphql.json |
    | Gift Cards (Shopify Plus) | /gift\_cards |
    | Inventory Items | /inventory\_items |
    | Inventory Items Using GraphQL | /graphql.json |
    | Fulfillments | /orders/{orderId}/fulfillments |
    | Locations | /locations |
    | Marketing Events | /marketing\_events |
    | Metafield Definitions | /graphql.json |
    | Orders | /orders |
    | Pages | /pages |
    | Payouts | /shopify\_payments/payouts |
    | Policies | /policies |
    | Price Rules | /price\_rules |
    | Product Variants (Deprecated) | /products/{productId}/variants |
    | Products (Deprecated) | /products |
    | Product Variants Using GraphQL | /graphql.json |
    | Products Using GraphQL | /graphql.json |
    | Provinces of Country | /countries/provinces |
    | Recurring Application Charges | /recurring\_application\_charges |
    | Redirects | /redirects |
    | Refunds | /orders/{orderId}/refunds |
    | Resource Feedback | /resource\_feedback |
    | Order Risks | /orders/{orderId}/risks |
    | Script Tags | /script\_tags |
    | Shipping Zones | /shipping\_zones |
    | Shop | /shop |
    | Smart Collections | /smart\_collections |
    | Themes | /themes |
    | Transactions | /orders/{orderId}/transactions |
    | Usage Charges | /recurring\_application\_charges/usage\_charges |
    | Webhooks | /webhooks |

CHANGE 21  [### FAQs]  (line 93 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?

CHANGE 22  [### FAQs]  (lines 95–96 in repo file)

  OLD (remove from repo):
    You need the name of your Shopify shop to power up this connector, and your Shopify username and password. Shop owners can use the connector without setting any additional permissions. However, if you have a limited user account - a **Staff** account, you will need to ensure that you have permissions to **Apps** and any other resources you wish to pull from the connector. Visit [Shopify documentation](https://help.shopify.com/en/manual/your-account/staff-accounts/staff-permissions "Shopify documentation").
    </Accordion>

  NEW (replace with / insert from CSV):
    You need the name of your Shopify shop to power up this connector, and your Shopify username and password. Shop owners can use the connector without setting any additional permissions. However, if you have a limited user account - a **Staff** account, you will need to ensure that you have permissions to **Apps** and any other resources you wish to pull from the connector. Visit [Shopify documentation](https://help.shopify.com/en/manual/your-account/staff-accounts/staff-permissions).

CHANGE 23  [### FAQs]  (line 98 in repo file)

  OLD (remove from repo):
    <Accordion title="Where do I get my Shopify shop name in the credentials section?">

  NEW (replace with / insert from CSV):
    #### Where do I get my Shopify shop name in the credentials section?

CHANGE 24  [### FAQs]  (lines 100–101 in repo file)

  OLD (remove from repo):
    The Shopify shop name is found in your shop dashboard URL. For example, if your shop dashboard is [domo.shopify.com](http://domo.shopify.com), your shop name is "domo".
    </Accordion>

  NEW (replace with / insert from CSV):
    The Shopify shop name is found in your shop dashboard URL. For example, if your shop dashboard is domo.shopify.com, your shop name is "domo".

CHANGE 25  [### FAQs]  (line 103 in repo file)

  OLD (remove from repo):
    <Accordion title="Can I use the same account for multiple datasets?">

  NEW (replace with / insert from CSV):
    #### Can I use the same account for multiple datasets?

CHANGE 26  [### FAQs]  (line 106 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 27  [### FAQs]  (line 108 in repo file)

  OLD (remove from repo):
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?

CHANGE 28  [### FAQs]  (lines 111–113 in repo file)

  OLD (remove from repo):
    
    </Accordion>
    </AccordionGroup>

  [3 image-only change(s) suppressed — handle separately if needed]

==============================================================================
FILE:   s/article/360042928074.mdx
TITLE:  ADP Connector
LANG:   English (en_US)
ID:     360042928074
==============================================================================

CHANGE 1  [### Creating a Mutual Authentication Certificate]  (line 64 in repo file)

  OLD (remove from repo):
    <Note>**Note:** Your CSR must not request S/MIME capabilities.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** Your CSR must not request S/MIME capabilities.

CHANGE 2  [### Creating a Mutual Authentication Certificate]  (line 76 in repo file)

  OLD (remove from repo):
       4. Enter your company name and ADP client ID.

  NEW (replace with / insert from CSV):
       4. Enter your company name and ADP client ID.</Note>

CHANGE 3  [### Creating a Mutual Authentication Certificate]  (line 79 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you are an ADP Workforce Now client, your ADP client ID is all the characters to the right of the @ symbol in your ADP Workforce Now login name. If you don't know your ADP client ID, contact your ADP representative.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are an ADP Workforce Now client, your ADP client ID is all the characters to the right of the @ symbol in your ADP Workforce Now login name. If you don't know your ADP client ID, contact your ADP representative.

CHANGE 4  [### Creating a Mutual Authentication Certificate]  (line 82 in repo file)

  OLD (remove from repo):
       7. Click **Submit**.

  NEW (replace with / insert from CSV):
       7. Click **Submit**.</Note>

CHANGE 5  [### Creating a Mutual Authentication Certificate]  (line 85 in repo file)

  OLD (remove from repo):
    <Note>**Note:** It might take up to a week to sign the CSR.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** It might take up to a week to sign the CSR.

CHANGE 6  [### Creating a Mutual Authentication Certificate]  (line 89 in repo file)

  OLD (remove from repo):
    10. Enter the **Export Password**.

  NEW (replace with / insert from CSV):
    10. Enter the **Export Password**.</Note>

CHANGE 7  [### Setting up SFTP]  (line 146 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you like, Domo can host your SFTP server. If you are interested, please reach out to your CSM for more information.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you like, Domo can host your SFTP server. If you are interested, please reach out to your CSM for more information.

CHANGE 8  [### Setting up SFTP]  (line 150 in repo file)

  OLD (remove from repo):
    4. Confirm receipt of the test you have been sent.

  NEW (replace with / insert from CSV):
    4. Confirm receipt of the test you have been sent.</Note>

CHANGE 9  [### Pulling Data into Domo Using SFTP]  (line 161 in repo file)

  OLD (remove from repo):
    4. Use Domo's [Microsoft Excel SFTP Push](/s/article/360043436733) to pull the report into Domo.

  NEW (replace with / insert from CSV):
    4. Use Domo's [Microsoft Excel SFTP Push](/s/article/360043436733)) to pull the report into Domo.

==============================================================================
FILE:   s/article/360042928374.mdx
TITLE:  Sage Intacct Connector
LANG:   English (en_US)
ID:     360042928374
==============================================================================

CHANGE 1  [(top of file)]  (lines 5–6 in repo file)

  OLD (remove from repo):
    <Warning>
    **Important:** Sage Intacct imposes a "concurrency limit" on API requests to their systems. Most customers are allowed 2 concurrent requests. However, depending on your service level agreement with Sage Intacct, you may have a greater concurrency limit. If you have a service level agreement that allows for a greater number of concurrent connections, you will need to go into your Domo account in the account center (https://[yourinstance].domo.com/datacenter/accounts) and update the value in your Sage Intacct account. Otherwise, Domo will simply default your account to the normal limit of 2 concurrent requests imposed by Sage Intacct. For more information, refer to the API documentation [https://developer.intacct.com/api/general-ledger/trial-balances/](https://developer.intacct.com/api/general-ledger/trial-balances/).

  NEW (replace with / insert from CSV):
    <Warning>**Important:** Sage Intacct imposes a "concurrency limit" on API requests to their systems. Most customers are allowed 2 concurrent requests. However, depending on your service level agreement with Sage Intacct, you may have a greater concurrency limit. If you have a service level agreement that allows for a greater number of concurrent connections, you will need to go into your Domo account in the account center (https://[yourinstance].domo.com/datacenter/accounts) and update the value in your Sage Intacct account. Otherwise, Domo will simply default your account to the normal limit of 2 concurrent requests imposed by Sage Intacct. For more information, refer to the API documentation [https://developer.intacct.com/api/general-ledger/trial-balances/](https://developer.intacct.com/api/general-ledger/trial-balances/).

CHANGE 2  [(top of file)]  (lines 10–11 in repo file)

  OLD (remove from repo):
    Additional steps can be taken to mitigate any potential slowdown by scheduling your Sage Intacct DataSets in such a way that they do not overlap each other.
    </Warning>

  NEW (replace with / insert from CSV):
    Additional steps can be taken to mitigate any potential slowdown by scheduling your Sage Intacct DataSets in such a way that they do not overlap each other.</Warning>

CHANGE 3  [### Details Pane]  (line 71 in repo file)

  OLD (remove from repo):
    <Note><div class="info-box-wrapper"><div class="info-box-icon-container"></div><div class="info-box-content-container"><div class="info-box-content"><b> Note </b>:There might be a possibility of having other/additional supported values for Reporting Period. Contact Sage Intacct for other possible Reporting Duration values for filtering the data. </div></div></div> </Note></td></tr><tr><td colspan="1" rowspan="1"> Date Filter </td><td colspan="1" rowspan="1"> Select whether you want to filter results by the date of creation or the date when they were last modified. </td></tr><tr><td colspan="1" rowspan="1"> Page Size </td><td colspan="1" rowspan="1"><span> Select the page size. It must be between 10 and 250. The page size number limits how much data is consumed from the API at once. If you are receiving an error, try decreasing the page size. </span></td></tr><tr><td colspan="1" rowspan="1"> Custom Report Name </td><td colspan="1" rowspan="1"><span> Enter the custom report name. </span></td></tr><tr><td colspan="1" rowspan="1"> Filter Data </td><td colspan="1" rowspan="1"><span> Select this checkbox if you'd like to filter your custom report by date. If your report supports date filtering, you'll be able to select options to set the date range. </span></td></tr><tr><td colspan="1" rowspan="1"> Filter Arguments </td><td colspan="1" rowspan="1"><span> Filter Arguments give you options to filter the custom report by a specific or relative date range, or by a reporting period such as 'Current Month' or 'Current Year'. If your custom report does not support date filters, this drop down will be empty. </span></td></tr><tr><td colspan="1" rowspan="1"> Module Key </td><td colspan="1" rowspan="1"><span> Select a module key. </span></td></tr><tr><td colspan="1" rowspan="1"> Date </td><td colspan="1" rowspan="1"><p> Specify whether the report's data is for a single date or a range of dates. </p><ul><li><b> Single date </b>: Choose a specific day for which you would like to receive the data by entering a number relative to today. For example, enter 0 for today, 1 for yesterday, or 7 for 7 days ago. </li><li><b> Date range: </b> When you select the date range option, the option to select the custom start and end dates appears. <ul><li><b> Start Date </b>: Select the start date for your report. You can either select a specific start date or a relative start date. </li><li><b> End Date </b>: Select the end date for your report. You can either select a specific end date or a relative end date. </li></ul></li></ul></td></tr><tr><td colspan="1" rowspan="1"> Start Date - Relative - <b> Days Back </b></td><td colspan="1" rowspan="1"><span> Choose the day for which you would like to start receiving the data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. </span></td></tr><tr><td colspan="1" rowspan="1"> End Date - Relative - <b> Days Back </b></td><td colspan="1" rowspan="1"><span> Choose the day for which you would like to stop receiving data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. </span></td></tr><tr><td colspan="1" rowspan="1"> Single date - Relative - <b> Days Back </b></td><td colspan="1" rowspan="1"><span> Specify the number of days before today for which you would like to receive the data. For example, enter 0 for today or 7 for seven days ago. </span></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Sage Intacct report you want to run. The following reports are available:   |  |  | | --- | --- | | Account Group | Receives data about Intacct Account Group accounting reports. | | AP Account Label | Receives data about Intacct Accounts Payable Account Label accounting reports. | | AP Adjustment | Receives data about Intacct Accounts Payable Adjustment accounting reports. **Date Filter** is required for this report. | | AP Bill | Receives data about Intacct Accounts Payable Bill accounting reports. **Date Filter** is required for this report. | | AP Bill Detail | Receives data about Intacct Accounts Payable Bill Detail accounting reports. | | AP Payment | Receives data about Intacct Accounts Payable Payment accounting reports. **Date Filter** is required for this report. | | AP Payment Request | Receives data about Intacct Accounts Payable Payment Request accounting reports. **Date Filter** is required for this report. | | AP Term | Receives data about Intacct Accounts Payable Term accounting reports. | | AR Account Label | Receives data about Intacct Accounts Receivable Account Label accounting reports. | | AR Adjustment | Receives data about Intacct Accounts Receivable Adjustment accounting reports. **Date Filter** is required for this report. | | AR Invoice | Receives data about Intacct Accounts Receivable Invoice accounting reports. **Date Filter** is required for this report. | | AR Invoice Detail | Receives data about Intacct Accounts Receivable Invoice Detail accounting reports. | | AR Payment | Receives data about Intacct Accounts Receivable Payment accounting reports. **Date Filter** is required for this report. | | AR Payment Summary | Receives data about Intacct Accounts Receivable Payment Summary accounting reports. **Date Filter** is required for this report. | | AR Term | Receives data about Intacct Accounts Receivable Term accounting reports. | | AR Transaction Definition | Receives data about Intacct Accounts Receivable Transaction Definition accounting reports. | | Bank Account | Receives data about Intacct Bank Account accounting reports. | | Bill | Receives data about Intacct Bill accounting reports. **Date Filter** is required for this report. | | Class | Receives data about Intacct Class accounting reports. | | Company Information | Receives data about Intacct Company Information accounting reports. | | Contact | Receives data about Intacct Contact accounting reports. | | Contact Tax Group | Receives data about Intacct Contact Tax Group accounting reports. | | CSN History | Receives data about Intacct CSN History accounting reports. | | Cust GL Group | Receives data about Intacct Cust General Group accounting reports. | | Customer | Receives data about Intacct Customer accounting reports. | | Customer Ach Info | Receives data about Intacct Customer Ach Info accounting reports. | | Customer Bank Account | Receives data about Intacct Customer Bank Account accounting reports. | | Customer Charge Card (Retired) | This report is no longer available. | | Customer P Package | Receives data about Intacct Customer P Package accounting reports. | | Custom Reports | Receives data about Intacct Custom accounting reports. | | Department | Receives data about Intacct Department accounting reports. | | Employee | Receives data about Intacct Employee accounting reports. | | Expense Report | Receives data about Intacct Expense Report accounting reports. | | Expense Report Summary | Receives data about Intacct Expense Report Summary accounting reports. | | Expense Types | Receives data about Intacct Expense Types accounting reports. | | GL Account | Receives data about Intacct General Account accounting reports. | | GL Account Balance | Receives data about Intacct General Account Balance accounting reports. | | GL Account Group | Receives data about Intacct General Account Group accounting reports. | | GL Account Group Member | Receives data about Intacct General Account Group Member accounting reports. | | GL Batch | Receives data about Intacct General Batch accounting reports. **Date Filter** is required for this report. | | GL Budget | Receives data about Intacct General Budget accounting reports. | | GL Budget Item | Receives data about Intacct General Budget Item accounting reports. | | GL Account Category | Receives data about Intacct General Account Category accounting reports. | | GL Entry | Receives data about Intacct General Entry accounting reports. **Date Filter** is required for this report. | | GL Transaction | Receives data about Intacct General Transaction accounting reports. **Date Filter** is required for this report. | | IC Item | Receives data about Intacct Inventory Control Item accounting reports. | | IC Transaction | Receives data about Intacct Inventory Control Transaction accounting reports. **Date Filter** is required for this report. | | IC Transaction Definition | Receives data about Intacct Inventory Control Transaction Definition accounting reports. | | Invoice | Receives data about Intacct Invoice accounting reports. **Date Filter** is required for this report. | | Item GL Group | Receives data about Intacct Item General Group accounting reports. | | Item Tax Group | Receives data about Intacct Item Tax Group accounting reports. | | Journal | Receives data about Intacct Journal accounting reports. | | Location | Receives data about Intacct Location accounting reports. | | Location Entity | Receives data about Intacct Location Entity accounting reports. | | Location Group | Receives data about Intacct Location Group accounting reports. | | PO Price List | Receives data about Intacct Purchase Order Price List accounting reports. | | PO Transaction | Receives data about Intacct Purchase Order Transaction accounting reports. **Date Filter** is required for this report. | | PO Transaction Definition | Receives data about Intacct Purchase Order Transaction Definition accounting reports. | | Pricelist Item | Receives data about Intacct Pricelist Item accounting reports. | | Product Line | Receives data about Intacct Product Line accounting reports. | | Project | Receives data about Intacct Project accounting reports. | | Renewal Macro | Receives data about Intacct Renewal Macro accounting reports. | | Reporting Period | Receives data about Intacct Reporting Period accounting reports. | | Revenue Recognition Schedule | Receives data about Intacct Revenue Recognition Schedule accounting reports. | | Revenue Recognition Schedule Entry | Receives data about Intacct Revenue Recognition Schedule Entry accounting reports. | | Revenue Recognition Template | Receives data about Intacct Revenue Recognition Schedule Template accounting reports. | | Sales Document | Receives data about Intacct Sales Document reports. | | Sales Document Detail | Receives data about Intacct Sales Document Detail reports. | | Sales Document Subtotals | Receives data about Intacct Sales Document Subtotal reports. | | Smart Event Log | Receives data about Intacct Smart Event Log reports. **Date Filter** is required for this report. | | SO Price List | Receives data about Intacct Sales Order Price List accounting reports. | | SO Transaction | Receives data about Intacct Sales Order Transaction accounting reports. **Date Filter** is required for this report. | | SO Transaction Definition | Receives data about Intacct Sales Order Transaction Definition accounting reports. | | SO Transaction Detail | Receives data about Intacct Sales Order Transaction Detail accounting reports. | | Stat GL Account | Receives data about Intacct Stat General Ledger Account accounting reports. | | Statistical Journal | Receives data about Intacct Statistical Journal accounting reports. | | ST Kit Transaction | Receives data about Intacct Kit Transaction accounting reports. **Date Filter** is required for this report. | | Subscription | Receives data about Intacct Subscription accounting reports. | | Tax Detail | Receives data about Intacct Tax Detail accounting reports. | | Tax Schedule | Receives data about Intacct Tax Schedule accounting reports. | | Tax Schedule Detail | Receives data about Intacct Tax Schedule Detail accounting reports. | | Tax Schedule Map | Receives data about Intacct Tax Schedule Map accounting reports. | | Territory | Receives data about Intacct Territory accounting reports. | | Trial Balance | Receives the Intacct data of balances of all accounts for a specific reporting periods or date range. | | Transaction Currency | Receives data about Intacct Transaction Currency accounting reports. **Date Filter** is required for this report. | | Unit of Measure | Receives data about Intacct Unit of Measure accounting reports. | | Vendor GL Group | Receives data about Intacct Vendor General Group accounting reports. | | Vendor | Receives data about Intacct Vendor accounting reports. **Date Filter** is required for this report. | | VSOE Item Pricelist | Receives data about Intacct Vendor-Specific Objective Evidence Item Pricelist accounting reports. | | VSOE Pricelist | Receives data about Intacct Vendor-Specific Objective Evidence Pricelist accounting reports. | | Warehouse | Receives data about Intacct Warehouse accounting reports. | |
    | Fixed or Relative Date Range | Select a fixed or relative date range. For a fixed date range, you enter a specific start date and end date, and your report always pulls data for that range. For a relative date range, you enter a number of days back for your report. Each time the report is run, data is pulled for that number of days based on today's date. |
    | Fixed Start Date | Select the start date for your report. |
    | Fixed End Date | Select the end date for your report. |
    | Days Back (Incremental Days) | Enter the number of days back from today's date that should appear in your report. For example, if you entered 7, the report would pull data for the past week each time it was run.  Enter an initial backfill amount if you are using a daily incremental data pull. The **Update Method** under **Scheduling** (below) must be set to **Append**. |
    | Reporting Period | Enter the reporting duration for filtering the data. This Connector supports 'current month' and 'current year' as the reporting period values.  Example: 'Current Year' or 'Current Month'  **Note** :There might be a possibility of having other/additional supported values for Reporting Period. Contact Sage Intacct for other possible Reporting Duration values for filtering the data. |
    | Date Filter | Select whether you want to filter results by the date of creation or the date when they were last modified. |
    | Page Size | Select the page size. It must be between 10 and 250. The page size number limits how much data is consumed from the API at once. If you are receiving an error, try decreasing the page size. |
    | Custom Report Name | Enter the custom report name. |
    | Filter Data | Select this checkbox if you'd like to filter your custom report by date. If your report supports date filtering, you'll be able to select options to set the date range. |
    | Filter Arguments | Filter Arguments give you options to filter the custom report by a specific or relative date range, or by a reporting period such as 'Current Month' or 'Current Year'. If your custom report does not support date filters, this drop down will be empty. |
    | Module Key | Select a module key. |
    | Date | Specify whether the report's data is for a single date or a range of dates.   - **Single date**: Choose a specific day for which you would like to receive the data by entering a number relative to today. For example, enter 0 for today, 1 for yesterday, or 7 for 7 days ago. - **Date range:** When you select the date range option, the option to select the custom start and end dates appears.   - **Start Date**: Select the start date for your report. You can either select a specific start date or a relative start date.   - **End Date**: Select the end date for your report. You can either select a specific end date or a relative end date. |
    | Start Date - Relative - **Days Back** | Choose the day for which you would like to start receiving the data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. |
    | End Date - Relative - **Days Back** | Choose the day for which you would like to stop receiving data by entering a number relative to today. For example, enter 1 for yesterday, 7 for 1 week ago, and 30 for 1 month ago. |
    | Single date - Relative - **Days Back** | Specify the number of days before today for which you would like to receive the data. For example, enter 0 for today or 7 for seven days ago. |

  [2 image-only change(s) suppressed — handle separately if needed]

==============================================================================
FILE:   s/article/360042929154.mdx
TITLE:  Adobe Analytics 1.4 Server to Server OAuth Connector
LANG:   English (en_US)
ID:     360042929154
==============================================================================

CHANGE 1  [#### "Intermediate - Custom" Report Type Options]  (line 101 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" class="mt-responsive-table"><thead><tr><th class="mt-column-width-35" colspan="1" rowspan="1"><p> Option </p></th><th class="mt-column-width-65" colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Elements </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select up to four element names. </p><p> An <em> element </em> is a structure that breaks down a report's metrics data. For example, you can generate a report that breaks down page views (metric) by the browsers used to access the page (element). The resulting report lists page views by browser type. </p><p> By default the Adobe Analytics API only returns the top 10 elements. If you want to view more, you can do so by entering information for the additional elements in the <b> Additional Element Information </b> field. </p><p> For more information about elements, see <a class="link-https" href="https://adobedocs.github.io/analytics-1.4-apis/" target="_blank" title="https://adobedocs.github.io/analytics-1.4-apis/"> https://adobedocs.github.io/analytics-1.4-apis/ </a>. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Additional Element Information </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Enter any additional element information using the JSON format defined in the API Explorer (<a class="link-https" href="https://adobedocs.github.io/analytics-1.4-apis/" target="_blank" title="https://adobedocs.github.io/analytics-1.4-apis/"> https://adobedocs.github.io/analytics-1.4-apis/ </a> ). </p><p> For example: </p><p><code> \{"top":50, <br/> "selected":[], <br/> "classifications":[], <br/> "id":"page", <br/> "name":"Page"} </code></p><p> This parameter is described in detail later in this topic: <a href="https://knowledge.domo.com/Connect/Connecting_to_Data_with_Connectors/Configuring_Each_Connector/Connectors_for_Analytics/Adobe_Analytics_Advanced_Connector#Understanding_The_.22Additional_Elements.22_Field" target="_blank" title="Adobe Analytics Advanced Connector"> Understanding the Additional Elements field </a>. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Segments </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select segment definitions for your report. You can select as many segments as you want. For more information about segments, see the API Explorer documentation (<a class="link-https" href="https://adobedocs.github.io/analytics-1.4-apis/" target="_blank" title="https://adobedocs.github.io/analytics-1.4-apis/"> https://adobedocs.github.io/analytics-1.4-apis/ </a> ). </p><div class="mdx-embed"><Note><p><b> Note: </b> Although you have the ability to select as many segments as you would like, be cautious about what those segments are doing to the overall picture of your data. Selecting segments will change the grouping of your data and can lead to unexpected results. </p></Note></div></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Inline Segments </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Enter inline segment information using the JSON format defined in the API Explorer (<a class="link-https" href="https://adobedocs.github.io/analytics-1.4-apis/" target="_blank" title="https://adobedocs.github.io/analytics-1.4-apis/"> https://adobedocs.github.io/analytics-1.4-apis/ </a> ). </p><p> Inline segments allow you to create a segment in the connector without having to define it first in Adobe Analytics. For example, if I wanted to segment (filter) the data to show only the Home Page and Shopping Cart, I would populate the inline segmentation window with the below JSON snippet. </p><p><code> \{ <br/> "id": "page", <br/> "selected": ["Home Page", "Shopping Cart"] <br/> } </code><br/></p><p> You can also apply logic to your filtering. </p><p> For more information about inline segments, see <a class="link-https" href="https://adobedocs.github.io/analytics-1.4-apis/" target="_blank" title="https://adobedocs.github.io/analytics-1.4-apis/"> https://adobedocs.github.io/analytics-1.4-apis/ </a>. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Metrics </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select up to 30 metrics for your report. For more information about Adobe Analytics metrics, see <a class="link-https" href="https://marketing.adobe.com/developer/documentation/analytics-reporting-1-4/metrics" target="_blank" title="https://marketing.adobe.com/developer/documentation/analytics-reporting-1-4/metrics"> https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/metrics.md </a>. </p><p> Some combinations of metrics will lead to errors. Adobe has provided a list of metrics that are blacklisted from being used together. <br/><a class="link-https" href="https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/combinations.md" target="_blank" title="https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/combinations.md"> https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/combinations.md </a></p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Date Granularity </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select a date grain for the report. This determines whether the data is broken down by day, by week, etc. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Duration </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Specify whether the data in this report is for a single date or a range of dates. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Report Date </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select whether the data in the report is for a specific date or for a relative date based on the selected <b> Date Granularity </b>. For example, if you selected <b> Date Granularity &gt; Day </b> , you could select <b> Today </b> as your report date. Then, each time the data updated, it would show date for the current date. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Date Offset </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Enter the number of periods back that will be used to offset your relative date. The period is based on your selected Date Granularity. </p><p> For example, if you selected <b> Date Granularity &gt; Day </b> and <b> Report Date &gt; Yesterday </b> , then entered <code> 2 </code> for <b> Date Offset </b> , the report would pull data for three days in the past (yesterday minus two days). </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Select Specific Date </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select the date you want to retrieve data for. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Date From </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b> Date To </b>. Options in these menus are determined by the selected <b> Date Granularity </b>. </p><p> For example, if you selected <b> Date Granularity &gt; Week </b> , you could then select <b> Date From &gt; Last Week </b> and <b> Date To &gt; This Week </b>. Each time your report updated, it would then show data for this week as well as last week. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Date To </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Specify whether the last date in your date range is a specific or relative date. You select the first date in your range in <b> Date From </b>. Options in these menus are determined by the selected <b> Date Granularity </b>. </p><p> For example, if you selected <b> Date Granularity &gt; Week </b> , you could then select <b> Date From &gt; Last Week </b> and <b> Date To &gt; This Week </b>. Each time your report updated, it would then show data for this week as well as last week. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Date From Offset </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Enter the number of periods back that will be used to offset the first date in your date range. The period is based on your selected <b> Date Granularity </b>. </p><p> For example, if you selected <b> Date Granularity &gt; Week </b> and <b> Date From &gt; Last Week </b> , then entered <code> 2 </code> for <b> Date From Offset </b> , the first date in the date range would be set to three weeks in the past (last week minus two weeks). </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Date To Offset </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Enter the number of periods back that will be used to offset the last date in your date range. The period is based on your selected <b> Date Granularity </b>. </p><p> For example, if you selected <b> Date Granularity &gt; Week </b> and <b> Date To &gt; This Week </b> , then entered <code> 2 </code> for <b> Date To Offset </b> , the last date in your date range would be set to two weeks in the past (this week minus two weeks). </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Select Specific Date From </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select the first date in your date range. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Select Specific Date To </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select the last date in your date range. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Time Zone </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Select the UTC time zone for your data. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Include All Time Zones </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Check this box if you want all time zones included in your report. </p></td></tr><tr><td class="mt-column-width-35" colspan="1" rowspan="1"><p> Backfill </p></td><td class="mt-column-width-65" colspan="1" rowspan="1"><p> Enter the number of periods back (based on the selected <b> Date Granularity </b> ) that should backfill the first time the report is run. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Option | Description |
    | --- | --- |
    | Elements | Select up to four element names.  An *element* is a structure that breaks down a report's metrics data. For example, you can generate a report that breaks down page views (metric) by the browsers used to access the page (element). The resulting report lists page views by browser type.  By default the Adobe Analytics API only returns the top 10 elements. If you want to view more, you can do so by entering information for the additional elements in the **Additional Element Information**field.  For more information about elements, see [https://adobedocs.github.io/analytics-1.4-apis/](https://adobedocs.github.io/analytics-1.4-apis/). |
    | Additional Element Information | Enter any additional element information using the JSON format defined in the API Explorer ([https://adobedocs.github.io/analytics-1.4-apis/](https://adobedocs.github.io/analytics-1.4-apis/)).  For example:  `{"top":50, "selected":[], "classifications":[], "id":"page", "name":"Page"}`  This parameter is described in detail later in this topic: [Understanding the Additional Elements field](https://knowledge.domo.com/Connect/Connecting_to_Data_with_Connectors/Configuring_Each_Connector/Connectors_for_Analytics/Adobe_Analytics_Advanced_Connector#Understanding_The_.22Additional_Elements.22_Field). |
    | Segments | Select segment definitions for your report. You can select as many segments as you want. For more information about segments, see the API Explorer documentation ([https://adobedocs.github.io/analytics-1.4-apis/](https://adobedocs.github.io/analytics-1.4-apis/)).
    <Note>**Note:** Although you have the ability to select as many segments as you would like, be cautious about what those segments are doing to the overall picture of your data. Selecting segments will change the grouping of your data and can lead to unexpected results. |
    | Inline Segments | Enter inline segment information using the JSON format defined in the API Explorer ([https://adobedocs.github.io/analytics-1.4-apis/](https://adobedocs.github.io/analytics-1.4-apis/)).  Inline segments allow you to create a segment in the connector without having to define it first in Adobe Analytics. For example, if I wanted to segment (filter) the data to show only the Home Page and Shopping Cart, I would populate the inline segmentation window with the below JSON snippet.  `{  "id": "page", "selected": ["Home Page", "Shopping Cart"] }`  You can also apply logic to your filtering.  For more information about inline segments, see [https://adobedocs.github.io/analytics-1.4-apis/](https://adobedocs.github.io/analytics-1.4-apis/). |
    | Metrics | Select up to 30 metrics for your report. For more information about Adobe Analytics metrics, see [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/metrics.md](https://marketing.adobe.com/developer/documentation/analytics-reporting-1-4/metrics).  Some combinations of metrics will lead to errors. Adobe has provided a list of metrics that are blacklisted from being used together.  [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/combinations.md](https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/combinations.md) |
    | Date Granularity | Select a date grain for the report. This determines whether the data is broken down by day, by week, etc. |
    | Duration | Specify whether the data in this report is for a single date or a range of dates. |
    | Report Date | Select whether the data in the report is for a specific date or for a relative date based on the selected **Date Granularity**. For example, if you selected **Date Granularity > Day**, you could select **Today** as your report date. Then, each time the data updated, it would show date for the current date. |
    | Date Offset | Enter the number of periods back that will be used to offset your relative date. The period is based on your selected Date Granularity.  For example, if you selected **Date Granularity > Day** and **Report Date > Yesterday**, then entered 2 for **Date Offset**, the report would pull data for three days in the past (yesterday minus two days). |
    | Select Specific Date | Select the date you want to retrieve data for. |
    | Date From | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **Date To**. Options in these menus are determined by the selected **Date Granularity**.  For example, if you selected **Date Granularity > Week**, you could then select **Date From > Last Week** and **Date To > This Week**. Each time your report updated, it would then show data for this week as well as last week. |
    | Date To | Specify whether the last date in your date range is a specific or relative date. You select the first date in your range in **Date From**. Options in these menus are determined by the selected **Date Granularity**.  For example, if you selected **Date Granularity > Week**, you could then select **Date From > Last Week** and **Date To > This Week**. Each time your report updated, it would then show data for this week as well as last week. |
    | Date From Offset | Enter the number of periods back that will be used to offset the first date in your date range. The period is based on your selected **Date Granularity**.  For example, if you selected **Date Granularity > Week** and **Date From > Last Week**, then entered 2 for **Date From Offset**, the first date in the date range would be set to three weeks in the past (last week minus two weeks). |
    | Date To Offset | Enter the number of periods back that will be used to offset the last date in your date range. The period is based on your selected **Date Granularity**.  For example, if you selected **Date Granularity > Week** and **Date To > This Week**, then entered 2 for **Date To Offset**, the last date in your date range would be set to two weeks in the past (this week minus two weeks). |
    | Select Specific Date From | Select the first date in your date range. |
    | Select Specific Date To | Select the last date in your date range. |
    | Time Zone | Select the UTC time zone for your data. |
    | Include All Time Zones | Check this box if you want all time zones included in your report. |
    | Backfill | Enter the number of periods back (based on the selected **Date Granularity**) that should backfill the first time the report is run. |</Note>

CHANGE 2  [## Understanding The "Additional Elements" Field]  (lines 117–165 in repo file)

  ⚠ LARGE CHANGE (61 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    ```json
    {
    
    "id":"
    
     string
    
    ",
    
    "classification":"
    
     string
    
    ",
    
    "top":"
    
     int
    
    ",
    
    "startingWith":"
    
     int
    
    ",
    
    "search":{
    
    "type":"
    
     string
    
    ",
    
    "keywords":[
    
    "
    
     string
    
    "
    
    ],
    
    }
    
    }
    ```

  NEW (replace with / insert from CSV):
    `{
    "id":"string",
    "classification":"string",
    "top":"int",
    "startingWith":"int",
    "search":{
    "type":"string",
    "keywords":[
    "string"
    ],
    }
    }`

CHANGE 3  [### Using Additional Elements to Increase Your Top Results]  (line 169 in repo file)

  OLD (remove from repo):
    By default, the Adobe Analytics API only return the top 10 results for the selected element. You can increase the number of top results using **Additional Elements**. For example, to increase the limit to 100 you would add the following to the **Additional Elements** field:

  NEW (replace with / insert from CSV):
    By default, the Adobe Analytics API only return the top 10 results for the selected element. You can increase the number of top results using **Additional Elements**. For example, to increase the limit to 100 you would add the following to the **Additional Elements**field:

CHANGE 4  [### Using Additional Elements to Increase Your Top Results]  (line 171 in repo file)

  OLD (remove from repo):
    `{“top”:100, “id”:”page”}`

  NEW (replace with / insert from CSV):
    `{“top”:100,
    “id”:”page”}`

CHANGE 5  [### Using Additional Elements to Increase Your Top Results]  (line 173 in repo file)

  OLD (remove from repo):
    *"* id" is the ID of the element you want. There is no upper limit to the “Top” number but the less you pull back in using the query, the more efficient it will be. You can do this for multiple elements by separating them with a comma.

  NEW (replace with / insert from CSV):
    *"*id" is the ID of the element you want. There is no upper limit to the “Top” number but the less you pull back in using the query, the more efficient it will be. You can do this for multiple elements by separating them with a comma.

CHANGE 6  [### Using Additional Elements to Increase Your Top Results]  (lines 175–183 in repo file)

  OLD (remove from repo):
    ```json
    {“top”:100,
    
    “id”:”page”},
    
    {“top”:100,
    
    “id”:”region”}
    ```

  NEW (replace with / insert from CSV):
    `{“top”:100,
    “id”:”page”},
    {“top”:100,
    “id”:”region”}`

CHANGE 7  [### Using Additional Elements to Search for Keywords in Elements]  (lines 216–232 in repo file)

  OLD (remove from repo):
    ```json
    {
    
    "id": "page",
    
    "top": 3,
    
    "search": {
    
    "type": "OR",
    
    "keywords": [“help*","^faq"]
    
    }
    
    }
    ```

  NEW (replace with / insert from CSV):
    `{
    "id": "page",
    "top": 3,
    "search": {
    "type": "OR",
    "keywords": [“help*","^faq"]
    }
    }`

CHANGE 8  [### Using Additional Elements to Search for Keywords in Elements]  (lines 236–252 in repo file)

  OLD (remove from repo):
    ```json
    {
    
    "id": "page",
    
    "top": 3,
    
    "search": {
    
    "type": "OR",
    
    "keywords": ["^faq$"]
    
    }
    
    }
    ```

  NEW (replace with / insert from CSV):
    `{
    "id": "page",
    "top": 3,
    "search": {
    "type": "OR",
    "keywords": ["^faq$"]
    }
    }`

CHANGE 9  [### Adding Classifications to the JSON Syntax]  (lines 258–268 in repo file)

  OLD (remove from repo):
    ```json
    {
    
    "id": “page",
    
    “classification”:”region”,
    
    "top": 3
    
    }
    ```

  NEW (replace with / insert from CSV):
    `{
    "id": “page",
    “classification”:”region”,
    "top": 3
    }`

CHANGE 10  [## FAQ]  (lines 295–296 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="When should I use this connector?">

  NEW (replace with / insert from CSV):
    #### Q: When should I use this connector?

CHANGE 11  [## FAQ]  (line 300 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Q: I successfully created an Adobe Analytics OAuth account, but about two weeks later I got an alert: "Account Adobe Analytics OAuth Account has been Deactivated". Why is this happening and what can I do about it?

CHANGE 12  [## FAQ]  (lines 302–304 in repo file)

  OLD (remove from repo):
    <Accordion title="I successfully created an Adobe Analytics OAuth account, but about two weeks later I got an alert: &quot;Account Adobe Analytics OAuth Account has been Deactivated&quot;. Why is this happening and what can I do about it?">
    
    A: Adobe Analytics OAuth has a refresh token that expires after two weeks (see [Adobe documentation](https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/OAuth/OAuth.md#exchanging-refresh-tokens "https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/OAuth/OAuth.md#exchanging-refresh-tokens") for details.) This causes Adobe OAuth to no longer authenticate after two weeks. This is an Adobe limitation. Older datasets use WSSE authentication (Username: Company, Password), but Adobe has deprecated WSSE authentication. Please contact Adobe support to request a resolution to this issue.

  NEW (replace with / insert from CSV):
    A: Adobe Analytics OAuth has a refresh token that expires after two weeks (see [Adobe documentation](https://www.adobe.io/authentication/auth-methods.html#!AdobeDocs/adobeio-auth/master/OAuth/OAuth.md#exchanging-refresh-tokens) for details.) This causes Adobe OAuth to no longer authenticate after two weeks. This is an Adobe limitation. Older datasets use WSSE authentication (Username: Company, Password), but Adobe has deprecated WSSE authentication. Please contact Adobe support to request a resolution to this issue.

CHANGE 13  [## FAQ]  (lines 310–312 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Do I need a certain kind of account with the data service to set up the connector?">

  NEW (replace with / insert from CSV):
    #### Q: Do I need a certain kind of account with the data service to set up the connector?

CHANGE 14  [## FAQ]  (line 316 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Q: My Adobe Analytics OAuth datasets are failing intermittently and displaying unhelpful error messages like "Unable to complete report at this time. Please try again later." What should I do?

CHANGE 15  [## FAQ]  (line 318 in repo file)

  OLD (remove from repo):
    <Accordion title="My Adobe Analytics OAuth datasets are failing intermittently and displaying unhelpful error messages like &quot;Unable to complete report at this time. Please try again later.&quot; What should I do?">

  NEW (replace with / insert from CSV):
    A: This error originates on Adobe’s end. Unfortunately, it is nothing we at Domo have direct control over, but we are working with Adobe to improve these types of errors. That said, you can take steps to alleviate the occurrence of these errors, which include reducing the number of connectors, the frequency at which they try to pull data, and the size of their requests. Attempting to schedule requests to run at different times can also help. Another possible solution is to use the “retry” setting in the connector.

CHANGE 16  [## FAQ]  (lines 320–324 in repo file)

  OLD (remove from repo):
    A: This error originates on Adobe ’s end. Unfortunately, it is nothing we at Domo have direct control over, but we are working with Adobe to improve these types of errors. That said, you can take steps to alleviate the occurrence of these errors, which include reducing the number of connectors, the frequency at which they try to pull data, and the size of their requests. Attempting to schedule requests to run at different times can also help. Another possible solution is to use the “retry” setting in the connector.
    
    </Accordion>
    
    <Accordion title="What do I do if I need to include more than 4 elements in my Adobe Analytics OAuth DataSet?">

  NEW (replace with / insert from CSV):
    #### Q: What do I do if I need to include more than 4 elements in my Adobe Analytics OAuth DataSet?

CHANGE 17  [## FAQ]  (lines 328–330 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="My report takes a long time. How do I speed up my report?">

  NEW (replace with / insert from CSV):
    #### Q: My report takes a long time. How do I speed up my report?

CHANGE 18  [## FAQ]  (line 336 in repo file)

  OLD (remove from repo):
    If you have a large number of datasets, try to stagger their execution times so that the datasets will not clog up Adobe's queue all at once.

  NEW (replace with / insert from CSV):
    If you have a large number of datasets, try to stagger their execution times so that the datasets will not clog up Adobe's queue all at once.

CHANGE 19  [## FAQ]  (line 338 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Q: Is there a way to see what data is being returned from the Adobe Analytics API?

CHANGE 20  [## FAQ]  (line 340 in repo file)

  OLD (remove from repo):
    <Accordion title="Is there a way to see what data is being returned from the Adobe Analytics API?">

  NEW (replace with / insert from CSV):
    A: Adobe Analytics has an API Explorer that you can use to run your report and check the results. It is available at [https://adobedocs.github.io/analytics-1.4-apis/](https://adobedocs.github.io/analytics-1.4-apis/).

CHANGE 21  [## FAQ]  (lines 342–346 in repo file)

  OLD (remove from repo):
    A: Adobe Analytics has an API Explorer that you can use to run your report and check the results. It is available at [https://adobedocs.github.io/analytics-1.4-apis/](https://adobedocs.github.io/analytics-1.4-apis/ "https://adobedocs.github.io/analytics-1.4-apis/").
    
    </Accordion>
    
    <Accordion title="Is there any limit to the number of Elements that can be retrieved?">

  NEW (replace with / insert from CSV):
    #### Q: Is there any limit to the number of Elements that can be retrieved?

CHANGE 22  [## FAQ]  (lines 350–352 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What is the workflow for pulling data from Adobe?">

  NEW (replace with / insert from CSV):
    #### Q: What is the workflow for pulling data from Adobe?

CHANGE 23  [## FAQ]  (lines 356–359 in repo file)

  OLD (remove from repo):
    1. **Queue report** -- Domo sends Adobe the parameters of the desired report. [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r\_Queue.md](https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_Queue.md "https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_Queue.md")
    2. **Get report ID** -- Domo receives a report ID.
    3. **Wait for report to be done** -- Domo watches Adobe's queue and wait for the report to be done. [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r\_GetQueue.md](https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_GetQueue.md "https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_GetQueue.md")
    4. **Pull report** -- When the report no longer appears in the queue, Domo uses the report ID received in step 2 to ask for the data of the report. [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r\_Get.md](https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_Get.md "https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_Get.md")

  NEW (replace with / insert from CSV):
    1. **Queue report** -- Domo sends Adobe the parameters of the desired report. [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r\_Queue.md](https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_Queue.md)
    2. **Get report ID**-- Domo receives a report ID.
    3. **Wait for report to be done** -- Domo watches Adobe's queue and wait for the report to be done. [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r\_GetQueue.md](https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_GetQueue.md)
    4. **Pull report** -- When the report no longer appears in the queue, Domo uses the report ID received in step 2 to ask for the data of the report. [https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r\_Get.md](https://github.com/AdobeDocs/analytics-1.4-apis/blob/master/docs/reporting-api/methods/r_Get.md)

CHANGE 24  [## FAQ]  (lines 361–362 in repo file)

  OLD (remove from repo):
    </Accordion>
    </AccordionGroup>

  [1 image-only change(s) suppressed — handle separately if needed]

==============================================================================
FILE:   s/article/360042929454.mdx
TITLE:  SEMRush Connector
LANG:   English (en_US)
ID:     360042929454
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    SEMRush is a competitive intelligence suite for online marketing, from SEO and PPC to social media and video advertising research.  To learn more about the SEMRush API, visit their page ([https://www.semrush.com/api-documentation/](https://www.semrush.com/api-documentation/ "https://www.semrush.com/api-documentation/") ).

  NEW (replace with / insert from CSV):
    SEMRush is a competitive intelligence suite for online marketing, from SEO and PPC to social media and video advertising research.  To learn more about the SEMRush API, visit their page ([https://www.semrush.com/api-documentation/](https://www.semrush.com/api-documentation/)).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your SEMRush account in the Data Center. This topic discusses the fields and menus that are specific to the SEMRush connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    You connect to your SEMRush account in the Data Center. This topic discusses the fields and menus that are specific to the SEMRush connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (line 13 in repo file)

  OLD (remove from repo):
    To connect to your SEMRush account and create a DataSet, you must have a SEMRush API key. For more information about getting going with SEMRush and obtaining an API key, see [https://www.semrush.com/api-use/](https://www.semrush.com/api-use/ "https://www.semrush.com/api-use/").

  NEW (replace with / insert from CSV):
    To connect to your SEMRush account and create a DataSet, you must have a SEMRush API key. For more information about getting going with SEMRush and obtaining an API key, see [https://www.semrush.com/api-use/](https://www.semrush.com/api-use/).

CHANGE 4  [## Connecting to Your SEMRush Account]  (line 17 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the SEMRush Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the SEMRush Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 5  [### Credentials Pane]  (line 21 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your SEMRush account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your SEMRush account. The following table describes what is needed for each field:

CHANGE 6  [### Credentials Pane]  (line 23 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"> Field </span></p></th><th colspan="1" rowspan="1"><p><span class="s1"> Description </span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> API Key </p></td><td colspan="1" rowspan="1"><p> Enter your SEMRush API key. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter your SEMRush API key. |

CHANGE 7  [### Credentials Pane]  (line 25 in repo file)

  OLD (remove from repo):
    Once you have entered valid SEMRush credentials, you can use the same account any time you go to create a new SEMRush DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054 "Managing User Accounts for Connectors").

  NEW (replace with / insert from CSV):
    Once you have entered valid SEMRush credentials, you can use the same account any time you go to create a new SEMRush DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 8  [### Details Pane]  (line 29 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 9  [### Details Pane]  (lines 31–32 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the SEMRush report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p> Overview </p></td><td colspan="1" rowspan="1"><p> Returns a summary of your SEMRush data. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Domain </p></td><td colspan="1" rowspan="1"><p> Returns competitive information for a specified website domain. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Keywords </p></td><td colspan="1" rowspan="1"><p> Returns competitive information for a specified keyword or keyword expression. </p></td></tr><tr><td colspan="1" rowspan="1"><p> URL </p></td><td colspan="1" rowspan="1"><p> Returns competitive information for a specified landing page URL. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Backlinks </p></td><td colspan="1" rowspan="1"><p> Returns backlinks information for a specified regional database. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Display Advertising </p></td><td colspan="1" rowspan="1"><p> Returns competitive information for a specified advertiser and publisher. </p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Domain </p></td><td colspan="1" rowspan="1"><p> Enter the domain of the website you want to retrieve information for, in the format <span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> example.com </a></span>. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Advertiser Domain </p></td><td colspan="1" rowspan="1"><p> Enter the website domain of the advertiser you want to retrieve information for, in the format <span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> example.com </a></span></p></td></tr><tr><td colspan="1" rowspan="1"><p> Publisher Domain </p></td><td colspan="1" rowspan="1"><p> Enter the website domain of the publisher you want to retrieve information for, in the format <span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> example.com </a></span></p></td></tr><tr><td colspan="1" rowspan="1"><p> Phrase </p></td><td colspan="1" rowspan="1"><p> Enter a keyword or keyword expression you want to retrieve information for. </p></td></tr><tr><td colspan="1" rowspan="1"><p> URL </p></td><td colspan="1" rowspan="1"><p> Enter the URL of a landing page you want to retrieve information for. Use the format <span class="mt-font-courier-new"><span class="nowiki"><a href="http://example.com" rel="noopener" target="_blank"> http://example.com </a></span></span>. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Database </p></td><td colspan="1" rowspan="1"><p> Select the regional database you want to retrieve data from. Select none to query all databases. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Target </p></td><td colspan="1" rowspan="1"><p> Enter a target domain (e.g. <span class="mt-font-courier-new"><span class="nowiki"><a href="http://www.example.com" rel="noopener" target="_blank"> www.example.com </a></span></span> ), root domain (e.g. <span class="nowiki"><span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> example.com </a></span></span> ), or URL (e.g. <span class="nowiki"><span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> http://example.com </a></span></span> ) for the "Backlinks" report. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Target Type </p></td><td colspan="1" rowspan="1"><p> Select the target type for the target you entered in the <b> Target </b> field. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Target List </p></td><td colspan="1" rowspan="1"><p> Enter a comma-separated list of domains (e.g. <span class="mt-font-courier-new"><span class="nowiki"><a href="http://www.example.com" rel="noopener" target="_blank"> www.example.com </a></span></span> ), root domains (e.g. <span class="nowiki"><span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> example.com </a></span></span> ), and/or URLs (e.g. <span class="nowiki"><span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> http://example.com </a></span></span> ) you want to retrieve information for. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Target Type List </p></td><td colspan="1" rowspan="1"><p> Enter a comma-separated list of target types matching the targets you entered for <b> Target List </b>. Target types are as follows: </p><ul><li><p><span class="mt-font-courier-new"> domain </span></p></li><li><p><span class="mt-font-courier-new"> root-domain </span></p></li><li><p><span class="mt-font-courier-new"> url </span></p></li></ul><p> For example, if your target list was <span class="mt-font-courier-new"><a href="http://example.com" rel="noopener" target="_blank"> example.com </a>, <a href="http://www.example.com" target="_blank" title="http://www.example.com"> http://www.example.com </a>, <a href="http://www.example.com" rel="noopener" target="_blank"> www.example.com </a></span>, you would enter <span class="mt-font-courier-new"> root domain, url, domain </span> in this field. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Year (Optional) </p></td><td colspan="1" rowspan="1"><p> Enter the year you want to retrieve information for. If you combine this with <b> Month </b>, you can pull data for a specific month. </p>
    <Note><div class="info-box-wrapper"><div class="info-box-icon-container"><span class="info-box-icon"></span></div><div class="info-box-content-container"><div class="info-box-content"><span><b> Note: </b> The SEMRush databases for the US (Google and Bing), the UK, Canada, Australia, France, Italy, Germany, Spain, Russia, and Brazil have data going back to January 2012. Those for Belgium, Netherlands, Israel, Ireland, Denmark, Finland, Norway, Sweden, Switzerland, Poland, Turkey, Argentina, Mexico, Hong Kong, and Singapore have data going back as far as January 2014. </span></div></div></div> </Note></td></tr><tr><td colspan="1" rowspan="1"><p> Month (Optional) </p></td><td colspan="1" rowspan="1"><p> Enter the month you want to retrieve information for. If you combine this with <b> Year </b>, you can pull data for a specific month. </p> <Note><div class="info-box-wrapper"><div class="info-box-icon-container"><span class="info-box-icon"></span></div><div class="info-box-content-container"><div class="info-box-content"><span><b> Note: </b> The SEMRush databases for the US (Google and Bing), the UK, Canada, Australia, France, Italy, Germany, Spain, Russia, and Brazil have data going back to January 2012. Those for Belgium, Netherlands, Israel, Ireland, Denmark, Finland, Norway, Sweden, Switzerland, Poland, Turkey, Argentina, Mexico, Hong Kong, and Singapore have data going back as far as January 2014. </span></div></div></div> </Note></td></tr><tr><td colspan="1" rowspan="1"><p> Report Type </p></td><td colspan="1" rowspan="1"><p> Select a daily or monthly report type. <b> Daily </b> returns the daily history of the domain for the last month only. <b> Monthly </b> returns the entire monthly history of the domain. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Display Positions </p></td><td colspan="1" rowspan="1"><p> Select the display position you want to retrieve information for. Options are as follows: </p><ul><li><p><b> New </b>. Shows keywords for which a domain entered Google's top 20 organic or paid search results. </p></li><li><p><b> Lost </b>. Shows keywords that no longer bring a domain into Google's top 20 organic or paid search results. </p></li><li><p><b> Rise </b>. Shows keywords that helped a domain to get a higher ranking in Google's top 20 organic or paid search results. </p></li><li><p><b> Fall </b>. Shows keywords that are currently helping a domain remain in Google's top 20 organic or paid search results but whose ranking has decreased. </p></li></ul></td></tr><tr><td colspan="1" rowspan="1"><p> Device Type </p></td><td colspan="1" rowspan="1"><p> Select a device type you want to retrieve information for. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Report Columns </p></td><td colspan="1" rowspan="1"><p> Select the type of data you want to retrieve. If no columns are selected, you will receive the report's default columns. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Sort By </p></td><td colspan="1" rowspan="1"><p> Select the type of data you want to sort by. If no column is selected, the default sort order will be used. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Display Limit </p></td><td colspan="1" rowspan="1"><p> Enter the number of rows you want to be returned. If you do not enter a value here, Domo requests the first 500 rows of data. The report may contain fewer rows than you request. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report Category | Select the SEMRush report you want to run. The following reports are available:   |  |  | | --- | --- | | Overview | Returns a summary of your SEMRush data. | | Domain | Returns competitive information for a specified website domain. | | Keywords | Returns competitive information for a specified keyword or keyword expression. | | URL | Returns competitive information for a specified landing page URL. | | Backlinks | Returns backlinks information for a specified regional database. | | Display Advertising | Returns competitive information for a specified advertiser and publisher. | |
    | Report | Select the report you want to run. |
    | Domain | Enter the domain of the website to be analyzed. **Format:** `example.com` |
    | Regional Database | Select one regional database to query. |
    | Domains | Enter one to five domains to compare by common keywords, unique keywords, all keywords, or search terms that are unique to a single domain. Use the format <sign>|<type>|<domain>, where: • <sign> means possible operations: +, -, \*, or /. • <type> can be **or** for organic keywords or **ad** for paid keywords. • <domain> indicates a domain name.  You can use the following combinations: • **Shared keywords:** \*|or|<your domain>|\*|or|<domain2>|\*|or|<domain3> (in this example) • **All keywords:** \*|or|<your domain>|+|or|<domain2>|+|or|<domain3> • **Unique keywords for your domain:** \*|or|<your domain>|-|or|<domain2>|-|or|<domain3> • **Untapped keywords for your domain:** \*|or|<domain2>|+|or|<domain3>|-|or|<your domain> • **Missing keywords for your domain:** \*|or|<domain2>|\*|or|<domain3>|-|or|<your domain> • **Keywords only in one of the domains:** \*|or|<your domain>|/|or|<domain2>|/|or|<domain3> |
    | Advertiser Domain | Enter the website domain of the advertiser you want to retrieve information for, in the format example.com |
    | Publisher Domain | Enter the website domain of the publisher you want to retrieve information for, in the format example.com |
    | Phrase | Enter a keyword or keyword expression you want to retrieve information for. |
    | URL | Enter the URL of a landing page you want to retrieve information for. Use the format http://example.com. |
    | Database | Select the regional database you want to retrieve data from. Select none to query all databases. |
    | Target | Enter a target domain (e.g. www.example.com), root domain (e.g. example.com), or URL (e.g. http://example.com) for the "Backlinks" report. |
    | Target Type | Select the target type for the target you entered in the **Target** field. |
    | Target List | Enter a comma-separated list of domains (e.g. www.example.com), root domains (e.g. example.com), and/or URLs (e.g. http://example.com) you want to retrieve information for. |
    | Target Type List | Enter a comma-separated list of target types matching the targets you entered for **Target List**. Target types are as follows:   - domain - root-domain - url   For example, if your target list was example.com, [http://www.example.com](http://www.example.com), www.example.com,  you would enter root domain, url, domain in this field. |
    | Year (Optional) | Enter the year you want to retrieve information for. If you combine this with **Month**, you can pull data for a specific month.
    <Note>**Note:** The SEMRush databases for the US (Google and Bing), the UK, Canada, Australia, France, Italy, Germany, Spain, Russia, and Brazil have data going back to January 2012. Those for Belgium, Netherlands, Israel, Ireland, Denmark, Finland, Norway, Sweden, Switzerland, Poland, Turkey, Argentina, Mexico, Hong Kong, and Singapore have data going back as far as January 2014. |
    | Month (Optional) | Enter the month you want to retrieve information for. If you combine this with **Year**, you can pull data for a specific month.  **Note:**The SEMRush databases for the US (Google and Bing), the UK, Canada, Australia, France, Italy, Germany, Spain, Russia, and Brazil have data going back to January 2012. Those for Belgium, Netherlands, Israel, Ireland, Denmark, Finland, Norway, Sweden, Switzerland, Poland, Turkey, Argentina, Mexico, Hong Kong, and Singapore have data going back as far as January 2014. |
    | Report Type | Select a daily or monthly report type. **Daily** returns the daily history of the domain for the last month only. **Monthly** returns the entire monthly history of the domain. |
    | Display Positions | Select the display position you want to retrieve information for. Options are as follows:   - **New**. Shows keywords for which a domain entered Google's top 20 organic or paid search results. - **Lost**. Shows keywords that no longer bring a domain into Google's top 20 organic or paid search results. - **Rise**. Shows keywords that helped a domain to get a higher ranking in Google's top 20 organic or paid search results. - **Fall**. Shows keywords that are currently helping a domain remain in Google's top 20 organic or paid search results but whose ranking has decreased. |
    | Device Type | Select a device type you want to retrieve information for. |
    | Report Columns | Select the type of data you want to retrieve. If no columns are selected, you will receive the report's default columns. |
    | Number of Rows | Specify the desired number of rows to be returned. The default limit is **500 rows** if the parameter is not specified. The final report size may be less than the requested limit. |
    | Sort By | Select the type of data you want to sort by. If no column is selected, the default sort order will be used. |
    | Display Limit | Enter the number of rows you want to be returned. If you do not enter a value here, Domo requests the first 500 rows of data. The report may contain fewer rows than you request. |</Note>

CHANGE 10  [### Other Panes]  (line 36 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/360042929554.mdx
TITLE:  GetSatisfaction Connector
LANG:   English (en_US)
ID:     360042929554
==============================================================================

CHANGE 1  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your GetSatisfaction account in the Data Center. This topic discusses the fields and menus that are specific to the GetSatisfaction connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    You connect to your GetSatisfaction account in the Data Center. This topic discusses the fields and menus that are specific to the GetSatisfaction connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 2  [## Connecting to Your GetSatisfaction Account]  (line 17 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Details** pane in the GetSatisfaction Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Details** pane in the GetSatisfaction Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [### Details Pane]  (line 21 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Topics By** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Topics By** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 4  [### Details Pane]  (line 23 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Topics By </p></td><td colspan="1" rowspan="1"><p> Select the GetSatisfaction report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p> All Topics </p></td><td colspan="1" rowspan="1"><p> Returns a list of all topics in all GetSatisfaction communities. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Topics by Community </p></td><td colspan="1" rowspan="1"><p> Returns a list of topics in a specified community. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Topics by Product </p></td><td colspan="1" rowspan="1"><p> Returns a list of topics associated with a specified product. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Topics by People </p></td><td colspan="1" rowspan="1"><p> Returns a list of topics posted by a specified user. </p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Community/People/Product Name (Optional) </p></td><td colspan="1" rowspan="1"><p> Enter the name of the community, product, or person for which topics need to be listed. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Topics Active Since </p></td><td colspan="1" rowspan="1"><p> Enter a date to retrieve topics active since that date. The format is <span class="mt-font-courier-new"> YYYY-MM-DD </span>, e.g. <span class="mt-font-courier-new"> 2011-12-31 </span>. You can also enter the word <span class="mt-font-courier-new"> Today </span> to use today's date, or <span class="mt-font-courier-new"> Today - 2 </span>, which would give you the day before yesterday's date. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Keyword (Optional) </p></td><td colspan="1" rowspan="1"><p> Enter a keyword to pull report data for. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Limit (Optional) </p></td><td colspan="1" rowspan="1"><p> Enter a limit on the number of topics to be returned. The maximum number is 10000. </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Topics By | Select the GetSatisfaction report you want to run. The following reports are available:   |  |  | | --- | --- | | All Topics | Returns a list of all topics in all GetSatisfaction communities. | | Topics by Community | Returns a list of topics in a specified community. | | Topics by Product | Returns a list of topics associated with a specified product. | | Topics by People | Returns a list of topics posted by a specified user. | |
    | Community/People/Product Name (Optional) | Enter the name of the community, product, or person for which topics need to be listed. |
    | Topics Active Since | Enter a date to retrieve topics active since that date. The format is YYYY-MM-DD, e.g. 2011-12-31. You can also enter the word Today to use today's date, or Today - 2, which would give you the day before yesterday's date. |
    | Keyword (Optional) | Enter a keyword to pull report data for. |
    | Limit (Optional) | Enter a limit on the number of topics to be returned. The maximum number is 10000. |

CHANGE 5  [### Other Panes]  (line 27 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](https://domo-support.domo.com/s/article/360042926274?language=en_US "Adding a DataSet Using a Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 6  [## FAQs]  (lines 31–32 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?

CHANGE 7  [## FAQs]  (lines 36–38 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits I should be aware of?

CHANGE 8  [## FAQs]  (lines 41–44 in repo file)

  OLD (remove from repo):
    
    </Accordion>
    </AccordionGroup>
    

==============================================================================
FILE:   s/article/360042929694.mdx
TITLE:  Zendesk Connector
LANG:   English (en_US)
ID:     360042929694
==============================================================================

CHANGE 1  [### Details Pane]  (line 51 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report (Core API) </p></td><td colspan="1" rowspan="1"><p> Select the Zendesk Core API report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p> General Query </p></td><td colspan="1" rowspan="1"><p> Returns general information about the company’s Zendesk instance for a given time period, such as ticket creation and update date, recipient, status, URL, and so on. </p></td></tr><tr><td colspan="1" rowspan="1"> Search (Ticket Basic) </td><td colspan="1" rowspan="1"> Designed to be faster than the other ticket reports, this report uses Zendesk's search API without loading any additional data. </td></tr><tr><td colspan="1" rowspan="1"><p> Specify Query </p></td><td colspan="1" rowspan="1"><p> Allows you to build a customized report by entering your own Zendesk search query. You can also select from two premade queries— <b> Open Tickets </b> and <b> Closed Tickets </b>. You can use these to return the number of open and closed tickets for your Zendesk instance. </p><p> The "Custom" option lets you specify your own Zendesk search query. For example, the query <span class="mt-font-courier-new"> type:user vip </span> would return information on all users with the <span class="mt-font-courier-new"> vip </span> tag. </p><p> For more information about entering Zendesk queries, see <a href="https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference" target="_blank" title="https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference"> https://support.zendesk.com/hc/en-us...arch-reference </a>. </p></td></tr><tr><td colspan="1" rowspan="1"> Ticket Comments </td><td colspan="1" rowspan="1"> Returns information about tickets comments for a given time period. </td></tr><tr><td colspan="1" rowspan="1"><p> Ticket Report </p></td><td colspan="1" rowspan="1"><p> Returns information about tickets for a given time period. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Tickets and Users Report </p></td><td colspan="1" rowspan="1"><p> Returns open tickets, tickets that have not been closed for &gt;120 days, and associated users, organizations, groups, and ticket metrics. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Users Report </p></td><td colspan="1" rowspan="1"><p> Returns information about users of the company’s Zendesk instance, such as name, email, time zone, user role, and so on. </p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Report (Talk API) </td><td colspan="1" rowspan="1"><p> Select the Zendesk Talk API report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p> Account Overview </p></td><td colspan="1" rowspan="1"><p> Returns account overview statistics. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Agent's Activity </p></td><td colspan="1" rowspan="1"><p> Returns statistics for the agent's activity. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Available Phone Numbers </p></td><td colspan="1" rowspan="1"><p> Returns available phone numbers. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Current Queue Activity </p></td><td colspan="1" rowspan="1"><p> Returns statistics for the current queue's activity. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Get Agent Availability </p></td><td colspan="1" rowspan="1"><p> Returns the agent's availability. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Get Greeting Categories </p></td><td colspan="1" rowspan="1"><p> Returns information about the selected greeting category. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Get Greeting </p></td><td colspan="1" rowspan="1"><p> Returns information about the specified greeting. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Get Phone Number </p></td><td colspan="1" rowspan="1"><p> Returns information for a specified phone number. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List Greeting Categories </p></td><td colspan="1" rowspan="1"><p> Lists all greeting categories. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List Greetings </p></td><td colspan="1" rowspan="1"><p> Lists all greetings. </p></td></tr><tr><td colspan="1" rowspan="1"><p> List Phone Numbers </p></td><td colspan="1" rowspan="1"><p> Lists all phone numbers. </p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p> Country Code </p></td><td colspan="1" rowspan="1"><p><span> Specify the ISO country code </span></p></td></tr><tr><td colspan="1" rowspan="1"><p> Contains </p></td><td colspan="1" rowspan="1"><p><span> Specify the regular expression to search for phone numbers. Valid characters are quotes ('') and [0-9a-zA-Z]. The quotes ('') character will match any single digit. </span></p></td></tr><tr><td colspan="1" rowspan="1"><p> Number Should be Toll-Free or Local </p></td><td colspan="1" rowspan="1"><p><span> Specify whether the number should be toll-free or local. </span></p></td></tr><tr><td colspan="1" rowspan="1"><p> Area Code </p></td><td colspan="1" rowspan="1"><p><span> Specify to find phone numbers in the specified area code (US and Canada only). </span></p></td></tr><tr><td colspan="1" rowspan="1"><p> From Days </p></td><td colspan="1" rowspan="1"><p> Enter the number of past days for your report. For example, if you entered <code> 30 </code>, the report would show data for the past 30 days. </p></td></tr><tr><td colspan="1" rowspan="1"><p> To Days </p></td><td colspan="1" rowspan="1"><p> Enter the number of past days that should <em> not </em> appear in your report. For example, if you entered <code> 3 </code>, your report would contain data up until 3 days ago. </p></td></tr><tr><td colspan="1" rowspan="1"><p> Agent </p></td><td colspan="1" rowspan="1"><p> S <span> elect an Agent. </span></p></td></tr><tr><td colspan="1" rowspan="1"> Greeting Category </td><td colspan="1" rowspan="1"><span> select a greeting category. </span></td></tr><tr><td colspan="1" rowspan="1"> Greeting </td><td colspan="1" rowspan="1"><span> select a greeting. </span></td></tr><tr><td colspan="1" rowspan="1"> Phone Number </td><td colspan="1" rowspan="1"><span> Select a phone number. </span></td></tr><tr><td colspan="1" rowspan="1"><p> Search Query </p></td><td colspan="1" rowspan="1"><p> Select the desired search query type. <b> Open Tickets </b> and <b> Closed Tickets </b> are premade queries you can use to return the number of open and closed tickets for your Zendesk instance. The Custom option lets you specify your own Zendesk search query. For example, the query <code> type:user vip </code> would return information on all users with the <code> vip </code> tag. </p><p> For more information about entering Zendesk queries, see <a href="https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference" target="_blank"> https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference </a>. </p></td></tr><tr><td colspan="1" rowspan="1"> Query Field </td><td colspan="1" rowspan="1"><span> Select the desired search query type. <b> Open Tickets </b> and <b> Closed Tickets </b> are premade queries you can use to return the number of open and closed tickets for your Zendesk instance. The <b> Custom </b> option lets you specify your own Zendesk search query. For more information about entering Zendesk queries, see <tt><a href="https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference" rel="noopener" target="_blank"> https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference </a></tt>. </span></td></tr><tr><td colspan="1" rowspan="1"> Include Attachments </td><td colspan="1" rowspan="1"><span> Specify whether you would like to include the attachments in the results. </span></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report (Core API) | Select the Zendesk Core API report you want to run. The following reports are available:   |  |  | | --- | --- | | General Query | Returns general information about the company’s Zendesk instance for a given time period, such as ticket creation and update date, recipient, status, URL, and so on. | | Search (Ticket Basic) | Designed to be faster than the other ticket reports, this report uses Zendesk's search API without loading any additional data. | | Specify Query | Allows you to build a customized report by entering your own Zendesk search query. You can also select from two premade queries—**Open Tickets** and **Closed Tickets**. You can use these to return the number of open and closed tickets for your Zendesk instance.  The "Custom" option lets you specify your own Zendesk search query. For example, the query type:user vip would return information on all users with the vip tag.  For more information about entering Zendesk queries, see [https://support.zendesk.com/hc/en-us...arch-reference](https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference). | | Ticket Comments | Returns information about tickets comments for a given time period. | | Ticket Report | Returns information about tickets for a given time period. | | Tickets and Users Report | Returns open tickets, tickets that have not been closed for >120 days, and associated users, organizations, groups, and ticket metrics. | | Users Report | Returns information about users of the company’s Zendesk instance, such as name, email, time zone, user role, and so on. | |
    | Report (Talk API) | Select the Zendesk Talk API report you want to run. The following reports are available:   |  |  | | --- | --- | | Account Overview | Returns account overview statistics. | | Agent's Activity | Returns statistics for the agent's activity. | | Available Phone Numbers | Returns available phone numbers. | | Current Queue Activity | Returns statistics for the current queue's activity. | | Get Agent Availability | Returns the agent's availability. | | Get Greeting Categories | Returns information about the selected greeting category. | | Get Greeting | Returns information about the specified greeting. | | Get Phone Number | Returns information for a specified phone number. | | List Greeting Categories | Lists all greeting categories. | | List Greetings | Lists all greetings. | | List Phone Numbers | Lists all phone numbers. | |
    | Country Code | Specify the ISO country code |
    | Contains | Specify the regular expression to search for phone numbers. Valid characters are quotes ('') and [0-9a-zA-Z]. The quotes ('') character will match any single digit. |
    | Number Should be Toll-Free or Local | Specify whether the number should be toll-free or local. |
    | Area Code | Specify to find phone numbers in the specified area code (US and Canada only). |
    | From Days | Enter the number of past days for your report. For example, if you entered `30`, the report would show data for the past 30 days. |
    | To Days | Enter the number of past days that should *not* appear in your report. For example, if you entered `3`, your report would contain data up until 3 days ago. |
    | Agent | Select an Agent. |
    | Greeting Category | select a greeting category. |
    | Greeting | select a greeting. |
    | Phone Number | Select a phone number. |
    | Search Query | Select the desired search query type. **Open Tickets** and **Closed Tickets** are premade queries you can use to return the number of open and closed tickets for your Zendesk instance. The Custom option lets you specify your own Zendesk search query. For example, the query `type:user vip` would return information on all users with the `vip` tag.  For more information about entering Zendesk queries, see [https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference](https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference). |
    | Query Field | Select the desired search query type. **Open Tickets** and **Closed Tickets** are premade queries you can use to return the number of open and closed tickets for your Zendesk instance. The **Custom** option lets you specify your own Zendesk search query. For more information about entering Zendesk queries, see https://support.zendesk.com/hc/en-us/articles/203663226-Zendesk-search-reference. |
    | Include Attachments | Specify whether you would like to include the attachments in the results. |

==============================================================================
FILE:   s/article/360042929714.mdx
TITLE:  Zoho CRM Connector
LANG:   English (en_US)
ID:     360042929714
==============================================================================

CHANGE 1  [## Intro]  (line 13 in repo file)

  OLD (remove from repo):
    <table class="mt-responsive-table" data-aura-rendered-by="33:195;a"><tbody><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Use Cases </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> This connector lets you track your sales pipeline, categorize leads, manage inventory, and keep on top of sales procurement. </p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Metrics </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><ul><li><p> Number of prospects </p></li><li><p> Number of new customers </p></li><li><p> Number of retained customers </p></li><li><p> Close rate </p></li><li><p> Renewal rate </p></li><li><p> Sales calls made </p></li><li><p> Sales calls per opportunity </p></li><li><p> Sales stage duration </p></li><li><p> Sales cycle duration </p></li><li><p> Proposals given </p></li><li><p> Customer lifetime value </p></li><li><p> Upsell ratio </p></li></ul></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Primary Company Roles </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><ul><li><p> Sales </p></li><li><p> Marketing </p></li></ul></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Average Implementation Time </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> some stuff </p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b> Ease of Use (on a 1-to-10 scale with 1 being easiest) </b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p> some stuff </p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector lets you track your sales pipeline, categorize leads, manage inventory, and keep on top of sales procurement. |
    | **Primary Metrics** | - Number of prospects - Number of new customers - Number of retained customers - Close rate - Renewal rate - Sales calls made - Sales calls per opportunity - Sales stage duration - Sales cycle duration - Proposals given - Customer lifetime value - Upsell ratio |
    | **Primary Company Roles** | - Sales - Marketing |
    | **Average Implementation Time** | some stuff |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | some stuff |

CHANGE 2  [#### To obtain your client id and client secret:]  (line 28 in repo file)

  OLD (remove from repo):
    <Note>

CHANGE 3  [#### To obtain your client id and client secret:]  (line 30 in repo file)

  OLD (remove from repo):
    </Note>

CHANGE 4  [### Details Pane]  (lines 53–54 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p><span class="s1"><b> Menu </b></span></p></th><th colspan="1" rowspan="1"><p><span class="s1"><b> Description </b></span></p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Method </p></td><td colspan="1" rowspan="1"><p><span class="s1"> Select the Zoho CRM data you want to retrieve. The following methods are available: </span></p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"> Attachments </td><td colspan="1" rowspan="1"> Returns a list of attachments </td></tr><tr><td colspan="1" rowspan="1"> Custom View Records </td><td colspan="1" rowspan="1"> Returns the custom views data of a particular module </td></tr><tr><td colspan="1" rowspan="1"> Deleted Records </td><td colspan="1" rowspan="1"> Returns a list of deleted records </td></tr><tr><td colspan="1" rowspan="1"> Fields </td><td colspan="1" rowspan="1"> Returns the field meta data for the specified module </td></tr><tr><td colspan="1" rowspan="1"> Layouts </td><td colspan="1" rowspan="1"> Returns the layouts associated with the particular module </td></tr><tr><td colspan="1" rowspan="1"> Modules </td><td colspan="1" rowspan="1"> Returns a list of modules available in the CRM account. </td></tr><tr><td colspan="1" rowspan="1"> Notes </td><td colspan="1" rowspan="1"> Returns a list of notes </td></tr><tr><td colspan="1" rowspan="1"> Organizations </td><td colspan="1" rowspan="1"> Returns the organization data </td></tr><tr><td colspan="1" rowspan="1"> Profiles </td><td colspan="1" rowspan="1"> Returns the data of profiles </td></tr><tr><td colspan="1" rowspan="1"> Records </td><td colspan="1" rowspan="1"> Returns a list of available records from a module </td></tr><tr><td colspan="1" rowspan="1"> Related List </td><td colspan="1" rowspan="1"> Returns the related list data of a particular module </td></tr><tr><td colspan="1" rowspan="1"> Related Records </td><td colspan="1" rowspan="1"> Returns the related list records </td></tr><tr><td colspan="1" rowspan="1"> Roles </td><td colspan="1" rowspan="1"> Returns the data of roles </td></tr><tr><td colspan="1" rowspan="1"> Search Records </td><td colspan="1" rowspan="1"> Returns the records that matches the search criteria </td></tr><tr><td colspan="1" rowspan="1"> Search using COQL </td><td colspan="1" rowspan="1"> Returns the records that matches the search criteria query </td></tr><tr><td colspan="1" rowspan="1"> Tags </td><td colspan="1" rowspan="1"> Returns all the tags information in an organization </td></tr><tr><td colspan="1" rowspan="1"> Users </td><td colspan="1" rowspan="1"> Returns the list of users in your organization </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p><span class="s1"> Module </span></p></td><td colspan="1" rowspan="1"><p><span class="s1"> Select the Zoho CRM module to pull data from. </span></p></td></tr><tr><td colspan="1" rowspan="1"> Approval State </td><td colspan="1" rowspan="1"> Select the approval status to get a list of records of that status. </td></tr><tr><td colspan="1" rowspan="1"><p><span class="s1"> Deleted Record Type </span></p></td><td colspan="1" rowspan="1"><table border="1"><tbody><tr><th colspan="1" rowspan="1"> Report </th><th colspan="1" rowspan="1"> Description </th></tr><tr><td colspan="1" rowspan="1"> All </td><td colspan="1" rowspan="1"> Returns a list of all deleted records </td></tr><tr><td colspan="1" rowspan="1"> Permanent </td><td colspan="1" rowspan="1"> Returns a list list of permanently deleted records </td></tr><tr><td colspan="1" rowspan="1"> Recycle </td><td colspan="1" rowspan="1"> Returns a list of deleted records from recycle bin </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p><span class="s1"> Record ID </span></p></td><td colspan="1" rowspan="1"><span> Select the Record Id. </span></td></tr><tr><td colspan="1" rowspan="1"><p><span class="s1"> Related List API Name </span></p></td><td colspan="1" rowspan="1"><span> Select the Related List Api Name. </span></td></tr><tr><td colspan="1" rowspan="1"><p><span class="s1"> Search Parameter </span></p></td><td colspan="1" rowspan="1"><span> Select the search parameter. </span></td></tr><tr><td colspan="1" rowspan="1"><p><span class="s1"> Past Days </span></p></td><td colspan="1" rowspan="1"> Enter the number of days of data to retrieve. For example, entering <span class="mt-font-courier-new"> 30 </span> would pull data for the last 30 days. </td></tr><tr><td colspan="1" rowspan="1"><p><span class="s1"> Search Condition </span></p></td><td colspan="1" rowspan="1"><p><span class="s1"><span> Enter the search statement to pull data for. </span></span></p><table border="1"><tbody><tr><td colspan="1" rowspan="1"></td><td colspan="1" rowspan="1"><b> Format </b></td><td colspan="1" rowspan="1"><b> Example </b></td></tr><tr><td colspan="1" rowspan="1"> Email </td><td colspan="1" rowspan="1"><span class="s1"><span><a href="mailto:username@domain.com"> username@domain.com </a></span></span></td><td colspan="1" rowspan="1"><span class="s1"><span><a href="mailto:daly@zylker.com"> daly@zylker.com </a></span></span></td></tr><tr><td colspan="1" rowspan="1"> Phone </td><td colspan="1" rowspan="1"><span class="s1"><span> xxx-xxx-xxxx </span></span></td><td colspan="1" rowspan="1"><span class="s1"><span> 888-555-2145 </span></span></td></tr><tr><td colspan="1" rowspan="1"> Word </td><td colspan="1" rowspan="1"><span class="s1"><span> Search Word </span></span></td><td colspan="1" rowspan="1"><span class="s1"><span> test </span></span></td></tr><tr><td colspan="1" rowspan="1"> Criteria </td><td colspan="1" rowspan="1"><span class="s1"><span> (({apiname}:{starts_with|equals}:{value}) and ({apiname}:{starts_with|equals}:{value})) </span></span></td><td colspan="1" rowspan="1"><p><span class="s1"> (Last_Name:equals:Boyle) for Lead Module </span></p><p><span class="s1"><span> will search the Last_Name column for rows equaling Boyle </span></span></p></td></tr></tbody></table><p><span class="s1"><span> See </span><a href="https://www.zoho.com/crm/help/api/v2/#ra-search-records" target="_blank" title="Zoho API Help Link"> Zoho API Help Link </a><span> for more help. </span></span></p></td></tr><tr><td colspan="1" rowspan="1"> Custom View Name </td><td colspan="1" rowspan="1"><span> Select the Custom View Name to get the list of records based on custom views. </span></td></tr><tr><td colspan="1" rowspan="1"> User Type </td><td colspan="1" rowspan="1"><p> Select the user type you want to pull data for. </p><table border="1"><tbody><tr><td colspan="1" rowspan="1"> Active Confirmed Users </td><td colspan="1" rowspan="1"> Returns the list of active users who are also confirmed </td></tr><tr><td colspan="1" rowspan="1"> Active Users </td><td colspan="1" rowspan="1"> Returns a list of list of all Active Users </td></tr><tr><td colspan="1" rowspan="1"> Admin Users </td><td colspan="1" rowspan="1"> Returns the the list of admin users </td></tr><tr><td colspan="1" rowspan="1"> All Users </td><td colspan="1" rowspan="1"> Returns the the list of All users </td></tr><tr><td colspan="1" rowspan="1"> Confirmed Users </td><td colspan="1" rowspan="1"> Returns the list of confirmed users </td></tr><tr><td colspan="1" rowspan="1"> Current User </td><td colspan="1" rowspan="1"> Returns the the list of current CRM users </td></tr><tr><td colspan="1" rowspan="1"> Deactivated Users </td><td colspan="1" rowspan="1"> Returns a list of all users who were deactivated </td></tr><tr><td colspan="1" rowspan="1"> Deleted Users </td><td colspan="1" rowspan="1"> Returns the list of deleted users </td></tr><tr><td colspan="1" rowspan="1"> Not Confirmed Users </td><td colspan="1" rowspan="1"> Returns the list of non-confirmed users </td></tr></tbody></table></td></tr></tbody></table>
    

  NEW (replace with / insert from CSV):
    | **Menu** | **Description** |
    | --- | --- |
    | Method | Select the Zoho CRM data you want to retrieve. The following methods are available:   |  |  | | --- | --- | | Attachments | Returns a list of attachments | | Custom View Records | Returns the custom views data of a particular module | | Deleted Records | Returns a list of deleted records | | Fields | Returns the field meta data for the specified module | | Layouts | Returns the layouts associated with the particular module | | Modules | Returns a list of modules available in the CRM account. | | Notes | Returns a list of notes | | Organizations | Returns the organization data | | Profiles | Returns the data of profiles | | Records | Returns a list of available records from a module | | Related List | Returns the related list data of a particular module | | Related Records | Returns the related list records | | Roles | Returns the data of roles | | Search Records | Returns the records that matches the search criteria | | Search using COQL | Returns the records that matches the search criteria query | | Tags | Returns all the tags information in an organization | | Users | Returns the list of users in your organization | |
    | Module | Select the Zoho CRM module to pull data from. |
    | Approval State | Select the approval status to get a list of records of that status. |
    | Deleted Record Type | | Report | Description | | --- | --- | | All | Returns a list of all deleted records | | Permanent | Returns a list list of permanently deleted records | | Recycle | Returns a list of deleted records from recycle bin | |
    | Record ID | Select the Record Id. |
    | Related List API Name | Select the Related List Api Name. |
    | Search Parameter | Select the search parameter. |
    | Past Days | Enter the number of days of data to retrieve. For example, entering 30 would pull data for the last 30 days. |
    | Search Condition | Enter the search statement to pull data for.   |  |  |  | | --- | --- | --- | |  | **Format** | **Example** | | Email | username@domain.com | daly@zylker.com | | Phone | xxx-xxx-xxxx | 888-555-2145 | | Word | Search Word | test | | Criteria | (({apiname}:{starts\_with|equals}:{value}) and ({apiname}:{starts\_with|equals}:{value})) | (Last\_Name:equals:Boyle) for Lead Module   will search the Last\_Name column for rows equaling Boyle |   See [Zoho API Help Link](https://www.zoho.com/crm/help/api/v2/#ra-search-records) for more help. |
    | Custom View Name | Select the Custom View Name to get the list of records based on custom views. |
    | User Type | Select the user type you want to pull data for.   |  |  | | --- | --- | | Active Confirmed Users | Returns the list of active users who are also confirmed | | Active Users | Returns a list of list of all Active Users | | Admin Users | Returns the the list of admin users | | All Users | Returns the the list of All users | | Confirmed Users | Returns the list of confirmed users | | Current User | Returns the the list of current CRM users | | Deactivated Users | Returns a list of all users who were deactivated | | Deleted Users | Returns the list of deleted users | | Not Confirmed Users | Returns the list of non-confirmed users | |

CHANGE 5  [## FAQs]  (lines 93–94 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="What version of the ZohoCRM API does this connector use?">

  NEW (replace with / insert from CSV):
    #### What version of the ZohoCRM API does this connector use?

CHANGE 6  [## FAQs]  (lines 98–100 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?

CHANGE 7  [## FAQs]  (lines 104–106 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Where can I find my client id and client secret?">

  NEW (replace with / insert from CSV):
    #### Where can I find my client id and client secret?

CHANGE 8  [## FAQs]  (lines 113–114 in repo file)

  OLD (remove from repo):
    4. Provide the client details and click **Create**.
    

  NEW (replace with / insert from CSV):
    4. Provide the client details and click **Create**.

CHANGE 9  [## FAQs]  (line 117 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    #### What is the redirect URI for Zoho CRM?

CHANGE 10  [## FAQs]  (line 119 in repo file)

  OLD (remove from repo):
    <Accordion title="What is the redirect URI for Zoho CRM?">

  NEW (replace with / insert from CSV):
    The redirect URI for Zoho CRM is
    [https://oauth.domo.com/api/data/v2/oauth/providers/zoho-enterprise/exchange](https://oauth.domo.com/api/data/v2/oauth/providers/zoho-enterprise/exchange) .

CHANGE 11  [## FAQs]  (lines 121–125 in repo file)

  OLD (remove from repo):
    The redirect URI for Zoho CRM is [https://oauth.domo.com/api/data/v2/oauth/providers/zoho-enterprise/exchange](https://oauth.domo.com/api/data/v2/oauth/providers/zoho-enterprise/exchange "https://oauth.domo.com/api/data/v2/oauth/providers/zoho-enterprise/exchange").
    
    </Accordion>
    
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?

CHANGE 12  [## FAQs]  (lines 129–131 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?

CHANGE 13  [## FAQs]  (lines 134–136 in repo file)

  OLD (remove from repo):
    
    </Accordion>
    </AccordionGroup>

==============================================================================
FILE:   s/article/360042929794.mdx
TITLE:  Klout Connector
LANG:   English (en_US)
ID:     360042929794
==============================================================================

CHANGE 1  [(top of file)]  (lines 5–7 in repo file)

  OLD (remove from repo):
    Lithium has made the decision to discontinue the Klout service. Due to this change, the Klout connector will no longer be usable and all Klout DataSets will cease to run.
    
    Please reach out to Support if you have any questions. Or, you can reach out to Klout directly via [KloutQuestions@lithium.com](mailto:KloutQuestions@lithium.com "mailto:KloutQuestions@lithium.com").

  NEW (replace with / insert from CSV):
    Lithium has made the decision to discontinue the Klout service. Due to this change, the Klout connector will no longer be usable and all Klout DataSets will cease to run.
    
    Please reach out to Support if you have any questions. Or, you can reach out to Klout directly via [KloutQuestions@lithium.com](mailto:KloutQuestions@lithium.com).

==============================================================================
FILE:   s/article/360042930154.mdx
TITLE:  Adaptive Insights Connector
LANG:   English (en_US)
ID:     360042930154
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Adaptive Insights, formerly Adaptive Planning, provides online software that allows users to replace multiple spreadsheets with an integrated system for consolidating data. To learn more about the Adaptive Insights API, log into Adaptive Insights and go to **Product Documents > Integration Guide**.

  NEW (replace with / insert from CSV):
    Adaptive Insights, formerly Adaptive Planning, provides online software that allows users to replace multiple spreadsheets with an integrated system for consolidating data. To learn more about the Adaptive Insights API, log into Adaptive Insights and go to **Product Documents > Integration Guide**.

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your Adaptive Insights account in the Data Center. This topic discusses the fields and menus that are specific to the Adaptive Insights connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You connect to your Adaptive Insights account in the Data Center. This topic discusses the fields and menus that are specific to the Adaptive Insights connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Intro]  (lines 11–96 in repo file)

  ⚠ LARGE CHANGE (93 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table class="mt-responsive-table" data-aura-rendered-by="33:201;a">
      <tbody>
        <tr>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <p>
              <b> Primary Use Cases </b>
            </p>
          </td>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <p> This connector helps you answer these questions: </p>
            <ul>
              <li>
                <p> What are my current financial forecasts? </p>
              </li>
              <li>
                <p> How have these tracked to actuals? </p>
              </li>
            </ul>
          </td>
        </tr>
        <tr>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <p>
              <b> Primary Metrics </b>
            </p>
          </td>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <ul>
              <li>
                <p> Forecasted financials </p>
              </li>
              <li>
                <p> Actual financials (budgets, income, various P&amp;L line items) </p>
              </li>
            </ul>
          </td>
        </tr>
        <tr>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <p>
              <b> Primary Company Roles </b>
            </p>
          </td>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <ul>
              <li>
                <p> FP&amp;A analyst </p>
              </li>
              <li>
                <p> Financial analysis </p>
              </li>
              <li>
                <p> Director of finance </p>
              </li>
              <li>
                <p> CPO </p>
              </li>
              <li>
                <p> VP of finance </p>
              </li>
            </ul>
          </td>
        </tr>
        <tr>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <p>
              <b> Average Implementation Time </b>
            </p>
          </td>
          <td class="mt-noheading" colspan="1" rowspan="1">
            {" "}
            Could be 40 hours or more, due to the complexity of Adaptive Insights' financial system{" "}
          </td>
        </tr>
        <tr>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <p>
              <b> Ease of Use (on a 1-to-10 scale with 1 being easiest) </b>
            </p>
          </td>
          <td class="mt-noheading" colspan="1" rowspan="1">
            <p> 8 </p>
          </td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector helps you answer these questions:   - What are my current financial forecasts? - How have these tracked to actuals? |
    | **Primary Metrics** | - Forecasted financials - Actual financials (budgets, income, various P&L line items) |
    | **Primary Company Roles** | - FP&A analyst - Financial analysis - Director of finance - CPO - VP of finance |
    | **Average Implementation Time** | Could be 40 hours or more, due to the complexity of Adaptive Insights' financial system |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 8 |

CHANGE 4  [## Best Practices]  (line 100 in repo file)

  OLD (remove from repo):
    If you need to unpivot data, you can do so using Magic's "Collapse Columns" action. For more information, see [ETL Actions: Edit Columns - Collapse Columns](/s/article/360043427353).

  NEW (replace with / insert from CSV):
    If you need to unpivot data, you can do so using Magic's "Collapse Columns" action. For more information, see [ETL Actions: Edit Columns - Collapse Columns](/s/article/360043427353).

CHANGE 5  [## Prerequisites]  (lines 107–108 in repo file)

  OLD (remove from repo):
    - Your Adaptive Insights instance code. You can find this by logging into Adaptive Insights and navigating to
      **Admin > System > General Setup**. You can then find the code under **Setup Information**.

  NEW (replace with / insert from CSV):
    - Your Adaptive Insights instance code. You can find this by logging into Adaptive Insights and navigating to **Admin > System > General Setup**. You can then find the code under **Setup Information**.

CHANGE 6  [## Connecting to Your Adaptive Insights Account]  (line 112 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Adaptive Insights Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Adaptive Insights Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 7  [### Credentials Pane]  (lines 116–117 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Adaptive Insights account. Based on the Authentication type selected, the fields vary,
    

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Adaptive Insights account. Based on the Authentication type selected, the fields vary,

CHANGE 8  [### Credentials Pane]  (lines 120–167 in repo file)

  ⚠ LARGE CHANGE (53 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:201;a">
      <thead>
        <tr>
          <th colspan="1" rowspan="1">
            <p>
              <span class="s1"> Field </span>
            </p>
          </th>
          <th colspan="1" rowspan="1">
            <p>
              <span class="s1"> Description </span>
            </p>
          </th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Username{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Enter your Adaptive Insights username.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Password{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Enter your Adaptive Insights password.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Instance{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Enter your Adaptive Insights instance code. To learn how to find your code, see "Prerequisites," above.{" "}
          </td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Username | Enter your Adaptive Insights username. |
    | Password | Enter your Adaptive Insights password. |
    | Instance | Enter your Adaptive Insights instance code. To learn how to find your code, see "Prerequisites," above. |

CHANGE 9  [### Credentials Pane]  (lines 171–238 in repo file)

  ⚠ LARGE CHANGE (75 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:201;a">
      <thead>
        <tr>
          <th colspan="1" rowspan="1">
            <p>
              <span class="s1"> Field </span>
            </p>
          </th>
          <th colspan="1" rowspan="1">
            <p>
              <span class="s1"> Description </span>
            </p>
          </th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Client ID{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Enter the client ID you obtained after registering a Workday API Client.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Integration System User Id{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            This ID should start with <b> PublicAPIISU_ </b>.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Private Key{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Enter the private key that matches the public key you uploaded in Workday{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Token Endpoint{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Enter the token endpoint obtained when registering a Workday API Client.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            REST Endpoint:{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Enter the REST endpoint obtained when registering a Workday API Client.{" "}
          </td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter the client ID you obtained after registering a Workday API Client. |
    | Integration System User Id | This ID should start with **PublicAPIISU\_**. |
    | Private Key | Enter the private key that matches the public key you uploaded in Workday |
    | Token Endpoint | Enter the token endpoint obtained when registering a Workday API Client. |
    | REST Endpoint: | Enter the REST endpoint obtained when registering a Workday API Client. |

CHANGE 10  [### Credentials Pane]  (line 240 in repo file)

  OLD (remove from repo):
    Once you have entered valid Adaptive Insights credentials, you can use the same account any time you go to create a new Adaptive Insights DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054 "Managing User Accounts for Connectors").

  NEW (replace with / insert from CSV):
    Once you have entered valid Adaptive Insights credentials, you can use the same account any time you go to create a new Adaptive Insights DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 11  [### Details Pane]  (line 244 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 12  [### Details Pane]  (lines 246–617 in repo file)

  ⚠ LARGE CHANGE (393 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:201;a">
      <thead>
        <tr>
          <th colspan="1" rowspan="1">
            <p> Menu </p>
          </th>
          <th colspan="1" rowspan="1">
            <p> Description </p>
          </th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Report </p>
          </td>
          <td colspan="1" rowspan="1">
            <p> Select the Adaptive Insights report you want to run. The following reports are available: </p>
            <table border="1" cellpadding="1" cellspacing="1">
              <tbody>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Accounts{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    <span> Retrieves metadata for the complete list of all accounts in the system. </span>
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Active Currencies{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    <span> Retrieves metadata for the complete list of all currencies configured in the system. </span>
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Attributes{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    <span> Returns the list of attributes. </span>
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    <p class="DatasetsPaper_title_3bKfQ"> Export Data </p>
                  </td>
                  <td colspan="1" rowspan="1">
                    <span> Retrieves a set of data from the requested version in the requested instance. </span>
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Export Data from Modeled and Cube Sheet{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Retrieves data for the accounts, dimensions and levels that correspond to the selected sheet.{" "}
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    <p class="DatasetsPaper_title_3bKfQ"> Customize data export </p>
                  </td>
                  <td colspan="1" rowspan="1">
                    <span> Retrieves data for the accounts, dimensions and levels specified. </span>
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    <p class="DatasetsPaper_title_3bKfQ"> Dimensions Structure </p>
                  </td>
                  <td colspan="1" rowspan="1">
                    <span> Retrieves metadata about custom dimensions in the system preserving their structure. </span>
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    <p class="DatasetsPaper_title_3bKfQ"> Dimensions with Attributes </p>
                  </td>
                  <td colspan="1" rowspan="1">
                    <span>
                      {" "}
                      Represents a single member value of a custom dimension being returned in the response to an exportDimensions
                      Call with Attributes passed in the request.{" "}
                    </span>
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Instances{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Retrieves metadata about the instances a user has access to, if the user has access to multiple instances.{" "}
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Levels{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Retrieves metadata about organization levels in the system.{" "}
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Roles{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Retrieves metadata about roles in the system.{" "}
                  </td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Versions{" "}
                  </td>
                  <td colspan="1" rowspan="1">
                    {" "}
                    Retrieves metadata about versions in the system.{" "}
                  </td>
                </tr>
              </tbody>
            </table>
            <p>
              {" "}
              Standard sheets are not available directly through the API. You have two options for retrieving standard sheet
              data:{" "}
            </p>
            <ul>
              <li> Export data from Custom and Assumption Groups. </li>
              <li> Export data from accounts that can be used to reproduce a standard sheet. </li>
            </ul>
            <p>
              <b> To reproduce a standard sheet, </b>
            </p>
            <ol>
              <li>
                <p> Log into your Adaptive Insights account. </p>
              </li>
              <li>
                <p>
                  {" "}
                  Go to <b> Model Management &gt; Sheets </b>.{" "}
                </p>
              </li>
              <li>
                <p> Locate the sheet you want to reproduce. </p>
              </li>
              <li>
                <p>
                  {" "}
                  Click on <b> Account Groups </b>.{" "}
                </p>
              </li>
              <li>
                <p> In the connector UI in Domo, select the accounts that are showing in Adaptive Insights. </p>
              </li>
            </ol>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Roll Up Values?{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Select whether you want to retrieve rolled up values.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Date Filtering{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Select whether you want to filter by dates.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Duration </p>
          </td>
          <td colspan="1" rowspan="1">
            <p> Select whether you want to pull data for a specific date or a date range. </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Report Date </p>
          </td>
          <td colspan="1" rowspan="1">
            <p> Select whether the report data is for a specific date or for a relative number of days back from today. </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Select Specific Date </p>
          </td>
          <td colspan="1" rowspan="1">
            <p> Select the date for the report. </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Days Back </p>
          </td>
          <td colspan="1" rowspan="1">
            <p> Enter the number of past days that should appear in the report. </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Start Date </p>
          </td>
          <td colspan="1" rowspan="1">
            <p>
              {" "}
              Specify whether the first date in your date range is a specific or relative date. You select the last date in your
              range in <b> End Date </b>.{" "}
            </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> End Date </p>
          </td>
          <td colspan="1" rowspan="1">
            <p>
              {" "}
              Specify whether the second date in your date range is a specific or relative date. You select the first date in
              your range in <b> Start Date </b>.{" "}
            </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Select Specific Start Date </p>
          </td>
          <td colspan="1" rowspan="1">
            <p> Select the first date in your date range. </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Select Specific End Date </p>
          </td>
          <td colspan="1" rowspan="1">
            <p> Select the second date in your date range. </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Days Back to Start From </p>
          </td>
          <td colspan="1" rowspan="1">
            <p>
              {" "}
              Enter the number of the farthest day back that should be represented in the report. Combine with
              <b>
                {" "}
                Days Back to End At{" "}
              </b>
              to create a range of represented days.{" "}
            </p>
            <p>
              {" "}
              For example, if you entered
              <span class="mt-font-courier-new"> 10
              </span> for
              <b>
                {" "}
                Days Back to Start From{" "}
              </b>
              and <span class="mt-font-courier-new"> 5 </span> for <b> Days Back to End At </b>, the report would contain
              data for 10 days ago up until 5 days ago.{" "}
            </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            <p> Days Back to End At </p>
          </td>
          <td colspan="1" rowspan="1">
            <p>
              {" "}
              Enter the number of the most recent day back that should be represented in the report. Combine with
               <b>
                {" "}
                Days Back to Start From{" "}
              </b>
              to create a range of represented days.{" "}
            </p>
            <p>
              {" "}
              For example, if you entered
              <span class="mt-font-courier-new"> 10
              </span> for
              <b>
                {" "}
                Days Back to Start From{" "}
              </b>
              and <span class="mt-font-courier-new"> 5 </span> for <b> Days Back to End At </b>, the report would contain
              data for 10 days ago up until 5 days ago.{" "}
            </p>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Data Version{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Select the version of the data you want to retrieve data for.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Currency{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Select whether to retrieve data for US dollars, your local currency, or the currency used by your corporation.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Data Sheet{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Select the sheet you want to retrieve data for.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Accounts{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Select all of the accounts you want to retrieve data for.{" "}
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1">
            {" "}
            Dimension Name{" "}
          </td>
          <td colspan="1" rowspan="1">
            {" "}
            Select the dimension you want to retrieve data for.{" "}
          </td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report Type | You can select **Data** or **Assets**.   |  |  | | --- | --- | | Data | Retrieves the data. | | Assets | Retrieves the assets such as accounts, dimensions, levels | |
    | Report | Select the Adaptive Insights report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Accounts | Retrieves metadata for all accounts in the system. | | Active Currencies | Retrieves metadata for all currencies configured in the system. | | Attributes | Returns the list of attributes. | | Export Data | Retrieves a set of data from the requested version in the requested instance. | | Export Data from Sheets (Standard, Modeled or Cube) | Retrieves data for the accounts, dimensions and levels that correspond to the selected sheet. | | Customize data export | Retrieves the data for the accounts, dimensions and levels specified. | | Dimensions Structure | Retrieves metadata about custom dimensions in the system preserving their structure. | | Dimensions with Attributes | Represents a single member value of a custom dimension being returned in the response to an exportDimensions Call with Attributes passed in the request. | | Instances | Retrieve metadata about the instances a user has access to if the user has access to multiple instances. | | Levels | Retrieves metadata about organization levels in the system. | | Roles | Retrieves metadata about roles in the system. | | Versions | Retrieves metadata about versions in the system. |   Standard sheets are not available directly through the API. You have two options for retrieving standard sheet data:   - Export data from Custom and Assumption Groups. - Export data from accounts that can be used to reproduce a standard sheet.   **To reproduce a standard sheet,**   1. Log into your Adaptive Insights account. 2. Go to **Model Management > Sheets**. 3. Locate the sheet you want to reproduce. 4. Click on **Account Groups**. 5. In the connector UI in Domo, select the accounts that are showing in Adaptive Insights. |
    | Roll Up Values? | Select whether you want to retrieve rolled up values. |
    | Date Filtering | Select whether you want to filter by dates. |
    | Duration | Select whether you want to pull data for a specific date or a date range. |
    | Report Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date | Select the date for the report. |
    | Days Back | Enter the number of past days that should appear in the report. |
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |
    | Select Specific End Date | Select the second date in your date range. |
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Data Version | Select the version of the data you want to retrieve data for. |
    | Currency | Select whether to retrieve data for US dollars, your local currency, or the currency used by your corporation. |
    | Data Sheet | Select the sheet you want to retrieve data for. |
    | Accounts | Select all of the accounts you want to retrieve data for. |
    | Dimension Name | Select the dimension you want to retrieve data for. |

CHANGE 13  [### Other Panes]  (line 621 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 14  [## FAQ]  (line 626 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 15  [## FAQ]  (line 627 in repo file)

  OLD (remove from repo):
    

CHANGE 16  [## FAQ]  (line 629 in repo file)

  OLD (remove from repo):
    

CHANGE 17  [## FAQ]  (line 633 in repo file)

  NEW (replace with / insert from CSV):
    No.
    </Accordion>

CHANGE 18  [## FAQ]  (lines 634–639 in repo file)

  OLD (remove from repo):
    No.
    
    </Accordion>
    </AccordionGroup>
    
    ## Troubleshooting for Consulting/Support

  NEW (replace with / insert from CSV):
    </AccordionGroup>## Troubleshooting for Consulting/Support

==============================================================================
FILE:   s/article/360042930314.mdx
TITLE:  Asana Connector
LANG:   English (en_US)
ID:     360042930314
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Asana is a cloud-based project management platform that helps users track projects, tasks, stories, and other project-related workflows. To learn more about the Asana API, visit their page ([https://asana.com/developers/documen...arted/overview](https://asana.com/developers/documentation/getting-started/overview "https://asana.com/developers/documentation/getting-started/overview") ).

  NEW (replace with / insert from CSV):
    Asana is a cloud-based project management platform that helps users track projects, tasks, stories, and other project-related workflows. To learn more about the Asana API, visit their page ([https://asana.com/developers/documen...arted/overview](https://asana.com/developers/documentation/getting-started/overview)).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your Asana account in the Data Center. This topic discusses the fields and menus that are specific to the Asana connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    You connect to your Asana account in the Data Center. This topic discusses the fields and menus that are specific to the Asana connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Connecting to Your Asana Account]  (line 17 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Asana Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Asana Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [## Credentials Pane]  (line 21 in repo file)

  OLD (remove from repo):
    The Domo Asana connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Asana accounts in Domo) to open the Asana OAuth screen where you can enter your Asana or Google credentials. Once you have entered valid credentials, you can use the same account any time you go to create a new Asana DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054 "Managing User Accounts for Connectors").

  NEW (replace with / insert from CSV):
    The Domo Asana connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Asana accounts in Domo) to open the Asana OAuth screen where you can enter your Asana or Google credentials. Once you have entered valid credentials, you can use the same account any time you go to create a new Asana DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 5  [## Credentials Pane]  (lines 23–25 in repo file)

  OLD (remove from repo):
    <Note>
    **Note:** If you are already logged into Asana when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Asana.
    </Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into Asana when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Asana.</Note>

CHANGE 6  [## Credentials Pane]  (line 27 in repo file)

  OLD (remove from repo):
    You will also need an API Key for your Asana account. Any user can get their own API key by visiting the Account Settings dialog and clicking on the Apps tab. The API key is located at the bottom of this dialog box.

  NEW (replace with / insert from CSV):
    You will also need an API Key for your Asana account. Any user can get their own API key by visiting the Account Settings dialog and clicking on the Apps tab. The API key is located at the bottom of this dialog box.

CHANGE 7  [## Details Pane]  (line 31 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select. If you select the "Tasks" report, a "Subreport" menu appears from which you may select a secondary report.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select. If you select the "Tasks" report, a "Subreport" menu appears from which you may select a secondary report.

CHANGE 8  [## Details Pane]  (line 33 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1" data-aura-rendered-by="33:195;a"><thead><tr><th colspan="1" rowspan="1"><p> Menu </p></th><th colspan="1" rowspan="1"><p> Description </p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p> Report </p></td><td colspan="1" rowspan="1"><p> Select the Asana report you want to run. The following reports are available: </p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"> Attachments </td><td colspan="1" rowspan="1"> This report pulls your asana attachments for a specified workspace. </td></tr><tr><td colspan="1" rowspan="1"> Custom Fields </td><td colspan="1" rowspan="1"> This report pulls your asana custom fields for a specified workspace. </td></tr><tr><td colspan="1" rowspan="1"> Projects </td><td colspan="1" rowspan="1"> This report gets all projects in a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Stories </td><td colspan="1" rowspan="1"> This report gets all stories in a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Subtasks </td><td colspan="1" rowspan="1"> This report gets all subtasks in a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Subtasks For Team </td><td colspan="1" rowspan="1"> This report gets all subtasks for selected team in a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Tags </td><td colspan="1" rowspan="1"> This report gets all tags made on all tasks in a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Tasks </td><td colspan="1" rowspan="1"> This report gets all tasks in a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Teams </td><td colspan="1" rowspan="1"> This report gets all teams in a workspace. </td></tr><tr><td colspan="1" rowspan="1"><p> Time Tracking Entries </p></td><td colspan="1" rowspan="1"> This report returns time tracking entries for a task. </td></tr><tr><td colspan="1" rowspan="1"> Users </td><td colspan="1" rowspan="1"> This report gets all users on a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Workspaces </td><td colspan="1" rowspan="1"> This report gets all workspaces for a client. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Subreport </td><td colspan="1" rowspan="1"><p> The following subreports are available: </p><table border="1" cellpadding="1" cellspacing="1" class="mt-responsive-table"><tbody><tr><td class="mt-noheading" colspan="1" rowspan="1"><b> Subreport </b></td><td class="mt-noheading" colspan="1" rowspan="1"><b> Description </b></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"> Custom Fields </td><td class="mt-noheading" colspan="1" rowspan="1"> This report gets all tasks in a workspace enhanced with custom fields data. </td></tr><tr><td colspan="1" rowspan="1"> Custom Field Settings </td><td colspan="1" rowspan="1"> Returns array of Custom Field Settings data. </td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"> Followers </td><td class="mt-noheading" colspan="1" rowspan="1"> This report gets all tasks in a workspace enhanced with followers data. </td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"> Hearts </td><td class="mt-noheading" colspan="1" rowspan="1"> This report gets all tasks in a workspace enhanced with hearts data. </td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"> Memberships </td><td class="mt-noheading" colspan="1" rowspan="1"> This report gets all tasks in a workspace enhanced with memberships data. </td></tr><tr><td colspan="1" rowspan="1"> None </td><td colspan="1" rowspan="1"> This report gets all tasks in a workspace. </td></tr><tr><td colspan="1" rowspan="1"> Projects </td><td colspan="1" rowspan="1"> This report gets all tasks in a workspace enhanced with task projects. </td></tr><tr><td colspan="1" rowspan="1"> Tags </td><td colspan="1" rowspan="1"> This report gets all tasks in a workspace enhanced with tags data. </td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"> Action on 403 Error </td><td colspan="1" rowspan="1"> Select the action to perform when request to fetch subtasks for particular task id is failed with 403 (no access error). <b> Skip failed record and continue </b> will skip failed task and continue with remaining tasks. </td></tr><tr><td colspan="1" rowspan="1"> Workspace </td><td colspan="1" rowspan="1"> Select the workspace you want to retrieve data for. </td></tr><tr><td colspan="1" rowspan="1"> Tasks Completed Since (optional) </td><td colspan="1" rowspan="1"> Select the date to return tasks that are either incomplete or those which have been completed till this date. (<b> Note: </b> For reports <b> Subtasks </b> and <b> Subtaks For Team </b>, it will bring subtasks for the tasks which are incomplete or completed since this date.) </td></tr><tr><td colspan="1" rowspan="1"> Use Extended Run (checkbox) </td><td colspan="1" rowspan="1"> Select the checkbox if you have large amounts of data, which will take more than 24 hours to import. Connector runs that take more than 24 hours may be canceled. The Extended Run functionality allows the connector to import as much data as possible during each 24 hour block, and resume the data import without user intervention in the next available 24 hour block. </td></tr><tr><td colspan="1" rowspan="1"> Exclude Nested Subtasks (checkbox) </td><td colspan="1" rowspan="1"> Select the checkbox if you don't want to import subtasks that are nested under other subtasks. </td></tr><tr><td colspan="1" rowspan="1"> Action On 403 Error </td><td colspan="1" rowspan="1"> The connector will fail with an error message as <b> 403 - No Access </b>, if the user running the connector does not have access to the details of a task in the task list. You have the option where the connector can ignore these errors and continue, by selecting <b> Skip failed record and continue </b>. If you want the connector to fail when a 403 error occurs, the you can select the <b> Throw Exception </b> option. The default behavior is to <b> Throw Exception </b>. </td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Asana report you want to run. The following reports are available:   |  |  | | --- | --- | | Attachments | This report pulls your Asana attachments for a specified workspace. | | Custom Fields | This report pulls your Asana custom fields for a specified workspace. | | Projects | This report gets all projects in a workspace. | | Stories | This report gets all stories in a workspace. | | Subtasks | This report gets all subtasks in a workspace. | | Subtasks For Team | This report gets all subtasks for the selected team in a workspace. | | Tags | This report gets all tags made on all tasks in a workspace. | | Tasks Per Section | This report retrieves all tasks within a workspace. Use this as an alternative to the task report when limit issues occur. | | Tasks | This report gets all tasks in a workspace. | | Teams | This report gets all teams in a workspace. | | Time Tracking Entries | This report returns time tracking entries for a task. | | Users | This report gets all users on a workspace. | | Workspaces | This report gets all workspaces for a client. | |
    | Subreport | The following subreports are available:   |  |  | | --- | --- | | **Subreport** | **Description** | | Custom Fields | This report gets all tasks in a workspace enhanced with custom fields data. | | Custom Field Settings | Returns array of Custom Field Settings data. | | Followers | This report gets all tasks in a workspace enhanced with followers data. | | Hearts | This report gets all tasks in a workspace enhanced with hearts data. | | Memberships | This report gets all tasks in a workspace enhanced with memberships data. | | None | This report gets all tasks in a workspace. | | Projects | This report gets all tasks in a workspace enhanced with task projects. | | Tags | This report gets all tasks in a workspace enhanced with tags data. | |
    | Action on 403 Error | Select the action to perform when request to fetch subtasks for particular task id is failed with 403 (no access error). **Skip failed record and continue** will skip failed task and continue with remaining tasks. |
    | Workspace | Select the workspace you want to retrieve data for. |
    | Tasks Completed Since (optional) | Select the date to return tasks that are either incomplete or those which have been completed till this date. (
    <Note>**Note:** For reports **Subtasks** and **Subtaks For Team**, it will bring subtasks for the tasks which are incomplete or completed since this date.) |
    | Use Extended Run (checkbox) | Select the checkbox if you have large amounts of data,  which will take more than 24 hours to import. Connector runs that take more than 24 hours may be canceled. The Extended Run functionality allows the connector to import as much data as possible during each 24 hour block, and resume the data import without user intervention in the next available 24 hour block. |
    | Exclude Nested Subtasks (checkbox) | Select the checkbox if you don't want to import subtasks that are nested under other subtasks. |
    | Action On 403 Error | The connector will fail with an error message as **403 - No Access**, if the user running the connector does not have access to the details of a task in the task list. You have the option where the connector can ignore these errors and continue, by selecting **Skip failed record and continue**. If you want the connector to fail when a 403 error occurs, the you can select the **Throw Exception** option. The default behavior is to **Throw Exception**. |</Note>

CHANGE 9  [## Other Panes]  (line 37 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274 "Adding a DataSet Using a Data Connector").

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 10  [## FAQ]  (line 40 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 11  [## FAQ]  (line 41 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 12  [## FAQ]  (line 44 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 13  [## FAQ]  (line 47 in repo file)

  NEW (replace with / insert from CSV):
    

==============================================================================
FILE:   s/article/360042930474.mdx
TITLE:  Smartsheet Connector
LANG:   English (en_US)
ID:     360042930474
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Smartsheet is an online project management, productivity, and team collaboration SaaS application. Domo's Smartsheet connector allows you to retrieve data about folders, sheets, workspaces, users, and so on. To learn more about the Smartsheet API, visit their website ([https://smartsheet-platform.github.io/api-docs/](https://smartsheet-platform.github.io/api-docs/)).

  NEW (replace with / insert from CSV):
    Smartsheet is an online project management, productivity, and team collaboration SaaS application. Domo's Smartsheet connector allows you to retrieve data about folders, sheets, workspaces, users, and so on. To learn more about the Smartsheet API, visit their website ( [https://smartsheet-platform.github.io/api-docs/](https://smartsheet-platform.github.io/api-docs/) ).

CHANGE 2  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    You connect to the Smartsheet in the Data Center. This topic discusses the fields and menus that are specific to the Smartsheet connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You connect to the Smartsheet in the Data Center. This topic discusses the fields and menus that are specific to the Smartsheet connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Intro]  (line 13 in repo file)

  OLD (remove from repo):
    <table class="mt-responsive-table"><tbody><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>This connector is typically used to pull spreadsheet data into Domo.</p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>N/A</p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>Product/project managers</p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>Depends on the amount of data and the scope of the system.</p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>4</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector is typically used to pull spreadsheet data into Domo. |
    | **Primary Metrics** | N/A |
    | **Primary Company Roles** | Product/project managers |
    | **Average Implementation Time** | Depends on the amount of data and the scope of the system. |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 4 |

CHANGE 4  [## Prerequisites]  (line 22 in repo file)

  OLD (remove from repo):
    To connect to the Smartsheet API and create a DataSet, you must have the following:

  NEW (replace with / insert from CSV):
    To connect to the Smartsheet API and create a DataSet, you must have the following:

CHANGE 5  [## Connecting to Smartsheet]  (line 29 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Smartsheet connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details**panes in the Smartsheet connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 6  [### Credentials Pane]  (line 33 in repo file)

  OLD (remove from repo):
    The Domo Smartsheet connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Smartsheet accounts in Domo) to open the Smartsheet OAuth screen where you can enter your Smartsheet email address and password. Once you have entered valid credentials, you can use the same account any time you go to create a new Smartsheet DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    The Domo Smartsheet connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Smartsheet accounts in Domo) to open the Smartsheet OAuth screen where you can enter your Smartsheet email address and password. Once you have entered valid credentials, you can use the same account any time you go to create a new Smartsheet DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 7  [### Details Pane]  (line 41 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Menu</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>Report</p></td><td colspan="1" rowspan="1"><p>Select a Smartsheet report. The following reports are available:</p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p>Columns - Columns in Sheet</p></td><td colspan="1" rowspan="1"><p>Returns the data about columns within a specified sheet. You must select a sheet name in <b>Sheet Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Folders - All Folders (Deprecated)</p></td><td colspan="1" rowspan="1"><p>Returns a list of all folders in your account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Folders - Folders in All Workspaces</p></td><td colspan="1" rowspan="1"><p>Returns a list of folders from all workspaces in your account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Folders - Single Folder (Deprecated)</p></td><td colspan="1" rowspan="1"><p>Returns data about a specified folder. You must select a folder name in <b>Folder Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Folders - Single Folder V2</p></td><td colspan="1" rowspan="1"><p>Returns data about a specified folder. You must select a folder name in <b>Folder Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Folders - All Folders Inside a Folder</p></td><td colspan="1" rowspan="1"><p>Returns a list of subfolders within a specified folder. You must select a folder name in <b>Folder Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Reports - Single Report</p></td><td colspan="1" rowspan="1"><p>Returns data from a specified report. You must select a report name in <b>Report Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Reports - All Reports</p></td><td colspan="1" rowspan="1"><p>Returns a list of all reports used by account users.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Search - All</p></td><td colspan="1" rowspan="1"><p>Searches for a specified search term within your entire account and returns results with that search term. You must enter a search term in <b>Search</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Search - In Sheet</p></td><td colspan="1" rowspan="1"><p>Searches for a specified search term within a specified sheet and returns results with that search term. You must select a sheet name in <b>Sheet Name</b> and a search term in <b>Search</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Shares - Shares for Workspace</p></td><td colspan="1" rowspan="1"><p>Returns a list of users who have access to a specified workspace. You must select a workspace name in <b>Workspace Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Shares - Shares for Sheet</p></td><td colspan="1" rowspan="1"><p>Returns a list of users who have access to a specified sheet. You must select a sheet name in <b>Sheet Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Sheets - All Sheets</p></td><td colspan="1" rowspan="1"><p>Returns a list of all sheets in your account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Sheets - Single Sheet</p></td><td colspan="1" rowspan="1"><p>Returns data from a specified sheet. You must select a sheet name in <b>Sheet Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Sheets - Sheet Version</p></td><td colspan="1" rowspan="1"><p>Returns the version number for a specified sheet. You must select a sheet name in <b>Sheet Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Sheets - Publish Status</p></td><td colspan="1" rowspan="1"><p>Returns publish status information for a specified sheet. You must select a sheet name in <b>Sheet Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Templates - All Templates (Deprecated)</p></td><td colspan="1" rowspan="1"><p>Returns a list of all templates in your account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Users - All Users</p></td><td colspan="1" rowspan="1"><p>Returns data about all users of the account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Users - Current User</p></td><td colspan="1" rowspan="1"><p>Returns data about the authenticated user.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Users - All Sheets</p></td><td colspan="1" rowspan="1"><p>Returns a list of all sheets used by account users.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Workspaces - All Workspaces</p></td><td colspan="1" rowspan="1"><p>Returns a list of all workspaces in your account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Workspaces - Single Workspace (Deprecated)</p></td><td colspan="1" rowspan="1"><p>Returns data about a specified workspace. You must select a workspace name in <b>Workspace Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Workspaces - Single Workspace V2</p></td><td colspan="1" rowspan="1"><p>Returns data about a specified workspace. You must select a workspace name in <b>Workspace Name</b> for this report to run.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Workspaces - All Folders in a Workspace</p></td><td colspan="1" rowspan="1"><p>Returns a list of folders within a specified workspace. You must select a workspace name in <b>Workspace Name</b> for this report to run.</p></td></tr></tbody></table><p></p></td></tr><tr><td colspan="1" rowspan="1"><p>Workspace/Folder/Sheet Name</p></td><td colspan="1" rowspan="1"><p>Enter the name of the workspace, folder, or sheet you want to retrieve information for. Many reports do not work unless a name is specified.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Search</p></td><td colspan="1" rowspan="1"><p>Enter the search term you want to retrieve data for. Some reports do not work unless a search term is specified.</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select a Smartsheet report. The following reports are available:   | Report Name | Description | | --- | --- | | Columns - Columns in Sheet | Returns the data about columns within a specified sheet. You must select a sheet name in **Sheet Name** for this report to run. | | Folders - All Folders (Deprecated) | Returns a list of all folders in your account. | | Folders - Folders in All Workspaces | Returns a list of folders from all workspaces in your account. | | Folders - Single Folder (Deprecated) | Returns data about a specified folder. You must select a folder name in **Folder Name** for this report to run. | | Folders - Single Folder V2 | Returns data about a specified folder. You must select a folder name in the **Folder Name** for this report to run. | | Folders - All Folders Inside a Folder | Returns a list of subfolders within a specified folder. You must select a folder name in **Folder Name** for this report to run. | | Reports - Single Report | Returns data from a specified report. You must select a report name in **Report Name** for this report to run. | | Reports - All Reports | Returns a list of all reports used by account users. | | Search - All | Searches for a specified search term within your entire account and returns results with that search term. You must enter a search term in **Search** for this report to run. | | Search - In Sheet | Searches for a specified search term within a specified sheet and returns results with that search term. You must select a sheet name in **Sheet Name** and a search term in **Search** for this report to run. | | Shares - Shares for Workspace | Returns a list of users who have access to a specified workspace. You must select a workspace name in **Workspace Name** for this report to run. | | Shares - Shares for Sheet | Returns a list of users who have access to a specified sheet. You must select a sheet name in **Sheet Name** for this report to run. | | Sheets - All Sheets | Returns a list of all sheets in your account. | | Sheets - Single Sheet | Returns data from a specified sheet. You must select a sheet name in **Sheet Name** for this report to run. | | Sheets - Sheet Version | Returns the version number for a specified sheet. You must select a sheet name in **Sheet Name** for this report to run. | | Sheets - Publish Status | Returns publish status information for a specified sheet. You must select a sheet name in **Sheet Name** for this report to run. | | Templates - All Templates (Deprecated) | Returns a list of all templates in your account. | | Users - All Users | Returns data about all users of the account. | | Users - Current User | Returns data about the authenticated user. | | Users - All Sheets | Returns a list of all sheets used by account users. | | Workspaces - All Workspaces | Returns a list of all workspaces in your account. | | Workspaces - Single Workspace (Deprecated) | Returns data about a specified workspace. You must select a workspace name in **Workspace Name** for this report to run. | | Workspaces - Single Workspace V2 | Returns data about a specified workspace. You must select a workspace name in **Workspace Name** for this report to run. | | Workspaces - All Folders in a Workspace | Returns a list of folders within a specified workspace. You must select a workspace name in **Workspace Name** for this report to run. | |
    | Workspace/Folder/Sheet Name | Enter the name of the workspace, folder, or sheet you want to retrieve information for. Many reports do not work unless a name is specified. |
    | Search | Enter the search term you want to retrieve data for. Some reports do not work unless a search term is specified. |

CHANGE 8  [### FAQ]  (lines 45–46 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="What version of the Smartsheet API does this connector use?">

  NEW (replace with / insert from CSV):
    #### What version of the Smartsheet API does this connector use?

CHANGE 9  [### FAQ]  (lines 49–50 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="Which endpoint(s) does each report call in this connector?">

CHANGE 10  [### FAQ]  (lines 52–54 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table><tbody><tr><th colspan="1" rowspan="1">Report Name</th><th colspan="1" rowspan="1">Endpoint URL</th></tr><tr><td colspan="1" rowspan="1">Columns - Columns in Sheet</td><td colspan="1" rowspan="1">/sheets/{sheetId}/columns</td></tr><tr><td colspan="1" rowspan="1">Folders - All Folders (Deprecated)</td><td colspan="1" rowspan="1">/home/folders</td></tr><tr><td colspan="1" rowspan="1">Folders - Folders in All Workspaces</td><td colspan="1" rowspan="1">/workspaces//{workspaceId}/children</td></tr><tr><td colspan="1" rowspan="1">Folders - Single Folder (Deprecated)</td><td colspan="1" rowspan="1">/folders/{folderId}</td></tr><tr><td colspan="1" rowspan="1">Folders - Single Folder V2</td><td colspan="1" rowspan="1">/folders/{folderId}/children</td></tr><tr><td colspan="1" rowspan="1">Folders - All Folders Inside a Folder</td><td colspan="1" rowspan="1">/folders/{folderId}/children</td></tr><tr><td colspan="1" rowspan="1">Reports - Single Report</td><td colspan="1" rowspan="1">/reports/{reportId}</td></tr><tr><td colspan="1" rowspan="1">Reports - All Reports</td><td colspan="1" rowspan="1">/reports/</td></tr><tr><td colspan="1" rowspan="1">Search - All</td><td colspan="1" rowspan="1">/search</td></tr><tr><td colspan="1" rowspan="1">Search - In Sheet</td><td colspan="1" rowspan="1">/search/sheets/</td></tr><tr><td colspan="1" rowspan="1">Shares - Shares for Workspace</td><td colspan="1" rowspan="1">/shares</td></tr><tr><td colspan="1" rowspan="1">Shares - Shares for Sheet</td><td colspan="1" rowspan="1">/shares</td></tr><tr><td colspan="1" rowspan="1">Sheets - All Sheets</td><td colspan="1" rowspan="1">/sheets</td></tr><tr><td colspan="1" rowspan="1">Sheets - Single Sheet</td><td colspan="1" rowspan="1">/sheets/</td></tr><tr><td colspan="1" rowspan="1">Sheets - Sheet Version</td><td colspan="1" rowspan="1">/sheets/{sheetId}/version</td></tr><tr><td colspan="1" rowspan="1">Sheets - Publish Status</td><td colspan="1" rowspan="1">/sheets/{sheetId}/publish</td></tr><tr><td colspan="1" rowspan="1">Templates - All Templates (Deprecated)</td><td colspan="1" rowspan="1">/templates</td></tr><tr><td colspan="1" rowspan="1">Users - All Users</td><td colspan="1" rowspan="1">/users</td></tr><tr><td colspan="1" rowspan="1">Users - Current User</td><td colspan="1" rowspan="1">/users/me</td></tr><tr><td colspan="1" rowspan="1">Users - All Sheets</td><td colspan="1" rowspan="1">/users/sheets</td></tr><tr><td colspan="1" rowspan="1">Workspaces - All Workspaces</td><td colspan="1" rowspan="1">/workspaces</td></tr><tr><td colspan="1" rowspan="1">Workspaces - Single Workspace (Deprecated)</td><td colspan="1" rowspan="1">/workspaces/{workspaceId}</td></tr><tr><td colspan="1" rowspan="1">Workspaces - Single Workspace V2</td><td colspan="1" rowspan="1">/workspaces/{workspaceId}/children</td></tr><tr><td colspan="1" rowspan="1">Workspaces - All Folders in a Workspace</td><td colspan="1" rowspan="1">/workspaces/{workspaceId}/children</td></tr></tbody></table>
    </Accordion>
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### Which endpoint(s) does each report call in this connector?
    
    | Report | EndPoint URL |
    | --- | --- |
    | Columns - Columns in Sheet | /sheets/{sheetId}/columns |
    | Folders - All Folders (Deprecated) | /home/folders |
    | Folders - Folders in All Workspaces | /workspaces//{workspaceId}/children |
    | Folders - Single Folder (Deprecated) | /folders/{folderId} |
    | Folders - Single Folder V2 | /folders/{folderId}/children |
    | Folders - All Folders Inside a Folder | /folders/{folderId}/children |
    | Reports - Single Report | /reports/{reportId} |
    | Reports - All Reports | /reports/ |
    | Search - All | /search |
    | Search - In Sheet | /search/sheets/ |
    | Shares - Shares for Workspace | /shares |
    | Shares - Shares for Sheet | /shares |
    | Sheets - All Sheets | /sheets |
    | Sheets - Single Sheet | /sheets/ |
    | Sheets - Sheet Version | /sheets/{sheetId}/version |
    | Sheets - Publish Status | /sheets/{sheetId}/publish |
    | Templates - All Templates (Deprecated) | /templates |
    | Users - All Users | /users |
    | Users - Current User | /users/me |
    | Users - All Sheets | /users/sheets |
    | Workspaces - All Workspaces | /workspaces |
    | Workspaces - Single Workspace (Deprecated) | /workspaces/{workspaceId} |
    | Workspaces - Single Workspace V2 | /workspaces/{workspaceId}/children |
    | Workspaces - All Folders in a Workspace | /workspaces/{workspaceId}/children |
    
    #### What kind of credentials do I need to power up this connector?

CHANGE 11  [### FAQ]  (lines 57–58 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="How do I know my Smartsheet account credentials are secure?">

  NEW (replace with / insert from CSV):
    
    #### How do I know my Smartsheet account credentials are secure?

CHANGE 12  [### FAQ]  (lines 61–62 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="Do I need a certain kind of account with the data service to set up the connector?">

  NEW (replace with / insert from CSV):
    
    #### Do I need a certain kind of account with the data service to set up the connector?

CHANGE 13  [### FAQ]  (lines 65–66 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="How frequently should I update the connector?">

  NEW (replace with / insert from CSV):
    
    #### How frequently should I update the connector?

CHANGE 14  [### FAQ]  (lines 69–70 in repo file)

  OLD (remove from repo):
    </Accordion>
    <Accordion title="Can this connector pull custom fields?">

  NEW (replace with / insert from CSV):
    
    #### Can this connector pull custom fields?

CHANGE 15  [### FAQ]  (lines 73–74 in repo file)

  OLD (remove from repo):
    </Accordion>
    </AccordionGroup>

==============================================================================
FILE:   s/article/360042930914.mdx
TITLE:  OpenWeatherMap Connector
LANG:   English (en_US)
ID:     360042930914
==============================================================================

CHANGE 1  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your OpenWeatherMap account in the Data Center. This topic discusses the fields and menus that are specific to the OpenWeatherMap connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You connect to your OpenWeatherMap account in the Data Center. This topic discusses the fields and menus that are specific to the OpenWeatherMap connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    <table class="mt-responsive-table"><tbody><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>Common use cases are for...</p><ul><li>Retailers that need to see how weather impacts sales, and</li><li>Logistics companies that need to understand forecasts and historical weather.</li></ul><p>Basically this connector is great for any organization that relies on weather trends for attendance, product delivery, or outdoor work.</p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><ul><li><p>Temperature and precipitation forecasts</p></li><li><p>Historical weather patterns</p></li><li><p>Likelihood of weather events occurring</p></li><li><p>Possibility of extreme events.</p></li></ul></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><ul><li><p>Facilities</p></li><li><p>Event planning</p></li><li><p>Operations manager</p></li><li><p>Program manager</p></li><li><p>Sales teams</p></li></ul></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>Less than 1 hour</p></td></tr><tr><td class="mt-noheading" colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td><td class="mt-noheading" colspan="1" rowspan="1"><p>3</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | Common use cases are for...   - Retailers that need to see how weather impacts sales, and - Logistics companies that need to understand forecasts and historical weather.   Basically this connector is great for any organization that relies on weather trends for attendance, product delivery, or outdoor work. |
    | **Primary Metrics** | - Temperature and precipitation forecasts - Historical weather patterns - Likelihood of weather events occurring - Possibility of extreme events. |
    | **Primary Company Roles** | - Facilities - Event planning - Operations manager - Program manager - Sales teams |
    | **Average Implementation Time** | Less than 1 hour |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 3 |

CHANGE 3  [## Connecting to Your OpenWeatherMap Account]  (line 23 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the OpenWeatherMap Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the OpenWeatherMap Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (line 27 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your OpenWeatherMap account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your OpenWeatherMap account. The following table describes what is needed for each field:

CHANGE 5  [### Credentials Pane]  (line 29 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Field</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1">API Key</td><td colspan="1" rowspan="1">Enter your OpenWeatherMap API key. For information about obtaining a key, see "Prerequisites," above.</td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter your OpenWeatherMap API key. For information about obtaining a key, see "Prerequisites," above. |

CHANGE 6  [### Details Pane]  (line 35 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 7  [### Details Pane]  (line 37 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Menu</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>Report</p></td><td colspan="1" rowspan="1"><p>Select the OpenWeatherMap report you want to run. The following reports are available:</p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1">Current Weather</td><td colspan="1" rowspan="1">Returns current weather data for a single location, using city ID, city name, geographic coordinates, or ZIP code.</td></tr><tr><td colspan="1" rowspan="1">Current Weather for Several Cities</td><td colspan="1" rowspan="1">Returns current weather data for multiple city IDs.</td></tr><tr><td colspan="1" rowspan="1">Five Day Forecast at Three-Hour Intervals</td><td colspan="1" rowspan="1">Returns a 5-day weather forecast, with 3-hour intervals, for a single location using the city ID, city name, or geographic coordinates.</td></tr><tr><td colspan="1" rowspan="1">Historical Data</td><td colspan="1" rowspan="1">Returns historical weather data for a single location using the city ID, city name, or geographic coordinates. This feature requires a separate paid plan. For more information, see <a href="https://openweathermap.org/price#history">OpenWeatherMap Subscription Pricing</a>.</td></tr><tr><td colspan="1" rowspan="1">Sixteen Day Daily Forecast</td><td colspan="1" rowspan="1">Returns a 16-day weather forecast with daily average parameters for a single location (identified by city ID, city name, or geographic coordinates).</td></tr><tr><td colspan="1" rowspan="1">Current Weather V3</td><td colspan="1" rowspan="1">Returns current weather data for multiple locations based on geographic coordinates.</td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1">Choose Location By</td><td colspan="1" rowspan="1">Select whether you want the location for your report to be based on city ID, city name, latitude and longitude, or zip code. For the most accurate results, choose <b>City ID</b>.</td></tr><tr><td colspan="1" rowspan="1">City ID</td><td colspan="1" rowspan="1">Enter the City ID. A list of City IDs can be downloaded from <a href="http://bulk.openweathermap.org/sample/">OpenWeatherMap</a>.</td></tr><tr><td colspan="1" rowspan="1">City Name</td><td colspan="1" rowspan="1">Enter the name of the city you want to retrieve data for.</td></tr><tr><td colspan="1" rowspan="1">Latitude</td><td colspan="1" rowspan="1">Enter the latitude of the region you want to retrieve data for. This can be a positive or negative number with up to 6 decimal places. Degree-minutes-seconds form is not accepted.</td></tr><tr><td colspan="1" rowspan="1">Longitude</td><td colspan="1" rowspan="1">Enter the longitude of the region you want to retrieve data for. This can be a positive or negative number with up to 6 decimal places. Degree-minutes-seconds form is not accepted.</td></tr><tr><td colspan="1" rowspan="1">Zip Code</td><td colspan="1" rowspan="1">Enter the ZIP code of the area you want to retrieve data for.</td></tr><tr><td colspan="1" rowspan="1">Units</td><td colspan="1" rowspan="1">Select whether to return temperature data in Kelvin, Celsius, or Fahrenheit units.</td></tr><tr><td colspan="1" rowspan="1">Search Accuracy</td><td colspan="1" rowspan="1"><p>Select whether to return the one closest match (<b>Accuracy</b>) or all similar values (<b>Like</b>).</p><p>For example, if this was set to <b>Accurate</b> and you searched for a city named "Cedar," your report would return data for "Cedar Rapids" (since this is the city determined by the server to be the best match). But if this parameter was set to <b>Like</b>, you would get results for "Cedar City," "Cedar Hills," as well as cities named "Cedar."</p></td></tr><tr><td colspan="1" rowspan="1">Multiple City IDs</td><td colspan="1" rowspan="1"><p>Enter a comma-separated list of City IDs. For example: <span class="mt-font-courier-new">524901,703448,2643743</span></p><p>A list of City IDs can be downloaded from <a href="http://bulk.openweathermap.org/sample/">OpenWeatherMap</a>.</p></td></tr><tr><td colspan="1" rowspan="1">Start Date</td><td colspan="1" rowspan="1">Select the start date for the range of data you want to pull.</td></tr><tr><td colspan="1" rowspan="1">End Date</td><td colspan="1" rowspan="1">Select the end date for the range of data you want to pull.</td></tr><tr><td colspan="1" rowspan="1">Choose Station By</td><td colspan="1" rowspan="1">Select whether to pull station data for a station ID, a geographic point (based on latitude and longitude), or a rectangular zone.</td></tr><tr><td colspan="1" rowspan="1">Station ID</td><td colspan="1" rowspan="1">Enter the ID of the station you want to retrieve data for. You can find station IDs by searching for weather stations using geographic coordinates and noting the IDs in the returned data.</td></tr><tr><td colspan="1" rowspan="1">Count</td><td colspan="1" rowspan="1">Enter the expected number of weather stations.</td></tr><tr><td colspan="1" rowspan="1">Bounding Box</td><td colspan="1" rowspan="1"><p>Enter the coordinates for the rectangular zone you want to retrieve data for. The format for this is: <span class="mt-font-courier-new">[longitude of top left point],[latitude of top left point],[longitude of bottom right point],[latitude of bottom right point],[map zoom level]</span>.</p><p>For example: <span class="mt-font-courier-new">8.87,49.07,65.21,61.26,6</span></p></td></tr><tr><td colspan="1" rowspan="1">Cluster</td><td colspan="1" rowspan="1">Select whether to use server clustering of points.</td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the OpenWeatherMap report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Current Weather | Returns current weather data for a single location, using city ID, city name, geographic coordinates, or ZIP code. | | Current Weather for Several Cities | Returns current weather data for multiple city IDs. | | Five Day Forecast at Three-Hour Intervals | Returns a 5-day weather forecast, with 3-hour intervals, for a single location using the city ID, city name, or geographic coordinates. | | Historical Data | Returns historical weather data for a single location using the city ID, city name, or geographic coordinates. This feature requires a separate paid plan. For more information, see [OpenWeatherMap Subscription Pricing](https://openweathermap.org/price#history). | | Sixteen Day Daily Forecast | Returns a 16-day weather forecast with daily average parameters for a single location (identified by city ID, city name, or geographic coordinates). | | Current Weather V3 | Returns current weather data for multiple locations based on geographic coordinates. | |
    | Choose Location By | Select whether you want the location for your report to be based on city ID, city name, latitude and longitude, or zip code. For the most accurate results, choose **City ID**. |
    | City ID | Enter the City ID. A list of City IDs can be downloaded from[OpenWeatherMap](http://bulk.openweathermap.org/sample/). |
    | City Name | Enter the name of the city you want to retrieve data for. |
    | Latitude | Enter the latitude of the region you want to retrieve data for. This can be a positive or negative number with up to 6 decimal places. Degree-minutes-seconds form is not accepted. |
    | Longitude | Enter the longitude of the region you want to retrieve data for. This can be a positive or negative number with up to 6 decimal places. Degree-minutes-seconds form is not accepted. |
    | Zip Code | Enter the ZIP code of the area you want to retrieve data for. |
    | Units | Select whether to return temperature data in Kelvin, Celsius, or Fahrenheit units. |
    | Search Accuracy | Select whether to return the one closest match (**Accuracy**) or all similar values (**Like**).  For example, if this was set to **Accurate** and you searched for a city named "Cedar," your report would return data for "Cedar Rapids" (since this is the city determined by the server to be the best match). But if this parameter was set to **Like**, you would get results for "Cedar City," "Cedar Hills," as well as cities named "Cedar." |
    | Multiple City IDs | Enter a comma-separated list of City IDs. For example: 524901,703448,2643743  A list of City IDs can be downloaded from [OpenWeatherMap](http://bulk.openweathermap.org/sample/). |
    | Start Date | Select the start date for the range of data you want to pull. |
    | End Date | Select the end date for the range of data you want to pull. |
    | Choose Station By | Select whether to pull station data for a station ID, a geographic point (based on latitude and longitude), or a rectangular zone. |
    | Station ID | Enter the ID of the station you want to retrieve data for. You can find station IDs by searching for weather stations using geographic coordinates and noting the IDs in the returned data. |
    | Count | Enter the expected number of weather stations. |
    | Bounding Box | Enter the coordinates for the rectangular zone you want to retrieve data for. The format for this is: [longitude of top left point],[latitude of top left point],[longitude of bottom right point],[latitude of bottom right point],[map zoom level].  For example: 8.87,49.07,65.21,61.26,6 |
    | Cluster | Select whether to use server clustering of points. |

CHANGE 8  [### Other Panes]  (line 41 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 9  [## Troubleshooting]  (line 46 in repo file)

  OLD (remove from repo):
    - If using city IDs, make sure the IDs are correct.

  NEW (replace with / insert from CSV):
    - If using city IDs, make sure the IDs are correct.

==============================================================================
FILE:   s/article/360042931294.mdx
TITLE:  MINDBODY Online Connector (DEPRECATED)
LANG:   English (en_US)
ID:     360042931294
==============================================================================

CHANGE 1  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    MINDBODY's provides management software for wellness services and fitness organizations. Use the MINDBODY Online Connector to get data about classes, clients, payments and more. To learn more about the MINDBODY API, visit their page ([https://developers.mindbodyonline.com/](https://developers.mindbodyonline.com/)).

  NEW (replace with / insert from CSV):
    MINDBODY's provides management software for wellness services and fitness organizations. Use the MINDBODY Online Connector to get data about classes, clients, payments and more. To learn more about the MINDBODY API, visit their page ([https://developers.mindbodyonline.com/](https://developers.mindbodyonline.com/)).

CHANGE 2  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    You connect to your MINDBODY account in the Data Center. This topic discusses the fields and menus that are specific to the MINDBODY connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You connect to your MINDBODY account in the Data Center. This topic discusses the fields and menus that are specific to the MINDBODY connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (line 20 in repo file)

  OLD (remove from repo):
    You can find these items in the **Account** section of the MINDBODY API portal.

  NEW (replace with / insert from CSV):
    You can find these items in the **Account** section of the MINDBODY API portal.

CHANGE 4  [## Connecting to Your MINDBODY Account]  (line 24 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the MINDBODY Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the MINDBODY Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 5  [### Credentials Pane]  (line 28 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your MINDBODY account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your MINDBODY account. The following table describes what is needed for each field:

CHANGE 6  [### Credentials Pane]  (line 30 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Field</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1">Source Name</td><td colspan="1" rowspan="1">Enter your MINDBODY source name as found in the <b>Account</b> section of the MINDBODY API portal.</td></tr><tr><td colspan="1" rowspan="1">Source Key</td><td colspan="1" rowspan="1">Enter your MINDBODY source key as found in the <b>Account</b> section of the MINDBODY API portal.</td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Source Name | Enter your MINDBODY source name as found in the **Account** section of the MINDBODY API portal. |
    | Source Key | Enter your MINDBODY source key as found in the **Account** section of the MINDBODY API portal. |

CHANGE 7  [### Details Pane]  (line 36 in repo file)

  OLD (remove from repo):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

  NEW (replace with / insert from CSV):
    This pane contains a primary **Reports** menu, along with various other menus which may or may not appear depending on the report type you select.

CHANGE 8  [### Details Pane]  (line 38 in repo file)

  ⚠ LARGE CHANGE (34 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Menu</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>Report</p></td><td colspan="1" rowspan="1"><p>Select the MINDBODY report you want to run. The following reports are available:</p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p>Get Accepted Card Type</p></td><td colspan="1" rowspan="1"><p>Returns a list of card types that the site accepts.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Active Client Membership</p></td><td colspan="1" rowspan="1"><p>Returns the list of active memberships for a given client.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Active Session Times</p></td><td colspan="1" rowspan="1"><p>Returns a list of times that are active for a given program schedule type.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Appointment Options</p></td><td colspan="1" rowspan="1"><p>Returns a list of appointment options.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Bookable Items</p></td><td colspan="1" rowspan="1"><p>Returns a list of availabilities with the information needed to book appointments.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Classes</p></td><td colspan="1" rowspan="1"><p>Returns a list of classes.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Class Descriptions</p></td><td colspan="1" rowspan="1"><p>Returns a list of class descriptions.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Class Schedules</p></td><td colspan="1" rowspan="1"><p>Returns a list of class schedules.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Class Visits</p></td><td colspan="1" rowspan="1"><p>Returns the list of visits containing information for a specified class.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Account Balances</p></td><td colspan="1" rowspan="1"><p>Returns account balances for the given clients.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Contracts</p></td><td colspan="1" rowspan="1"><p>Returns a list of contracts for a given client.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Formula Notes</p></td><td colspan="1" rowspan="1"><p>Returns a list of formula notes for a specified client.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Indexes</p></td><td colspan="1" rowspan="1"><p>Returns a list of client indices where each index represents a multiple choice option a client may choose.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Purchases</p></td><td colspan="1" rowspan="1"><p>Returns a list of purchases of a specific client.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Referral Types</p></td><td colspan="1" rowspan="1"><p>Returns a list of referral types, which are options that new clients choose to identify how they know the business.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Clients</p></td><td colspan="1" rowspan="1"><p>Returns a list of clients.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Schedule</p></td><td colspan="1" rowspan="1"><p>Returns the schedule for a given client.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Services</p></td><td colspan="1" rowspan="1"><p>Returns the purchased services for a given client.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Client Visits</p></td><td colspan="1" rowspan="1"><p>Returns the list of visits for a given client.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Courses</p></td><td colspan="1" rowspan="1"><p>Returns a list of courses offered by the studio.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Custom Client Fields</p></td><td colspan="1" rowspan="1"><p>Returns customized client fields that consist of a name and data type.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Custom Payment Methods</p></td><td colspan="1" rowspan="1"><p>Returns the custom payment methods of a site.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Enrollments</p></td><td colspan="1" rowspan="1"><p>Returns a list of enrollments offered by the studio.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Function Data</p></td><td colspan="1" rowspan="1"><p>Returns a custom function report.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Locations</p></td><td colspan="1" rowspan="1"><p>Returns a list of locations within the specified sites.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Packages</p></td><td colspan="1" rowspan="1"><p>Returns a list of packages available for sale.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Products</p></td><td colspan="1" rowspan="1"><p>Returns a list of products available for sale.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Programs</p></td><td colspan="1" rowspan="1"><p>Returns a list of programs or service categories.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Relationships</p></td><td colspan="1" rowspan="1"><p>Returns a list of relationships.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Required Client Fields</p></td><td colspan="1" rowspan="1"><p>Returns the list of fields that a new client has to fill out in business mode, specifically for the sign-up process.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Resources</p></td><td colspan="1" rowspan="1"><p>Returns a list of resources or rooms.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Sales</p></td><td colspan="1" rowspan="1"><p>Returns a list of sales for reports.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Semesters</p></td><td colspan="1" rowspan="1"><p>Returns a list of semesters offered by the studio.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Services</p></td><td colspan="1" rowspan="1"><p>Returns a list of services available for sale.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Session Types</p></td><td colspan="1" rowspan="1"><p>Returns a list of session types.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Sites</p></td><td colspan="1" rowspan="1"><p>Returns a list of sites available to the authenticated user.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Staff</p></td><td colspan="1" rowspan="1"><p>Returns a list of staff members including information about them.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Get Staff Appointments</p></td><td colspan="1" rowspan="1"><p>Returns a list of appointments by staff member.</p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p>Sites Selection</p></td><td colspan="1" rowspan="1"><p>Select whether you want to pull data for all sites or specified sites.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Sites</p></td><td colspan="1" rowspan="1"><p>Check the boxes for all sites you want to pull data for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Client ID</p></td><td colspan="1" rowspan="1"><p>Enter the ID of the client you want to retrieve information for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Schedule Type</p></td><td colspan="1" rowspan="1"><p>Select the schedule type you want to retrieve information for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Programs</p></td><td colspan="1" rowspan="1"><p>Check the boxes for all programs you want to pull data for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Session Types</p></td><td colspan="1" rowspan="1"><p>Check the boxes for all session types you want to pull data for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Duration</p></td><td colspan="1" rowspan="1"><p>Select whether you want to pull data for a specific date or a date range.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Report Date</p></td><td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Select Specific Date</p></td><td colspan="1" rowspan="1"><p>Select the date for the report.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Days Back</p></td><td colspan="1" rowspan="1"><p>Enter the number of past days that should appear in the report.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Start Date</p></td><td colspan="1" rowspan="1"><p>Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b>End Date</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>End Date</p></td><td colspan="1" rowspan="1"><p>Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in <b>Start Date</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Select Specific Start Date</p></td><td colspan="1" rowspan="1"><p>Select the first date in your date range.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Select Specific End Date</p></td><td colspan="1" rowspan="1"><p>Select the second date in your date range.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Days Back to Start From</p></td><td colspan="1" rowspan="1"><p>Enter the number of the farthest day back that should be represented in the report. Combine with <b>Days Back to End At</b> to create a range of represented days.</p><p>For example, if you entered <span class="mt-font-courier-new">10</span> for <b>Days Back to Start From</b> and <span class="mt-font-courier-new">5</span> for <b>Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Days Back to End At</p></td><td colspan="1" rowspan="1"><p>Enter the number of the most recent day back that should be represented in the report. Combine with <b>Days Back to Start From</b> to create a range of represented days.</p><p>For example, if you entered <span class="mt-font-courier-new">10</span> for <b>Days Back to Start From</b> and <span class="mt-font-courier-new">5</span> for <b>Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Hide Cancelled Classes</p></td><td colspan="1" rowspan="1"><p>Select <b>True</b> if you do not want cancelled classes to appear in your report. To show all classes regardless of cancellation, select <b>False</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Scheduling Window</p></td><td colspan="1" rowspan="1"><p>Select <b>True</b> to show a scheduling window in your report; otherwise select <b>False</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Class ID</p></td><td colspan="1" rowspan="1"><p>Enter the ID of the class you want to retrieve information for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Choice of User Credentials</p></td><td colspan="1" rowspan="1"><p>Select a user credentials option. To use the credentials you entered to connect to MINDBODY in Domo, select the second option. To use a different set of credentials, select the first option.</p></td></tr><tr><td colspan="1" rowspan="1"><p>User's Username</p></td><td colspan="1" rowspan="1"><p>Enter the username of the user you want to retrieve information for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>User's Password</p></td><td colspan="1" rowspan="1"><p>Enter the password of the user you want to retrieve information for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Search Text (Optional)</p></td><td colspan="1" rowspan="1"><p>Enter a search term to filter by.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Show Active Only</p></td><td colspan="1" rowspan="1"><p>Select <b>True</b> if you want only active services to appear in your report. To show all services regardless of status, select <b>False</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Functions</p></td><td colspan="1" rowspan="1"><p>Select all functions you want to retrieve data for.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Number of Date Parameters</p></td><td colspan="1" rowspan="1"><p>Select the number of required date parameters.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Aggregation Period</p></td><td colspan="1" rowspan="1"><p>Select whether you want to aggregate data by day or retrieve data from the entire date range at once. The first option is recommended only if your function normally aggregates over the entire date range but you want a daily date grain. Otherwise select the second option.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Sell Online</p></td><td colspan="1" rowspan="1"><p>Select <b>True</b> if you want Sell Online packages to appear in your report; otherwise select <b>False</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Online Only</p></td><td colspan="1" rowspan="1"><p>Select <b>True</b> if you want to show only online programs in your report. To show all programs, select <b>False</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Staff Filters</p></td><td colspan="1" rowspan="1"><p>Enter a comma-separated list of filter keywords you want to filter your staff report by. For example: <span class="mt-font-courier-new">StaffViewable,AppointmentInstructor</span></p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the MINDBODY report you want to run. The following reports are available:   |  |  | | --- | --- | | Get Accepted Card Type | Returns a list of card types that the site accepts. | | Get Active Client Membership | Returns the list of active memberships for a given client. | | Get Active Session Times | Returns a list of times that are active for a given program schedule type. | | Get Appointment Options | Returns a list of appointment options. | | Get Bookable Items | Returns a list of availabilities with the information needed to book appointments. | | Get Classes | Returns a list of classes. | | Get Class Descriptions | Returns a list of class descriptions. | | Get Class Schedules | Returns a list of class schedules. | | Get Class Visits | Returns the list of visits containing information for a specified class. | | Get Client Account Balances | Returns account balances for the given clients. | | Get Client Contracts | Returns a list of contracts for a given client. | | Get Client Formula Notes | Returns a list of formula notes for a specified client. | | Get Client Indexes | Returns a list of client indices where each index represents a multiple choice option a client may choose. | | Get Client Purchases | Returns a list of purchases of a specific client. | | Get Client Referral Types | Returns a list of referral types, which are options that new clients choose to identify how they know the business. | | Get Clients | Returns a list of clients. | | Get Client Schedule | Returns the schedule for a given client. | | Get Client Services | Returns the purchased services for a given client. | | Get Client Visits | Returns the list of visits for a given client. | | Get Courses | Returns a list of courses offered by the studio. | | Get Custom Client Fields | Returns customized client fields that consist of a name and data type. | | Get Custom Payment Methods | Returns the custom payment methods of a site. | | Get Enrollments | Returns a list of enrollments offered by the studio. | | Get Function Data | Returns a custom function report. | | Get Locations | Returns a list of locations within the specified sites. | | Get Packages | Returns a list of packages available for sale. | | Get Products | Returns a list of products available for sale. | | Get Programs | Returns a list of programs or service categories. | | Get Relationships | Returns a list of relationships. | | Get Required Client Fields | Returns the list of fields that a new client has to fill out in business mode, specifically for the sign-up process. | | Get Resources | Returns a list of resources or rooms. | | Get Sales | Returns a list of sales for reports. | | Get Semesters | Returns a list of semesters offered by the studio. | | Get Services | Returns a list of services available for sale. | | Get Session Types | Returns a list of session types. | | Get Sites | Returns a list of sites available to the authenticated user. | | Get Staff | Returns a list of staff members including information about them. | | Get Staff Appointments | Returns a list of appointments by staff member. | |
    | Sites Selection | Select whether you want to pull data for all sites or specified sites. |
    | Sites | Check the boxes for all sites you want to pull data for. |
    | Client ID | Enter the ID of the client you want to retrieve information for. |
    | Schedule Type | Select the schedule type you want to retrieve information for. |
    | Programs | Check the boxes for all programs you want to pull data for. |
    | Session Types | Check the boxes for all session types you want to pull data for. |
    | Duration | Select whether you want to pull data for a specific date or a date range. |
    | Report Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date | Select the date for the report. |
    | Days Back | Enter the number of past days that should appear in the report. |
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |
    | Select Specific End Date | Select the second date in your date range. |
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Hide Cancelled Classes | Select **True** if you do not want cancelled classes to appear in your report. To show all classes regardless of cancellation, select **False**. |
    | Scheduling Window | Select **True** to show a scheduling window in your report; otherwise select **False**. |
    | Class ID | Enter the ID of the class you want to retrieve information for. |
    | Choice of User Credentials | Select a user credentials option. To use the credentials you entered to connect to MINDBODY in Domo, select the second option. To use a different set of credentials, select the first option. |
    | User's Username | Enter the username of the user you want to retrieve information for. |
    | User's Password | Enter the password of the user you want to retrieve information for. |
    | Search Text (Optional) | Enter a search term to filter by. |
    | Show Active Only | Select **True** if you want only active services to appear in your report. To show all services regardless of status, select **False**. |
    | Functions | Select all functions you want to retrieve data for. |
    | Number of Date Parameters | Select the number of required date parameters. |
    | Aggregation Period | Select whether you want to aggregate data by day or retrieve data from the entire date range at once. The first option is recommended only if your function normally aggregates over the entire date range but you want a daily date grain. Otherwise select the second option. |
    | Sell Online | Select **True** if you want Sell Online packages to appear in your report; otherwise select **False**. |
    | Online Only | Select **True** if you want to show only online programs in your report. To show all programs, select **False**. |
    | Staff Filters | Enter a comma-separated list of filter keywords you want to filter your staff report by. For example: StaffViewable,AppointmentInstructor |

CHANGE 9  [### Other Panes]  (line 42 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/360042931494.mdx
TITLE:  JSON Webhook Connector
LANG:   English (en_US)
ID:     360042931494
==============================================================================

CHANGE 1  [### Intro]  (line 7 in repo file)

  OLD (remove from repo):
    Domo's JSON Webhook connector allows you to connect to data sources that push JSON-formatted data, such as IFTTT, Github, and Fitbit. Using JSON Webhooks, you can get access to real-time data from weather devices, vending machines, wearables, and more. The Webhook link includes a long-living single use token so anyone with the link can publish data to your DataSet.

  NEW (replace with / insert from CSV):
    Domo's JSON Webhook connector allows you to connect to data sources that push JSON-formatted data, such as IFTTT, Github, and Fitbit. Using JSON Webhooks, you can get access to real-time data from weather devices, vending machines, wearables, and more. The Webhook link includes a long-living single use token so anyone with the link can publish data to your DataSet.

CHANGE 2  [### Intro]  (line 13 in repo file)

  NEW (replace with / insert from CSV):
    Navigate this article in the following topics:
    
    ---
    

CHANGE 3  [### Configure a JSON Webhook Connection]  (line 15 in repo file)

  OLD (remove from repo):
    This section explains how to set up a JSON Webhook connection in Domo and in the third-party application. After you have set up a successful connection, you can use the same account any time you go to create a new JSON Webhook DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    This section explains how to set up a JSON Webhook connection in Domo and in the third-party application. After you have set up a successful connection, you can use the same account any time you go to create a new JSON Webhook DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 4  [### Configure a JSON Webhook Connection]  (line 19 in repo file)

  OLD (remove from repo):
    1. In the navigation header, select **Appstore** to open the Appstore.

  NEW (replace with / insert from CSV):
    1. In the navigation header, select **Appstore**to open the Appstore.

CHANGE 5  [### Configure a JSON Webhook Connection]  (line 21 in repo file)

  OLD (remove from repo):
    3. Select **Get the Data**.

  NEW (replace with / insert from CSV):
    3. Select **Get the Data**.
    

CHANGE 6  [### Configure a JSON Webhook Connection]  (line 24 in repo file)

  OLD (remove from repo):
    

CHANGE 7  [### Configure a JSON Webhook Connection]  (lines 26–28 in repo file)

  OLD (remove from repo):
    5. Paste the link you just copied into the appropriate location in your third-party application.
    
       The following screenshot comes from Github:

  NEW (replace with / insert from CSV):
    5. Paste the link you just copied into the appropriate location in your third-party application.
    
       The following screenshot comes from Github:
    

CHANGE 8  [### Configure a JSON Webhook Connection]  (lines 32–33 in repo file)

  OLD (remove from repo):
       This location will *usually* be a field with a name like "Payload URL," like in the above screenshot. However, there are exceptions. Please refer to the third-party application's documentation if you are not sure.
    

  NEW (replace with / insert from CSV):
    
    
       This location will *usually* be a field with a name like "Payload URL," like in the above screenshot. However, there are exceptions. Please refer to the third-party application's documentation if you are not sure.
    

CHANGE 9  [### Configure a JSON Webhook Connection]  (line 35 in repo file)

  OLD (remove from repo):
    6. Also in the third-party application, be sure to set the content type to "application/json."

  NEW (replace with / insert from CSV):
    6. Also in the third-party application, be sure to set the content type to "application/json."
    

CHANGE 10  [### Configure a JSON Webhook Connection]  (line 38 in repo file)

  OLD (remove from repo):
    

CHANGE 11  [### Configure a JSON Webhook Connection]  (lines 41–42 in repo file)

  OLD (remove from repo):
    9. (Optional) Enter a **Webhook Secret** if desired.
    

  NEW (replace with / insert from CSV):
    9. (Optional) Enter a **Webhook Secret** if desired.
    

CHANGE 12  [### Configure a JSON Webhook Connection]  (lines 44–45 in repo file)

  OLD (remove from repo):
    10. (Optional) If you want to include metadata in the DataSet, such as IP and timestamp, select **Yes** in the last menu in the **Details** view; otherwise leave this set to **No**.
    11. (Optional) If you want to use "Replace" instead of "Append" when the DataSet is updated, click **Change update settings**, select **Replace** in the menu, then click **Save**.

  NEW (replace with / insert from CSV):
    10. (Optional) If you want to include metadata in the DataSet, such as IP and timestamp, select **Yes** in the last menu in the **Details**view; otherwise leave this set to **No**.
    11. (Optional) If you want to use "Replace" instead of "Append" when the DataSet is updated, click **Change update settings**, select **Replace** in the menu, then click **Save**.

CHANGE 13  [### Details Pane]  (line 56 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Menu</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>Webhook URL</p></td><td colspan="1" rowspan="1"><p>Copy the Webhook URL created from the above steps.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Webhook Secret</p></td><td colspan="1" rowspan="1"><p>If your data provider supports secrets in the X-Hub signature, use sha1=(sha1 hashed payload, using secret as key). Create your own secret and enter it here.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Webhook Signature Header</p></td><td colspan="1" rowspan="1"><p>Enter your webhook secret header. By default, <b>X-Hub Signature</b> is used.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Include Metadata about the Request, including the IP and Timestamp</p></td><td colspan="1" rowspan="1"><p>Select the appropriate option to include or exclude Metadata about the Request including the IP and Timestamp.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Enable Flattening</p></td><td colspan="1" rowspan="1"><p>When you select this checkbox, the <b>Flattening Type</b> drop-down appears.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Flattening Type</p></td><td colspan="1" rowspan="1"><p>This drop-down appears when you select the <b>Enable Flattening</b> checkbox. You have three options to select from:</p><table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Drop-down option</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>(Default) Column Wise Flattening</p></td><td colspan="1" rowspan="1"><p>This option performs a <b>cross product</b> to flatten nested lists into multiple rows. It is the default method when flattening is enabled without a specific option selected.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Column Wise Stream Flattening</p></td><td colspan="1" rowspan="1"><p>Performs a <b>cross product</b> to flatten nested lists into multiple rows. While similar to Column Wise Flattening, this option is characterized by lower memory consumption and slower execution.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Row Wise Flattening</p></td><td colspan="1" rowspan="1"><p>This method flattens lists into multiple columns. It uses less memory than Column Wise Flattening, but results in a greater number of output columns.</p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p>Lists to Exclude from Flattening</p></td><td colspan="1" rowspan="1"><p>This text box appears when you select the <b>Column Wise Stream Flattening</b> option from the <b>Flattening Type</b> drop-down. Enter the field names (comma-separated) you want to exclude from flattening.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Update Method</p></td><td colspan="1" rowspan="1"><p>When Domo retrieves your data from the source, you can determine how that new data is imported into the current DataSet. Learn more about <a href="/s/article/360043430733">update methods</a>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Reset Schema</p></td><td colspan="1" rowspan="1"><p>Select this checkbox to reset the schema according to the current data.</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Webhook URL | Copy the Webhook URL created from the above steps. |
    | Webhook Secret | If your data provider supports secrets in the X-Hub signature, use sha1=(sha1 hashed payload, using secret as key). Create your own secret and enter it here. |
    | Webhook Signature Header | Enter your webhook secret header. By default, **X-Hub Signature** is used. |
    | Include Metadata about the Request, including the IP and Timestamp | Select the appropriate option to include or exclude Metadata about the Request including the IP and Timestamp. |
    | Enable Flattening | When you select this checkbox, the **Flattening Type** drop-down appears. |
    | Flattening Type | This drop-down appears when you select the **Enable Flattening** checkbox.  You have three options to select from:     |  |  | | --- | --- | | **Drop-down option** | **Description** | | (Default) Column Wise Flattening | This option performs a **cross product** to flatten nested lists into multiple rows. It is the default method when flattening is enabled without a specific option selected. | | Column Wise Stream Flattening | Performs a **cross product** to flatten nested lists into multiple rows. While similar to Column Wise Flattening, this option is characterized bylower memory consumption and slower execution. | | Row Wise Flattening | This method flattens lists into multiple columns. It uses less memory than Column Wise Flattening, but results in a greater number of output columns. | |
    | Lists to Exclude from Flattening | This text box appears when you select the **Column Wise Stream Flattening** option from the **Flattening Type** drop-down.  Enter the field names (comma-separated) you want to exclude from flattening. |
    | Update Method | When Domo retrieves your data from the source, you can determine how that new data is imported into the current DataSet.  Learn more about [update methods](/s/article/360043430733). |
    | Reset Schema | Select this checkbox to reset the schema according to the current data. |

CHANGE 14  [## FAQ]  (line 103 in repo file)

  OLD (remove from repo):
    We will return 200 "OK" response after we store the JSON file. If they get anything other than that, it indicates that we were not able to store the file. On the connector side, you will see an execution error if it runs into problems ingesting the data. During the next execution, it will attempt to process all the records since the last connector run.

  NEW (replace with / insert from CSV):
    We will return 200 “OK” response after we store the JSON file.  If they get anything other than that, it indicates that we were not able to store the file.  On the connector side, you will see an execution error if it runs into problems ingesting the data.  During the next execution, it will attempt to process all the records since the last connector run.

CHANGE 15  [## FAQ]  (line 107 in repo file)

  OLD (remove from repo):
    Ideally, we would want to consume all the data during each connector execution, to prevent the queue from getting backed up with no chance of getting caught up. As mentioned, we can tweak the connector/dataset for packet size, rate, and scheduling to make sure that we are consistently pulling everything in the queue.

  NEW (replace with / insert from CSV):
    Ideally, we would want to consume all the data during each connector execution, to prevent the queue from getting backed up with no chance of getting caught up.  As mentioned, we can tweak the connector/dataset for packet size, rate, and scheduling to make sure that we are consistently pulling everything in the queue.

==============================================================================
FILE:   s/article/360042931734.mdx
TITLE:  MongoDB Connector
LANG:   English (en_US)
ID:     360042931734
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    MongoDB is an open-source document-oriented database system that stores structured data as JSON-like documents with dynamic schemas. Domo provides connectors that allow you to pull data from a MongoDB collection and compile custom reports.

  NEW (replace with / insert from CSV):
    MongoDB is an open-source document-oriented database system that stores structured data as JSON-like documents with dynamic schemas. Domo provides connectors that allow you to pull data from a MongoDB collection and compile custom reports.

CHANGE 2  [## Intro]  (lines 9–12 in repo file)

  OLD (remove from repo):
    - The MongoDB connector uses Java driver version 3.2.1, and supports Find and Aggregate query methods. You need to enter the Collection name manually in the MongoDB connector.
    - The MongoDB Advanced connector uses Java driver version 3.2.1, and supports a plain JSON query. You need to enter the Collection name manually in the MongoDB Advanced connector.
    - The MongoDB V2 connector uses Java driver version 3.12.1, and supports Find and Aggregate query methods. You can either select the Collection name from the drop down or enter it manually in the MongoDB V2 connector. Also, MongoDB V2 connector doesn't require a CA certificate.
    - The MongoDB SSH Connector uses Java driver version 3.12.1, and supports Find and Aggregate query methods. You can either select the Collection name from the drop down or enter it manually in the MongoDB SSH connector. You need your SSH credentials (host, port, username, and password) to pull data from a MongoDB collection through an SSH tunnel.

  NEW (replace with / insert from CSV):
    - The MongoDB connector uses Java driver version 3.2.1, and supports Find and Aggregate query methods. You need to enter the Collection name manually in the MongoDB connector.
    - The MongoDB Advanced connector uses Java driver version 3.2.1, and supports a plain JSON query. You need to enter the Collection name manually in the MongoDB Advanced connector.
    - The MongoDB V2 connector uses Java driver version 3.12.1, and supports Find and Aggregate query methods. You can either select the Collection name from the drop down or enter it manually in the MongoDB V2 connector. Also, MongoDB V2 connector doesn’t require a CA certificate.
    - The MongoDB SSH Connector uses Java driver version 3.12.1, and supports Find and Aggregate query methods. You can either select the Collection name from the drop down or enter it manually in the MongoDB SSH connector. You need your SSH credentials (host, port, username, and password) to pull data from a MongoDB collection through an SSH tunnel.

CHANGE 3  [## Intro]  (line 14 in repo file)

  OLD (remove from repo):
    This article discusses the **MongoDB Connector**. You can use Domo's MongoDB Connector to pull data from a MongoDB collection and compile custom reports. You indicate the data you want by inputting JSON queries. For more information about the MongoDB API, go to [http://api.mongodb.org/](http://api.mongodb.org/).

  NEW (replace with / insert from CSV):
    This article discusses the **MongoDB Connector**. You can use Domo's MongoDB Connector to pull data from a MongoDB collection and compile custom reports. You indicate the data you want by inputting JSON queries. For more information about the MongoDB API, go to [http://api.mongodb.org/](http://api.mongodb.org/).

CHANGE 4  [## Intro]  (line 18 in repo file)

  OLD (remove from repo):
    This topic discusses the fields and menus that are specific to the MongoDB connector user interface. For general information about adding DataSets, setting update schedules, and editing DataSet information, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This topic discusses the fields and menus that are specific to the MongoDB connector user interface. For general information about adding DataSets, setting update schedules, and editing DataSet information, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 5  [## Prerequisites]  (line 24 in repo file)

  OLD (remove from repo):
    To connect to a MongoDB collection and create a DataSet, you must have the following:

  NEW (replace with / insert from CSV):
    To connect to a MongoDB collection and create a DataSet, you must have the following:

CHANGE 6  [## Prerequisites]  (lines 26–27 in repo file)

  OLD (remove from repo):
    - The username and password you use to log into your MongoDB database
    - The host name for the database server (e.g. db.company.com).

  NEW (replace with / insert from CSV):
    - The username and password you use to log into your MongoDB database
    - The host name for the database server (e.g. db.company.com).

CHANGE 7  [## Prerequisites]  (line 30 in repo file)

  OLD (remove from repo):
    - The CA certificate text or URL path is required *only* if you select **Certificate String** or **URL Path**, respectively, in the **Certificate Format** menu.

  NEW (replace with / insert from CSV):
    - The CA certificate text or URL path is required *only* if you select **Certificate String** or **URL Path**, respectively, in the **Certificate Format** menu.

CHANGE 8  [## Prerequisites]  (line 35 in repo file)

  OLD (remove from repo):
    Before you can connect to a MongoDB database, you must also whitelist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses for Connectors](/s/article/360043630093).

  NEW (replace with / insert from CSV):
    Before you can connect to a MongoDB database, you must also allowlist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses for Connectors](/s/article/360043630093).

CHANGE 9  [## Prerequisites]  (line 37 in repo file)

  OLD (remove from repo):
    ## Connecting to a MongoDB Collection

  NEW (replace with / insert from CSV):
    ## Connecting to a MongoDB Collection

CHANGE 10  [## Connecting to a MongoDB Collection]  (line 39 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the MongoDB Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the MongoDB Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 11  [### Credentials Pane]  (line 45 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Field</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>Host</p></td><td colspan="1" rowspan="1"><p>Enter the host name for the MongoDB database. For example: <span class="mt-font-courier-new">db.company.com</span></p></td></tr><tr><td colspan="1" rowspan="1"><p>Database Name</p></td><td colspan="1" rowspan="1"><p>Enter the name of the MongoDB authentication database. This needs to be the specific database the user was created in.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Username</p></td><td colspan="1" rowspan="1"><p>Enter your MongoDB username.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Password</p></td><td colspan="1" rowspan="1"><p>Enter your MongoDB password.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Database Port</p></td><td colspan="1" rowspan="1"><p>Enter the port number for the MongoDB database.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Certificate Format</p></td><td colspan="1" rowspan="1"><p>Select the certificate format. If you do not want to include a certificate, select <b>No Certificate</b>. If you select <b>Certificate String</b>, you must paste the text for your certificate in the <b>Certificate</b> field. If you select <b>URL Path</b>, you must enter the URL where your certificate is located in the <b>Certificate</b> field.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Certificate</p></td><td colspan="1" rowspan="1"><p>Paste the text for your CA certificate or enter the URL where your certificate is located. This is optional. If you do not want to include a certificate, select <b>No Certificate</b> in the <b>Certificate Format</b> menu.</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Host | Enter the host name for the MongoDB database. For example:  db.company.com |
    | Database Name | Enter the name of the MongoDB authentication database. This needs to be the specific database the user was created in. |
    | Username | Enter your MongoDB username. |
    | Password | Enter your MongoDB password. |
    | Database Port | Enter the port number for the MongoDB database. |
    | Certificate Format | Select the certificate format. If you do not want to include a certificate, select **No Certificate**. If you select **Certificate String**, you must paste the text for your certificate in the **Certificate** field. If you select **URL Path**, you must enter the URL where your certificate is located in the **Certificate** field. |
    | Certificate | Paste the text for your CA certificate or enter the URL where your certificate is located. This is optional. If you do not want to include a certificate, select **No Certificate** in the **Certificate Format** menu. |

CHANGE 12  [### Credentials Pane]  (line 47 in repo file)

  OLD (remove from repo):
    After you have entered valid MongoDB credentials, you can use the same account in Domo any time you create a MongoDB DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    After you have entered valid MongoDB credentials, you can use the same account in Domo any time you create a MongoDB DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center . For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 13  [### Details Pane]  (line 51 in repo file)

  OLD (remove from repo):
    In this pane you create a JSON query to pull data from your MongoDB collection. You can also create optional WHERE, SELECT, GROUP, and SORT clauses, also in JSON format.

  NEW (replace with / insert from CSV):
    In this pane you create a JSON query to pull data from your MongoDB collection. You can also create optional WHERE, SELECT, GROUP, and SORT clauses, also in JSON format.

CHANGE 14  [### Details Pane]  (lines 53–69 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Menu</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>JSON Query</p></td><td colspan="1" rowspan="1"><p>Enter the JSON query to use in selecting the data you want. Two query methods are available, <span class="mt-font-courier-new">Find</span> and <span class="mt-font-courier-new">Aggregate</span>.</p><p>As an example, let's suppose you have a collection with the following data:</p><p><pre>{ "_id": 1, "subject": "History", "score": 88 },
    { "_id": 2, "subject": "History", "score": 92 },
    { "_id": 3, "subject": "History", "score": 97 },
    { "_id": 4, "subject": "History", "score": 71 },
    { "_id": 5, "subject": "History", "score": 79 },
    { "_id": 6, "subject": "History", "score": 83 }</pre></p><p><b>Find:</b> You could use the <span class="mt-font-courier-new">Find</span> method to select from this collection all documents where the subject equals "History." The query would be formatted as follows:</p><p><pre>{ "subject": "History" }</pre></p><p><b>Aggregate:</b> To use the <span class="mt-font-courier-new">Aggregate</span> method, your query will look something like the following:</p><p><pre>[
      {
        $match: {
          score: {
            $gt: 80
          }
        }
      },
      {
        $count: "passing_scores"
      }
    ]</pre></p><p>In this example, the $match stage excludes documents that have a score value of less than or equal to 80 to pass along the documents with a score greater than 80 to the next stage. The $count stage returns a count of the remaining documents in the aggregation pipeline and assigns the value to a field called "passing scores."</p></td></tr><tr><td colspan="1" rowspan="1"><p>Where Clause JSON</p></td><td colspan="1" rowspan="1"><p>Enter a <span class="mt-font-courier-new">WHERE</span> clause in JSON format.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Select Clause JSON</p></td><td colspan="1" rowspan="1"><p>Enter a <span class="mt-font-courier-new">SELECT</span> clause in JSON format.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Group Clause JSON</p></td><td colspan="1" rowspan="1"><p>Enter a <span class="mt-font-courier-new">GROUP</span> clause in JSON format.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Sort Clause JSON</p></td><td colspan="1" rowspan="1"><p>Enter a <span class="mt-font-courier-new">SORT</span> clause in JSON format.</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | JSON Query | Enter the JSON query to use in selecting the data you want. Two query methods are available, Find and Aggregate.   As an example, let's suppose you have a collection with the following data:  `{ "_id" : 1, "subject" : "History", "score" : 88 },  { "_id" : 2, "subject" : "History", "score" : 92 },  { "_id" : 3, "subject" : "History", "score" : 97 },  { "_id" : 4, "subject" : "History", "score" : 71 },  { "_id" : 5, "subject" : "History", "score" : 79 },  { "_id" : 6, "subject" : "History", "score" : 83 }`  **Find:**You could use the Find method to select from this collection all documents where the subject equals "History." The query would be formatted as follows:  `{ subject: "History" }`  **Aggregate:**To use the Aggregate method, your query will look something like the following:  `[     {        $match: {          score: {            $gt: 80          }        }      },      {        $count: "passing_scores"      }    ]`  In this example, the $match stage excludes documents that have a score value of less than or equal to 80 to pass along the documents with a score greater than 80 to the next stage. The $count stage returns a count of the remaining documents in the aggregation pipeline and assigns the value to a field called "passing scores." |
    | Where Clause JSON | Enter a WHERE clause in JSON format. |
    | Select Clause JSON | Enter a SELECT clause in JSON format. |
    | Group Clause JSON | Enter a GROUP clause in JSON format. |
    | Sort Clause JSON | Enter a SORT clause in JSON format. |

CHANGE 15  [### Other Panes]  (line 73 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/360042931814.mdx
TITLE:  Snowflake Connector
LANG:   English (en_US)
ID:     360042931814
==============================================================================

CHANGE 1  [## Snowflake Connector Migration Walkthrough]  (line 14 in repo file)

  OLD (remove from repo):
    As Snowflake is discontinuing username and password authentication, Snowflake connectors using this authentication method will be migrated to the OAuth version. This section helps users walk through the complete process of migrating their existing username and password Snowflake datasets to use the OAuth authentication version. Please refer to the migration video ([https://youtu.be/rlG_uNZwl_U](https://youtu.be/rlG_uNZwl_U)) to make the transition smoother and minimize disruptions.

  NEW (replace with / insert from CSV):
    As Snowflake is discontinuing username and password authentication, Snowflake connectors using this authentication method will be migrated to the OAuth version. This section helps users walk through the complete process of migrating their existing username and password Snowflake datasets to use the OAuth authentication version. Please refer to the migration video ([https://youtu.be/rlG\_uNZwl\_U](https://youtu.be/rlG_uNZwl_U)) to make the transition smoother and minimize disruptions.

CHANGE 2  [## Intro]  (line 18 in repo file)

  OLD (remove from repo):
    Snowflake is data warehouse that makes it easy to bring together all your data and make it available to all of the users and systems that need to analyze it. You can use Domo's Snowflake connector to retrieve data from a specified warehouse, database, and database schema by inputting an SQL query. For more information about the Snowflake API, go to [https://docs.snowflake.net/manuals](https://docs.snowflake.net/manuals).

  NEW (replace with / insert from CSV):
    Snowflake is data warehouse that makes it easy to bring together all your data and make it available to all of the users and systems that need to analyze it. You can use Domo's Snowflake connector to retrieve data from a specified warehouse, database, and database schema by inputting an SQL query. For more information about the Snowflake API, go to [https://docs.snowflake.net/manuals](https://docs.snowflake.net/manuals).

CHANGE 3  [## Intro]  (line 22 in repo file)

  OLD (remove from repo):
    This topic discusses the fields and menus that are specific to the Snowflake connector user interface. For general information about adding DataSets, setting update schedules, and editing DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This topic discusses the fields and menus that are specific to the Snowflake connector user interface. For general information about adding DataSets, setting update schedules, and editing DataSet information, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 4  [## Prerequisites]  (line 28 in repo file)

  OLD (remove from repo):
    - The full name of your account (provided by Snowflake). This is the portion of your Snowflake URL immediately following https://.

  NEW (replace with / insert from CSV):
    - The full name of your account (provided by Snowflake). This is the portion of your Snowflake URL immediately following https://.
    
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    

CHANGE 5  [## Prerequisites]  (lines 30–35 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    
    <Frame>
    ![snowflake_account_names.png](/images/kb/0EM5w000005vO6C.png)
    </Frame>
    

  NEW (replace with / insert from CSV):
      <img alt="snowflake_account_names.png" src="/images/kb/0EM5w000005vO6C.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>

CHANGE 6  [## Connecting to Your Snowflake Account]  (line 40 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 7  [### Credentials Pane]  (line 46 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Field</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>Account Name</p></td><td colspan="1" rowspan="1"><p>Enter the name of your account (provided by Snowflake). For US West Region <b>account_name</b>, and All other regions <b>account_name.region_id</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Host</p></td><td colspan="1" rowspan="1"><p>Enter the Host name (provided by Snowflake). For example, <a href="http://snowflakecomputing.com">snowflakecomputing.com</a>, <a href="http://snowflakecomputing.cn">snowflakecomputing.cn</a></p></td></tr><tr><td colspan="1" rowspan="1"><p>Username</p></td><td colspan="1" rowspan="1"><p>Enter the username associated with your Snowflake account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Password</p></td><td colspan="1" rowspan="1"><p>Enter the password associated with your Snowflake account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Role</p></td><td colspan="1" rowspan="1"><p>Enter your role.</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the name of your account (provided by Snowflake). For US West Region **account\_name**, and All other regions **account\_name.region\_id**. |
    | Host | Enter the Host name (provided by Snowflake). For example, snowflakecomputing.com**,** snowflakecomputing.cn |
    | Username | Enter the username associated with your Snowflake account. |
    | Password | Enter the password associated with your Snowflake account. |
    | Role | Enter your role. |

CHANGE 8  [### Credentials Pane]  (line 48 in repo file)

  OLD (remove from repo):
    After you have entered valid Snowflake credentials, you can use the same account in Domo any time you create a Snowflake DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    After you have entered valid Snowflake credentials, you can use the same account in Domo any time you create a Snowflake DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

CHANGE 9  [### Details Pane]  (line 54 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1"><thead><tr><th colspan="1" rowspan="1"><p>Parameter</p></th><th colspan="1" rowspan="1"><p>Description</p></th></tr></thead><tbody><tr><td colspan="1" rowspan="1"><p>Query Type</p></td><td colspan="1" rowspan="1"><p>Select the query type.</p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p>Enter Query</p></td><td colspan="1" rowspan="1"><p>It enables an option to enter the user defined query.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Query History</p></td><td colspan="1" rowspan="1"><p>It will fetch the data from the Snowflake query history for the last 70 days using the predefined query.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Table Information</p></td><td colspan="1" rowspan="1"><p>It will fetch the metadata information about the objects created in your account.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Warehouse Metering History</p></td><td colspan="1" rowspan="1"><p>It will fetch the warehouse usage and metering information for the last 70 days.</p></td></tr></tbody></table></td></tr><tr><td colspan="1" rowspan="1"><p>Query</p></td><td colspan="1" rowspan="1"><p>Enter the SQL query to use to retrieve your data. You can use the <b>Query Helper</b> parameter to help you write a usable SQL query. To use the <b>Query Helper</b>, do the following:</p><ol><li><p>Select your desired warehouse, database, database schema, database table, and table columns in each menu.</p></li><li><p>Copy the SQL statement that appears in the <b>Query Helper</b> field.</p></li><li><p>Paste the copied SQL statement into the <b>Query</b> field.</p></li></ol></td></tr><tr><td colspan="1" rowspan="1"><p>Warehouses</p></td><td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates with all of the warehouses you have access to based on your credentials.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Databases</p></td><td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve. The list populates with all of the databases you have access to based on your credentials.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Database Schema</p></td><td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates with all of the schemas you have access to based on your credentials.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Database Objects</p></td><td colspan="1" rowspan="1"><p>Select the database object from the drop-down list.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Database Tables (Optional)</p></td><td colspan="1" rowspan="1"><p>Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Table Columns (Optional)</p></td><td colspan="1" rowspan="1"><p>Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Query Helper (Optional)</p></td><td colspan="1" rowspan="1"><p>When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste the SQL statement in this field into the <b>Query</b> field. For more information, see the entry for <b>Query</b>, above.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Database Views</p></td><td colspan="1" rowspan="1"><p>Select the database view.</p></td></tr><tr><td colspan="1" rowspan="1"><p>View Columns</p></td><td colspan="1" rowspan="1"><p>Select which view columns you want to include in your report.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Fetch Size (Optional)</p></td><td colspan="1" rowspan="1"><p>Enter a fetch size for memory performance. If this field is left blank, the default value of 1000 will be used. If the DataSet throws an "Out of memory" error, try decreasing the fetch size.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Convert BIGINT to LONG</p></td><td colspan="1" rowspan="1"><p>Select this checkbox if you want to convert BIGINT columns to LONG.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Disable NetWork Timeout</p></td><td colspan="1" rowspan="1"><p>Select this checkbox if you observe errors regarding network timeout.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Query Tag</p></td><td colspan="1" rowspan="1"><p>This is an optional parameter that can be used to tag connector SQL executions in Snowflake as a <b>Query Tag</b>.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Convert Columns to String</p></td><td colspan="1" rowspan="1"><p>Enter the column names separated by comma to convert the columns to string. For example, column1, column2, column3.</p></td></tr></tbody></table>

  NEW (replace with / insert from CSV):
    | Parameter | Description |
    | --- | --- |
    | Query Type | Select the query type.   |  |  | | --- | --- | | Enter Query | It enables an option to enter the user defined query. | | Query History | It will fetch the data from the Snowflake query history for the last 70 days using the predefined query. | | Table Information | It will fetch the metadata information about the objects created in your account. | | Warehouse Metering History | It will fetch the warehouse usage and metering information for the last 70 days. | |
    | Query | Enter the SQL query to use to retrieve your data. You can use the **Query Helper** parameter to help you write a usable SQL query. To use the **Query Helper**, do the following:   1. Select your desired warehouse, database, database schema, database table, and table columns in each menu. 2. Copy the SQL statement that appears in the **Query Helper** field. 3. Paste the copied SQL statement into the **Query** field. |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates with all of the warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve. The list populates with all of the databases you have access to based on your credentials. |
    | Database Schema | Select the database schema containing the data you want to retrieve. The list populates with all of the schemas you have access to based on your credentials. |
    | Database Objects | Select the database object from the drop-down list. |
    | Database Tables (Optional) | Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Table Columns (Optional) | Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table. |
    | Query Helper (Optional) | When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste the SQL statement in this field into the **Query** field. For more information, see the entry for **Query**, above. |
    | Database Views | Select the database view. |
    | View Columns | Select which view columns you want to include in your report. |
    | Fetch Size (Optional) | Enter a fetch size for memory performance. If this field is left blank, the default value of 1000 will be used. If the DataSet throws an "Out of memory" error, try decreasing the fetch size. |
    | Convert BIGINT to LONG | Select this checkbox if you want to convert BIGINT columns to LONG. |
    | Disable NetWork Timeout | Select this checkbox if you observe errors regarding network timeout. |
    | Query Tag | This is an optional parameter that can be used to tag connector SQL executions in Snowflake as a **Query Tag**. |
    | Convert Columns to String | Enter the column names separated by comma to convert the columns to string. For example, column1, column2, column3. |

CHANGE 10  [### <img src="/images/kb/0EM5w000005vO6B.png" style={{width: 40, height: 40, display: 'inline', verticalAlign: 'start', margin: '0'}}/> Other Panes]  (line 58 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 11  [## FAQs]  (lines 62–63 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="When should I use this connector?">

  NEW (replace with / insert from CSV):
    ##### When should I use this connector?

CHANGE 12  [## FAQs]  (lines 67–69 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What permissions do I need on Snowflake?">

  NEW (replace with / insert from CSV):
    ##### What permissions do I need on Snowflake?

CHANGE 13  [## FAQs]  (lines 73–75 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    ##### What kind of credentials do I need to power up this connector?

CHANGE 14  [## FAQs]  (lines 79–81 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Can I use the same account to create multiple datasets?">

  NEW (replace with / insert from CSV):
    ##### Can I use the same account to create multiple datasets?

CHANGE 15  [## FAQs]  (line 85 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    ##### How do I find the Input Dataset ID?

CHANGE 16  [## FAQs]  (line 87 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I find the Input Dataset ID?">

  NEW (replace with / insert from CSV):
    Your Domo input dataset ID is in the URL of the dataset you are exporting data from. For example: https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings

CHANGE 17  [## FAQs]  (lines 89–94 in repo file)

  OLD (remove from repo):
    Your Domo input dataset ID is in the URL of the dataset you are exporting data from. For example:
    `https://customer.domo.com/datasources/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/details/settings`
    
    </Accordion>
    
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    ##### Are there any API limits I should be aware of?

CHANGE 18  [## FAQs]  (lines 98–100 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    ##### How frequently will my data update?

CHANGE 19  [## FAQs]  (lines 104–106 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What should I be aware of while writing a query?">

  NEW (replace with / insert from CSV):
    ##### What should I be aware of while writing a query?

CHANGE 20  [## FAQs]  (line 110 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    ##### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?

CHANGE 21  [## FAQs]  (line 112 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    Before you can connect to a Snowflake database, you must also allowlist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses for Connectors & Federated Adapters.](/s/article/360043630093)

CHANGE 22  [## FAQs]  (lines 114–118 in repo file)

  OLD (remove from repo):
    Before you can connect to a Snowflake database, you must also whitelist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses for Connectors & Federated Adapters.](/s/article/360043630093 "Whitelisting IP Addresses for Connectors & Federated Adapters.")
    
    </Accordion>
    
    <Accordion title="Is this a Production-level connector?">

  NEW (replace with / insert from CSV):
    ##### Is this a Production-level connector?

CHANGE 23  [## FAQs]  (lines 122–124 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Why are the lists of warehouses, databases, schemas, etc. not populating correctly?">

  NEW (replace with / insert from CSV):
    ##### Why are the lists of warehouses, databases, schemas, etc. not populating correctly?

CHANGE 24  [## FAQs]  (line 128 in repo file)

  OLD (remove from repo):
    If you do not enter a role in Snowflake, the SYSADMIN role is used by default.

  NEW (replace with / insert from CSV):
    If you do not enter a role in Snowflake, the SYSADMIN role is used by default.

CHANGE 25  [## FAQs]  (line 130 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    ##### How do I get a client id and secret to use with this connector?

CHANGE 26  [## FAQs]  (line 132 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I get a client id and secret to use with this connector?">

  NEW (replace with / insert from CSV):
    This connector uses Snowflake's built-in OAuth service. To get a client id and secret to use with this connector, you will need to create a security integration in Snowflake as described here: [https://docs.snowflake.com/en/user-guide/oauth-custom](https://docs.snowflake.com/en/user-guide/oauth-custom).

CHANGE 27  [## FAQs]  (line 134 in repo file)

  OLD (remove from repo):
    This connector uses Snowflake's built-in OAuth service. To get a client id and secret to use with this connector, you will need to create a security integration in Snowflake as described here: [https://docs.snowflake.com/en/user-guide/oauth-custom](https://docs.snowflake.com/en/user-guide/oauth-custom "https://docs.snowflake.com/en/user-guide/oauth-custom").

  NEW (replace with / insert from CSV):
    When creating the integration, use the following for the OAUTH\_REDIRECT\_URI: [https://oauth.domo.com/api/data/v1/oauth/providers/snowflake-oauth/exchange](https://oauth.domo.com/api/data/v1/oauth/providers/snowflake-oauth/exchange).

CHANGE 28  [## FAQs]  (line 136 in repo file)

  OLD (remove from repo):
    When creating the integration, use the following for the OAUTH\_REDIRECT\_URI: [https://oauth.domo.com/api/data/v1/oauth/providers/snowflake-oauth/exchange](https://oauth.domo.com/api/data/v1/oauth/providers/snowflake-oauth/exchange "https://oauth.domo.com/api/data/v1/oauth/providers/snowflake-oauth/exchange").

  NEW (replace with / insert from CSV):
    ##### Do I need to share the client id and secret with each user who wants to use this connector?

CHANGE 29  [## FAQs]  (lines 138–145 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Do I need to share the client id and secret with each user who wants to use this connector?">
    
    Yes, this connector requires each user who powers up the connector to provide the client id and secret. An alternative to this connector is the [Snowflake Enterprise OAuth Connector](https://domo-support.domo.com/s/article/000005534?language=en_US "Snowflake Enterprise OAuth Connector"). This connector enables Major Domos to save a client id and secret pair, which users can reference when powering this connector.
    
    </Accordion>
    </AccordionGroup>

  NEW (replace with / insert from CSV):
    Yes, this connector requires each user who powers up the connector to provide the client id and secret. An alternative to this connector is the [Snowflake Enterprise OAuth Connector](/s/article/000005534). This connector enables Major Domos to save a client id and secret pair, which users can reference when powering this connector.

  [1 image-only change(s) suppressed — handle separately if needed]

==============================================================================
FILE:   s/article/360042931834.mdx
TITLE:  Snowflake Unload V2 Connector
LANG:   English (en_US)
ID:     360042931834
==============================================================================

CHANGE 1  [## Intro]  (line 17 in repo file)

  OLD (remove from repo):
    Snowflake is a data warehouse built for the cloud for all your data and users. Use Domo's Snowflake Unload V2 Connector to unload data from your Snowflake into Amazon S3. Unloaded files are imported from Amazon S3 into Domo. Choose this connector instead of the Snowflake connector if you are executing very large queries. To learn more about the Snowflake API, visit their page ([https://docs.snowflake.net/manuals](https://docs.snowflake.net/manuals)).

  NEW (replace with / insert from CSV):
    Snowflake is a data warehouse built for the cloud for all your data and users. Use Domo's Snowflake Unload V2 Connector to unload data from your Snowflake into Amazon S3. Unloaded files are imported from Amazon S3 into Domo. Choose this connector instead of the Snowflake connector if you are executing very large queries. To learn more about the Snowflake API, visit their page ([https://docs.snowflake.net/manuals](https://docs.snowflake.net/manuals)).

CHANGE 2  [## Intro]  (line 19 in repo file)

  OLD (remove from repo):
    This Connector is the same as the standard [Snowflake Unload](/s/article/360043436313) connector, except that it allows you to enter your own custom query in addition to using the auto-generate option, whereas the standard version of the connector only includes the auto-generate option.

  NEW (replace with / insert from CSV):
    This Connector is the same as the standard [Snowflake Unload](/s/article/360043436313) connector, except that it allows you to enter your own custom query in addition to using the auto-generate option, whereas the standard version of the connector only includes the auto-generate option.

CHANGE 3  [## Intro]  (line 21 in repo file)

  OLD (remove from repo):
    You connect to your Snowflake account in the Data Center. This topic discusses the fields and menus that are specific to the Snowflake Unload V2 connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You connect to your Snowflake account in the Data Center. This topic discusses the fields and menus that are specific to the Snowflake Unload V2 connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [## Prerequisites]  (line 27 in repo file)

  OLD (remove from repo):
    - The full name of your account (provided by Snowflake). This is the portion of your Snowflake URL immediately following https://.

  NEW (replace with / insert from CSV):
    - The full name of your account (provided by Snowflake). This is the portion of your Snowflake URL immediately following https://.
    
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    

CHANGE 5  [## Prerequisites]  (line 29 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

  NEW (replace with / insert from CSV):
      <img alt="snowflake_account_names.png" src="/images/kb/0EM5w000005vO5u.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    - The username and password used to connect to your Snowflake host.
    - Your AWS access key. This is available in the AWS Console in the **Security Credentials**section (unless you are using IAM, in which case it is under **Users**).
    - Your AWS secret key. This was provided when you created your access key. You can generate a new key in the AWS Console.
    - The name of the AWS S3 bucket in which Snowflake data will be unloaded.
    - The following Permissions are required:
    

CHANGE 6  [## Prerequisites]  (lines 31–71 in repo file)

  ⚠ LARGE CHANGE (50 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
      <Frame>![snowflake_account_names.png](/images/kb/0EM5w000005vO5u.png)</Frame>
    - The username and password used to connect to your Snowflake host.
    - Your AWS access key. This is available in the AWS Console in the **Security Credentials** section (unless you are using IAM, in which case it is under **Users**).
    - Your AWS secret key. This was provided when you created your access key. You can generate a new key in the AWS Console.
    - The name of the AWS S3 bucket in which Snowflake data will be unloaded.
    - The following Permissions are required:
    
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Permission Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Create Stage</p></td>
          <td colspan="1" rowspan="1"><p>Creates a new stage to unload data from tables into files.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Copy into &lt;location&gt;</p></td>
          <td colspan="1" rowspan="1"><p>Command to unload all the rows from a table into one or more files into the stage.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Create file format</p></td>
          <td colspan="1" rowspan="1"><p>Create file format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>LIST</p></td>
          <td colspan="1" rowspan="1"><p>Returns a list of files that have been staged.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>GET</p></td>
          <td colspan="1" rowspan="1"><p>Downloads data files from the above created Snowflake stage.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>REMOVE</p></td>
          <td colspan="1" rowspan="1"><p>Removes files that have been staged - unloaded from a table.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
      |  |  |
      | --- | --- |
      | **Permission Field** | **Description** |
      | Create Stage | Creates a new stage to unload data from tables into files. |
      | Copy into <location> | Command to unload all the rows from a table into one or more files into the stage. |
      | Create file format | Create file format. |
      | LIST | Returns a list of files that have been staged. |
      | GET | Downloads data files from the above created Snowflake stage. |
      | REMOVE | Removes files that have been staged - unloaded from a table. |

CHANGE 7  [## Connecting to your Snowflake account]  (line 75 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Unload V2 Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Unload V2 Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 8  [### Credentials pane]  (line 79 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Snowflake account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Snowflake account. The following table describes what is needed for each field:

CHANGE 9  [### Credentials pane]  (lines 81–122 in repo file)

  ⚠ LARGE CHANGE (52 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Snowflake account name. For more information, see "Prerequisites."</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS access key. For information about obtaining an access key, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Secret Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS secret key. For information about obtaining a secret key, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Bucket</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the AWS S3 bucket.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Region</p></td>
          <td colspan="1" rowspan="1"><p>Select your S3 bucket region.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter your Snowflake account name. For more information, see "Prerequisites." |
    | Username | Enter the username you use to connect to your Snowflake host. |
    | Password | Enter the password you use to connect to your Snowflake host. |
    | Access Key | Enter your AWS access key. For information about obtaining an access key, see "Prerequisites," above. |
    | Secret Key | Enter your AWS secret key. For information about obtaining a secret key, see "Prerequisites," above. |
    | Bucket | Enter the name of the AWS S3 bucket. |
    | Region | Select your S3 bucket region. |
    | Role | Enter your role. |

CHANGE 10  [### Details Pane]  (line 128 in repo file)

  OLD (remove from repo):
    In this pane you create an SQL query to pull data from your database. You can choose a specific database table and partition column. You can also specify the number of past days you want to get data for.

  NEW (replace with / insert from CSV):
    In this pane you create an SQL query to pull data from your database. You can choose a specific database table and partition column. You can also specify the number of past days you want to get data for.

CHANGE 11  [### Details Pane]  (lines 130–230 in repo file)

  ⚠ LARGE CHANGE (121 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to import data into Domo?</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to import your data by using the standard update method (replace/append), by using partition, or upsert mode.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to build your Query?</p></td>
          <td colspan="1" rowspan="1">
            <table border="1" cellpadding="1" cellspacing="1">
              <thead>
                <tr>
                  <th colspan="1" rowspan="1"><p>Query Type</p></th>
                  <th colspan="1" rowspan="1"><p>Description</p></th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td colspan="1" rowspan="1"><p>Custom Query</p></td>
                  <td colspan="1" rowspan="1"><p>Enter the SQL query to execute.</p></td>
                </tr>
                <tr>
                  <td colspan="1" rowspan="1"><p>Query Builder</p></td>
                  <td colspan="1" rowspan="1"><p>Select a table and fields to autogenerate your query.</p></td>
                </tr>
              </tbody>
            </table>
          </td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates all warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve. The list populates all databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Schemas</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates all schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to retrieve. The list populates all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>A query is generated based on the selected fields. Copy and paste this text into the QUERY field. You may edit this query, if desired.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query you want to execute.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to partition your data using the date keys, non-date keys, or meta query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Partition Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name. Only date fields will be shown in this discovery.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. Example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Please select/enter proper date format according to the date format present in partition column. By default <b>yyyy-MM-dd</b> will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom date format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select a non-date key column name to partition the data. All fields excluding Date fields will appear in this dropdown.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter a non-date key column name to partition the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Query to determine partition tags</p></td>
          <td colspan="1" rowspan="1"><p>Enter the partition meta query to determine the distinct partition tags (or keys). The column containing the <b>Date</b> data is your partition column.</p><p>Example: SELECT DISTINCT (DATE_COLUMN_NAME) FROM TABLENAME</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Upsert Key Column(s)</p></td>
          <td colspan="1" rowspan="1"><p>Enter upsert key column name or a comma separated list of upsert key column names. <b>Required only when Update method is Merge</b>.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | How would you like to import data into Domo? | Select whether you want to import your data by using the standard update method (replace/append), by using partition, or upsert mode. |
    | How would you like to build your Query? | | Query Type | Description | | --- | --- | | Custom Query | Enter the SQL query to execute. | | Query Builder | Select a table and fields to autogenerate your query. | |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates all warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve. The list populates all databases you have access to based on your credentials. |
    | Schemas | Select the database schema containing the data you want to retrieve. The list populates all schemas you have access to based on your credentials. |
    | Tables | Select the database table you want to retrieve. The list populates all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Table Column Name | Select the partition column name. |
    | Table Query Helper | A query is generated based on the selected fields. Copy and paste this text into the QUERY field. You may edit this query, if desired. |
    | Query | Enter the SQL query you want to execute. |
    | Partition Criteria | Select whether you want to partition your data using the date keys, non-date keys, or meta query. |
    | Select Partition Date Key Column Name | Select the partition column name. Only date fields will be shown in this discovery. |
    | Past Days | Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. Example: 30. |
    | Date Format | Please select/enter proper date format according to the date format present in partition column. By default **yyyy-MM-dd** will be used. |
    | Custom Date Format | Enter the custom date format. |
    | Select Partition Non Date Key Column Name | Select a non-date key column name to partition the data. All fields excluding Date fields will appear in this dropdown. |
    | Enter Partition Non Date Key Column Name | Enter a non-date key column name to partition the data. |
    | Partition Query to determine partition tags | Enter the partition meta query to determine the distinct partition tags (or keys). The column containing the **Date** data is your partition column.  Example: SELECT DISTINCT (DATE\_COLUMN\_NAME) FROM TABLENAME |
    | Upsert Key Column(s) | Enter upsert key column name or a comma separated list of upsert key column names. **Required only when Update method is Merge**. |

CHANGE 12  [### Selecting the Update Mode]  (line 234 in repo file)

  OLD (remove from repo):
    Once you decide how you want to import your data into Domo (by using the standard update method (replace/append), by using partition, or upsert mode), you need to select the relevant update mode in the **Scheduling section** in the Connector.

  NEW (replace with / insert from CSV):
    Once you decide how you want to import your data into Domo (by using the standard update method (replace/append), by using partition, or upsert mode), you need to select the relevant update mode in the **Scheduling section** in the Connector.

CHANGE 13  [### Other Panes]  (line 246 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 14  [## FAQs]  (line 250 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### What is the difference between this connector and the standard Snowflake Unload connector?

CHANGE 15  [## FAQs]  (line 252 in repo file)

  OLD (remove from repo):
    <Accordion title="What is the difference between this connector and the standard Snowflake Unload connector?">

  NEW (replace with / insert from CSV):
    This connector allows you to enter your own custom query in addition to using the auto-generate option, whereas the standard version of the connector only includes the auto-generate option. Additionally, with this Connector, you can import your data by using the standard update mode (replace/append), by using partition, or upsert mode.

CHANGE 16  [## FAQs]  (lines 254–258 in repo file)

  OLD (remove from repo):
    This connector allows you to enter your own custom query in addition to using the auto-generate option, whereas the standard version of the connector only includes the auto-generate option. Additionally, with this Connector, you can import your data by using the standard update mode (replace/append), by using partition, or upsert mode.
    
    </Accordion>
    
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits I should be aware of?

CHANGE 17  [## FAQs]  (lines 262–264 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?

CHANGE 18  [## FAQs]  (lines 268–270 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What should I be aware of while writing a query?">

  NEW (replace with / insert from CSV):
    #### What should I be aware of while writing a query?

CHANGE 19  [## FAQs]  (lines 274–276 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?

CHANGE 20  [## FAQs]  (lines 279–282 in repo file)

  OLD (remove from repo):
    
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360042931874.mdx
TITLE:  Vertica Connector
LANG:   English (en_US)
ID:     360042931874
==============================================================================

CHANGE 1  [## Intro]  (line 7 in repo file)

  OLD (remove from repo):
    The cluster-based, column-oriented, Vertica Analytics Platform is designed to manage large, fast-growing volumes of data and provide fast query performance. Use Domo's Vertica connector to retrieve data from Vertica tables using SQL queries. To learn more about Vertica, visit their documentation home page ([https://www.vertica.com/documentation/vertica/9-3-x-documentation/](https://my.vertica.com/documentation/vertica/)).

  NEW (replace with / insert from CSV):
    The cluster-based, column-oriented, Vertica Analytics Platform is designed to manage large, fast-growing volumes of data and provide fast query performance. Use Domo's Vertica connector to retrieve data from Vertica tables using SQL queries. To learn more about Vertica, visit their documentation home page ([https://www.vertica.com/documentation/vertica/9-3-x-documentation/](https://my.vertica.com/documentation/vertica/)).

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    You connect to your Vertica account in the Data Center. This topic discusses the fields and menus that are specific to the Vertica connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You connect to your Vertica account in the Data Center. This topic discusses the fields and menus that are specific to the Vertica connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Prerequisites]  (line 15 in repo file)

  OLD (remove from repo):
    - The name of the server where the Vertica database is hosted

  NEW (replace with / insert from CSV):
    - The name of the server where the Vertica database is hosted

CHANGE 4  [## Prerequisites]  (line 18 in repo file)

  OLD (remove from repo):
    - The name of the Vertica database

  NEW (replace with / insert from CSV):
    - The name of the Vertica database

CHANGE 5  [## Connecting to Your Vertica Account]  (line 22 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Vertica Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Vertica Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 6  [### Credentials Pane]  (line 26 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Vertica account. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Vertica account. The following table describes what is needed for each field:

CHANGE 7  [### Credentials Pane]  (lines 28–61 in repo file)

  ⚠ LARGE CHANGE (42 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>JDBC Driver</p></td>
          <td colspan="1" rowspan="1"><p>Select the appropriate JDBC driver to use.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Host</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the server where the Vertica database is hosted.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to connect to the Vertica database.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password you use to connect to the Vertica database.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Port Number</p></td>
          <td colspan="1" rowspan="1"><p>Enter the port number of the Vertica database.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the Vertica database.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | JDBC Driver | Select the appropriate JDBC driver to use. |
    | Host | Enter the name of the server where the Vertica database is hosted. |
    | Username | Enter the username you use to connect to the Vertica database. |
    | Password | Enter the password you use to connect to the Vertica database. |
    | Port Number | Enter the port number of the Vertica database. |
    | Database Name | Enter the name of the Vertica database. |

CHANGE 8  [### Whitelisting]  (line 67 in repo file)

  OLD (remove from repo):
    Before you can connect to a Vertica database, you must also whitelist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses for Connectors](/s/article/360043630093).

  NEW (replace with / insert from CSV):
    Before you can connect to a Vertica database, you must also allowlist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses for Connectors](/s/article/360043630093).

CHANGE 9  [### Details Pane]  (lines 73–98 in repo file)

  ⚠ LARGE CHANGE (32 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query to use to retrieve your data. You can use the <b>Query Helper</b> parameter to help you write a usable SQL query. To use the <b>Query Helper</b>, do the following:</p><ol><li><p>Select your database table and table columns in the appropriate menus.</p></li><li><p>Copy the SQL statement that appears in the <b>Query Helper</b> field.</p></li><li><p>Paste the copied SQL statement into the <b>Query</b> field.</p></li></ol></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Tables (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the database tables you want to pull into Domo, if desired.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the table columns you want to pull into Domo.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Helper (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Copy and paste the SQL statement in this field into the <b>Query</b> field. For more information, see <a href="#query">Query</a>, above.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Query | Enter the SQL query to use to retrieve your data. You can use the **Query Helper**parameter to help you write a usable SQL query. To use the **Query Helper**, do the following:   1. Select your database table and table columns in the appropriate menus. 2. Copy the SQL statement that appears in the **Query Helper**field. 3. Paste the copied SQL statement into the **Query** field. |
    | Database Tables (Optional) | Select the database tables you want to pull into Domo, if desired. |
    | Table Columns (Optional) | Select the table columns you want to pull into Domo. |
    | Query Helper (Optional) | (Optional) Copy and paste the SQL statement in this field into the **Query**field. For more information, see [Query](#query), above. |

CHANGE 10  [### Other Panes]  (line 102 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/360042932014.mdx
TITLE:  Google Drive Service Connector
LANG:   English (en_US)
ID:     360042932014
==============================================================================

CHANGE 1  [(top of file)]  (lines 5–7 in repo file)

  OLD (remove from repo):
    <Warning>
    **Important:** Due to Google's change in allowing Domo connectors to use OAuth authentication, all users must now use service account key authentication to create new data connections. To create new Google Drive DataSets, use this connector. All pre-existing Google Drive DataSets will continue to run normally.
    </Warning>

  NEW (replace with / insert from CSV):
    ********Important****************:********Due to Google’s change in allowing Domo connectors to use OAuth authentication, all users must now use service account key authentication to create new data connections. To create new Google Drive DataSets, use this connector. All pre-existing Google Drive DataSets will continue to run normally.

CHANGE 2  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    Google Drive is a file storage and synchronization service developed by Google that allows users to store files in the cloud, synchronize files across devices, and share files. Use Domo's Google Drive Service connector to retrieve Excel and CSV files from a Google Drive account and output them as Domo DataSets. To learn more about the Google Drive API, visit their page ([https://developers.google.com/drive/](https://developers.google.com/drive/)).

  NEW (replace with / insert from CSV):
    Google Drive is a file storage and synchronization service developed by Google that allows users to store files in the cloud, synchronize files across devices, and share files. Use Domo's Google Drive Service connector to retrieve Excel and CSV files from a Google Drive account and output them as Domo DataSets. To learn more about the Google Drive API, visit their page ([https://developers.google.com/drive/](https://developers.google.com/drive/)).

CHANGE 3  [## Intro]  (line 13 in repo file)

  OLD (remove from repo):
    You connect to your Google account in the Data Center. This topic discusses the fields and menus that are specific to the Google Drive connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You connect to your Google account in the Data Center. This topic discusses the fields and menus that are specific to the Google Drive connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [#### Enabling the Google Drive API]  (line 27 in repo file)

  OLD (remove from repo):
    2. Click **Library** under **APIs and services** in the left pane.

  NEW (replace with / insert from CSV):
    2. Click **Library** under **APIs and services** in the left pane.

CHANGE 5  [#### Enabling the Google Drive API]  (line 31 in repo file)

  NEW (replace with / insert from CSV):
    

CHANGE 6  [#### Enabling the Google Drive API]  (line 33 in repo file)

  OLD (remove from repo):
    4. Towards the top of the screen you will see an **ENABLE** button.

  NEW (replace with / insert from CSV):
    4. Towards the top of the screen you will see an **ENABLE** button.

CHANGE 7  [#### To generate a service account JSON key, do the following:]  (line 41 in repo file)

  OLD (remove from repo):
    1. In the [GCP Console](https://console.cloud.google.com/apis/dashboard), open the **IAM & Admin** page.

  NEW (replace with / insert from CSV):
    1. In the [GCP Console](https://console.cloud.google.com/apis/dashboard), open the ****IAM & Admin**** page.

CHANGE 8  [#### To generate a service account JSON key, do the following:]  (line 43 in repo file)

  OLD (remove from repo):
    2. Click **Service accounts** in the left-hand navigation pane.

  NEW (replace with / insert from CSV):
    2. Click ********Service accounts******** in the left-hand navigation pane.

CHANGE 9  [#### To generate a service account JSON key, do the following:]  (line 47 in repo file)

  OLD (remove from repo):
    3. Click **Create Service Account**.

  NEW (replace with / insert from CSV):
    3. Click ********Create Service Account********.

CHANGE 10  [#### To generate a service account JSON key, do the following:]  (line 49 in repo file)

  OLD (remove from repo):
    4. In the **Service account details** section, enter a name and description for the service account.

  NEW (replace with / insert from CSV):
    4. In the **Service account details** section, enter a name and description for the service account.

CHANGE 11  [#### To generate a service account JSON key, do the following:]  (line 51 in repo file)

  OLD (remove from repo):
    5. Click **Create and Continue**.

  NEW (replace with / insert from CSV):
    5. Click **Create and Continue**.

CHANGE 12  [#### To generate a service account JSON key, do the following:]  (line 53 in repo file)

  OLD (remove from repo):
    6. Now, in the **Grant this service account access to the project** section, click **Select a role** drop down.

  NEW (replace with / insert from CSV):
    6. Now, in the **Grant this service account access to the project** section, click **Select a role** drop down.

CHANGE 13  [#### To generate a service account JSON key, do the following:]  (line 55 in repo file)

  OLD (remove from repo):
    7. Select **Project-> Owner**.

  NEW (replace with / insert from CSV):
    7. Select **Project-> Owner**.

CHANGE 14  [#### To generate a service account JSON key, do the following:]  (line 59 in repo file)

  OLD (remove from repo):
    8. Click **Continue**.

  NEW (replace with / insert from CSV):
    8. Click **Continue**.

CHANGE 15  [#### To generate a service account JSON key, do the following:]  (line 61 in repo file)

  OLD (remove from repo):
    9. Click **Done**. An overview of your service account will appear.

  NEW (replace with / insert from CSV):
    9. Click **Done**. An overview of your service account will appear.

CHANGE 16  [#### To generate a service account JSON key, do the following:]  (line 63 in repo file)

  OLD (remove from repo):
    10. In the **Actions** column, click the three dots to expand the menu. Click **Manage Keys**.

  NEW (replace with / insert from CSV):
    10. In the **Actions** column, click the three dots to expand the menu. Click **Manage Keys**.

CHANGE 17  [#### To generate a service account JSON key, do the following:]  (line 67 in repo file)

  OLD (remove from repo):
    11. Click **Add Key**. Select **Create new key**.

  NEW (replace with / insert from CSV):
    11. Click **Add Key**. Select **Create new key**.

CHANGE 18  [#### To generate a service account JSON key, do the following:]  (line 71 in repo file)

  OLD (remove from repo):
    12. **Create private key** window will appear. Select private key type as **JSON** and click **Create**.

  NEW (replace with / insert from CSV):
    12. **Create private key** window will appear. Select private key type as **JSON** and click **Create**.

CHANGE 19  [## Connecting to Your Google Service Account]  (line 77 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and **Details** panes in the Google Drive Service Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Google Drive Service Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 20  [### Credentials Pane]  (lines 83–96 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Service Account Key JSON</p></td>
          <td colspan="1" rowspan="1"><p>Copy and paste the JSON for your Google service account key. For information about creating a key, see "Prerequisites," above.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Service Account Key JSON | Copy and paste the JSON for your Google service account key. For information about creating a key, see "Prerequisites," above. |

CHANGE 21  [### Credentials Pane]  (line 98 in repo file)

  OLD (remove from repo):
    Once you have entered a valid key, you can use the same account any time you go to create a new Google Drive Service DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    Once you have entered a valid key, you can use the same account any time you go to create a new Google Drive Service DataSet. You can manage connector accounts in the ********Accounts******** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 22  [### Details Pane]  (lines 104–201 in repo file)

  ⚠ LARGE CHANGE (123 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to import a list of files ("Files") or download a specific files ("FileDownload").</p><table border="1" cellpadding="1" cellspacing="1"><tbody><tr><td colspan="1" rowspan="1"><p>Files</p></td><td colspan="1" rowspan="1"><p>Returns list of files in drive.</p></td></tr><tr><td colspan="1" rowspan="1"><p>File Download</p></td><td colspan="1" rowspan="1"><p>Imports a file from google drive into Domo.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Fetch Data from Shared Drives</p></td><td colspan="1" rowspan="1"><p>Select <b>Yes</b> if you want the connector to fetch data from the Shared Drives.</p></td></tr></tbody></table></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>How Would You Like to Choose Your File?</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to choose the file you intend to import into Domo. Select <b>Enter File Name</b> or <b>Enter File ID</b> to enter the name or ID yourself. Select <b>Files Discovery</b> to choose your file from a list of files.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to match the Sheet Name?</p></td>
          <td colspan="1" rowspan="1"><p>If the user selects <b>Contains</b>, the connector will retrieve the most recently uploaded file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the file you want to import. To see all IDs for files in your account, run the "Files" report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the file you want to import from Google Drive.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Files Discovery</p></td>
          <td colspan="1" rowspan="1"><p>Select the file you want to import into Domo.</p><p><b>Note:</b> The File Discovery option is available only if the number of files in the Google Drive account is less than or equal to 100.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>MIME Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the MIME type of the file you want to import into Domo.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>What File Type Would You Like to Import?</p></td>
          <td colspan="1" rowspan="1"><p>Select the file type that you would like to parse and import.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select the delimiting character</p></td>
          <td colspan="1" rowspan="1"><p>Select the delimiting character used in your file. If your delimiter is not listed select 'Other.'</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specify your delimiter</p></td>
          <td colspan="1" rowspan="1"><p>Enter the character used to delimit your character separated values (CSV) text.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Quote Character</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired quote character for parsing CSV files. (Double quote is the default quote character for CSV standard).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Quote Character</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom quote character you want to use to parse your CSV file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Escape Character</p></td>
          <td colspan="1" rowspan="1"><p>Select the escape character to use to parse your CSV file. To choose a custom escape character, select <b>Other</b>, then enter your delimiter in the <b>Custom Escape Character</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Escape Character</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom quote character you want to use to parse your CSV file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the date format that is used in the selected file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Header Start Row</p></td>
          <td colspan="1" rowspan="1"><p>Enter the header start row for your Excel file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Data Start Row</p></td>
          <td colspan="1" rowspan="1"><p>Enter the data start row for your Excel file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Footer Rows to Skip</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of rows to skip at the end of your Excel file, if any.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to match the sheet name?</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you would like to match the sheet name with a sheet that contains the provided name or is exactly equivalent to the provided name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Password</p></td>
          <td colspan="1" rowspan="1"><p>If your file is password protected please enter file password.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Sheet Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the sheet name you want to retrieve from the specified spreadsheet be sure to check sheet name for accidental spaces, first sheet of the workbook will be used if the field is left blank.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Blank Rows</p></td>
          <td colspan="1" rowspan="1"><p>If you encounter blank rows, you can either skip the blank rows or stop at the first blank row.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select whether you want to import a list of files ("Files") or download a specific files ("FileDownload").   |  |  | | --- | --- | | Files | Returns list of files in drive. | | File Download | Imports a file from google drive into Domo. | | Fetch Data from Shared Drives | Select **Yes** if you want the connector to fetch data from the Shared Drives. | |
    | How Would You Like to Choose Your File? | Select how you want to choose the file you intend to import into Domo. Select ****Enter File Name**** or **Enter File ID** to enter the name or ID yourself. Select ****Files Discovery**** to choose your file from a list of files. |
    | How would you like to match the Sheet Name? | If the user selects **Contains**, the connector will retrieve the most recently uploaded file. |
    | File ID | Enter the ID of the file you want to import. To see all IDs for files in your account, run the "Files" report. |
    | File Name | Enter the name of the file you want to import from Google Drive. |
    | Files Discovery | Select the file you want to import into Domo.
    <Note>**Note:** The File Discovery option is available only if the number of files in the Google Drive account is less than or equal to 100. |
    | MIME Type | Select the MIME type of the file you want to import into Domo. |
    | What File Type Would You Like to Import? | Select the file type that you would like to parse and import. |
    | Select the delimiting character | Select the delimiting character used in your file. If your delimiter is not listed select 'Other.' |
    | Specify your delimiter | Enter the character used to delimit your character separated values (CSV) text. |
    | Quote Character | Select the desired quote character for parsing CSV files. (Double quote is the default quote character for CSV standard). |
    | Custom Quote Character | Enter the custom quote character you want to use to parse your CSV file. |
    | Escape Character | Select the escape character to use to parse your CSV file. To choose a custom escape character, select ****Other****, then enter your delimiter in the ****Custom Escape Character**** field. |
    | Custom Escape Character | Enter the custom quote character you want to use to parse your CSV file. |
    | Date Format | Select the date format that is used in the selected file. |
    | Header Start Row | Enter the header start row for your Excel file. |
    | Data Start Row | Enter the data start row for your Excel file. |
    | Footer Rows to Skip | Enter the number of rows to skip at the end of your Excel file, if any. |
    | How would you like to match the sheet name? | Select whether you would like to match the sheet name with a sheet that contains the provided name or is exactly equivalent to the provided name. |
    | File Password | If your file is password protected please enter file password. |
    | Sheet Name | Enter the sheet name you want to retrieve from the specified spreadsheet be sure to check sheet name for accidental spaces, first sheet of the workbook will be used if the field is left blank. |
    | Blank Rows | If you encounter blank rows, you can either skip the blank rows or stop at the first blank row. |</Note>

CHANGE 23  [### Other Panes]  (line 205 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 24  [## FAQs]  (lines 209–211 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    
    <Accordion title="What do I do if my Google Drive file is not visible in the &quot;Files&quot; report?">

  NEW (replace with / insert from CSV):
    #### What do I do if my Google Drive file is not visible in the "Files" report?

CHANGE 25  [## FAQs]  (lines 215–217 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="My DataSet is returning a '403 Forbidden' error. Why?">

  NEW (replace with / insert from CSV):
    #### My DataSet is returning a '403 Forbidden' error. Why?

CHANGE 26  [## FAQs]  (lines 221–223 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?

CHANGE 27  [## FAQs]  (lines 227–229 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits I should be aware of?

CHANGE 28  [## FAQs]  (lines 232–235 in repo file)

  OLD (remove from repo):
    
    </Accordion>
    
    </AccordionGroup>

  [1 image-only change(s) suppressed — handle separately if needed]

==============================================================================
FILE:   s/article/360042932074.mdx
TITLE:  Excel SFTP Connector
LANG:   English (en_US)
ID:     360042932074
==============================================================================

CHANGE 1  [## Prerequisites]  (lines 15–25 in repo file)

  OLD (remove from repo):
    ```
    54.208.95.237
    54.208.87.122
    54.208.95.167
    54.208.94.194
    50.207.240.162
    50.207.241.62
    52.18.90.222
    52.62.103.83
    34.198.214.100
    ```

  NEW (replace with / insert from CSV):
    `54.208.95.237`
    
    `54.208.87.122`
    
    `54.208.95.167`
    
    `54.208.94.194`
    
    `50.207.240.162`
    
    `50.207.241.62`
    
    `52.18.90.222`
    
    `52.62.103.83`
    
    `34.198.214.100`

CHANGE 2  [## Connecting to Your Excel File]  (line 29 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Excel SFTP Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Excel SFTP Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 35–60 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to authenticate to the SFTP server.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password you use to authenticate to the SFTP server.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Host Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name or IP address of the SFTP host you want to connect to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Port Number</p></td>
          <td colspan="1" rowspan="1"><p>Enter the port number of the SFTP server.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Username | Enter the username you use to authenticate to the SFTP server. |
    | Password | Enter the password you use to authenticate to the SFTP server. |
    | Host Name | Enter the name or IP address of the SFTP host you want to connect to. |
    | Port Number | Enter the port number of the SFTP server. |
    

CHANGE 4  [### Details Pane]  (lines 67–112 in repo file)

  ⚠ LARGE CHANGE (58 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>File Name or Portion of File Name to Search For</p></td>
          <td colspan="1" rowspan="1"><p>Enter the file name or a portion of the file name of the Excel file you want to pull into Domo. Both .xls and .xlsx files are supported.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Type of Match to Perform</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the filename starts with or contains the string you entered in the <b>File Name or Portion of File Name to Search For</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Is Password Protected?</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the Excel file you are pulling into Domo is password protected.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password for the Excel file you want to pull into Domo.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Sheet Name (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the Excel sheet you want to pull into Domo. If you leave this blank, Domo pulls the first sheet of the file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Layout</p></td>
          <td colspan="1" rowspan="1"><p>Select the layout of the Excel file you are pulling into Domo.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Mode</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired parsing mode. If you select <b>Manual</b>, you are asked to enter a column header range and date range; if you select <b> Automatic</b>, these ranges are determined automatically.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Column Header Range</p></td>
          <td colspan="1" rowspan="1"><p>Enter the range of header cells in the Excel file, e.g. A2:F8.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Data Range</p></td>
          <td colspan="1" rowspan="1"><p>Enter the range of data cells in the Excel file, e.g. A3:F20.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | File Name or Portion of File Name to Search For | Enter the file name or a portion of the file name of the Excel file you want to pull into Domo. Both .xls and .xlsx files are supported. |
    | Type of Match to Perform | Select whether the filename starts with or contains the string you entered in the **File Name or Portion of File Name to Search For** field. |
    | Is Password Protected? | Select whether the Excel file you are pulling into Domo is password protected. |
    | File Password | Enter the password for the Excel file you want to pull into Domo. |
    | Sheet Name (Optional) | Enter the name of the Excel sheet you want to pull into Domo. If you leave this blank, Domo pulls the first sheet of the file. |
    | Layout | Select the layout of the Excel file you are pulling into Domo. |
    | Mode | Select the desired parsing mode. If you select **Manual**, you are asked to enter a column header range and date range; if you select **Automatic**, these ranges are determined automatically. |
    | Column Header Range | Enter the range of header cells in the Excel file, e.g. A2:F8. |
    | Data Range | Enter the range of data cells in the Excel file, e.g. A3:F20. |
    

==============================================================================
FILE:   s/article/360042932114.mdx
TITLE:  Microsoft Exchange Mailbox Connector
LANG:   English (en_US)
ID:     360042932114
==============================================================================

CHANGE 1  [## Connecting to Your Microsoft Exchange Account]  (line 20 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Microsoft Exchange Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Microsoft Exchange Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [### Credentials Pane]  (lines 26–43 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Client ID. For step-by-step instructions on obtaining a Client ID and Client Secret, see <a href="https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app">https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Client Secret. For step-by-step instructions on obtaining a Client ID and Client Secret, see <a href="https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app">https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app</a>.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter your Client ID. For step-by-step instructions on obtaining a Client ID and Client Secret, see [https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app](https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app). |
    | Client Secret | Enter your Client Secret. For step-by-step instructions on obtaining a Client ID and Client Secret, see [https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app](https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app). |
    

CHANGE 3  [### Configuring Your Azure App]  (line 71 in repo file)

  OLD (remove from repo):
    4. Click **Register**. An overview of your registered App will appear. The ** Application ID** that appears here is the ** Client ID** you will enter in the ** Credentials** pane in Domo.

  NEW (replace with / insert from CSV):
    4. Click **Register**. An overview of your registered App will appear. The **Application ID** that appears here is the **Client ID** you will enter in the **Credentials**pane in Domo.

CHANGE 4  [### Configuring Your Azure App]  (line 89 in repo file)

  OLD (remove from repo):
    10. Copy the value and paste it into the **Client Secret** field in the ** Connector credentials** section in Domo.

  NEW (replace with / insert from CSV):
    10. Copy the value and paste it into the **Client Secret** field in the **Connector credentials** section in Domo.

CHANGE 5  [### Details Pane]  (lines 95–292 in repo file)

  ⚠ LARGE CHANGE (248 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Microsoft Exchange report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Folder</p></td>
          <td colspan="1" rowspan="1"><p>Select the folder containing the content you want to import.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Search Type</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to search for a specific term or apply filters to import a subset of a collection.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Filter Type</p></td>
          <td colspan="1" rowspan="1"><p>Select a filter type, either <b>Starts with</b> or <b> Equals</b>. If you select <b> Starts with</b>, all files starting with the specified characters will be returned. If you select <b> Equals</b>, only files that are an exact match will be returned.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Subject (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter a subject to filter to show all emails containing the given subject line.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>From (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of a user to filter returned data specific to that user.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Search Option</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired search method. If you select <b>Custom Search</b>, you are prompted to enter a subject line and sender name for the search. If you select <b> Query</b>, you are prompted to enter a search query string.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter a Keyword Query Language (KQL) query. For more information about searchable email properties, KQL syntax, supported operators, and search tips, see the Microsoft Exchange user documentation.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Calendar Type</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to import a shared or personal (<b>Own</b>) calendar.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Shared Calendar Search Type</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to search for shared calendars by name or email address.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Shared Calendars</p></td>
          <td colspan="1" rowspan="1"><p>Select the shared calendar(s) you want to import into Domo.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Shared Users Email Address</p></td>
          <td colspan="1" rowspan="1"><p>Enter the email address of the user whose calendar you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Type</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the attachment you are importing is a CSV, XLS, XLSX, JSON, or XML file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the attachment you want to import. If the file name contains an extension, you must include the extension.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Folder Path (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the path of the folder containing messages you want to search. If you do not enter a folder path, the "Inbox" folder will be searched. For example, if the root folder was "DOMO" and in that root folder you had a folder called "DOMO\_TEST," you would enter the following to return messages for "DOMO\_TEST": DOMO/DOMO\_TEST</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Password (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>If the file you are uploading is password-protected, enter the password here.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Delimiter</p></td>
          <td colspan="1" rowspan="1"><p>Select the delimiter to use in parsing CSV files. If your desired delimiter is not in the menu, select <b>Other</b> then enter the delimiter in the <b> Custom Delimiter</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Delimiter</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom delimiter for your CSV file. Enter \t for tab or Unicode characters in \uxxxx form (e.g. \u005e).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Quote Character</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired quote character for parsing your CSV file. A double quote (") is the CSV standard.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Quote Character</p></td>
          <td colspan="1" rowspan="1"><p>Enter the desired custom quote character. Enter \t for tab or Unicode characters in \uxxxx form (e.g. \u005e).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Escape Character</p></td>
          <td colspan="1" rowspan="1"><p>Select the escape character to use in parsing CSV files. If your desired escape character is not in the menu, select <b>Other</b> then enter the escape character in the <b> Custom Escape Character</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Escape Character</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom escape character for your CSV file. Enter \t for tab or Unicode characters in \uxxxx form (e.g. \u005e).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back you want to begin pulling data for. For example, if you entered 7 here, each time the report ran it would pull data from 7 days ago up until today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Header Start Row (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the header row number. If you do not enter a number, the first row is considered the header row.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Data Start Row (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the starting data row number. If you do not enter a number, the first row after the header row is considered the starting row.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Footer Rows to Skip (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>If there are one or more rows at the bottom of the file you do not want to import, enter the number of rows to skip.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Sheet Name (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>If the Excel file you are uploading is found on a specific sheet, enter the sheet name here. If you do not enter a sheet name, the first sheet of the workbook will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Field Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the data tag in your JSON or XML file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Sublist to Flatten</p></td>
          <td colspan="1" rowspan="1"><p>Enter the lists you want to flatten, separated by commas.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the date for the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that should appear in the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b>End Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in <b>Start Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific End Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the second date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to Start From</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the farthest day back that should be represented in the report. Combine with <b>Days Back to End At</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to End At</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the most recent day back that should be represented in the report. Combine with <b>Days Back to Start From</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the date for the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that should appear in the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b>End Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in <b>Start Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific End Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the second date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to Start From</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the farthest day back that should be represented in the report. Combine with <b>Days Back to End At</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to End At</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the most recent day back that should be represented in the report. Combine with <b>Days Back to Start From</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Microsoft Exchange report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Attachments | Returns the parsed data from the specified email attachment. | | Calendar | Returns the user's or shared user's calendars. | | Groups | Lists all organizational groups, excluding dynamic distribution groups. | | Group Members | Retrieves a list of the group's direct members. | | Group Owners | Retrieve a list of the group's owners. | | Group Transitive Members | Retrieves a flat, transitive list of all members in the group, including nested members. | | List Events | Retrieve the list of event objects in the user's mailbox. The resulting list includes single-instance meetings and series masters. | | Messages | Returns message details, including recipients, body, subject, size, importance, sender name, and other relevant information. | | Tasks | Returns task details, including start date, due date, subject, and more. | |
    | Folder | Select the folder containing the content you want to import. |
    | Search Type | Select whether you want to search for a specific term or apply filters to import a subset of a collection. |
    | Filter Type | Select a filter type, either **Starts with** or **Equals**. If you select **Starts with**, all files starting with the specified characters will be returned. If you select **Equals**, only files that are an exact match will be returned. |
    | Subject (Optional) | Enter a subject to filter to show all emails containing the given subject line. |
    | From (Optional) | Enter the ID of a user to filter returned data specific to that user. |
    | Search Option | Select the desired search method. If you select **Custom Search**, you are prompted to enter a subject line and sender name for the search. If you select **Query**, you are prompted to enter a search query string. |
    | Query | Enter a Keyword Query Language (KQL) query. For more information about searchable email properties, KQL syntax, supported operators, and search tips, see the Microsoft Exchange user documentation. |
    | Calendar Type | Select whether you want to import a shared or personal (**Own**) calendar. |
    | Shared Calendar Search Type | Select whether you want to search for shared calendars by name or email address. |
    | Shared Calendars | Select the shared calendar(s) you want to import into Domo. |
    | Shared Users Email Address | Enter the email address of the user whose calendar you want to retrieve. |
    | File Type | Select whether the attachment you are importing is a CSV, XLS, XLSX, JSON, or XML file. |
    | File Name | Enter the name of the attachment you want to import. If the file name contains an extension, you must include the extension. |
    | Folder Path (Optional) | Enter the path of the folder containing messages you want to search. If you do not enter a folder path, the "Inbox" folder will be searched. For example, if the root folder was "DOMO" and in that root folder you had a folder called "DOMO\_TEST," you would enter the following to return messages for "DOMO\_TEST": DOMO/DOMO\_TEST |
    | File Password (Optional) | If the file you are uploading is password-protected, enter the password here. |
    | Delimiter | Select the delimiter to use in parsing CSV files. If your desired delimiter is not in the menu, select **Other**then enter the delimiter in the **Custom Delimiter** field. |
    | Custom Delimiter | Enter the custom delimiter for your CSV file. Enter \t for tab or Unicode characters in \uxxxx form (e.g. \u005e). |
    | Quote Character | Select the desired quote character for parsing your CSV file. A double quote (") is the CSV standard. |
    | Custom Quote Character | Enter the desired custom quote character. Enter \t for tab or Unicode characters in \uxxxx form (e.g. \u005e). |
    | Escape Character | Select the escape character to use in parsing CSV files. If your desired escape character is not in the menu, select **Other**then enter the escape character in the **Custom Escape Character** field. |
    | Custom Escape Character | Enter the custom escape character for your CSV file. Enter \t for tab or Unicode characters in \uxxxx form (e.g. \u005e). |
    | Start Days | Enter the number of days back you want to begin pulling data for. For example, if you entered 7 here, each time the report ran it would pull data from 7 days ago up until today. |
    | Header Start Row (Optional) | Enter the header row number. If you do not enter a number, the first row is considered the header row. |
    | Data Start Row (Optional) | Enter the starting data row number. If you do not enter a number, the first row after the header row is considered the starting row. |
    | Footer Rows to Skip (Optional) | If there are one or more rows at the bottom of the file you do not want to import, enter the number of rows to skip. |
    | Sheet Name (Optional) | If the Excel file you are uploading is found on a specific sheet, enter the sheet name here. If you do not enter a sheet name, the first sheet of the workbook will be used. |
    | Date Field Key | Enter the data tag in your JSON or XML file. |
    | Sublist to Flatten | Enter the lists you want to flatten, separated by commas. |
    | Report Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date | Select the date for the report. |
    | Days Back | Enter the number of past days that should appear in the report. |
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |
    | Select Specific End Date | Select the second date in your date range. |
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Report Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date | Select the date for the report. |
    | Days Back | Enter the number of past days that should appear in the report. |
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |
    | Select Specific End Date | Select the second date in your date range. |
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    

==============================================================================
FILE:   s/article/360042932474.mdx
TITLE:  SFTP Writeback Connector
LANG:   English (en_US)
ID:     360042932474
==============================================================================

CHANGE 1  [(top of file)]  (lines 5–6 in repo file)

  OLD (remove from repo):
    
    

CHANGE 2  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    Use this connector to export your data from a Domo DataSet to an SFTP server. To learn more about SFTP, visit the [SSH File Transfer Protocol Wikipedia page](https://en.wikipedia.org/wiki/SSH_File_Transfer_Protocol).

  NEW (replace with / insert from CSV):
    Use this connector to export your data from a Domo DataSet to an SFTP server. To learn more about SFTP, visit the Wikipedia page at [https://en.wikipedia.org/wiki/SSH\_Fi...nsfer\_Protocol](https://en.wikipedia.org/wiki/SSH_File_Transfer_Protocol).

CHANGE 3  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    You export data to an SFTP server in the Data Center. This topic discusses the fields and menus specific to the SFTP Writeback connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    You export data to an SFTP server in the Data Center. This topic discusses the fields and menus that are specific to the SFTP Writeback connector user interface. General information for adding DataSets, setting update schedules, and editing DataSet information is discussed in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [## Intro]  (lines 13–19 in repo file)

  OLD (remove from repo):
    <Note>**Note:** The owner of a writeback DataSet must also be an owner or co-owner of the input DataSet.</Note>
    
    
    
    ---
    
    

  NEW (replace with / insert from CSV):
    <Note>**Note:** The owner of a writeback dataset must also be an owner or co-owner of the input dataset.</Note>

CHANGE 5  [## Prerequisites]  (line 23 in repo file)

  OLD (remove from repo):
    To configure this connector, you need the following:

  NEW (replace with / insert from CSV):
    To configure this connector, you will need the following:

CHANGE 6  [## Prerequisites]  (lines 28–29 in repo file)

  OLD (remove from repo):
      <Warning>**Important:** The SFTP server hostname must have `sftp://` at the beginning of the URL and must not end with a forward slash (`/`).</Warning>
    

  NEW (replace with / insert from CSV):
    
    <Warning>**Important:** The SFTP Server host name must have **sftp://** in the beginning of the URL and should not end with a **front slash (/)**.

CHANGE 7  [## Prerequisites]  (line 33 in repo file)

  OLD (remove from repo):
    - The passphrase for your SSH private key.

  NEW (replace with / insert from CSV):
    - The passphrase for your SSH private key.</Warning>

CHANGE 8  [## Prerequisites]  (line 35 in repo file)

  OLD (remove from repo):
    <Note>**Note:** Domo does not support SSH keys generated using `ssh-keygen`. The SSH keys must be DES or RSA keys (in PEM format) generated by OpenSSL.</Note>

  NEW (replace with / insert from CSV):
    **Note: Domo does not support the SSH keys generated using ssh-keygen. The SSH keys need to be the DES or RSA keys (in PEM format) generated by OpenSSL.**

CHANGE 9  [## Prerequisites]  (line 37 in repo file)

  OLD (remove from repo):
    You can convert an OpenSSH private key to the old PEM format using the `ssh-keygen` utility:

  NEW (replace with / insert from CSV):
    OpenSSH private key format can be converted using ssh-keygen utility to the old PEM format.

CHANGE 10  [## Prerequisites]  (line 40 in repo file)

  OLD (remove from repo):
    ssh-keygen -p -m PEM -f ~/.ssh/id_rsa

  NEW (replace with / insert from CSV):
     ssh-keygen -p -m PEM -f ~/.ssh/id_rsa

CHANGE 11  [## Prerequisites]  (line 43 in repo file)

  OLD (remove from repo):
    This updates the same file with the RSA key.

  NEW (replace with / insert from CSV):
    This will update the same file with the RSA key.

CHANGE 12  [## Prerequisites]  (line 45 in repo file)

  NEW (replace with / insert from CSV):
    ### To obtain your Domo Client ID and Client Secret, do the following:

CHANGE 13  [## Prerequisites]  (line 46 in repo file)

  NEW (replace with / insert from CSV):
    1. Login to [Domo developer account](https://developer.domo.com/login)
    2. In the top right corner under **My Account,** click **New Client.**
    3. Enter the application name and description.
    4. Provide the application scope by selecting **Account**, **Data**, and **User**.
    5. Click **Create.**
    6. Once you have created a client, you can manage the client by clicking on **Manage Client.**
    7. Your **Client Secret** will appear in the **Manage Client** section.

CHANGE 14  [## Prerequisites]  (line 47 in repo file)

  OLD (remove from repo):
    ### Obtain Your Domo Client ID and Client Secret

  NEW (replace with / insert from CSV):
    ## Configuring the Connection

CHANGE 15  [### Obtain Your Domo Client ID and Client Secret]  (lines 49–67 in repo file)

  OLD (remove from repo):
    1. Log in to your [Domo developer account](https://developer.domo.com/login).
    
    2. In the top right corner under **My Account**, select **New Client**.
    
    3. Enter the application name and description.
    
    4. Provide the application scope by selecting **Account**, **Data**, and **User**.
    
    5. Select **Create**.
    
       After you create a client, you can manage the client by selecting **Manage Client**. Your **Client Secret** appears in the **Manage Client** section.
    
    
    
    ## Configure the Connection
    
    This section lists the options in the **Credentials** and **Details** panes on the SFTP Writeback Connector page. The components of the other panes on this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).
    
    

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the SFTP Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 16  [### Credentials Pane]  (line 71 in repo file)

  OLD (remove from repo):
    This pane contains fields for entering credentials to connect to your Domo developer account as well as your SFTP server. Depending on the configuration of the SFTP server, you may need a Private Key and Passphrase, username and password, or both. The following table describes what is needed for each field:

  NEW (replace with / insert from CSV):
    This pane contains fields for entering credentials to connect to your Domo developer account as well as your SFTP server. Depending on the configuration of the SFTP server, you may need either a Private Key and Passphrase, username and password, or both. The following table describes what is needed for each field:

CHANGE 17  [### Credentials Pane]  (lines 73–82 in repo file)

  OLD (remove from repo):
    | Field              | Description                                                                                                                                                           |
    | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | Domo Client ID     | Enter your Domo Client ID.                                                                                                                                            |
    | Domo Client Secret | Enter your Domo Client Secret.                                                                                                                                        |
    | Hostname           | Enter your SFTP server hostname. **Important:** The SFTP server hostname must have `sftp://` at the beginning of the URL and must not end with a forward slash (`/`). |
    | Port               | Enter your SFTP server port number.                                                                                                                                   |
    | Username           | Enter your SFTP server username.                                                                                                                                      |
    | Password           | Enter your SFTP server password.                                                                                                                                      |
    | SSH Private Key    | Enter your SFTP server SSH private key (RSA).                                                                                                                         |
    | Passphrase         | Enter the passphrase for your SSH private key.                                                                                                                        |

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domo Client ID | Enter your Domo client ID. |
    | Domo Client Secret | Enter your Domo client secret. |
    | Hostname | Enter your SFTP server hostname.
    <Warning>**Important:** The SFTP Server host name must have **sftp://** in the beginning of the URL and should not end with a **front slash (/)**. |
    | Port | Enter your SFTP server port number. |
    | Username | Enter your SFTP server username. |
    | Password | Enter your SFTP server password. |
    | SSH Private Key | Enter your SFTP server SSH private key (RSA). |
    | Passphrase | Enter the passphrase for your SSH private key. |</Warning>

CHANGE 18  [### Credentials Pane]  (lines 84–86 in repo file)

  OLD (remove from repo):
    After you enter valid credentials, you can use the same account any time you set up a new Domo-SFTP connection. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).
    
    

  NEW (replace with / insert from CSV):
    Once you have entered valid credentials, you can use the same account any time you go to set up a new Domo-SFTP connection. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 19  [### Details Pane]  (lines 92–113 in repo file)

  ⚠ LARGE CHANGE (43 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    | Menu                         | Description                                                                                                                                                                                                                                                                                                                                                                                                                    |
    | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
    | Input DataSet ID             | Enter the DataSet ID (GUID) for the DataSet you want to copy to the SFTP server. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following `datasources/`. For example, in the URL `https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview`, the DataSet ID is `845305d8-da3d-4107-a9d6-13ef3f86d4a4`. **Note:** You must either own this DataSet or have the Admin role in Domo. |
    | Path                         | Enter the path in your SFTP server where you want the data to be saved.                                                                                                                                                                                                                                                                                                                                                        |
    | Select the Filename          | Select the output filename. If you choose **Use Input Dataset Name** and your DataSet name contains the macro `<current_date>`, the connector replaces `<current_date>` with today's date in the format selected from the **Date Format** dropdown.                                                                                                                                                                            |
    | Filename                     | Enter the output filename for your data. To add the current date to the filename, use the `<current_date>` macro. For example, if the filename is `domo_<current_date>`, the output file generated in SFTP is `domo_` followed by today's date in the format selected from the **Date Format** dropdown. If the Date Format is set to `MMddyyyy_HHmmss`, the output file generated is `domo_05162021_161244`. You can also fetch data from any number of days back from the current date. For example, to fetch yesterday's data, enter the filename as `domo_<current_date-1>`. Other examples: `domo_<current_date-2>` fetches data from two days ago; `domo_<current_date-3>` fetches data from three days ago. **Note:** Your output file is stored as a CSV file. You do not need to add the extension to your filename. |
    | Date Format                  | Select the date format for the macro replacement in the filename. This option is only applicable when the **Filename** or **Input Dataset Name** contains `<current_date>`.                                                                                                                                                                                                                                                    |
    | Include Advanced Functions   | Select this checkbox to include the Advanced Functions.                                                                                                                                                                                                                                                                                                                                                                        |
    | File Extension               | This field appears when you select the **Include Advanced Functions** checkbox. Select the extension you want to use for your output file.                                                                                                                                                                                                                                                                                     |
    | Enter File Extension         | This field appears when you select the **Include Advanced Functions** checkbox and set **File Extension** to **User Defined Extension**. Enter the file extension.                                                                                                                                                                                                                                                             |
    | Delimiter                    | This field appears when you select the **Include Advanced Functions** checkbox. Select the character to be used as a delimiter for your output file.                                                                                                                                                                                                                                                                           |
    | Enter Delimiter              | This field appears when you select the **Include Advanced Functions** checkbox and set **Delimiter** to **Other**. Enter the character to be used as a delimiter for your output file.                                                                                                                                                                                                                                         |
    | Quote Character              | This field appears when you select the **Include Advanced Functions** checkbox. Select the character to be used for enclosing values in your output file.                                                                                                                                                                                                                                                                      |
    | Choose the Escape Character  | This field appears when you select the **Include Advanced Functions** checkbox. Select the escape character for the input DataSet.                                                                                                                                                                                                                                                                                             |
    | Enter Quote Character        | This field appears when you select the **Include Advanced Functions** checkbox and set **Quote Character** to **Other**. Enter the character to be used as a quote character for your output file.                                                                                                                                                                                                                             |
    | Line Feed Delimiter          | This field appears when you select the **Include Advanced Functions** checkbox. Select the character to be used as a line feed delimiter in your output file.                                                                                                                                                                                                                                                                  |
    | Enter Line Feed Delimiter    | This field appears when you select the **Include Advanced Functions** checkbox and set **Line Feed Delimiter** to **Other**. Enter the character to be used as a line feed delimiter for your output file.                                                                                                                                                                                                                     |
    | File Encoding                | This field appears when you select the **Include Advanced Functions** checkbox. Select the encoding scheme for your output file.                                                                                                                                                                                                                                                                                               |
    | Skip Headers                 | Select this checkbox to skip the header of your output file.                                                                                                                                                                                                                                                                                                                                                                   |
    | My Domo Input DataSet is very large. | Select this checkbox to export a very large DataSet from Domo to SFTP. When this checkbox is selected, the connector uses a different and faster process. The export process does not support Advanced Functions.                                                                                                                                                                                                       |
    
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Input DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to the SFTP server. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.
    <Note>**Note:** You must either own this DataSet or have the Admin role in Domo. |
    | Path | Enter the path in your SFTP server where you would like the data to be saved. |
    | Select the Filename | Select the output filename.  If you choose **Use Input Dataset Name** and your dataset name contains a macro <current\_date>, then the connector will replace <current\_date> with today's date in the format selected from the **Date Format** dropdown. |
    | Filename | Enter the output file name for your data.  If you wish to add the current date to the file name, then use the <current\_date> macro. For example: If the file name is **domo\_<current\_date>**, then the output file generated in SFTP would be domo\_(today's date in the format selected from the **Date Format** dropdown). If the Date Format is selected as **MMddyyyy\_HHmmss**, the output file generated would be **domo\_05162021\_161244**.  You also have the option to fetch the data any number of days back from the current date.  For example, if you want to fetch yesterday's data, then you need to enter the file name as **domo\_<current\_date-1>**, and the format of the output file generated in SFTP would be domo\_date format selected for previous day from Date Format drop-down.   - **domo\_<current\_date-2>** -fetches the data the day before yesterday. - **domo\_<current\_date-3>** - fetches the data two day's before with respect to the current date.   **Note:** Your output file is stored as a CSV file. You do not need to add the extension onto your file name. |
    | Date Format | Select the date format for the macro replacement in the filename. This option is only applicable when the 'File Name' or 'Input Dataset Name' contains &ltcurrent\_date&gt. |
    | Include Advanced Function | Select this checkbox to include the Advanced Functions. |
    | File Extension | This field appears when you select the **Include Advanced Functions** checkbox.  Select the extension you want to use for your output file. |
    | Enter File Extension | This field appears when you select the **Include Advanced Functions** checkbox and the File Extension as **User Defined Extension**.  Enter the file extension. |
    | Delimiter | This field appears when you select the **Include Advanced Functions** checkbox.  Select the character to be used as a delimiter for your output file. |
    | Enter Delimiter | This field appears when you select the **Include Advanced Functions** checkbox and the Delimiter as **Other**.  Enter the character to be used as a delimiter for your output file. |
    | Quote Character | This field appears when you select the **Include Advanced Functions** checkbox.  Select the character to be used for enclosing values in your output file. |
    | Choose the Escape Character | This field appears when you select the **Include Advanced Functions** checkbox.  Select the escape character for the input dataset. |
    | Quote Character | This field appears when you select the **Include Advanced Functions** checkbox and the Quote Character as **Other**.  Enter the character to be used as a quote character for your output file. |
    | Line Feed Delimiter | This field appears when you select the **Include Advanced Functions** checkbox.  Select the character to be used as a line feed delimiter in your output file. |
    | Line Feed Delimiter | This field appears when you select the **Include Advanced Functions** checkbox and the Line Field Delimiter as **Other**.  Enter the character that should be used as a line feed delimiter for your output file. |
    | File Encoding | This field appears when you select the **Include Advanced Functions** checkbox.  Select the encoding scheme for your output file. |
    | Skip Headers | Select this checkbox if you want to skip the header of your output file. |
    | My Domo Input DataSet is very large. | Select this checkbox to export a very large DataSet from Domo to SFTP. When this checkbox is selected, the connector uses a different and faster process. The export process does not support Advanced functions. |</Note>

CHANGE 20  [### Other Panes]  (line 117 in repo file)

  OLD (remove from repo):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    For information about the remaining sections of the connector interface, including how to configure scheduling, retry, and update options, see [Adding a DataSet Using a Data Connector](/s/article/360042926274).

==============================================================================
FILE:   s/article/360042932514.mdx
TITLE:  Smartsheet Writeback Connector
LANG:   English (en_US)
ID:     360042932514
==============================================================================

CHANGE 1  [## Prerequisites]  (line 21 in repo file)

  OLD (remove from repo):
    3. Create a new client with Application Scopes of **Data** and ** User**.

  NEW (replace with / insert from CSV):
    3. Create a new client with Application Scopes of **Data** and **User**.

CHANGE 2  [## Configuring the Connection]  (line 27 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Smartsheet Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Smartsheet Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 33–50 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID. For steps on obtaining your client ID and client secret, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID. For steps on obtaining your client ID and client secret, see "Prerequisites," above.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domo Client ID | Enter your Domo client ID. For steps on obtaining your client ID and client secret, see "Prerequisites," above. |
    | Domo Client Secret | Enter your Domo client ID. For steps on obtaining your client ID and client secret, see "Prerequisites," above. |
    

CHANGE 4  [### Details Pane]  (lines 57–106 in repo file)

  ⚠ LARGE CHANGE (63 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Where would you like to write this file?</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to send your Domo DataSet data to a Smartsheet workspace or folder.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Folder</p></td>
          <td colspan="1" rowspan="1"><p>Select the Smartsheet folder where you want to copy your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Folder ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Folder Id in which you want to store the data. To fetch the Folder Id for any folder, refer to the  <b><i>Retrieving a Folder ID from SmartSheet UI</b></i> section.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Workspace</p></td>
          <td colspan="1" rowspan="1"><p>Select the Smartsheet workspace where you want to copy your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Overwrite settings</p></td>
          <td colspan="1" rowspan="1"><p>Select, if you want to create Smartsheet for each run or overwrite the existing Smartsheet for each run.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the DataSet ID (GUID) for the DataSet you want to copy to a Smartsheet folder or workspace. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Filename</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to choose the file where data will be copied.   - <b>Use Input DataSet GUID</b>. The filename will be the number you entered for <b> Domo DataSet ID</b>. - <b> Use Input DataSet Name</b>. The filename will be the same as that of the input DataSet. - <b> Enter File Name</b>. You will give the file a custom name in the <b> File Name</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Primary Column</p></td>
          <td colspan="1" rowspan="1"><p>Enter the column name exactly as it appears in the Domo DataSet. The primary key must be a single column, and the DataType is not editable.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Filename</p></td>
          <td colspan="1" rowspan="1"><p>Enter the desired output filename.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Choose the Escape character</p></td>
          <td colspan="1" rowspan="1"><p>Select the escape character for the input dataset.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Where would you like to write this file? | Select whether you want to send your Domo DataSet data to a Smartsheet workspace or folder. |
    | Folder | Select the Smartsheet folder where you want to copy your data. |
    | Folder ID | Enter the Folder Id in which you want to store the data. To fetch the Folder Id for any folder, refer to the  ***Retrieving a Folder ID from SmartSheet UI*** section. |
    | Workspace | Select the Smartsheet workspace where you want to copy your data. |
    | Overwrite settings | Select, if you want to create Smartsheet for each run or overwrite the existing Smartsheet for each run. |
    | Domo DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to a Smartsheet folder or workspace. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4. |
    | Select Filename | Select how you want to choose the file where data will be copied.   - ****Use Input DataSet GUID****. The filename will be the number you entered for ****Domo DataSet ID****. - ****Use Input DataSet Name****. The filename will be the same as that of the input DataSet. - ****Enter File Name****. You will give the file a custom name in the ****File Name**** field. |
    | Primary Column | Enter the column name exactly as it appears in the Domo DataSet. The primary key must be a single column, and the DataType is not editable. |
    | Filename | Enter the desired output filename. |
    | Choose the Escape character | Select the escape character for the input dataset. |
    

==============================================================================
FILE:   s/article/360042932914.mdx
TITLE:  InContact Echo Connector
LANG:   English (en_US)
ID:     360042932914
==============================================================================

CHANGE 1  [## Steps to Connect]  (line 26 in repo file)

  OLD (remove from repo):
          2. Leave the **Username** and ** Password** fields blank.

  NEW (replace with / insert from CSV):
          2. Leave the **Username**and **Password**fields blank.

CHANGE 2  [## Steps to Connect]  (line 32 in repo file)

  OLD (remove from repo):
          1. In the **What is the connection method to access the JSON file?** dropdown, select ** HTTP Request**.

  NEW (replace with / insert from CSV):
          1. In the **What is the connection method to access the JSON file?** dropdown, select **HTTP Request**.

CHANGE 3  [## Steps to Connect]  (lines 42–43 in repo file)

  OLD (remove from repo):
    - In the **Do you require additional options?** dropdown, select ** Yes**.
    - In the **HTTPS Headers** field, enter the API key received from InContact.

  NEW (replace with / insert from CSV):
    - In the **Do you require additional options?** dropdown, select **Yes**.
    - In the **HTTPS Headers**field, enter the API key received from InContact.

CHANGE 4  [## Best Practices]  (line 55 in repo file)

  OLD (remove from repo):
    - The primary use of this API is to collect survey data. The endpoint for surveys is the [https://echo.incontact.com/company/api/echoapi.taf?classname=surveys&method=getSurveyResults&p1=101](https://echo.incontact.com/company/api/echoapi.taf?classname=surveys&method=getSurveyResults&p1=101) example. This is explained in the API documentation in the **SURVEYS/getSurveyResults** section. It is possible to grab multiple surveys in one API pull; this is explained in the ** Requests** section of the API documentation.

  NEW (replace with / insert from CSV):
    - The primary use of this API is to collect survey data. The endpoint for surveys is the [https://echo.incontact.com/company/api/echoapi.taf?classname=surveys&method=getSurveyResults&p1=101](https://echo.incontact.com/company/api/echoapi.taf?classname=surveys&method=getSurveyResults&p1=101) example. This is explained in the API documentation in the **SURVEYS/getSurveyResults** section. It is possible to grab multiple surveys in one API pull; this is explained in the **Requests** section of the API documentation.

==============================================================================
FILE:   s/article/360043431133.mdx
TITLE:  Google Ad Manager Connector
LANG:   English (en_US)
ID:     360043431133
==============================================================================

CHANGE 1  [## Connecting to Your Google Ad Manager Account]  (line 17 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Google Ad Manager Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet,** are universal across most Connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Google Ad Manager Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most Connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 2  [### Credentials Pane]  (line 21 in repo file)

  OLD (remove from repo):
    The Domo Google Ad Manager Connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select ** Add Account** if you have existing Google Ad Manager accounts in Domo) to open the Google OAuth screen where you can enter your Google credentials. Once you have entered valid Google credentials, you can use the same account any time you go to create a new Google DataSet. You can manage Connector accounts in the ** Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    The Domo Google Ad Manager Connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Google Ad Manager accounts in Domo) to open the Google OAuth screen where you can enter your Google credentials. Once you have entered valid Google credentials, you can use the same account any time you go to create a new Google DataSet. You can manage Connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

CHANGE 3  [### Credentials Pane]  (line 23 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you are already logged into Google when you connect in Domo, you are authenticated automatically when you click ** Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Google.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into Google when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Google.</Note>

CHANGE 4  [### Details Pane]  (lines 29–234 in repo file)

  ⚠ LARGE CHANGE (258 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Google Ad Manager report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Network Code</p></td>
          <td colspan="1" rowspan="1"><p>Select the network you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>AdUnit ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID for the AdUnit object you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>AdUnit Name (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the AdUnit object you want to retrieve information for (255-character max).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>AdUnit Code (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the code for the AdUnit object you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>AdUnit Status (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select a status to filter by, either <b>Any Value</b>, <b> Active, Archived,</b> or <b> Inactive</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Last Modified Date (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select a last modified date in the date picker to filter by.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Company ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the company you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Company Name (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the company you want to retrieve information for. This field supports % wildcard characters.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Company Type (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the type of company you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the list item you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Group ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the group you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Name (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the list item you want to retrieve information for. This field supports % wildcard characters.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Order IDs (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter a comma-separated list of order IDs to retrieve information for. For example: 1234,9876,4565</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>External ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the external ID of the list item you want to retrieve information for. This field supports % wildcard characters.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Is Missing Creatives (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select <b>Yes</b> if the content you want to retrieve information for is missing creatives.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Is Set Stop Box Enabled (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select <b>Yes</b> if the set stop box is enabled for the content you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Delivery Rate Type (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the delivery rate type for the content you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Cost Type (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the cost type for the content you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Line Item Type (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the line item type for the content you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Status (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the status for the content you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Creation Date Time (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the date of creation in the date picker for the content you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Duration (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to pull data for a specific date, a date range, or not at all.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Date (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the date for the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that should appear in the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b>End Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in <b>Start Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Start Date (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific End Date (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the second date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to Start From (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the farthest day back that should be represented in the report. Combine with <b>Days Back to End At</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to End At (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the most recent day back that should be represented in the report. Combine with <b>Days Back to Start From</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Order ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the Order object you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Order Name (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the Order object you want to retrieve information for. This field supports % wildcard characters.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Advertiser ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the advertiser you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Salesperson ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the salesperson you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Trafficker ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the trafficker you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Order Status (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select an order status to filter by.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Saved Query (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the saved query you want to run.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Impressions Look Back (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days (1 to 30) for which you want to retrieve view-through conversions for users who have viewed a DFP ad.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Clicks Look Back (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days (1 to 30) for which you want to retrieve click-through conversions for users who have clicked on a DFP ad.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Priority (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the priority value for which you want to retrieve data. You can enter any value between 1 and 1000, with 1 being the highest priority.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Rate Card ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the rate card you want to retrieve data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Master Creative ID (optional)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the master creative you want to retrieve data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ordering</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want your wrappers to be ordered.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Entity Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the type of entity you want to retrieve data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Visibility</p></td>
          <td colspan="1" rowspan="1"><p>Select the visibility filter you want for your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Refresh Rate</p></td>
          <td colspan="1" rowspan="1"><p>Select the refresh rate you want to retrieve data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Direction</p></td>
          <td colspan="1" rowspan="1"><p>Select the direction for your data.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Google Ad Manager report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Ad Rule By Statement | Fetches the AdRule that satisfies the given query. | | AdUnit By Statement | Fetches the AdUnits that satisfies the given query. | | All Networks | Returns the list of Network objects to which the current login has access. | | Audience Segment By Statement | Fetches the Audience Segment that satisfies the given query. | | Cms Metadata Service | Returns the list of CMS metadata keys and values. | | Company By Statement | Fetches the Companies that satisfies the given query. | | Contacts By Statement | Fetches the Contacts that satisfies the given query. | | Contents By Statement | Fetches the Contents that satisfies the given query. | | Creative By Statement | Fetches the Creatives that satisfies the given query. | | Creative Set By Statement | Fetches the CreativeSet that satisfies the given query. | | Creative Templates By Statement | Fetches the CreativeTemplates that satisfies the given query. | | Creative Wrappers By Statement | Fetches the CreativeWrappers that satisfies the given query. | | Custom Fields By Statement | Fetches the CustomFields that satisfies the given query. | | Custom Targeting Keys By Statement | Fetches the CustomTargetingKeys that satisfies the given query. | | Labels By Statement | Fetches the Labels that satisfies the given query. | | Line Item Templates By Statement | Fetches the Line Item Templates that satisfies the given query. | | Line Items By Statement | Fetches Line Items that satisfies the given query. | | Line Items By Statement With Targeting Details | Fetches the Line Items that satisfies the given query. | | Line Items Creative Associations By Statement | Fetches the LineItems Creative Asso. that satisfies the given query. | | Live Stream Events By Statement | Fetches the Live Stream Events that satisfies the given query. | | Mobile Applications By Statement | Fetches the Mobile Applications that satisfies the given query. | | Native Styles By Statement | Fetches the Native Styles that satisfies the given query. | | Orders By Statement | Fetches the Orders that satisfies the given query. | | Placements By Statement | Fetches the Placements that satisfies the given query. | | Proposal Line Items By Statement | Fetches the Proposal Line Items that satisfies the given query. | | Proposals By Statement | Fetches the Proposals that satisfies the given query. | | Saved Query | Using the ReportService, a saved query is run and downloaded. | | Suggested AdUnits By Statement | Fetches the Suggested AdUnits that satisfies the given query. | | Team By Statement | Fetches Teams that satisfies the given query. | | User Team Associations By Statement | Fetches the User Team Asso. that satisfies the given query. | | Users By Statement | Fetches the Users that satisfies the given query. | |
    | Network Code | Select the network you want to retrieve information for. |
    | AdUnit ID (optional) | Enter the ID for the AdUnit object you want to retrieve information for. |
    | AdUnit Name (optional) | Enter the name of the AdUnit object you want to retrieve information for (255-character max). |
    | AdUnit Code (optional) | Enter the code for the AdUnit object you want to retrieve information for. |
    | AdUnit Status (optional) | Select a status to filter by, either **Any Value**, **Active, Archived,** or **Inactive**. |
    | Last Modified Date (optional) | Select a last modified date in the date picker to filter by. |
    | Company ID (optional) | Enter the ID of the company you want to retrieve information for. |
    | Company Name (optional) | Enter the name of the company you want to retrieve information for. This field supports % wildcard characters. |
    | Company Type (optional) | Select the type of company you want to retrieve information for. |
    | ID (optional) | Enter the ID of the list item you want to retrieve information for. |
    | Group ID (optional) | Enter the ID of the group you want to retrieve information for. |
    | Name (optional) | Enter the name of the list item you want to retrieve information for. This field supports % wildcard characters. |
    | Order IDs (optional) | Enter a comma-separated list of order IDs to retrieve information for. For example: 1234,9876,4565 |
    | External ID (optional) | Enter the external ID of the list item you want to retrieve information for. This field supports % wildcard characters. |
    | Is Missing Creatives (optional) | Select **Yes** if the content you want to retrieve information for is missing creatives. |
    | Is Set Stop Box Enabled (optional) | Select **Yes** if the set stop box is enabled for the content you want to retrieve. |
    | Delivery Rate Type (optional) | Select the delivery rate type for the content you want to retrieve. |
    | Cost Type (optional) | Select the cost type for the content you want to retrieve. |
    | Line Item Type (optional) | Select the line item type for the content you want to retrieve. |
    | Status (optional) | Select the status for the content you want to retrieve. |
    | Creation Date Time (optional) | Select the date of creation in the date picker for the content you want to retrieve. |
    | Duration (optional) | Select whether you want to pull data for a specific date, a date range, or not at all. |
    | Report Date (optional) | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date (optional) | Select the date for the report. |
    | Days Back (optional) | Enter the number of past days that should appear in the report. |
    | Start Date (optional) | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date (optional) | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date (optional) | Select the first date in your date range. |
    | Select Specific End Date (optional) | Select the second date in your date range. |
    | Days Back to Start From (optional) | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At (optional) | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Order ID (optional) | Enter the ID of the Order object you want to retrieve information for. |
    | Order Name (optional) | Enter the name of the Order object you want to retrieve information for. This field supports % wildcard characters. |
    | Advertiser ID (optional) | Enter the ID of the advertiser you want to retrieve information for. |
    | Salesperson ID (optional) | Enter the ID of the salesperson you want to retrieve information for. |
    | Trafficker ID (optional) | Enter the ID of the trafficker you want to retrieve information for. |
    | Order Status (optional) | Select an order status to filter by. |
    | Saved Query (optional) | Select the saved query you want to run. |
    | Impressions Look Back (optional) | Enter the number of days (1 to 30) for which you want to retrieve view-through conversions for users who have viewed a DFP ad. |
    | Clicks Look Back (optional) | Enter the number of days (1 to 30) for which you want to retrieve click-through conversions for users who have clicked on a DFP ad. |
    | Priority (optional) | Enter the priority value for which you want to retrieve data. You can enter any value between 1 and 1000, with 1 being the highest priority. |
    | Rate Card ID (optional) | Enter the ID of the rate card you want to retrieve data for. |
    | Master Creative ID (optional) | Enter the ID of the master creative you want to retrieve data for. |
    | Ordering | Select how you want your wrappers to be ordered. |
    | Entity Type | Select the type of entity you want to retrieve data for. |
    | Visibility | Select the visibility filter you want for your data. |
    | Refresh Rate | Select the refresh rate you want to retrieve data for. |
    | Direction | Select the direction for your data. |
    

CHANGE 5  [### FAQs]  (line 237 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### I am receiving an error saying "ReportError.START\_DATE\_MORE\_THAN\_THREE\_YEARS\_AGO" while using the Saved Query report. Why?

CHANGE 6  [### FAQs]  (line 239 in repo file)

  OLD (remove from repo):
    <Accordion title="I am receiving an error saying "ReportError.START\_DATE\_MORE\_THAN\_THREE\_YEARS\_AGO" while using the Saved Query report. Why?">

CHANGE 7  [### FAQs]  (lines 244–377 in repo file)

  ⚠ LARGE CHANGE (167 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Report Name</p></th>
          <th colspan="1" rowspan="1"><p>Endpoint URL(s)</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Ad Rule By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/AdRuleService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ad Unit By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/InventoryService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>All Networks</p></td>
          <td colspan="1" rowspan="1"><p>/NetworkService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Audience Segment By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/AudienceSegmentService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>CMS Metadata Service</p></td>
          <td colspan="1" rowspan="1"><p>/CmsMetadataService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Company By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/CompanyService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contacts By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/ContactService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contents By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/ContentService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Creatives By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/CreativeService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Creative Set By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/CreativeSetService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Creative Templates By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/CreativeTemplateService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Creative Wrappers By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/CreativeWrapperService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Fields By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/CustomFieldService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Targeting Keys By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/CustomTargetingService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Labels By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/LabelService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Line Item Templates By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/LineItemTemplateService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Line Items By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/LineItemService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Line Items By Statement With Targeting Detail</p></td>
          <td colspan="1" rowspan="1"><p>/LineItemTargetingService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Line Items Creative Associations By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/LineItemCreativeAssociationService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Live Stream Events By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/LiveStreamEventService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Mobile Applications By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/MobileApplicationService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Native Styles By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/NativeStyleService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Orders By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/OrderService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Placements By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/PlacementService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Proposal Line Items By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/ProposalLineItemService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Proposals By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/ProposalService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Saved Query Download</p></td>
          <td colspan="1" rowspan="1"><p>/SavedQueryDownload</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Suggested AdUnits By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/SuggestedAdUnitService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Team By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/TeamService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>User Team Associations By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/UserTeamAssociationService</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Users By Statement</p></td>
          <td colspan="1" rowspan="1"><p>/UserService</p></td>
        </tr>
      </tbody>
    </table></Accordion>

  NEW (replace with / insert from CSV):
    | Report Name | Endpoint URL(s) |
    | --- | --- |
    | Ad Rule By Statement | /AdRuleService |
    | Ad Unit By Statement | /InventoryService |
    | All Networks | /NetworkService |
    | Audience Segment By Statement | /AudienceSegmentService |
    | CMS Metadata Service | /CmsMetadataService |
    | Company By Statement | /CompanyService |
    | Contacts By Statement | /ContactService |
    | Contents By Statement | /ContentService |
    | Creatives By Statement | /CreativeService |
    | Creative Set By Statement | /CreativeSetService |
    | Creative Templates By Statement | /CreativeTemplateService |
    | Creative Wrappers By Statement | /CreativeWrapperService |
    | Custom Fields By Statement | /CustomFieldService |
    | Custom Targeting Keys By Statement | /CustomTargetingService |
    | Labels By Statement | /LabelService |
    | Line Item Templates By Statement | /LineItemTemplateService |
    | Line Items By Statement | /LineItemService |
    | Line Items By Statement With Targeting Detail | /LineItemTargetingService |
    | Line Items Creative Associations By Statement | /LineItemCreativeAssociationService |
    | Live Stream Events By Statement | /LiveStreamEventService |
    | Mobile Applications By Statement | /MobileApplicationService |
    | Native Styles By Statement | /NativeStyleService |
    | Orders By Statement | /OrderService |
    | Placements By Statement | /PlacementService |
    | Proposal Line Items By Statement | /ProposalLineItemService |
    | Proposals By Statement | /ProposalService |
    | Saved Query Download | /SavedQueryDownload |
    | Suggested AdUnits By Statement | /SuggestedAdUnitService |
    | Team By Statement | /TeamService |
    | User Team Associations By Statement | /UserTeamAssociationService |
    | Users By Statement | /UserService |

CHANGE 8  [### FAQs]  (line 379 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?
    

CHANGE 9  [### FAQs]  (line 381 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 10  [### FAQs]  (line 383 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I know my credentials are secure?">

  NEW (replace with / insert from CSV):
    #### How do I know my credentials are secure?
    

CHANGE 11  [### FAQs]  (line 385 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 12  [### FAQs]  (line 387 in repo file)

  OLD (remove from repo):
    <Accordion title="Can I use the same account multiple times to create datasets?">

  NEW (replace with / insert from CSV):
    #### Can I use the same account multiple times to create datasets?
    

CHANGE 13  [### FAQs]  (line 389 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 14  [### FAQs]  (line 391 in repo file)

  OLD (remove from repo):
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?
    

CHANGE 15  [### FAQs]  (line 393 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 16  [### FAQs]  (line 395 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 17  [### FAQs]  (line 397 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 18  [### FAQs]  (line 399 in repo file)

  OLD (remove from repo):
    <Accordion title="Why are my reports showing as 'Not Compatible'?">

  NEW (replace with / insert from CSV):
    #### Why are my reports showing as "Not Compatible"?
    

CHANGE 19  [### FAQs]  (lines 401–403 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360043431633.mdx
TITLE:  Moz Connector
LANG:   English (en_US)
ID:     360043431633
==============================================================================

CHANGE 1  [## Intro]  (lines 13–42 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>This connector lets you optimize your most important web pages for searches.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Status - Page errors - Number of links - Page to domain</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>Digital marketing</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>1 hour</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>3</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector lets you optimize your most important web pages for searches. |
    | **Primary Metrics** | - Status - Page errors - Number of links - Page to domain |
    | **Primary Company Roles** | Digital marketing |
    | **Average Implementation Time** | 1 hour |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 3 |
    

CHANGE 2  [## Connecting to Your Moz Account]  (line 59 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Moz Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Moz Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 65–82 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Access ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Moz access ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Secret Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Moz secret key.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Access ID | Enter your Moz access ID. |
    | Secret Key | Enter your Moz secret key. |
    

CHANGE 4  [### Details Pane]  (lines 91–128 in repo file)

  ⚠ LARGE CHANGE (48 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Moz report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Web Page URL</p></td>
          <td colspan="1" rowspan="1"><p>Enter the URL of the web page for which you want to retrieve the metrics. For example: <a href="https://www.google.com">www.google.com</a>.  Enter any web page URL. For example: <a href="https://www.google.com">www.google.com</a>.  For the Fetch Multiple Site Metrics report, you may specify multiple web page URLs separated by commas. For example: WEB\_PAGE\_URL\_1,WEB\_PAGE\_URL\_2,...,WEB\_PAGE\_URL\_n.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Scope</p></td>
          <td colspan="1" rowspan="1"><p>Select the scope for which you want the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Locale</p></td>
          <td colspan="1" rowspan="1"><p>Select the locale for which you want the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Device</p></td>
          <td colspan="1" rowspan="1"><p>Select the device for which you want the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Engine</p></td>
          <td colspan="1" rowspan="1"><p>Select the engine for which you want the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Keyword</p></td>
          <td colspan="1" rowspan="1"><p>Enter the keyword to query.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Moz report you want to run. The following reports are available:     | Report Name | Description | | --- | --- | | Count Ranking Keywords | This report provides a summary of the number of keywords for which the provided query ranks in the top 50. | | Fetch All Metrics | This report retrieves the most recent difficulty, volume, organic CTR, and/or priority metrics for any keyword that exists in the database. | | Fetch Brand Authority | This report retrieves a brand Authority score for the provided site. | | Fetch Multiple Site Metrics | This report retrieves metrics such as Domain Authority, Page Authority, Spam Score, link counts, and more about one or more URLs | | Fetch Search Intent | This report retrieves the most recent search intent scores for any keyword that exists in the database. | | Get Top Ranking Domains | This report retrieves the top-ranking domains globally. | | Get Top Ranking Pages | This report retrieves the top ranking pages globally. | | List Ranking Keywords | This report retrieves a list of keywords for which the provided site ranks in the top 50, along with the ranking position and the difficulty and monthly search volume for each keyword. | | List Related Keywords | This report retrieves a list of related keyword suggestions for a given input keyword. | | List Top Pages | This report displays the top pages for a specified domain. | |
    | Web Page URL | Enter the URL of the web page for which you want to retrieve the metrics. For example: [www.google.com](https://www.google.com).  Enter any web page URL. For example: [www.google.com](https://www.google.com).  For the Fetch Multiple Site Metrics report, you may specify multiple web page URLs separated by commas. For example: WEB\_PAGE\_URL\_1,WEB\_PAGE\_URL\_2,...,WEB\_PAGE\_URL\_n. |
    | Scope | Select the scope for which you want the data. |
    | Locale | Select the locale for which you want the data. |
    | Device | Select the device for which you want the data. |
    | Engine | Select the engine for which you want the data. |
    | Keyword | Enter the keyword to query. |
    

==============================================================================
FILE:   s/article/360043432813.mdx
TITLE:  Encompass REST Connector
LANG:   English (en_US)
ID:     360043432813
==============================================================================

CHANGE 1  [## Prerequisites]  (line 20 in repo file)

  OLD (remove from repo):
    You can find your client ID inside Encompass by selecting **About Encompass** in the ** Help** menu and then About Encompass. You can also find it in the footer of any

  NEW (replace with / insert from CSV):
    You can find your client ID inside Encompass by selecting **About Encompass** in the **Help**menu and then About Encompass. You can also find it in the footer of any

CHANGE 2  [## Connecting to Your Encompass Account]  (line 25 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Encompass REST Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Encompass REST Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 31–60 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username for your Encompass account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password for your Encompass account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Instance ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Encompass instance ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the client ID for your Encompass OAuth app.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter the client secret for your Encompass OAuth app.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Username | Enter the username for your Encompass account. |
    | Password | Enter the password for your Encompass account. |
    | Instance ID | Enter your Encompass instance ID. |
    | Client ID | Enter the client ID for your Encompass OAuth app. |
    | Client Secret | Enter the client secret for your Encompass OAuth app. |
    

CHANGE 4  [### Details Pane]  (lines 67–156 in repo file)

  ⚠ LARGE CHANGE (112 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Encompass report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Loan ID Selection Type</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to choose your desired loan. The following options are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Duration</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to pull data for a specific date or a date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the date for the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that should appear in the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b>End Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in <b>Start Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific End Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the second date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to Start From</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the farthest day back that should be represented in the report. Combine with <b>Days Back to End At</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to End At</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the most recent day back that should be represented in the report. Combine with <b>Days Back to Start From</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Field to Flatten</p></td>
          <td colspan="1" rowspan="1"><p>Select the column in your report you want to flatten.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Loan ID (when your <b>Loan ID Selection Type</b> is set to <b> Discovery</b>)</p></td>
          <td colspan="1" rowspan="1"><p>Select the loan you want to retrieve data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Loan ID (when your <b>Loan ID Selection Type</b> is set to <b> Manual</b>)</p></td>
          <td colspan="1" rowspan="1"><p>Enter a comma-separated list of loan IDs you want to return data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Entities</p></td>
          <td colspan="1" rowspan="1"><p>Select the entities you want to retrieve data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contact Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the contact type you want to filter your data by.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Group Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the group type you want to filter your data by.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Group ID</p></td>
          <td colspan="1" rowspan="1"><p>Select the group you want to retrieve data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partial Data Checkbox</p></td>
          <td colspan="1" rowspan="1"><p>Check this box if you would like your report to return partial data in the event that Encompass fails to return data for certain loans.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Encompass report you want to run. The following reports are available:   |  |  | | --- | --- | | All Borrower Pairs | Returns all borrower pairs for a specified loan. | | All Contact Groups | Returns all contact groups for a given contact type and group type, e.g. borrower, public business ,or private business contact groups. | | All Contacts in Contact Group | Returns all contacts associated with a specified group ID. | | All Custom Fields | Returns the schema for all loan custom fields on the Encompass instance. | | All Milestone-Free Roles | Returns all milestone-free logs for the specified loan. | | All Milestones | Returns all milestones for the specified loan. | | All Milestones V2 | The All Milestones V2 report returns all milestone records for a specified loan in Encompass. | | All Post-Closing Conditions | Returns all post-closing conditions for the specified loan. | | All Preliminary Conditions | Returns all preliminary conditions for the specified loan. | | All Underwriting Conditions | Returns all underwriting conditions for the specified loan. | | Borrower Canonical Names | Returns a list of canonical field names for borrower contact fields. | | Borrower Contact List | Returns borrower contacts. | | Business Canonical Names | Returns a list of canonical field names for business contact fields. | | Business Contact List | Returns a list of business contacts. | | List of Associates | Returns a list of loan associates involved with the specified loan. | | List of Attachments | Returns a list of files attached to a specified loan. | | List of Documents | Returns all eFolder documents for a specified loan file. | | List of Resource Locks | Returns a list of locked resources and metadata from Encompass. | | Loan | Returns the entire loan or specific entities of a loan. | | Loan Canonical Names | Returns a list of descriptive field definitions for all canonical field names represented in the Encompass Pipeline. | | V3 Loan Pipeline | The V3 Loan Pipeline report allows you to query the loan pipeline in Encompass. | | V3 Loan Pipeline With Date Filter | The V3 Loan Pipeline With Date Filter report allows filtering of loan data, including the use of date filters. | | V3 Pipeline Canonical Names | Returns a list of canonical field names for use with the Encompass Loan Pipeline. | |
    | Loan ID Selection Type | Select how you want to choose your desired loan. The following options are available:   |  |  | | --- | --- | | All | Returns all loans that have been modified in the specified time range. | | Discovery | Choose from a list of loans that have been modified in the last 90 days. | | Manual | Manually enter the IDs of the loans you are interested in. | |
    | Duration | Select whether you want to pull data for a specific date or a date range. |
    | Report Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date | Select the date for the report. |
    | Days Back | Enter the number of past days that should appear in the report. |
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |
    | Select Specific End Date | Select the second date in your date range. |
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Field to Flatten | Select the column in your report you want to flatten. |
    | Loan ID (when your **Loan ID Selection Type** is set to **Discovery**) | Select the loan you want to retrieve data for. |
    | Loan ID (when your **Loan ID Selection Type**is set to **Manual**) | Enter a comma-separated list of loan IDs you want to return data for. |
    | Entities | Select the entities you want to retrieve data for. |
    | Contact Type | Select the contact type you want to filter your data by. |
    | Group Type | Select the group type you want to filter your data by. |
    | Group ID | Select the group you want to retrieve data for. |
    | Partial Data Checkbox | Check this box if you would like your report to return partial data in the event that Encompass fails to return data for certain loans. |

==============================================================================
FILE:   s/article/360043432953.mdx
TITLE:  QuickBooks Online Reports Connector
LANG:   English (en_US)
ID:     360043432953
==============================================================================

CHANGE 1  [## Intro]  (lines 17–46 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>Pulling financial accounting reports.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Revenue - Costs - P&L line items - Balance sheet items</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>- Accountants - Purchasers - CFOs - Controllers</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>Report runtime will depend on how large the reports in QuickBooks are. Smaller date ranges will take less.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>3</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | Pulling financial accounting reports. |
    | **Primary Metrics** | - Revenue - Costs - P&L line items - Balance sheet items |
    | **Primary Company Roles** | - Accountants - Purchasers - CFOs - Controllers |
    | **Average Implementation Time** | Report runtime will depend on how large the reports in QuickBooks are. Smaller date ranges will take less. |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 3 |
    

CHANGE 2  [## Best Practices]  (line 49 in repo file)

  OLD (remove from repo):
    The connector should run on **Replace** at any reasonable schedule. ** Append** will cause duplicates.

  NEW (replace with / insert from CSV):
    The connector should run on **Replace** at any reasonable schedule. **Append** will cause duplicates.

CHANGE 3  [## Connecting to Your QuickBooks Online Reports Account]  (line 60 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the QuickBooks Online Reports Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the QuickBooks Online Reports Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (line 64 in repo file)

  OLD (remove from repo):
    The Domo QuickBooks Online Reports connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select ** Add Account** if you have existing QuickBooks Online accounts in Domo) to open the QuickBooks Online OAuth screen where you can enter your login credentials. After you log in using your credentials, you are requested to authorize Domo to access your QuickBooks Online data. Once you enter credentials and authorize Domo, you can use the same account any time you go to create a new QuickBooks Online Reports DataSet. You can manage connector accounts in the ** Accounts** tab in the Data Center. For more information about this tab, see [Creating and Managing User Groups](/s/article/360043438913).

  NEW (replace with / insert from CSV):
    The Domo QuickBooks Online Reports connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing QuickBooks Online accounts in Domo) to open the QuickBooks Online OAuth screen where you can enter your login credentials. After you log in using your credentials, you are requested to authorize Domo to access your QuickBooks Online data. Once you enter credentials and authorize Domo, you can use the same account any time you go to create a new QuickBooks Online Reports DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Creating and Managing User Groups](/s/article/360043438913).

CHANGE 5  [### Credentials Pane]  (line 66 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you are already logged into QuickBooks Online when you connect in Domo, you are authenticated automatically when you click ** Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of QuickBooks Online.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into QuickBooks Online when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of QuickBooks Online.</Note>

CHANGE 6  [### Details Pane]  (lines 72–153 in repo file)

  ⚠ LARGE CHANGE (103 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select a QuickBooks Online report. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Filter By</p></td>
          <td colspan="1" rowspan="1"><p>Select the date filter method.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Created Date Duration</p></td>
          <td colspan="1" rowspan="1"><p>Select the created date duration for the report (a Single Date, or a Date Range).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Status</p></td>
          <td colspan="1" rowspan="1"><p>Select account status.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Transaction Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the start date for the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Date</p></td>
          <td colspan="1" rowspan="1"><p>Select report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using the date picker, this option is only available when Report Date is set to 'Specific'</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Offset</p></td>
          <td colspan="1" rowspan="1"><p>This adjusts the current date the given number of days into the past. Example: 0 for today, 1 for yesterday.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Aging Method</p></td>
          <td colspan="1" rowspan="1"><p>Select aging method.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Per Aging Period</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days per aging period.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Number of Periods</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of aging periods to be shown in the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Due Date Duration</p></td>
          <td colspan="1" rowspan="1"><p>Select the due date duration for the report (a Single Date, or a Date Range).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Transaction Date Duration</p></td>
          <td colspan="1" rowspan="1"><p>Select the transaction date range for the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Accounting Method</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired accounting method.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Summarize Column By</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want your report data to be broken down. For example, if you select <b>Month</b>, your report data is broken down by month.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Receivable Paid Status</p></td>
          <td colspan="1" rowspan="1"><p>Select account receivable paid status.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Columns</p></td>
          <td colspan="1" rowspan="1"><p>Check this to be given an option, otherwise a default selection will be given. Schema will always be maintained, even if selections are changed. Note - this is for advanced users as QuickBooks may return errors based on column selection.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enable Pivot Parsing</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to pivot your report data by the selected <b>Summarize Column By</b> value.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select a QuickBooks Online report. The following reports are available:   | Report | Description | | --- | --- | | Account List | Shows the name, type, and balance for each account. | | Accounts Payable Aging Detail | Shows detailed information about unpaid bills in accounts payable. | | Accounts Payable Aging Summary | Summarizes the status of unpaid bills in accounts payable. | | Accounts Receivable Aging Detail | Shows detailed information about outstanding amounts in accounts receivable. | | Accounts Receivable Aging Summary | Shows accounts receivable, along with the amount of time the amounts have been outstanding. | | Balance Sheet | Summarizes assets, liabilities, and shareholders' equities | | Customer Balance Detail | Shows a detailed list of customer balances. | | Customer Balance Summary | Shows a summary of customer balances. | | Expenses by Vendor | Shows expenses by vendor. | | General Ledger | Shows all transactions that have occurred in each account, with the beginning balance and total for each account. | | General Ledger V2 | Displays all transactions, the beginning balance, and the total balance for each account. | | Income by Customer Summary | Shows income by customer. | | Inventory Valuation Summary | Shows the monetary value of items that make up the inventory. | | Profit and Loss Summary | Summarizes income and expenses. | | Profit and Loss Detail | Shows detailed information about income and expenses. | | Sales by Class Summary | Shows sales by class. | | Sales by Customer Summary | Shows sales by customer. | | Sales by Department Summary | Shows sales by department. | | Sales by Product/Service Summary | Shows sales by product or service. | | Statement of Cash Flows | Returns a cash flow statement. | | Trial Balance | Returns credit and debit information for all accounts. | | Vendor Balance Detail | Shows detailed information about vendor balances. | | Vendor Balance Summary | Lists vendor balances. | |
    | Date Filter By | Select the date filter method. |
    | Created Date Duration | Select the created date duration for the report (a Single Date, or a Date Range). |
    | Account Status | Select account status. |
    | Transaction Date | Select the start date for the report. |
    | Report Date | Select report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector |
    | Specific Date | Select the report date using the date picker, this option is only available when Report Date is set to 'Specific' |
    | Date Offset | This adjusts the current date the given number of days into the past. Example: 0 for today, 1 for yesterday. |
    | Aging Method | Select aging method. |
    | Days Per Aging Period | Enter the number of days per aging period. |
    | Number of Periods | Enter the number of aging periods to be shown in the report. |
    | Due Date Duration | Select the due date duration for the report (a Single Date, or a Date Range). |
    | Transaction Date Duration | Select the transaction date range for the report. |
    | Accounting Method | Select the desired accounting method. |
    | Summarize Column By | Select how you want your report data to be broken down. For example, if you select **Month**, your report data is broken down by month. |
    | Account Receivable Paid Status | Select account receivable paid status. |
    | Select Columns | Check this to be given an option, otherwise a default selection will be given. Schema will always be maintained, even if selections are changed. Note - this is for advanced users as QuickBooks may return errors based on column selection. |
    | Enable Pivot Parsing | Select whether you want to pivot your report data by the selected **Summarize Column By** value. |
    

==============================================================================
FILE:   s/article/360043432993.mdx
TITLE:  SAP Ariba Connector
LANG:   English (en_US)
ID:     360043432993
==============================================================================

CHANGE 1  [## Connecting to your SAP Ariba Account]  (line 22 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Sentry Connector page. The components of the other panes in this page, ** Scheduling,** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the ********Credentials******** and ********Details******** panes in the Sentry Connector page. The components of the other panes in this page, ********Scheduling,******** and ********Name & Describe Your DataSet********, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [## Credentials Pane]  (lines 28–61 in repo file)

  ⚠ LARGE CHANGE (43 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>API Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter API Key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter Client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter Client Secret.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Data Center</p></td>
          <td colspan="1" rowspan="1"><p>Select adata center from the list.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Environment</p></td>
          <td colspan="1" rowspan="1"><p>Select 'Production' or 'Sandbox' as the environment.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Realm</p></td>
          <td colspan="1" rowspan="1"><p>Enter Realm.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter API Key. |
    | Client ID | Enter Client ID. |
    | Client Secret | Enter Client Secret. |
    | Data Center | Select adata center from the list. |
    | Environment | Select 'Production' or 'Sandbox' as the environment. |
    | Realm | Enter Realm. |
    

CHANGE 3  [## Details Pane]  (lines 68–69 in repo file)

  OLD (remove from repo):
    | Report | Select the Ariba report you want to run. The following report is available: <table border="1"><tbody><tr><td><b>Report Name</b></td><td><b>Description</b></td></tr><tr><td>Analytical Reporting</td><td>Returns analytical reporting data for the selected analytical template name.</td></tr><tr><td>Analytics View Template</td><td>Returns all template views for the customer.</td></tr><tr><td>Transactional Procurement</td><td>Returns transactional procurement data to make operational decisions.</td></tr><tr><td>Views</td><td>Returns analytical reporting data, appropriate for smaller results sets.</td></tr></tbody></table> |
    | View Template Name | Select a View Template Name. <Tip>**Tip:** If you do not get any response for view template name, please check for realm provided.</Tip> |

  NEW (replace with / insert from CSV):
    | Report | Select the Ariba report you want to run. The following report is available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Analytical Reporting | Returns analytical reporting data for the selected analytical template name. | | Analytics View Template | Returns all template views for the customer. | | Transactional Procurement | Returns the transactional procurement data to make operational decisions. | | Transactional Procurement V2 | Returns the transactional procurement data to make operational decisions. | |
    | View Template Name | Select a View Template Name
    <Tip>**Tip:** If you do not get any response for view template name, please check for realm provided. |

CHANGE 4  [## Details Pane]  (lines 74–76 in repo file)

  OLD (remove from repo):
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |

  NEW (replace with / insert from CSV):
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in ****End Date****. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in ****Start Date****. |
    | Select Specific Start Date | Select the first date in your date range. |

CHANGE 5  [## Details Pane]  (lines 78–79 in repo file)

  OLD (remove from repo):
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days. For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days. For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |

  NEW (replace with / insert from CSV):
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with ****Days Back to End At**** to create a range of represented days.  For example, if you entered 10 for ****Days Back to Start From**** and 5 for ****Days Back to End At****, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with ****Days Back to Start From**** to create a range of represented days.  For example, if you entered 10 for ****Days Back to Start From**** and 5 for ****Days Back to End At****, the report would contain data for 10 days ago up until 5 days ago. |</Tip>

==============================================================================
FILE:   s/article/360043433033.mdx
TITLE:  Workday Connector
LANG:   English (en_US)
ID:     360043433033
==============================================================================

CHANGE 1  [## Intro]  (lines 11–36 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>Use this connector to pull DataSets related to...   - HR - Workforce planning - Recruiting - Talent management - Payroll</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>- CIO - CEO - HR roles</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>An hour or less</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>2</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | Use this connector to pull DataSets related to...   - HR - Workforce planning - Recruiting - Talent management - Payroll |
    | **Primary Company Roles** | - CIO - CEO - HR roles |
    | **Average Implementation Time** | An hour or less |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 2 |
    

CHANGE 2  [## Connecting to Your Workday Account]  (line 47 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Workday Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Workday Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 53–70 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Workday username.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Workday password.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Username | Enter your Workday username. |
    | Password | Enter your Workday password. |
    

CHANGE 4  [### Details Pane]  (lines 77–114 in repo file)

  ⚠ LARGE CHANGE (48 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter the URL to Your Workday XML Report</p></td>
          <td colspan="1" rowspan="1"><p>Enter the URL for your Workday or Simple XML Report. You must provide the URL along with the fully qualified path, including the report name.  For example:  https://com.workday.report/service/Report\_Name?format=simplexml</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Additional Parameters</p></td>
          <td colspan="1" rowspan="1"><p>Enter any additional query parameters that might be required to run the provided XML report here, separated by a new line. Example: <b>parameterName=value</b></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Fields to Exclude</p></td>
          <td colspan="1" rowspan="1"><p>Enter a comma-separated list of fields (columns) to exclude from your XML import, if any.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Your Sublisttoflatten</p></td>
          <td colspan="1" rowspan="1"><p>Enter the comma separated lists that you want to flatten out in your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Flatten data</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to bring data for all duplicate elements.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Reset Schema</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to reset the schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Merge Keys</p></td>
          <td colspan="1" rowspan="1"><p>Enter the comma separated lists of keys that will be used to merge your data. This field is required if you are using the <b>Merge Update</b> mode.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Enter the URL to Your Workday XML Report | Enter the URL for your Workday or Simple XML Report. You must provide the URL along with the fully qualified path, including the report name.  For example:  https://com.workday.report/service/Report\_Name?format=simplexml |
    | Additional Parameters | Enter any additional query parameters that might be required to run the provided XML report here, separated by a new line. Example: **parameterName=value** |
    | Enter Fields to Exclude | Enter a comma-separated list of fields (columns) to exclude from your XML import, if any. |
    | Enter Your Sublisttoflatten | Enter the comma separated lists that you want to flatten out in your data. |
    | Flatten data | Select this checkbox to bring data for all duplicate elements. |
    | Reset Schema | Select this checkbox to reset the schema. |
    | Merge Keys | Enter the comma separated lists of keys that will be used to merge your data. This field is required if you are using the **Merge Update** mode. |
    

CHANGE 5  [## Troubleshooting]  (line 127 in repo file)

  OLD (remove from repo):
    3. Click either the **Workday XML** or ** Simple XML link**.

  NEW (replace with / insert from CSV):
    3. Click either the **Workday XML** or **Simple XML link**.

==============================================================================
FILE:   s/article/360043433453.mdx
TITLE:  NetSuite SuiteAnalytics Connect Connector
LANG:   English (en_US)
ID:     360043433453
==============================================================================

CHANGE 1  [## Intro]  (lines 26–55 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>Use this connector to pull data from tables you access through NetSuite using custom SQL queries.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Revenue - Cost - Projected Revenue - Customer/Lead Dates</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>- Finance (P&L data) - Sales managers</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>5-10 hours</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>6</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | Use this connector to pull data from tables you access through NetSuite using custom SQL queries. |
    | **Primary Metrics** | - Revenue - Cost - Projected Revenue - Customer/Lead Dates |
    | **Primary Company Roles** | - Finance (P&L data) - Sales managers |
    | **Average Implementation Time** | 5-10 hours |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 6 |
    

CHANGE 2  [## Prerequisites]  (line 73 in repo file)

  OLD (remove from repo):
    2. Click **Set Up SuiteAnalytics Connect**, which is located under ** Settings** in the bottom left corner of the screen.

  NEW (replace with / insert from CSV):
    2. Click **Set Up SuiteAnalytics Connect**, which is located under **Settings** in the bottom left corner of the screen.

CHANGE 3  [## Connecting to NetSuite SuiteAnalytics Connect Connector]  (line 84 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the NetSuite SuiteAnalytics Connect Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the NetSuite SuiteAnalytics Connect Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 90–127 in repo file)

  ⚠ LARGE CHANGE (48 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Service Host</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of your NetSuite Service Host. For more information, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Service Port</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite Service Port number. For more information, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Service Data Source</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of your NetSuite Service Data Source. For more information, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite username.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite password.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite Role ID number. For more information, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Account ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite Account ID number. For more information, see "Prerequisites," above.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Service Host | Enter the name of your NetSuite Service Host. For more information, see "Prerequisites," above. |
    | Service Port | Enter your NetSuite Service Port number. For more information, see "Prerequisites," above. |
    | Service Data Source | Enter the name of your NetSuite Service Data Source. For more information, see "Prerequisites," above. |
    | Username | Enter your NetSuite username. |
    | Password | Enter your NetSuite password. |
    | Role ID | Enter your NetSuite Role ID number. For more information, see "Prerequisites," above. |
    | Account ID | Enter your NetSuite Account ID number. For more information, see "Prerequisites," above. |
    

CHANGE 5  [### Details Pane]  (lines 134–159 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Structured Query Language (SQL) query to use in selecting the data you want. For example:  select \* from Employees  You can see the available columns to use in building a query by selecting the desired table in the <b>Database Tables</b> menu. The columns for that table then appear in the <b> Table Columns</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select a database table to see the columns in that table in the <b>Table Columns</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns</p></td>
          <td colspan="1" rowspan="1"><p>Browse to see the available columns in the table selected in the <b>Database Tables</b> menu. You can also select columns. Selected columns appear in the sample SQL query in the <b> Query Helper</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>Use this to help build SQL queries. When you select a column in the <b>Table Columns</b> field, a sample query referencing that column appears in this field. If you select additional columns, those columns are added to the query. You can edit the sample query as desired. Queries you build here can be copied and pasted into the <b> Query</b> field for use in retrieving data.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Query | Enter the Structured Query Language (SQL) query to use in selecting the data you want. For example:  select \* from Employees  You can see the available columns to use in building a query by selecting the desired table in the **Database Tables** menu. The columns for that table then appear in the **Table Columns** field. |
    | Database Tables | Select a database table to see the columns in that table in the **Table Columns** field. |
    | Table Columns | Browse to see the available columns in the table selected in the **Database Tables** menu. You can also select columns. Selected columns appear in the sample SQL query in the **Query Helper** field. |
    | Query Helper | Use this to help build SQL queries. When you select a column in the **Table Columns** field, a sample query referencing that column appears in this field. If you select additional columns, those columns are added to the query. You can edit the sample query as desired. Queries you build here can be copied and pasted into the **Query** field for use in retrieving data. |
    

==============================================================================
FILE:   s/article/360043433653.mdx
TITLE:  Adobe Analytics v2 OAuth Partition Connector - Server to Server
LANG:   English (en_US)
ID:     360043433653
==============================================================================

CHANGE 1  [### Creating a new OAuth project in Adobe Developer Console]  (line 33 in repo file)

  OLD (remove from repo):
    4. The APIs available to you will be listed in the **Add an API** window. Select ** Adobe Analytics**.

  NEW (replace with / insert from CSV):
    4. The APIs available to you will be listed in the **Add an API** window. Select **Adobe Analytics**.

CHANGE 2  [### Creating a new OAuth project in Adobe Developer Console]  (line 41 in repo file)

  OLD (remove from repo):
    6. Select the **Web** platform to configure the API.

  NEW (replace with / insert from CSV):
    6. Select the **Web**platform to configure the API.

CHANGE 3  [### Creating a new OAuth project in Adobe Developer Console]  (line 43 in repo file)

  OLD (remove from repo):
        Enter the **Default** ** redirect URI** as ** https://oauth.domo.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange**

  NEW (replace with / insert from CSV):
        Enter the **Default** **redirect URI** as **https://oauth.domo.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange**

CHANGE 4  [### Creating a new OAuth project in Adobe Developer Console]  (line 45 in repo file)

  OLD (remove from repo):
        Enter the **Redirect URI pattern** as ** https://oauth/.domo/.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange**

  NEW (replace with / insert from CSV):
        Enter the **Redirect URI pattern** as **https://oauth/.domo/.com/api/data/v2/oauth/providers/adobe-analytics-oauth/exchange**

CHANGE 5  [## Connecting to the Adobe Analytics API v2]  (line 51 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Adobe Analytics v2 connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Adobe Analytics v2 connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 6  [## Credentials Pane]  (lines 57–75 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the client id associated with your Adobe Analytics v2 account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter the client secret associated with your Adobe Analytics v2 account.</p></td>
        </tr>
      </tbody>
    </table>
    Once you have entered valid credentials, you can use the same account any time you go to create a new Adobe Analytics v2 DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter the client id associated with your Adobe Analytics v2 account. |
    | Client Secret | Enter the client secret associated with your Adobe Analytics v2 account. |
    
    Once you have entered valid credentials, you can use the same account any time you go to create a new Adobe Analytics v2 DataSet. You can manage connector accounts in the **Accounts**tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 7  [## Details Pane]  (line 83 in repo file)

  OLD (remove from repo):
    | Report | Select the Adobe Analytics v2 report you want to run: <table border="1"><tbody><tr><td><b>Report</b></td><td><b>Description</b></td></tr><tr><td>Calculated Metrics</td><td>This report returns a list of calculated metrics.</td></tr><tr><td>Collections</td><td>This report returns a list of report suites.</td></tr><tr><td>Dimensions</td><td>This report returns a list of dimensions for a given report suite.</td></tr><tr><td>Metrics</td><td>This report returns a list of metrics for a given report suite.</td></tr><tr><td>Report Data</td><td>This report returns reporting data for the requested metric and dimension for the given date range.</td></tr><tr><td>Segments</td><td>This report returns a list of segments.</td></tr><tr><td>Users</td><td>This report returns a list of users for the company.</td></tr></tbody></table> |

  NEW (replace with / insert from CSV):
    | Report | Select the Adobe Analytics v2 report you want to run:   |  |  | | --- | --- | | Calculated Metrics | This report returns a list of calculated metrics. | | Collections | This report returns a list of report suites. | | Dimensions | This report returns a list of dimensions for a given report suite. | | Metrics | This report returns a list of metrics for a given report suite. | | Report Data | This report returns reporting data for the requested metric and dimension for the given date range. | | Segments | This report returns a list of segments. | | Users | This report returns a list of users for the company. | |

CHANGE 8  [## Details Pane]  (line 87 in repo file)

  OLD (remove from repo):
    | Body | Paste the Body here, according to Adobe specifications (see [Adobe Swagger UI](https://adobedocs.github.io/analytics-2.0-apis/#/reports/runReport)). Do not include the dimensions or date range in the report body; instead, specify the dimensions and date range below. Domo uses the provided dimensions to create the recursive report bodies to get the multiple dimension data. The report body must be a valid JSON and must contain the rsid and metrics. |

  NEW (replace with / insert from CSV):
    | Body | Paste the Body here, according to Adobe specifications (see [Adobe Swagger UI](https://www.w3schools.com/%22https://adobedocs.github.io/analytics-2.0-apis/#/reports/runReport\%22)). Do not include the dimensions or date range in the report body; instead, specify the dimensions and date range below. Domo uses the provided dimensions to create the recursive report bodies to get the multiple dimension data. The report body must be a valid JSON and must contain the rsid and metrics. |

CHANGE 9  [## Details Pane]  (line 96 in repo file)

  OLD (remove from repo):
    | Segments | Select one or more segments to filter the data by segments. |

  NEW (replace with / insert from CSV):
    | Segments | Select one or more segments to filter the data by segments |

CHANGE 10  [## Details Pane]  (lines 98–101 in repo file)

  OLD (remove from repo):
    | Date Selection type | This field appears when you select the **Report Data** report. Select the date selection type for the report. You can either select a custom date range or use predefined date ranges from Adobe. |
    | Predefined Date Range | This field appears when you select the **Predefined Date Range from Adobe** option in the **Data Selection type** field. Select the predefined date range from Adobe. |
    | Report Dates | This field appears when you select the **Custom Date Range** option in the **Data Selection type** field. Select the report dates. |
    | Include Type | Include additional calculated metrics not owned by the user. The 'All' option takes precedence over 'Shared'. |

  NEW (replace with / insert from CSV):
    | Date Selection type | This field appears when you select the **Report Data** report.  Select the date selection type for the report. You can either select a custom date range or use predefined date ranges from Adobe. |
    | Predefined Date Range | This field appears when you select the **Predefined Date Range from Adobe** option in the **Data Selection type** field.  Select the predefined date range from Adobe. |
    | Report Dates | This field appears when you select the **Custom Date Range** option in the **Data Selection type** field.  Select the report dates. |
    | Include Type | Include additional calculated metrics not owned by the user. The 'All' option takes precedence over 'Shared. |

CHANGE 11  [## Details Pane]  (lines 105–108 in repo file)

  OLD (remove from repo):
    | Start Date - Relative | Enter the number of days back that you would like to get data from (start day). Combine with End Date to create a range of represented days. For example, if you enter 10 for Start Date and 5 for the End Date, the report would contain data starting from 10 days ago until 5 days ago. |
    | End Date - Relative | Enter the number of days back that you would like to get data to (end day). Combine with Start Date to create a range of represented days. For example, if you enter 10 for Start Date and 5 for End Date, the report would contain data for 10 days ago up until 5 days ago. |
    | Reset Data | Select the box to reset the data if required. Run the data once by selecting the check box and then clear the checkbox to proceed normally. <Note>**Note:** This will replace all the data in your dataset, and may result in loss of data if the dataset uses the Append mode.</Note> |
    | Exclude Formula Definition (checkbox) | Check the box to exclude formula definition columns. |

  NEW (replace with / insert from CSV):
    | Start Date - Relative | Enter the number of days back that you would like to get data from (start day). Combine with End Date to create a range of represented days.  For example, if you enter10 for Start Date and 5 for the End Date, the report would contain data starting from10 days ago until 5 days ago. |
    | End Date - Relative | Enter the number of days back that you would like to get data to (end day). Combine with Start Date to create a range of represented days.    For example, if you enter 10 for Start Date and 5 for End Date, the report would contain data for 10 days ago up until 5 days ago. |
    | Reset Data | Select the box to reset the data if required. Run the data once by selecting the check box and then clear the checkbox to proceed normally.
    <Note>**Note:** This will replace all the data in your dataset, and may result in loss of data if the dataset uses the Append mode. |
    | Exclude Formula Definition (checkbox) | Check the box to exclude formula definition columns. |</Note>

CHANGE 12  [## FAQ]  (lines 118–119 in repo file)

  OLD (remove from repo):
    <Accordion title="How does this connector differ from the other Adobe Analytics connectors?">
    A: The Adobe Analytics and Adobe Analytics Advanced connectors use version 1.4 of the Adobe Analytics API, while this connector uses version 2.

  NEW (replace with / insert from CSV):
    <Accordion title="Q: How does this connector differ from the other Adobe Analytics connectors?">
    A: The Adobe Analytics and Adobe Analytics Advanced connectors use version 1.4 of the Adobe Analytics API, while this connector uses version 2.

==============================================================================
FILE:   s/article/360043433813.mdx
TITLE:  DomoStats Connector
LANG:   English (en_US)
ID:     360043433813
==============================================================================

CHANGE 1  [### Connecting to DomoStats DataSets]  (line 21 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Details** pane in the DomoStats Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most Connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Details** pane in the DomoStats Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most Connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [# Details Pane]  (line 27 in repo file)

  OLD (remove from repo):
    | Report Name | Description |

  NEW (replace with / insert from CSV):
    | **Report Name** | **Description** |

CHANGE 3  [# Details Pane]  (lines 31–38 in repo file)

  OLD (remove from repo):
    | Activity Log | Returns information about team member activities within Domo, such as logins, card creation, card viewing, and DataSet creation. When this report is first installed, 30 days' worth of history is loaded. After this initial run, every time the report runs it will pull in all new records since it last ran, up to 30 days prior. We recommend running this report daily, but it should be run at least once every 30 days. Otherwise, there will be gaps in the data. See [reference](#activity_log). |
    | AI Chat Sessions | Returns information about AI Chat sessions in your instance. See [reference](#ai_chat_sessions). |
    | AI Model Endpoints | Returns information on when the model was used and additional information, including the model tier, source, and status. See [reference](#ai_model_endpoints). |
    | AI Models | Returns a list of models and model providers in your instance. See [reference](#ai_models). |
    | AI Projects | Returns a list of AI projects in your instance. See [reference](#ai_projects). |
    | AI Readiness | Returns information about your data's AI Readiness. See [reference](#ai_readiness). |
    | AI Readiness Details | Returns details about AI Readiness components. See [reference](#ai_readiness_details). |
    | AI Services | Returns a list of AI services and information, including provider and service type. See [reference](#ai_services). |

  NEW (replace with / insert from CSV):
    | Activity Log | Returns information about team member activities within Domo, such as logins, card creation, card viewing, and DataSet creation.  When this report is first installed, 30 days' worth of history is loaded. After this initial run, every time the report runs it will pull in all new records since it last ran, up to 30 days prior. We recommend running this report daily, but it should be run at least once every 30 days. Otherwise, there will be gaps in the data.  See [reference](/s/article/000005946). |
    | AI Chat Sessions | Returns information about AI Chat sessions in your instance. See [reference](/s/article/000005946). |
    | AI Model Endpoints | Returns information on when the model was used and additional information, including the model tier, source, and status. See [reference](/s/article/000005946). |
    | AI Models | Returns a list of models and model providers in your instance. See [reference](/s/article/000005946). |
    | AI Projects | Returns a list of AI projects in your instance. See [reference](/s/article/000005946). |
    | AI Readiness | Returns information about your data's AI Readiness. See [reference](/s/article/000005946). |
    | AI Readiness Details | Returns details about AI Readiness components. See [reference](/s/article/000005946). |
    | AI Services | Returns a list of AI services and information, including provider and service type. See [reference](/s/article/000005946). |

CHANGE 4  [# Details Pane]  (lines 40–41 in repo file)

  OLD (remove from repo):
    | App Studio App Pages | Returns information about app pages inside of App Studio apps in your instance. See [reference](#app_studio_app_pages). |
    | App Studio Apps | Returns information about App Studio apps in your instance. See [reference](#app_studio_apps). |

  NEW (replace with / insert from CSV):
    | App Studio App Pages | Returns information about app pages inside of App Studio apps in your instance. See [reference](/s/article/000005946). |
    | App Studio Apps | Returns information about App Studio apps in your instance. See [reference](/s/article/000005946). |

CHANGE 5  [# Details Pane]  (lines 43–44 in repo file)

  OLD (remove from repo):
    | Beast Mode Datasource | Returns the Datasource ID linked to each calculated field (Beast Mode) in your instance. A `Status` column specifies whether the link is valid or invalid. See [reference](#beast_mode_datasource). |
    | Beast Modes | Returns information about all Beast Mode calculations in your instance. See [Beast Modes DataSet](#beast_modes_dataset) below. |

  NEW (replace with / insert from CSV):
    | Beast Mode Datasource | Returns the Datasource ID linked to each calculated field (Beast Mode) in your instance. A `Status` column specifies whether the link is valid or invalid.  See [reference](/s/article/000005946). |
    | Beast Modes | Returns information about all Beast Mode calculations in your instance. See [reference](/s/article/000005946). |

CHANGE 6  [# Details Pane]  (line 47 in repo file)

  OLD (remove from repo):
    | Buzz | Returns information about Buzz usage within Domo, such as conversation participants and number of messages posted. |

  NEW (replace with / insert from CSV):
    | Buzz | Returns information about Buzz usage within Domo such as conversation participants and number of messages posted. |

CHANGE 7  [# Details Pane]  (line 49 in repo file)

  OLD (remove from repo):
    | Card Loads | Returns information about cards and card loads in your Domo environment. See [Card Loads DataSet](#card_loads_dataset) below. |

  NEW (replace with / insert from CSV):
    | Card Loads | Returns information about cards and card loads in your Domo environment. See [reference](/s/article/000005946). |

CHANGE 8  [# Details Pane]  (line 51 in repo file)

  OLD (remove from repo):
    | Card Permissions | Returns information about card permissions. See [Card Permissions DataSet](#card_permissions) below. |

  NEW (replace with / insert from CSV):
    | Card Permissions | Returns information about card permissions. See [reference](/s/article/000005946). |

CHANGE 9  [# Details Pane]  (lines 55–56 in repo file)

  OLD (remove from repo):
    | Connector Failures | — |
    | Connector Permissions | Returns information about connector access policies created within your instance. See [reference](#connector_permissions). |

  NEW (replace with / insert from CSV):
    | Connector Permissions | Returns information about connector access policies created within your instance. See [reference](/s/article/000005946). |

CHANGE 10  [# Details Pane]  (line 60 in repo file)

  NEW (replace with / insert from CSV):
    | DataFlows | Returns information about DataFlows within your Domo instance, such as DataFlow name, type, status, last updated date, etc. See[#dataflows](#dataflows)[reference.](/s/article/000005946) |
    | DataFlows History | Returns DataFlow run history information for your Domo instance, such as DataFlow name, run start and end time, whether runs were successful, etc. See [reference](/s/article/000005946).
    <Note>**Note:** This report may have up to a 24-hour window without data. For example, if your report ran at 1 a.m. on 10/10, the report may not pull anything more recent than 1 a.m. on 10/09. |

CHANGE 11  [# Details Pane]  (lines 61–62 in repo file)

  OLD (remove from repo):
    | DataFlows | Returns information about DataFlows within your Domo instance, such as DataFlow name, type, status, last updated date, etc. See [DataFlows DataSet](#dataflows) below. |
    | DataFlows History | Returns DataFlow run history information for your Domo instance, such as DataFlow name, run start and end time, whether runs were successful, etc. See [DataFlows History DataSet](#dataflows_history) below. <Note>**Note:** This report may have up to a 24-hour window without data. For example, if your report ran at 1 a.m. on 10/10, the report may not pull anything more recent than 1 a.m. on 10/09.</Note> |

CHANGE 12  [# Details Pane]  (line 66 in repo file)

  OLD (remove from repo):
    | DataSets | Returns information about DataSets within your Domo instance, such as DataSet name, owner, connector, last run date, etc. See [DataSets DataSet](#h_01GFXVRGE2XND3QNJTPJ0DRE93) below. |

  NEW (replace with / insert from CSV):
    | DataSets | Returns information about DataSets within your Domo instance, such as DataSet name, owner, connector, last run date, etc. See [reference](/s/article/000005946). |

CHANGE 13  [# Details Pane]  (lines 68–73 in repo file)

  OLD (remove from repo):
    | DataSets History | Returns DataSet run history information for your Domo instance, such as DataSet name, run start and end time, whether runs were successful, etc. See [DataSets History DataSet](#h_01GFXVVABHEGEQRSM9A3VA7Y26) below. |
    | Deleted DataSets | Returns information on the deleted DataSets. |
    | Grants | Returns information on access that is granted. |
    | Group Members | Returns one row for each member of each group in Domo. Each row includes the group ID, group name, and member user ID. Only active users and groups appear in this report. **System users and system groups are excluded.** Groups without members are also excluded. |
    | Group Owners | Returns one row for each owner of each group within Domo. Rows list the group ID, owner ID, and owner type (user or group). Only active groups appear in this report. Groups without owners do not appear, including system groups and ad hoc groups. |
    | Groups (Beta) | Returns one row for each group in Domo. Each row includes the following: <ul><li>group ID</li><li>group name</li><li>LDAP group name (this is the group name as it appears in the Identity Provider [IdP] and is only applicable to Directory Groups)</li><li>avatar ID</li><li>group type</li><li>group creation date</li><li>creator ID</li><li>Is Default</li></ul> Only active groups appear in this report. |

  NEW (replace with / insert from CSV):
    | DataSets History | Returns DataSet run history information for your Domo instance, such as DataSet name, run start and end time, whether runs were successful, etc. See [reference](/s/article/000005946). |
    | Deleted DataSets | Returns information on the deleted DataSets |
    | Grants | Returns information on access that is granted |
    | Group Members | Returns one row for each member of each group in Domo. Each row includes the group ID, group name, and member user ID.  Only active users and groups appear in this report. **System users and system groups are excluded.** Groups without members are also excluded. |
    | Group Owners | Returns one row for each owner of each group within Domo. Rows list the group ID, owner ID, and owner type (user or group).  Only active groups appear in this report. Groups without owners do not appear, including system groups and ad hoc groups. |
    | Groups (Beta) | Returns one row for each group in Domo.  Each row includes the following:   - group ID - group name - LDAP group name (this is the group name as it appears in the Identity Provider [IdP] and is only applicable to Directory Groups) - avatar ID - group type - group creation date - creator ID - Is Default.   Only active groups appear in this report. |

CHANGE 14  [# Details Pane]  (line 78 in repo file)

  OLD (remove from repo):
    | OKR Key Results (included with Domo Goals) | Returns information about the key results/metrics that have been created in Domo Goals. See [OKR Key Results DataSet](#okr_results) below. |

  NEW (replace with / insert from CSV):
    | OKR Key Results (included with Domo Goals) | Returns information about the key results/metrics that have been created in Domo Goals. See [reference](/s/article/000005946). |

CHANGE 15  [# Details Pane]  (lines 81–83 in repo file)

  OLD (remove from repo):
    | People | Returns information about users in your Domo instance, such as user name, security role, contact information, and most recent login date. See [People DataSet](#people) below. |
    | Project Stages | Returns information about project stages in your Domo instance. See [Project Stages DataSet](#project_stages) below. |
    | Projects | Returns information about projects in your Domo instance, such as project name, owner, associated tasks, etc. See [Projects DataSet](#projects) below. |

  NEW (replace with / insert from CSV):
    | People | Returns information about users in your Domo instance, such as user name, security role, contact information, and most recent login date. See [#people](#people)[reference](/s/article/000005946). |
    | Project Stages | Returns information about project stages in your Domo instance. See [reference](/s/article/000005946). |
    | Projects | Returns information about projects in your Domo instance, such as project name, owner, associated tasks, etc. See [#projects](#projects)[reference](/s/article/000005946). |

CHANGE 16  [# Details Pane]  (lines 95–97 in repo file)

  OLD (remove from repo):
    | Task Center | Returns information about task usage in your instance. Each row in the DataSet represents a specific task. See [Task Center DataSet](#task_center_dataset) below for the full schema. |
    | Task Owners | Returns information about task owners in your Domo instance. See [Task Owners DataSet](#task_owners) below. |
    | Tasks | Returns information about tasks in your Domo instance, such as task name, parent project, assignees, etc. See [Tasks DataSet](#tasks) below. |

  NEW (replace with / insert from CSV):
    | Task Center | Returns information about task usage in your instance. Each row in the DataSet represents a specific task. See [reference](/s/article/000005946). |
    | Task Owners | Returns information about task owners in your Domo instance. See [reference](/s/article/000005946). |
    | Tasks | Returns information about tasks in your Domo instance, such as task name, parent project, assignees, etc. See [reference](/s/article/000005946). |

CHANGE 17  [# Details Pane]  (line 99 in repo file)

  NEW (replace with / insert from CSV):
    | User Activity — AI Services Billing | Returns User Activity in relation to AI Services Billing info. |

CHANGE 18  [# Details Pane]  (line 100 in repo file)

  OLD (remove from repo):
    | User Activity — AI Services Billing | Returns User Activity in relation to AI Services Billing info. |

CHANGE 19  [# Details Pane]  (lines 105–106 in repo file)

  OLD (remove from repo):
    | Workbench | Returns information about Workbench job statuses, last run times, which machines are running the jobs, what DataSets are powered by the jobs, and schedule type. See [Workbench DataSet](#workbench) below. |
    | Workflows | Returns information about Workflows usage in your instance. See [Workflows DataSet](#workflows_dataset) below for schema details. <Note>**Note:** A DataSet for the Workflows DomoStats report can only be created once per instance.</Note> |

  NEW (replace with / insert from CSV):
    | Workbench | Returns information about Workbench job statuses, last run times, which machines are running the jobs, what DataSets are powered by the jobs, and schedule type. See [reference](/s/article/000005946). |
    | Workflows | Returns information about Workflows usage in your instance.  **Note:**A DataSet for the Workflows DomoStats report can only be created once per instance. See [reference](/s/article/000005946). |</Note>

CHANGE 20  [#### Locating DomoStats DataSets]  (line 118 in repo file)

  OLD (remove from repo):
    2. Select **DataSets** from the <img src="/images/kb/0EM5w000005vO0y.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> navigation rail.

  NEW (replace with / insert from CSV):
    2. Select **DataSets** from the
    
    <Frame>![Screen_Shot_2022-07-19_at_2.16.07_PM.png](/images/kb/0EM5w000005vO0y.png)</Frame>
    
     navigation rail.

CHANGE 21  [#### Locating DomoStats DataSets]  (line 122 in repo file)

  OLD (remove from repo):
    3. In the **Search** bar, select ** Add Filter**.

  NEW (replace with / insert from CSV):
    3. In the **Search** bar, select **Add Filter**.

CHANGE 22  [#### Locating DomoStats DataSets]  (line 124 in repo file)

  OLD (remove from repo):
    4. Choose **Type** and enter ** DomoStats**.

  NEW (replace with / insert from CSV):
    4. Choose **Type** and enter **DomoStats**.

CHANGE 23  [#### Tagging a DomoStats DataSet]  (line 142 in repo file)

  OLD (remove from repo):
    4. Select **+** next to ** Create new tag.**

  NEW (replace with / insert from CSV):
    4. Select **+** next to **Create new tag.**

==============================================================================
FILE:   s/article/360043434093.mdx
TITLE:  Pendo Connector
LANG:   English (en_US)
ID:     360043434093
==============================================================================

CHANGE 1  [## Connecting to Your Pendo Account]  (line 23 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Pendo Connector page. The components of the other panes on this page, ** Scheduling**, and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Pendo Connector page. The components of the other panes on this page, **Scheduling**, and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 2  [### Credentials Pane]  (lines 29–46 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Integration Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Pendo integration key. For information about generating a key, see "Prerequisites," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Region</p></td>
          <td colspan="1" rowspan="1"><p>Select the region from the drop-down list.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Integration Key | Enter your Pendo integration key. For information about generating a key, see "Prerequisites," above. |
    | Region | Select the region from the drop-down list. |
    

CHANGE 3  [### Details Pane]  (lines 53–162 in repo file)

  ⚠ LARGE CHANGE (138 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Pendo Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Pendo report you want to pull into Domo.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Source</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired source for the <b>Row Aggregation</b> report. The following table describes the available sources:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Guide ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ID of the guide you want to retrieve information for. To find the ID for a desired guide, navigate to the page for that guide (from the <b>Guides</b> page in Pendo), then copy the last portion of the URL. For example, in the URL https://app.pendo.io/guides/abcd\_xyz1234, the guide ID would be  abcd\_xyz1234.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Poll ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Poll ID to get the details for a specific poll.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Guide Step ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Guide Step ID to get the details for a specific guide or step.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Aggregation</p></td>
          <td colspan="1" rowspan="1"><p>Enter the request json. For example: { "pipeline": [ { "limit": 5 } ], "source": { "pageEvents": null }, "timeSeries": { "count": 1, "first": 1493596800000, "period": "dayRange" } } <b>Specifying dates - Common examples:</b> 'now()', 'date(yyyy,MM,dd)', '1693540800000' (epoch timestamp), 'yyyy-MM-dd'"</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>All Guides</p></td>
          <td colspan="1" rowspan="1"><p>Select 'Yes' if you wish to query for all guides.  'No' to select specific ones.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Guides</p></td>
          <td colspan="1" rowspan="1"><p>Select the guides to query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Do not hash the Visitor ID?</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to NOT use hashing for the visitor ID, by default.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Application Id Selection Type</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to retrieve data from all or only specific application id.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Application IDs</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Application ID for which we need to retrieve the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Handle Records</p></td>
          <td colspan="1" rowspan="1"><p>This field appears when you select the <b>Row Aggregation</b> report and the source as <b> Guides</b>.  If you encounter an error when the columns exceeds the allowed limit, you can use one of the following options to handle the records.  <b> None</b> – This option is selected by default. It does not handle the error when the column exceeds the specified limit.  <b> Skip Step\_Translations Data</b> – The stepTranslations data will be skipped and not added to the dataset.  <b> Handle Step\_Translations Data</b> – The data will be flattened to reduce the row count.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Flatten Rules Columns</p></td>
          <td colspan="1" rowspan="1"><p>Select the checkbox to flatten the <b><i>rules</b></i>, <b><i>rulesJson</b></i>, and <b><i>excludeRules</b></i> columns for improved data structure.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Duration (GMT)</p></td>
          <td colspan="1" rowspan="1"><p>Select the duration for the report (a Single Date or a Date Range).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days back</p></td>
          <td colspan="1" rowspan="1"><p>The number of days back that you would like to get data for.  Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the report start date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days back to start from</p></td>
          <td colspan="1" rowspan="1"><p>The number of days back that you would like to get data from (start day).  Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the report end date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific End Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days back to end at</p></td>
          <td colspan="1" rowspan="1"><p>The number of days back that you would like to get data to (end day).  Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Use Report Schema</p></td>
          <td colspan="1" rowspan="1"><p>Select the checkbox if you want to have report schema as column names.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Contents of Reports | Returns the contents of a selected Pendo report. | | Get Change History for Guide | Returns the change history for a specified guide. | | Guides Seen | Returns the Change History for Guide. | | Row Aggregation | Returns row aggregation data for a given source and timeframe. | | Public Reports | Returns a list of public Pendo reports. | |
    | Pendo Report | Select the Pendo report you want to pull into Domo. |
    | Source | Select the desired source for the **Row Aggregation**report. The following table describes the available sources:   | Source | Description | Date Filter Field | | --- | --- | --- | | Accounts | The accounts your users (usually your customers) belong to. | metadata.auto.lastvisit | | Custom | Lets you define your own request JSON in the **Custom Aggregation** field. For more information, see [http://help.pendo.io/resources/suppo...w-sources.html](http://help.pendo.io/resources/support-library/api-dev/aggregation-row-sources.html). | You define your own | | Events | All recorded click and pageview events (tagged or untagged). | timeSeries dayRange | | Features | Sets of rules that identify individual product features (defined by a DOM rule). | lastUpdatedAt | | Feature Events | All recorded click events matching tagged features. | timeSeries dayRange | | Guides | The in-app messages you show visitors. | lastUpdatedAt | | Pages | Sets of rules that idenfity individual pages in your product (defined by a URL rule). | lastUpdatedAt | | Page Events | All recorded pageviews matching tagged pages. | timeSeries dayRange | | Visitors | Your product's users (including internal users who access customer accounts). | metadata.auto.lastvisit | |
    | Guide ID | Enter the ID of the guide you want to retrieve information for. To find the ID for a desired guide, navigate to the page for that guide (from the **Guides** page in Pendo), then copy the last portion of the URL. For example, in the URL https://app.pendo.io/guides/abcd\_xyz1234, the guide ID would be  abcd\_xyz1234. |
    | Poll ID | Enter the Poll ID to get the details for a specific poll. |
    | Guide Step ID | Enter the Guide Step ID to get the details for a specific guide or step. |
    | Custom Aggregation | Enter the request json. For example: { "pipeline": [ { "limit": 5 } ], "source": { "pageEvents": null }, "timeSeries": { "count": 1, "first": 1493596800000, "period": "dayRange" } } **Specifying dates - Common examples:** 'now()', 'date(yyyy,MM,dd)', '1693540800000' (epoch timestamp), 'yyyy-MM-dd'" |
    | All Guides | Select 'Yes' if you wish to query for all guides.  'No' to select specific ones. |
    | Guides | Select the guides to query. |
    | Do not hash the Visitor ID? | Select this checkbox to NOT use hashing for the visitor ID, by default. |
    | Application Id Selection Type | Select whether to retrieve data from all or only specific application id. |
    | Enter Application IDs | Enter the Application ID for which we need to retrieve the data. |
    | Handle Records | This field appears when you select the **Row Aggregation** report and the source as **Guides**.  If you encounter an error when the columns exceeds the allowed limit, you can use one of the following options to handle the records.  **None** – This option is selected by default. It does not handle the error when the column exceeds the specified limit.  **Skip Step\_Translations Data** – The stepTranslations data will be skipped and not added to the dataset.  **Handle Step\_Translations Data** – The data will be flattened to reduce the row count. |
    | Flatten Rules Columns | Select the checkbox to flatten the ***rules***, ***rulesJson***, and ***excludeRules*** columns for improved data structure. |
    | Duration (GMT) | Select the duration for the report (a Single Date or a Date Range). |
    | Report Date | Select the report date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific Date | Select the report date using the date selector. |
    | Days back | The number of days back that you would like to get data for.  Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Start Date | Select the report start date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific Start Date | Select the report date using the date selector. |
    | Days back to start from | The number of days back that you would like to get data from (start day).  Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | End Date | Select the report end date using relative or specific dates. Relative indicates the number of days from today or a specific date using the date selector. |
    | Select Specific End Date | Select the report date using the date selector. |
    | Days back to end at | The number of days back that you would like to get data to (end day).  Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Use Report Schema | Select the checkbox if you want to have report schema as column names. |
    

==============================================================================
FILE:   s/article/360043434413.mdx
TITLE:  Facebook Advanced Connector
LANG:   English (en_US)
ID:     360043434413
==============================================================================

CHANGE 1  [## Intro]  (lines 13–42 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>Retrieving social media metrics, tracking content, and viewing your Facebook page performance.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Impressions - Engagement - Comments - Likes - Shares - Reactions - Page fans</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>- CMO - Director of marketing - Marketing analyst</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>Usually less than an hour, but could take much longer to pull the desired data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>4</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | Retrieving social media metrics, tracking content, and viewing your Facebook page performance. |
    | **Primary Metrics** | - Impressions - Engagement - Comments - Likes - Shares - Reactions - Page fans |
    | **Primary Company Roles** | - CMO - Director of marketing - Marketing analyst |
    | **Average Implementation Time** | Usually less than an hour, but could take much longer to pull the desired data. |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 4 |
    

CHANGE 2  [## Connecting to Your Facebook Account]  (line 52 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Facebook Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Facebook Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Credentials Pane]  (line 56 in repo file)

  OLD (remove from repo):
    The Domo Facebook Advanced connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select ** Add Account** if you have existing Facebook Advanced accounts in Domo) to open the Facebook OAuth screen where you can enter your Facebook credentials. Once you have entered valid Facebook credentials, you can use the same account any time you go to create a new Facebook Advanced DataSet. You can manage connector accounts in the ** Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    The Domo Facebook Advanced connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Facebook Advanced accounts in Domo) to open the Facebook OAuth screen where you can enter your Facebook credentials. Once you have entered valid Facebook credentials, you can use the same account any time you go to create a new Facebook Advanced DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

CHANGE 4  [## Credentials Pane]  (line 58 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you are already logged into Facebook when you connect in Domo, you are authenticated automatically when you click ** Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Facebook. If you have connected Facebook to Domo in the past your pop-up bro screen will look slightly different. By clicking the button ** Edit Settings** on the Facebook OAuth popup. This option allows users to select the specific pages they want Domo to access.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into Facebook when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Facebook. If you have connected Facebook to Domo in the past your pop-up bro screen will look slightly different. By clicking the button **Edit Settings** on the Facebook OAuth popup. This option allows users to select the specific pages they want Domo to access.</Note>

CHANGE 5  [## Details Pane]  (lines 65–70 in repo file)

  OLD (remove from repo):
    |------|-------------|
    | Report | Select the Facebook Advanced report you want to run. The following reports are available: <table border="1" cellpadding="1" cellspacing="1"><tbody><tr><th>Report Name</th><th>Description</th></tr><tr><td>Account Information</td><td>Returns information about the authenticated user, such as first and last name, gender, Facebook URL, location, time zone, etc.</td></tr><tr><td>Application Insights (Deprecated)</td><td>Returns information on insights for specified application IDs. <Note>This report is deprecated. Do not use this report.</Note></td></tr><tr><td>Complete Domain Insights</td><td>Returns domain insights for the specified domain IDs.</td></tr><tr><td>Complete Page Insights</td><td>Returns a full list of available insights for pages selected under <b>Accounts</b>.</td></tr><tr><td>Complete Page Post Insights</td><td>Returns a full list of metrics for the posts on the pages selected under <b>Accounts</b>.</td></tr><tr><td>Instant Article Aggregated Insights</td><td>Returns information about all instant articles on the pages selected under <b>Accounts</b>, aggregated together.</td></tr><tr><td>My Post Comments (Deprecated)</td><td>Returns a list of comments from your posts. For security reasons, the response includes only your comments and excludes those from other users.</td></tr><tr><td>My Posts (Deprecated)</td><td>Returns information about all posts created by authenticated users within the specified date range.</td></tr><tr><td>Page Fans (Deprecated)</td><td>Returns information on users who have "liked" the pages selected under <b>Accounts</b>.</td></tr><tr><td>Page Fans by City (Deprecated)</td><td>Returns information on users who have "liked" the pages selected under <b>Accounts</b>, broken down by city.</td></tr><tr><td>Page Fans by Country (Deprecated)</td><td>Returns information on users who have "liked" the pages selected under <b>Accounts</b>, broken down by country.</td></tr><tr><td>Page Fans Online (Deprecated)</td><td>Returns information on users who have "liked" the pages selected under <b>Accounts</b>, broken down by the hours in which they are online.</td></tr><tr><td>Page Feed</td><td>Returns a list of Posts on the pages selected under <b>Accounts</b>. This includes Posts created by the Page and those created by other users.</td></tr><tr><td>Page Follows</td><td>Returns the number of unique Facebook users who follow the Page.</td></tr><tr><td>Page Follows By City</td><td>Returns the number of unique followers for the page, grouped by city.</td></tr><tr><td>Page Follows By Country</td><td>Returns the number of unique followers for the page, grouped by country.</td></tr><tr><td>Page Impression Details</td><td>Returns metrics relating to how many people saw or interacted with the pages selected under <b>Accounts</b>.</td></tr><tr><td>Page Impressions by Age, Gender (Deprecated)</td><td>Returns information on users that have seen any content associated with the pages selected under <b>Accounts</b>, broken down by age and gender.</td></tr><tr><td>Page Impressions by City (Deprecated)</td><td>Returns information on users that have seen any content associated with the pages selected under <b>Accounts</b>, broken down by city.</td></tr><tr><td>Page Impressions by Country (Deprecated)</td><td>Returns information on users that have seen any content associated with the pages selected under <b>Accounts</b>, broken down by country.</td></tr><tr><td>Page Impressions by View Frequency (Deprecated)</td><td>Returns information on users that have seen any content associated with the pages selected under <b>Accounts</b>, broken down by the number of times they have seen the content.</td></tr><tr><td>Page Info</td><td>Returns information about pages selected under <b>Accounts</b>.</td></tr><tr><td>Page Interactions (Deprecated)</td><td>Returns information about how many users have interacted with any content associated with the pages selected under <b>Accounts</b>.</td></tr><tr><td>Page Posts</td><td>Returns information about posts on the pages selected under <b>Accounts</b>.</td></tr><tr><td>Page Post Comments</td><td>Returns a list of comments from Posts from pages selected under <b>Accounts</b>.</td></tr><tr><td>Page Post Daily Metrics</td><td>Returns a list of Daily InsightsResult metrics.</td></tr><tr><td>Page Post Lifetime Metrics</td><td>Returns information about views/impressions that each post in selected pages has received, and where those views came from.</td></tr><tr><td>Page Ratings</td><td>Returns details about ratings that the selected pages have received.</td></tr><tr><td>Page Post Video Daily Metrics</td><td>Returns information about the number of people that watch or interacted with videos on the selected page(s), broken down by day.</td></tr><tr><td>Page Post Video Metrics</td><td>Returns metrics related to the number of users who watched or interacted with videos on the pages selected under <b>Accounts</b>.</td></tr><tr><td>Public Page Info</td><td>Returns publicly available information for pages whose IDs are entered in the <b>Page IDs</b> field.</td></tr><tr><td>Public Page Insights (Deprecated)</td><td>Returns publicly available insight metrics for pages whose IDs are entered in the <b>Page IDs</b> field.</td></tr><tr><td>Public Page Posts</td><td>Returns information about posts on the public pages specified by Page IDs.</td></tr><tr><td>Published Posts</td><td>Returns information about everything you have published to the selected Pages. This includes status updates, videos, links, etc.</td></tr><tr><td>Referrals from Page (Deprecated)</td><td>Returns information about the number of users who followed links on the pages selected under <b>Accounts</b>.</td></tr><tr><td>Video Crosspost Information</td><td>Returns about crossposting of videos posted by the pages selected under <b>Accounts</b> during the specified time range.</td></tr><tr><td>Video Insights</td><td>Returns lifetime insight metrics for videos posted by the pages selected under <b>Accounts</b> during the specified time range.</td></tr><tr><td>Videos</td><td>Returns a list of videos posted by the pages selected under <b>Accounts</b> during the specified time range.</td></tr></tbody></table> |
    | Accounts | Check the box for each page you want to pull data from. You can select all attributes for the entity by clicking **Select All** or clear all selected pages by clicking **Clear All**. |
    | Start Date | Enter the start date for the report using the format `yyyy-MM-dd` (for example, `2015-01-15`). You can also use the keywords `today` or `yesterday` to return data for those days. If you want, you can specify an offset like `yesterday-3`. This would set the start date to 3 days in the past. |
    | End Date | Enter the end date for the report using the format `yyyy-MM-dd` (for example, `2015-01-15`). You can also use the keywords `today` or `yesterday` to return data for those days. If you want, you can specify an offset like `yesterday-3`. This would set the end date to 3 days in the past. |
    | End Date Should Be Inclusive? | Select **Yes** if you want data to be retrieved for all days up to and including the specified end date. Select **No** if you want data to be retrieved for all days up to but not including the specified end date. |

  NEW (replace with / insert from CSV):
    | --- | --- |
    | Report | Select the Facebook Advanced report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Account Information | Returns information about the authenticated user, such as first and last name, gender, Facebook URL, location, time zone, etc. | | Application Insights (deprecated) | Returns information on insights for specified application IDs. | | Audience Network Metrics (deprecated) |
    <Note>**Note:** This report is deprecated. Do not use this report. | | Complete Domain Insights | Returns domain insights for the specified domain IDs. | | Complete Page Insights | Returns a full list of available insights for pages selected under **Accounts**. | | Complete Page Post Insights | Returns a full list of metrics for the posts on the pages selected under **Accounts**. | | Instant Article Aggregated Insights | Returns information about all instant articles on the pages selected under **Accounts**, aggregated together. | | My Post Comments (Deprecated) | Returns a list of comments from your posts. For security reasons, the response includes only your comments and excludes those from other users. | | My Posts (Deprecated) | Returns information about all posts created by authenticated users within the specified date range. | | Page Fans (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**. | | Page Fans by City (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**, broken down by city. | | Page Fans by Country (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**, broken down by country. | | Page Fans Online (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**, broken down by the hours in which they are online. | | Page Feed | Returns a list of Posts on the pages selected under **Accounts**. This includes Posts created by the Page and those created by other users. | | Page Follows | Returns the number of unique Facebook users who follow the Page. | | Page Follows By City | Returns the number of unique followers for the page, grouped by city. | | Page Follows By Country | Returns the number of unique followers for the page, grouped by country. | | Page Impression Details | Returns metrics relating to how many people saw or interacted with the pages selected under **Accounts**. | | Page Impressions by Age, Gender (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by age and gender. | | Page Impressions by City (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by city. | | Page Impressions by Country (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by country. | | Page Impressions by View Frequency (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by the number of times they have seen the content. | | Page Info | Returns information about pages selected under **Accounts**. | | Page Interactions (Deprecated) | Returns information about how many users have interacted with any content associated with the pages selected under **Accounts**. | | Page Posts | Returns information about posts on the pages selected under **Accounts**. | | Page Post Comments | Returns a list of comments from Posts from pages selected under **Accounts**. | | Page Post Daily Metrics | Returns a list of Daily InsightsResult metrics. | | Page Post Lifetime Metrics | Returns information about views/impressions that each post in selected pages has received, and where those views came from. | | Page Ratings | Returns details about ratings that the selected pages have received. | | Page Post Video Daily Metrics | Returns information about the number of people that watch or interacted with videos on the selected page(s), broken down by day. | | Page Post Video Metrics | Returns metrics related to the number of users who watched or interacted with videos on the pages selected under **Accounts**. | | Public Page Info | Returns publicly available information for pages whose IDs are entered in the **Page IDs** field. | | Public Page Insights (Deprecated) | Returns publicly available insight metrics for pages whose IDs are entered in the **Page IDs** field. | | Public Page Posts | Returns information about posts on the public pages specified by Page IDs. | | Published Posts | Returns information about everything you have published to the selected Pages. This includes status updates, videos, links, etc. | | Referrals from Page (Deprecated) | Returns information about the number of users who followed links on the pages selected under **Accounts**. | | Video Crosspost Information | Returns about crossposting of videos posted by the pages selected under **Accounts** during the specified time range. | | Video Insights | Returns lifetime insight metrics for videos posted by the pages selected under **Accounts** during the specified time range. | | Videos | Returns a list of videos posted by the pages selected under **Accounts** during the specified time range. | |
    | Accounts | Check the box for each page you want to pull data from. You can select all attributes for the entity by clicking **Select All** or clear all selected pages by clicking **Clear All**. |
    | Start Date | Enter the start date for the report using the format yyyy-MM-dd (for example, 2015-01-15).  You can also use the keywords today or yesterday to return data for those days. If you want, you can specify an offset like yesterday-3. This would set the start date to 3 days in the past. |
    | End Date | Enter the end date for the report using the format yyyy-MM-dd (for example, 2015-01-15).  You can also use the keywords today or yesterday to return data for those days. If you want, you can specify an offset like yesterday-3. This would set the end date to 3 days in the past. |
    | End Date Should Be Inclusive? | Select **Yes** if you want data to be retrieved for all days up to and including the specified end date.  Select **No** if you want data to be retrieved for all days up to but not including the specified end date. |

CHANGE 6  [## Details Pane]  (lines 76–77 in repo file)

  OLD (remove from repo):
    | Select Insight Metrics | Select all metrics you want to retrieve information for. If you select none, information is retrieved for all of them. |
    | Skip Errors | If you select **True**, pages that return errors are silently skipped, and all other Pages will be included in the results. If you select **False**, the DataSet fails with an error if any page returns an error. |

  NEW (replace with / insert from CSV):
    | Select Insight Metrics | Select all metrics you want to retrieve information for. If you select none, information is retrieved for all of them. |
    | Skip Errors | If you select **True**, pages that return errors are silently skipped, and all other Pages will be included in the results. If you select **False**, the DataSet fails with an error if any page returns an error. |

CHANGE 7  [## Details Pane]  (line 79 in repo file)

  OLD (remove from repo):
    | Fields | Select all fields you want to retrieve data for. If you don't select any fields, a default list of fields is retrieved. |

  NEW (replace with / insert from CSV):
    | Fields | Select all fields you want to retrieve data for. If you don't select any fields, a default list of fields is retrieved. |</Note>

CHANGE 8  [## FAQ]  (lines 93–95 in repo file)

  OLD (remove from repo):
    </AccordionGroup>
    
    ## Troubleshooting

  NEW (replace with / insert from CSV):
    </AccordionGroup>## Troubleshooting

==============================================================================
FILE:   s/article/360043434433.mdx
TITLE:  Facebook Connector
LANG:   English (en_US)
ID:     360043434433
==============================================================================

CHANGE 1  [## Intro]  (lines 15–45 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>This connector lets you retrieve data about Facebook impressions, followers, likes, and sentiment toward your company.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Followers - Post likes - Trending posts - Reaction to posts - Audience demographics - Page ratings - Impressions - Views - Comments - Top post metrics over time</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>- Director of marketing - Social media manager - CMO - Sales and marketing teams</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>Typically less than an hour, but it may take longer to get the right data in.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>4</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector lets you retrieve data about Facebook impressions, followers, likes, and sentiment toward your company. |
    | **Primary Metrics** | - Followers - Post likes - Trending posts - Reaction to posts - Audience demographics - Page ratings - Impressions - Views - Comments - Top post metrics over time |
    | **Primary Company Roles** | - Director of marketing - Social media manager - CMO - Sales and marketing teams |
    | **Average Implementation Time** | Typically less than an hour, but it may take longer to get the right data in. |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 4 |

CHANGE 2  [## Connecting to Your Facebook Account]  (line 62 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the Credentials and Details panes on the Facebook Connector page. The components of the other panes in this page, **Scheduling** and ** Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the Credentials and Details panes on the Facebook Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Credentials Pane]  (line 66 in repo file)

  OLD (remove from repo):
    The Domo Facebook connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select ** Add Account** if you have existing Facebook accounts in Domo) to open the Facebook OAuth screen where you can enter your Facebook username and password. Once you have entered valid Facebook credentials, you can use the same account any time you go to create a new Facebook DataSet. You can manage connector accounts in the ** Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    The Domo Facebook connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Facebook accounts in Domo) to open the Facebook OAuth screen where you can enter your Facebook username and password. Once you have entered valid Facebook credentials, you can use the same account any time you go to create a new Facebook DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector/Adapter Accounts](/s/article/360042926054).

CHANGE 4  [## Credentials Pane]  (line 68 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you are already logged into Facebook when you connect in Domo, you are authenticated automatically when you click ** Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Facebook.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into Facebook when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of Facebook.</Note>

CHANGE 5  [## Details Pane]  (lines 74–95 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Facebook report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Accounts</p></td>
          <td colspan="1" rowspan="1"><p>Check the box for each page you want to pull data from. You can select all attributes for the entity by clicking <b>Select All</b> or clear all selected pages by clicking <b> Clear All</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Period</p></td>
          <td colspan="1" rowspan="1"><p>Select the time period for which insight metrics will be shown.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Facebook report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Complete Page Insights | Returns a full list of available insights for pages selected under **Accounts**. | | Page Fans (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**. | | Page Fans by City (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**, broken down by city. | | Page Fans by Country (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**, broken down by country. | | Page Fans Online (Deprecated) | Returns information on users who have “liked” the pages selected under **Accounts**, broken down by the hours in which they are online. | | Page Feed | Returns a list of Posts on the pages selected under **Accounts**. This includes Posts created by the Page, and those created by other users. | | Page Follows | Returns the number of unique Facebook users who follow the Page. | | Page Follows By City | Returns the number of unique followers for the page, grouped by city. | | Page Follows By Country | Returns the number of unique followers for the page, grouped by country. | | Page Impressions by Age, Gender (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by age and gender. | | Page Impressions by City (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by city. | | Page Impressions by Country (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by country. | | Page Impressions by View Frequency (Deprecated) | Returns information on users that have seen any content associated with the pages selected under **Accounts**, broken down by the number of times they have seen the content. | | Page Impressions Details | Returns metrics relating to how many people saw or interacted with the pages selected under **Accounts**. | | Page Info | Returns information about pages selected under **Accounts**. | | Page Interactions (Deprecated) | Returns metrics relating to how many users interacted with certain parts of the pages selected under **Accounts**. | | Page Posts | Returns information about posts on the pages selected under **Accounts**. | | Page Post Daily Metrics | Returns a list of Daily Insights Result metrics. | | Page Post Lifetime Metrics | Returns the lifetime metrics related to page posts selected under **Accounts**. | |
    | Accounts | Check the box for each page you want to pull data from. You can select all attributes for the entity by clicking **Select All** or clear all selected pages by clicking **Clear All**. |
    | Period | Select the time period for which insight metrics will be shown. |
    

CHANGE 6  [## FAQ]  (line 108 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 7  [## FAQ]  (line 110 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    **Are there any API limits I should be aware of?**
    

CHANGE 8  [## FAQ]  (lines 114–116 in repo file)

  OLD (remove from repo):
    </AccordionGroup>
    
    ## Troubleshooting

  NEW (replace with / insert from CSV):
    </AccordionGroup>## Troubleshooting

==============================================================================
FILE:   s/article/360043435013.mdx
TITLE:  Procore Connector
LANG:   English (en_US)
ID:     360043435013
==============================================================================

CHANGE 1  [## Connecting to Your Procore Account]  (line 17 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Procore Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Procore Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [### Credentials Pane]  (line 21 in repo file)

  OLD (remove from repo):
    The Domo Procore connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select ** Add Account** if you have existing Procore accounts in Domo) to open the Procore OAuth screen where you can enter your Procore email address and password. Once you have entered valid Procore credentials, you can use the same account any time you go to create a new Procore DataSet. You can manage connector accounts in the ** Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    The Domo Procore connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing Procore accounts in Domo) to open the Procore OAuth screen where you can enter your Procore email address and password. Once you have entered valid Procore credentials, you can use the same account any time you go to create a new Procore DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 3  [### Details Pane]  (lines 27–149 in repo file)

  ⚠ LARGE CHANGE (153 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Procore report you want to run. The following reports are available</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Company</p></td>
          <td colspan="1" rowspan="1"><p>Select the company you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Sub Report</p></td>
          <td colspan="1" rowspan="1"><p>Choose the sub list to flatten.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Project Filter</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether you want to retrieve data for all projects, active projects only, or inactive projects only.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Project</p></td>
          <td colspan="1" rowspan="1"><p>Select the project you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contract</p></td>
          <td colspan="1" rowspan="1"><p>Select the contract you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contract Selection Method</p></td>
          <td colspan="1" rowspan="1"><p>Choose the method to select contracts. This discovery will return a list of contracts. Manual entry will allow you to enter a Contract ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contract ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Contract ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>PCO Id Selection Method</p></td>
          <td colspan="1" rowspan="1"><p>Choose the method to select PCO Ids. Selecting "All" will returns data for all PCO Ids. Selecting "Manual" will return a list of PCO Ids for entered contract Id.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>PCO ID</p></td>
          <td colspan="1" rowspan="1"><p>Select PCO Id to run the report for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>COR ID Selection Method</p></td>
          <td colspan="1" rowspan="1"><p>Choose the method to select COR Ids. Selecting "All" will returns data for all COR Ids. Selecting "Manual" will return a list of COR Ids for entered contract Id.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>COR ID</p></td>
          <td colspan="1" rowspan="1"><p>Select COR Id to run the report for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Company Vendor</p></td>
          <td colspan="1" rowspan="1"><p>Select the vendor you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Drawing Area</p></td>
          <td colspan="1" rowspan="1"><p>Select the drawing area you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Meetings</p></td>
          <td colspan="1" rowspan="1"><p>Select the meeting you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Album</p></td>
          <td colspan="1" rowspan="1"><p>Select the album you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Standard Cost Code</p></td>
          <td colspan="1" rowspan="1"><p>Select the standard cost code list you want to retrieve information for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Duration</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to pull data for a specific date or a date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Number of photos</p></td>
          <td colspan="1" rowspan="1"><p>Enter the numbers of recent photos you want to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the date for the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that should appear in the report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in <b>End Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in <b>Start Date</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific End Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the second date in your date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to Start From</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the farthest day back that should be represented in the report. Combine with <b>Days Back to End At</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Days Back to End At</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of the most recent day back that should be represented in the report. Combine with <b>Days Back to Start From</b> to create a range of represented days.  For example, if you entered 10 for <b> Days Back to Start From</b> and 5 for <b> Days Back to End At</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Procore report you want to run. The following reports are available   |  |  | | --- | --- | | **Report Name** | **Description** | | ​​All Activity Details | Returns activity details of all RFIs from all projects of a company. | | ​​All RFI Replies | Returns replies of all RFIs from all projects of a company. | | ​​Budget ​Items | Returns a list of all Budget Line Items. | | ​​Calendar ​Events | Returns a list of all Calendar Events for a specified project. | | ​​Change ​Orders | Returns a list of all Change Orders for a specified contract in a project. | | ​​Change ​Orders ​Requests | Returns a list of all Change Orders Requests (COR) for a specified contract in a project. | | Change Order Request Details | Returns detailed information about Change Order Request (COR) for a specified contract in a project. | | ​​Change Reason ​Templates | Returns a list of Change Reasons for change orders. | | ​​Checklist ​Templates | Returns a list of all Inspection Checklist Templates for a specified Project. | | ​​Checklists | Returns a list of all Inspection Checklist for a specified Project. | | ​​Companies | Returns a list of Companies visible by the authenticated user. | | ​​Company ​Insurances | Returns a list of all Insurances associated with the specified Company. | | ​​Company ​Users | Returns a list of all Users associated with a Company. | | ​​Company ​Vendor ​Insurances | Returns a list of Insurances from the specified Company Vendor. | | ​​Company ​Vendors | Returns a list of all Vendors associated with a Company. | | ​​Contract ​Payments | Returns a list of Contract Payments. | | ​​Cost ​Codes | Returns a list of all Standard Cost Code Lists at the Company level. | | ​​Direct ​Costs | Returns a list of all Direct Cost Items for a Project. | | ​​Drawing ​Areas | Returns a list of all Drawing Areas in the specified Project. | | ​​Drawings | Returns a list of all Drawings for a specified drawing area. | | ​​Environmentals | Returns a list of Environmental records for a given project. | | ​​Equipment | Returns a list of all Project Equipment. | | ​​Harm Sources | Return a list of all Harm Sources associated with a Company. | | ​​Incidents | Returns a list of Incidents for a given project. | | ​​Injuries | Returns a list of Injuries for a given project. | | ​​Inspectors (checklists) | Returns a list of Possible Inspectors in a specified Project. | | ​​Line ​Item ​Types | Returns a list of all defined Line Item Types. | | ​​Locations | Returns a list of Locations associated with a Project. | | Manpower ​Logs | Returns all approved Manpower Logs for the specified date. | | ​​Meetings | Returns a list of all Meetings for a specified Project. | | ​​Meetings ​Categories | Returns all Meeting Categories for a given Meeting. | | ​​Near Misses | Returns a list of Near Misses for a given project. | | ​​Notes ​Logs | Returns all Notes Logs for the current date. | | Observation Category Configurable Field Sets | Returns a collection of observation category configurable field sets from the specified project. | | Observation Default Distribution Members | Returns a collection of users from the observations default distribution list. | | Observations Response Logs | Returns a collection of Response Logs scoped to viewable Observations. | | Observation Items | Returns all observations in the specified project. | | Observation Potential Distribution Members | Returns a collection of potential users for the observation distribution list. | | Observation Types | Returns the available observation Types in the specified project. | | Observation Item Response Logs | Returns a collection of response logs for the specified observation item. | | ​​Photo ​Albums | Returns a list of all Photo Albums (Image Categories) in a specified project. | | ​​Photos | Returns a list of all Images from a Project's Photo Album (Image Category). | | ​​Plan ​Revision ​Logs | Returns all Plan Revision Logs for the current date. | | ​​Potential ​Change ​Orders | Returns a list of all Potential Change Orders (PCO). | | Potential Change Order Details | Returns detailed information about a Potential Change Order(PCO). | | ​​Prime ​Contract (​Attachment) | Returns the Prime Contract for the specified Project and associated Attachments. | | ​​Prime ​Contract (​Change ​Orders) | Returns the Prime Contract for the specified Project and associated Change Orders. | | ​​Prime ​Contract (​Line ​Items) | Returns the Prime Contract for the specified Project and associated Line Items. | | ​​Prime ​Contract (​Order ​Packages) | Returns the Prime Contract for the specified Project and associated Order Packages. | | ​​Prime ​Contract (​Order ​Requests) | Returns the Prime Contract for the specified Project and associated Order Requests. | | ​​Prime ​Contract (​Payment ​Received) | Returns the Prime Contract for the specified Project and associated Payments Received. | | ​​Programs | Returns a list of Programs associated to the specified Company. | | ​​Project ​Dates | Returns a list of Project Dates. | | ​Project ​Details | Returns details of the specified Project. | | ​​Project ​Details (​Budget) | Returns the budget specific information to the Project Details report. | | ​​Project ​Folders | Returns a list of Folders and Files for a specified Project. | | ​​Project ​Insurances | Returns a list of all Insurances associated with the specified Project. | | ​​Project ​Roles | Returns a list of all defined Project Roles in a specified Project. | | ​​Project ​Users | Returns a list of Users associated with a Project. | | ​​Project ​Vendors | Returns a list of Vendors associated with a specified Project. | | ​​Projects | Returns a list of active Projects. | | ​Property Damages | Returns a list of Property Damage records for a given project. | | Punch Items | Returns a list of all Punch Items for the specified project. | | ​​Punch ​Item ​Types | Returns a list of all Punch Item Types on a specified Project. | | ​​Purchase ​Order ​Contract | Returns a list of all Purchase Order Contracts. | | ​​Quantity ​Logs | Returns all Quantity Logs for the current date. | | ​​Recently ​Updated ​Photos | Returns a list of the most recently updated photos for a specified Project. | | ​​RFIs | Returns all RFIs in a specified Project. | | ​​Standard ​Cost ​Code ​Lists | Returns a list of all Standard Cost Code Lists at the Company level. | | ​​Standard ​Cost ​Codes | Returns a list of all Standard Cost Codes in a specified Standard Cost Code list. | | ​​Submittals | Returns a list of all Submittals from a specified Project. | | ​​Time​Card ​Entries | Returns a list of all Timecard Entries for the current date. | | ​​Time​Card ​Time ​Types | Returns a list of all Timecard Time Types for a specified Project. | | ​​Trades | Returns a list of all Trades associated with a Company. | | ​​Visitor ​Logs | Returns all Visitor Logs for the current date. | | ​​Waste ​Logs | Returns all Waste Logs for the current date. | | Work ​Breakdown ​Structure ​Codes | Returns all Work Breakdown Structure codes for a given project. | | ​​Work ​Logs | Returns all Work Logs for the current date. | | ​​Work ​Order ​Contract | Returns a list of all Work Order Contracts of a specified Project. | |
    | Company | Select the company you want to retrieve information for. |
    | Sub Report | Choose the sub list to flatten. |
    | Project Filter | Specify whether you want to retrieve data for all projects, active projects only, or inactive projects only. |
    | Project | Select the project you want to retrieve information for. |
    | Contract | Select the contract you want to retrieve information for. |
    | Contract Selection Method | Choose the method to select contracts. This discovery will return a list of contracts. Manual entry will allow you to enter a Contract ID. |
    | Contract ID | Enter the Contract ID. |
    | PCO Id Selection Method | Choose the method to select PCO Ids. Selecting "All" will returns data for all PCO Ids. Selecting "Manual" will return a list of PCO Ids for entered contract Id. |
    | PCO ID | Select PCO Id to run the report for. |
    | COR ID Selection Method | Choose the method to select COR Ids. Selecting "All" will returns data for all COR Ids. Selecting "Manual" will return a list of COR Ids for entered contract Id. |
    | COR ID | Select COR Id to run the report for. |
    | Company Vendor | Select the vendor you want to retrieve information for. |
    | Drawing Area | Select the drawing area you want to retrieve information for. |
    | Meetings | Select the meeting you want to retrieve information for. |
    | Album | Select the album you want to retrieve information for. |
    | Standard Cost Code | Select the standard cost code list you want to retrieve information for. |
    | Duration | Select whether you want to pull data for a specific date or a date range. |
    | Number of photos | Enter the numbers of recent photos you want to retrieve. |
    | Report Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Select Specific Date | Select the date for the report. |
    | Days Back | Enter the number of past days that should appear in the report. |
    | Start Date | Specify whether the first date in your date range is a specific or relative date. You select the last date in your range in **End Date**. |
    | End Date | Specify whether the second date in your date range is a specific or relative date. You select the first date in your range in **Start Date**. |
    | Select Specific Start Date | Select the first date in your date range. |
    | Select Specific End Date | Select the second date in your date range. |
    | Days Back to Start From | Enter the number of the farthest day back that should be represented in the report. Combine with **Days Back to End At** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |
    | Days Back to End At | Enter the number of the most recent day back that should be represented in the report. Combine with **Days Back to Start From** to create a range of represented days.  For example, if you entered 10 for **Days Back to Start From** and 5 for **Days Back to End At**, the report would contain data for 10 days ago up until 5 days ago. |

==============================================================================
FILE:   s/article/360043435113.mdx
TITLE:  Workfront Connector
LANG:   English (en_US)
ID:     360043435113
==============================================================================

CHANGE 1  [## Intro]  (lines 13–43 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>Use this connector to pull data about the status of projects and the individual tasks that apply to those projects.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Project status - Start and complete dates - Hours per project - User hours - Resources - Common issues</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>- Project manager - Operations</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>5-10 hours for basic implementation</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>5</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | Use this connector to pull data about the status of projects and the individual tasks that apply to those projects. |
    | **Primary Metrics** | - Project status - Start and complete dates - Hours per project - User hours - Resources - Common issues |
    | **Primary Company Roles** | - Project manager - Operations |
    | **Average Implementation Time** | 5-10 hours for basic implementation |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 5 |

CHANGE 2  [## Connecting to Your Workfront Account]  (line 62 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials**, ** Details**, and ** Advanced** panes in the Workfront Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials**, **Details**, and **Advanced** panes in the Workfront Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 68–97 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username to log into your Workfront account. You must provide a username and password OR an API Key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password to log into your Workfront account. You must provide a username and password OR an API Key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>API Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Workfront API key. You must update the API key in the connector connection when it expires. You must provide a username and password OR an API Key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>URL</p></td>
          <td colspan="1" rowspan="1"><p>Enter the URL for your AtTask/Workfront server (for example, <a href="https://yourcompany.attask-ondemand.com">https://yourcompany.attask-ondemand.com</a>>).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>API Version</p></td>
          <td colspan="1" rowspan="1"><p>Choose the API version to connect to.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Username | Enter the username to log into your Workfront account. You must provide a username and password OR an API Key. |
    | Password | Enter the password to log into your Workfront account. You must provide a username and password OR an API Key. |
    | API Key | Enter your Workfront API key. You must update the API key in the connector connection when it expires. You must provide a username and password OR an API Key. |
    | URL | Enter the URL for your AtTask/Workfront server (for example, [https://yourcompany.attask-ondemand.com](https://yourcompany.attask-ondemand.com)>). |
    | API Version | Choose the API version to connect to. |
    

CHANGE 4  [### Details Pane]  (lines 104–125 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Parameter</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Workfornt report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Fields</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom fields for your report, separated by commas</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Reference Fields</p></td>
          <td colspan="1" rowspan="1"><p>Enter the reference fields for your report as a comma-separated list.  Format: <span class="mt-font-courier-new">reference object:reference field</span></p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Parameter | Description |
    | --- | --- |
    | Report | Select the Workfornt report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report** | **Description** | | Approval | Returns a list of approvals for the account. | | Assignment | Returns a list of assignments, including assigner and assignee, average work per day, project, and current status. | | Baseline | Returns a list of project baselines, including project ID, start and completion dates, customer ID, budget, and planned hours. | | Category | Returns a list of categories (custom data forms). | | Group | Returns a list of groups, including group ID, customer ID, and description. | | Hour | Returns the number of hours entered for users in the account. | | Hour Type | Returns a list of hour types used in the account, with name, description. | | Issue | Returns a list of issues, including start date, assignee, current status, description, planned hours, and responsible team. | | Job Role | Returns a list of job roles used in the account, with name, description, and cost per hour. | | Milestone | Returns a list of milestones, including name, description, customer ID. | | Milestone Path | Returns a list of milestone paths, including name, description, and customer ID. | | Note | Returns a list of notes, including note subject, note text, project, and owner. | | Parameter | Returns a list of parameters (fields in custom data forms). | | Portfolio | Returns a list of portfolios, including name, customer, and entry date. | | Program | Returns a list of programs, including name, owner, description. | | Project | Returns a list of projects, including name, budget, actual costs, hours worked, status, and owner. | | Project User | Returns a list of project users, including customer ID, project ID, and user ID. | | Resource Allocation | Returns information about resource allocation for given projects and customers, including scheduled hours, budgeted hours. | | Resource Pool | Returns a list of available resource pools, including customer ID and description. | | Schedule | Returns a list of schedules in the account. | | Task | Returns a list of tasks, including start date, assignee, current status, description, planned hours, and responsible team. | | Team | Returns a list of teams in the account. | | Templates | Returns a list of templates in the account. | | Timesheet | Returns timesheet information for all users in the account. | | Users | Returns information for all users in the account, including name, title, email address, billing information. | | Work | Returns information about work done, including expense and labor cost, duration, and revenue. | |
    | Custom Fields | Enter the custom fields for your report, separated by commas |
    | Reference Fields | Enter the reference fields for your report as a comma-separated list.  Format: `reference object:reference field` |
    

CHANGE 5  [## FAQs]  (lines 132–133 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="Can I use the same account to create multiple DataSets?">

  NEW (replace with / insert from CSV):
    ##### Can I use the same account to create multiple DataSets?

CHANGE 6  [## FAQs]  (lines 137–139 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    ##### How often can the data be updated?

CHANGE 7  [## FAQs]  (lines 143–145 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    ##### Are there any API limits I should be aware of?

CHANGE 8  [## FAQs]  (line 149 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    ##### Why do I need to add DE: as a prefix for custom fields?

CHANGE 9  [## FAQs]  (lines 151–156 in repo file)

  OLD (remove from repo):
    <Accordion title="Why do I need to add DE: as a prefix for custom fields?">
    
    To learn about adding prefixes for custom fields, see the following: [https://support.workfront.com/hc/en-...ng-custom-data](https://support.workfront.com/hc/en-us/articles/115003574147-API-Basics#retrieving-custom-data "https://support.workfront.com/hc/en-us/articles/115003574147-API-Basics#retrieving-custom-data").
    
    </Accordion>
    </AccordionGroup>

  NEW (replace with / insert from CSV):
    To learn about adding prefixes for custom fields, see the following: [https://support.workfront.com/hc/en-...ng-custom-data](https://support.workfront.com/hc/en-us/articles/115003574147-API-Basics#retrieving-custom-data).

==============================================================================
FILE:   s/article/360043436133.mdx
TITLE:  Microsoft Dynamics CRM Connector
LANG:   English (en_US)
ID:     360043436133
==============================================================================

CHANGE 1  [## Intro]  (lines 11–43 in repo file)

  ⚠ LARGE CHANGE (40 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    ---
    
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>This connector is appropriate for:   - Sales management - Marketing lead generation - Pipeline management</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Leads generated - MQL - MQO - SAO - Won Deals - Open Opportunities - Win Rates - Average Deal Size</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>Salespeople, executives, etc.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>30 hours</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>8</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector is appropriate for:   - Sales management - Marketing lead generation - Pipeline management |
    | **Primary Metrics** | - Leads generated - MQL - MQO - SAO - Won Deals - Open Opportunities - Win Rates - Average Deal Size |
    | **Primary Company Roles** | Salespeople, executives, etc. |
    | **Average Implementation Time** | 30 hours |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 8 |

CHANGE 2  [## Connecting to Your Microsoft Dynamics CRM Installation]  (line 59 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Microsoft Dynamics CRM Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Microsoft Dynamics CRM Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 65–90 in repo file)

  ⚠ LARGE CHANGE (32 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>URL</p></td>
          <td colspan="1" rowspan="1"><p>Enter the URL of your Dynamics CRM installation. For example: mycompany.crm.dynamics.com</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to log into the Dynamics CRM installation URL.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password you use to log into the Dynamics CRM installation URL.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Version</p></td>
          <td colspan="1" rowspan="1"><p>Select the Dynamics CRM version that you want to connect to.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | URL | Enter the URL of your Dynamics CRM installation. For example: mycompany.crm.dynamics.com |
    | Username | Enter the username you use to log into the Dynamics CRM installation URL. |
    | Password | Enter the password you use to log into the Dynamics CRM installation URL. |
    | Version | Select the Dynamics CRM version that you want to connect to. |

CHANGE 4  [### Details Pane]  (lines 98–116 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Entity</p></td>
          <td colspan="1" rowspan="1"><p>Select the entity for your report. This list includes every entity in the Dynamics CRM installation that the authenticated user has access to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Attributes</p></td>
          <td colspan="1" rowspan="1"><p>Select all attributes for the given entity you want to appear in your report. This menu is not populated until you select an entity in the Entity menu.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Entity | Select the entity for your report. This list includes every entity in the Dynamics CRM installation that the authenticated user has access to. |
    | Attributes | Select all attributes for the given entity you want to appear in your report. This menu is not populated until you select an entity in the Entity menu. |

==============================================================================
FILE:   s/article/360043436273.mdx
TITLE:  PostgreSQL Connector
LANG:   English (en_US)
ID:     360043436273
==============================================================================

CHANGE 1  [## Intro]  (lines 15–47 in repo file)

  ⚠ LARGE CHANGE (40 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>This connector is appropriate for pulling database information into Domo.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>N/A</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>- IT - Project management</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>~10 hours</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>9</p></td>
        </tr>
      </tbody>
    </table>
    
    ---
    

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector is appropriate for pulling database information into Domo. |
    | **Primary Metrics** | N/A |
    | **Primary Company Roles** | - IT - Project management |
    | **Average Implementation Time** | ~10 hours |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 9 |

CHANGE 2  [## Prerequisites]  (line 63 in repo file)

  OLD (remove from repo):
    CA certificate text or URL path is required *only* if you select **Certificate text** or ** URL path**, respectively, in the ** Certificate type** menu.

  NEW (replace with / insert from CSV):
    CA certificate text or URL path is required *only* if you select **Certificate text** or **URL path**, respectively, in the **Certificate type** menu.

CHANGE 3  [## Connecting to Your PostgreSQL Database]  (line 69 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the PostgreSQL Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the PostgreSQL Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 75–112 in repo file)

  ⚠ LARGE CHANGE (48 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter your PostgreSQL username.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter your PostgreSQL password.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Host</p></td>
          <td colspan="1" rowspan="1"><p>Enter the host name for the SQL database. For example: db.company.com</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Port</p></td>
          <td colspan="1" rowspan="1"><p>Enter the port number for the database.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Certificate</p></td>
          <td colspan="1" rowspan="1"><p>Paste the text for your CA certificate or enter the URL where your certificate is located. This is optional. If you do not want to include a certificate, select <b>No certificate</b> in the <b> Certificate Type</b> menu.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Certificate Type</p></td>
          <td colspan="1" rowspan="1"><p>Select a certificate type. If you do not want to include a certificate, select <b>No certificate</b>. If you select Certificate text, you must paste the text for your certificate in the <b> Certificate</b> field. If you select <b> URL path</b>, you must enter the URL where your certificate is located in the <b> Certificate</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the SQL database.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Username | Enter your PostgreSQL username. |
    | Password | Enter your PostgreSQL password. |
    | Host | Enter the host name for the SQL database. For example: db.company.com |
    | Port | Enter the port number for the database. |
    | Certificate | Paste the text for your CA certificate or enter the URL where your certificate is located. This is optional. If you do not want to include a certificate, select **No certificate** in the **Certificate Type** menu. |
    | Certificate Type | Select a certificate type. If you do not want to include a certificate, select **No certificate**. If you select Certificate text, you must paste the text for your certificate in the **Certificate** field. If you select **URL path**, you must enter the URL where your certificate is located in the **Certificate** field. |
    | Database | Enter the name of the SQL database. |
    

CHANGE 5  [### Details Pane]  (lines 119–177 in repo file)

  ⚠ LARGE CHANGE (73 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Type</p></td>
          <td colspan="1" rowspan="1"><p>Select a query type</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query to be executed.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>SQL Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Structured Query Language (SQL) query to use in selecting the data you want. For example:  select \* from Employee</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Parameter (Check box)</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to add query parameters.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Parameter (Text box)</p></td>
          <td colspan="1" rowspan="1"><p>Enter the query parameter value. It is the initial value for query parameter. The last run date is optional. By default it is '02/01/1700' if is not provided. Example: !{lastvalue:\_id}!=1,!{lastrundate:start\_date}!=02/01/1944</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Fetch Size</p></td>
          <td colspan="1" rowspan="1"><p>Enter fetch size for memory performance. If it is blank, default value will be 400.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>BigInt To Long</p></td>
          <td colspan="1" rowspan="1"><p>Select to always convert a bigint column to a long column in Domo. This means even if the bigint column stores small integer values, Domo will still consider the column to have long values.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Disable Statement Timeout</p></td>
          <td colspan="1" rowspan="1"><p>Select, this checkbox if you observe errors for statement timeout.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to appear in your report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns</p></td>
          <td colspan="1" rowspan="1"><p>Select the table columns you want to appear in your report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>Copy and paste this query into the <b>Query</b> field if you need help building a query. This query is automatically generated when you select a table and columns in the <b> Database Table</b> and <b> Table Columns</b> fields, respectively.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Fetch Columns Description (Comment)</p></td>
          <td colspan="1" rowspan="1"><p>Toggle this to fetch the column description(comment) for the current execution.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Query Type | Select a query type   | Query Type | Description | | --- | --- | | Query Builder | Build an SQL query by selecting the database table and table columns | | Custom Query | Enter a customized SQL query with or without parameters to execute | |
    | Query | Enter the SQL query to be executed. |
    | SQL Query | Enter the Structured Query Language (SQL) query to use in selecting the data you want. For example:  select \* from Employee |
    | Query Parameter (Check box) | Select this checkbox to add query parameters. |
    | Query Parameter (Text box) | Enter the query parameter value. It is the initial value for query parameter. The last run date is optional. By default it is '02/01/1700' if is not provided. Example: !{lastvalue:\_id}!=1,!{lastrundate:start\_date}!=02/01/1944 |
    | Fetch Size | Enter fetch size for memory performance. If it is blank, default value will be 400. |
    | BigInt To Long | Select to always convert a bigint column to a long column in Domo. This means even if the bigint column stores small integer values, Domo will still consider the column to have long values. |
    | Disable Statement Timeout | Select, this checkbox if you observe errors for statement timeout. |
    | Database Tables | Select the database table you want to appear in your report. |
    | Table Columns | Select the table columns you want to appear in your report. |
    | Query Helper | Copy and paste this query into the **Query** field if you need help building a query. This query is automatically generated when you select a table and columns in the **Database Table** and **Table Columns** fields, respectively. |
    | Fetch Columns Description (Comment) | Toggle this to fetch the column description(comment) for the current execution. |

==============================================================================
FILE:   s/article/360043436313.mdx
TITLE:  Snowflake Unload Connector
LANG:   English (en_US)
ID:     360043436313
==============================================================================

CHANGE 1  [## Intro]  (lines 21–22 in repo file)

  OLD (remove from repo):
    ---
    

CHANGE 2  [## Prerequisites]  (lines 29–30 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345." <img alt="snowflake_account_names.png" src="/images/kb/0EM5w000005vO5q.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> - The username and password used to connect to your Snowflake host.
    - Your AWS access key. This is available in the AWS Console in the **Security Credentials** section (unless you are using IAM, in which case it is under ** Users**).

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    
    
      <img alt="snowflake_account_names.png" src="/images/kb/0EM5w000005vO5q.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    - The username and password used to connect to your Snowflake host.
    - Your AWS access key. This is available in the AWS Console in the **Security Credentials**section (unless you are using IAM, in which case it is under **Users**).

CHANGE 3  [## Connecting to Your Snowflake Account]  (line 36 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 42–75 in repo file)

  ⚠ LARGE CHANGE (43 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Snowflake account name. For more information, see <a href="#h_1538daeb-ce8e-4a65-8963-e6f483d28ebe">Prerequisites</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS access key. For information about obtaining an access key, see "<a href="#h_1538daeb-ce8e-4a65-8963-e6f483d28ebe">Prerequisites</a>," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Secret Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS secret key. For information about obtaining a secret key, see "<a href="#h_1538daeb-ce8e-4a65-8963-e6f483d28ebe">Prerequisites</a>," above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Bucket</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the AWS S3 bucket.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter your Snowflake account name. For more information, see [Prerequisites](#h_1538daeb-ce8e-4a65-8963-e6f483d28ebe). |
    | Username | Enter the username you use to connect to your Snowflake host. |
    | Password | Enter the password you use to connect to your Snowflake host. |
    | Access Key | Enter your AWS access key. For information about obtaining an access key, see "[Prerequisites](#h_1538daeb-ce8e-4a65-8963-e6f483d28ebe)," above. |
    | Secret Key | Enter your AWS secret key. For information about obtaining a secret key, see "[Prerequisites](#h_1538daeb-ce8e-4a65-8963-e6f483d28ebe)," above. |
    | Bucket | Enter the name of the AWS S3 bucket. |
    

CHANGE 5  [### Details Pane]  (lines 82–123 in repo file)

  ⚠ LARGE CHANGE (52 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query to use to retrieve your data. You can use the <b>Query Helper</b> parameter to help you write a usable SQL query. To use the <b> Query Helper</b>, do the following:   1. Select your desired warehouse, database, database schema, database table, and table columns in each menu. 2. Copy the SQL statement that appears in the <b> Query Helper</b> field. 3. Paste the copied SQL statement into the <b> Query</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve.  The list populates with all of the warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve.  The list populates with all of the databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Schema (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve.  The list populates with all of the schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Tables (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>(Optional) Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>(Optional) Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Helper (Optional)</p></td>
          <td colspan="1" rowspan="1"><p>(Optional) When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste the SQL statement in this field into the <b>Query</b> field. For more information, see <b> Query</b>, above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Use Snowflake Schema</p></td>
          <td colspan="1" rowspan="1"><p>Check this box if you want to use the schema from Snowflake instead of having Domo determine the schema automatically.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Query | Enter the SQL query to use to retrieve your data. You can use the ****Query Helper****parameter to help you write a usable SQL query. To use the ****Query Helper****, do the following:   1. Select your desired warehouse, database, database schema, database table, and table columns in each menu. 2. Copy the SQL statement that appears in the ****Query Helper****field. 3. Paste the copied SQL statement into the ****Query**** field. |
    | Warehouses (Optional) | Select the warehouse containing the data you want to retrieve.  The list populates with all of the warehouses you have access to based on your credentials. |
    | Databases (Optional) | Select the database containing the data you want to retrieve.  The list populates with all of the databases you have access to based on your credentials. |
    | Database Schema (Optional) | Select the database schema containing the data you want to retrieve.  The list populates with all of the schemas you have access to based on your credentials. |
    | Database Tables (Optional) | (Optional) Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Table Columns (Optional) | (Optional) Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table. |
    | Query Helper (Optional) | (Optional) When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste the SQL statement in this field into the ****Query****field. For more information, see ****Query****, above. |
    | Use Snowflake Schema | Check this box if you want to use the schema from Snowflake instead of having Domo determine the schema automatically. |

CHANGE 6  [## FAQs]  (lines 133–134 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    ##### Are there any API limits I should be aware of?

CHANGE 7  [## FAQs]  (lines 138–140 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    ##### How frequently will my data update?

CHANGE 8  [## FAQs]  (lines 144–146 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What should I be aware of while writing a query?">

  NEW (replace with / insert from CSV):
    ##### What should I be aware of while writing a query?

CHANGE 9  [## FAQs]  (line 150 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    ##### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?

CHANGE 10  [## FAQs]  (line 152 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    Before you can connect to a Snowflake database, you must also allowlist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses in Domo](/s/article/360043439173).

CHANGE 11  [## FAQs]  (lines 154–158 in repo file)

  OLD (remove from repo):
    Before you can connect to a Snowflake database, you must also whitelist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses in Domo](/s/article/360043439173 "Whitelisting IP Addresses in Domo").
    
    </Accordion>
    
    <Accordion title="Is this a Production-level connector?">

  NEW (replace with / insert from CSV):
    ##### Is this a Production-level connector?

CHANGE 12  [## FAQs]  (lines 161–163 in repo file)

  OLD (remove from repo):
    
    </Accordion>
    </AccordionGroup>

==============================================================================
FILE:   s/article/360043436753.mdx
TITLE:  Microsoft OneDrive Business Connector
LANG:   English (en_US)
ID:     360043436753
==============================================================================

CHANGE 1  [### To obtain the OneDrive CLIENT SECRET:]  (line 60 in repo file)

  OLD (remove from repo):
    7. Your new value will appear. Copy the Value field and paste it in your Domo connector Value field. <img alt="Secret.png" src="/images/kb/0EM5w000005vO71.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>

  NEW (replace with / insert from CSV):
    7. Your new value will appear. Copy the Value field and paste it in your Domo connector Value field.
    
       <img alt="Secret.png" src="/images/kb/0EM5w000005vO71.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>

CHANGE 2  [### Creating an Azure App for Enterprise OAuth]  (line 67 in repo file)

  OLD (remove from repo):
    2. Click on **App registrations**. <img alt="Capture1.PNG" src="/images/kb/0EM5w000005vO6q.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> 3. Click ** New registration**.

  NEW (replace with / insert from CSV):
    2. Click on **App registrations**.
    
    
       <img alt="Capture1.PNG" src="/images/kb/0EM5w000005vO6q.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    3. Click **New registration**.

CHANGE 3  [### Creating an Azure App for Enterprise OAuth]  (line 73 in repo file)

  OLD (remove from repo):
    6. Now, click **API Permissions**. <img alt="API_permission.png" src="/images/kb/0EM5w000005vO6o.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> 7. Click **+Add Permissions** button.

  NEW (replace with / insert from CSV):
    6. Now, click **API Permissions**.
    
    
       <img alt="API_permission.png" src="/images/kb/0EM5w000005vO6o.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    7. Click **+Add Permissions** button.
    
    
       <img alt="Capture8.PNG" src="/images/kb/0EM5w000005vO6u.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    
    8. Select **Microsoft Graph > Delegated Permissions**.

CHANGE 4  [## Connecting to Your OneDrive for Business Account]  (line 81 in repo file)

  OLD (remove from repo):
    This section enumerates the **Permission** settings within Microsoft needed for Domo access as well as options in the ** Credentials** and ** Details** panes in the OneDrive for Business Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the **Permission** settings within Microsoft needed for Domo access as well as options in the **Credentials** and **Details** panes in the OneDrive for Business Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 5  [### Credentials Pane]  (lines 87–104 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your OneDrive for Business client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your OneDrive for Business client secret.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Client ID | Enter your OneDrive for Business client ID. |
    | Client Secret | Enter your OneDrive for Business client secret. |
    

CHANGE 6  [### Details Pane]  (lines 112–113 in repo file)

  OLD (remove from repo):
    |------|-------------|
    | Report | Select the OneDrive for Business report you want to run. The following reports are available: <table border="1" cellpadding="1" cellspacing="1"><tbody><tr><th>Report Name</th><th>Description</th></tr><tr><td>Children</td><td>Lists the children of the item with the specified folder or path.</td></tr><tr><td>Default Drive</td><td>Returns metadata about the authenticated user's default drive in OneDrive.</td></tr><tr><td>Download Children Shared File - DEPRECATED</td><td>Downloads a shared file from the children folder into Domo as a DataSet. You specify the file type, file or folder path, and other information. <Note>This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.</Note></td></tr><tr><td>Download File</td><td>Downloads a file into Domo as a DataSet. You specify the file type, file or folder path, and other information.</td></tr><tr><td>Download Nested Children Shared File - DEPRECATED</td><td>Downloads a shared file from a nested children folder into Domo as a DataSet. You specify the folder name and the complete file path, along with other information. <Note>This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.</Note></td></tr><tr><td>Download Shared File (Search Endpoint) - DEPRECATED</td><td>Downloads a shared file into Domo as a DataSet. You specify the file type, file or folder path, and other information. <Note>This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.</Note></td></tr><tr><td>Download Shared File (SharedWithMe Endpoint) - DEPRECATED</td><td>Downloads a shared file into Domo as a DataSet. You specify the file type and file/folder or path, along with other information. <Note>This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.</Note></td></tr><tr><td>Download Shared File (Using SharingURL)</td><td>Downloads a shared file into Domo as a DataSet. You specify the file type, file or folder path, and other information.</td></tr><tr><td>Drive By Id</td><td>Lists drives for a selected drive ID.</td></tr><tr><td>Drives</td><td>Lists the available drives for the authenticated user.</td></tr><tr><td>Properties</td><td>Returns properties for the item with the specified file/folder or path.</td></tr><tr><td>Recent Items</td><td>Returns a list of items that have been recently used by the authenticated user. This list includes items that are in the user's drive as well as items they have access to from other drives.</td></tr><tr><td>Shared Items - DEPRECATED</td><td>Lists items that are shared with the authenticated user. <Note>This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.</Note></td></tr></tbody></table> |

  NEW (replace with / insert from CSV):
    | --- | --- |
    | Report | Select the OneDrive for Business report you want to run. The following reports are available:   |  |  |  | | --- | --- | --- | |  | **Report Name** | **Description** | | Most Used Reports | Download Shared File (Using SharingURL) | This report requires a URLinput for the file to download. The advanced options are identical to those of other reports. | | Other Reports | Children | Lists the children of the item specified by the folder or path. | | Default Drive | Returns metadata about the authenticated user's default drive in OneDrive | | Download Children Shared File (SharedWithMe Endpoint) - DEPRECATED |
    <Note>**Note:** This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.  Downloads a shared file from the children folder into Domo as a DataSet. You specify the file type, file or folder path, and other information. | | Download File | Downloads a file into Domo as a DataSet. You specify the file type, file or folder path, and other information. | | Download Nested Children Shared File - DEPRECATED | **Note:** This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.  Downloads a shared file from a nested children folder into Domo as a DataSet. You specify the folder name and the complete file path, along with other information. | | Download Shared File (Search Endpoint) - DEPRECATED | **Note:** This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.  Downloads a shared file into Domo as a DataSet. You specify the file type, file or folder path, and other information. | | Download Shared File (SharedWithMe Endpoint) - DEPRECATED | **Note:** This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.  Downloads a shared file into Domo as a DataSet. You specify the file type and file/folder or path, along with other information. | | Drive By Id | List drives for a selected drive ID. | | Drives | Lists the available drives for the authenticated user. | | Properties | Returns properties for the item with the specified file/folder or path. | | Recent Items | Returns a list of items that have been recently used by the authenticated user. This list includes items that are in the user's drive as well as items they have access to from other drives. | | Shared Items - DEPRECATED | **Note:** This report has been deprecated. Use the **Download Shared File (Using SharingURL)** report instead.  Lists items that are shared with the authenticated user. | |

CHANGE 7  [### Details Pane]  (lines 115–117 in repo file)

  OLD (remove from repo):
    | Path | Enter the relative path of the file or folder you want to retrieve a report for. For example: `/Documents/test.csv` |
    | File/Folder Name | Enter the name (or a portion of the name), without extension, of the MS OneDrive file/folder to retrieve. The connector returns the most recently created or modified file matching the name, based on what you have selected in the **Date Field** menu. |
    | Match Type | Specify whether the retrieve file starts with or contains the name you have entered in **File Name**. |

  NEW (replace with / insert from CSV):
    | Path | Enter the relative path of the file or folder you want to retrieve a report for. For example: /Documents/test.csv |
    | File/Folder Name | Enter the name (or a portion of the name), without extension, of the MS OneDrive file/folder to retrieve. The connector returns the most recently created or modified file matching the name, based on what you have selected in the **Date Field** menu. |
    | Match Type | Specify whether the retrieve file starts with or contains the name you have entered in **File Name**. |

CHANGE 8  [### Details Pane]  (line 119 in repo file)

  OLD (remove from repo):
    | Date Field | Specify whether to retrieve a file based on its creation or last modified date. This is useful when there are multiple files containing the name you have entered in **File Name**. |

  NEW (replace with / insert from CSV):
    | Date Field | Specify whether to retrieve a file based on its creation or last modified date. This is useful when there are multiple files containing the name you have entered in **File Name**. |

CHANGE 9  [### Details Pane]  (line 126 in repo file)

  OLD (remove from repo):
    | Drive ID | Select the drive you want to retrieve information for. |

  NEW (replace with / insert from CSV):
    | Drive ID | Select the drive you want to retrieve information for. |</Note>

CHANGE 10  [## FAQ]  (line 140 in repo file)

  OLD (remove from repo):
      - **OneDrive Generation:** Utilize the URL generated by ** OneDrive** upon selecting ** Copy link** from the ** Share** menu.

  NEW (replace with / insert from CSV):
      - **OneDrive Generation:** Utilize the URL generated by **OneDrive** upon selecting **Copy link** from the **Share** menu.

==============================================================================
FILE:   s/article/360043436873.mdx
TITLE:  Amazon Aurora Writeback Connector
LANG:   English (en_US)
ID:     360043436873
==============================================================================

CHANGE 1  [## Intro]  (lines 12–13 in repo file)

  OLD (remove from repo):
    
    ---

CHANGE 2  [## Configuring the Connection]  (line 34 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Aurora Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Aurora Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 40–81 in repo file)

  ⚠ LARGE CHANGE (52 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client secret.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Host</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Aurora database hostname.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Port</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Aurora database port number.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of your Aurora database or schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Aurora username.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Aurora password.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Certificate</p></td>
          <td colspan="1" rowspan="1"><p>Enter the URL where the SSL Ca Certificate is located (optional).</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domo Client ID | Enter your Domo client ID. |
    | Domo Client Secret | Enter your Domo client secret. |
    | Host | Enter your Aurora database hostname. |
    | Port | Enter your Aurora database port number. |
    | Database Name | Enter the name of your Aurora database or schema. |
    | Username | Enter your Aurora username. |
    | Password | Enter your Aurora password. |
    | Certificate | Enter the URL where the SSL Ca Certificate is located (optional). |

CHANGE 4  [### Details Pane]  (lines 91–124 in repo file)

  ⚠ LARGE CHANGE (42 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Input DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the DataSet ID (GUID) for the DataSet you want to copy to Aurora. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the table name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the table in your Aurora database where you want to copy your DataSet data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Pre-Existing Table</p></td>
          <td colspan="1" rowspan="1"><p>Select the table to write the dataset data to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Operation Type</p></td>
          <td colspan="1" rowspan="1"><p>Select <b>Create New Table</b> or <b> Update An Existing Table</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Append or Overwrite</p></td>
          <td colspan="1" rowspan="1"><p>Select <b>Append Data</b> or <b> Overwrite with New Data</b>.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Input DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to Aurora. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4. |
    | Select Table Name | Select the table name. |
    | Table Name | Enter the name of the table in your Aurora database where you want to copy your DataSet data. |
    | Pre-Existing Table | Select the table to write the dataset data to. |
    | Operation Type | Select **Create New Table** or **Update An Existing Table**.   | Operation | Description | | --- | --- | | Create a New Table | Creates a new table. (This creates a new table for every execution. The table name is the name specified above, and a numeric counter.) | | Update an Existing Table | Enables you to update an existing table (if the table name matches the existing one in the MySQL Server; otherwise, it creates a new table in the first run). | |
    | Append or Overwrite | Select **Append Data** or **Overwrite with New Data**.   | Operation | Description | | --- | --- | | Create a New Table | Creates a new table. (This creates a new table for every execution. The table name is the name specified above, additionally, a numeric counter). | | Update an Existing Table | Enables you to update an existing table (A new table will be created on the first run, if a table with the specified name cannot be found). | |

==============================================================================
FILE:   s/article/360043437013.mdx
TITLE:  Microsoft SQL Writeback Connector
LANG:   English (en_US)
ID:     360043437013
==============================================================================

CHANGE 1  [## Configuring the Connection]  (line 35 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the MS SQL Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the MS SQL Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 2  [### Credentials Pane]  (lines 41–82 in repo file)

  ⚠ LARGE CHANGE (52 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client secret.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter your MS SQL username.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter your MS SQL password.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Host</p></td>
          <td colspan="1" rowspan="1"><p>Enter your MS SQL database hostname.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Port</p></td>
          <td colspan="1" rowspan="1"><p>Enter your MS SQL database port number.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of your MS SQL database.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Certificate</p></td>
          <td colspan="1" rowspan="1"><p>Paste the text for your MS SQL SSL CA certificate.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domo Client ID | Enter your Domo client ID. |
    | Domo Client Secret | Enter your Domo client secret. |
    | Username | Enter your MS SQL username. |
    | Password | Enter your MS SQL password. |
    | Host | Enter your MS SQL database hostname. |
    | Port | Enter your MS SQL database port number. |
    | Database | Enter the name of your MS SQL database. |
    | Certificate | Paste the text for your MS SQL SSL CA certificate. |

CHANGE 3  [### Details Pane]  (lines 92–161 in repo file)

  ⚠ LARGE CHANGE (87 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Input DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the DataSet ID (GUID) for the DataSet you want to copy to MS SQL. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>My input DataSet is large</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to export a large dataset from Domo to Microsoft SQL Server.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Schema Name</p></td>
          <td colspan="1" rowspan="1"><p>Specify the schema to use when creating the table.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Name Source</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to name the table where data will be copied.   - <b>DataSet ID</b>. The table name will be the number you entered for <b> DataSet ID</b>. - <b> DataSet Name</b>. The table name will be the same as that of the input DataSet. - <b> Custom Name</b>. You will give the table a custom name in the <b> Custom Table Name</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the table in your MS SQL database where you want your DataSet data to be copied.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Operation Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the operation you want to perform:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Update Type</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to update the database table:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Insert Using Select Into statements</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox if you want to upload your data using <b>Select Into</b> statements.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Merge Keys</p></td>
          <td colspan="1" rowspan="1"><p>Enter the upsert/merge key. By default, sys\_id will be used as an upsert/merge key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>SQL Data Type for Integer Values</p></td>
          <td colspan="1" rowspan="1"><p>The SQL Data Type for Integer Values dropdown lists <b>INT</b> and <b> BIGINT</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>SQL Data Type for String Values</p></td>
          <td colspan="1" rowspan="1"><p>The SQL Data Type for String Values dropdown lists <b>VARCHAR</b> and <b> NVARCHAR</b> data types.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>String Size</p></td>
          <td colspan="1" rowspan="1"><p>Choose whether to use the maximum size for STRING types (VARCHAR/NVARCHAR) or specify a custom size.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom String Size</p></td>
          <td colspan="1" rowspan="1"><p>Enter the size for STRING types (VARCHAR/NVARCHAR)</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Fire Triggers</p></td>
          <td colspan="1" rowspan="1"><p>This causes the server to fire the insert triggers for the inserted database rows.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enbale Data Validation</p></td>
          <td colspan="1" rowspan="1"><p>Select the checkbox to enable data type validation and sanitization.   If selected, invalid values (such as text in numeric columns) will be converted to NULL, preventing upload failures and ensuring smoother data quality management.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Input DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to MS SQL. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4. |
    | My input DataSet is large | Select this checkbox to export a large dataset from Domo to Microsoft SQL Server. |
    | Schema Name | Specify the schema to use when creating the table. |
    | Table Name Source | Select how you want to name the table where data will be copied.   - **DataSet ID**. The table name will be the number you entered for **DataSet ID**. - **DataSet Name**. The table name will be the same as that of the input DataSet. - **Custom Name**. You will give the table a custom name in the **Custom Table Name** field. |
    | Custom Table Name | Enter the name of the table in your MS SQL database where you want your DataSet data to be copied. |
    | Operation Type | Select the operation you want to perform:   | Operation | Description | | --- | --- | | Create a New Table | Select this option to create a new table for every run. The table name uses the name in the Table Name Source field plus a numeric counter. | | Update an Existing Table | Enables you to update an existing table if the table name matches the one in Microsoft SQL Server. If no match is found, it creates a new table during the first run. | |
    | Update Type | Select how you want to update the database table:   |  |  | | --- | --- | | Append Data | Append the fetched data to the existing table. | | Overwrite With New Data | Overwrites all the existing data with the new data in the existing table. | | Merge Data | Merges the new version of data with the existing version into the existing table. |   **Note**: If the table does not exist during the first run, the system creates a new table. In subsequent runs, the system updates the table based on the selected option. |
    | Insert Using Select Into statements | Select this checkbox if you want to upload your data using **Select Into** statements. |
    | Merge Keys | Enter the upsert/merge key. By default, sys\_id will be used as an upsert/merge key. |
    | SQL Data Type for Integer Values | The SQL Data Type for Integer Values dropdown lists **INT** and **BIGINT**. |
    | SQL Data Type for String Values | The SQL Data Type for String Values dropdown lists **VARCHAR** and **NVARCHAR** data types. |
    | String Size | Choose whether to use the maximum size for STRING types (VARCHAR/NVARCHAR) or specify a custom size. |
    | Custom String Size | Enter the size for STRING types (VARCHAR/NVARCHAR) |
    | Fire Triggers | This causes the server to fire the insert triggers for the inserted database rows. |
    | Enbale Data Validation | Select the checkbox to enable data type validation and sanitization.   If selected, invalid values (such as text in numeric columns) will be converted to NULL, preventing upload failures and ensuring smoother data quality management. |

==============================================================================
FILE:   s/article/360043437093.mdx
TITLE:  Snowflake Writeback Connector
LANG:   English (en_US)
ID:     360043437093
==============================================================================

CHANGE 1  [(top of file)]  (line 5 in repo file)

  OLD (remove from repo):
    <Note>**Note:** ** Snowflake will no longer support the username and password authentication mechanism as of November 2025. As a result, this connector will be retired in November 2025 and will no longer function. Please consider using one of the following connection methods instead:**</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** **Snowflake will no longer support the username and password authentication mechanism as of November 2025. As a result, this connector will be retired in November 2025 and will no longer function. Please consider using one of the following connection methods instead:**</Note>

CHANGE 2  [## Prerequisites]  (lines 29–32 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    <Frame>
    ![snowflake_account_names.png](/images/kb/0EM5w000005vO9R.png)
    </Frame>

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    
    
      <img alt="snowflake_account_names.png" src="/images/kb/0EM5w000005vO9R.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>

CHANGE 3  [## Prerequisites]  (line 40 in repo file)

  OLD (remove from repo):
    <Note>**Note:** When creating a new client, if you use multiple Domo instances, make sure you're logged into the Developer Portal using the correct instance.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** When creating a new client, if you use multiple Domo instances, make sure you're logged into the Developer Portal using the correct instance.
      3. When creating the client, select ALL of the scopes. (Account, Audit, Buzz, Data, Dashboard, User, and Workflow.)</Note>

CHANGE 4  [## Prerequisites]  (line 42 in repo file)

  OLD (remove from repo):
    3. When creating the client, select ALL of the scopes. (Account, Audit, Buzz, Data, Dashboard, User, and Workflow.) <img alt="snowflake_writeback_new_client.png" src="/images/kb/0EM5w000005vO9S.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> 4. Click **Create**.

  NEW (replace with / insert from CSV):
         <img alt="snowflake_writeback_new_client.png" src="/images/kb/0EM5w000005vO9S.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
      4. Click ****Create****.

CHANGE 5  [## Prerequisites]  (line 44 in repo file)

  OLD (remove from repo):
    <Warning>

CHANGE 6  [## Prerequisites]  (line 53 in repo file)

  OLD (remove from repo):
    </Warning>

CHANGE 7  [## Configuring the connection]  (line 57 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 8  [### Credentials Pane]  (lines 63–96 in repo file)

  ⚠ LARGE CHANGE (43 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the account name for your Snowflake account. For information about finding your account name, see <a href="#prerequisites">Prerequisites</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username associated with your Snowflake account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password associated with your Snowflake account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client secret.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the account name for your Snowflake account. For information about finding your account name, see [Prerequisites](#prerequisites). |
    | Username | Enter the username associated with your Snowflake account. |
    | Password | Enter the password associated with your Snowflake account. |
    | Role | Enter your role. |
    | Domo Client ID | Enter your Domo client ID. |
    | Domo Client Secret | Enter your Domo client secret. |
    

CHANGE 9  [### Details Pane]  (lines 103–136 in repo file)

  ⚠ LARGE CHANGE (43 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Parameter</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Input DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the DataSet ID (GUID) for the DataSet you want to copy to Snowflake. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to name the table where data will be copied.   - <b>Use Input DataSet GUID</b>. The table name will be the number you entered for <b> Input</b><b>DataSet ID</b>. - <b> Use Input DataSet Name</b>. The table name will be the same as that of the input DataSet. - <b> Enter Table Name</b>. You will give the table a custom name in the <b> Custom Table Name</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the Snowflake table you want your data copied to (spaces and special characters will be replaced with underscores).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the table you want to send data to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the table you want to send data to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Schema</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Parameter | Description |
    | --- | --- |
    | Input DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to Snowflake. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4. |
    | Select Table Name | Select how you want to name the table where data will be copied.   - ****Use Input DataSet GUID****. The table name will be the number you entered for **Input******DataSet ID****. - ****Use Input DataSet Name****. The table name will be the same as that of the input DataSet. - **Enter Table Name**. You will give the table a custom name in the ****Custom Table Name**** field. |
    | Enter Table Name | Enter the name of the Snowflake table you want your data copied to (spaces and special characters will be replaced with underscores). |
    | Warehouses | Select the warehouse containing the table you want to send data to. |
    | Databases | Select the database containing the table you want to send data to. |
    | Database Schema | Select the database schema containing the data you want to retrieve. |
    

CHANGE 10  [### FAQs]  (line 143 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### When should I use this connector?

CHANGE 11  [### FAQs]  (line 145 in repo file)

  OLD (remove from repo):
    <Accordion title="When should I use this connector?">

CHANGE 12  [### FAQs]  (line 147 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 13  [### FAQs]  (line 149 in repo file)

  OLD (remove from repo):
    <Accordion title="What permissions do I need on Snowflake?">

  NEW (replace with / insert from CSV):
    #### What permissions do I need on Snowflake?
    

CHANGE 14  [### FAQs]  (line 151 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 15  [### FAQs]  (line 153 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up the Snowflake Writeback connector?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up the Snowflake Writeback connector?
    

CHANGE 16  [### FAQs]  (line 155 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 17  [### FAQs]  (line 157 in repo file)

  OLD (remove from repo):
    <Accordion title="Where can I find my Domo client id and client secret?">

  NEW (replace with / insert from CSV):
    #### Where can I find my Domo client id and client secret?
    

CHANGE 18  [### FAQs]  (line 161 in repo file)

  OLD (remove from repo):
    - In the top right corner under **My Account** click ** New Client**.

  NEW (replace with / insert from CSV):
    - In the top right corner under **My Account** click **New Client**.

CHANGE 19  [### FAQs]  (line 163 in repo file)

  OLD (remove from repo):
    - Provide the application scope by selecting the checkboxes for **Data** and ** User**.

  NEW (replace with / insert from CSV):
    - Provide the application scope by selecting the checkboxes for **Data** and **User**.

CHANGE 20  [### FAQs]  (line 167 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 21  [### FAQs]  (line 169 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 22  [### FAQs]  (line 171 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 23  [### FAQs]  (line 173 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 24  [### FAQs]  (line 175 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 25  [### FAQs]  (line 177 in repo file)

  OLD (remove from repo):
    <Accordion title="Can I use the same Snowflake Writeback account for multiple datasets?">

  NEW (replace with / insert from CSV):
    #### Can I use the same Snowflake Writeback account for multiple datasets?
    

CHANGE 26  [### FAQs]  (line 179 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 27  [### FAQs]  (line 181 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?
    

CHANGE 28  [### FAQs]  (line 183 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 29  [### FAQs]  (line 185 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I find the Input Dataset ID?">

  NEW (replace with / insert from CSV):
    #### How do I find the Input Dataset ID?
    

CHANGE 30  [### FAQs]  (lines 187–189 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360043437533.mdx
TITLE:  NetSuite Connector - DEPRECATED
LANG:   English (en_US)
ID:     360043437533
==============================================================================

CHANGE 1  [### Intro]  (lines 20–46 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>This connector is great for extracting REST data in JSON format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>Most appropriate for ERP/finance roles.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>4-8 hours</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>8</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | This connector is great for extracting REST data in JSON format. |
    | **Primary Company Roles** | Most appropriate for ERP/finance roles. |
    | **Average Implementation Time** | 4-8 hours |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 8 |

CHANGE 2  [## Configuration Instructions]  (lines 81–82 in repo file)

  OLD (remove from repo):
    10. For the **Status**, select ** Released** and leave the ** Log Level** at ** Debug**.
    11. For the **Audience**, please check ** Select All** for ** Roles**, ** Employees**, and ** Partners**.

  NEW (replace with / insert from CSV):
    10. For the **Status**, select **Released** and leave the **Log Level** at **Debug**.
    11. For the **Audience**, please check **Select All** for **Roles**, **Employees**, and **Partners**.

CHANGE 3  [## Connecting to Your NetSuite Data in Domo]  (line 91 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the NetSuiteApp Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the NetSuiteApp Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (line 97 in repo file)

  OLD (remove from repo):
    Once you have connected to NetSuite, you can use the same account any time you go to create a new NetSuiteApp DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    Once you have connected to NetSuite, you can use the same account any time you go to create a new NetSuiteApp DataSet. You can manage connector accounts in the **Accounts**tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 5  [### Credentials Pane]  (lines 99–137 in repo file)

  ⚠ LARGE CHANGE (48 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p><b>Field</b></p></th>
          <th colspan="1" rowspan="1"><p><b> Description</b></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Environment</p></td>
          <td colspan="1" rowspan="1"><p>Select the environment (Production or Sandbox).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite username.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite password.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite Role ID. It's only required if no default role is specified or if you do not want to use the default role.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Account ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your NetSuite account ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>NetSuite Script Bundle ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the script variable from the NetSuite bundle URL.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>NetSuite Deploy Bundle ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the deploy variable from the NetSuite bundle URL.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | **Field** | **Description** |
    | --- | --- |
    | Environment | Select the environment (Production or Sandbox). |
    | Username | Enter your NetSuite username. |
    | Password | Enter your NetSuite password. |
    | Role ID | Enter your NetSuite Role ID. It's only required if no default role is specified or if you do not want to use the default role. |
    | Account ID | Enter your NetSuite account ID. |
    | NetSuite Script Bundle ID | Enter the script variable from the NetSuite bundle URL. |
    | NetSuite Deploy Bundle ID | Enter the deploy variable from the NetSuite bundle URL. |

CHANGE 6  [#### Choosing Your Environment]  (line 141 in repo file)

  OLD (remove from repo):
    In the **Environment** menu, select ** Sandbox** if your NetSuite account is a sandbox account. You can find this by hovering over your name/account in the top right corner of NetSuite and clicking ** View all roles**. Your environment should be listed under ** Account Type**.

  NEW (replace with / insert from CSV):
    In the **Environment** menu, select **Sandbox** if your NetSuite account is a sandbox account. You can find this by hovering over your name/account in the top right corner of NetSuite and clicking **View all roles**. Your environment should be listed under **Account Type**.

CHANGE 7  [### Details Pane]  (lines 173–219 in repo file)

  ⚠ LARGE CHANGE (58 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p><b>Menu</b></p></th>
          <th colspan="1" rowspan="1"><p><b> Description</b></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Saved Search ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the NetSuite Saved Search you want to run.  Example: For customsearch776, you would only enter 776.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date From</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date From Offset</p></td>
          <td colspan="1" rowspan="1"><p>This adjusts the current date to the given number of days into the past. Example: 0 for today, 1 for yesterday.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date From</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using the date picker. This option is only available when Report Date is set to 'Specific.'</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date To</p></td>
          <td colspan="1" rowspan="1"><p>Select report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date To Offset</p></td>
          <td colspan="1" rowspan="1"><p>This adjusts the current date the given number of days into the past. Example: 0 for today, 1 for yesterday.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Specific Date To</p></td>
          <td colspan="1" rowspan="1"><p>Select the report date using the date picker. This option is only available when Report Date is set to 'Specific.'</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Duplicate Row Handling</p></td>
          <td colspan="1" rowspan="1"><p>Select <b>Remove Duplicate Rows</b> to remove duplicate rows in your dataset. Select <b> Keep Duplicate Rows</b> to keep duplicate rows in your dataset.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Big Number Handling</p></td>
          <td colspan="1" rowspan="1"><p>Select <b>Treat Big Numbers As Strings</b> to treat numbers with over 35 digits as strings.  Select <b> Truncate Big Numbers</b> to truncate them.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | **Menu** | **Description** |
    | --- | --- |
    | Saved Search ID | Enter the NetSuite Saved Search you want to run.  Example: For customsearch776, you would only enter 776. |
    | Date From | Select the report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. |
    | Date From Offset | This adjusts the current date to the given number of days into the past. Example: 0 for today, 1 for yesterday. |
    | Select Specific Date From | Select the report date using the date picker. This option is only available when Report Date is set to 'Specific.' |
    | Date To | Select report date using relative or specific dates. Relative meaning number of days from today or a specific date using the date selector. |
    | Date To Offset | This adjusts the current date the given number of days into the past. Example: 0 for today, 1 for yesterday. |
    | Select Specific Date To | Select the report date using the date picker. This option is only available when Report Date is set to 'Specific.' |
    | Duplicate Row Handling | Select **Remove Duplicate Rows** to remove duplicate rows in your dataset. Select **Keep Duplicate Rows** to keep duplicate rows in your dataset. |
    | Big Number Handling | Select **Treat Big Numbers As Strings** to treat numbers with over 35 digits as strings.  Select **Truncate Big Numbers** to truncate them. |

==============================================================================
FILE:   s/article/360052122394.mdx
TITLE:  Amazon Athena Partition Connector
LANG:   English (en_US)
ID:     360052122394
==============================================================================

CHANGE 1  [## Intro]  (lines 11–12 in repo file)

  OLD (remove from repo):
    ---
    

CHANGE 2  [## Prerequisites]  (line 19 in repo file)

  OLD (remove from repo):
    - The Amazon S3 location to which the query output is written (Example: s3://aws-athena-query-results-1234-us-west-2/). You can find this by going to **Settings** in the Athena web console.

  NEW (replace with / insert from CSV):
    - The Amazon S3 location to which the query output is written (Example: s3://aws-athena-query-results-1234-us-west-2/). You can find this by going to ****Settings**** in the Athena web console.

CHANGE 3  [## Connecting to Your Amazon Athena Account]  (line 25 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Amazon Athena Partition Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Amazon Athena Partition Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 31–60 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS access key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Secret Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS secret key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>S3 Staging Directory</p></td>
          <td colspan="1" rowspan="1"><p>Enter the S3 staging directory to which the query output will be written. For more information, see <a href="#h_01EFFRDDXA0TM5J5DRR6WR3K88">Prerequisites</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Region</p></td>
          <td colspan="1" rowspan="1"><p>Select your AWS region.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Workgroup</p></td>
          <td colspan="1" rowspan="1"><p>Enter the workgroup.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Access Key | Enter your AWS access key. |
    | Secret Key | Enter your AWS secret key. |
    | S3 Staging Directory | Enter the S3 staging directory to which the query output will be written. For more information, see [Prerequisites](#h_01EFFRDDXA0TM5J5DRR6WR3K88). |
    | Region | Select your AWS region. |
    | Workgroup | Enter the workgroup. |
    

CHANGE 5  [### Details Pane]  (lines 67–104 in repo file)

  ⚠ LARGE CHANGE (48 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query you want to execute (Example: select \* from table\_name).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the database you want to query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the table in the database you want to query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days you want to get data for. Value can be X, where X is a positive integer. Example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the required date format. By default <b>yyyy-MM-dd</b> will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom date format you wish to apply.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Query | Enter the SQL query you want to execute (Example: select \* from table\_name). |
    | Database Name | Select the database you want to query. |
    | Table Name | Select the table in the database you want to query. |
    | Partition Column Name | Select the partition column name. |
    | Past Days | Enter the number of past days you want to get data for. Value can be X, where X is a positive integer. Example: 30. |
    | Date Format | Select the required date format. By default **yyyy-MM-dd** will be used. |
    | Custom Date Format | Enter the custom date format you wish to apply. |
    

==============================================================================
FILE:   s/article/360052884153.mdx
TITLE:  JIRA Cloud Connector
LANG:   English (en_US)
ID:     360052884153
==============================================================================

CHANGE 1  [## Intro]  (line 11 in repo file)

  OLD (remove from repo):
    The JIRA connector is a "Cloud App" connector, meaning it retrieves data stored in the cloud. In the Data Center, you can access the connector page for this and other Cloud App connectors by clicking **Cloud App** in the toolbar at the top of the window.

  NEW (replace with / insert from CSV):
    The JIRA connector is a "Cloud App" connector, meaning it retrieves data stored in the cloud. In the Data Center, you can access the connector page for this and other Cloud App connectors by clicking ****Cloud App**** in the toolbar at the top of the window.

CHANGE 2  [## Intro]  (lines 15–44 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>- Tracking progress and movement of tickets - Number of tickets assigned to specific users or groups - Seeing if too many tickets of a certain type are open (bugs)</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- Bug / issue / ticket tracking (by age, type, status, submitter) - Completed tickets per team - Ticket resolution time - Project management metrics (tracking individual tasks of a larger project or epic).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>Product managers</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>This can vary wildly depending on the implementation and the user's level of knowledge of their JIRA system.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>7</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | ****Primary Use Cases**** | - Tracking progress and movement of tickets - Number of tickets assigned to specific users or groups - Seeing if too many tickets of a certain type are open (bugs) |
    | ****Primary Metrics**** | - Bug / issue / ticket tracking (by age, type, status, submitter) - Completed tickets per team - Ticket resolution time - Project management metrics (tracking individual tasks of a larger project or epic). |
    | ****Primary Company Roles**** | Product managers |
    | ****Average Implementation Time**** | This can vary wildly depending on the implementation and the user's level of knowledge of their JIRA system. |
    | ****Ease of Use (on a 1-to-10 scale with 1 being easiest)**** | 7 |
    

CHANGE 3  [## Connecting to Your JIRA Cloud Account]  (line 59 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes on the Jira Cloud Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the Jira Cloud Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 65–78 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domain</p></td>
          <td colspan="1" rowspan="1"><p>The domain of your JIRA Cloud account. Jira Server accounts are not supported for OAuth.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domain | The domain of your JIRA Cloud account. Jira Server accounts are not supported for OAuth. |
    

CHANGE 5  [#### Get JIRA Issue Data]  (line 87 in repo file)

  OLD (remove from repo):
    When you select **Get JIRA Issue Data**, a JQL Query Builder appears on the screen. Here you can build JIRA queries to returns columns for nearly every JIRA ticket parameter, like issue summary and description, creation date, resolution date, impacted customer name, and environment. You can also create a JQL query in JIRA and paste it into the query builder.

  NEW (replace with / insert from CSV):
    When you select ****Get JIRA Issue Data****, a JQL Query Builder appears on the screen. Here you can build JIRA queries to returns columns for nearly every JIRA ticket parameter, like issue summary and description, creation date, resolution date, impacted customer name, and environment. You can also create a JQL query in JIRA and paste it into the query builder.

CHANGE 6  [#### Get JIRA Issue Data]  (line 105 in repo file)

  OLD (remove from repo):
    If you would prefer to pull in premade reports rather than build custom queries, you can do so by expanding the **Report Selection** tab and then choosing the desired report type. The following reports are available:

  NEW (replace with / insert from CSV):
    If you would prefer to pull in premade reports rather than build custom queries, you can do so by expanding the ****Report Selection**** tab and then choosing the desired report type. The following reports are available:

CHANGE 7  [#### Get JIRA Issue Data]  (lines 107–160 in repo file)

  ⚠ LARGE CHANGE (67 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Field</b></p></td>
          <td colspan="1" rowspan="1"><p><b> Description</b></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>JQL Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter a JQL query to pull custom data. For information see, <a href="https://confluence.atlassian.com/display/JIRA/Advanced+Searching">Jira Documentation.</a></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Data Option</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Filter Type</p></td>
          <td colspan="1" rowspan="1"><p>Select to include or exclude specific fields from the returned dataset.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Flatten All Fields</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to flatten all sublists.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Sublists To Flatten</p></td>
          <td colspan="1" rowspan="1"><p>Enter the sublists to be flattened.  Note: If you clear the <b>Flatten All Fields</b> checkbox, the <b> Sublists To Flatten</b> field appears.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Include All Sprints</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to pull data for all sprints. If not selected, only data for active sprints will be retrieved.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Reset Schema</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox to only include columns that have been imported in the current execution. Please note this option only works for the Replace update method.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Group Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the group that contains the users you wish to retrieve. This report requires an Administrator account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Project</p></td>
          <td colspan="1" rowspan="1"><p>Select the JIRA Project you want to retrieve data for.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Field** | **Description** |
    | Report | |  |  | | --- | --- | | **Report Name** | **Description** | | Issue Query | Allows the user to query issues using JQL (JIRA Query Language). | | Fields | Returns a list of fields used in the user's JIRA account. | | Group Members | Returns a list of users that belong to the specified group. You must have admin access to run this report. | | Projects | Returns a list of all projects the user has access to. | | Project Versions | Returns a list of all versions of the specified project. | | Users Groups | Returns a list of groups for all users. | |
    | JQL Query | Enter a JQL query to pull custom data. For information see, [Jira Documentation.](https://confluence.atlassian.com/display/JIRA/Advanced+Searching) |
    | Data Option | |  |  | | --- | --- | | **Data Option** | **Description** | | Comments Report | Returns JIRA tickets with their related comments. | | Changelog Report | Returns changelogs per issue applicable to the query. The Changelog report is only available to Jira Cloud users. | | Labels Report | Returns labels for issues applicable to the query. | | Linked Issues Report | Returns linked issues for issues applicable to the query. | | Sprint Report | Returns details of sprints for issues applicable to the query. | | Standard Ticket Report | Returns columns for nearly every parameter for JIRA tickets, such as issue summary and description, creation date, resolution date, impacted customer name, environment, etc. | | Ticket History Report | Returns historical information about tickets. | | Worklog Report | Returns worklogs per issue applicable to the query. | |
    | Filter Type | Select to include or exclude specific fields from the returned dataset. |
    | Flatten All Fields | Select this checkbox to flatten all sublists. |
    | Sublists To Flatten | Enter the sublists to be flattened.  Note: If you clear the **Flatten All Fields**checkbox, the **Sublists To Flatten** field appears. |
    | Include All Sprints | Select this checkbox to pull data for all sprints. If not selected, only data for active sprints will be retrieved. |
    | Reset Schema | Select this checkbox to only include columns that have been imported in the current execution. Please note this option only works for the Replace update method. |
    | Group Name | Enter the group that contains the users you wish to retrieve. This report requires an Administrator account. |
    | Project | Select the JIRA Project you want to retrieve data for. |

CHANGE 8  [### FAQ]  (line 164 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### When should I use this connector?

CHANGE 9  [### FAQ]  (line 166 in repo file)

  OLD (remove from repo):
    <Accordion title="When should I use this connector?">

CHANGE 10  [### FAQ]  (line 168 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 11  [### FAQ]  (lines 170–209 in repo file)

  ⚠ LARGE CHANGE (41 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <Accordion title="Which endpoint(s) does each report call in this connector?">
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Report Name</b></p></td>
          <td colspan="1" rowspan="1"><p><b> Endpoint URL</b></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Issue Query</p></td>
          <td colspan="1" rowspan="1"><p>/rest/api/2/search</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Fields</p></td>
          <td colspan="1" rowspan="1"><p>/rest/api/2/field</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Group Members</p></td>
          <td colspan="1" rowspan="1"><p>/rest/api/2/group/member/{groupname}</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Projects</p></td>
          <td colspan="1" rowspan="1"><p>/rest/api/2/project</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Project Versions</p></td>
          <td colspan="1" rowspan="1"><p>/rest/api/2/project/{projectId}/versions</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Users Groups</p></td>
          <td colspan="1" rowspan="1"><p>/rest/api/2/user/groups</p></td>
        </tr>
      </tbody>
    </table>
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Which endpoint(s) does each report call in this connector?

CHANGE 12  [### FAQ]  (line 211 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Report Name** | **Endpoint URL** |
    | Issue Query | /rest/api/2/search |
    | Fields | /rest/api/2/field |
    | Group Members | /rest/api/2/group/member/{groupname} |
    | Projects | /rest/api/2/project |
    | Project Versions | /rest/api/2/project/{projectId}/versions |
    | Users Groups | /rest/api/2/user/groups |
    
    #### What kind of credentials do I need to power up this connector?
    

CHANGE 13  [### FAQ]  (line 213 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 14  [### FAQ]  (line 215 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 15  [### FAQ]  (line 217 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 16  [### FAQ]  (line 219 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I know if my query is right?">

  NEW (replace with / insert from CSV):
    #### How do I know if my query is right?
    

CHANGE 17  [### FAQ]  (line 221 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 18  [### FAQ]  (line 223 in repo file)

  OLD (remove from repo):
    <Accordion title="Can this connector pull custom fields?">

  NEW (replace with / insert from CSV):
    #### Can this connector pull custom fields?
    

CHANGE 19  [### FAQ]  (line 227 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 20  [### FAQ]  (line 229 in repo file)

  OLD (remove from repo):
    <Accordion title="Why will the connectors not reset the schema?">

  NEW (replace with / insert from CSV):
    #### Why will the connectors not reset the schema?
    

CHANGE 21  [### FAQ]  (line 231 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 22  [### FAQ]  (line 233 in repo file)

  OLD (remove from repo):
    </AccordionGroup>

CHANGE 23  [## Troubleshooting]  (line 237 in repo file)

  OLD (remove from repo):
    - If users have trouble understanding how to format JQL, the easiest thing is to log into the JIRA front end. Via the search bar, JIRA provides a tool that can construct a JQL query with preset filters. After constructing your query, if you hit the **Advanced** button, JIRA presents you with your selections in JQL format.

  NEW (replace with / insert from CSV):
    - If users have trouble understanding how to format JQL, the easiest thing is to log into the JIRA front end. Via the search bar, JIRA provides a tool that can construct a JQL query with preset filters. After constructing your query, if you hit the ****Advanced**** button, JIRA presents you with your selections in JQL format.

==============================================================================
FILE:   s/article/360057013754.mdx
TITLE:  Snowflake Partition Connector
LANG:   English (en_US)
ID:     360057013754
==============================================================================

CHANGE 1  [## Intro]  (lines 21–22 in repo file)

  OLD (remove from repo):
    ---
    

CHANGE 2  [## Prerequisites]  (line 27 in repo file)

  OLD (remove from repo):
    - The full name of your account (provided by Snowflake). For US West Region **account\_name**, and All other regions ** account\_name.region\_id**. This is the portion of your Snowflake URL immediately following https://.

  NEW (replace with / insert from CSV):
    - The full name of your account (provided by Snowflake). For US West Region ********account\_name********, and All other regions ********account\_name.region\_id********. This is the portion of your Snowflake URL immediately following https://.

CHANGE 3  [## Prerequisites]  (line 29 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345." <img alt="Region.png" src="/images/kb/0EM5w000005vO8h.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> - The username and password used to connect to your Snowflake host.

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    
    
      <img alt="Region.png" src="/images/kb/0EM5w000005vO8h.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    - The username and password used to connect to your Snowflake host.

CHANGE 4  [## Connect to Your Snowflake Account]  (line 33 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake Partition Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Partition Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 5  [## Credentials Pane]  (lines 39–64 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of your account (provided by Snowflake). For US West Region <b>account\_name</b>, and All other regions <b> account\_name.region\_id</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username associated with your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password associated with your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the name of your account (provided by Snowflake). For US West Region **account\_name**, and All other regions **account\_name.region\_id**. |
    | Username | Enter the username associated with your Snowflake host. |
    | Password | Enter the password associated with your Snowflake host. |
    | Role | Enter your role. |
    

CHANGE 6  [## Details Pane]  (lines 71–188 in repo file)

  ⚠ LARGE CHANGE (148 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to import data into Domo?</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to import your data by using the standard update method (replace/append), by using partition, or upsert mode.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>How would you like to build your Query?</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the query to execute. The query format must be like: select \* from \"databaseName\".\"schemaName\".\"tableName\"</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates all warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve. The list populates all databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Schemas</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates all schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Objects</p></td>
          <td colspan="1" rowspan="1"><p>Select the database object containing the data you want to retrieve. The list populates all objects you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to retrieve. The list populates all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Views</p></td>
          <td colspan="1" rowspan="1"><p>Select the view.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>A query is generated based on the selected fields. Copy and paste this text into the QUERY field. You may edit this query, if desired.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>A query is generated based on the selected fields. Copy and paste this text into the QUERY field. You may edit this query, if desired.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to partition your data using the date keys, non-date keys, or meta query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name. Only date fields will be shown in this discovery.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Support Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition support format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select a non-date key column name to partition the data. All fields excluding Date fields will appear in this dropdown.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Query to determine partition tags</p></td>
          <td colspan="1" rowspan="1"><p>Enter the partition meta query to determine the distinct partition tags (or keys). The column containing the <b>Date</b> data is your partition column.  Example: SELECT DISTINCT (DATE\_COLUMN\_NAME) FROM TABLENAME</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. Example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Years</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past years that you want to get data for. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Future Years</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of future years that you want to get data for. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Months</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past months that you want to get data for. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Future Months</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of future months that you want to get data for. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Please select/enter proper date format according to the date format present in partition column. By default <b>yyyy-MM-dd</b> will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom date format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Upsert Key Column(s)</p></td>
          <td colspan="1" rowspan="1"><p>Enter upsert key column name or a comma separated list of upsert key column names. <b>Required only when Update method is Merge</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Tag</p></td>
          <td colspan="1" rowspan="1"><p>Optional parameter that can be used to tag connector SQL executions in Snowflake as a <b>Query Tag</b>.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | How would you like to import data into Domo? | Select whether you want to import your data by using the standard update method (replace/append), by using partition, or upsert mode. |
    | How would you like to build your Query? | | Query Type | Description | | --- | --- | | Custom Query | Enter the SQL query to execute. | | Query Builder | Select a table and fields to autogenerate your query. | |
    | Query | Enter the query to execute. The query format must be like: select \* from \"databaseName\".\"schemaName\".\"tableName\" |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates all warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve. The list populates all databases you have access to based on your credentials. |
    | Schemas | Select the database schema containing the data you want to retrieve. The list populates all schemas you have access to based on your credentials. |
    | Database Objects | Select the database object containing the data you want to retrieve. The list populates all objects you have access to based on your credentials. |
    | Tables | Select the database table you want to retrieve. The list populates all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Views | Select the view. |
    | Table Column Name | Select the partition column name. |
    | Table Query Helper | A query is generated based on the selected fields. Copy and paste this text into the QUERY field. You may edit this query, if desired. |
    | View Column Name | Select the partition column name. |
    | View Query Helper | A query is generated based on the selected fields. Copy and paste this text into the QUERY field. You may edit this query, if desired. |
    | Partition Criteria | Select whether you want to partition your data using the date keys, non-date keys, or meta query. |
    | Partition Column Name | Select the partition column name. Only date fields will be shown in this discovery. |
    | Partition Support Format | Select the partition support format. |
    | Partition Non Date Key Column Name | Select a non-date key column name to partition the data. All fields excluding Date fields will appear in this dropdown. |
    | Partition Query to determine partition tags | Enter the partition meta query to determine the distinct partition tags (or keys). The column containing the **Date** data is your partition column.  Example: SELECT DISTINCT (DATE\_COLUMN\_NAME) FROM TABLENAME |
    | Past Days | Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. Example: 30. |
    | Past Years | Enter the number of past years that you want to get data for. Value can be X, where X is a positive integer. For example: 30. |
    | Future Years | Enter the number of future years that you want to get data for. Value can be X, where X is a positive integer. For example: 30. |
    | Past Months | Enter the number of past months that you want to get data for. Value can be X, where X is a positive integer. For example: 30. |
    | Future Months | Enter the number of future months that you want to get data for. Value can be X, where X is a positive integer. For example: 30. |
    | Date Format | Please select/enter proper date format according to the date format present in partition column. By default **yyyy-MM-dd** will be used. |
    | Custom Date Format | Enter the custom date format. |
    | Upsert Key Column(s) | Enter upsert key column name or a comma separated list of upsert key column names. **Required only when Update method is Merge**. |
    | Query Tag | Optional parameter that can be used to tag connector SQL executions in Snowflake as a **Query Tag**. |
    

==============================================================================
FILE:   s/article/360057028014.mdx
TITLE:  Azure Blob Storage SAS Token Writeback Connector
LANG:   English (en_US)
ID:     360057028014
==============================================================================

CHANGE 1  [## Prerequisites]  (line 20 in repo file)

  OLD (remove from repo):
    <Note>**Note:** This connector works only with the ** account access level SAS** token. So, if you receive an authentication error, please confirm if you are entering a container access level token instead.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** This connector works only with the **account access level SAS** token. So, if you receive an authentication error, please confirm if you are entering a container access level token instead.</Note>

CHANGE 2  [## Configuring the Connection]  (line 24 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Azure Blob Storage SAS Token Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the ****Credentials**** and ****Details**** panes in the Azure Blob Storage SAS Token Writeback Connector page. The components of the other panes in this page, ****Scheduling**** and ****Name & Describe Your DataSet****, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 30–48 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the storage account name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Shared Access Signature (SAS) Token</p></td>
          <td colspan="1" rowspan="1"><p>Enter your account access level SAS token.</p></td>
        </tr>
      </tbody>
    </table>
    Once you have entered valid credentials, you can use the same account any time you go to set up a new Domo-Azure Blob Storage connection. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the storage account name. |
    | Shared Access Signature (SAS) Token | Enter your account access level SAS token. |
    
    Once you have entered valid credentials, you can use the same account any time you go to set up a new Domo-Azure Blob Storage connection. You can manage connector accounts in the ****Accounts****tab in the Data Center. For more information about this tab, see [Manage Connector Accounts](/s/article/360042926054).

CHANGE 4  [### Details Pane]  (lines 54–80 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Blob Containers</p></td>
          <td colspan="1" rowspan="1"><p>Select the Azure Blob container you want to send your Domo data to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Input DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the DataSet ID (GUID) for the DataSet you want to copy to the selected Azure Blob Storage container. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Filename</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to reference your DataSet using the <b>Input DataSet ID</b> or an output file name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Filename</p></td>
          <td colspan="1" rowspan="1"><p>Enter the output file name for your data.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Blob Containers | Select the Azure Blob container you want to send your Domo data to. |
    | Input DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to the selected Azure Blob Storage container. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4. |
    | Select Filename | Select whether you want to reference your DataSet using the ********Input DataSet ID******** or an output file name. |
    | Filename | Enter the output file name for your data. |

==============================================================================
FILE:   s/article/360058713713.mdx
TITLE:  CSV SFTP Advanced Security Connector
LANG:   English (en_US)
ID:     360058713713
==============================================================================

CHANGE 1  [## Intro]  (line 15 in repo file)

  OLD (remove from repo):
    The CSV SFTP Advanced Security connector is a "File" connector, meaning it retrieves files and output them to Domo. In the Data Center, you can access the connector page for this and other File connectors by clicking **File** in the toolbar at the top of the window.

  NEW (replace with / insert from CSV):
    The CSV SFTP Advanced Security connector is a "File" connector, meaning it retrieves files and output them to Domo. In the Data Center, you can access the connector page for this and other File connectors by clicking ****File****in the toolbar at the top of the window.

CHANGE 2  [## Intro]  (lines 18–19 in repo file)

  OLD (remove from repo):
    
    ---

CHANGE 3  [## Connecting to CSV Files]  (line 39 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes for the CSV SFTP Advanced Pull connector. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the ****Credentials**** and ****Details**** panes for the CSV SFTP Advanced Pull connector. The components of the other panes in this page, ****Scheduling**** and ****Name & Describe Your DataSet****, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 45–67 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Host</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the SFTP host you want to connect to. This <i>must</i> begin with sftp://. For example: sftp://hostname.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Port</p></td>
          <td colspan="1" rowspan="1"><p>Enter the port number for your SFTP server.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to authenticate to the SFTP server.</p></td>
        </tr>
      </tbody>
    </table>
    After entering the host, port, and username, click the **Generate Key** button. Copy your ** Public Key** to the SFTP server.

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Host | Enter the name of the SFTP host you want to connect to. This *must* begin with sftp://. For example: sftp://hostname. |
    | Port | Enter the port number for your SFTP server. |
    | Username | Enter the username you use to authenticate to the SFTP server. |
    
    After entering the host, port, and username, click the **Generate Key** button. Copy your **Public Key** to the SFTP server.

CHANGE 5  [### Details Pane]  (lines 75–213 in repo file)

  ⚠ LARGE CHANGE (173 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>How Would You Like To Choose Your Directory?</p></td>
          <td colspan="1" rowspan="1"><p>Select how would you like to choose the directory.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Directory</p></td>
          <td colspan="1" rowspan="1"><p>Select the directory that contains the file you want to access.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Directory</p></td>
          <td colspan="1" rowspan="1"><p>Enter the directory path.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the file type of the file selected from the directory.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Filter Your CSV File</p></td>
          <td colspan="1" rowspan="1"><p>You can select the below options to filter your CSV file.   - <b>Select file -</b> You can filter from the available CSV files using the <b> Select CSV File Name</b> field. - <b> Enter file name –</b> You can enter the name of the specific CSV file you want to filter using the <b> Enter</b> <b> CSV File Name</b> field. - <b> All matching files -</b> Filters out all the matching CSV files.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select CSV File Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the CSV file name from the drop-down list.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter CSV File Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the file name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Extension</p></td>
          <td colspan="1" rowspan="1"><p>You can either enter csv or txt and search for files with the .csv or .txt extension. This is an optional field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partial/ Complete CSV File Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the CSV file name you want to match and filter using the <b>File Name Match Criteria</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Name Match Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select the match criteria to filter out the CSV files.   - <b>Exact Match -</b> Filters for the exact CSV file(s) that match the same file name entered in the <b> Partial/ Complete CSV File Name</b> field. - <b> Contains -</b> Filters the CSV file(s) which contains the file name entered in the <b> Partial/ Complete CSV File Name</b> field. - <b> Starts With -</b> Filters the CSV file(s) that starts with the file name entered in the <b> Partial/ Complete CSV File Name</b> field. - <b> Ends With -</b> Filters the CSV file(s) that ends with the file name entered in the <b> Partial/ Complete CSV File Name</b> field. - <b> Regular Expression -</b> Filters for a regular expression. For more information refer <a href="https://en.wikipedia.org/wiki/Regular_expression">Regular expression</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Match Case Sensitive</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox if you want to filter the CSV file(s) that matches the case sensitive of the entered file name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Filter Your ZIP File</p></td>
          <td colspan="1" rowspan="1"><p>You can select the below options to filter your Zip file.   - <b>Select file -</b> You can filter from the available ZIP file(s) using the <b> Select ZIP File</b> field. - <b> Enter file name -</b> You can enter the name of the specific ZIP file you want to filter using the <b> Enter ZIP File Name</b> field. - <b> All matching files -</b> Filters out all the matching ZIP file(s).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select ZIP File</p></td>
          <td colspan="1" rowspan="1"><p>Select a CSV file to parse from the unzip folder.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter ZIP File Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter a file name to parse from the zip archive.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partial/ Complete ZIP File Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ZIP file name that you want to match and filter using the <b>ZIP File Name Match Criteria</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Zip File Name Match Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select the match criteria to filter for the ZIP files.   - <b>Exact Match -</b> Filters for the exact ZIP file(s) that match the same file name entered in the <b> Partial/ Complete ZIP File Name</b> field. - <b> Contains -</b> Filters the ZIP file(s) which contains the file name entered in the <b> Partial/ Complete ZIP File Name</b> field. - <b> Starts With -</b> Filters the ZIP file(s) that starts with the file name entered in the <b> Partial/ Complete ZIP File Name</b> field. - <b> Ends With -</b> Filters the ZIP file(s) that ends with the file name entered in the <b> Partial/ Complete ZIP File Name</b> field. - <b> Regular Expression -</b> Filters for a regular expression. For information refer to <a href="https://en.wikipedia.org/wiki/Regular_expression">Regular Expression</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Match Case Sensitive</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox if you want to filter the ZIP file that match the case sensitive of the entered file name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Filter Your ZIP Sub File</p></td>
          <td colspan="1" rowspan="1"><p>You can select the below options to filter your file(s) from the selected Zip file.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Sub File From ZIP File</p></td>
          <td colspan="1" rowspan="1"><p>Select a CSV file to parse from the unzipped folder.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Sub File Name From ZIP File</p></td>
          <td colspan="1" rowspan="1"><p>Enter a file name to parse from the zip archive.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter ZIP Sub File Extension</p></td>
          <td colspan="1" rowspan="1"><p>Limits the search only to files with the specified extension, for example, csv, txt, and so on.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partial/ Complete ZIP Sub-File Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the ZIP sub-file name you want to match and filter using the <b>Zip Sub-File Name Match Criteria</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Zip Sub-File Name Match Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select the match criteria to filter for the ZIP sub-files.   - <b>Exact Match -</b> Filters for the exact ZIP sub-file(s) that match the same file name entered in the<b> Partial/ Complete ZIP Sub-File Name</b> field. - <b> Contains -</b> Filters the ZIP file(s) which contains the file name entered in the <b> Partial/ Complete ZIP Sub-File Name</b> field. - <b> Starts With -</b> Filters the ZIP file(s) that starts with the file name entered in the <b> Partial/ Complete ZIP Sub-File Name</b> field. - <b> Ends With -</b> Filters the ZIP file(s) that ends with the file name entered in the <b> Partial/ Complete ZIP Sub-File Name</b> field. - <b> Regular Expression -</b> Filters with a regular expression. For information refer to <a href="https://en.wikipedia.org/wiki/Regular_expression">Regular Expression</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Match Case Sensitive</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox if you want to filter the ZIP sub-files that match the case sensitive of the entered file name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Delimiting Character</p></td>
          <td colspan="1" rowspan="1"><p>Select the delimiting character used in your file. If your delimiter is not listed, then select 'Other.'</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specify your delimiter</p></td>
          <td colspan="1" rowspan="1"><p>Enter the character used to delimit your character separated values (CSV) text.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Quote Character</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired quote character for parsing CSV files. Double quote is the CSV standard default quote character.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Quote Character</p></td>
          <td colspan="1" rowspan="1"><p>Enter the desired CSV Quote character.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Escape Character</p></td>
          <td colspan="1" rowspan="1"><p>Select the desired escape character for parsing CSV files.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Escape Character</p></td>
          <td colspan="1" rowspan="1"><p>Enter the desired CSV escape character.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>File Encoding</p></td>
          <td colspan="1" rowspan="1"><p>Select the file encoding.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the required date format. This feature helps to correct the Date if formatted incorrectly.  You can select any one of the following date formats:   - DD/MM/YYYY - MM/DD/YYYY - YYYY/MM/DD - YYYY/DD/MM</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | How Would You Like To Choose Your Directory? | Select how would you like to choose the directory. |
    | Directory | Select the directory that contains the file you want to access. |
    | Enter Directory | Enter the directory path. |
    | File Type | Select the file type of the file selected from the directory. |
    | Filter Your CSV File | You can select the below options to filter your CSV file.   - **Select file -**You can filter from the available CSV files using the **Select CSV File Name** field. - **Enter file name –** You can enter the name of the specific CSV file you want to filter using the **Enter** **CSV File Name** field. - **All matching files -**Filters out all the matching CSV files. |
    | Select CSV File Name | Select the CSV file name from the drop-down list. |
    | Enter CSV File Name | Enter the file name. |
    | File Extension | You can either enter csv or txt and search for files with the .csv or .txt extension. This is an optional field. |
    | Partial/ Complete CSV File Name | Enter the CSV file name you want to match and filter using the **File Name Match Criteria**field. |
    | File Name Match Criteria | Select the match criteria to filter out the CSV files.   - **Exact Match -**Filters for the exact CSV file(s) that match the same file name entered in the **Partial/ Complete CSV File Name** field. - **Contains -**Filters the CSV file(s) which contains the file name entered in the **Partial/ Complete CSV File Name** field. - **Starts With -**Filters the CSV file(s) that starts with the file name entered in the **Partial/ Complete CSV File Name** field. - **Ends With -**Filters the CSV file(s) that ends with the file name entered in the **Partial/ Complete CSV File Name** field. - **Regular Expression -**Filters for a regular expression. For more information refer [Regular expression](https://en.wikipedia.org/wiki/Regular_expression). |
    | Match Case Sensitive | Select this checkbox if you want to filter the CSV file(s) that matches the case sensitive of the entered file name. |
    | Filter Your ZIP File | You can select the below options to filter your Zip file.   - **Select file -**You can filter from the available ZIP file(s) using the **Select ZIP File** field. - **Enter file name -** You can enter the name of the specific ZIP file you want to filter using the **Enter ZIP File Name** field. - **All matching files -**Filters out all the matching ZIP file(s). |
    | Select ZIP File | Select a CSV file to parse from the unzip folder. |
    | Enter ZIP File Name | Enter a file name to parse from the zip archive. |
    | Partial/ Complete ZIP File Name | Enter the ZIP file name that you want to match and filter using the **ZIP File Name Match Criteria** field. |
    | Zip File Name Match Criteria | Select the match criteria to filter for the ZIP files.   - **Exact Match -**Filters for the exact ZIP file(s) that match the same file name entered in the **Partial/ Complete ZIP File Name** field. - **Contains -**Filters the ZIP file(s) which contains the file name entered in the **Partial/ Complete ZIP File Name** field. - **Starts With -**Filters the ZIP file(s) that starts with the file name entered in the **Partial/ Complete ZIP File Name** field. - **Ends With -**Filters the ZIP file(s) that ends with the file name entered in the **Partial/ Complete ZIP File Name** field. - **Regular Expression -**Filters for a regular expression. For information refer to [Regular Expression](https://en.wikipedia.org/wiki/Regular_expression). |
    | Match Case Sensitive | Select this checkbox if you want to filter the ZIP file that match the case sensitive of the entered file name. |
    | Filter Your ZIP Sub File | You can select the below options to filter your file(s) from the selected Zip file. |
    | Select Sub File From ZIP File | Select a CSV file to parse from the unzipped folder. |
    | Enter Sub File Name From ZIP File | Enter a file name to parse from the zip archive. |
    | Enter ZIP Sub File Extension | Limits the search only to files with the specified extension, for example, csv, txt, and so on. |
    | Partial/ Complete ZIP Sub-File Name | Enter the ZIP sub-file name you want to match and filter using the **Zip Sub-File Name Match Criteria**. |
    | Zip Sub-File Name Match Criteria | Select the match criteria to filter for the ZIP sub-files.   - **Exact Match -**Filters for the exact ZIP sub-file(s) that match the same file name entered in the**Partial/ Complete ZIP Sub-File Name** field. - **Contains -**Filters the ZIP file(s) which contains the file name entered in the **Partial/ Complete ZIP Sub-File Name** field. - **Starts With -**Filters the ZIP file(s) that starts with the file name entered in the **Partial/ Complete ZIP Sub-File Name** field. - **Ends With -**Filters the ZIP file(s) that ends with the file name entered in the **Partial/ Complete ZIP Sub-File Name** field. - **Regular Expression -**Filters with a regular expression. For information refer to [Regular Expression](https://en.wikipedia.org/wiki/Regular_expression). |
    | Match Case Sensitive | Select this checkbox if you want to filter the ZIP sub-files that match the case sensitive of the entered file name. |
    | Delimiting Character | Select the delimiting character used in your file. If your delimiter is not listed, then select 'Other.' |
    | Specify your delimiter | Enter the character used to delimit your character separated values (CSV) text. |
    | Quote Character | Select the desired quote character for parsing CSV files. Double quote is the CSV standard default quote character. |
    | Custom Quote Character | Enter the desired CSV Quote character. |
    | Escape Character | Select the desired escape character for parsing CSV files. |
    | Custom Escape Character | Enter the desired CSV escape character. |
    | File Encoding | Select the file encoding. |
    | Date Format | Select the required date format. This feature helps to correct the Date if formatted incorrectly.  You can select any one of the following date formats:   - DD/MM/YYYY - MM/DD/YYYY - YYYY/MM/DD - YYYY/DD/MM |

==============================================================================
FILE:   s/article/360058716313.mdx
TITLE:  HubSpot Upsert Connector
LANG:   English (en_US)
ID:     360058716313
==============================================================================

CHANGE 1  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    The HubSpot Upsert connector is a "Cloud App" connector, meaning it retrieves data stored in the cloud. In the Data Center, you can access the connector page for this and other Cloud App connectors by clicking **Cloud App** in the toolbar at the top of the window.

  NEW (replace with / insert from CSV):
    The HubSpot Upsert connector is a "Cloud App" connector, meaning it retrieves data stored in the cloud. In the Data Center, you can access the connector page for this and other Cloud App connectors by clicking ****Cloud App**** in the toolbar at the top of the window.

CHANGE 2  [## Intro]  (lines 12–13 in repo file)

  OLD (remove from repo):
    
    ---

CHANGE 3  [## Connecting to Your HubSpot Account]  (line 23 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes on the HubSpot Upsert Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes on the HubSpot Upsert Connector page. The components of the other panes in this page, **Scheduling**and **Name & Describe Your DataSet,** are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 4  [## Credentials Pane]  (line 27 in repo file)

  OLD (remove from repo):
    The Domo HubSpot Upsert connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select ** Add Account** if you have existing HubSpot accounts in Domo) to open the HubSpot OAuth screen where you can enter your HubSpot credentials. Once you have entered valid HubSpot credentials, you can use the same account any time you go to create a new HubSpot DataSet. You can manage connector accounts in the ** Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    The Domo HubSpot Upsert connector uses OAuth to connect, so there is no need to enter credentials within Domo. Click **Connect** (or select **Add Account** if you have existing HubSpot accounts in Domo) to open the HubSpot OAuth screen where you can enter your HubSpot credentials. Once you have entered valid HubSpot credentials, you can use the same account any time you go to create a new HubSpot DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 5  [## Credentials Pane]  (line 29 in repo file)

  OLD (remove from repo):
    <Note>**Note:** If you are already logged into HubSpot when you connect in Domo, you are authenticated automatically when you click ** Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of HubSpot.</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** If you are already logged into HubSpot when you connect in Domo, you are authenticated automatically when you click **Add account**. If you want to connect to an account that is different from the one you are logged into, you must first log out of HubSpot.</Note>

CHANGE 6  [## Details Pane]  (lines 35–124 in repo file)

  ⚠ LARGE CHANGE (112 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the HubSpot report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Object Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the Custom Object.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Properties To Retrieve</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to retrieve tickets for all or only specific properties.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Properties</p></td>
          <td colspan="1" rowspan="1"><p>Select the properties for the tickets to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Company Properties to Retrieve</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to retrieve all or only specific company properties.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Company Properties</p></td>
          <td colspan="1" rowspan="1"><p>Select company properties.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select custom object properties to retrieve</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to retrieve all or only specific company properties.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Object Properties</p></td>
          <td colspan="1" rowspan="1"><p>Select the custom object properties to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Type of Owners</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to retrieve all owners or only archived owners.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Contact Properties to Retrieve</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to retrieve all or only specific contact properties.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contact Properties</p></td>
          <td colspan="1" rowspan="1"><p>Select contact properties.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select line items properties to retrieve</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to retrieve all or only specific line items properties.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Line Items Properties</p></td>
          <td colspan="1" rowspan="1"><p>Select the line items properties to retrieve.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select archived (checkbox)</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to return the results that only have been archived.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select association type</p></td>
          <td colspan="1" rowspan="1"><p>Select whether to retrieve all or only specific associations.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select from object</p></td>
          <td colspan="1" rowspan="1"><p>Select objects from which you want to retrieve associations.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select to object</p></td>
          <td colspan="1" rowspan="1"><p>Select objects for which you want to retrieve associations.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select analytics object type</p></td>
          <td colspan="1" rowspan="1"><p>Select the type of object for which you want the analytics data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select analytics time period</p></td>
          <td colspan="1" rowspan="1"><p>Select the time period used to group the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Range</p></td>
          <td colspan="1" rowspan="1"><p>Enter the date range for which you would like to fetch the data.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the HubSpot report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Appointments | Fetches all the appointments available for your account. | | Associations | Select whether to retrieve all or only specific associations. | | Calls | Fetches all the calls available for your account. | | Companies | Fetches all the companies available for your account. | | Contacts | Fetches all the contacts available for your account. | | Custom Objects | Fetches all the records for a specific custom object available for your account. | | Emails Report | Fetches all the emails report available for your account. | | Line Items | Select whether to retrieve all or only specific line items properties. | | Listings | Fetches all the listings available for your account. | | Marketing Emails Report | Fetches all the marketing email details for your account. | | Meetings Report | Fetches all the meetings report available for your account. | | Notes Report | Fetches all the notes report available for your account. | | Object Analytics Report | Fetches the analytics data for specific objects, based on the type of object. | | Owners | Fetches all the owners available for your account. | | Postal Mail Report | Fetches all the postal mail report available for your account. | | Tasks Report | Fetches all the tasks report available for your account. | | Ticket Report | Fetches all tickets from a portal. | |
    | Custom Object Type | Select the Custom Object. |
    | Select Properties To Retrieve | Select whether to retrieve tickets for all or only specific properties. |
    | Properties | Select the properties for the tickets to retrieve. |
    | Select Company Properties to Retrieve | Select whether to retrieve all or only specific company properties. |
    | Company Properties | Select company properties. |
    | Select custom object properties to retrieve | Select whether to retrieve all or only specific company properties. |
    | Custom Object Properties | Select the custom object properties to retrieve. |
    | Select Type of Owners | Select whether you want to retrieve all owners or only archived owners. |
    | Select Contact Properties to Retrieve | Select whether to retrieve all or only specific contact properties. |
    | Contact Properties | Select contact properties. |
    | Select line items properties to retrieve | Select whether to retrieve all or only specific line items properties. |
    | Line Items Properties | Select the line items properties to retrieve. |
    | Select archived (checkbox) | Select whether to return the results that only have been archived. |
    | Select association type | Select whether to retrieve all or only specific associations. |
    | Select from object | Select objects from which you want to retrieve associations. |
    | Select to object | Select objects for which you want to retrieve associations. |
    | Select analytics object type | Select the type of object for which you want the analytics data. |
    | Select analytics time period | Select the time period used to group the data. |
    | Date Range | Enter the date range for which you would like to fetch the data. |

CHANGE 7  [## FAQs]  (line 132 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### What endpoint is the base URL for this connector?

CHANGE 8  [## FAQs]  (line 134 in repo file)

  OLD (remove from repo):
    <Accordion title="What endpoint is the base URL for this connector?">

CHANGE 9  [## FAQs]  (line 136 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 10  [## FAQs]  (lines 138–217 in repo file)

  ⚠ LARGE CHANGE (81 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <Accordion title="Which endpoint(s) does each report call in this connector?">
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Report Name</p></th>
          <th colspan="1" rowspan="1"><p>Endpoint URL(s)</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Appointments</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/APPOINTMENT</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Associations</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v4/associations/{FromObjectTypes}/{ToObjectTypes}/batch/read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Calls</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/calls</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Companies</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/properties/company</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contacts</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/contacts</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Objects</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/schemas</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Emails Report</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/emails</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Line Items</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Listings</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/LISTING</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Marketing Emails Report</p></td>
          <td colspan="1" rowspan="1"><p>/marketing/v3/emails</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Meetings Report</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/meetings</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Notes Report</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/notes</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Object Analytics Report</p></td>
          <td colspan="1" rowspan="1"><p>/analytics/v2/reports/{AnalyticsObjectType}/{AnalyticsTimePeriod}</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Owners</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/owners/</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Postal Mail Report</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/postal\_mail</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Tasks Report</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/tasks</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Report</p></td>
          <td colspan="1" rowspan="1"><p>/crm/v3/objects/tickets</p></td>
        </tr>
      </tbody>
    </table>
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Which endpoint(s) does each report call in this connector?

CHANGE 11  [## FAQs]  (line 219 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up the HubSpot Upsert connector?">

  NEW (replace with / insert from CSV):
    | Report Name | Endpoint URL(s) |
    | --- | --- |
    | Appointments | /crm/v3/objects/APPOINTMENT |
    | Associations | /crm/v4/associations/{FromObjectTypes}/{ToObjectTypes}/batch/read |
    | Calls | /crm/v3/objects/calls |
    | Companies | /crm/v3/properties/company |
    | Contacts | /crm/v3/contacts |
    | Custom Objects | /crm/v3/schemas |
    | Emails Report | /crm/v3/objects/emails |
    | Line Items | null |
    | Listings | /crm/v3/objects/LISTING |
    | Marketing Emails Report | /marketing/v3/emails |
    | Meetings Report | /crm/v3/objects/meetings |
    | Notes Report | /crm/v3/objects/notes |
    | Object Analytics Report | /analytics/v2/reports/{AnalyticsObjectType}/{AnalyticsTimePeriod} |
    | Owners | /crm/v3/owners/ |
    | Postal Mail Report | /crm/v3/objects/postal\_mail |
    | Tasks Report | /crm/v3/objects/tasks |
    | Ticket Report | /crm/v3/objects/tickets |
    
    #### What kind of credentials do I need to power up the HubSpot Upsert connector?
    

CHANGE 12  [## FAQs]  (line 221 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 13  [## FAQs]  (line 223 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 14  [## FAQs]  (line 225 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 15  [## FAQs]  (line 227 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 16  [## FAQs]  (line 229 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 17  [## FAQs]  (line 231 in repo file)

  OLD (remove from repo):
    <Accordion title="Can I use the same HubSpot account to create multiple datasets?">

  NEW (replace with / insert from CSV):
    #### Can I use the same HubSpot account to create multiple datasets?
    

CHANGE 18  [## FAQs]  (line 233 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 19  [## FAQs]  (line 235 in repo file)

  OLD (remove from repo):
    <Accordion title="What's the difference between the Hubspot and Hubspot Upsert connectors?">

  NEW (replace with / insert from CSV):
    #### What's the difference between the Hubspot and Hubspot Upsert connectors?
    

CHANGE 20  [## FAQs]  (lines 238–240 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360058716453.mdx
TITLE:  Gong Connector
LANG:   English (en_US)
ID:     360058716453
==============================================================================

CHANGE 1  [## Prerequisites]  (line 18 in repo file)

  OLD (remove from repo):
    To obtain your credentials, go to the [Gong API Page](https://app.gong.io/welcome/login?targeturl=/company/api) and click **Create** to receive the ** Access Key** and ** Access Key Secret**.

  NEW (replace with / insert from CSV):
    To obtain your credentials, go to the [Gong API Page](https://app.gong.io/welcome/login?targeturl=/company/api) and click **Create** to receive the **Access Key** and **Access Key Secret**.

CHANGE 2  [## Prerequisites]  (line 20 in repo file)

  OLD (remove from repo):
    <Note>**NOTE**: To receive the Access Key and Access Key Secret, you must be a technical administrator in Gong.</Note>

  NEW (replace with / insert from CSV):
    **NOTE**: To receive the Access Key and Access Key Secret, you must be a technical administrator in Gong.

CHANGE 3  [## Connecting to Your Gong Account]  (line 24 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Gong Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Gong Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 4  [### Credentials Pane]  (lines 30–51 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the access key for your Gong API user. For more information, see <a href="https://domohelp.domo.com/Connect/Connecting_to_Data_with_Connectors/Configuring_Each_Connector/Connectors_for_Sales/Gong_Connector#Prerequisites">Prerequisites</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter the access key secret for your Gong API user. For more information, see <a href="https://domohelp.domo.com/Connect/Connecting_to_Data_with_Connectors/Configuring_Each_Connector/Connectors_for_Sales/Gong_Connector#Prerequisites">Prerequisites</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domain</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Domain. For example, api.gong.io.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Access Key | Enter the access key for your Gong API user. For more information, see [Prerequisites](https://domohelp.domo.com/Connect/Connecting_to_Data_with_Connectors/Configuring_Each_Connector/Connectors_for_Sales/Gong_Connector#Prerequisites). |
    | Access Key Secret | Enter the access key secret for your Gong API user. For more information, see [Prerequisites](https://domohelp.domo.com/Connect/Connecting_to_Data_with_Connectors/Configuring_Each_Connector/Connectors_for_Sales/Gong_Connector#Prerequisites). |
    | Domain | Enter the Domain. For example, api.gong.io. |
    

CHANGE 5  [### Details Pane]  (lines 58–139 in repo file)

  ⚠ LARGE CHANGE (102 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Gong report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select the date format for your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Single Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Relative Date</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data for in the <b>Days Back</b> field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Range</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific or relative date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the last date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data from (start day). Combine with <b>End Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data to (end day). Combine with <b>Start Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Time Period</p></td>
          <td colspan="1" rowspan="1"><p>Specify the time period that you would like to receive data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Starting Day of the Week</p></td>
          <td colspan="1" rowspan="1"><p>Select the day you would like your week to start with.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Aggregation Period</p></td>
          <td colspan="1" rowspan="1"><p>Select the period you would like the data to be aggregated for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Subreport</p></td>
          <td colspan="1" rowspan="1"><p>Select the sub report data you are interested in.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Context Links</p></td>
          <td colspan="1" rowspan="1"><p>Returns the user activity for selected aggregation and date range with respect to the context links.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Points of Interest</p></td>
          <td colspan="1" rowspan="1"><p>Returns the user activity for selected aggregation and date range with respect to the user's points of interests.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Topics</p></td>
          <td colspan="1" rowspan="1"><p>Returns the user activity for selected aggregation and date range with respect to the user's topics.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Trackers</p></td>
          <td colspan="1" rowspan="1"><p>Returns the user activity for selected aggregation and date range with respect to the user's trackers.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Gong report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Calls | This report returns the call data for calls that occurred in the selected date range. | | Call Details | This report returns the call detail information broken down by the selected sub-report for the selected date range. | | Calls Details Extensive | Lists the detailed call data for calls that took place during a specified date range and have specified call IDs. | | Call Transcripts | This report returns the call transcripts broken down by sentence for the selected date range. | | Company Productivity (Calls & Interaction Stats) | Returns the metrics on the basis of calls & interaction stats data for the selected date range. | | Daily Activity for applicable users | Retrieves the daily activity of multiple users within the Gong system for a range of dates. | | Flows | This report returns all company flows, together with personal flows and flows shared with any user. | | Interaction Stats | This report returns the interaction statistics for calls that occurred in the selected date range. | | Scorecards | This report returns the scorecard data for scorecards with a review date in the selected range. | | User Activity | This report returns the user activity by selected aggregation and date range. | | Users | This report returns a list of users. | | Workspaces | This report returns a list of workspaces. | |
    | Date Selection | Select the date format for your data. |
    | Single Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Specific Date | Select the specific date using the date selector. |
    | Relative Date | Enter the number of days back that you would like to get data for in the **Days Back** field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Date Range | Select the specific or relative date range. |
    | Start Date - Specific | Select the first date in your date range using the date selector. |
    | End Date - Specific | Select the last date in your date range using the date selector. |
    | Start Date - Relative | Enter the number of days back that you would like to get data from (start day). Combine with ********End Date******** to create a range of represented days.  For example, if you entered 10 for ********Start Date******** and 5 for ********End Date********, the report would contain data for 10 days ago up until 5 days ago. |
    | End Date - Relative | Enter the number of days back that you would like to get data to (end day). Combine with ********Start Date********to create a range of represented days.  For example, if you entered 10 for ********Start Date******** and 5 for ********End Date********, the report would contain data for 10 days ago up until 5 days ago. |
    | Time Period | Specify the time period that you would like to receive data for. |
    | Starting Day of the Week | Select the day you would like your week to start with. |
    | Aggregation Period | Select the period you would like the data to be aggregated for. |
    | Subreport | Select the sub report data you are interested in. |
    | Context Links | Returns the user activity for selected aggregation and date range with respect to the context links. |
    | Points of Interest | Returns the user activity for selected aggregation and date range with respect to the user's points of interests. |
    | Topics | Returns the user activity for selected aggregation and date range with respect to the user's topics. |
    | Trackers | Returns the user activity for selected aggregation and date range with respect to the user's trackers. |

==============================================================================
FILE:   s/article/360058757134.mdx
TITLE:  Snowflake Managed Unload Connector
LANG:   English (en_US)
ID:     360058757134
==============================================================================

CHANGE 1  [## Prerequisites]  (line 25 in repo file)

  OLD (remove from repo):
    - The full name of your account (provided by Snowflake). For US West Region **account\_name**, and All other regions ** account\_name.region\_id**. This is the portion of your Snowflake URL immediately following https://.

  NEW (replace with / insert from CSV):
    - The full name of your account (provided by Snowflake). For US West Region ****account\_name****, and All other regions ****account\_name.region\_id****. This is the portion of your Snowflake URL immediately following https://.

CHANGE 2  [## Prerequisites]  (line 27 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345." <img alt="snowflake_account_names.png" src="/images/kb/0EM5w000005vO5c.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/> - The username and password used to connect to your Snowflake host.

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."
    
    
      <img alt="snowflake_account_names.png" src="/images/kb/0EM5w000005vO5c.png" style={{width: 20, height: 20, display: 'inline', verticalAlign: 'start', margin: '0'}}/>
    - The username and password used to connect to your Snowflake host.

CHANGE 3  [## Prerequisites]  (line 34 in repo file)

  OLD (remove from repo):
    | COPY INTO `<location>` | command to unload all the rows from a table into one or more files into the stage |

  NEW (replace with / insert from CSV):
    | COPY INTO <location> | command to unload all the rows from a table into one or more files into the stage |

CHANGE 4  [## Connecting to Your Snowflake Managed Unload Account]  (line 42 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake Managed Unload Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Managed Unload Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 5  [## Credentials Pane]  (lines 48–73 in repo file)

  ⚠ LARGE CHANGE (32 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Snowflake account name. For more information, see <a href="#h_01ES6VKF5A1N9WPTTBV00ZQDBF">Prerequisites</a>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter your Snowflake account name. For more information, see [Prerequisites](#h_01ES6VKF5A1N9WPTTBV00ZQDBF). |
    | Username | Enter the username you use to connect to your Snowflake host. |
    | Password | Enter the password you use to connect to your Snowflake host. |
    | Role | Enter your role. |

CHANGE 6  [## Details Pane]  (lines 81–151 in repo file)

  ⚠ LARGE CHANGE (88 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Query Type. The following query types are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query to retrieve your data. You can use the <b>Query Helper</b> parameter to help you write a usable SQL query. To use the <b> Query Helper</b>, do the following:   1. Select your desired warehouse, database, database schema, database object, and table columns or views in each menu. 2. Copy the SQL statement that appears in the <b> Query Helper</b> field. 3. Paste the copied SQL statement into the <b> Query</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates with all of the warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve. The list populates with all of the databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Schemas</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates with all of the schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Objects</p></td>
          <td colspan="1" rowspan="1"><p>Select the database object containing the data you want to retrieve. The list populates with all of the objects you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Views</p></td>
          <td colspan="1" rowspan="1"><p>Select the database view you want to retrieve. The list populates with all of the views you have access to based on your credentials. If you do not select a database view, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns</p></td>
          <td colspan="1" rowspan="1"><p>Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste the SQL statement in this field into the <b>Query</b> field. For more information, see <b> Query</b>, above.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Support</p></td>
          <td colspan="1" rowspan="1"><p>Choose whether or not to partition your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the required date format. By default, <b>yyyy-MM-dd</b> will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom date format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Tag</p></td>
          <td colspan="1" rowspan="1"><p>Optional parameter that can be used to tag connector SQL executions in Snowflake as a <b>Query Tag</b>.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Query Type. The following query types are available:   |  |  | | --- | --- | | Custom Query | Enter the SQL query to execute. | | Query Builder | Select a table and fields to auto-generate your query. | |
    | Query | Enter the SQL query to retrieve your data. You can use the ****Query Helper****parameter to help you write a usable SQL query. To use the ****Query Helper****, do the following:   1. Select your desired warehouse, database, database schema, database object, and table columns or views in each menu. 2. Copy the SQL statement that appears in the ****Query Helper****field. 3. Paste the copied SQL statement into the ****Query**** field. |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates with all of the warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve. The list populates with all of the databases you have access to based on your credentials. |
    | Database Schemas | Select the database schema containing the data you want to retrieve. The list populates with all of the schemas you have access to based on your credentials. |
    | Database Objects | Select the database object containing the data you want to retrieve. The list populates with all of the objects you have access to based on your credentials. |
    | Database Views | Select the database view you want to retrieve. The list populates with all of the views you have access to based on your credentials. If you do not select a database view, the connector retrieves your entire database schema. |
    | Database Tables | Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Table Columns | Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table. |
    | Query Helper | When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste the SQL statement in this field into the ****Query****field. For more information, see ****Query****, above. |
    | Partition Support | Choose whether or not to partition your data. |
    | Past Days | Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. For example: 30. |
    | Date Format | Select the required date format. By default, **yyyy-MM-dd** will be used. |
    | Custom Date Format | Enter the custom date format. |
    | Query Tag | Optional parameter that can be used to tag connector SQL executions in Snowflake as a **Query Tag**. |

CHANGE 7  [## FAQs]  (lines 159–160 in repo file)

  OLD (remove from repo):
    <AccordionGroup>
    <Accordion title="When should I use this Connector?">

  NEW (replace with / insert from CSV):
    ##### When should I use this Connector?

CHANGE 8  [## FAQs]  (lines 164–166 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="Are there any API limits I should be aware of?">

  NEW (replace with / insert from CSV):
    ##### Are there any API limits I should be aware of?

CHANGE 9  [## FAQs]  (lines 170–172 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    ##### How frequently will my data update?

CHANGE 10  [## FAQs]  (lines 176–178 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    <Accordion title="What should I be aware of while writing a query?">

  NEW (replace with / insert from CSV):
    ##### What should I be aware of while writing a query?

CHANGE 11  [## FAQs]  (line 182 in repo file)

  OLD (remove from repo):
    </Accordion>

  NEW (replace with / insert from CSV):
    ##### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?

CHANGE 12  [## FAQs]  (lines 184–189 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">
    
    Before you can connect to a Snowflake database, you must also whitelist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses in Domo](https://domo-support.domo.com/s/article/360043439173?language=en_US "Whitelisting IP Addresses in Domo").
    
    </Accordion>
    </AccordionGroup>

  NEW (replace with / insert from CSV):
    Before you can connect to a Snowflake database, you must also allowlist a number of IP addresses on your database server on the port you want to connect to. For the full list of IP addresses, see [Whitelisting IP Addresses in Domo](/s/article/360043439173).

==============================================================================
FILE:   s/article/360058759794.mdx
TITLE:  Power BI AAD Writeback Connector
LANG:   English (en_US)
ID:     360058759794
==============================================================================

CHANGE 1  [## Intro]  (lines 12–13 in repo file)

  OLD (remove from repo):
    
    ---

CHANGE 2  [### Creating an Azure App for Enterprise OAuth]  (line 85 in repo file)

  OLD (remove from repo):
    10. Click on **Grant admin consent**, then click ** Yes** to allow the admin consent for your permissions.The added permissions will appear in the ** Configured permissions** section with their details.

  NEW (replace with / insert from CSV):
    10. Click on **Grant admin consent**, then click **Yes**to allow the admin consent for your permissions.The added permissions will appear in the **Configured permissions** section with their details.

CHANGE 3  [### Creating an Azure App for Enterprise OAuth]  (line 93 in repo file)

  OLD (remove from repo):
    14. Copy the **value** and paste it into the ** Client Secret** field in the Connector credentials section in Domo.

  NEW (replace with / insert from CSV):
    14. Copy the **value** and paste it into the **Client Secret** field in the Connector credentials section in Domo.

CHANGE 4  [## Configuring the Connection]  (line 129 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Power BI AAD Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most Connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Power BI AAD Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most Connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 5  [### Credentials Pane]  (lines 135–157 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Azure App Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the client ID of the client web app obtained from Azure Active Directory configuration. For more information, see "Obtaining the Azure App Client ID".</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Azure App Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter the secret key of the client web app. For more information, see "Obtaining the Azure App Client Secret".</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Azure Tenant ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Tenant ID. For more information, see "Obtaining the Tenant ID".</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Azure App Client ID | Enter the client ID of the client web app obtained from Azure Active Directory configuration. For more information, see "Obtaining the Azure App Client ID". |
    | Azure App Client Secret | Enter the secret key of the client web app. For more information, see "Obtaining the Azure App Client Secret". |
    | Azure Tenant ID | Enter your Tenant ID. For more information, see "Obtaining the Tenant ID". |

CHANGE 6  [### Details Pane]  (lines 173–174 in repo file)

  OLD (remove from repo):
    | Cast Decimal to Double (checkbox) | Select this checkbox to convert the **Decimal** column from the input dataset to ** Double** in Power BI. |
    | Batch Size | Set a batch size for your dataset.  Enter a batch size between **1** and **10,000**. The default is **300** records per API call.  You can adjust this setting to improve performance:   - To ** reduce dataset execution time**, increase the batch size. - If you get an ** Out of Memory** error, try decreasing the batch size. |

  NEW (replace with / insert from CSV):
    | Cast Decimal to Double (checkbox) | Select this checkbox to convert the **Decimal** column from the input dataset to **Double** in Power BI. |
    | Batch Size | Set a batch size for your dataset.  Enter a batch size between **1** and **10,000**. The default is **300** records per API call.  You can adjust this setting to improve performance:   - To **reduce dataset execution time**, increase the batch size. - If you get an **Out of Memory** error, try decreasing the batch size. |

==============================================================================
FILE:   s/article/360058760154.mdx
TITLE:  Snowflake Key Pair Authentication Writeback Connector
LANG:   English (en_US)
ID:     360058760154
==============================================================================

CHANGE 1  [## Intro]  (lines 13–14 in repo file)

  OLD (remove from repo):
    --
    

CHANGE 2  [## Prerequisites]  (line 21 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region **account\_name** and All other regions ** account\_name.region\_id**. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region ****account\_name**** and All other regions ****account\_name.region\_id****. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

CHANGE 3  [## Prerequisites]  (line 29 in repo file)

  OLD (remove from repo):
    <Warning>**Important**: You need the CREATE TABLE, COPY INTO, PUT, CREATE FILE FORMAT, REMOVE, and LIST permissions on Snowflake.</Warning>

  NEW (replace with / insert from CSV):
    **Important**: You need the CREATE TABLE, COPY INTO, PUT, CREATE FILE FORMAT, REMOVE, and LIST permissions on Snowflake.

CHANGE 4  [### Configuring the public/private key pair]  (line 72 in repo file)

  OLD (remove from repo):
       <Note>

CHANGE 5  [### Configuring the public/private key pair]  (lines 76–77 in repo file)

  OLD (remove from repo):
       </Note>
    

CHANGE 6  [### Configuring the public/private key pair]  (lines 80–109 in repo file)

  ⚠ LARGE CHANGE (36 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Property</p></th>
          <th colspan="1" rowspan="1"><p>Value</p></th>
          <th colspan="1" rowspan="1"><p>Default</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Name</p></td>
          <td colspan="1" rowspan="1"><p>JSMITH</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Name</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_FP</p></td>
          <td colspan="1" rowspan="1"><p>SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA=</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's RSA public key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_2\_FP</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's second RSA public key.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
       | Property | Value | Default | Description |
       | --- | --- | --- | --- |
       | Name | JSMITH | null | Name |
       | RSA\_PUBLIC\_KEY\_FP | SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA= | null | Fingerprint of user's RSA public key. |
       | RSA\_PUBLIC\_KEY\_2\_FP | null | null | Fingerprint of user's second RSA public key. |
    

CHANGE 7  [### Configuring the public/private key pair]  (line 112 in repo file)

  OLD (remove from repo):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:</Note>

  NEW (replace with / insert from CSV):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:
       - Update the security parameters:
         - <path> specifies the local path to the private key file you created.
       - Update the session parameters:
         - <user> specifies your Snowflake login name.
         - <account> specifies the name of your account (provided by Snowflake).
       - The sample code is as follows:</Note>

CHANGE 8  [### Configuring the public/private key pair]  (lines 114–119 in repo file)

  OLD (remove from repo):
    - Update the security parameters:
    - <path> specifies the local path to the private key file you created.
    - Update the session parameters:
    - <user> specifies your Snowflake login name.
    - <account> specifies the name of your account (provided by Snowflake).
    - The sample code is as follows:

  NEW (replace with / insert from CSV):
         `import java.util.Properties;
          import java.sql.Connection;
          import java.sql.Statement;
          import java.sql.ResultSet;
          import java.sql.DriverManager;
          import java.io.File;
          import java.io.FileInputStream;
          import java.io.DataInputStream;
          import java.util.Base64;
          import java.security.spec.PKCS8EncodedKeySpec;
          import java.security.KeyFactory;
          import java.security.PrivateKey;
          import javax.crypto.EncryptedPrivateKeyInfo;
          import javax.crypto.SecretKeyFactory;
          import javax.crypto.spec.PBEKeySpec;`

CHANGE 9  [### Configuring the public/private key pair]  (lines 121–136 in repo file)

  OLD (remove from repo):
         ```java
         import java.util.Properties;
         import java.sql.Connection;
         import java.sql.Statement;
         import java.sql.ResultSet;
         import java.sql.DriverManager;
         import java.io.File;
         import java.io.FileInputStream;
         import java.io.DataInputStream;
         import java.util.Base64;
         import java.security.spec.PKCS8EncodedKeySpec;
         import java.security.KeyFactory;
         import java.security.PrivateKey;
         import javax.crypto.EncryptedPrivateKeyInfo;
         import javax.crypto.SecretKeyFactory;
         import javax.crypto.spec.PBEKeySpec;

  NEW (replace with / insert from CSV):
         `public class TestJdbc
          {
              public static void main(String[] args)
                  throws Exception
              {
                File f = new File("<path>/rsa_key.p8");
                FileInputStream fis = new FileInputStream(f);
                DataInputStream dis = new DataInputStream(fis);
                byte[] keyBytes = new byte[(int) f.length()];
                dis.readFully(keyBytes);
                dis.close();`

CHANGE 10  [### Configuring the public/private key pair]  (lines 138–148 in repo file)

  OLD (remove from repo):
         public class TestJdbc
         {
            public static void main(String[] args)
               throws Exception
            {
               File f = new File("<path>/rsa_key.p8");
               FileInputStream fis = new FileInputStream(f);
               DataInputStream dis = new DataInputStream(fis);
               byte[] keyBytes = new byte[(int) f.length()];
               dis.readFully(keyBytes);
               dis.close();

  NEW (replace with / insert from CSV):
         `String encrypted = new String(keyBytes);
                String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
                encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
                encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
                EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
                PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
                SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
                PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
                KeyFactory keyFactory = KeyFactory.getInstance("RSA");
                PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);`

CHANGE 11  [### Configuring the public/private key pair]  (lines 150–159 in repo file)

  OLD (remove from repo):
               String encrypted = new String(keyBytes);
               String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
               encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
               encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
               EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
               PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
               SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
               PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
               KeyFactory keyFactory = KeyFactory.getInstance("RSA");
               PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);

  NEW (replace with / insert from CSV):
         `String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
                Properties prop = new Properties();
                prop.put("user", "<user>");
                prop.put("account", "<account>");
                prop.put("privateKey", encryptedPrivateKey);`

CHANGE 12  [### Configuring the public/private key pair]  (lines 161–175 in repo file)

  OLD (remove from repo):
               String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
               Properties prop = new Properties();
               prop.put("user", "<user>");
               prop.put("account", "<account>");
               prop.put("privateKey", encryptedPrivateKey);
    
               Connection conn = DriverManager.getConnection(url, prop);
               Statement stat = conn.createStatement();
               ResultSet res = stat.executeQuery("select 1");
               res.next();
               System.out.println(res.getString(1));
               conn.close();
            }
         }
         ```

  NEW (replace with / insert from CSV):
         `Connection conn = DriverManager.getConnection(url, prop);
                Statement stat = conn.createStatement();
                ResultSet res = stat.executeQuery("select 1");
                res.next();
                System.out.println(res.getString(1));
                conn.close();
              }
          }`

CHANGE 13  [## Configuring the connection]  (line 201 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake Key Pair Authentication Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake Key Pair Authentication Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 14  [### Credentials Pane]  (lines 207–236 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the full name of your account (provided by Snowflake.) For US West Region <b>account\_name</b> and All other regions <b> account\_name.region\_id</b></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username used to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Private Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the private key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Passphrase</p></td>
          <td colspan="1" rowspan="1"><p>If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the full name of your account (provided by Snowflake.) For US West Region ****account\_name**** and All other regions ****account\_name.region\_id**** |
    | Username | Enter the username used to connect to your Snowflake host. |
    | Private Key | Enter the private key. |
    | Passphrase | If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here. |
    | Role | Enter your role. |
    

CHANGE 15  [### Details Pane]  (lines 243–288 in repo file)

  ⚠ LARGE CHANGE (58 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Input DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the DataSet ID (GUID) for the DataSet you want to copy to Snowflake. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL <a href="https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview">https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview</a>, the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Select how you want to name the table where data will be copied.    - <b>Use Input DataSet GUID</b>: The table name must be the number you entered for the <b> Input</b><b>DataSet ID</b>. - <b> Use Input Dataset Name</b>: The table name must be the name used for naming the Input DataSet. - <b> Enter Table Name</b>: Enter a custom table name in the <b> Enter</b> Table Name**** field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Enter Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the Snowflake table you want your data copied to (spaces and special characters will be replaced with underscores).</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Schema</p></td>
          <td colspan="1" rowspan="1"><p>Select the schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Use All Caps for Column Names and Table Name</p></td>
          <td colspan="1" rowspan="1"><p>Write back your column names and table name in ALL CAPS, so the names are case insensitive within Snowflake.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>How do you want to update your data in snowflake?</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Choose the Escape character</p></td>
          <td colspan="1" rowspan="1"><p>Select the escape character for the input dataset.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Input DataSet ID | Enter the DataSet ID (GUID) for the DataSet you want to copy to Snowflake. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/. For example, in the URL [https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview](https://mycompany.domo.com/datasources/845305d8-da3d-4107-a9d6-13ef3f86d4a4/details/overview), the DataSet ID is 845305d8-da3d-4107-a9d6-13ef3f86d4a4. |
    | Select Table Name | Select how you want to name the table where data will be copied.    - **Use Input DataSet GUID**: The table name must be the number you entered for the ****Input************DataSet ID********. - **Use Input Dataset Name**: The table name must be the name used for naming the Input DataSet. - **Enter Table Name**: Enter a custom table name in the **Enter******Table Name******** field. |
    | Enter Table Name | Enter the name of the Snowflake table you want your data copied to (spaces and special characters will be replaced with underscores). |
    | Warehouses | Select the warehouse. |
    | Databases | Select the database. |
    | Database Schema | Select the schema. |
    | Use All Caps for Column Names and Table Name | Write back your column names and table name in ALL CAPS, so the names are case insensitive within Snowflake. |
    | How do you want to update your data in snowflake? | | Table Options | Description | | --- | --- | | Drop Table and Load Data | Deletes the existing table and creates a new table if the table exists. | | Truncate Table and Load Data | Truncates an existing table and loads the new data, if the table already exists. The connector will preserve the schema of the existing table if this option is selected. | | Append Data to the existing table | Appends the data to the existing data. | |
    | Choose the Escape character | Select the escape character for the input dataset. |
    

CHANGE 16  [### FAQs]  (line 291 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### When should I use this connector?

CHANGE 17  [### FAQs]  (line 293 in repo file)

  OLD (remove from repo):
    <Accordion title="When should I use this connector?">

CHANGE 18  [### FAQs]  (line 295 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 19  [### FAQs]  (line 297 in repo file)

  OLD (remove from repo):
    <Accordion title="What permissions do I need on Snowflake?">

  NEW (replace with / insert from CSV):
    #### What permissions do I need on Snowflake?
    

CHANGE 20  [### FAQs]  (line 299 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 21  [### FAQs]  (line 301 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?
    

CHANGE 22  [### FAQs]  (line 303 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 23  [### FAQs]  (line 305 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 24  [### FAQs]  (line 307 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 25  [### FAQs]  (line 309 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 26  [### FAQs]  (line 311 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 27  [### FAQs]  (line 313 in repo file)

  OLD (remove from repo):
    <Accordion title="Can I use the same account to create multiple datasets?">

  NEW (replace with / insert from CSV):
    #### Can I use the same account to create multiple datasets?
    

CHANGE 28  [### FAQs]  (line 315 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 29  [### FAQs]  (line 317 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?
    

CHANGE 30  [### FAQs]  (line 319 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 31  [### FAQs]  (line 321 in repo file)

  OLD (remove from repo):
    <Accordion title="How do I find the Input Dataset ID?">

  NEW (replace with / insert from CSV):
    #### How do I find the Input Dataset ID?
    

CHANGE 32  [### Other Panes]  (lines 327–329 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360059173794.mdx
TITLE:  Snowflake KeyPair Unload Connector
LANG:   English (en_US)
ID:     360059173794
==============================================================================

CHANGE 1  [## Intro]  (lines 11–12 in repo file)

  OLD (remove from repo):
    ---
    

CHANGE 2  [## Prerequisites]  (line 18 in repo file)

  OLD (remove from repo):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region **account\_name** and All other regions ** account\_name.region\_id**. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

  NEW (replace with / insert from CSV):
      Note that your full account name may include additional segments that identify the region and cloud platform where your account is hosted. For US West Region ****account\_name**** and All other regions ****account\_name.region\_id****. The following table provides account name examples by cloud platform/region. For each row, it assumes your account name is "xy12345."

CHANGE 3  [## Prerequisites]  (line 25 in repo file)

  OLD (remove from repo):
    - Your AWS access key. It is available in the AWS Console in the **Security Credentials** section or under ** Users** if you are using ** IAM**.

  NEW (replace with / insert from CSV):
    - Your AWS access key. It is available in the AWS Console in the **Security Credentials** section or under **Users** if you are using **IAM**.

CHANGE 4  [### Configuring the public/private key pair]  (lines 75–104 in repo file)

  ⚠ LARGE CHANGE (36 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Property</p></th>
          <th colspan="1" rowspan="1"><p>Value</p></th>
          <th colspan="1" rowspan="1"><p>Default</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Name</p></td>
          <td colspan="1" rowspan="1"><p>JSMITH</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Name</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_FP</p></td>
          <td colspan="1" rowspan="1"><p>SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA=</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's RSA public key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>RSA\_PUBLIC\_KEY\_2\_FP</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>null</p></td>
          <td colspan="1" rowspan="1"><p>Fingerprint of user's second RSA public key.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
       | Property | Value | Default | Description |
       | --- | --- | --- | --- |
       | Name | JSMITH | null | Name |
       | RSA\_PUBLIC\_KEY\_FP | SHA256:nvnONUsfiuycCLMXIEWG4eTp4FjhVUZQUQbNpbSHXiA= | null | Fingerprint of user's RSA public key. |
       | RSA\_PUBLIC\_KEY\_2\_FP | null | null | Fingerprint of user's second RSA public key. |
    

CHANGE 5  [### Configuring the public/private key pair]  (line 107 in repo file)

  OLD (remove from repo):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:</Note>

  NEW (replace with / insert from CSV):
    6. Modify and execute the sample code, below. The code decrypts the private key file and passes it to the Snowflake driver to create a connection:
       - Update the security parameters:
         - <path> specifies the local path to the private key file you created.
       - Update the session parameters:
         - <user> specifies your Snowflake login name.
         - <account> specifies the name of your account (provided by Snowflake).
       - The sample code is as follows:</Note>

CHANGE 6  [### Configuring the public/private key pair]  (lines 109–114 in repo file)

  OLD (remove from repo):
    - Update the security parameters:
    - <path> specifies the local path to the private key file you created.
    - Update the session parameters:
    - <user> specifies your Snowflake login name.
    - <account> specifies the name of your account (provided by Snowflake).
    - The sample code is as follows:

  NEW (replace with / insert from CSV):
         `import java.util.Properties;
          import java.sql.Connection;
          import java.sql.Statement;
          import java.sql.ResultSet;
          import java.sql.DriverManager;
          import java.io.File;
          import java.io.FileInputStream;
          import java.io.DataInputStream;
          import java.util.Base64;
          import java.security.spec.PKCS8EncodedKeySpec;
          import java.security.KeyFactory;
          import java.security.PrivateKey;
          import javax.crypto.EncryptedPrivateKeyInfo;
          import javax.crypto.SecretKeyFactory;
          import javax.crypto.spec.PBEKeySpec;`

CHANGE 7  [### Configuring the public/private key pair]  (line 116 in repo file)

  NEW (replace with / insert from CSV):
         `public class TestJdbc
          {
              public static void main(String[] args)
                  throws Exception
              {
                File f = new File("<path>/rsa_key.p8");
                FileInputStream fis = new FileInputStream(f);
                DataInputStream dis = new DataInputStream(fis);
                byte[] keyBytes = new byte[(int) f.length()];
                dis.readFully(keyBytes);
                dis.close();`

CHANGE 8  [### Configuring the public/private key pair]  (lines 117–132 in repo file)

  OLD (remove from repo):
          ```java
          import java.util.Properties;
          import java.sql.Connection;
          import java.sql.Statement;
          import java.sql.ResultSet;
          import java.sql.DriverManager;
          import java.io.File;
          import java.io.FileInputStream;
          import java.io.DataInputStream;
          import java.util.Base64;
          import java.security.spec.PKCS8EncodedKeySpec;
          import java.security.KeyFactory;
          import java.security.PrivateKey;
          import javax.crypto.EncryptedPrivateKeyInfo;
          import javax.crypto.SecretKeyFactory;
          import javax.crypto.spec.PBEKeySpec;

  NEW (replace with / insert from CSV):
         `String encrypted = new String(keyBytes);
                String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
                encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
                encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
                EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
                PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
                SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
                PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
                KeyFactory keyFactory = KeyFactory.getInstance("RSA");
                PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);`

CHANGE 9  [### Configuring the public/private key pair]  (lines 134–144 in repo file)

  OLD (remove from repo):
          public class TestJdbc
          {
              public static void main(String[] args)
                  throws Exception
              {
                File f = new File("<path>/rsa_key.p8");
                FileInputStream fis = new FileInputStream(f);
                DataInputStream dis = new DataInputStream(fis);
                byte[] keyBytes = new byte[(int) f.length()];
                dis.readFully(keyBytes);
                dis.close();

  NEW (replace with / insert from CSV):
         `String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
                Properties prop = new Properties();
                prop.put("user", "<user>");
                prop.put("account", "<account>");
                prop.put("privateKey", encryptedPrivateKey);`

CHANGE 10  [### Configuring the public/private key pair]  (lines 146–171 in repo file)

  ⚠ LARGE CHANGE (34 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
                String encrypted = new String(keyBytes);
                String passphrase = System.getenv("PRIVATE_KEY_PASSPHRASE");
                encrypted = encrypted.replace("-----BEGIN ENCRYPTED PRIVATE KEY-----", "");
                encrypted = encrypted.replace("-----END ENCRYPTED PRIVATE KEY-----", "");
                EncryptedPrivateKeyInfo pkInfo = new EncryptedPrivateKeyInfo(Base64.getMimeDecoder().decode(encrypted));
                PBEKeySpec keySpec = new PBEKeySpec(passphrase.toCharArray());
                SecretKeyFactory pbeKeyFactory = SecretKeyFactory.getInstance(pkInfo.getAlgName());
                PKCS8EncodedKeySpec encodedKeySpec = pkInfo.getKeySpec(pbeKeyFactory.generateSecret(keySpec));
                KeyFactory keyFactory = KeyFactory.getInstance("RSA");
                PrivateKey encryptedPrivateKey = keyFactory.generatePrivate(encodedKeySpec);
    
                String url = "jdbc:snowflake://<account>.snowflakecomputing.com";
                Properties prop = new Properties();
                prop.put("user", "<user>");
                prop.put("account", "<account>");
                prop.put("privateKey", encryptedPrivateKey);
    
                Connection conn = DriverManager.getConnection(url, prop);
                Statement stat = conn.createStatement();
                ResultSet res = stat.executeQuery("select 1");
                res.next();
                System.out.println(res.getString(1));
                conn.close();
              }
          }
          ```

  NEW (replace with / insert from CSV):
         `Connection conn = DriverManager.getConnection(url, prop);
                Statement stat = conn.createStatement();
                ResultSet res = stat.executeQuery("select 1");
                res.next();
                System.out.println(res.getString(1));
                conn.close();
              }
          }`

CHANGE 11  [## Connecting to Your Snowflake Account]  (line 195 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Snowflake KeyPair Unload Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Snowflake KeyPair Unload Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 12  [### Credentials Pane]  (lines 201–246 in repo file)

  ⚠ LARGE CHANGE (58 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter the full name of your account (provided by Snowflake.) For US West Region <b>account\_name</b> and All other regions <b> account\_name.region\_id</b></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username used to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Private Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the private key.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Passphrase</p></td>
          <td colspan="1" rowspan="1"><p>If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS access key. It is available in the AWS Console in the Security Credentials section or under Users if you are using IAM.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Secret Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS secret access key. It was provided when you created your Access Key. You can generate a new one in the AWS Console.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Bucket</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the AWS S3 bucket that your Snowflake data will be unloaded into.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Region</p></td>
          <td colspan="1" rowspan="1"><p>Select the Amazon S3 Region.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter the full name of your account (provided by Snowflake.) For US West Region ****account\_name**** and All other regions ****account\_name.region\_id**** |
    | Username | Enter the username used to connect to your Snowflake host. |
    | Private Key | Enter the private key. |
    | Passphrase | If you create an encrypted private key, then OpenSSL prompts for a passphrase used to encrypt the private key file. Enter that passphrase here. |
    | Role | Enter your role. |
    | Access Key | Enter your AWS access key. It is available in the AWS Console in the Security Credentials section or under Users if you are using IAM. |
    | Secret Key | Enter your AWS secret access key. It was provided when you created your Access Key. You can generate a new one in the AWS Console. |
    | Bucket | Enter the name of the AWS S3 bucket that your Snowflake data will be unloaded into. |
    | Region | Select the Amazon S3 Region. |
    

CHANGE 13  [### Details Pane]  (lines 253–294 in repo file)

  ⚠ LARGE CHANGE (53 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query to use to retrieve your data. You can use the <b>Query Helper</b> parameter to help you write a usable SQL query. To use the <b> Query Helper</b>, do the following:   1. Select your desired warehouse, database, database schema, database table, and table columns in each menu. 2. Copy the SQL statement that appears in the <b> Query Helper</b> field. 3. Paste the copied SQL statement into the <b> Query</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates with all warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve.  The list populates with all databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Schema</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates with all schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to retrieve. The list populates with all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns</p></td>
          <td colspan="1" rowspan="1"><p>Select the table columns you would like to include in your report. If you do not select any table column, the connector retrieves your entire selected database table.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste this text into the QUERY field. You may edit this query if desired.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Use Snowflake Schema</p></td>
          <td colspan="1" rowspan="1"><p>Check this box if you wish to use the schema directly from Snowflake instead of letting Domo determine the schema.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Query | Enter the SQL query to use to retrieve your data. You can use the **Query Helper**parameter to help you write a usable SQL query. To use the **Query Helper**, do the following:   1. Select your desired warehouse, database, database schema, database table, and table columns in each menu. 2. Copy the SQL statement that appears in the **Query Helper**field. 3. Paste the copied SQL statement into the **Query** field. |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates with all warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve.  The list populates with all databases you have access to based on your credentials. |
    | Database Schema | Select the database schema containing the data you want to retrieve. The list populates with all schemas you have access to based on your credentials. |
    | Database Tables | Select the database table you want to retrieve. The list populates with all tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Table Columns | Select the table columns you would like to include in your report. If you do not select any table column, the connector retrieves your entire selected database table. |
    | Query Helper | When you select warehouses, databases, schemas, tables, etc. from the above menus, a usable SQL query is built for you in this field. Copy and paste this text into the QUERY field. You may edit this query if desired. |
    | Use Snowflake Schema | Check this box if you wish to use the schema directly from Snowflake instead of letting Domo determine the schema. |
    

CHANGE 14  [## FAQs]  (line 301 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### Are there any API limits I should be aware of?

CHANGE 15  [## FAQs]  (line 303 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits I should be aware of?">

CHANGE 16  [## FAQs]  (line 305 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 17  [## FAQs]  (line 307 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 18  [## FAQs]  (line 309 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 19  [## FAQs]  (line 311 in repo file)

  OLD (remove from repo):
    <Accordion title="What should I be aware of while writing a query?">

  NEW (replace with / insert from CSV):
    #### What should I be aware of while writing a query?
    

CHANGE 20  [## FAQs]  (line 313 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 21  [## FAQs]  (line 315 in repo file)

  OLD (remove from repo):
    <Accordion title="Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Why can't I connect to my Snowflake database? Do I need to whitelist any IP addresses?
    

CHANGE 22  [## FAQs]  (lines 317–319 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360060476953.mdx
TITLE:  Awardco Connector
LANG:   English (en_US)
ID:     360060476953
==============================================================================

CHANGE 1  [#### Obtaining the API key:]  (line 20 in repo file)

  OLD (remove from repo):
    4. Next to the **API key** field, click ** Generate key**.

  NEW (replace with / insert from CSV):
    4. Next to the **API key** field, click **Generate key**.

CHANGE 2  [## Connecting to Your Awardco Account]  (line 27 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Awardco Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Awardco Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 33–46 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>API Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Awardco API key. See "Prerequisites" for more details.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter your Awardco API key. See "Prerequisites" for more details. |
    

CHANGE 4  [### Details Pane]  (lines 53–110 in repo file)

  ⚠ LARGE CHANGE (72 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Awardco report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select the date format for your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Single Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Relative Date</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data for in the <b>Days Back</b> field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Range</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific or relative date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the last date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data from (start day). Combine with <b><i></b><b></i>End Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b><b><i></b></i>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data to (end day). Combine with <b><i></b><b></i>Start Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b><b><i></b></i>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Time Period</p></td>
          <td colspan="1" rowspan="1"><p>Choose the time period that you would like to receive data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Starting Day of the Week</p></td>
          <td colspan="1" rowspan="1"><p>Select the day you would like your week to start with.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Awardco report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Client Fulfillment |  | | Funding Reconciliation |  | | Login |  | | MemoryBook Usage |  | | Money Account (Legacy Report Deprecated) |  | | Money Deposited |  | | Money Spent |  | | Point Budgets |  | | Points Redeemed |  | | Point Transactions |  | | Recognition Detail | Returns a detail level report for recognition within the selected time frame. | | Recognition Detail (Legacy Report Deprecated) |  | | Recognition Program Utilization |  | | Recognition Status |  | | Recognition Summary (Legacy Report Deprecated) | Returns a summary level report for recognition within the selected time frame. | | Redemption Account |  | | Redemption Detail (Legacy Report Deprecated) | Returns a detail level report for redemption within the selected time frame. | | Redemption Fifo (Legacy Report Deprecated) | Returns the information for redemption fifo report within the selected time frame | | Redemption Point Source (FIFO) |  | | Redemption Summary (Legacy Report Deprecated) | Returns a summary level report for redemption within the selected time frame. | | Transactions | Returns all transactions within the selected time frame. | | User Point Balance |  | | Users | Returns a list of all users. | |
    | Date Selection | Select the date format for your data. |
    | Single Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Specific Date | Select the specific date using the date selector. |
    | Relative Date | Enter the number of days back that you would like to get data for in the ****Days Back**** field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Date Range | Select the specific or relative date range. |
    | Start Date - Specific | Select the first date in your date range using the date selector. |
    | End Date - Specific | Select the last date in your date range using the date selector. |
    | Start Date - Relative | Enter the number of days back that you would like to get data from (start day). Combine with ****************End Date**************** to create a range of represented days.  For example, if you entered 10 for ****************Start Date**************** and 5 for ****************End Date****************, the report would contain data for 10 days ago up until 5 days ago. |
    | End Date - Relative | Enter the number of days back that you would like to get data to (end day). Combine with ****************Start Date****************to create a range of represented days.  For example, if you entered 10 for ****************Start Date**************** and 5 for ****************End Date****************, the report would contain data for 10 days ago up until 5 days ago. |
    | Time Period | Choose the time period that you would like to receive data for. |
    | Starting Day of the Week | Select the day you would like your week to start with. |

==============================================================================
FILE:   s/article/360060508773.mdx
TITLE:  Zendesk Support Connector with Upsert
LANG:   English (en_US)
ID:     360060508773
==============================================================================

CHANGE 1  [## Intro]  (line 9 in repo file)

  OLD (remove from repo):
    The Zendesk Support Connector with Upsert is a "Cloud App" Connector, meaning it retrieves data stored in the cloud. In the Data Center, you can access the Connector page for this and other Cloud App Connectors by clicking **Cloud App** in the toolbar at the top of the window.

  NEW (replace with / insert from CSV):
    The Zendesk Support Connector with Upsert is a "Cloud App" Connector, meaning it retrieves data stored in the cloud. In the Data Center, you can access the Connector page for this and other Cloud App Connectors by clicking ****Cloud App**** in the toolbar at the top of the window.

CHANGE 2  [## Connecting to Your Zendesk Support Connector with Upsert Account]  (line 19 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Zendesk Support Connector with Upsert page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most Connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Zendesk Support Connector with Upsert page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most Connector types and are discussed in great detail in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [### Credentials Pane]  (lines 25–38 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Zendesk URL</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Zendesk URL in the format <i>mycompany.zendesk.com<b></i>.</b> Do not include<i>https://</i>.  Once you enter the Zendesk URL, you will be prompted to enter the Zendesk credentials.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Zendesk URL | Enter the Zendesk URL in the format *mycompany.zendesk.com***.** Do not include*https://*.  Once you enter the Zendesk URL, you will be prompted to enter the Zendesk credentials. |
    

CHANGE 4  [### Details Pane]  (lines 45–94 in repo file)

  ⚠ LARGE CHANGE (63 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Zendesk Support Connector with Upsert report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Associated Data</p></td>
          <td colspan="1" rowspan="1"><p>Select the associated data you want loaded along with the tickets.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Include Attachments</p></td>
          <td colspan="1" rowspan="1"><p>Specify whether you would like to include the attachments in the results.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select the date to start retrieving data on the first run. Subsequent runs will automatically retrieve newly created and updated tickets, users or organizations</p></td>
          <td colspan="1" rowspan="1"><p>Select the date to start retrieving data on the first run. Subsequent runs will automatically retrieve newly created and updated items. If you modify the date after the first run, this date will be used for the next run and the existing data will be replaced.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select the Date to start retrieving data on the first run. Subsequent runs will automatically retrieve newly created and updated tickets, users, or organizations.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Single Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Relative Date</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data for in the <b>Days Back</b> field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Legacy Schema Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select this check box to use legacy schema for backward compatibility with the Zendesk Support Connector with Upsert Connector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Exclude Deleted Records</p></td>
          <td colspan="1" rowspan="1"><p>Select this check box to exclude the deleted records from the data.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Zendesk Support Connector with Upsert report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Brands | Retrieves a list of all brands for your account sorted by name. | | Organizations | Retrieves information about the organizations in your Zendesk Support Connector with Upsert instance. | | Specify Query | Allows you to build a customized report by entering your own Zendesk Support Connector with Upsert search query. You can also select from two premade queries—**Open Tickets** and **Closed Tickets**. You can use these to return the number of open and closed tickets for your Zendesk Support Connector with Upsert instance. For more information about entering Zendesk Support Connector with Upsert queries, see [Zendesk Support Connector with Upsert Support search reference](https://support.zendesk.com/hc/en-us/articles/4408886879258). | | Tickets | Retrieves information about the created and updated tickets. | | Ticket Activities | Retrieves Lists ticket activities in the last 30 days, affecting the agent making the request. | | Ticket Audits | Retrieve the list of ticket audits. | | Ticket Fields | Retrieve tickets fields in your Zendesk Support Connector with Upsert instance. | | Ticket Forms - Agent Conditions | Retrieve ticket forms in your Zendesk Support Connector with Upsert instance and displays the condition sets for agent workplace. | | Ticket Forms - End User Conditions | Retrieve ticket forms in your Zendesk Support Connector with Upsert instance and displays the condition sets for end user products. | | Ticket Comments | Retrieves the comments on the tickets. | | Ticket Metric Events | Retrieves the ticket metric events. | | Users | Retrieves the information about the users in your Zendesk Support Connector with Upsert instance. | |
    | Associated Data | Select the associated data you want loaded along with the tickets. |
    | Include Attachments | Specify whether you would like to include the attachments in the results. |
    | Select the date to start retrieving data on the first run. Subsequent runs will automatically retrieve newly created and updated tickets, users or organizations | Select the date to start retrieving data on the first run. Subsequent runs will automatically retrieve newly created and updated items. If you modify the date after the first run, this date will be used for the next run and the existing data will be replaced. |
    | Date Selection | Select the Date to start retrieving data on the first run. Subsequent runs will automatically retrieve newly created and updated tickets, users, or organizations. |
    | Single Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Specific Date | Select the specific date using the date selector. |
    | Relative Date | Enter the number of days back that you would like to get data for in the ****Days Back**** field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Legacy Schema Selection | Select this check box to use legacy schema for backward compatibility with the Zendesk Support Connector with Upsert Connector. |
    | Exclude Deleted Records | Select this check box to exclude the deleted records from the data. |
    

CHANGE 5  [## FAQs]  (line 101 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### What endpoint is the base URL for this connector?

CHANGE 6  [## FAQs]  (line 103 in repo file)

  OLD (remove from repo):
    <Accordion title="What endpoint is the base URL for this connector?">

CHANGE 7  [## FAQs]  (line 105 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 8  [## FAQs]  (lines 107–166 in repo file)

  ⚠ LARGE CHANGE (61 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <Accordion title="Which endpoint(s) does each report call in this connector?">
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Report Name</p></th>
          <th colspan="1" rowspan="1"><p>Enpoint URL</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Brands</p></td>
          <td colspan="1" rowspan="1"><p>/api/v2/brands.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Organizations</p></td>
          <td colspan="1" rowspan="1"><p>/incremental/organizations.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specify Query</p></td>
          <td colspan="1" rowspan="1"><p>/api/v2/search/export.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Tickets</p></td>
          <td colspan="1" rowspan="1"><p>/incremental/tickets.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Activities</p></td>
          <td colspan="1" rowspan="1"><p>/api/v2/activities.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Audits</p></td>
          <td colspan="1" rowspan="1"><p>/ticket\_audits.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Fields</p></td>
          <td colspan="1" rowspan="1"><p>/ticket\_fields.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Forms - Agent Conditions</p></td>
          <td colspan="1" rowspan="1"><p>/ticket\_forms.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Forms - End User Conditions</p></td>
          <td colspan="1" rowspan="1"><p>/ticket\_forms.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Comments</p></td>
          <td colspan="1" rowspan="1"><p>/tickets/{currentTicketId}/comments.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Ticket Metric Events</p></td>
          <td colspan="1" rowspan="1"><p>/incremental/ticket\_metric\_events.json</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Users</p></td>
          <td colspan="1" rowspan="1"><p>/incremental/users.json</p></td>
        </tr>
      </tbody>
    </table>
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Which endpoint(s) does each report call in this connector?

CHANGE 9  [## FAQs]  (line 168 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up the this connector?">

  NEW (replace with / insert from CSV):
    | Report Name | Enpoint URL |
    | --- | --- |
    | Brands | /api/v2/brands.json |
    | Organizations | /incremental/organizations.json |
    | Specify Query | /api/v2/search/export.json |
    | Tickets | /incremental/tickets.json |
    | Ticket Activities | /api/v2/activities.json |
    | Ticket Audits | /ticket\_audits.json |
    | Ticket Fields | /ticket\_fields.json |
    | Ticket Forms - Agent Conditions | /ticket\_forms.json |
    | Ticket Forms - End User Conditions | /ticket\_forms.json |
    | Ticket Comments | /tickets/{currentTicketId}/comments.json |
    | Ticket Metric Events | /incremental/ticket\_metric\_events.json |
    | Users | /incremental/users.json |
    
    #### What kind of credentials do I need to power up the this connector?
    

CHANGE 10  [## FAQs]  (line 170 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 11  [## FAQs]  (line 172 in repo file)

  OLD (remove from repo):
    <Accordion title="How frequently will my data update?">

  NEW (replace with / insert from CSV):
    #### How frequently will my data update?
    

CHANGE 12  [## FAQs]  (line 174 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 13  [## FAQs]  (line 176 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 14  [## FAQs]  (line 178 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 15  [## FAQs]  (line 180 in repo file)

  OLD (remove from repo):
    <Accordion title="Can I use the same Zendesk Support Connector with Upsert account to create multiple datasets?">

  NEW (replace with / insert from CSV):
    #### Can I use the same Zendesk Support Connector with Upsert account to create multiple datasets?
    

CHANGE 16  [## FAQs]  (line 182 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 17  [## FAQs]  (line 184 in repo file)

  OLD (remove from repo):
    <Accordion title="What's the difference between the Zendesk Support Connector with Upsert and Zendesk Support Connector with Upsert Upsert connectors?">

  NEW (replace with / insert from CSV):
    #### What's the difference between the Zendesk Support Connector with Upsert and Zendesk Support Connector with Upsert Upsert connectors?
    

CHANGE 18  [## FAQs]  (lines 187–189 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>

==============================================================================
FILE:   s/article/360061691114.mdx
TITLE:  Snowflake High Bandwidth With Advanced Partitions Connector
LANG:   English (en_US)
ID:     360061691114
==============================================================================

CHANGE 1  [## Prerequisites]  (line 27 in repo file)

  OLD (remove from repo):
    - The full name of your account (provided by Snowflake). For US West Region **account\_name**, and All other regions ** account\_name.region\_id**. This is the portion of your Snowflake URL immediately following https://.

  NEW (replace with / insert from CSV):
    - The full name of your account (provided by Snowflake). For US West Region **account\_name**, and All other regions **account\_name.region\_id**. This is the portion of your Snowflake URL immediately following https://.

CHANGE 2  [## Connecting to Your Snowflake Account]  (line 34 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the Credentials and Details panes in the Snowflake High Bandwidth With Advanced Partitions Connector page. The components of the other panes in this page, **Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the Credentials and Details panes in the Snowflake High Bandwidth With Advanced Partitions Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 3  [## Credentials Pane]  (lines 40–65 in repo file)

  ⚠ LARGE CHANGE (32 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Snowflake account name. For more information, see Prerequisites.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password you use to connect to your Snowflake host.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Role</p></td>
          <td colspan="1" rowspan="1"><p>Enter your role.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter your Snowflake account name. For more information, see Prerequisites. |
    | Username | Enter the username you use to connect to your Snowflake host. |
    | Password | Enter the password you use to connect to your Snowflake host. |
    | Role | Enter your role. |

CHANGE 4  [## Credentials Pane]  (line 67 in repo file)

  OLD (remove from repo):
    <Note>**Note:** For any role that is used to set up the connector, the ** READ**, ** WRITE**, ** CREATE STAGE**, and ** SELECT** privileges must be turned on. For more details on each privileges, see [https://docs.snowflake.com/en/user-guide/security-access-control-privileges](https://docs.snowflake.com/en/user-guide/security-access-control-privileges#table-privileges).</Note>

  NEW (replace with / insert from CSV):
    <Note>**Note:** For any role that is used to set up the connector, the **READ**, **WRITE**, **CREATE STAGE**, and **SELECT** privileges must be turned on. For more details on each privileges, see [https://docs.snowflake.com/en/user-guide/security-access-control-privileges](https://docs.snowflake.com/en/user-guide/security-access-control-privileges#table-privileges).</Note>

CHANGE 5  [## Details Pane]  (lines 75–185 in repo file)

  ⚠ LARGE CHANGE (138 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Query Type. The following query types are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Warehouses</p></td>
          <td colspan="1" rowspan="1"><p>Select the warehouse containing the data you want to retrieve. The list populates with all of the warehouses you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Databases</p></td>
          <td colspan="1" rowspan="1"><p>Select the database containing the data you want to retrieve. The list populates with all of the databases you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Schemas</p></td>
          <td colspan="1" rowspan="1"><p>Select the database schema containing the data you want to retrieve. The list populates with all of the schemas you have access to based on your credentials.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Database Objects</p></td>
          <td colspan="1" rowspan="1"><p>Select the database object.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Tables</p></td>
          <td colspan="1" rowspan="1"><p>Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Columns</p></td>
          <td colspan="1" rowspan="1"><p>Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Table Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>This field shows the query that is generated based on the selected fields.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Views</p></td>
          <td colspan="1" rowspan="1"><p>Select the view.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View Columns</p></td>
          <td colspan="1" rowspan="1"><p>Select the view columns that you would like to include in your report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View Query Helper</p></td>
          <td colspan="1" rowspan="1"><p>This field shows the query that is generated based on the selected fields.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Query</p></td>
          <td colspan="1" rowspan="1"><p>Enter the SQL query to use to retrieve your data. You can use the <b>Query Helper</b> parameter to help you write a usable SQL query. To use the <b> Query Helper</b>, do the following:   1. Select your desired warehouse, database, database schema, database table, and table columns in each menu. 2. Copy the SQL statement that appears in the <b> Query Helper</b> field. 3. Paste the copied SQL statement into the <b> Query</b> field.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Support</p></td>
          <td colspan="1" rowspan="1"><p>Choose whether or not to partition your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to partition the data using the date keys, non date keys, or meta query.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Query to determine partition tags</p></td>
          <td colspan="1" rowspan="1"><p>Enter the partition meta query to determine the distinct partition tags (or keys). The column containing the <b>Date</b> data is your partition column.  Example: SELECT DISTINCT(DATE\_COLUMN\_NAME) FROM TABLENAME</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Support Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition support format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Year</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past years that you want to get data for. Value can be X, where X is a positive integer. For example: 5.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Future Years</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of future years that you want to get data for. Value can be X, where X is a positive integer. For example:5.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Months</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past months that you want to get data for. Value can be X, where X is a positive integer. For example:5</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Future Months</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of future months that you want to get data for. Value can be X, where X is a positive integer. For example:5.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Past Days</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. For example: 30.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Select the required date format. By default <b>yyyy-MM-dd</b> will be used.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Custom Date Format</p></td>
          <td colspan="1" rowspan="1"><p>Enter the custom date format.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Partition Non Date Key Column Name</p></td>
          <td colspan="1" rowspan="1"><p>Select the partition column name.</p></td>
        </tr>
      </tbody>
    </table>
    

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Query Type. The following query types are available:   |  |  | | --- | --- | | Custom Query | Enter the SQL query to execute. | | Query Builder | Select a table and fields to auto generate your query. | |
    | Warehouses | Select the warehouse containing the data you want to retrieve. The list populates with all of the warehouses you have access to based on your credentials. |
    | Databases | Select the database containing the data you want to retrieve. The list populates with all of the databases you have access to based on your credentials. |
    | Schemas | Select the database schema containing the data you want to retrieve. The list populates with all of the schemas you have access to based on your credentials. |
    | Database Objects | Select the database object. |
    | Tables | Select the database table you want to retrieve. The list populates with all of the tables you have access to based on your credentials. If you do not select a database table, the connector retrieves your entire database schema. |
    | Table Columns | Select the table columns you want to retrieve. The list populates with all of the columns you have access to based on your credentials. If you do not select a table column, the connector retrieves your entire selected database table. |
    | Table Query Helper | This field shows the query that is generated based on the selected fields. |
    | Views | Select the view. |
    | View Columns | Select the view columns that you would like to include in your report. |
    | View Query Helper | This field shows the query that is generated based on the selected fields. |
    | Query | Enter the SQL query to use to retrieve your data. You can use the ********Query Helper********parameter to help you write a usable SQL query. To use the ********Query Helper********, do the following:   1. Select your desired warehouse, database, database schema, database table, and table columns in each menu. 2. Copy the SQL statement that appears in the ********Query Helper********field. 3. Paste the copied SQL statement into the ********Query******** field. |
    | Partition Support | Choose whether or not to partition your data. |
    | Partition Criteria | Select whether you want to partition the data using the date keys, non date keys, or meta query. |
    | Partition Query to determine partition tags | Enter the partition meta query to determine the distinct partition tags (or keys). The column containing the **Date**data is your partition column.  Example: SELECT DISTINCT(DATE\_COLUMN\_NAME) FROM TABLENAME |
    | Partition Support Format | Select the partition support format. |
    | Partition Column Name | Select the partition column name. |
    | Past Year | Enter the number of past years that you want to get data for. Value can be X, where X is a positive integer. For example: 5. |
    | Future Years | Enter the number of future years that you want to get data for. Value can be X, where X is a positive integer. For example:5. |
    | Past Months | Enter the number of past months that you want to get data for. Value can be X, where X is a positive integer. For example:5 |
    | Future Months | Enter the number of future months that you want to get data for. Value can be X, where X is a positive integer. For example:5. |
    | Past Days | Enter the number of past days that you want to get data for. Value can be X, where X is a positive integer. For example: 30. |
    | Date Format | Select the required date format. By default ****yyyy-MM-dd**** will be used. |
    | Custom Date Format | Enter the custom date format. |
    | Partition Non Date Key Column Name | Select the partition column name. |

CHANGE 6  [## FAQ]  (lines 219–221 in repo file)

  OLD (remove from repo):
    </AccordionGroup>
    
    ## Troubleshooting

  NEW (replace with / insert from CSV):
    </AccordionGroup>## Troubleshooting

==============================================================================
FILE:   s/article/4405291644311.mdx
TITLE:  Amazon S3 Unload Writeback Connector
LANG:   English (en_US)
ID:     4405291644311
==============================================================================

CHANGE 1  [## Intro]  (lines 13–14 in repo file)

  OLD (remove from repo):
    ---
    

CHANGE 2  [## Prerequisites]  (line 20 in repo file)

  OLD (remove from repo):
    - Your AWS access key. You can find this in the **Security Credentials** section of the AWS Console. Alternatively, if you are using IAM, you can find it under ** Users**.

  NEW (replace with / insert from CSV):
    - Your AWS access key. You can find this in the ****Security Credentials**** section of the AWS Console. Alternatively, if you are using IAM, you can find it under ****Users****.

CHANGE 3  [## Prerequisites]  (line 27 in repo file)

  OLD (remove from repo):
    2. In the top right corner under **My Account** click ** New Client**.

  NEW (replace with / insert from CSV):
    2. In the top right corner under **My Account** click **New Client**.

CHANGE 4  [## Prerequisites]  (line 29 in repo file)

  OLD (remove from repo):
    4. Provide the application scope by selecting the checkboxes for **Data** and ** User**.

  NEW (replace with / insert from CSV):
    4. Provide the application scope by selecting the checkboxes for **Data** and **User**.

CHANGE 5  [## Prerequisites]  (line 33 in repo file)

  OLD (remove from repo):
    Your **Client Secret** will appear in the ** Manage Client** section.

  NEW (replace with / insert from CSV):
    Your **Client Secret** will appear in the **Manage Client** section.

CHANGE 6  [## Configuring the Connection]  (line 39 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Amazon S3 Unload Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Amazon S3 Unload Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 7  [### Credentials Pane]  (lines 45–78 in repo file)

  ⚠ LARGE CHANGE (43 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client secret.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Access Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS access key. You can find this in the <b>Security Credentials</b> section of the AWS Console. Alternatively, if you are using <b> IAM</b>, you can find it under <b> Users</b>.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Secret Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your AWS secret key that was provided when you created your access key. You can generate a new secret key in the AWS Console.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Bucket</p></td>
          <td colspan="1" rowspan="1"><p>Enter the name of the bucket you want to push Domo data to.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Region</p></td>
          <td colspan="1" rowspan="1"><p>Select the Amazon S3 region where your bucket is located.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domo Client ID | Enter your Domo client ID. |
    | Domo Client Secret | Enter your Domo client secret. |
    | Access Key | Enter your AWS access key. You can find this in the **Security Credentials** section of the AWS Console. Alternatively, if you are using **IAM**, you can find it under **Users**. |
    | Secret Key | Enter your AWS secret key that was provided when you created your access key. You can generate a new secret key in the AWS Console. |
    | Bucket | Enter the name of the bucket you want to push Domo data to. |
    | Region | Select the Amazon S3 region where your bucket is located. |
    

CHANGE 8  [### Details Pane]  (lines 85–114 in repo file)

  ⚠ LARGE CHANGE (37 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Input DataSet ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo dataset ID (GUID) located in the dataset URL. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/.  Example: https://customer.domo.com/datasources/<b>aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee</b>/details/settings</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Path</p></td>
          <td colspan="1" rowspan="1"><p>Enter the path where you would like the file to be saved in your Amazon S3 bucket.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Primary Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter the primary key of the datafusion dataset.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specific Day</p></td>
          <td colspan="1" rowspan="1"><p>Enter an integer that specifies the day you want to get the data for. 0 pulls today's data, 1 pulls yesterday's data, and 7 pulls the data from 7 days ago each time the connector runs. Only a single day of data is returned for each connector run.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Select the Data Type for Date Column(s) to be parsed</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Input DataSet ID | Enter your Domo dataset ID (GUID) located in the dataset URL. You can find the ID by opening the details view for the DataSet in the Data Center and looking at the portion of the URL following datasources/.  Example: https://customer.domo.com/datasources/**aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee**/details/settings |
    | Path | Enter the path where you would like the file to be saved in your Amazon S3 bucket. |
    | Primary Key | Enter the primary key of the datafusion dataset. |
    | Specific Day | Enter an integer that specifies the day you want to get the data for. 0 pulls today's data, 1 pulls yesterday's data, and 7 pulls the data from 7 days ago each time the connector runs. Only a single day of data is returned for each connector run. |
    | Select the Data Type for Date Column(s) to be parsed | |  |  | | --- | --- | | **Date Time** | **Description** | | Parse as is (Do not convert) | Metrics columns say- 'started' & 'finished' will be shown as is received in the response. | | Parse and convert into Date Time | Metrics columns say- 'started' & 'finished' will be parsed into Date Time. |   **Note**: This is just for the dataset metrics to be displayed; there will be no impact on the data to be unloaded. |

==============================================================================
FILE:   s/article/4409512475927.mdx
TITLE:  LINE (Messaging API) Connector for Domo Japan
LANG:   English (en_US)
ID:     4409512475927
==============================================================================

CHANGE 1  [#### **「期間」**（Predefined specific term）]  (line 75 in repo file)

  OLD (remove from repo):
               「今週」（Previous Week）

  NEW (replace with / insert from CSV):
    「今週」（Previous Week）

CHANGE 2  [#### **「期間」**（Predefined specific term）]  (line 77 in repo file)

  OLD (remove from repo):
               「今月」（This Month）

  NEW (replace with / insert from CSV):
    「今月」（This Month）

CHANGE 3  [#### **「期間」**（Predefined specific term）]  (line 79 in repo file)

  OLD (remove from repo):
               「昨日」（Yesterday）

  NEW (replace with / insert from CSV):
    「昨日」（Yesterday）

CHANGE 4  [#### **「期間」**（Predefined specific term）]  (line 81 in repo file)

  OLD (remove from repo):
               「前の週」（Previous week）

  NEW (replace with / insert from CSV):
    「前の週」（Previous week）

CHANGE 5  [#### **「期間」**（Predefined specific term）]  (line 83 in repo file)

  OLD (remove from repo):
               「前の月」（Previous Month）

  NEW (replace with / insert from CSV):
    「前の月」（Previous Month）

CHANGE 6  [## Connecting to Your LINE Official Account]  (line 97 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Domo Japan Connector for LINE(Messaging API) page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector.](/s/article/360042926274)

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Domo Japan Connector for LINE(Messaging API) page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector.](/s/article/360042926274)

CHANGE 7  [### Credentials Pane]  (line 109 in repo file)

  OLD (remove from repo):
    Once you have entered LINE Messaging API credentials, you can use the same account any time you go to create a new Domo Japan Connector for LINE(Messaging API) DataSet. You can manage connector accounts in the **Accounts** tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

  NEW (replace with / insert from CSV):
    Once you have entered LINE Messaging API credentials, you can use the same account any time you go to create a new Domo Japan Connector for LINE(Messaging API) DataSet. You can manage connector accounts in the **Accounts**tab in the Data Center. For more information about this tab, see [Managing User Accounts for Connectors](/s/article/360042926054).

CHANGE 8  [#### **3. Demographic**]  (line 372 in repo file)

  OLD (remove from repo):
    **subscriptionPeriods** ** Array**

  NEW (replace with / insert from CSV):
    **subscriptionPeriods** **Array**

==============================================================================
FILE:   s/article/4411277235351.mdx
TITLE:  Cvent REST Connector
LANG:   English (en_US)
ID:     4411277235351
==============================================================================

CHANGE 1  [## Intro]  (lines 13–42 in repo file)

  ⚠ LARGE CHANGE (37 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p></p></th>
          <th colspan="1" rowspan="1"><p></p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Use Cases</b></p></td>
          <td colspan="1" rowspan="1"><p>Use this connector to analyze and report conference metrics to learn from and optimize these large meetings.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Metrics</b></p></td>
          <td colspan="1" rowspan="1"><p>- How many people signed up for an event? - How many people actually showed up? - How many total people attended each meeting, survey, event? - What survey feedback are we getting?</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Primary Company Roles</b></p></td>
          <td colspan="1" rowspan="1"><p>It varies based on the organization, but most companies will want C-level roles to have access to the data at some point.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Average Implementation Time</b></p></td>
          <td colspan="1" rowspan="1"><p>1 hour</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p><b>Ease of Use (on a 1-to-10 scale with 1 being easiest)</b></p></td>
          <td colspan="1" rowspan="1"><p>2</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    |  |  |
    | --- | --- |
    | **Primary Use Cases** | Use this connector to analyze and report conference metrics to learn from and optimize these large meetings. |
    | **Primary Metrics** | - How many people signed up for an event? - How many people actually showed up? - How many total people attended each meeting, survey, event? - What survey feedback are we getting? |
    | **Primary Company Roles** | It varies based on the organization, but most companies will want C-level roles to have access to the data at some point. |
    | **Average Implementation Time** | 1 hour |
    | **Ease of Use (on a 1-to-10 scale with 1 being easiest)** | 2 |

CHANGE 2  [## Connect to Your Cvent Account]  (line 56 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Cvent REST Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Cvent REST Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 3  [## Credentials Pane]  (lines 62–87 in repo file)

  ⚠ LARGE CHANGE (33 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Account Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter a name for your Cvent Account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Region</p></td>
          <td colspan="1" rowspan="1"><p>Select the region for your Cvent instance.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Client ID from your Cvent app.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter the Client Secret from your Cvent app.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Account Name | Enter a name for your Cvent Account. |
    | Region | Select the region for your Cvent instance. |
    | Client ID | Enter the Client ID from your Cvent app. |
    | Client Secret | Enter the Client Secret from your Cvent app. |
    

CHANGE 4  [## Details Pane]  (lines 96–193 in repo file)

  ⚠ LARGE CHANGE (122 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the Cvent REST report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Event Survey Selection Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to retrieve data for all event surveys or selected event surveys.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Exclude Contacts</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox if you want to exclude the contact information of the attendee. For example, if an attendee record includes contact information, the Connector will exclude that contact information in your Attendees report.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Event Selection Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to retrieve data for all events or selected events.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Events</p></td>
          <td colspan="1" rowspan="1"><p>Select the events.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Event Subreports</p></td>
          <td colspan="1" rowspan="1"><p>Select the sub reports you want to expand in the data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Survey Selection Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to retrieve data for all surveys or selected surveys.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Standard Surveys</p></td>
          <td colspan="1" rowspan="1"><p>Select the standard surveys.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Event Surveys</p></td>
          <td colspan="1" rowspan="1"><p>Select the event surveys.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Skip Failing Records</p></td>
          <td colspan="1" rowspan="1"><p>Select this checkbox if you want to skip the failing records.  For example, select this checkbox in case a selected survey or event has been deleted.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Meeting Requests from Selection Criteria</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to retrieve data for all meeting requests form ids or selected meeting requests form ids.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Selection</p></td>
          <td colspan="1" rowspan="1"><p>Select the date format for your data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Single Date</p></td>
          <td colspan="1" rowspan="1"><p>Select whether the report data is for a specific date or for a relative number of days back from today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Specific Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific date using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Relative Date</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data for in the <b>Days Back</b> field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Date Range</p></td>
          <td colspan="1" rowspan="1"><p>Select the specific or relative date range.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the first date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Specific</p></td>
          <td colspan="1" rowspan="1"><p>Select the last date in your date range using the date selector.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data from (start day). Combine with <b>End Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date - Relative</p></td>
          <td colspan="1" rowspan="1"><p>Enter the number of days back that you would like to get data to (end day). Combine with <b>Start Date</b> to create a range of represented days.  For example, if you entered 10 for <b> Start Date</b> and 5 for <b> End Date</b>, the report would contain data for 10 days ago up until 5 days ago.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Time Period</p></td>
          <td colspan="1" rowspan="1"><p>Specify the time period that you would like to receive data for.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Starting Day of the Week</p></td>
          <td colspan="1" rowspan="1"><p>Select the day you would like your week to start with.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the Cvent REST report you want to run. The following reports are available:   |  |  | | --- | --- | | **Report Name** | **Description** | | Air Actual | Get attendee air actual details. | | Air Requests | Get attendee air request details. | | All Event Responses | Gets a paginated list of event survey responses for all events and surveys. | | Appointments | Retrieves a list of Appointments. | | Appointment Attendees | Retrieves information of attendees of either events or appointment events. | | Appointment Events | Get a paginated list of Appointment Events. | | Appointment Meeting Interests | Retrieves information of appointment attendees' preferences in meeting with another organization or individual. | | Attendance Duration | Retrieves attendance duration. | | Attendees | Retrieves attendees for events associated with your account. | | Attendee Activities | Retrieves information about your customers activities for an event. | | Attendee Insights | Retrieves attendee insights for events associated with your account. | | Available Appointment Times | Retrieves available appointment times associated with events. | | Booth Staff | Retrieves booth staff associated with the exhibitor for an event. | | Budget Items | Gets a paginated list of budget items for event associated to the account of the access token. | | Budget Totals | Gets a paginated list of budget totals for event associated to the account of the access token. | | Contacts | Retrieves contacts associated with your account. | | Contact Groups | Retrieves contact group. | | Conversion Rates | Retrieves the conversion rates for a currency in an account. | | E-literature Requests | Retrieves e-literature requests. | | Emails History | Returns the paginated list of sent emails. Maximum 6 months of records can be fetched. | | EMarketing Campaigns | Gets a list of eMarketing campaigns. | | Events | Retrieves information about your events. | | Event Features | Retrieves event features. | | Event Surveys | Retrieves surveys associated with an event. | | Event Role Assignments | Retrieves the event role assignments associated with an event. | | Event Survey Questions | Retrieves questions associated with event survey. | | Event Survey Responses | Retrieves responses associated with event survey. | | Exhibitor | Retrieves exhibitors for an event. | | Exhibitor Admins | Retrieves admins associated with exhibitors for an event. | | Exhibitors Answers | Gets a list of answers to exhibitor questions for a given exhibitor id. | | Exhibitors Categories | Get a paginated list of exhibitor-categories assigned to the provided exhibitor Id. | | Exhibitors Questions | Gets a list of exhibitor questions for a given event id. | | External Activities Metadata | Gets a paginated list of external attendee activities metadata. | | Hotel Requests | Get attendee hotel requests. | | Leads | Retrieves leads gathered for an event. | | Meeting Requests | Retrieves information about the Meeting Requests. | | Meeting Request Forms | Retrieves information about the Meeting Request Forms. | | Meeting Request Questions | Retrieves information about the Meeting Request Questions. | | Orders | Retrieves orders associated with events. | | Order Items | Retrieves order items associated with events. | | Process Form Submissions | Retrieves a list of process form submissions. | | Qualifying Answers | Retrieves answers provided by leads to qualifying questions. | | Qualifying Questions | Retrieves qualifying questions which are asked to identify the lead. | | Registration Packs | Retrieves registration packs associated with an exhibitor. | | Registration Paths | Gets a paginated list of Registration Paths. | | Registration Types | Gets a paginated list of Registration Types. | | Score | Retrieves engagement scores provided by attendees for events associated with your account. | | Sessions | Retrieves sessions associated with events. | | Session Attendance | Retrieves session attendance associated with your events. | | Session Enrollments | Retrieve enrollments for sessions associated with your events. | | Session Speakers | Retrieve information about speakers assigned to the provided session ID. | | Speakers | Retrieves speakers associated with your events. | | Sponsorship Levels | Retrieves sponsorship levels of exhibitors for your event. | | Standalone Survey | Retrieves the details for the standalone survey associated with your account. | | Standalone Survey Questions | Retrieves questions asked in standalone survey. | | Standalone Survey Respondents | Retrieves standalone survey respondents. | | Standard Survey Responses | Retrieves responses to the standard surveys. | | Stats | Retrieves a quick overview of attendee insight scores. | | Surveys | Gets a paginated list of event and standalone surveys associated to an account. | | Survey Questions | Gets a paginated list of event and standalone survey questions. | | Survey Respondents | Gets a paginated list of event and standalone surveys respondents. | | Survey Responses | Gets a paginated list of event and standalone survey responses. | | Transactions | Retrieves Transactions associated with events. | | Transaction Items | Retrieves transaction items associated with events. | | Users | Gets a list of users. | | Webcasts | Retrieves information about webcasts associated with your event. | |
    | Event Survey Selection Criteria | Select whether you want to retrieve data for all event surveys or selected event surveys. |
    | Exclude Contacts | Select this checkbox if you want to exclude the contact information of the attendee. For example, if an attendee record includes contact information, the Connector will exclude that contact information in your Attendees report. |
    | Event Selection Criteria | Select whether you want to retrieve data for all events or selected events. |
    | Events | Select the events. |
    | Event Subreports | Select the sub reports you want to expand in the data. |
    | Survey Selection Criteria | Select whether you want to retrieve data for all surveys or selected surveys. |
    | Standard Surveys | Select the standard surveys. |
    | Event Surveys | Select the event surveys. |
    | Skip Failing Records | Select this checkbox if you want to skip the failing records.  For example, select this checkbox in case a selected survey or event has been deleted. |
    | Meeting Requests from Selection Criteria | Select whether you want to retrieve data for all meeting requests form ids or selected meeting requests form ids. |
    | Date Selection | Select the date format for your data. |
    | Single Date | Select whether the report data is for a specific date or for a relative number of days back from today. |
    | Specific Date | Select the specific date using the date selector. |
    | Relative Date | Enter the number of days back that you would like to get data for in the **Days Back** field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. |
    | Date Range | Select the specific or relative date range. |
    | Start Date - Specific | Select the first date in your date range using the date selector. |
    | End Date - Specific | Select the last date in your date range using the date selector. |
    | Start Date - Relative | Enter the number of days back that you would like to get data from (start day). Combine with ********End Date******** to create a range of represented days.  For example, if you entered 10 for ********Start Date******** and 5 for ********End Date********, the report would contain data for 10 days ago up until 5 days ago. |
    | End Date - Relative | Enter the number of days back that you would like to get data to (end day). Combine with ********Start Date********to create a range of represented days.  For example, if you entered 10 for ********Start Date******** and 5 for ********End Date********, the report would contain data for 10 days ago up until 5 days ago. |
    | Time Period | Specify the time period that you would like to receive data for. |
    | Starting Day of the Week | Select the day you would like your week to start with. |

CHANGE 5  [## FAQs]  (line 205 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### What endpoint is the base URL for this connector?

CHANGE 6  [## FAQs]  (line 207 in repo file)

  OLD (remove from repo):
    <Accordion title="What endpoint is the base URL for this connector?">

CHANGE 7  [## FAQs]  (line 209 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 8  [## FAQs]  (lines 211–620 in repo file)

  ⚠ LARGE CHANGE (411 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <Accordion title="Which endpoint does each report call in this connector?">
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>S.No</p></th>
          <th colspan="1" rowspan="1"><p>Report Name</p></th>
          <th colspan="1" rowspan="1"><p>Endpoint URL</p></th>
          <th colspan="1" rowspan="1"><p>Required Scope</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>1</p></td>
          <td colspan="1" rowspan="1"><p>Air Actual</p></td>
          <td colspan="1" rowspan="1"><p>/events/%s/event-travel/air-actuals</p></td>
          <td colspan="1" rowspan="1"><p>event/air-request:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>2</p></td>
          <td colspan="1" rowspan="1"><p>Air Requests</p></td>
          <td colspan="1" rowspan="1"><p>/events/%s/event-travel/air-requests</p></td>
          <td colspan="1" rowspan="1"><p>event/air-request:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>3</p></td>
          <td colspan="1" rowspan="1"><p>All Event Responses</p></td>
          <td colspan="1" rowspan="1"><p>events/surveys/responses</p></td>
          <td colspan="1" rowspan="1"><p>survey/survey-responses:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>4</p></td>
          <td colspan="1" rowspan="1"><p>Appointments</p></td>
          <td colspan="1" rowspan="1"><p>/appointment</p></td>
          <td colspan="1" rowspan="1"><p>appointments/appointments:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>5</p></td>
          <td colspan="1" rowspan="1"><p>Appointment Attendees</p></td>
          <td colspan="1" rowspan="1"><p>/appointment-attendees</p></td>
          <td colspan="1" rowspan="1"><p>appointments/appointment-attendees:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>6</p></td>
          <td colspan="1" rowspan="1"><p>Appointment Events</p></td>
          <td colspan="1" rowspan="1"><p>/appointment-events</p></td>
          <td colspan="1" rowspan="1"><p>appointments/appointment-events:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>7</p></td>
          <td colspan="1" rowspan="1"><p>Appointment Meeting Interests</p></td>
          <td colspan="1" rowspan="1"><p>/appointment-meeting-interests</p></td>
          <td colspan="1" rowspan="1"><p>appointments/appointment-attendees:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>8</p></td>
          <td colspan="1" rowspan="1"><p>Attendance Duration</p></td>
          <td colspan="1" rowspan="1"><p>/attendance-durations</p></td>
          <td colspan="1" rowspan="1"><p>event/attendance-durations:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>9</p></td>
          <td colspan="1" rowspan="1"><p>Attendees</p></td>
          <td colspan="1" rowspan="1"><p>/attendees</p></td>
          <td colspan="1" rowspan="1"><p>event/attendees:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>10</p></td>
          <td colspan="1" rowspan="1"><p>Attendee Activities</p></td>
          <td colspan="1" rowspan="1"><p>/attendees/activities</p></td>
          <td colspan="1" rowspan="1"><p>event/attendee-activities:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>11</p></td>
          <td colspan="1" rowspan="1"><p>Attendee Insights</p></td>
          <td colspan="1" rowspan="1"><p>/attendee-insights</p></td>
          <td colspan="1" rowspan="1"><p>attendee-insights/attendee-insights:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>12</p></td>
          <td colspan="1" rowspan="1"><p>Available Appointment Times</p></td>
          <td colspan="1" rowspan="1"><p>/appointment-events/{id}/appointment-available-times</p></td>
          <td colspan="1" rowspan="1"><p>appointments/available-times:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>13</p></td>
          <td colspan="1" rowspan="1"><p>Booth Staff</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitors/{exhibitorId}/booth-staff</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/booth-staff:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>14</p></td>
          <td colspan="1" rowspan="1"><p>Budget Items</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/budget-items</p></td>
          <td colspan="1" rowspan="1"><p>budget/budget-items:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>15</p></td>
          <td colspan="1" rowspan="1"><p>Budget Totals</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/budget-totals</p></td>
          <td colspan="1" rowspan="1"><p>budget/budget-totals:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>16</p></td>
          <td colspan="1" rowspan="1"><p>Contacts</p></td>
          <td colspan="1" rowspan="1"><p>/contacts</p></td>
          <td colspan="1" rowspan="1"><p>event/contacts:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>17</p></td>
          <td colspan="1" rowspan="1"><p>Contact Groups</p></td>
          <td colspan="1" rowspan="1"><p>/contact-groups</p></td>
          <td colspan="1" rowspan="1"><p>event/contact-groups:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>18</p></td>
          <td colspan="1" rowspan="1"><p>Conversion Rates</p></td>
          <td colspan="1" rowspan="1"><p>currencies/{currency code}/conversion-rates</p></td>
          <td colspan="1" rowspan="1"><p>budget/currency-conversion-rate:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>19</p></td>
          <td colspan="1" rowspan="1"><p>E-literature Requests</p></td>
          <td colspan="1" rowspan="1"><p>/eliterature-requests</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/eliterature-requests:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>20</p></td>
          <td colspan="1" rowspan="1"><p>Emails History</p></td>
          <td colspan="1" rowspan="1"><p>/emails</p></td>
          <td colspan="1" rowspan="1"><p>email/emails:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>21</p></td>
          <td colspan="1" rowspan="1"><p>EMarketing Campaigns</p></td>
          <td colspan="1" rowspan="1"><p>/campaigns</p></td>
          <td colspan="1" rowspan="1"><p>eMarketing/campaigns:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>22</p></td>
          <td colspan="1" rowspan="1"><p>Events</p></td>
          <td colspan="1" rowspan="1"><p>/events</p></td>
          <td colspan="1" rowspan="1"><p>event/events:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>23</p></td>
          <td colspan="1" rowspan="1"><p>Event Features</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/features</p></td>
          <td colspan="1" rowspan="1"><p>event/event-features:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>24</p></td>
          <td colspan="1" rowspan="1"><p>Event Surveys</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/surveys</p></td>
          <td colspan="1" rowspan="1"><p>survey/surveys:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>25</p></td>
          <td colspan="1" rowspan="1"><p>Event Role Assignments</p></td>
          <td colspan="1" rowspan="1"><p>event-roles/assignments</p></td>
          <td colspan="1" rowspan="1"><p>event/role-assignments:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>26</p></td>
          <td colspan="1" rowspan="1"><p>Event Survey Questions</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/surveys/ {surveyId/questions</p></td>
          <td colspan="1" rowspan="1"><p>survey/survey-questions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>27</p></td>
          <td colspan="1" rowspan="1"><p>Event Survey Responses</p></td>
          <td colspan="1" rowspan="1"><p>/events/{eventId}/surveys/{surveyId}/responses</p></td>
          <td colspan="1" rowspan="1"><p>survey/survey-responses:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>28</p></td>
          <td colspan="1" rowspan="1"><p>Exhibitor</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitors</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/exhibitors:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>29</p></td>
          <td colspan="1" rowspan="1"><p>Exhibitor Admins</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}exhibitors/{exhibitorId}/admins</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/exhibitor-admins:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>30</p></td>
          <td colspan="1" rowspan="1"><p>Exhibitors Answers</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitors/{exhibitorId}/exhibitor-answers</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/exhibitor-answers:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>31</p></td>
          <td colspan="1" rowspan="1"><p>Exhibitors Categories</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitor-categories</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/exhibitor-categories:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>32</p></td>
          <td colspan="1" rowspan="1"><p>Exhibitors Questions</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitor-questions</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/exhibitor-questions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>33</p></td>
          <td colspan="1" rowspan="1"><p>External Activities Metadata</p></td>
          <td colspan="1" rowspan="1"><p>attendees/activities/external/metadata</p></td>
          <td colspan="1" rowspan="1"><p>event/attendee-activities-metadata:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>34</p></td>
          <td colspan="1" rowspan="1"><p>Hotel Requests</p></td>
          <td colspan="1" rowspan="1"><p>/events/%s/event-travel/hotel-requests</p></td>
          <td colspan="1" rowspan="1"><p>event/hotel-request:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>35</p></td>
          <td colspan="1" rowspan="1"><p>Leads</p></td>
          <td colspan="1" rowspan="1"><p>/leads</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/leads:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>36</p></td>
          <td colspan="1" rowspan="1"><p>Meeting Requests</p></td>
          <td colspan="1" rowspan="1"><p>/meeting-request-forms/{id}/requests</p></td>
          <td colspan="1" rowspan="1"><p>event/meeting-requests:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>37</p></td>
          <td colspan="1" rowspan="1"><p>Meeting Request Forms</p></td>
          <td colspan="1" rowspan="1"><p>/meeting-request-forms</p></td>
          <td colspan="1" rowspan="1"><p>event/meeting-request-forms:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>38</p></td>
          <td colspan="1" rowspan="1"><p>Meeting Request Questions</p></td>
          <td colspan="1" rowspan="1"><p>/meeting-request-forms/{id}/requests</p></td>
          <td colspan="1" rowspan="1"><p>event/meeting-requests:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>39</p></td>
          <td colspan="1" rowspan="1"><p>Orders</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/orders</p></td>
          <td colspan="1" rowspan="1"><p>event/orders:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>40</p></td>
          <td colspan="1" rowspan="1"><p>Order Items</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/orders/items</p></td>
          <td colspan="1" rowspan="1"><p>event/orders:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>41</p></td>
          <td colspan="1" rowspan="1"><p>Process Form Submissions</p></td>
          <td colspan="1" rowspan="1"><p>/process-forms/submissions</p></td>
          <td colspan="1" rowspan="1"><p>event/process-form-submissions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>42</p></td>
          <td colspan="1" rowspan="1"><p>Qualifying Answers</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitors/{exhibitorId}/leads/{leadId}/answers</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/lead-qualification-answers:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>43</p></td>
          <td colspan="1" rowspan="1"><p>Qualifying Questions</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitors/{exhibitorId}/questions</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/lead-qualification-questions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>44</p></td>
          <td colspan="1" rowspan="1"><p>Registration Packs</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/exhibitors/{exhibitorId}/registration-packs</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/registration-packs:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>45</p></td>
          <td colspan="1" rowspan="1"><p>Registration Paths</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/registration-paths</p></td>
          <td colspan="1" rowspan="1"><p>event/registration-paths:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>46</p></td>
          <td colspan="1" rowspan="1"><p>Registration Types</p></td>
          <td colspan="1" rowspan="1"><p>/events/%s/registration-types</p></td>
          <td colspan="1" rowspan="1"><p>event/registration-types:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>47</p></td>
          <td colspan="1" rowspan="1"><p>Score</p></td>
          <td colspan="1" rowspan="1"><p>/attendee-insights/{id}/scores</p></td>
          <td colspan="1" rowspan="1"><p>attendee-insights/scores:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>48</p></td>
          <td colspan="1" rowspan="1"><p>Sessions</p></td>
          <td colspan="1" rowspan="1"><p>/sessions</p></td>
          <td colspan="1" rowspan="1"><p>event/sessions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>49</p></td>
          <td colspan="1" rowspan="1"><p>Session Attendance</p></td>
          <td colspan="1" rowspan="1"><p>/sessions/attendance</p></td>
          <td colspan="1" rowspan="1"><p>event/session-attendance:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>50</p></td>
          <td colspan="1" rowspan="1"><p>Session Enrollments</p></td>
          <td colspan="1" rowspan="1"><p>/sessions/enrollment</p></td>
          <td colspan="1" rowspan="1"><p>event/session-enrollment:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>51</p></td>
          <td colspan="1" rowspan="1"><p>Session Speakers</p></td>
          <td colspan="1" rowspan="1"><p>/sessions/{id}/speakers</p></td>
          <td colspan="1" rowspan="1"><p>event/speakers:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>52</p></td>
          <td colspan="1" rowspan="1"><p>Speakers</p></td>
          <td colspan="1" rowspan="1"><p>/speakers</p></td>
          <td colspan="1" rowspan="1"><p>event/speakers:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>53</p></td>
          <td colspan="1" rowspan="1"><p>Sponsorship Levels</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/sponsorship-levels</p></td>
          <td colspan="1" rowspan="1"><p>exhibitor/sponsorship-levels:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>54</p></td>
          <td colspan="1" rowspan="1"><p>Standalone Survey</p></td>
          <td colspan="1" rowspan="1"><p>/standard-surveys</p></td>
          <td colspan="1" rowspan="1"><p>survey/standard-surveys:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>55</p></td>
          <td colspan="1" rowspan="1"><p>Standalone Survey Questions</p></td>
          <td colspan="1" rowspan="1"><p>/standard-surveys/{surveyId}/questions</p></td>
          <td colspan="1" rowspan="1"><p>survey/standard-survey-questions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>56</p></td>
          <td colspan="1" rowspan="1"><p>Standalone Survey Respondents</p></td>
          <td colspan="1" rowspan="1"><p>/standard-surveys/{surveyId}/respondents</p></td>
          <td colspan="1" rowspan="1"><p>survey/standard-survey-respondents:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>57</p></td>
          <td colspan="1" rowspan="1"><p>Standard Survey Responses</p></td>
          <td colspan="1" rowspan="1"><p>/standard-surveys/responses</p></td>
          <td colspan="1" rowspan="1"><p>survey/standard-survey-responses:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>58</p></td>
          <td colspan="1" rowspan="1"><p>Stats</p></td>
          <td colspan="1" rowspan="1"><p>/attendee-insights/{id}/stats</p></td>
          <td colspan="1" rowspan="1"><p>attendee-insights/stats:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>59</p></td>
          <td colspan="1" rowspan="1"><p>Surveys</p></td>
          <td colspan="1" rowspan="1"><p>/surveys</p></td>
          <td colspan="1" rowspan="1"><p>survey/surveys:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>60</p></td>
          <td colspan="1" rowspan="1"><p>Survey Questions</p></td>
          <td colspan="1" rowspan="1"><p>/questions</p></td>
          <td colspan="1" rowspan="1"><p>survey/questions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>61</p></td>
          <td colspan="1" rowspan="1"><p>Survey Respondents</p></td>
          <td colspan="1" rowspan="1"><p>/respondents</p></td>
          <td colspan="1" rowspan="1"><p>survey/respondents:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>62</p></td>
          <td colspan="1" rowspan="1"><p>Survey Responses</p></td>
          <td colspan="1" rowspan="1"><p>/responses</p></td>
          <td colspan="1" rowspan="1"><p>survey/responses:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>63</p></td>
          <td colspan="1" rowspan="1"><p>Transactions</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/transactions</p></td>
          <td colspan="1" rowspan="1"><p>event/transactions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>64</p></td>
          <td colspan="1" rowspan="1"><p>Transaction Items</p></td>
          <td colspan="1" rowspan="1"><p>/events/{id}/transactions/items</p></td>
          <td colspan="1" rowspan="1"><p>event/transactions:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>65</p></td>
          <td colspan="1" rowspan="1"><p>Users</p></td>
          <td colspan="1" rowspan="1"><p>/scim/v2/Users</p></td>
          <td colspan="1" rowspan="1"><p>account/users:read</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>66</p></td>
          <td colspan="1" rowspan="1"><p>Webcasts</p></td>
          <td colspan="1" rowspan="1"><p>/webcasts</p></td>
          <td colspan="1" rowspan="1"><p>event/webcasts:read</p></td>
        </tr>
      </tbody>
    </table>
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Which endpoint does each report call in this connector?

CHANGE 9  [## FAQs]  (line 622 in repo file)

  ⚠ LARGE CHANGE (72 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    | S.No | Report Name | Endpoint URL | Required Scope |
    | --- | --- | --- | --- |
    | 1 | Air Actual | /events/%s/event-travel/air-actuals | event/air-request:read |
    | 2 | Air Requests | /events/%s/event-travel/air-requests | event/air-request:read |
    | 3 | All Event Responses | events/surveys/responses | survey/survey-responses:read |
    | 4 | Appointments | /appointment | appointments/appointments:read |
    | 5 | Appointment Attendees | /appointment-attendees | appointments/appointment-attendees:read |
    | 6 | Appointment Events | /appointment-events | appointments/appointment-events:read |
    | 7 | Appointment Meeting Interests | /appointment-meeting-interests | appointments/appointment-attendees:read |
    | 8 | Attendance Duration | /attendance-durations | event/attendance-durations:read |
    | 9 | Attendees | /attendees | event/attendees:read |
    | 10 | Attendee Activities | /attendees/activities | event/attendee-activities:read |
    | 11 | Attendee Insights | /attendee-insights | attendee-insights/attendee-insights:read |
    | 12 | Available Appointment Times | /appointment-events/{id}/appointment-available-times | appointments/available-times:read |
    | 13 | Booth Staff | /events/{id}/exhibitors/{exhibitorId}/booth-staff | exhibitor/booth-staff:read |
    | 14 | Budget Items | /events/{id}/budget-items | budget/budget-items:read |
    | 15 | Budget Totals | /events/{id}/budget-totals | budget/budget-totals:read |
    | 16 | Contacts | /contacts | event/contacts:read |
    | 17 | Contact Groups | /contact-groups | event/contact-groups:read |
    | 18 | Conversion Rates | currencies/{currency code}/conversion-rates | budget/currency-conversion-rate:read |
    | 19 | E-literature Requests | /eliterature-requests | exhibitor/eliterature-requests:read |
    | 20 | Emails History | /emails | email/emails:read |
    | 21 | EMarketing Campaigns | /campaigns | eMarketing/campaigns:read |
    | 22 | Events | /events | event/events:read |
    | 23 | Event Features | /events/{id}/features | event/event-features:read |
    | 24 | Event Surveys | /events/{id}/surveys | survey/surveys:read |
    | 25 | Event Role Assignments | event-roles/assignments | event/role-assignments:read |
    | 26 | Event Survey Questions | /events/{id}/surveys/ {surveyId/questions|survey/survey-questions:read| |Event Survey Responses|/events/ {id} /surveys/{surveyId}responses | survey/survey-responses:read |
    | 27 | Event Survey Responses | /events/{eventId}/surveys/{surveyId}/responses | survey/survey-responses:read |
    | 28 | Exhibitor | /events/{id}/exhibitors | exhibitor/exhibitors:read |
    | 29 | Exhibitor Admins | /events/{id}exhibitors/{exhibitorId}/admins | exhibitor/exhibitor-admins:read |
    | 30 | Exhibitors Answers | /events/{id}/exhibitors/{exhibitorId}/exhibitor-answers | exhibitor/exhibitor-answers:read |
    | 31 | Exhibitors Categories | /events/{id}/exhibitor-categories | exhibitor/exhibitor-categories:read |
    | 32 | Exhibitors Questions | /events/{id}/exhibitor-questions | exhibitor/exhibitor-questions:read |
    | 33 | External Activities Metadata | attendees/activities/external/metadata | event/attendee-activities-metadata:read |
    | 34 | Hotel Requests | /events/%s/event-travel/hotel-requests | event/hotel-request:read |
    | 35 | Leads | /leads | exhibitor/leads:read |
    | 36 | Meeting Requests | /meeting-request-forms/{id}/requests | event/meeting-requests:read |
    | 37 | Meeting Request Forms | /meeting-request-forms | event/meeting-request-forms:read |
    | 38 | Meeting Request Questions | /meeting-request-forms/{id}/requests | event/meeting-requests:read |
    | 39 | Orders | /events/{id}/orders | event/orders:read |
    | 40 | Order Items | /events/{id}/orders/items | event/orders:read |
    | 41 | Process Form Submissions | /process-forms/submissions | event/process-form-submissions:read |
    | 42 | Qualifying Answers | /events/{id}/exhibitors/{exhibitorId}/leads/{leadId}/answers | exhibitor/lead-qualification-answers:read |
    | 43 | Qualifying Questions | /events/{id}/exhibitors/{exhibitorId}/questions | exhibitor/lead-qualification-questions:read |
    | 44 | Registration Packs | /events/{id}/exhibitors/{exhibitorId}/registration-packs | exhibitor/registration-packs:read |
    | 45 | Registration Paths | /events/{id}/registration-paths | event/registration-paths:read |
    | 46 | Registration Types | /events/%s/registration-types | event/registration-types:read |
    | 47 | Score | /attendee-insights/{id}/scores | attendee-insights/scores:read |
    | 48 | Sessions | /sessions | event/sessions:read |
    | 49 | Session Attendance | /sessions/attendance | event/session-attendance:read |
    | 50 | Session Enrollments | /sessions/enrollment | event/session-enrollment:read |
    | 51 | Session Speakers | /sessions/{id}/speakers | event/speakers:read |
    | 52 | Speakers | /speakers | event/speakers:read |
    | 53 | Sponsorship Levels | /events/{id}/sponsorship-levels | exhibitor/sponsorship-levels:read |
    | 54 | Standalone Survey | /standard-surveys | survey/standard-surveys:read |
    | 55 | Standalone Survey Questions | /standard-surveys/{surveyId}/questions | survey/standard-survey-questions:read |
    | 56 | Standalone Survey Respondents | /standard-surveys/{surveyId}/respondents | survey/standard-survey-respondents:read |
    | 57 | Standard Survey Responses | /standard-surveys/responses | survey/standard-survey-responses:read |
    | 58 | Stats | /attendee-insights/{id}/stats | attendee-insights/stats:read |
    | 59 | Surveys | /surveys | survey/surveys:read |
    | 60 | Survey Questions | /questions | survey/questions:read |
    | 61 | Survey Respondents | /respondents | survey/respondents:read |
    | 62 | Survey Responses | /responses | survey/responses:read |
    | 63 | Transactions | /events/{id}/transactions | event/transactions:read |
    | 64 | Transaction Items | /events/{id}/transactions/items | event/transactions:read |
    | 65 | Users | /scim/v2/Users | account/users:read |
    | 66 | Webcasts | /webcasts | event/webcasts:read |
    
    #### What kind of credentials do I need to power up this connector?
    

CHANGE 10  [## FAQs]  (line 624 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 11  [## FAQs]  (line 626 in repo file)

  OLD (remove from repo):
    <Accordion title="Can I use the same account to create multiple datasets?">

  NEW (replace with / insert from CSV):
    #### Can I use the same account to create multiple datasets?
    

CHANGE 12  [## FAQs]  (line 628 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 13  [## FAQs]  (line 630 in repo file)

  OLD (remove from repo):
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?
    

CHANGE 14  [## FAQs]  (line 632 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 15  [## FAQs]  (line 634 in repo file)

  OLD (remove from repo):
    <Accordion title="Do I need to whitelist any IP addresses?">

  NEW (replace with / insert from CSV):
    #### Do I need to whitelist any IP addresses?
    

CHANGE 16  [## FAQs]  (lines 637–640 in repo file)

  OLD (remove from repo):
    Please refer to this link for detailed explanation: [/s/article/360043630093](/s/article/360043630093).
    </Accordion>
    
    </AccordionGroup>

  NEW (replace with / insert from CSV):
    Please refer to this link for detailed explanation: [https://domo-support.domo.com/s/article/360043630093?language=en\_US](/s/article/360043630093).

==============================================================================
FILE:   s/article/4423536836631.mdx
TITLE:  Tableau Connector
LANG:   English (en_US)
ID:     4423536836631
==============================================================================

CHANGE 1  [## Connecting to Your Tableau Account]  (line 45 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the Credentials and Details panes in the Tableau Connector page. The components of the other panes in this page, **Scheduling** and ** Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the Credentials and Details panes in the Tableau Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Connector](/s/article/360042926274).

CHANGE 2  [### Credentials Pane]  (lines 51–76 in repo file)

  ⚠ LARGE CHANGE (32 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domain</p></td>
          <td colspan="1" rowspan="1"><p>Enter the domain. It is the sub-path of the site's full URL. When you sign into Tableau Server or Tableau Online manually, the domain is the value that appears after <b>https://</b> in the browser address bar. Example: https://<b> Server</b>/#/site/Team/projects Here, the domain is Server.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Username</p></td>
          <td colspan="1" rowspan="1"><p>Enter the username associated with your Tableau account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Password</p></td>
          <td colspan="1" rowspan="1"><p>Enter the password associated with your Tableau account.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Site Attribute</p></td>
          <td colspan="1" rowspan="1"><p>Enter the site attribute. It is the sub-path of the site's full URL. When you sign into Tableau Server or Tableau Online manually, the site attribute is the value that appears after <b>/site/</b> in the browser address bar. Example: <a href="http://Server/#/site/Team/projects">http://Server/#/site/</a><b><a href="http://Server/#/site/Team/projects">Team</a></b><a href="http://Server/#/site/Team/projects">/projects</a>  Here, the site attribute is Team. To specify the default site, enter <b> DEFAULT</b> here.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domain | Enter the domain. It is the sub-path of the site's full URL. When you sign into Tableau Server or Tableau Online manually, the domain is the value that appears after **https://** in the browser address bar. Example: https://**Server**/#/site/Team/projects Here, the domain is Server. |
    | Username | Enter the username associated with your Tableau account. |
    | Password | Enter the password associated with your Tableau account. |
    | Site Attribute | Enter the site attribute. It is the sub-path of the site's full URL. When you sign into Tableau Server or Tableau Online manually, the site attribute is the value that appears after **/site/** in the browser address bar. Example: [http://Server/#/site/](http://Server/#/site/Team/projects)**[Team](http://Server/#/site/Team/projects)**[/projects](http://Server/#/site/Team/projects)  Here, the site attribute is Team. To specify the default site, enter **DEFAULT** here. |

CHANGE 3  [### Details Pane]  (lines 84–105 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p></p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Workbook ID</p></td>
          <td colspan="1" rowspan="1"><p>Select the workbook ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>View ID</p></td>
          <td colspan="1" rowspan="1"><p>Select the view ID.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | | Report | Description | | --- | --- | | View Data | Returns a specified view rendered as data in comma-separated-value (CSV) format. | |
    | Workbook ID | Select the workbook ID. |
    | View ID | Select the view ID. |

CHANGE 4  [## FAQs]  (line 113 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### What endpoint is the base URL for this connector?

CHANGE 5  [## FAQs]  (line 115 in repo file)

  OLD (remove from repo):
    <Accordion title="What endpoint is the base URL for this connector?">

CHANGE 6  [## FAQs]  (line 117 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 7  [## FAQs]  (lines 119–134 in repo file)

  OLD (remove from repo):
    <Accordion title="Which endpoint(s) does each report call in this connector?">
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Report Name</p></th>
          <th colspan="1" rowspan="1"><p>Endpoint URL</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>View Data</p></td>
          <td colspan="1" rowspan="1"><p>/api/3.14/sites/{siteId}/views/{viewId}/data</p></td>
        </tr>
      </tbody>
    </table>
    </Accordion>

  NEW (replace with / insert from CSV):
    #### Which endpoint(s) does each report call in this connector?

CHANGE 8  [## FAQs]  (line 136 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

  NEW (replace with / insert from CSV):
    | Report Name | Endpoint URL |
    | --- | --- |
    | View Data | /api/3.14/sites/{siteId}/views/{viewId}/data |
    
    #### What kind of credentials do I need to power up this connector?
    

CHANGE 9  [## FAQs]  (line 138 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 10  [## FAQs]  (line 140 in repo file)

  OLD (remove from repo):
    <Accordion title="Where can I find the site attribute?">

  NEW (replace with / insert from CSV):
    #### Where can I find the site attribute?
    

CHANGE 11  [## FAQs]  (line 144 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 12  [## FAQs]  (line 146 in repo file)

  OLD (remove from repo):
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?
    

CHANGE 13  [## FAQs]  (line 148 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 14  [## FAQs]  (line 150 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 15  [## FAQs]  (line 152 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 16  [## FAQs]  (line 154 in repo file)

  OLD (remove from repo):
    </AccordionGroup>

==============================================================================
FILE:   s/article/5366610360983.mdx
TITLE:  Sansan Connector
LANG:   English (en_US)
ID:     5366610360983
==============================================================================

CHANGE 1  [## Connecting to Your Sansan Account]  (line 23 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Sansan Connector page. The components of the other panes in this page, Scheduling and Name & Describe Your DataSet, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](https://domohelp.domo.com/hc/en-us/articles/360042926274-Adding-a-DataSet-Using-a-Data-Connector target=_blank).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Sansan Connector page. The components of the other panes in this page, Scheduling and Name & Describe Your DataSet, are universal across most connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](https://domohelp.domo.com/hc/en-us/articles/360042926274-Adding-a-DataSet-Using-a-Data-Connector target=_blank).

CHANGE 2  [## Credentials Pane]  (lines 29–42 in repo file)

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>API Key</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Sansan API Key.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | API Key | Enter your Sansan API Key. |

CHANGE 3  [## Details Pane]  (lines 52–93 in repo file)

  ⚠ LARGE CHANGE (52 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Report</p></td>
          <td colspan="1" rowspan="1"><p>Select the report you want to run. The following reports are available:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Updated</p></td>
          <td colspan="1" rowspan="1"><p>In the <b>Date selection</b>, select whether to use a single day or a date range when filtering the results by item update date.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Updated - Date</p></td>
          <td colspan="1" rowspan="1"><p>In the <b>Date Type</b>, select from the following whether the report data is for a specific date or for a relative number of days back from today:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Updated - Start Date</p></td>
          <td colspan="1" rowspan="1"><p>In the <b>Date Type</b>, select the following:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Updated - End Date</p></td>
          <td colspan="1" rowspan="1"><p>In the <b>Date Type</b>, select the following:</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Updated - Time period</p></td>
          <td colspan="1" rowspan="1"><p>Select the time period for which you would like to receive data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Contact type</p></td>
          <td colspan="1" rowspan="1"><p>Select the contact type.    Following contact types are available:   - Meeting - Call - Email - BizCard Exchange - Online Meeting</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Category type</p></td>
          <td colspan="1" rowspan="1"><p>Select whether you want to use filters by <b>Category</b>.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Report | Select the report you want to run. The following reports are available:   | Business Cards | Returns the list of business cards accessible in your account. | | Contact | Returns the list of report by contact type. | |
    | Updated | In the **Date selection**, select whether to use a single day or a date range when filtering the results by item update date.   | Single Date | Select the specific or relative single date. | | Date range | Select the specific or relative date range. | | Time period | Select the time period from **Time period** dropdown. | |
    | Updated - Date | In the **Date Type**, select from the following whether the report data is for a specific date or for a relative number of days back from today:   | Relative | Enter the number of days back that you would like to get data for in the **Days Back** field. Specify either today or 0, yesterday or 1, or today-7 or 7 to get data for 7 days into the past. | | Specific | Select the specific date using the date selector in the **Date** field. | |
    | Updated - Start Date | In the **Date Type**, select the following:   | Relative | Enter the number of days back that you would like to get data from (start day). Combine with the **End Date** to create a range of represented days.  For example, if you entered 10 for **Start Date** and 5 for **End Date**, the report would contain data for 10 days ago up until 5 days ago. | | Specific | Select the first date in your date range using the date selector. | |
    | Updated - End Date | In the **Date Type**, select the following:   | Relative | Enter the number of days back that you would like to get data to (end day). Combine with the  **Start Date** to create a range of represented days.  For example, if you entered 10 for **Start Date** and 5 for **End Date**, the report would contain data for 10 days ago up until 5 days ago. | | Specific | Select the last date in your date range using the date selector. | |
    | Updated - Time period | Select the time period for which you would like to receive data. |
    | Contact type | Select the contact type.    Following contact types are available:   - Meeting - Call - Email - BizCard Exchange - Online Meeting |
    | Category type | Select whether you want to use filters by **Category**. |

==============================================================================
FILE:   s/article/5563069185815.mdx
TITLE:  Splunk Writeback Connector
LANG:   English (en_US)
ID:     5563069185815
==============================================================================

CHANGE 1  [## Intro]  (lines 12–13 in repo file)

  OLD (remove from repo):
    
    ---

CHANGE 2  [## Prerequisites]  (line 27 in repo file)

  OLD (remove from repo):
    2. In the top right corner under **My Account** click ** New Client**.

  NEW (replace with / insert from CSV):
    2. In the top right corner under **My Account** click **New Client**.

CHANGE 3  [## Prerequisites]  (line 29 in repo file)

  OLD (remove from repo):
    4. Provide the application scope by selecting the checkboxes for **Data** and ** User**.

  NEW (replace with / insert from CSV):
    4. Provide the application scope by selecting the checkboxes for **Data** and **User**.

CHANGE 4  [## Configuring the Connection]  (line 36 in repo file)

  OLD (remove from repo):
    This section enumerates the options in the **Credentials** and ** Details** panes in the Splunk Writeback Connector page. The components of the other panes in this page, ** Scheduling** and ** Name & Describe Your DataSet**, are universal across most Connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

  NEW (replace with / insert from CSV):
    This section enumerates the options in the **Credentials** and **Details** panes in the Splunk Writeback Connector page. The components of the other panes in this page, **Scheduling** and **Name & Describe Your DataSet**, are universal across most Connector types and are discussed in greater length in [Adding a DataSet Using a Data Connector](/s/article/360042926274).

CHANGE 5  [### Credentials Pane]  (lines 42–71 in repo file)

  ⚠ LARGE CHANGE (38 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Field</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client ID</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client ID.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Domo Client Secret</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Domo client secret.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Splunk Host Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter your host name. <b>Note</b>- Do not include <b> https://</b> or <b> http://</b>.  Example: "subdomain.domain.info"</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Port</p></td>
          <td colspan="1" rowspan="1"><p>Enter the port number.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>HTTP Event Collector Token</p></td>
          <td colspan="1" rowspan="1"><p>Enter the event collector token.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Field | Description |
    | --- | --- |
    | Domo Client ID | Enter your Domo client ID. |
    | Domo Client Secret | Enter your Domo client secret. |
    | Splunk Host Name | Enter your host name. **Note**- Do not include **https://** or **http://**.  Example: "subdomain.domain.info" |
    | Port | Enter the port number. |
    | HTTP Event Collector Token | Enter the event collector token. |
    

CHANGE 6  [### Details Pane]  (lines 78–123 in repo file)

  ⚠ LARGE CHANGE (57 lines).  Consider re-running
    update_kb_articles.py for this article instead.

  OLD (remove from repo):
    <table border="1" cellpadding="1" cellspacing="1">
      <thead>
        <tr>
          <th colspan="1" rowspan="1"><p>Menu</p></th>
          <th colspan="1" rowspan="1"><p>Description</p></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="1" rowspan="1"><p>Splunk Index Name</p></td>
          <td colspan="1" rowspan="1"><p>Enter your Splunk instance index name.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Source</p></td>
          <td colspan="1" rowspan="1"><p>Enter the domo source name.   <b>Example:</b> customername.domo.com</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Agg</p></td>
          <td colspan="1" rowspan="1"><p>Enter the aggregation value.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the Start date selection type.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the start date from which you want to start fetching data.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>Start Date Offset</p></td>
          <td colspan="1" rowspan="1"><p>Select the number of days back from which you want to start fetching data. For example, Enter 0 for today.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date Type</p></td>
          <td colspan="1" rowspan="1"><p>Select the End date selection type.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date</p></td>
          <td colspan="1" rowspan="1"><p>Select the End Date.</p></td>
        </tr>
        <tr>
          <td colspan="1" rowspan="1"><p>End Date Offset</p></td>
          <td colspan="1" rowspan="1"><p>Select the number of days back till which you want to fetch data. For Example, enter 0 for today.</p></td>
        </tr>
      </tbody>
    </table>

  NEW (replace with / insert from CSV):
    | Menu | Description |
    | --- | --- |
    | Splunk Index Name | Enter your Splunk instance index name. |
    | Source | Enter the domo source name.   **Example:** customername.domo.com |
    | Agg | Enter the aggregation value. |
    | Start Date Type | Select the Start date selection type. |
    | Start Date | Select the start date from which you want to start fetching data. |
    | Start Date Offset | Select the number of days back from which you want to start fetching data. For example, Enter 0 for today. |
    | End Date Type | Select the End date selection type. |
    | End Date | Select the End Date. |
    | End Date Offset | Select the number of days back till which you want to fetch data. For Example, enter 0 for today. |

CHANGE 7  [## FAQs]  (line 131 in repo file)

  OLD (remove from repo):
    <AccordionGroup>

  NEW (replace with / insert from CSV):
    #### What kind of credentials do I need to power up this connector?

CHANGE 8  [## FAQs]  (line 133 in repo file)

  OLD (remove from repo):
    <Accordion title="What kind of credentials do I need to power up this connector?">

CHANGE 9  [## FAQs]  (line 135 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 10  [## FAQs]  (line 137 in repo file)

  OLD (remove from repo):
    <Accordion title="How often can the data be updated?">

  NEW (replace with / insert from CSV):
    #### How often can the data be updated?
    

CHANGE 11  [## FAQs]  (line 139 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 12  [## FAQs]  (line 141 in repo file)

  OLD (remove from repo):
    <Accordion title="Are there any API limits that I need to be aware of?">

  NEW (replace with / insert from CSV):
    #### Are there any API limits that I need to be aware of?
    

CHANGE 13  [## FAQs]  (line 143 in repo file)

  OLD (remove from repo):
    </Accordion>

CHANGE 14  [## FAQs]  (line 145 in repo file)

  OLD (remove from repo):
    <Accordion title="What's the Source?">

  NEW (replace with / insert from CSV):
    #### What's the Source?
    

CHANGE 15  [## FAQs]  (lines 147–149 in repo file)

  OLD (remove from repo):
    </Accordion>
    
    </AccordionGroup>


==============================================================================
IMAGE CHANGE REPORT
==============================================================================

The following image changes were found in connector-changes.txt.
Per instruction, NO image changes should be applied without explicit approval.

INLINE IMAGE CHANGES (embedded within larger text changes — NOT suppressed):
These appear inside normal CHANGE blocks and will be applied unless excluded.

  File: s/article/000005920.mdx (YouTube Analytics Multi Account Connector)
    Changes 5, 6, 7, 8, 9, 10, 12, 14 each include an image reference change:
    Old format: <Frame><img src="/images/kb/ka0Vq000000PVF7-00N5w00000Ri7BU-[ID].jpg"
                style={{width: NNN, height: NNN}}/></Frame>
    New format: <Frame>![Screenshot](/images/kb/[SHORT_ID].jpg)</Frame>
    Nature: Format conversion (inline img → Markdown image) AND path shortening
            (removes Salesforce article-scoped prefix from filename)
    Images affected: 0EMVq00000CxIOb, 0EMVq00000CxIbV, 0EMVq00000CxIq1,
                     0EMVq00000CxJXZ, 0EMVq00000CxJAz, 0EMVq00000CxO5p,
                     0EMVq00000CxLHe, 0EMVq00000CxP57

  File: s/article/360042927994.mdx (Shopify Connector)
    Changes 8 and 9 include image reference changes:
    Same format conversion and path shortening as above.
    Images affected: 0EMVq000006Tb3x, 0EMVq000006Tb2M

SUPPRESSED IMAGE-ONLY CHANGES (noted in file but content not shown):
These are already excluded from the change blocks. Listed here for awareness.

  s/article/000005920.mdx          — 6 image-only changes suppressed (line 2362)
  s/article/360042927994.mdx       — 3 image-only changes suppressed (line 6384)
  s/article/360042928374.mdx       — 2 image-only changes suppressed (line 6515)
  s/article/360042929154.mdx       — 1 image-only change suppressed  (line 6892)
  s/article/360042931294.mdx       — 1 image-only change suppressed  (line 9159)
  s/article/360042932014.mdx       — 1 image-only change suppressed  (line 10113)

ACTION REQUIRED:
  Before applying changes to 000005920.mdx and 360042927994.mdx,
  manually remove or skip the image reference lines from Changes 5, 6, 7, 8,
  9, 10, 12, 14 (000005920) and Changes 8, 9 (360042927994).
  Apply only the non-image portions of those changes.

==============================================================================
END IMAGE CHANGE REPORT
==============================================================================
